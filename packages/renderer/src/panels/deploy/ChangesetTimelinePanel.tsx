/**
 * packages/renderer/src/panels/deploy/ChangesetTimelinePanel.tsx
 * STUB — real implementation in plan 04-04b.
 *
 * Registered in WorkspaceShell.tsx panelComponents as 'changesets'.
 * Registered in workspace-config.ts buildInitialLayout as a tab in the inspector group.
 *
 * Source: 04-02-PLAN.md Task 1 (R2-B4: stub must be at panels/deploy/, not components/).
 */

import React from 'react';
import type { IDockviewPanelProps } from 'dockview';

export default function ChangesetTimelinePanel(_props: IDockviewPanelProps): React.ReactElement {
  return <div />;
}
