/**
 * packages/renderer/src/panels/deploy/VcsPanel.tsx
 * STUB — real implementation in plan 04-05.
 *
 * Registered in WorkspaceShell.tsx panelComponents as 'vcs'.
 * Registered in workspace-config.ts buildInitialLayout as a tab in the inspector group.
 *
 * Source: 04-02-PLAN.md Task 1 (R2-B4: stub must be at panels/deploy/, not components/).
 */

import React from 'react';
import type { IDockviewPanelProps } from 'dockview';

export default function VcsPanel(_props: IDockviewPanelProps): React.ReactElement {
  return <div />;
}
