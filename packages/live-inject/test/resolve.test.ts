// RED STUB — implement in Plan 03-02 (make GREEN when resolve.cpp lands)
// Tests the name-keyed resolve path for the EngineHookPoints table.
// Note: Plan specified .spec.ts but project convention is .test.ts (vitest include: *.test.ts)

import { describe, it, expect } from 'vitest';

describe('name-keyed resolve — synthetic EngineHookPoints table', () => {
  it('resolves a known name to the correct address', () => {
    expect(true).toBe(false);
  });
});

describe('isAdvertisedClient detection', () => {
  it('returns true when GetEngineHookPoints export is present', () => {
    expect(true).toBe(false);
  });
});

describe('version mismatch is soft', () => {
  it('still resolves by name when version differs from ENGINE_HOOKPOINTS_VERSION', () => {
    expect(true).toBe(false);
  });
});
