# Handoff — Phase 04.3 execution (at UAT checkpoint) + Sonnet 5 model-config change

**Written:** 2026-07-01
**Author:** Opus 4.8 session (pre-restart)
**Why restart:** Session context is very large (full phase execution + a big claude-api skill dump loaded to verify the model ID). Restart for a lean context, then do the two tasks below in order.

---

## TL;DR — two workstreams, do in this order

1. **Sonnet 5 config change** (the user's active request). Verified model ID is **`claude-sonnet-5`**. Point both the cross-AI crew "fresh Sonnet" and the GSD `sonnet`-tier model at it.
2. **Resume Phase 04.3 execution** — it is **11/12 plans complete, D-16 gate = GO**, paused at the **plan 13 combined in-client UAT** (a manual `autonomous:false` `human-verify` checkpoint that only the maintainer can run).

---

## VERIFIED FACT — Claude Sonnet 5 exists (do not re-doubt this)

My training cutoff is Jan 2026 and the bundled `claude-api` skill's model catalog is cached 2026-06-04 — both predate Sonnet 5, so neither lists it. I verified via live WebFetch of `https://platform.claude.com/docs/en/about-claude/models/overview.md`:

- **Model ID / alias:** `claude-sonnet-5` (both the API ID and the alias are `claude-sonnet-5` — dateless pinned snapshot, Claude 4.6-generation naming convention).
- Bedrock: `anthropic.claude-sonnet-5`; Google Cloud: `claude-sonnet-5`.
- It is the **current** Sonnet ("best combination of speed and intelligence"), 1M context, 128k output, adaptive thinking yes, effort defaults to `high` on Claude API + Claude Code, Jan 2026 knowledge cutoff.
- Pricing $3/$15 per MTok, with **introductory $2/$10 through Aug 31, 2026**.
- **Sonnet 4.6 (`claude-sonnet-4-6`) is now a LEGACY model** (still active). So "the new Sonnet 5" = migrate `sonnet` references from 4.6 → `claude-sonnet-5`.

If a fresh session's `claude-api` skill still doesn't list it, trust this handoff + re-verify with the same WebFetch; do NOT fall back to `claude-sonnet-4-6`.

### Task 1 — apply the Sonnet 5 change

Two places use the Sonnet tier:

**(a) Cross-AI "phone a friend" crew — CLAUDE.md.** The crew's "fresh Sonnet" is spawned via the **Agent tool with `model: sonnet`**. The Agent `model` param only accepts tier aliases (`sonnet|opus|haiku|fable`), NOT raw IDs — so the crew already picks up whatever the harness maps `sonnet` to. Action: update the CLAUDE.md "Phone a friend" section text so "fresh Sonnet" explicitly notes it targets **Sonnet 5 (`claude-sonnet-5`)**, and confirm the harness's `sonnet` alias resolves to Sonnet 5 (if the maintainer wants a hard pin and the tooling supports raw IDs, use `claude-sonnet-5`). CLAUDE.md is git-tracked (maintainer's choice) — edit the `## Phone a friend (cross-AI consult crew)` block, `- **fresh Sonnet** …` line.

**(b) GSD model profile — `sonnet`-tier → `claude-sonnet-5`.** `gsd-sdk query init.execute-phase` currently returns `"executor_model": "sonnet"` and `"verifier_model": "sonnet"` (also researcher/synthesizer per memory `gsd-model-overrides`). These resolve the `sonnet` tier via the GSD model profile / `.planning/config.json`. Action: set the GSD `sonnet`-tier model to `claude-sonnet-5` — via `/gsd:settings` (or `/gsd:config`), or directly `gsd-sdk query config-set` on the relevant model key. Inspect `.planning/config.json` for the current model-profile shape first (it holds the profile that maps executor/verifier/researcher/synthesizer to tiers). Confirm afterward with `gsd-sdk query init.execute-phase 04.3` and check the model fields.

Ask the maintainer if unsure whether they want the alias re-pointed globally vs. an explicit per-role `claude-sonnet-5`. Commit only when they ask (project rule).

---

## Task 2 — Phase 04.3 execution state (resume the UAT)

Phase dir: `.planning/phases/04.3-versioning-and-searchtoc-mount/`

### What happened this session
- **Wave defect fix:** `phase-plan-index` clamps `wave:0` → `wave:1`, which merged the scaffold wave into the impl wave and produced a false intra-wave file overlap. Fixed by renumbering all plan `wave:` frontmatter to 1-based contiguous (0→1, 1→2, 2→3, 3→4). Commit `6443d13`. **Plan content unchanged.** Real wave structure now: **W1 {02,03} · W2 {04–11} · W3 {12} · W4 {13}**.
- **Execution mode:** set **`workflow.use_worktrees=false`** (project config, uncommitted change to `.planning/config.json`). Reason: this is a Windows native monorepo with **gitignored `node_modules` and NO npm workspaces** — git worktrees lack `node_modules`, so worktree-isolated executors would fail every vitest/electron-forge/native build. All plans ran **sequentially on the main tree** via `gsd-executor` (sonnet) subagents.
- **Waves 1–2 (plans 02–11): COMPLETE.** All SUMMARYs present, atomic commits landed, contract-stub → implementation pattern held (RED tests → green). Notable: plan 06 fixed the flat-list→branch-tree (sketch 002-A) regression; plan 10 = native C++ v6000 per-payload classification (addon rebuilt, static zlib preserved).
- **Post-Wave-2 integration gate** caught 10 test failures. Fixed:
  - 7 = jsdom env: `useNoProjectDim.test` + `undoStore.test` used browser globals but were `.test.ts` (root runs `.test.ts` under node; `.test.tsx` under renderer jsdom). Renamed both to `.test.tsx`. Committed.
  - 3 = v6000 harness tests asserted the FALSIFIED "v6000 = always enumerate-only" contract → deferred to plan 12 (correct owner) and fixed there.
- **Wave 3 (plan 12) — D-16 de-anchoring crew gate: COMPLETE, verdict GO.** Ran all 4 consultants (Codex + Cursor via PowerShell; fresh Sonnet + Opus via Agent) on non-overlapping angles vs real `swg-client-v2` source + real `.toc`/`.tre` bytes + `tre_reader.py`.
  - Byte path verified **byte-exact** (Cursor byte-map; 2 byte-exact tests pass). No layout disagreement; "size-first" absent.
  - Crew found a **BLOCKING** integration defect **F-2** (searchTOC extraction unwired in UI — `TreVfsBrowser` called `readVfsEntryBytes` with no descriptor → `readMountEntry` → -1 → silent null; `openVfsEntry` was dead code) → initial **NO-GO**. Plus F-3 (shadow/override double-count), F-4 (encrypted false-positive guard weaker than oracle), F-5 (int32 narrowing).
  - **Maintainer chose "fix everything F-2..F-5".** A loop-back `gsd-executor` applied all fixes + reconciled the 3 v6000 tests to per-payload truth (7 atomic commits, native rebuilt). **Notable:** the executor de-anchored and CORRECTED Opus's F-4 suggestion — `desc.crc` is the CRC-32 of the *path* (`crc32("appearance/lod/...")==0x058f215e`), not the content, so it used a `producedLength==desc.length` guard instead (a content-CRC check would have been a 100% false-negative bug). "Bytes beat consensus" working as intended.
  - Re-verified GO: root node suite **376 passed / 2 skipped (50 files)**, renderer jsdom **258 passed (35 files)**, byte-exact + F-2 integration **7 passed / 2 skipped**. SWG-Source plain-zlib extraction still byte-identical.
  - Artifacts committed (`bd31fe0`): 4 CONSULT briefs + `04.3-12-GATE-FINDINGS.md` (verdict: GO, full reconciled register) + `04.3-12-SUMMARY.md`. (Crew `.out` raw outputs are gitignored — findings captured in GATE-FINDINGS.) Plan 12 marked complete (`465278e`).

### Where it's paused — plan 13 (Wave 4), the combined in-client UAT
`04.3-13-PLAN.md`, `autonomous: false`. Two tasks:
- **Task 1 (auto) — mostly done.** Gate GO confirmed; full suite green; both client paths verified present: searchTree = `D:/SWG Infinity`; searchTOC = `../swg-client-v2/stage-x64/client.cfg` → `D:/Code/SWGSource Client v3.0/` (sku0–3 v6000). **`04.3-13-UAT-RESULTS.md` skeleton is written + committed** (`d11d94e`) — criteria A–D checklist + element-by-element sketch-diff table (002-A/005/006/007/008). **Still pending in Task 1:** run E2E (`npm run test:e2e`, playwright), `npm start` build, and seed a branched project programmatically (trunk v1←v2←v3 + branch off non-tip: v4→v2, v5→v4) so the 002-A graph has topology to render.
- **Task 2 (checkpoint:human-verify, BLOCKING) — the maintainer must run this.** Launch real Electron; verify on BOTH clients:
  - **A** (searchTree/Infinity): click version → *silent* reconcile; unsaved-edit switch → confirm; Undo/Ctrl+Z; Baseline → stock (cfg byte-pristine); idempotent fwd/back; no per-row deploy, "Branch from here" present, model-picker only on new forward deploy.
  - **B** (searchTOC/swg-client-v2 loose): same nav; Baseline removes loose override, `client.cfg` byte-untouched.
  - **C** (v6000 mount): `patch_sku3_*` non-zero entryCount; open a sku3 `.dds/.msh` → **byte-exact** extract (this is the F-2 fix — the whole reason it must be tested in-client); encrypted chip only on genuine Restoration payloads; lazy searchPath (no bogus rows) but loose override wins; v5000 space-terminal texture resolves.
  - **D** (sketch-diff every element observed/missing).
  - Resume signal: maintainer types **"approved"** (→ close phase: verification + `phase.complete` + supersede 04.1-11 & 04.2-06) OR lists failing criteria/missing sketch elements (→ each becomes a gap-closure `must_have` for `/gsd:plan-phase 04.3 --gaps`).

### Open items to surface at phase close
- **S3 (VCS tab placement)** — plan 09 left it AMBIGUOUS pending maintainer decision. It's a row in the UAT sketch-diff table; decide it during the UAT.
- **F-6 / F-7** (crew LOW findings) recorded as notes in GATE-FINDINGS, no behavior change. F-4/F-5 were fixed (maintainer chose everything).

---

## How to resume execution after restart

Re-running `/gsd:execute-phase 04.3` will discover 11/12 SUMMARYs, skip them, and land on plan 13 (the checkpoint). Because plan 13 is `autonomous:false`, present the UAT checkpoint to the maintainer (Task 1 auto-prep, then the A–D human-verify). **Keep `workflow.use_worktrees=false`** (already set) — do NOT let it revert to worktree mode for this repo.

### Session mechanics that matter
- **Crew invocation (CLAUDE.md "Phone a friend"):** Codex + Cursor via the **PowerShell tool** by full path; fresh Sonnet/Opus via the **Agent tool** (`model: sonnet`/`opus`). `gsd-executor` subagents lack the Agent + PowerShell tools, so crew-orchestration plans must be run inline by the main orchestrator, not delegated.
- `ant` CLI is **not installed / not on PATH**, and there is **no `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` in env** — the Models API can't be queried locally; use WebFetch for model-catalog checks.
- Commit only when the maintainer asks; stage explicit paths (never `git commit -a` in this dirty tree — lots of pre-existing untracked `.planning/research/CONSULT-*` files).
- Uncommitted right now: `.planning/config.json` (the `use_worktrees=false` toggle) + this handoff + pre-existing untracked research/fixtures.

### Key commits this session (newest → oldest, phase-relevant)
`d11d94e` UAT skeleton · `465278e` mark plan-12 complete · `bd31fe0` gate artifacts+GATE-FINDINGS(GO)+SUMMARY · (7 loop-back fix commits F-2..F-5 + v6000 tests) · jsdom `.test.tsx` rename · `6443d13` wave renumber · Wave-2 impl commits (`5a75261`…`785cfee`) · Wave-1 scaffold commits (`4f4da71`…`e8b9644`).
