# Handoff — Phase 04.2 planned + cross-AI reviewed (2026-06-29)

**Status:** PLANNING COMPLETE, NOT executed. Phase 04.2 (Dev-Client Support & Loose-Override Deploy)
has 6 plans in 5 waves, plan-checker **PASS (0 blockers)**, finalized after a full 4-AI de-anchoring
crew review + a ground-truth fact-check. All planning docs committed (HEAD `5aed03f`). Nothing running.

**Resume to execute:** `/clear` first, then `/gsd:execute-phase 04.2`.

Read first: `.planning/phases/04.2-dev-client-support-loose-override-deploy/` (INPUTS, RESEARCH,
VALIDATION, PATTERNS, the 6 PLANs), `.planning/research/CONSULT-P4.2-SYNTHESIS.md` (the review findings),
and memories `reference-swg-client-mount-mechanisms` (now augmented), `feedback-crew-catches-what-plancheck-cannot` (new).

---

## What this phase delivers (3 capabilities, 1 vertical slice)

The dev/modder client where the binary is DECOUPLED from its TRE data (swg-client-v2: `stage-x64/` has
the exe + `client.cfg` + ZERO `.tre`; data lives at `D:\Code\SWGSource Client v3.0\` by absolute path).
1. **CLIENT-02** — detect `client.cfg`-style installs + manual cfg/TRE-dir override.
2. **TRE-05** — mount the FULL base via `searchTOC` (.toc index) + `searchPath` loose dirs, not just `searchTree`.
3. **DEPLOY-08** — deploy by writing loose files into the top-priority `searchPath` override dir (no TRE
   pack, no `.cfg` surgery — the cfg is machine-generated).
Spine = the space-terminal retexture E2E (extract `texture/ksk_all_spaceterminal.dds` → edit → deploy-to-
override → in-game), reproducing the hand-verified 2026-06-28 proof with zero manual copy. Requirements
formalized into REQUIREMENTS.md (coverage 41→44; note CLIENT-01 intentionally reserved/unused).

## Plan map (waves)
- **W1** 04.2-01 — contracts (`LooseDeployRecord` + `snapshotPath`/`skippedDeletes`, `DeployModel`) + RED
  stubs + real `sku0_client.toc` header oracle fixture + synthetic duplicate-TOC fixture.
- **W2** 04.2-02 — `client.cfg` layout detection; `clientSearchOrder` parses searchTOC/searchPath/TOCTreePath
  + raw priorities; `resolveClientMountOrder(cfgPath, installRoot)` two-arg form.
- **W3 (parallel)** 04.2-03 — `tocReader.ts` full-index reader + `crc32.ts`; treAutoMount ONE unified ordered
  mount. 04.2-04 — `looseOverrideDeploy.ts` (resolve/deploy-snapshot+prune/reset-restore) + path-safety.
- **W4** 04.2-05 — DeployDialog third radio + handleDeploy/Reset branches + override-dir guard + cfg fallback.
- **W5** 04.2-06 — in-client UAT (E2E + searchTree-only regression) **[autonomous: false]** — the phase gate.

## The cross-AI review — what it caught (the important part)
Plan-checker PASSED the original plans on internal consistency, but the crew (Codex/Cursor/Opus/Sonnet,
non-overlapping angles, reading real `../swg-client-v2` source + bytes) + my fact-check found **8 real
defects the checker structurally could not see** — all FIXED and re-checked:
- **B1** double `mountTrePaths` → second `mountComplete` `set()` REPLACE clobbered the searchTree overlays
  (`treStore.ts:159-170`). **B2** per-family priority COMPACTION (`clientSearchOrder.ts:137`) inverted
  cross-family order. → Fix: ONE ordered list across all 3 families, sorted `(priority DESC, sku DESC,
  cfgIndex DESC)`, single mount call. **B4** loose overlay marked every searchPath always-top → conditional.
- **B3** `resetLoose` SKIPPED `preExisted` files → overwriting the already-present `ksk_all_spaceterminal.dds`
  lost the original + failed UAT step 6. → snapshot-before-overwrite + restore-on-reset (sha256 invariant).
- **H1** the engine resolves a `.toc` by **CRC-keyed binary search** (`SearchTOC::localExists`
  TreeFile_SearchNode.cpp:783-836), NOT tree-name order / flat priority. Plan resolved ~3/193,475 duplicate
  paths wrong (`space_faction.stf` → must be `patch_15_02.tre` tree 42, not tree 54). → full-index reader
  + `crc32.ts` (port of `native-core/.../tre/Crc.cpp`, poly 0x04C11DB7), replicating the binary search.
  (Maintainer chose FULL fidelity over accept-and-warn.)
- **H2** cross-deploy orphaning → `deployLoose` prunes prior record. **H3** hardcoded `swgemu.cfg` fallback
  → `resolveLayout()?.cfgFile`. **MED** path-safety (drive-relative `C:foo` + `override_evil` prefix
  collision), warn-only-delete banner, override-dir guard (moved to Plan 05 handleDeploy).
- **VERIFIED & retired:** the `.toc` byte format (Cursor, triple-checked: C++ struct + blender reader +
  real bytes) — 36-byte header, 2793-byte oracle slice, magic `" COT1000"`, all LE.

## Open / watch items for execution
1. **`crc32.ts` oracle `3830594` is UNVERIFIED** — Plan 03 Task 1 flags "CONFIRM against Crc.cpp before
   executing". The real de-anchoring gate is the real-`.toc` → `treeFileIndex===42` assertion (skipIf in CI).
2. **Real assets are gitignored** — the real `sku0_client.toc` (D:\Code\SWGSource Client v3.0) + the
   swg-client-v2 install are on-disk only; the byte-exact tests `skipIf` they're absent (CI runs the
   synthetic fixtures). Run on the maintainer's machine for the real-byte gates.
3. **Plan 03↔04 share Wave 3** — confirmed non-overlapping `files_modified` (03: tocReader/treAutoMount/
   treMount/treStore/crc32; 04: looseOverrideDeploy/changesetService). Safe to parallelize.
4. **04.1 still has its in-client UAT (04.1-11) OPEN** — 04.2 is planned but sits behind 04.1 completion in
   numeric order. The other active handoff (`2026-06-28-phase04.1-deploy-uat-iteration.md`) covers it: the
   in-game absolute-path load check + Hardlink-Shadow keep/remove decision are still pending. Consider
   closing 04.1 first.

## Environment / gotchas (carried from 04.1 handoff, still true)
- App needs a FULL restart after contracts/backend changes (renderer consumes built `dist/`). native-core
  via `require('@swg/native-core')` (Path B) — a top-level ESM import crashes the renderer; use the
  `services/nativeTre.ts` seam. `native-core/test/resolve-prebuild.test.ts` EPERM is environmental (Electron
  locks the `.node`) — run renderer-only for a clean signal.
- **Do NOT `git add .`** — untracked pre-existing `CONSULT-*` (older sessions) + `fixtures-real/animation/`
  must stay uncommitted; STATE.md + `todos/pending/client-detection-and-layout-model.md` had pre-existing
  uncommitted edits NOT authored this session — left untouched. Stage explicit paths only.
- Verify-against-ground-truth is the prime directive; crew protocol + de-anchoring in `CLAUDE.md`.

## Commits this session (all on `main`, not pushed)
`cbbbceb` research · `4c38324` validation · `8405569` plans (6) + REQUIREMENTS · `4af7ba9` checker-fixes +
inputs/patterns · `0637ad2` finalize after crew review · `5aed03f` ROADMAP refresh. (The crew revisions +
polish were folded into `0637ad2`.) Memories: updated `reference-swg-client-mount-mechanisms` (SearchTOC
resolution = CRC binary search); new `feedback-crew-catches-what-plancheck-cannot`.
