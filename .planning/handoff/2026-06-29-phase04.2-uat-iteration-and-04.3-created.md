# Handoff — Phase 04.2 executed + long UAT iteration → Phase 04.3 created (2026-06-29)

**Status:** ACTIVE. ← start here. Phase **04.2 plans 01–05 executed + merged** (all GREEN, worktree
executors). Plan **04.2-06 (in-client UAT) is OPEN** and was walked interactively — it surfaced a string
of bugs/gaps; the small ones were fixed inline on `main`, the big reworks were captured as todos and rolled
into a **new inserted Phase 04.3**. Nothing is marked complete. HEAD `6864b90`, all committed, **not pushed**.

Read first: `.planning/STATE.md`, `.planning/ROADMAP.md` (new Phase 04.3 block),
`.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-SCOPE.md`, and the three new todos +
two corrected memories (below).

---

## Where the phases stand

- **04.2 (Dev-Client Support & Loose-Override Deploy):** plans 01–05 DONE + merged (contracts, client.cfg
  detection, crc32+tocReader+unified mount, looseOverrideDeploy, DeployDialog wire-up). **04.2-06 UAT OPEN.**
  Part A (swg-client-v2 loose-override) was walked far enough to prove the spine — the space-terminal texture
  resolves and the loose-override deploy/reset path works after fixes. Part B (SWGEmu/Infinity searchTree) not
  yet run. NOT complete.
- **04.1 (Deploy & Project UX):** all 11 plans coded; **04.1-11 UAT OPEN.** The one open *decision* —
  Hardlink-Shadow keep/remove — was RESOLVED this session: **demoted behind an "Advanced" disclosure**
  (commit `2011e12`), justified by the loose-override-loads-in-game evidence. NOT complete.
- **04.3 (Versioning Model & SearchTOC Mount Completion) — NEW, INSERTED, not planned.** Bundles the two big
  queued reworks. See below.

**Key reframe (maintainer):** the current UATs can't fully finish without 04.3's reworks ("we've done all we
can without the queued changes"). So the likely next workstream is **plan + execute 04.3** (starting with the
crew UI-vs-sketch review), then one **combined UAT** that supersedes 04.1-11 + 04.2-06. Confirm with maintainer.

## Phase 04.3 — what it is + how to start

`/clear` → `/gsd:plan-phase 04.3`. The planner reads `04.3-SCOPE.md`. Structure:
- **Plan 01 (FIRST, gating): cross-AI crew UI-vs-sketch gap review.** Diff the as-built UI against the sketches
  (`002-version-graph-timeline`, `005-deploy-inspect-tab`, `006`, `007`, `008`) element-by-element; the gaps
  *feed* the rest of the plan list. Operationalizes the new AGENTS.md "Sketches are the UI contract" rule.
- **Pillar A — versioning rework** (todo `version-navigation-live-sync-deploy-model`): live client mirrors the
  SELECTED version (navigating the tree = deploy/revert); per-version deploy flag; reconcile-to-version engine
  (`syncLiveToVersion`, forward-apply/backward-revert in one op) behind a confirm; B3 stock snapshot/restore
  across versions; deploy-model picker = new-forward-deploy only; first-class revert from any state (subsumes
  the reopen-Reset gap); **visual branch-tree** history per sketches 002/005 (currently a flat list).
- **Pillar B — searchTOC/v6000 mount completion** (todo `v6000-swg-source-plain-zlib-read-support`): mount
  SearchTOC clients from the **master `.toc` index** (swg-client-v2's empty-internal-TOC v6000 container `.tre`
  files contribute entries; payloads read by offset); v6000 enumerate-only → per-payload runtime check (plain
  zlib for SWG-Source, encrypted only for Restoration); fix loose-overlay over-enumeration; loose-entry mesh
  viewport. **FORMAT-HEAVY → run the de-anchoring crew vs real source+bytes before merging.**
- **Closes with a combined in-client UAT** (autonomous:false).
- Both todos tagged `resolves_phase: "04.3"` (auto-close on completion).

## Bugs/gaps FIXED inline this session (on `main`, all 112/112 renderer + tsc clean)

1. `bcf48e6` — **empty TRE browser on swg-client-v2.** Root cause: `workspaceService.openWorkspace` mounted ALL
   client kinds via the legacy `autoMountTarget` (searchTree-only) → a searchTOC client mounted nothing. Now
   routes `kind:'client'` → `autoMountClient` (with a treDir directory-scan fallback). Also fixed the misleading
   "TREs: Install Root" chip → "from client.cfg" for `treDirFromCfg` clients.
2. `a6e4f7e` — already-bound clients filtered out of the Welcome detect list; detection also probes the 32-bit
   `swg-client-v2/stage` (was only `stage-x64`).
3. `2011e12` — Hardlink-shadow demoted behind "Advanced".
4. `e6c2cc0` — **version-selection messaging + Baseline loose-revert.** The "You have unsaved changes" banner was
   false → reworded; Baseline reset-to-stock now reverts a **loose-override** deploy via `resetLoose` (it only
   handled cfg-insertion before — sniffs the record shape since the radio is unreliable on reopen). **This is a
   STOPGAP on the old model; 04.3 replaces it.**
5. `a3b825f` + `a0d8c2f` — **Extract + IFF viewer did nothing for loose-overlay entries** (winnerArchiveIndex<0,
   not in native mount). Added shared `readVfsEntryBytes(entry, mountHandle)` (loose→disk, archived→mount). 3D
   mesh viewport for loose entries still needs Pillar B (resolveAppearance is mount-index based) — falls back to
   IFF Structure.
6. `f130c24` — **mount spinner** during auto-mount (the UI existed but `autoMountClient` never set the status);
   indeterminate sweep; `mountStatus.pct` now optional.

## New todos created (all in `todos/pending/`)

- `version-navigation-live-sync-deploy-model` → **04.3 Pillar A** (incl. branch-tree viz, picker-scope, reopen-Reset).
- `v6000-swg-source-plain-zlib-read-support` → **04.3 Pillar B** (incl. loose-overlay over-enumeration, viewer mesh gap).
- `delete-project-with-restore` → **SEPARATE future item** (NOT in 04.3 — maintainer's call). Delete must restore
  the client to stock (model-appropriate Reset) before removing, and be **UNDOABLE** via a session-scoped undo
  stack (park in `.trash`, purge on app teardown / stale on next startup).

## Process improvement (the "why aren't sketches reaching implementation" thread)

Root cause: sketch fidelity lived only in a recalled memory, not the plan→build→verify chain; and
extract/relocate plans (e.g. `VersionHistoryBody` moved from `ChangesetTimelinePanel`) carried forward
pre-sketch rendering with nothing flagging the drift. **Fix shipped:** new **AGENTS.md "Sketches are the UI
contract"** section (`5e97ef1`) — plan rule (enumerate sketch elements as must_haves), refactor caveat (moving a
component doesn't excuse divergence), verify rule (diff built surface vs sketch). Memory
`feedback-sketches-are-ui-source-of-truth` strengthened to match.

## Memories changed this session

- `tre-version-oracles-and-v6000-encryption` — **CORRECTED.** v6000 is a DUAL format: SWG-Source/swg-client-v2
  patch_sku3 v6000 = plain zlib READABLE; Restoration v6000 = encrypted. Encrypted-ness is per-payload-runtime,
  NOT the version tag. The "no plain-zlib 6000 exists" conclusion was FALSIFIED. entryCount-0 RESOLVED = empty
  internal TOC (entries live in the master `.toc`).
- `feedback-sketches-are-ui-source-of-truth` — added the structural recurrence + the AGENTS.md fix.

## Environment / gotchas (carried, still true)

- **App needs a FULL restart after contracts/backend changes** (renderer consumes built `dist/`); renderer-only
  changes are Ctrl-R-able. Rebuild `@swg/contracts` (`cd packages/contracts && npx tsc`) before the renderer
  typechecks — its `dist` is gitignored and goes stale (bit us mid-session).
- **Do NOT `git add .`** — `.planning/todos/pending/client-detection-and-layout-model.md` (modified) and the
  untracked `.planning/research/CONSULT-*.md` + `packages/harness/fixtures-real/animation/` are PRE-EXISTING and
  NOT ours. Stage explicit paths only.
- Renderer tests: `cd packages/renderer && npx vitest run` (112/112). Typecheck: `npx tsc --noEmit -p tsconfig.json`.
  native-core `resolve-prebuild.test.ts` EPERM is environmental (Electron locks the `.node`) — run renderer-only.
- Worktree executors work on this setup (prior phases + 04.2 waves 1–4). This session's UAT fixes were inline on
  `main` (interactive), not via worktree.
- **Verify-against-ground-truth** is the prime directive — Pillar B is format-heavy; use the crew + real
  swg-client-v2 source / real `.toc`/`.tre` bytes / the maintainer's working `swg-blender-plugin` reader.

## Ground-truth notes worth keeping (from the v6000 investigation)

- swg-client-v2 data root: `D:\Code\SWGSource Client v3.0\`; cfg `D:\Code\swg-client-v2\stage-x64\client.cfg`
  (searchTOC sku0–3 + `TOCTreePath` + `searchPath_00_5 ilm_extract` / `searchPath_00_9` data-root / `..._00_10`
  override). `sku0_client.toc` = SearchTOC, 193,475 entries. `ksk_all_spaceterminal.dds` lives in `patch_11_0x.tre`
  (v5000, self-indexed → resolves today). `patch_sku3_*.tre` = v6000, numberOfFiles=0 (empty internal TOC),
  payloads `comp:2` zlib in the master `.toc`.

## Commits this session (all on `main`, not pushed)
HEAD `6864b90`. Production fixes: `bcf48e6 a6e4f7e 2011e12 e6c2cc0 a3b825f a0d8c2f f130c24`. Process: `5e97ef1`
(AGENTS.md). Roadmap: `6864b90` (04.3 insert). Plus 04.2-05 wave merge `8fa18c0` + tracking `06a2ee5`, and the
`docs(todo)` series. Memories updated outside the repo.
