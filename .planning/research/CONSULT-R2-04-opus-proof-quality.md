# CONSULT R2-04 — fresh Opus — Proof quality: do the new tests prove what they claim?

Angle: spec & coverage rigor. For each high-value claim, ask "could this proof PASS while the
thing it claims is FALSE?" You did not see round 1; judge the proofs as written.

## LOCKED AXIOMS (given)
- Native SAB gating (`NAPI_EXPERIMENTAL` + node-addon-api `^8.8.0`) is settled-correct.
- Whether cross-process SAB sharing is physically achievable is a separate reviewer's empirical
  question; YOUR job is whether the TEST correctly distinguishes shared-from-copy regardless of
  the outcome.

## OPEN QUESTIONS (your angle)
1. **Cross-write test validity (D-04 / SC-4).** 00-03 Task 2: utility allocates the SAB, writes
   `0xDEAD`→view[0], posts it; renderer reads view[0], writes `0xBEEF`→view[1]; utility re-reads
   view[1] from its HELD SAB (no re-post) and acks. 00-04 records `__crossWriteOk`; 00-05
   asserts it.
   - If the renderer's SAB is a COPY, the utility re-reading its own held buffer's view[1] sees
     0 (never written) → ack != 0xBEEF → test fails. Confirm this logic is airtight: is there ANY
     path where a copy-only world still produces ack === 0xBEEF (false-pass)? e.g. timing, the
     renderer's write going to the wrong slot, default-zero collisions, or the ack reading view[0].
   - Conversely, any path where genuine sharing produces a FALSE-FAIL (e.g., the utility re-reads
     before the renderer's write is visible — memory-ordering/Atomics: should this use
     `Atomics.store`/`Atomics.load` rather than plain `view[i] =`)? A flaky proof is a bad proof.
2. **FND-02 distribution proof (deep).** prebuilds/ is gitignored, generated by prebuildify on
   the build machine, then "resolved by node-gyp-build without a compiler." Is "node-gyp-build
   found the local prebuilds/ file" actually a proof of *no-compiler distribution*, or a proxy?
   What would a NON-circular proof look like (e.g., resolve on a machine/container with no MSVC,
   or delete build/ and the compiler from PATH and re-resolve)? Is the current acceptance
   criterion strong enough, or should it be tightened or honestly downgraded to "resolution-path
   wired, full no-compiler proof deferred"?
3. **SC-5 real-restart.** 00-05 uses `app.close()` + relaunch with a fixed userDataDir and bans
   `page.reload`. Confirm this genuinely exercises main-process re-init of persisted layout, and
   that the fixed userDataDir doesn't mask a real bug (e.g., default-temp dir that wouldn't
   persist in production).
4. **Coverage**: every FND-01..05 + SC-1..5 still traces to a concrete green assertion after the
   replan? Any criterion now proven only by a `grep` of source text rather than behavior?

## Files
- All five PLANs + RESEARCH + VALIDATION in
  D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/

Deliverable: markdown, severity-tagged. For each proof, a one-word verdict AIRTIGHT / PROXY /
FLAKY / FALSE-PASSABLE with the precise failure path if not airtight.
