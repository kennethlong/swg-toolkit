---
phase: 5.1
round: 11
reviewers: [codex, cursor, sonnet, opus, fable]
reviewed_at: 2026-08-01T22:09:23Z
plans_reviewed: [05.1-01-PLAN.md, 05.1-02-PLAN.md, 05.1-03-PLAN.md, 05.1-04-PLAN.md, 05.1-05-PLAN.md, 05.1-06-PLAN.md, 05.1-07-PLAN.md, 05.1-08-PLAN.md, 05.1-09-PLAN.md, 05.1-10-PLAN.md, 05.1-11-PLAN.md, 05.1-12-PLAN.md, 05.1-13-PLAN.md, 05.1-14-PLAN.md, 05.1-15-PLAN.md]
reviewed_commit: eafec8b
---

# Cross-AI Plan Review — Phase 05.1 — Round 11 (post-CC1–CC18 convergence check)

Reviewed commit `eafec8b` (round-10 replan on the Fable planner: CC1–CC18; HOST_CMD ACK PROTOCOL
rewritten as a 13-row total state × event table; durable-surface narrowing disclosed as ledger
(m)/(n); CC12 refuted by measurement). Five reviewers, non-overlapping angles. Task files led with
FOUR LOCKED measured axioms (channel zero-fill; 218-line count; `:173`-blank; reset-before-render
PROVEN closed) — no reviewer contradicted them, no settled premise re-derived. Task files:
`.planning/research/CONSULT-05.1-R11-*.md`, outputs `…-*.out`.

**Verdicts:** Codex NOT CONVERGED (2 MED, 1 LOW) · Cursor NOT CONVERGED (2 MED; 40+ MATCH) ·
Sonnet NOT CONVERGED (1 HIGH) · Opus NOT CONVERGED (1 HIGH, 1 LOW; table certified TOTAL) ·
Fable NOT CONVERGED (1 FALSE, LOW; 26/28 verified).

**Overall: NOT CONVERGED — 2 HIGH, 2 MED, 2 LOW (unified work-list EE1–EE6).**

## Round-over-round signal — narrowest round of the loop

- **Both HIGHs are the SAME meta-pattern, and both are one-transition fixes:** a per-transition
  rule stated ONCE (at a declaration or in the precedence prose) but not enumerated across ALL
  transitions of the new ack table. EE1 (`isPlacementPending` cleared "on every exit row" but only
  operationalized on the ack branch) and EE2 (the `studioDir` re-check added to the ack and timeout
  transitions but not the store-writing detach transition). This is the CC1 defect class — a
  lifecycle rule stated once, not enumerated — reappearing on the mechanisms round 10 introduced
  (CC17 and CC2). It is NOT a new architectural seam: no reviewer found the table incomplete, the
  precedence model wrong, or a redesign needed.
- **The ack table is certified genuinely TOTAL** (Opus: {idle,pending} × {place, ack, timeout,
  detach, switch, unmount} fully partitioned, ack cell exhaustively split by epoch×code×studioDir,
  no missing event, no two reachable cells conflicting except the EE2 detach-vs-switch straddle).
  CC13 quantifier fix landed clean; late-ack rule correctly derived and holds across switches.
- **Zero fabricated source facts.** Fable: 26/28 verified incl. every ground-truth citation, the
  11_000 derivation inputs, the CC12 refutation (re-measured at HEAD and 751464a), the 39/33/6
  task-count, ledger (a)–(n), all four CC18 self-description corrections. The one FALSE (EE4) is a
  wrong TEST-FILE example in a recipe, not a claim about the code under design.
- **All prior CLEAN findings hold** (Opus CLEAN-2 reset-before-render, CLEAN-3/4/5); the loop's
  two structural redesigns (reset-before-render, ack-table-totality) are both now sound — the
  residue is enumeration completeness and string hygiene.

## Unified work-list (EE1–EE6)

| ID | Sev | Plans/files | Finding | Found by |
|----|-----|-------------|---------|----------|
| EE1 | HIGH | 14 (`:745-746` decl, `:753-757`, `:760-761`, `:763-764`, `:765-774`; test `:813`) | **`isPlacementPending` UI-lock is reset "on EVERY exit row" only in one prose line at its declaration — never operationalized in the per-transition action specs.** `setPlacementPending` appears exactly once across Plans 08+14 (grep-verified). The timeout-fire, ack-mismatch-abort, detach-abort, and project-switch-abort transitions each enumerate exactly what to clear (`pendingPlacementRef`, `placementTimeoutRef`, durable records) and never mention the UI mirror; only the ack MATCH branch plausibly covers it. None of timeout/detach/switch unmounts the component (unlike row 12, where a fresh `useState` would rescue it), and epochs are globally monotonic (no future ack re-correlates), so an executor following the plan literally leaves the "+ Add decoration…" trigger — an SC4 entry point — **permanently disabled for the rest of the session** after the single most ordinary interruption to a pending placement. No test asserts `isPlacementPending`/trigger-enabled state on the timeout/detach/unmount/switch paths (only `:813` "after the ack resolves"). Undisclosed in Plan 15's ledger and VALIDATION. Fix: enumerate the `setPlacementPending(false)` clear in every exit row's action + acceptance, and add trigger-re-enable assertions to the timeout/detach/switch/unmount tests. | Sonnet (HIGH) |
| EE2 | HIGH | 08 (`:284` row 10, `:289-296` precedence), 14 (`:699-703`, `:763-764`) | **The CC2 `studioDir`-in-slot local guard was half-propagated: added to the ack (row 7) and timeout (row 9) transitions but NOT the detach transition (row 10) — which is also store-writing** (`recordPersistResult` → `outcome:'error'` + `hasFailureBadge`, per the Durable-surface clause `:361-367`). The precedence paragraph's universal "Enforcement is LOCAL, never hook-declaration-order dependent" (`:293-294`) is therefore FALSE for the detach-vs-project-switch pair. Sequence: pending in A (`slot.studioDir='A'`) → switch to B (worldEditorStore resets synchronously, B's history/badge empty) → by the exact overdue-~1 Hz-poll-before-passive-flush model the plan ACCEPTS for CC2, the poll loop sets `liveStore.status` off `'attached'` in that window → if the detach effect is declared before the `[studioDir]` effect, detach runs first with the slot still `'A'` and no studioDir check → writes a "detached" error record + badge into **Project B's reset store** → switch effect then clears an already-consumed slot. Reachability bounded (per D-07b a switch doesn't auto-detach, so needs an independent client-exit at switch-time), but unclosed under the plan's own "never rely on effect order" principle. Fix: mirror row 7's `slot.studioDir === getState().studioDir` re-check into the detach effect (one line), OR add an explicit detach-vs-switch unreachability argument (the section currently argues only detach-vs-ack). | Opus DD1 (HIGH) |
| EE3 | MED | 08 (`:352-354` canonical string 2), 14 (`:696-698`), 15 (`:280-281`) | **Canonical string 2 (the timeout toast) is quoted in Plans 14/15 as an ellipsis-prefixed fragment**, not character-identical to Plan 08's full sentence (which includes the prefix "Placement request got no response from the game (it may be loading, zoning, or busy) — …"). CC8 requires character-exact when quoted, and Plan 14's own test at `:816-817` requires full-string equality — so the partial quote is both a discipline miss and a latent test inconsistency. Fix: carry the full canonical string 2 at both sites, or reference-without-quoting per string 3's pattern. | Codex SP-1/SP-2 + Cursor STR-2 |
| EE4 | MED | 04 (`:414-416`) | **The CC9 test-isolation recipe cites `panels/deploy/ProjectBindingBar.test.tsx` as a `beforeEach` that seeds `studioDir` — it does not.** That file's only `beforeEach` (`:91-98`) seeds `status`/`clientPath`/`workspaceName`; `studioDir` appears only in a `WorkspaceInfo` fixture at `:78` (nested under `status.info`) that never moves the store field the module-scope subscription watches — so that suite can never fire the reset the recipe warns about. The example was inherited from round-10 CC9's GENERIC-setState note (Sonnet L3 named the file for the setState convention), narrowed into a studioDir-specific claim, and restamped "verified this session" — the exact provenance failure mode this same commit's CC18(a) rule was written against. The co-cited `DeployDialog.test.tsx:142-175` example IS correct (and `changesetService.test.ts:42/47`, `StagingPanelBody.test.tsx:111` also seed studioDir). Fix: drop the ProjectBindingBar example (or replace with a real studioDir-seeding suite); apply the provenance rule to reviewer-supplied examples. | Cursor TEST-1 + Fable FG-10 |
| EE5 | LOW | 14 (`:782-784`), 15 (`:294-295`) | The local-refuse toast is referred to as the shorthand "still waiting" toast rather than canonical string 4. Cursor rates this a paraphrase (not a quote claim → acceptable under CC8); Codex rates it LOW for consistency. Resolve by either quoting string 4 in full or clearly marking it a descriptive reference, consistently at both sites. | Codex SP-3 + Cursor STR-5 |
| EE6 | LOW | 08 (`:256-265`, `:441-442` ledger (n)) | The `11_000 = 10_000 + 1_000` derivation calls the +1 s the "worst-case READ lag" under "the documented ~1 Hz throttle" — but 1 s is worst-case only under MODERATE throttling. With the toolkit occluded by the fullscreen game >5 min (the normal workflow), Chromium/Electron `backgroundThrottling` escalates to intensive throttling (~1/min), so read lag can reach ~60 s. The design SURVIVES (the timeout `setTimeout` is throttled by the same clock and fires ~1/min late too — throttle symmetry, not the +1000 margin), but the cited derivation isn't a true bound. Also: ledger (n) attributes the false-timeout residual solely to "agent silent through a long zoning screen"; renderer-side read starvation under intensive throttling is a second cause of the same observable that (n)'s wording doesn't name. Disclosure-completeness only. Fix: restate the +1 s justification as throttle-symmetry (not a lag bound), and widen (n) to name renderer-side starvation. | Opus DD2 |

## Rejected / no-action findings

None. All five reviewers honored the LOCKED axioms; no finding contradicted measured ground truth.
(Fable's FG-28 UNVERIFIABLE items are correctly-framed future-code specs in a greenfield phase —
no false "already lands" framing found — and need no action.)

## What was proven or propagated cleanly this round (do not re-litigate)

- **The ack table is TOTAL** (Opus): every (state,event) cell specified or proven-unreachable;
  ack cell exhaustively partitioned by epoch×code×studioDir; no missing event (second-ack-per-epoch,
  unknown-code, detach-during-timeout-callback, switch-during-unmount all probed and covered/excluded);
  only the detach-vs-switch pair conflicts = EE2. CC13 quantifier fix self-consistent (row 13 scopes
  to "every EXIT transition (rows 5-12)").
- **Late-ack rule sound** — case (ii) no-false-match DERIVED from the module-scope monotonic-from-1
  counter, verified to hold across project switches (session-scope counter) (Opus CLEAN; Fable FG-22).
- **CC5 poll-order premise is real** — three reviewers independently opened `useChannelReader.ts:237-282`
  and confirmed the CURRENT order is liveness-check-then-return-then-read, so Plan 08 Task 2's
  read-before-liveness reorder is a genuine fix, not a no-op (Cursor CC5-1 with full quote; Sonnet;
  Fable FG-1).
- **Ledgers (m)/(n) faithful** — (m) covers the D-12 badge narrowing INCLUDING the same-project
  round-trip case; (n) scopes the timeout false-failure; step 13 annotates all five narrowed
  decisions (D-02/D-03/D-06/D-09/D-12). No new un-ledgered narrowing — `isPlacementPending` gates
  an affordance never promised always-enabled (Opus CLEAN; Codex propagation list; Fable FG-15).
- **Timeout constant propagated** — every operative site reads 11_000/~11 s; the only `10_000`/`10 s`
  hits are the publish-sub-budget inside the derivation (Codex; Cursor WIRE-3; Fable FG-24).
- **Canonical strings 1 and 4 character-identical** across 08/14/15; string 3 referenced-not-quoted
  (CC8-OK). Only string 2 drifts (EE3) (Cursor STR-1/3/4; Codex).
- **CC6 stale Plan 04 block** supersede-marked and internally corrected; no other plan cites the
  retired five-field/WorldPanel-caller design as live (Codex; Cursor).
- **40+ real-source citations byte-verified MATCH** incl. channel zero-fill, `useChannelReader.ts`
  poll/throttle/decode lines, `workspaceStore.ts`, zustand 5.0.14, `package.json` no-sideEffects,
  zero prior `import.meta.hot`/`.subscribe`/`flushSync` in the renderer, DeleteUndoToast line map
  (Cursor AX/WIRE/HMR/CC15/DEC/FM; Fable FG-1…FG-27).
- **Reset-before-render mechanism untouched** except CC15's two premise notes; Opus CLEAN-2/3/4/5
  all still hold; all prior structural closures intact.

## Per-reviewer summaries

### Codex (seam-propagation trace) — NOT CONVERGED
SP-1/SP-2 (MED) = EE3 string-2 fragments. SP-3 (LOW) = EE5. Explicitly confirms the ack table rules,
11 s constant, ledger (m)/(n)+D-12, and retired reset text all propagated fully with site lists.
Output: `CONSULT-05.1-R11-codex-seam-trace.out`.

### Cursor (line-level citation & wiring) — NOT CONVERGED
STR-2 (MED) = EE3; TEST-1 (MED) = EE4. 40+ MATCH rows incl. the full ack wiring chain, CC5 current
poll order (quoted), all locked axioms, HMR/flushSync/sideEffects premises, and frontmatter mechanics.
Output: `CONSULT-05.1-R11-cursor-line-verify.out`.

### Sonnet (lateral new-seam hunt) — NOT CONVERGED
One HIGH = EE1 (`isPlacementPending` never cleared per-transition → SC4 trigger permanently disabled
after timeout/detach/switch). Swept clean: studioDir/reset interleaving, double-write races among
timeout/unmount/detach/late-ack, the poll reorder vs other consumers, untouched Plans 01–03/05–07/09,
the CC6 fix. Output: `CONSULT-05.1-R11-sonnet-lateral.out`.

### Opus (locked-decision fidelity & spec math) — NOT CONVERGED
DD1 (HIGH) = EE2 (detach transition missing row-7's studioDir guard → precedence universal false for
detach-vs-switch). DD2 (LOW) = EE6 (timeout margin derivation isn't a true bound under intensive
throttling; survives via symmetry). Certifies the table TOTAL and 6 dimensions CLEAN; all prior-round
CLEAN findings re-affirmed. Output: `CONSULT-05.1-R11-opus-spec-math.out`.

### Fable (fact-check of NEW claims, diff 3f050ab..eafec8b) — NOT CONVERGED
28 claim groups: 26 VERIFIED, 1 FALSE (EE4's ProjectBindingBar example — LOW, a wrong test-file
citation), 1 UNVERIFIABLE (correctly-framed greenfield future-code). Both LOCKED axioms independently
re-confirmed at HEAD and 751464a. Output: `CONSULT-05.1-R11-fable-factcheck.out`.

## Consensus themes for the round-12 replan

1. **Enumerate the two per-transition rules across ALL exit transitions** (EE1, EE2 — both HIGH,
   both one-transition fixes). For `isPlacementPending`: add `setPlacementPending(false)` to every
   exit row's action + acceptance, and trigger-re-enable assertions to the timeout/detach/unmount/
   switch tests. For the `studioDir` guard: add row 7's re-check to the detach transition (row 10),
   or an explicit detach-vs-switch unreachability argument. This is a completeness sweep of the new
   table, not a redesign — the table itself is proven total and correct.
2. **String hygiene and provenance** (EE3, EE4, EE5): carry canonical string 2 in full where quoted,
   fix or drop the ProjectBindingBar test example (applying the provenance rule to reviewer-supplied
   examples), and make the local-refuse reference consistent.
3. **Disclosure completeness** (EE6): restate the +1 s timeout justification as throttle-symmetry
   and widen ledger (n) to name renderer-side read starvation.

The loop's two hardest problems — the false-"restored" race and the ack protocol's under-specification
— are both now structurally closed and independently certified. Round 11's residue is entirely
enumeration completeness, string canonicalization, and one wrong test-file citation. This is the
closest the phase has come to convergence.
