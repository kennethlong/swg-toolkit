---
phase: 5.1
round: 9
reviewers: [codex, cursor, sonnet, opus, fable]
reviewed_at: 2026-08-01T20:09:32Z
plans_reviewed: [05.1-01-PLAN.md, 05.1-02-PLAN.md, 05.1-03-PLAN.md, 05.1-04-PLAN.md, 05.1-05-PLAN.md, 05.1-06-PLAN.md, 05.1-07-PLAN.md, 05.1-08-PLAN.md, 05.1-09-PLAN.md, 05.1-10-PLAN.md, 05.1-11-PLAN.md, 05.1-12-PLAN.md, 05.1-13-PLAN.md, 05.1-14-PLAN.md, 05.1-15-PLAN.md]
reviewed_commit: 2445b88
---

# Cross-AI Plan Review — Phase 05.1 — Round 9 (post-AA1–AA17 convergence check)

Reviewed commit `2445b88` (round-8 replan: AA1–AA17 + checker-loop iteration-2 fixes to Plan 08's
producer wire and Plan 13's must_have hedges). Five reviewers, non-overlapping angles: Codex
(seam-propagation trace), Cursor (line-level citation & wiring verification), Sonnet (lateral
new-seam hunt), Opus (locked-decision fidelity & spec math), Fable (fact-check of NEW claims in
commit 2445b88). Task files: `.planning/research/CONSULT-05.1-R9-*.md`, raw outputs `…-*.out`.

**Verdicts:** Codex NOT CONVERGED (2 HIGH, 5 MED, 1 LOW) · Cursor NOT CONVERGED (2 HIGH, 3 MED,
3 LOW; 1 rejected) · Sonnet NOT CONVERGED (2 HIGH, 2 MED) · Opus NOT CONVERGED (7 HIGH, 6 MED,
3 LOW) · Fable NOT CONVERGED (2 FALSE label claims; 18/21 claim groups verified).

**Overall: NOT CONVERGED — 7 HIGH, 9 MED, 7 LOW (unified work-list BB1–BB23).**

## Round-over-round signal

- **The fix-spawns-seams pattern is now on its 5th consecutive round.** Every HIGH except BB4/BB6/BB7
  was created or newly exposed by this round's own AA-fixes (AA2×AA5 compose into BB2/BB3; AA4's
  new ack mechanism carries BB1/BB5; and BB4/BB6/BB7 are protocol holes in that same new mechanism
  that simply had never existed to be reviewed before).
- **Fact-check is the cleanest yet:** 18/21 NEW claim groups byte-verified, zero fabricated source
  facts; the only FALSE items are two round-LABEL errors (BB17). Round 8's 219→218 correction is
  confirmed correct against real bytes (file ends in newline; `wc -l` = `awk END{NR}` = 218) —
  Cursor's 219 counter-claim is FALSIFIED and rejected (see Rejected section).
- **Convergence-from-divergence:** BB1 was found independently by 3 reviewers on 3 different angles
  (seam trace, citation verify, must_have satisfiability); BB5 by 3 (lateral, spec math, wiring);
  BB2 and BB6 by 2 each with matching concrete event sequences derived independently.

## Unified work-list (BB1–BB23)

| ID | Sev | Plans/files | Finding | Found by |
|----|-----|-------------|---------|----------|
| BB1 | HIGH | 14 (`:241`, `:361-362`, `:464`, `:481`, `:531-532`) | AA4 landed only in Task 3. Task 2's interfaces block still declares `sendStartPlacement(...): void` (Plan 08 says `: number`), and Task 2's behavior/action/acceptance/test still specify the OLD synchronous placement toast — Task 2's test at `:481` asserts the toast appears on Place click while Task 3's test at `:623-625` asserts NO toast appears on the same click, both written into the same `WorldPanel.test.tsx`. Executor running Task 2 then Task 3 breaks Task 2's own test; resolving by keeping Task 2's assertion re-creates the exact AA4 defect. must_have `14:26` unsatisfiable with Task 2 acceptance as written. | Opus BB1 + Codex SP-1/SP-2 + Cursor C01/C02 |
| BB2 | HIGH | 13 (`:716-741`, `:919-930`, `:31`(a)), 14 (`:598-609`) | AA2×AA5 ordering race → false "restored" toast on project switch. `key={studioDir}` remount runs AA2's lazy initializers during render, reading the store BEFORE the parent WorldPanel's `[studioDir]` effect calls `reset()` (React: child effects fire before parent effects; `openComplete` swaps `studioDir` in one step, `workspaceStore.ts:79-86`; WorldPanel never unmounts, `App.tsx:98`). `prevRef` seeds with Project A's entries; the later `reset()` empties `pending`; the diff reports `removed=[A]` → "A restored" renders in Project B. Violates `13:31`(a) "structurally impossible". | Opus BB2 + Sonnet L2 |
| BB3 | HIGH | 13 (`:734-738`, `:20`, `:22`, `:31`), 15 (`:389-399`) | AA2's mount seeding creates a FIFTH exit route that permanently loses a SINGLE, MOST-RECENT arrival — outside the AA3 carve-out. Sequence: A sticky → C removed (guard defers fold) → tab away/return → step-0 restores A's sticky display BUT seeds `prevRef = current pending = [A,C]` (C folded without ever being shown) and `wasStickyRef` initializes `false`, disarming the thaw-priority rule → dismiss/retry A → diff is empty → C's undo affordance gone. Falsifies `13:20`/`:22` outright (not a narrowing ledger (k) covers). | Opus BB3 |
| BB4 | HIGH | 08 (`:267-270`, `:308-309`), 07 (`:91-95`), 14 (`:26`) | Epoch-counter lifetime mismatch: renderer `hostCmdEpoch` is module-scope (resets to 0 on toolkit restart); the agent's `hostCmdResultEpoch` is a struct field in the still-running client (persists, e.g. at 47). After toolkit restart + re-attach, the reader's `lastHostCmdResultEpoch` seeds from the stale 47; new placements ack with epoch 1, `1 > 47` fails → every ack silently dropped until the counter passes 47. No toast of any kind for the whole session. | Opus BB4 |
| BB5 | HIGH | 04 (`:327-328`, `:347`, `:454-456`), 14 (`:614`, `:598-609`) | AA5's reset contract omits both pieces of state AA4 added in the SAME commit. Plan 04 calls its five-field `reset()` "the store's full module-initial state" while `lastHostCommandResult` is a sixth field 20 lines later (acceptance asserts "all five"); Plan 14's `[studioDir]` effect clears `prevTreeRef`/`suppressNextDiffRef` but NOT `pendingPlacementEpochRef`. Place in Project A → switch to B before the ack → ack matches the surviving ref → Project B shows Project A's cellName in the placement toast (cross-project misattribution). | Opus BB5 + Sonnet L1 + Cursor C03/C04 |
| BB6 | HIGH | 14 (`:587-597`, `:611-649`, `T-05.1-14j`), 07 (`:91-95`) | Lost-ack is unbounded: no timeout, no fallback, no test. (i) The channel result is a single overwritten slot and the real `useChannelReader.ts:114-117` documents ~1 Hz background-timer throttling while the game holds the foreground — two host commands inside one throttle window lose one result; `pendingPlacementEpochRef` then never resolves. (ii) The agent is silent during load/login/zoning; a placement issued then never acks. (iii) Client exit stops polling entirely (`useChannelReader.ts:237-249`) — deferred toast pends forever. AA4 traded a guaranteed-wrong toast for a possibly-absent one. Acceptance covers only match/unrelated-epoch. | Opus BB6 + Sonnet L3 |
| BB7 | HIGH | 14 (`:589-593`, `:614-617`), 12 (`:210-214`) | Single-slot `pendingPlacementEpochRef` inverts AA4's own double-click repro into a false NEGATIVE: click 1 → epoch 5 (accepted, result 1); click 2 → ref overwritten to 6 (refused by `g_placementActive`, result 0). Ack {5,1} doesn't match and is ignored; {6,0} matches → refusal toast — while placement mode IS armed from click 1. User told "refused" with a live placement armed. Needs a pending-epoch set/queue or refusing a second send while the ref is non-null. | Opus BB7 |
| BB8 | MED | 13 (`:20`, `:22`), 15 (`:356-366`) | Ledger (g), as rewritten by AA2 this round, states a SECOND deterministic D-03 narrowing (navigate away mid-window after a plain Remove and return inside 8s → toast does NOT re-show; recovery only via manual Add) that Plan 13's EXCEPT clauses do not name. Same half-propagation class as AA3, one round later, on the sibling case. | Opus BB8 |
| BB9 | MED | 13 (`:931-941`, `:814-818`, `:995-1003`) | AA14's "one synchronous turn, no intervening commit" assumption is load-bearing for the ROUND 8 retry-resolution guarantee, not hardening: an interleaved commit after `clearUndoError` consumes `wasStickyRef`, surfaces-and-folds C, then "A restored" replaces C — the outcome `:1001-1003` asserts impossible. It survives only via React's automatic batching. The ROUND 8 test never clears `undoErrors`, so it cannot detect this premature-thaw failure. | Opus BB9 |
| BB10 | MED | 12 (`:206-214`, `:248-250`), 09 (`:161-164`, T-05.1-09b) | Plan 12 (untouched this round) tells the executor the exclusivity guards are "verifiable by code inspection to early-return with result 0"; Plan 09's contract is that the ack is published centrally AFTER dispatch ("always publishes a result so epoch tracking never stalls"). A literal early `return;` publishes NO ack → BB6's permanent silence lands on exactly the case AA4 exists to fix. Caught only at Plan 15's Wave-6 human checkpoint. | Opus BB10 |
| BB11 | MED | 14 (`:598-609`), 10 (`:520`), 04 (`:335-337`) | Nothing schedules the newly-opened project's first `refreshTree()`. Plan 04 says reset runs "BEFORE the first refreshTree()" but Plan 10's only automatic trigger re-runs on `liveStatus.kind` changes — a `studioDir` change is not one, and WorldPanel stays mounted. After a switch the World panel shows an empty tree until a manual refresh — contradicts D-05 disk-scan-truth. | Opus BB11 |
| BB12 | MED | 14 (`:587-597`, `:611-622`), 04 (`:340-344`), CONTEXT D-12 | The AA4 refusal surface is a transient toast only — no history entry, no failure badge, no Activity-accordion record — unlike every other failure surface this phase ships (C8's `recordArmFailure` precedent). Compounded by BB6's throttling: the deferred toast most likely renders while the user is alt-tabbed into the fullscreen game, and there is no durable record when they return. | Opus BB12 |
| BB13 | MED | 13 (`:31`(c), `:819-825`, `:1143-1145`), 15 (`:389-399`) | Ledger (k)'s accounting understates multi-queued loss: A sticky; C then D queue; thaw shows D (C folded); user successfully Undoes D → diff shows "D restored"; C never surfaces on this route either, and A remains un-surfaceable per AA10 — after one freeze with two queued removals, TWO entries are stranded in `pending` with no UI path. | Opus BB13 |
| BB14 | MED | 13 (`:61`, `:1127-1129`), 05.1-VALIDATION.md (`:75`) | The AA3/D-03 hedge is still missing at three narrative restatement sites: Plan 13's D-03 summary line ("guarded by an undo toast (no confirm dialog"), Plan 13's done text ("reversible within the 8-second toast window"), and VALIDATION.md's matrix row ("undo toast is the sole guard" — file was outside this round's edit set entirely). | Codex SP-3/SP-4/SP-5 |
| BB15 | MED | 13 (`:30`), 14 (`:21`, `:711-712`) | AA8's concurrent-Undo removal is incomplete: must_haves in Plans 13/14 and Plan 14's summary still phrase the concurrent/simultaneous two-Undo scenario as a contract case ("concurrent Undos within one flush window are each individually pre-credited"; "a SECOND concurrent Undo's identity key is never clobbered"). | Codex SP-6/SP-7/SP-8 |
| BB16 | MED | 13 (AA2 blocks) | The AA2 mount-derivation mechanism (lazy initializers + step-0 sticky restore) has zero test coverage anywhere in Plan 13 — no unmount/remount/rerender test exercises it (grep for `.unmount`/`rerender`: zero hits). BB2/BB3 would both have been caught by such a test. | Sonnet L4 |
| BB17 | LOW | 13 (`:519`), 04 (`:23`), 13 (`:33`), 14 (`:26`, `:27`) | Two FALSE round-label claims (the commit's only fact-check failures): `13:519` credits `worldEditorStore.reset()` to "ROUND 3" (it is ROUND 10/AA5), and four NEW must_haves cite "REVIEWS.md ROUND 10" — no REVIEWS round 10 exists (the work-list is REVIEWS round 8; the same commit writes the correct "REVIEWS.md ROUND 8 crew — AA4/AA5" form elsewhere). | Fable FC-20/FC-21 |
| BB18 | LOW | 15 (`:7-8` vs `:206`) | Plan 15 `files_modified` lists only the handoff file; Task 2 also names `WorldPanel.tsx` and `overlay.cpp`. | Cursor C05 |
| BB19 | LOW | 04 (`:155`) | Citation off-by-one: cites `liveStore.ts:26-31` for the status union; `:26` is a comment, the union is `:27-31` (Plans 10/13 already cite `:27-31` correctly). | Cursor C06 |
| BB20 | LOW | 10 (`:189` vs `:58`, `:276`) | Internal label drift: interfaces block says "4-place" registration while objective/Task 1 say "5-place" (LAYOUT_VERSION bump is the 5th site). | Cursor C08 |
| BB21 | LOW | 04 (`:327-328`), CONTEXT D-06 | `reset()` clears `history` on PROJECT SWITCH; D-06's wording is "clears on app restart" — an undisclosed narrowing with no ledger item or step-13 annotation, where smaller narrowings (D-02/D-09/D-03) each got one. | Opus BB14 |
| BB22 | LOW | 13 (`:726-731`) | Mount initializer scans `pending` for "any entry" with a sticky error — a nondeterministic pick if the at-most-one invariant ever breaks, with no `wasStickyRef` history after remount to disambiguate. Pick the LAST such entry and state the invariant as an assertion. | Opus BB15 |
| BB23 | LOW | 15 (`:17` vs `:344-350`, `:461-462`) | must_have `15:17` enumerates "every later-round disclosed carve-out of a LOCKED decision" but omits ledger item (e) — D-02's REMOVE-scope narrowing (R10b) — though success_criteria covers it. A verifier recording against the must_have would not be prompted for it. | Opus BB16 |

## Rejected / no-action findings

- **Cursor C07 (DeleteUndoToast.tsx "219 lines") — FALSIFIED by bytes.** The file ends with a
  trailing newline; `wc -l` = `awk 'END{print NR}'` = **218**. The plans' "218-line" wording
  (round-8 AA15 fix) is correct. Verified by the orchestrator directly, 2026-08-01.
- **Fable FC-19 (AA16 process claim "verified by re-reading… see the replan's own return summary")**
  — UNVERIFIABLE-AS-STATED (the return summary is not an on-disk artifact). Presented as process
  record, not source fact; no plan change required. Optionally reword to drop the pointer.

## What propagated cleanly this round (do not re-litigate)

- **AA1 genuinely closed** — guard-and-swap reachable on its only branch, idempotent, terminates (Opus dim 1).
- **AA7 fully conditioned** — all three diskState absolutes carry the hedge (Opus dim 2; Fable FC-8).
- **AA3's D-03 disclosure IS a narrowing, not a violation** — neither forbidden thing (confirm
  dialog, in-game delete hotkey) returns (Opus dim 3); the five R8-named sites + ledger (k) +
  step-13 annotation all carry it (residuals are the NARRATIVE sites in BB14 and the sibling case in BB8).
- **Epoch ordering itself is sound** — strictly-increasing per renderer lifetime, code-then-epoch
  publish order via `InterlockedExchange`, torn reads rejected by the gate (Opus dim 4; the
  *lifetime* mismatch is BB4, the ordering is not in question).
- **AA4 identifier chain is character-exact at the revision layer** — producer call, store field,
  consumer read all agree (Cursor chain table; Codex "propagated consistently" list; Fable FC-11) —
  the BB1 residue is Task 2's stale text, not the chain itself.
- **Project-switch reset sites agree** on stores + the two named refs (Codex) — the gap is the
  *enumeration* (BB5), not inconsistency between sites.
- **Citation discipline against real source is clean:** 20+ renderer citations byte-verified MATCH
  (Cursor W01–W36; Fable FC-1…FC-18): `DeleteUndoToast.tsx` all cited ranges, `useChannelReader.ts:270-273`,
  `workspaceStore.ts:100-109`, `workspaceService.ts:219`, `decorationChannel.ts:36-59`,
  `decorationPersist.ts:119`, LAYOUT_VERSION=4, all `depends_on` targets exist, key_link regexes valid.
- **Wave/dependency arithmetic** for the ROUND 10 additions is correct (Opus dim 9; Cursor W27–W31).

## Per-reviewer summaries

### Codex (seam-propagation trace) — NOT CONVERGED
SP-1/SP-2 (HIGH) = BB1's Plan 14 stale interfaces + Task 2 test. SP-3/SP-4/SP-5 (MED) = BB14's
unhedged narrative sites incl. VALIDATION.md:75. SP-6/SP-7/SP-8 = BB15 residual concurrent-Undo
wording. Explicitly confirms three contracts propagated fully (reset sites, sticky mechanism +
ledger (g) determinism, HOST_CMD core). Full output: `CONSULT-05.1-R9-codex-seam-trace.out`.

### Cursor (line-level citation & wiring) — NOT CONVERGED
C01/C02 (HIGH) = BB1. C03/C04 (MED) = BB5's reset omissions. C05/C06/C08 = BB18/BB19/BB20.
C07 rejected (falsified). 30+ MATCH verifications of renderer citations and the full AA4 wiring
chain (W01–W36). Full output: `CONSULT-05.1-R9-cursor-line-verify.out`.

### Sonnet (lateral new-seam hunt) — NOT CONVERGED
L1 (HIGH) = BB5's cross-project ack leak. L2 (HIGH) = BB2's remount/reset race (verified against
real `DeleteUndoToast.tsx:73-83` effect semantics). L3 = BB6's no-timeout/client-exit hole
(real `useChannelReader.ts:237-249`). L4 = BB16 zero AA2 test coverage. Swept clean: untouched
plans 01–03/05–07/09/11/12 for renamed-contract seams, `close()` vs `openComplete()` distinction,
AA7 vs Plan 06, AA8 substitute, wave ordering. Full output: `CONSULT-05.1-R9-sonnet-lateral.out`.

### Opus (locked-decision fidelity & spec math) — NOT CONVERGED
BB1–BB7 (HIGH), BB8–BB13 (MED), plus three LOW (unified as BB21–BB23), each with a concrete event
sequence; 9 dimensions explicitly clean (incl. AA1 closed, epoch ordering sound, D-03 disclosure
legitimate, undo-window algebra internally consistent — "the failures are the routes that BYPASS
the algebra, not the algebra"). Full output: `CONSULT-05.1-R9-opus-spec-math.out`.

### Fable (fact-check of NEW claims, diff 6725f72..2445b88) — NOT CONVERGED
21 claim groups: 18 VERIFIED (incl. every new line-range into `packages/renderer/src/**`, the 218
correction, AA-attribution table vs round-8 rows, cross-plan sibling claims), 2 FALSE (BB17's two
round-label errors — no fabricated source facts), 1 UNVERIFIABLE (FC-19 process claim). Full
output: `CONSULT-05.1-R9-fable-factcheck.out`.

## Consensus themes for the round-10 replan

1. **The AA4 ack mechanism needs a design pass, not spot fixes.** BB1, BB4, BB5, BB6, BB7, BB10,
   BB12 are all facets of one under-specified protocol: epoch lifetime, pending-set vs single slot,
   timeout/fallback, durable failure surface, agent always-acks, and Task 2/Task 3 consistency.
   Fixing them one-by-one risks a sixth round of spawned seams; specify the protocol once
   (state machine: request → pending(epoch set) → resolved(code)/timed-out/aborted-by-switch) and
   restate every consumer from that single spec.
2. **The remount/reset lifecycle needs an explicit ordering contract.** BB2, BB3, BB16, BB22 —
   define which mechanism (key remount vs reset effect) is the single source of project-switch
   truth, make the other subordinate, and add the unmount/remount test BB16 demands.
3. **Close the disclosure ledger loop.** BB8, BB13, BB14, BB21, BB23 — every narrowing gets its
   ledger item AND every restatement site (incl. VALIDATION.md and narrative/done text) carries
   the hedge, per the AA16 enumeration-sweep prescription.
4. **Mechanical cleanups:** BB15, BB17, BB18, BB19, BB20.
