---
phase: 05.1-live-world-editor-productization
plan: 13
type: execute
wave: 4
depends_on: ["05.1-11", "05.1-01", "05.1-08", "05.1-10"]
files_modified:
  - packages/renderer/src/state/removeUndoStore.ts
  - packages/renderer/src/state/removeUndoStore.test.ts
  - packages/renderer/src/services/decorationPersistOrchestrator.ts
  - packages/renderer/src/services/decorationPersistOrchestrator.test.ts
  - packages/renderer/src/panels/world/RemoveUndoToast.tsx
  - packages/renderer/src/panels/world/RemoveUndoToast.test.tsx
  - packages/renderer/src/panels/world/WorldPanel.tsx
  - packages/renderer/src/panels/world/WorldPanel.test.tsx
autonomous: true
requirements: [pivot-driven]
must_haves:
  truths:
    - "Any decoration, stock included, is removable as a World-panel row action guarded by an undo toast, with no confirm dialog (D-02/D-03, SC4) — EXCEPT the ROUND 9 disclosed narrowing (AA3): a removal that arrives during ANOTHER entry's sticky-error freeze and is NOT the most recent such arrival never gets its own undo-toast turn (its data removal remains durable; see this plan's ROUND 9 revision and Plan 15's gap ledger item (k)) — and EXCEPT the disclosed plain-remount narrowing (Plan 15 gap ledger item (g); R9 review, BB8): navigating away from the World tab mid-window after a plain (non-sticky) Remove unmounts the panel-scoped toast, and returning inside the 8-second window does NOT re-show it (the removal stays durable; recovery only via the manual Add flow)"
    - "Remove ALWAYS performs a durable, data-only .ilf row removal (D-02) — this is the guaranteed contract this phase ships. The opportunistic live-despawn branch (sendDespawnNode when a live WS node id is tracked for the row) is implemented and unit-tested as a mechanism, but AS OF THIS PHASE no producer populates a live id on any row the World panel shows, so it is forward-compatible groundwork, not an exercised capability in any session this phase ships — this is stated honestly here rather than implied as delivered (REVIEWS.md C4)"
    - "Remove is reversible via the 8-second undo toast window; after the window elapses, restoring a removed decoration requires manually re-adding it via the Add flow (D-02's 'inherently reversible' claim is scoped to this window, per Sonnet F11) — EXCEPT the ROUND 9 disclosed narrowing (AA3): if a removal arrives during another entry's sticky-error freeze and is not the most recent such arrival, it loses its own undo-toast turn entirely, regardless of the 8-second window, and is recoverable only via the manual Add flow (see this plan's ROUND 9 revision and Plan 15's gap ledger item (k)) — and EXCEPT the disclosed plain-remount narrowing (ledger item (g); R9 review, BB8): a mid-window navigation away from the World tab deterministically forfeits the remainder of that toast's window (the toast does not re-show on return), leaving manual re-add via the Add flow as the only recovery for that entry too"
    - "removeDecorationRow sources its mandatory buildingTemplateVfsPath from the WorldEditorBuilding the caller already has (Plan 04's durable per-project map, ROUND 3/R3), and fails closed with a words-only message — never crashes on a missing readVfs entry — when that value is unknown for a given building (ROUND 3, R3)"
    - "WorldPanel.tsx builds a real overrideDir/readVfs/studioDir triple (via Plan 10's shared, null-safe resolveOverridePair() helper, which itself guards studioDir before overrideDir, ROUND 5 V6) before calling removeDecorationRow — not a call the plan describes but never shows how to construct, and never a second, unguarded resolution path (ROUND 3, R4; ROUND 4, W3; ROUND 5, V6/V12)"
    - "removeDecorationRow receives mappingName from its caller instead of referencing a variable it was never given (ROUND 3, R7)"
    - "Remove's recorded history message routes through formatPersistMessage (D-10), so a mirror-OFF removal carries the same hybrid-session-invisibility detail an edit/add persist does (ROUND-3-REVIEW R10a)"
    - "The Remove click handler resolves overrideDir/readVfs/worldEditorBuildingTemplates FRESH at click time (via Plan 10's resolveOverridePair()) and refreshes the tree via Plan 10's shared refreshTree() helper, never a cached mount-time value (ROUND-3-REVIEW R2; ROUND 4, W3)"
    - "Undo re-adds the removed row via the append-only add path (D-01's one-code-path mandate), which reorders the cell relative to the row's original position — an accepted, disclosed consequence, not a defect (ROUND-3-REVIEW R7)"
    - "WorldPanel.tsx owns the ENTIRE Undo restore end-to-end via a required onUndo(entry) prop — RemoveUndoToast.tsx is purely presentational (matching DeleteUndoToast.tsx's own precedent) and has NO access to resolveOverridePair()/studioDir/overrideDir/readVfs, closing the round's anchor finding that the prior design was unimplementable as written (ROUND 5, V1 — HIGH, Codex/Cursor/Opus, three independent angles). A null resolveOverridePair() result on Undo leaves the entry in removeUndoStore.pending untouched (never silently dropped) and never calls restore() (the same fail-honest posture as Remove)"
    - "Undo's own refreshTree() call is preceded by PUSHING the restored row's identity key (buildingId+cellName+objectTemplateName) onto a shared suppressNextDiffRef QUEUE (an array, NOT a single nullable ref and NOT a blanket boolean) — forward-looking groundwork this plan adds for Plan 14's content-identity '(NEW)' diff effect to drain-and-consume, so an Undo-restored row is never marked as a fresh placement even on the very next refresh, WITHOUT ALSO suppressing the diff for any OTHER, unrelated row that lands new in the SAME refresh window, and WITHOUT any push ever overwriting a not-yet-drained identity key (the push is a spread-append onto the queue — append-only semantics proven via the static grep gate per AA8; through this phase's single-slot toast UI, Undos are only reachable SEQUENTIALLY, so multi-entry queue occupancy is a structural guarantee, not an exercisable contract case — R9 review, BB15) (ROUND 5, V2 groundwork, closing the delayed-misattribution class Sonnet found; ROUND 6, X3 — HIGH, Sonnet, scoping the suppression so it can never permanently swallow an unrelated genuinely-new row; ROUND 7, Z5 — MEDIUM, Sonnet, replacing the single-scalar ref with an append-only queue; reworded per AA8 + R9 review BB15, since the earlier 'concurrent Undos' framing described a scenario the shipped single-slot toast UI cannot produce) — the tree is never left stale after a restore either way, the SAME refresh discipline this plan's own Remove path already follows"
    - "A failed Undo is NEVER presented as a false success and NEVER silently erases another entry's failure signal (ROUND 7, Z1 — HIGH, Opus/Sonnet, respecifying ROUND 6's X4 must_have, which collided with its own 'near-verbatim copy of DeleteUndoToast' mandate on the thrown-add-back branch): (a) the add-back helper is now called BEFORE restore(id) pops the entry from pending — restore(id) runs ONLY after the add-back succeeds — so a thrown add-back NEVER removes the entry from pending, and the pending-diff effect (which reads an entry LEAVING pending as 'restored') can therefore never fire on a failed attempt; a false 'restored' toast is structurally impossible, not just unlikely (and the round-9 project-switch remount route that transiently violated this absolute is closed by Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT — the stores reset synchronously inside the studioDir-changing set() call, before any render/remount can observe mixed state — so the absolute holds again with no carve-out; R9 review, BB2). (b) removeUndoStore.lastUndoError becomes undoErrors: a per-entry-keyed map (Map<id, message>) — setUndoError(id, message) touches ONLY that id's slot, and clearUndoError(id) (now REQUIRES an id, never a global clear) deletes ONLY that id's slot, so acting on one entry's Undo can never wipe a DIFFERENT entry's stored failure. (c) because this toast remains a single-slot design (matching DeleteUndoToast's own precedent), RemoveUndoToast additionally refuses to let a NEW removal's toast preempt an entry that currently has an unresolved undoErrors entry AND is still in pending — that entry's error toast stays displayed (a 'sticky' error) until the user either dismisses it explicitly (which also clears ONLY that entry's undoErrors slot, an explicit acknowledgement) or retries Undo on it (success removes it from pending; a repeat failure keeps the SAME sticky display shown with the new error text, WITHOUT ever re-arming a dismiss timer — no timeout is scheduled while an error is sticky, ROUND 9 below) — so a stuck entry's failure is never silently superseded by an unrelated Remove before the user can act on it, AND is never silently dismissed by its own inherited 8-second auto-dismiss timer while unresolved (ROUND 9). **(ROUND 8, checker blocker — the mechanism this guarantee actually requires, since a naive near-verbatim copy of DeleteUndoToast's unconditional trailing `prevRef.current = pending` would silently fold ANY removal that arrives during the sticky window into the toast's 'already seen' baseline, permanently losing its own Undo affordance even after the sticky entry resolves):** while sticky, the toast's pending-diff effect must NOT fold `pending` into its `prevRef` baseline at all — this is the ONE point where the copy deviates from `DeleteUndoToast.tsx` on purpose. Once the sticky entry is resolved (dismissed OR successfully retried), the SAME effect (now re-triggered because it also depends on `undoErrors`, not only `pending`) diffs the frozen baseline against the CURRENT `pending` and, if any entries arrived during the freeze, shows the newest of THEM — taking priority over a bare 'restored' confirmation for the just-resolved entry on that one transition tick — and only THEN folds the baseline forward. This closes the gap on BOTH exit routes (an explicit dismiss, and a successful retry), not only the dismiss path. Together these three fixes replace the unsatisfiable 'NEVER invisible' framing with three independently-testable, always-true guarantees: never a false success, never a cross-entry erasure, never silently superseded while unresolved — and this ROUND 8 mechanism is what makes the third guarantee actually deliver the 'advances to the next queued removal' behavior the guarantee's own parenthetical promises, on both exit routes, not just one. **(ROUND 9, revision — the sticky guarantee's own inherited-timer gap and a disclosed narrowing):** the toast's own 8-second auto-dismiss timer (`DeleteUndoToast.tsx`'s `armTimer`, mirrored here) is NEVER armed while a sticky error is displayed — entering the sticky state calls `clearTimer()` only, so the toast cannot be silently dismissed by its own timeout regardless of the fold-deferral fix above; the timer idiom applies only to this toast's non-sticky states. Separately, if MORE THAN ONE removal arrives during a single sticky freeze (e.g., C then D), only the MOST RECENT (D) surfaces its own toast on thaw — this is a disclosed, accepted narrowing, not a defect: the earlier one(s) (e.g., C) keep their already-successful `.ilf` removal but lose their own dedicated undo-toast affordance once the baseline folds past them, recoverable only via the manual Add flow, the SAME accepted-after-expiry framing Sonnet F11 already established for the plain 8-second-window case. (R9 review, BB13 — the full accounting: after ONE freeze with N>1 queued removals, the entries left in `pending` with NO remaining UI surface number N-1 non-newest arrivals PLUS the sticky entry itself if it exits via dismiss (per AA10, a dismissed entry has no second surface) — e.g. a freeze with C and D queued strands TWO entries (the dismissed A and C), not one; Plan 15's ledger item (k) states this same arithmetic.)"
    - "handleUndo resolves the WorldEditorBuilding it needs for the add-back call from the current tree by entry.buildingId (useWorldEditorStore.getState().tree.find(...)), and fails closed via log() + setUndoError(entry.id, ...) (per-entry keyed, ROUND 7/Z1) (never calling the add-back helper/restore()) when that lookup comes back empty (ROUND 6, X6 — MEDIUM, Cursor)"
    - "removeUndoStore's pending/undoErrors are reset to their initial empty state on a project switch via a new reset() action (this plan's Task 1), invoked by the store's OWN module-scope workspaceStore subscription per Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT (reset-before-render: the reset runs synchronously inside the studioDir-changing set() call, strictly before React re-renders, so RemoveUndoToast's key-remount initializers can never seed from the OLD project's pending — R9 review, BB2) — a module-scope singleton store surviving in the renderer process across the OpenProject flow would otherwise leak Project A's stuck removal(s)/sticky-error toast into Project B's session; Plan 04's worldEditorStore registers the IDENTICAL subscription for its own six fields, and Plan 14's WorldPanel's [studioDir] effect now handles ONLY its component-local refs, the pending-placement abort, and the new project's first refreshTree() (REVIEWS.md round 8 crew, AA5 — HIGH, Sonnet L2; re-derived R9 review, BB2/BB11)"
  artifacts:
    - path: "packages/renderer/src/panels/world/RemoveUndoToast.tsx"
      provides: "the D-03 undo-toast affordance"
      contains: "Undo"
    - path: "packages/renderer/src/services/decorationPersistOrchestrator.ts"
      provides: "removeDecorationRow — data-only removeNode + a live-despawn code path that no current caller ever exercises with a non-null id"
      contains: "removeDecorationRow"
  key_links:
    - from: "packages/renderer/src/panels/world/WorldPanel.tsx"
      to: "packages/renderer/src/services/decorationPersistOrchestrator.ts"
      via: "the row's Remove action calling removeDecorationRow"
      pattern: "removeDecorationRow\\("
    - from: "packages/renderer/src/services/decorationPersistOrchestrator.ts"
      to: "packages/renderer/src/services/hostCommand.ts"
      via: "removeDecorationRow's conditional sendDespawnNode call — mechanism exists, no current caller passes a non-null liveNetworkId (see C4 note)"
      pattern: "sendDespawnNode\\("
    - from: "packages/renderer/src/panels/world/RemoveUndoToast.tsx"
      to: "packages/renderer/src/panels/world/WorldPanel.tsx"
      via: "the required onUndo(entry) prop — WorldPanel owns the whole resolve-then-restore-then-refresh sequence; the toast never resolves overrideDir/readVfs/studioDir itself (ROUND 5, V1)"
      pattern: "onUndo\\("
    - from: "packages/renderer/src/panels/world/WorldPanel.tsx"
      to: "packages/renderer/src/panels/world/WorldPanel.tsx"
      via: "handleUndo pushing the restored row's identity key (buildingId+cellName+objectTemplateName) onto the shared suppressNextDiffRef QUEUE before calling refreshTree() — forward groundwork Plan 14's '(NEW)' diff effect drains to pre-consume exactly those rows, never a blanket skip and never a clobbered single scalar (ROUND 5, V2; ROUND 6, X3 — scoped to content identity; ROUND 7, Z5 — queue, not a single ref)"
      pattern: "suppressNextDiffRef"
---

<objective>
Build D-02/D-03's Remove capability: a World-panel row action, guarded by an undo toast (no confirm dialog,
the exact `DeleteUndoToast.tsx` pending-diff precedent scoped to decoration rows — subject to the two
disclosed D-03 narrowings recorded in Plan 15's gap ledger: (k) freeze multi-arrival, ROUND 9/AA3, and (g)
plain-remount toast loss; R9 review, BB14/BB8), that removes a `.ilf` row
via Plan 01's `assembleDecorationEdit(kind='remove')` and re-derives the building template — the SAME
model-D data path as moving a stock table, per D-02 ("any decoration is removable, stock included; the stock
file is never touched, so removal is inherently reversible"). Undo re-adds the row via the SAME
`kind='add'`-equivalent data path (not a bespoke "undo" mutator), matching the phase's "one code path" ethos.

**ROUND 3 REVISIONS (2026-08-01, REVIEWS.md R3/R4/R7):**
- **R3 (HIGH, Opus, ground-truth confirmed):** `assembleDecorationEdit`'s FIRST operation on every call is
  `deps.readVfs(edit.buildingTemplateVfsPath)` — a value `removeDecorationRow` has no live capture to source
  (Remove is an offline, disk-scan-driven action). `WorldEditorBuilding.buildingTemplateVfsPath` (Plan 04 ROUND
  3) now carries this durably, sourced from the new `WorkspaceBindingMeta.worldEditorBuildingTemplates` map
  (Plan 02/06 ROUND 3) that Plan 06's orchestrator populates on every LIVE edit/add capture. `building.
  buildingTemplateVfsPath` may still be the empty string for a building this project has never captured a live
  edit/add for (e.g., a fresh clone, or a project where every existing edit predates this ROUND 3 revision) —
  Task 1 below fails CLOSED on that case with a clear, words-only error, rather than letting
  `assembleDecorationEdit` throw an unfriendly VFS-resolution error deep inside `readVfs`.
- **R4 (HIGH, Opus):** `removeDecorationRow` takes `overrideDir`/`readVfs` parameters, but no plan showed how
  its caller (WorldPanel.tsx, Task 2) constructs them. Task 2 now explicitly resolves them via Plan 10's shared
  `resolveOverridePair()` helper (ROUND 4 revision below) — the SAME helper Plan 10's refresh()/mirror-toggle
  flow already uses (this file is edited by both plans; the pattern is now consistent across every
  WorldPanel.tsx action that needs disk access).
- **R7 (MEDIUM, Opus):** `removeDecorationRow`'s body calls `sendDespawnNode(mappingName, liveNetworkId)` but
  `mappingName` was never among its declared parameters — a real undeclared-variable bug. `removeDecorationRow`
  now takes an explicit `mappingName: string | null` parameter, threaded from `useLiveStore` by its caller
  (WorldPanel.tsx). If `mappingName` is null while `liveNetworkId` is somehow non-null (should not happen in
  practice — there is no live id to despawn without a live session), the despawn call is skipped with a logged
  warning, matching the function's existing best-effort/never-blocks-the-data-removal posture.

**CROSS-AI REVIEW REVISION (2026-08-01, C4 — HIGH, Codex/Sonnet/Cursor):** the original design of this plan's
`removeDecorationRow` accepted a `liveNetworkId: string | null` parameter and, when non-null, ALSO called
`sendDespawnNode` — described as "opportunistic live despawn where tracked." The cross-AI crew found this was
functionally dead code as originally scoped: the live id would need to come from `worldEditorStore`'s session
overlay, but Plan 04's `sessionOverlay` type is `Map<rowId, 'armed' | 'saved' | 'failed'>` — it carries NO
node-id association at all — and Plan 12's placement flow DESPAWNS ITS OWN temp `wsAddObject` node
IMMEDIATELY after a successful persist (per this SAME phase's own design), so by the time a Remove action
could ever act on a row, no row in this phase's delivered scope EVER carries a live id to despawn. Rather than
either (a) building a materially larger cross-cutting feature (threading a live node id into a NAMED
`sessionOverlay` field and deferring Plan 12's auto-cleanup — the crew's alternative option) at this late
stage of an already-large replan, or (b) silently leaving the misleading "opportunistic" framing in place,
this revision takes the crew's OTHER explicitly-offered resolution: **relabel the branch as forward-
compatible groundwork and state plainly that Remove is data-only in every session this phase ships.** The
`liveNetworkId` parameter and the `sendDespawnNode` call inside `removeDecorationRow` are KEPT (they are
correctly implemented and unit-testable in isolation — a future plan that DOES thread a live id through
`sessionOverlay` can call this function with a non-null id and it will work), but Task 2's WorldPanel.tsx
wiring now ALWAYS passes `liveNetworkId: null` (there is no other honest value to pass, since nothing in this
phase's delivered scope populates one), and this plan's `must_haves`/objective say so explicitly rather than
implying an exercised live-despawn path.

**Sonnet F11:** D-02's "inherently reversible" framing is now explicitly scoped to the 8-second undo-toast
window in this plan's must_haves — after that window elapses, the ONLY way to restore a removed decoration is
to manually re-add it via the 021-A Add flow (Plan 14), which is possible (the stock `.ilf` is never touched,
so the decoration's original template+transform are always recoverable by hand) but is NOT itself an
"undo" action once the toast has expired.

For D-04's live-despawn branch (retained as groundwork, per C4 above): `removeDecorationRow` checks whether
the target row is CURRENTLY associated with a live WS node id the toolkit is tracking from this session — as
of this phase, this is ALWAYS null (no producer populates it) — if a future plan adds that tracking, this
same function's existing branch calls `hostCommand.sendDespawnNode` before/alongside the data removal;
otherwise (today, always) the removal is data-only and the row's status reads "removed — reload scene to see
it gone" (matching D-10's honesty-pattern precedent for mirror-off warnings) until the user reloads.

**ROUND 4 REVISIONS (2026-08-01, REVIEWS.md W2/W3):**
- **W2 (HIGH, Sonnet — a new seam opened by R2's own refresh discipline, crossed with round-3's "(NEW)"
  marker):** the original Task 2 had Undo call `restore(id)` + the add-back helper + show a confirmation toast,
  but NO `refreshTree()` call — while this SAME plan's own R2 requirement (see below) already requires every
  disk mutation in this file to re-read state fresh, applied to Remove but missed on Undo. Because `addNode` is
  append-only (D-01/R7's disclosed reorder), an undone middle row lands at the END of its cell on disk; with no
  refresh, `worldEditorStore.tree` and Plan 14's local "(NEW)" tracking both stay frozen until some UNRELATED
  later event triggers a refresh — and when that later refresh eventually lands, Plan 14's content-identity
  "(NEW)" diff (its own ROUND 4 fix) would otherwise see the restored row appear at a new position and could
  misread it as a fresh placement. Fixed below: Undo's re-add now ALSO calls Plan 10's shared `refreshTree()`
  immediately after it completes, closing the gap at its root (a missing refresh) rather than only in Plan 14's
  diff logic.
- **W3 (MEDIUM, Cursor — the R8/M3 null-guard half-propagated to `refreshTree()` but not to Remove):** the
  original Task 2 resolved `overrideDir`/`readVfs` for the Remove click handler INLINE (its own
  `resolveScanRoot`/`makeReadVfs` call), with no guard for `overrideDir === null` — a real `tsc` error
  (`string | null` feeding `removeDecorationRow`'s `string`-typed `overrideDir` parameter) the moment a user
  clicks Remove with no resolvable scan root. Fixed: the Remove click handler now calls Plan 10's shared,
  null-safe `resolveOverridePair()` helper (the SAME one `refreshTree()` and the mirror toggle use) instead of
  resolving inline a second way.

**ROUND 5 REVISION (2026-08-01, REVIEWS.md V1/V2 groundwork/V5/V6/V12):**
- **V1 (HIGH, Codex/Cursor/Opus — three independent angles converged on the same defect, the round's anchor
  finding):** the ROUND 4 design gave `RemoveUndoToast` (a sibling file to `WorldPanel.tsx`) an inline
  add-back needing `studioDir`/`overrideDir`/`readVfs`/`building` — but Plan 10's `resolveOverridePair()`
  is component-local to `WorldPanel.tsx`, unreachable from a sibling file. Task 2 below makes
  `RemoveUndoToast` purely presentational (the SAME `DeleteUndoToast.tsx:48`/`:68-92` precedent it was always
  supposed to match — zero props, pending-diff subscription lives at `:68-92`, not `:96-101`; ROUND 6/X5, ROUND
  10/AA11 citation fix) with a required `onUndo: (entry: RemovedRowEntry) => void` prop; `WorldPanel.tsx` now
  owns the WHOLE restore: `resolveOverridePair()` (null → words-only no-op, entry stays in `pending`,
  same reasoning as Remove) → `restore(id)` → the add-back helper with the FRESH pair → `refreshTree()`. This
  makes D-03's undo guarantee (the only guard on a destructive no-confirm action) actually implementable, and
  keeps W2's refresh-on-Undo guarantee intact by construction (WorldPanel already owns `refreshTree()`).
- **V2 groundwork (paired with Plan 14's own ROUND 5 fix):** WorldPanel's Undo handler sets a shared
  `suppressNextDiffRef.current = true` immediately before its own `refreshTree()` call, so Plan 14's
  content-identity "(NEW)" diff effect (consuming this SAME ref, later wave, same file) can silently re-baseline
  on an Undo-triggered refresh instead of marking the restored row "(NEW)" — closing the "delayed, misattributed
  (NEW)" class Sonnet found in round 5 at its root (the diff's OWN trigger/seed story), not just at Undo's call
  site. (Refined in ROUND 6/X3 below: the ref now carries the restored row's identity key rather than a bare
  boolean, so the suppression scopes to exactly that one row instead of blanket-skipping the whole diff.)
- **V5 (MEDIUM, Cursor):** the Remove click handler's `mappingName` read is now the EXPLICIT narrowed form —
  `const st = useLiveStore.getState().status; const mappingName = st.kind === 'attached' ? st.mappingName :
  null;` — `mappingName` exists ONLY on the `'attached'` member of `liveStore.ts`'s status union; reading it
  unconditionally is a real `tsc` error, the same class the R8/M2 `clientLabel` fix (Plan 10) already closed.
- **V6/V12 (MEDIUM/LOW, Cursor):** Plan 10's `resolveOverridePair()` now guards `studioDir` (ROUND 5, Plan
  10's own V6 fix) and returns it as part of the resolved pair; Task 2's Remove call-site bullet below now
  explicitly names `pair.studioDir` in `removeDecorationRow`'s call (Task 1's signature already required it
  as its first parameter — this was previously omitted from the bullet, V12).

**ROUND 6 REVISION (2026-08-01, REVIEWS.md X3/X4/X5/X6/X7/X13):**
- **X3 (HIGH, Sonnet):** the ROUND 5 `suppressNextDiffRef` was a blanket boolean — Plan 14's diff effect skipped
  its ENTIRE count-map diff whenever it saw `true`, which would also fold any OTHER building's genuinely-new
  row (landed on disk in the same poll window, e.g. via a concurrent live persist) into the re-baselined
  snapshot undiffed, permanently losing its "(NEW)" marking. `handleUndo` below now sets
  `suppressNextDiffRef.current` to the restored row's IDENTITY KEY — `{ buildingId: entry.buildingId, cellName:
  entry.cellName, objectTemplateName: entry.removedNode.objectTemplateName }` — instead of the boolean `true`;
  Plan 14's diff effect (consuming this SAME ref) pre-consumes exactly that one tuple from its normal
  count-map diff rather than skipping the diff outright, so an unrelated new row in the same building (or any
  other building) is still correctly diffed and marked "(NEW)" (see Plan 14's own ROUND 6 revision for the
  consumer-side half).
- **X4 (HIGH, Sonnet):** a failed Undo (`resolveOverridePair()` returning `null`, the building lookup coming up
  empty per X6, or the add-back helper throwing) previously only called `log()` — invisible to the toast, whose
  own pending-diff effect observes nothing on a failed attempt (the entry never left `pending`) and whose
  original 8-second timer keeps counting down regardless. Task 1 below adds an observable `lastUndoError: {
  id: string; message: string } | null` field (+ `setUndoError`/`clearUndoError` actions) to `removeUndoStore`;
  Task 2's `handleUndo` calls `clearUndoError()` FIRST on every attempt (clearing any stale prior error) and
  `setUndoError(entry.id, <words-only reason>)` on every failure branch; `RemoveUndoToast.tsx` watches
  `lastUndoError` and, when it names the CURRENTLY-DISPLAYED toast's entry, swaps its content to the failure
  reason and re-arms its 8-second timer — mirroring `DeleteUndoToast.tsx`'s own catch-and-show-and-rearm
  precedent (`handleUndo`, lines 96-111) even though, in this design, the actual write path lives in a
  DIFFERENT component (`WorldPanel.tsx`, not the toast itself) per V1's own separation of concerns.
  **(SUPERSEDED by ROUND 7, Z1 below — this single `lastUndoError`/no-arg-`clearUndoError()` shape is
  unsatisfiable on the thrown-add-back branch and vulnerable to cross-entry erasure; the CURRENT contract is
  the per-entry-keyed `undoErrors: Map<string, string>` + `clearUndoError(id)` + reordered add-back-before-
  restore design in this plan's frontmatter must_haves and ROUND 7 REVISION section — and, per R9 review
  BB9, the 'calls clearUndoError() FIRST on every attempt' instruction above is ALSO superseded:
  `clearUndoError` now fires only at settle time (an explicit dismiss, or after a successful retry's
  `restore()`), never at the top of a retry — read this X4 note as
  history of WHY the mechanism exists, not as the shape to implement.)**
- **X5 (MEDIUM, Fable — factual correction, no design change):** the read_first note below previously
  mischaracterized `DeleteUndoToast.tsx` as a component that "only calls a prop the OWNER passed in." The real
  bytes: `DeleteUndoToast` (`DeleteUndoToast.tsx:48`) takes ZERO props and is mounted globally in `App.tsx`;
  its OWN `handleUndo` (lines 96-111) calls `useDeleteUndoStore.getState().restore(entry.id)` DIRECTLY on the
  store, catches its own failure, and re-arms its own timer — it does not delegate to an owner via a prop at
  all. The load-bearing HALF of the original rationale still holds (the toast resolves no write-path
  dependencies of its own — no `overrideDir`/`readVfs`/`studioDir`/`building`), but `RemoveUndoToast` actually
  goes ONE STEP FURTHER than this precedent: because ITS restore needs a resolved write path only
  `WorldPanel.tsx` has, `RemoveUndoToast` cannot even call `restore()` itself the way `DeleteUndoToast` does —
  it must delegate the ENTIRE restore (not just the write path) to the `onUndo` prop. The corrected line-range
  citations below: the pending-diff detection effect is `DeleteUndoToast.tsx:68-92`; the 8-second timer is
  `:30`/`:61-64`; the toast shell (the "delete" variant this component's shape mirrors) is `:146-166`; `:96-101`
  is `handleUndo` specifically, not the toast-shell/props claim the earlier draft attached it to.
- **X6 (MEDIUM, Cursor):** `handleUndo`'s add-back call needs the target row's `WorldEditorBuilding`, but no
  prior revision named where it comes from. Task 2 below resolves it explicitly:
  `useWorldEditorStore.getState().tree.find((b) => b.buildingId === entry.buildingId)`; if that lookup is
  `undefined` (the building is no longer in the disk-scanned tree — e.g. removed via some other path since the
  toast was shown), `handleUndo` fails closed exactly like the null-`resolveOverridePair()` case: logs a
  words-only reason, calls `setUndoError` (X4), and returns WITHOUT calling `restore()` or the add-back helper
  — the entry stays in `pending` for a retry.
- **X7 (MEDIUM, Opus):** the ROUND 5 acceptance criteria asked a test to assert "`suppressNextDiffRef` is set
  before `refreshTree()` is called" via a runtime spy-sequence — but the ref has zero OBSERVABLE runtime
  consequence within this plan's own wave (its consumer, Plan 14, lands in wave 5), so that assertion was
  unwritable as literally specified. Task 2's acceptance criteria below demote this ONE assertion to a static
  source-grep gate (matching this plan set's own `SWG_TOOLKIT_DECO_TRACE` grep-gate precedent, Plan 06) instead
  of a runtime spy — the REST of `handleUndo`'s behavior (resolve → restore → add-back → refresh ordering)
  remains fully runtime-tested.
- **X13 (LOW, Opus — documentation only, no behavior change):** Remove records a `recordPersistResult` history
  entry (Task 2's action below); Undo does not. This asymmetry is now LOAD-BEARING for Plan 14's V2 fix (Undo's
  refresh must be a SILENT re-baseline, not a history-growth event) **(SUPERSEDED by ROUND 7, Z6 below — this
  "LOAD-BEARING" framing is FALSE: V2's actual fix keys the "(NEW)" diff on the `tree` state value and content
  identity, NOT on `history.length` growth, so a future Undo history entry would NOT break Plan 14's suppress
  mechanism — read this X13 note as history of why the asymmetry was originally noticed, not as a live
  load-bearing constraint; see ROUND 7/Z6 below for the corrected rationale.)** — documented here explicitly:
  after an Undo,
  the Activity accordion's most recent entry for that row still reads whatever Remove last recorded (typically
  "removed — reload scene to see it gone"), even though the row is back on disk. This is a disclosed, accepted
  stale-accordion consequence (Opus verified a future Undo history entry would NOT break Plan 14's suppress
  mechanism, so this can be closed later without redesign) — recorded here and in Plan 15's gap ledger item (i),
  not silently left for a future reader to discover.

**ROUND 7 REVISION (2026-08-01, REVIEWS.md Z1/Z5/Z6/Z10/Z11/Z12/Z17):**
- **Z1 (HIGH, Opus Y1 + Sonnet L1, two independent angles, same seam):** ROUND 6's X4 must_have ("a failed
  Undo is NEVER invisible to the toast") was unsatisfiable as specified — see the frontmatter must_haves above
  for the full respecification. Two structural fixes replace the single `lastUndoError` scalar: (1) `handleUndo`
  now calls the add-back helper BEFORE `restore(entry.id)` — `restore()` only pops the entry from `pending`
  AFTER the add-back has actually succeeded, so a thrown add-back can never trigger the pending-diff effect's
  "entry left pending → show restored" branch (the false-success class Opus found is now structurally
  impossible, not merely guarded against). (2) `removeUndoStore.lastUndoError: {id,message}|null` becomes
  `undoErrors: Map<string, string>` (keyed by `RemovedRowEntry.id`); `setUndoError(id, message)` and
  `clearUndoError(id)` (now REQUIRING an id — the prior no-arg `clearUndoError()` form is REMOVED) touch ONLY
  that one id's slot, so Undo/Remove on one row can never erase a DIFFERENT row's still-unresolved failure
  signal (the cross-entry-erasure class Sonnet found). (3) Because this toast remains single-slot (matching
  `DeleteUndoToast`'s own precedent — a deliberate, disclosed limitation, not a defect), `RemoveUndoToast`'s
  append-diff branch additionally refuses to switch its displayed entry away from one that currently HAS an
  unresolved `undoErrors` entry and is STILL in `pending` — that "sticky" error toast persists until the user
  either dismisses it explicitly (✕, which also clears ONLY that entry's `undoErrors` slot, an explicit
  acknowledgement) or retries Undo on it (success removes it from `pending`; a repeat failure re-arms the SAME
  sticky display). This closes the "surviving affordance for stuck pending entries" ask without inventing a
  multi-toast stack this phase does not otherwise need.
- **Z5 (MEDIUM, Sonnet L2):** `suppressNextDiffRef` changes from a single nullable identity-key ref to a QUEUE
  (`useRef<Array<{ buildingId: string; cellName: string; objectTemplateName: string }>>([])`). `handleUndo`'s
  success path now PUSHES onto this array (`suppressNextDiffRef.current = [...suppressNextDiffRef.current,
  { buildingId, cellName, objectTemplateName }]`) instead of assigning a single value — a SECOND successful
  Undo before Plan 14's diff pass has drained the first Undo's entry can no longer clobber it; both entries
  survive in the queue until Plan 14's effect drains and pre-credits all of them (see Plan 14's own ROUND 7
  revision for the consumer-side half).
- **Z6 (MEDIUM, Opus Y3):** this plan's ROUND 6/X13 note previously called the Undo-history asymmetry
  "LOAD-BEARING for Plan 14's V2 fix (silent re-baseline)" — this framing was FALSE and is corrected here: V2's
  actual fix keys the "(NEW)" diff on the `tree` state value and content identity, NOT on `history.length`
  growth, and X3 (same commit) deleted the "skip the diff wholesale" behavior entirely — so a future Undo
  history entry would NOT break Plan 14's suppress mechanism in any way. The asymmetry (Remove records a
  `recordPersistResult` entry; Undo does not) is a COSMETIC completeness gap, free to fix later, not a
  load-bearing constraint. Additionally, per Opus's D-09-disclosure precedent (see Plan 15's gap ledger item
  (j)): D-06 defines history as "this session's persists/failures," and an Undo add-back IS a persist by that
  literal wording — recording no history entry for it is therefore a narrowing of D-06's own text, the SAME
  class of disclosure D-02's narrowing already required (Plan 15's gap ledger item (e)); this is now named
  explicitly here rather than left implicit.
- **Z10/Z11/Z12 (Cursor F2/F3/F4 — citation fixes, no behavior change):** `DeleteUndoToast.tsx:96-101` was cited
  as the presentational/subscribe precedent in several places below; `:96-101` is only the START of `handleUndo`
  (try + `restore()` + success comment) — the pending-diff SUBSCRIPTION this component's own structure mirrors
  is the `useEffect` at `:68-92`. The interfaces block's "toast shell at lines 118-218" is corrected to `:146-166`
  (the delete-variant shell; `:118` is only `if (toast.kind === 'none') return null;`, not a shell — the
  restored-variant shell is `:120-137`). `liveStore.ts:26-31` for the `ConnectionStatus` union is corrected to
  `:27-31` (`:26` is a comment line, not part of the union). Every citation below is corrected in place.
- **Z17 (Fable #14 note):** an untouched pre-existing line below previously said `DeleteUndoToast` "is mounted
  at its own panel's top level" — the real bytes (confirmed this session, `App.tsx:27` import / `:105` render)
  mount it GLOBALLY, contradicting this plan's own ROUND 6/X5 correction elsewhere. Corrected in place below.

**ROUND 8 REVISION (2026-08-01, plan-check blocker — the Z1 sticky-error guarantee's own mechanism could not
produce its stated acceptance criterion):**
- **Checker blocker:** ROUND 7's Z1 sticky-error fix specified `RemoveUndoToast` as a "near-verbatim structural
  copy" of `DeleteUndoToast.tsx`'s pending-diff effect — including that effect's UNCONDITIONAL trailing
  `prevRef.current = pending` (`DeleteUndoToast.tsx:91`) — while ALSO requiring the sticky guard to suppress a
  toast-switch when the displayed entry has a live `undoErrors` entry. Read literally, the guard suppressed the
  DISPLAY switch but the copied code still ran the trailing fold every render, so a removal (C) arriving while
  A's error was sticky got silently folded into `prevRef`'s "already seen" set before ever being shown — C could
  then never again be detected as newly `added`, on EITHER exit route (an explicit dismiss of A, or a
  successful retry of A's Undo): dismissing A only clears A's `undoErrors` slot and hides the toast, it never
  re-diffs `pending`; a successful retry pops A from `pending` but the diff, run against a `prevRef` that had
  already silently absorbed C, would show "A restored" and fold C in without ever surfacing it. Both routes
  contradicted this plan's own acceptance test, which asserts the toast "advances to C" after A is dismissed.
- **Fix (chosen: option (a), explicit next-to-show resolution — deviating from the near-verbatim-copy
  instruction at exactly one point, stated here rather than left implicit):** the sticky-suppression branch of
  the pending-diff effect returns EARLY and skips the trailing `prevRef.current = pending` fold entirely while
  the displayed entry remains sticky (unresolved `undoErrors` entry + still in `pending`) — this is the one
  deliberate divergence from `DeleteUndoToast.tsx`'s literal shape. The effect's dependency array is widened
  from `DeleteUndoToast.tsx`'s `[pending, armTimer]` to also include `undoErrors` (already a value this
  component subscribes to for the error-content swap, ROUND 6/X4) so that DISMISSING the sticky entry — which
  touches `undoErrors`, not `pending` — re-triggers the SAME diff logic the RETRY-success route already reaches
  via its own `pending` change; no separate imperative "scan pending" code is needed in the dismiss handler or
  in a distinct "retry-resolution" code path, because both routes converge on one guard becoming false (the
  entry is no longer sticky either because `undoErrors` no longer names it, or because it left `pending`) and
  re-run the SAME effect body. On the tick immediately following a sticky freeze (tracked by a boolean the
  effect sets while suppressing and consumes/resets on its next real run), if the still-frozen `prevRef`
  baseline reveals BOTH a resolved entry (removed from `pending`, e.g. a successful retry) AND an unseen queued
  entry (e.g. C, which arrived during the freeze and was never folded), the unseen queued entry's own
  "removed — Undo" toast takes priority over a bare "restored" confirmation for the just-resolved entry —
  closing the retry-resolution route the SAME way as the dismiss route, per the checker's explicit note that
  BOTH exit routes needed this treatment, not only dismiss. Outside of a sticky-affected sequence, this priority
  ordering is UNCHANGED from `DeleteUndoToast.tsx`'s own removed-before-added precedence — this reordering
  applies ONLY on the tick immediately following a sticky thaw, never to an ordinary tick with no sticky
  history.
- Every restatement of this contract inside this plan (must_haves, behavior, action, interfaces, threat_model,
  success_criteria, regression tests) is updated below to describe this corrected mechanism; a grep sweep of
  Plans 14/15 found no other restatement of the sticky-toast contract to update (both only reference
  `suppressNextDiffRef`/`undoErrors` factually, not the sticky-suppression algorithm itself).

**ROUND 9 REVISION (revision request — 1 blocker + 1 warning found reviewing ROUND 8's fix against
`DeleteUndoToast.tsx` ground truth):**
- **Blocker (sticky guarantee vs. the inherited 8-second auto-dismiss timer):** ROUND 8 fixed the pending-diff
  effect's own fold-deferral, but left ROUND 6/X4's original instruction — swap the toast's content to the
  stored message and re-arm the 8-second timer, mirroring `DeleteUndoToast.tsx`'s own catch-and-show-and-rearm
  behavior — unchanged. Ground truth: `DeleteUndoToast.tsx`'s `armTimer` (`:61-64`) unconditionally schedules
  `setToast({ kind: 'none' })` after `TOAST_MS`, with zero sticky-guard awareness, and its own `handleUndo`
  catch block (`:109`) calls `armTimer()` on every failed retry. Mirroring that literally means the sticky error
  toast this plan's Z1 guarantee depends on silently vanishes 8 seconds after it appears — a THIRD, undisclosed
  exit route, contradicting the must_have's "until the user either dismisses it explicitly or retries Undo on
  it" wording, and one that orphans the entry's `undoErrors` slot entirely (the timeout calls ONLY `setToast`,
  never `clearUndoError`) with no remaining UI surface to act on it.
  **Fix:** entering the sticky/error display state calls `clearTimer()` and does NOT call `armTimer()` — no
  timeout is EVER scheduled while a sticky error is displayed. This is a second, disclosed deviation from
  `DeleteUndoToast.tsx`'s literal shape (alongside ROUND 8's fold-deferral): the 8-second re-arm idiom applies
  ONLY to this toast's non-sticky success/informational states (a fresh "removed" toast, a "restored"
  confirmation); a sticky error toast has NO timer running until it stops being sticky (dismissed or
  successfully retried), making "until dismissed or retried" literally true rather than aspirationally true.
- **Warning (disclosed narrowing — multiple removals queued during one freeze):** if a SECOND AND THIRD removal
  (C, then D) both arrive while an earlier entry (A) is sticky, the thaw's diff computes `added = [C, D]` and,
  per ROUND 8, shows only the NEWEST (D) — the fold that runs immediately after permanently absorbs C into the
  baseline. C's `.ilf` removal already succeeded (the data-only guarantee is unaffected), but C's OWN undo-toast
  affordance is lost — it never gets its own turn to display. Rather than building a sequential-display queue
  (out of scope — this would balloon Task 2 well past a single toast's presentational design), this is a
  DISCLOSED narrowing, the SAME "EXCEPT the disclosed …" construction this plan set already uses elsewhere: when
  multiple removals arrive during one sticky freeze, only the MOST RECENT surfaces its own undo toast on thaw;
  earlier ones keep their persisted removal (never lost from disk) but must be manually re-added via the Add
  flow if the user wants them back — the SAME accepted-after-expiry framing Sonnet F11 already established for
  the ordinary 8-second-window case, just triggered by a freeze instead of a plain timeout.
- Every restatement of the re-arm behavior or the exit routes below (must_haves, behavior, action,
  acceptance_criteria, threat model T-05.1-13f, success_criteria, `<done>`) is updated to describe both fixes.
  A fake-timer regression test (entering sticky, advancing fake timers 8s+, asserting the toast and
  `undoErrors` entry both survive) and a disclosed-narrowing regression test (C then D queued during one
  freeze, thaw shows only D) are added to Task 2. A grep sweep of Plans 14/15 for "8-second"/"re-arm"/
  `TOAST_MS`/exit-route language re-confirmed no other restatement to update (same result as ROUND 8's sweep).
  **(ROUND 10, AA16 — LOW, process note, Fable FC-34 + Opus cross-cutting)** This sweep methodology — grepping
  MECHANISM keywords — is itself the process root cause of the half-propagation defects ROUND 10 fixed (AA3,
  AA9, and 4 consecutive rounds before it): a mechanism-keyword grep reads as "clean" even when sibling
  GUARANTEE-restatement sites (must_have enumerations, sketch_elements, gap ledgers, threat-model mitigations,
  success_criteria, acceptance blocks, `<done>` text) still assert the superseded absolute in different words.
  This replan's own edits were verified by re-reading and re-grepping each of THOSE site categories explicitly
  for every touched contract (see the replan's own return summary), not by re-running this same
  mechanism-keyword sweep — recorded here so a future round does not silently repeat ROUND 9's grep-empty
  false confidence.

**R9 REVISION (2026-08-01, REVIEWS.md round 9 — BB2/BB3/BB8/BB9/BB13/BB14/BB15/BB16/BB17/BB22; citation
convention: in-plan revision labels ('ROUND N REVISION') and REVIEWS-round numbers are DIFFERENT sequences —
two round-8-crew items in this plan were mislabeled 'REVIEWS.md ROUND 10' (no such REVIEWS round exists) and
one line credited reset() to 'ROUND 3' (BB17, the round-9 fact-check's only failures); all of these
mislabels are corrected below (R10 review, CC18(b): a prior draft said 'both' over this three-item
enumeration), and from this round on review items are cited as 'R9 review, BBn'):**
- **BB2 (HIGH):** the AA5 store-reset moves OUT of Plan 14's effect into each store's own module-scope
  workspaceStore subscription — Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT (reset-before-render). The
  `key={studioDir}` remount is UNCHANGED, but its lazy initializers now provably read post-reset store state
  (the reset runs synchronously inside the `studioDir`-changing `set()`, before React re-renders), so the
  child-initializers-before-parent-effect ordering can no longer produce the false 'A restored' toast in
  Project B.
- **BB3 (HIGH — a FIFTH exit route the AA2 mount seeding created, closed structurally, not disclosed):**
  AA2's step-0 seeded `prevRef` from the FULL current `pending` and `wasStickyRef` from `false` on every
  mount — so a remount DURING a sticky freeze folded every freeze arrival (e.g. C, deferred by the ROUND 8
  fold-deferral precisely so it could surface later) into the 'already seen' baseline unshown, and disarmed
  the thaw-priority rule; after the sticky entry resolved, C's undo affordance was gone — a SINGLE,
  MOST-RECENT arrival lost, outside the AA3 carve-out (which covers only NON-newest arrivals), falsifying
  this plan's own must_haves. Fix (step 0 below): when the mount scan finds a sticky entry, the initializers
  RECONSTRUCT the pre-unmount freeze state instead of blank-slating it — `prevRef` seeds to
  `pending.slice(0, pending.findIndex((e) => e.id === stickyEntry.id) + 1)` (id-keyed findIndex, R10
  review CC16 — everything up to and INCLUDING the sticky entry — exactly
  the baseline the fold-deferral had frozen, since no entry AFTER the sticky one can have been displayed,
  dismissed, or folded while the guard held: the guard blocks display switches, and only the displayed entry
  can be dismissed) and `wasStickyRef` seeds to `true` (re-arming the thaw priority). When NO sticky entry
  exists, the AA2 seed (`prevRef = pending`, `wasStickyRef = false`) applies unchanged — the plain-remount
  case (ledger item (g)) is unaffected.
- **BB22 (LOW):** the step-0 mount scan picks the LAST entry in `pending` whose id has a live `undoErrors`
  entry (deterministic under ANY state) and ASSERTS the at-most-one invariant (a `console.warn` naming every
  matching id if it ever breaks) instead of an unstated 'any entry' pick.
- **BB9 (MED — the one-synchronous-turn assumption removed, not hedged):** `handleUndo` no longer calls
  `clearUndoError(entry.id)` at the top of a retry. Failure branches call `setUndoError(entry.id, ...)` (an
  overwrite — no pre-clear needed for a fresh message); the success branch calls `restore(entry.id)` and
  THEN `clearUndoError(entry.id)` (settle-time cleanup). The sticky guard therefore stays armed for the
  WHOLE retry — there is no clear->re-set window for an interleaved commit to observe — so ROUND 10/AA14's
  'one synchronous turn, no intervening commit' assumption is no longer load-bearing (it was the only thing
  standing between a mid-retry commit and a premature thaw that could surface-and-fold C then show 'A
  restored' — the exact outcome the ROUND 8 guarantee asserts impossible).
- **BB8/BB14 (MED — D-03 hedge completeness):** the plain-remount narrowing (ledger item (g)) is now named
  in this plan's must_haves' EXCEPT clauses alongside AA3's, and the D-03 hedge is added at the three
  narrative sites that still stated the absolute: this objective's D-03 summary line, Task 2's `<done>`
  text, and `05.1-VALIDATION.md`'s 13-T2 row.
- **BB13 (MED):** the multi-queued-freeze accounting is corrected everywhere it is stated (must_have `:31`,
  the ROUND 9 warning block's restatements, `<done>`, Plan 15's ledger item (k)): one freeze with C and D
  queued strands TWO OR MORE entries with no remaining UI path — every non-newest arrival PLUS the sticky
  entry itself if it exits via dismiss (AA10) — not one.
- **BB15 (MED):** residual 'concurrent Undo' contract phrasing (must_have `:30`, success_criteria) is
  reworded to the AA8 framing: the queue's multi-entry capacity is a structural (grep-gated, append-only)
  guarantee; sequential Undos are the only UI-reachable case.
- **BB16 (MED):** Task 2 gains the unmount/remount test family the AA2 mechanism previously had ZERO
  coverage of (plain remount, sticky remount, the BB3 freeze-remount repro on both exit routes, the BB22
  determinism case, and the BB2 project-switch case), plus the BB9 premature-thaw regression test.
- **BB17 (LOW):** the 'ROUND 3 worldEditorStore.reset()' credit and the 'REVIEWS.md ROUND 10' citations are
  corrected (reset() is a round-8-crew AA5 addition; the in-plan label for that revision is ROUND 10).

Purpose: close SC4's REMOVE path end-to-end (data-only guaranteed — this is the delivered contract; live
despawn is implemented but not yet reachable, honestly stated per C4).
Output: `removeUndoStore.ts`, `RemoveUndoToast.tsx`, `decorationPersistOrchestrator.ts`'s `removeDecorationRow`,
and the World panel's per-row Remove action, fully tested.

Byte-exact format coverage note: this plan's `removeNode` calls (inside `assembleDecorationEdit(kind='remove')`,
Plan 01) are ALREADY proven byte-exact against both a byte-recipe fixture AND a REQUIRED real `.ilf` asset by
Plan 01 Task 4's revised CORE-05 registration (`packages/harness/test/ilf-roundtrip.test.ts`, C5-revised). This
plan's own tests exercise the ORCHESTRATION/store layer (removeUndoStore, the (currently-always-null)
live-despawn branch, the World-panel row action) — they do NOT re-prove ilf.ts's byte-exact format contract,
per the "inherit, don't duplicate" instruction; Task 1's tests may use lighter in-memory fixtures for
orchestration-level assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-live-world-editor-productization/05.1-RESEARCH.md
@.planning/phases/05.1-live-world-editor-productization/05.1-PATTERNS.md
@.planning/phases/05.1-live-world-editor-productization/05.1-01-SUMMARY.md
@.planning/phases/05.1-live-world-editor-productization/05.1-08-SUMMARY.md
@.planning/phases/05.1-live-world-editor-productization/05.1-11-SUMMARY.md
@.planning/phases/05.1-live-world-editor-productization/05.1-REVIEWS.md
</context>

<interfaces>
From packages/renderer/src/state/deleteUndoStore.ts (the FULL structural precedent — flat pending array +
push/restore, reuse the shape, scope it to decoration rows):
```typescript
export interface TrashEntry { id: string; /* ... */ }
export interface DeleteUndoStore { pending: TrashEntry[]; push: (entry: TrashEntry) => void; restore: (id: string) => void; }
```
From packages/renderer/src/panels/deploy/DeleteUndoToast.tsx (the full 218-line component — pending-diff
useEffect at lines 68-92, 8s auto-dismiss at lines 30/61-64, toast shell: the "delete"-variant shell (the one
this component's shape mirrors) is lines 146-166, the "restored"-variant shell is 120-137, `:118` is only
`if (toast.kind === 'none') return null;` — ROUND 7/Z11 citation fix — copy the structure, swap the copy for
decoration-removal wording):
```typescript
useEffect(() => {
  const prev = prevRef.current;
  const removed = prev.filter((e) => !new Set(pending.map((p) => p.id)).has(e.id));
  const added = pending.filter((e) => !new Set(prev.map((p) => p.id)).has(e.id));
  // removed.length > 0 → show "restored" toast; added.length > 0 → show "deleted"/"removed" toast + Undo button
  prevRef.current = pending;
}, [pending]);
```
**ROUND 8 correction — the ONE point `RemoveUndoToast` deviates from this exact shape:** the quoted effect's
trailing `prevRef.current = pending` (`:91`) runs UNCONDITIONALLY in `DeleteUndoToast.tsx` — `RemoveUndoToast`'s
copy of this effect does NOT run that line while the displayed entry is sticky (see this plan's ROUND 8
revision above and Task 2's behavior/action spec below for the exact guard and the deferred-fold mechanism this
requires, and for why the effect's own dependency array must widen to include `undoErrors`, not only `pending`).

From packages/renderer/src/services/decorationPersist.ts (Plan 01 output, kind-aware assembly):
```typescript
export function assembleDecorationEdit(edit: DecorationEdit & { kind?: 'edit' | 'add' | 'remove' }, deps: DecorationPersistDeps): DecorationPersistResult;
```
From packages/renderer/src/services/hostCommand.ts (Plan 08 output):
```typescript
export function sendDespawnNode(mappingName: string, networkId: string): void;
```
From packages/renderer/src/state/worldEditorStore.ts (Plan 04 output — sessionOverlay carries NO node-id
association, per C4's finding; do not assume otherwise):
```typescript
export function worldEditorRowId(buildingId: string, cellName: string, rowIndex: number): string;
// sessionOverlay: Map<rowId, 'armed' | 'saved' | 'failed'> — NO live-node-id field exists on this map or
// anywhere else in this phase's delivered store shape (C4 finding, confirmed against Plan 04's real
// interface — a forward output this Wave-4 task depends on by wave ordering (Plan 04 = wave 0), not yet
// present in the repo as of this planning round — ROUND 7 seam-sweep, Z4's class).
```
From packages/renderer/src/services/worldEditorScan.ts (Plan 04 ROUND 3 output — the resolved scan root helper
and the widened building shape, R3/R4):
```typescript
export interface WorldEditorBuilding { buildingId: string; displayLabel: string; editedIlfPath: string; derivedTemplatePath: string; buildingTemplateVfsPath: string; decorations: { cellName: string; rowIndex: number; objectTemplateName: string; transform: number[] }[]; }
export function resolveScanRoot(clientExe: string | null, offlineBinding: { cfgPath?: string; clientPath: string | null } | null): string | null;
```
From packages/renderer/src/services/decorationPersistOrchestrator.ts (Plan 06 ROUND 3 output — now-exported
readVfs factory, R4):
```typescript
export function makeReadVfs(overrideDir: string): (vfsPath: string) => Buffer;
```
From packages/renderer/src/panels/world/WorldPanel.tsx (Plan 10 ROUND 4/ROUND 5 output — the shared,
null-safe override-resolution helper this plan's Remove AND Undo handlers MUST reuse, never re-resolving
inline; this is a component-local function, not a module export — Plan 13 extends the SAME file Plan 10
defines it in; ROUND 5, V6 — the return type now ALSO carries the confirmed-non-null `studioDir`):
```typescript
function resolveOverridePair(): { overrideDir: string; readVfs: (vfsPath: string) => Buffer; meta: WorkspaceBindingMeta; studioDir: string } | null;
function refreshTree(): void;
```
This plan's own Task 2 declares (ROUND 6, X3 — identity-keyed, not a boolean; ROUND 7, Z5 — a QUEUE, not a
single nullable ref; consumed by Plan 14, later wave, same file):
```typescript
const suppressNextDiffRef = useRef<Array<{ buildingId: string; cellName: string; objectTemplateName: string }>>([]);
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: removeUndoStore.ts + removeDecorationRow (data-only remove, ALWAYS; live-despawn mechanism kept but unreachable, C4)</name>
  <files>packages/renderer/src/state/removeUndoStore.ts, packages/renderer/src/state/removeUndoStore.test.ts, packages/renderer/src/services/decorationPersistOrchestrator.ts, packages/renderer/src/services/decorationPersistOrchestrator.test.ts</files>
  <read_first>
    packages/renderer/src/state/deleteUndoStore.ts (full file — the pending-array + push/restore shape)
    packages/renderer/src/services/decorationPersist.ts (assembleDecorationEdit's kind='add'/'remove'
    branches, Plan 01)
    packages/harness/test/ilf-roundtrip.test.ts (Plan 01 Task 4, C5-revised — the CORE-05 standing-gate
    registration for `.ilf`; addNode/removeNode are ALREADY byte-exact-proven there at the format level,
    against BOTH a byte-recipe AND a REQUIRED real-asset fixture — this plan does not need to re-prove that;
    skim only to confirm the contract this plan's removeNode calls rely on)
    packages/renderer/src/services/decorationPersistOrchestrator.ts (full file — the mirrorToStockIlf
    resolution + stageDurable helpers this new function reuses, Plan 06's reconcileMirrorMode as the sibling
    "full-project-settings-aware" function to structurally match)
    packages/renderer/src/services/hostCommand.ts (sendDespawnNode)
    packages/renderer/src/state/worldEditorStore.ts (sessionOverlay shape, Plan 04 — CONFIRM it carries no
    node-id field, per C4's finding, before writing this task's tests)
  </read_first>
  <behavior>
    - `RemovedRowEntry` (removeUndoStore's pending-entry shape): `{ id: string (worldEditorRowId), buildingId:
      string, cellName: string, rowIndex: number, removedNode: IlfNode (the exact node removed, so Undo can
      re-add it byte-identical) }`.
    - `useRemoveUndoStore`: `pending: RemovedRowEntry[]`, `push(entry)`, `restore(id): RemovedRowEntry |
      undefined` (returns the entry; per ROUND 7/Z1 below, the CALLER now invokes `restore(id)` only AFTER the
      add-back helper has already succeeded — `restore(id)`'s job is to formally pop the entry from `pending`
      once the restoration is CONFIRMED, not to hand the caller data it needs to attempt one, since the caller
      already holds `entry` from the toast's `onUndo` argument). **(ROUND 6, X4 — HIGH, Sonnet; ROUND 7, Z1 —
      HIGH, Opus/Sonnet, respecifying the error-display contract into a per-entry-keyed form)** ALSO:
      `undoErrors: Map<string, string>` (starts empty, keyed by `RemovedRowEntry.id`), `setUndoError(id: string,
      message: string): void` (returns/sets a NEW Map with `id → message` set, preserving every OTHER key
      untouched — never a global overwrite), `clearUndoError(id: string): void` (REQUIRES an id — there is no
      no-argument overload; returns/sets a NEW Map with ONLY `id`'s entry removed, preserving every OTHER key's
      entry untouched) — this is the observable, PER-ENTRY failure signal Task 2's `RemoveUndoToast` watches so
      a failed Undo is never silently invisible AND a DIFFERENT entry's Undo attempt can never erase THIS
      entry's stored failure (closing X4 and Z1's cross-entry-erasure finding; see Task 2's behavior spec for
      the consumer). Both actions MUST construct a new `Map` instance (never mutate the existing one in place)
      so a selector subscribed to `undoErrors` re-renders on change.
    - **(ROUND 10, AA5 — HIGH, Sonnet L2; re-derived R9 review, BB2)** `reset(): void` — clears `pending` to
      `[]` and `undoErrors` to a new empty `Map`, restoring the store to its module-initial state. This exists
      SOLELY for a project switch: `removeUndoStore` is a module-scope singleton that outlives any single
      project's session in the renderer process, so switching projects without clearing it would leak Project
      A's stuck removal(s)/sticky-error toast into Project B's session (verified against
      `packages/renderer/src/state/workspaceStore.ts:100-109`'s real `close()` implementation, which touches
      neither this store nor `worldEditorStore`). Per Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT (the
      canonical statement — cite, don't paraphrase), the CALLER is this store's OWN module-scope subscription,
      registered at the bottom of removeUndoStore.ts: `useWorkspaceStore.subscribe((state, prevState) => { if
      (prevState.studioDir !== null && state.studioDir !== prevState.studioDir)
      useRemoveUndoStore.getState().reset(); });` — zustand v5's vanilla `subscribe` runs this SYNCHRONOUSLY
      inside the `studioDir`-changing `set()` call (`openComplete`, `workspaceStore.ts:79-86`; `close`,
      `:100-109`), strictly before React re-renders, so `RemoveUndoToast`'s `key={studioDir}` remount
      initializers (Task 2, step 0) always read POST-reset state — the false-'restored'-on-switch race (R9
      review, BB2) is structurally impossible. Plan 04's `worldEditorStore` registers the identical
      subscription for its own six fields (its round-8-crew AA5 addition, in-plan ROUND 10 — the prior 'ROUND
      3' credit here was BB17's mislabel); Plan 14's WorldPanel no longer calls either store's reset().
      **(R10 review, CC9 — identical lifecycle hygiene to Plan 04's canonical contract, cite don't
      re-derive):** keep the unsubscribe in a module const (`const unsubscribeProjectSwitchReset =
      useWorkspaceStore.subscribe(...)`), follow it with the HMR dispose block (`if (import.meta.hot) {
      import.meta.hot.dispose(() => unsubscribeProjectSwitchReset()); }`), and apply the SAME test-isolation
      recipe as a comment beside it and in this file's tests: seed `useWorkspaceStore`'s `studioDir` FIRST
      in any `beforeEach`, THEN seed this store's `pending`/`undoErrors` — a non-null->different studioDir
      seed now fires a REAL synchronous reset, harmless only under that ordering. Plan 04 Task 2's CC9/CC15
      block is the canonical statement of the recipe and the two ordering premises; this store applies it
      identically.
    - **(ROUND 3, R3/R7 — revised signature)** `removeDecorationRow(studioDir: string, overrideDir: string,
      readVfs, building: WorldEditorBuilding, cellName: string, rowIndex: number, liveNetworkId: string | null,
      mappingName: string | null): DecorationPersistResult` — resolves `edit.cellName`/`decorationTemplateName`/
      `originalO2p` from the target row (looked up from `building.decorations` by cellName+rowIndex — the
      CALLER, WorldPanel.tsx, already has this from its seeded tree). **(ROUND 3, R3)** BEFORE calling
      `assembleDecorationEdit`, checks `building.buildingTemplateVfsPath` — if it is the empty string (Plan 04
      ROUND 3's documented "unknown" sentinel — this project/building has never observed a live capture for it
      under the new durable-map mechanism), throws a clear, words-only error: `removeDecorationRow: this
      building's stock template path isn't known yet — hover and arm/persist any decoration in it once first,
      then Remove will work` (fail closed, never passes an empty string into `assembleDecorationEdit`, which
      would otherwise throw a much less friendly VFS-resolution error deep inside `readVfs`). Otherwise, calls
      `assembleDecorationEdit({ ...edit, buildingTemplateVfsPath: building.buildingTemplateVfsPath, kind:
      'remove' }, { readVfs, overrideDir, log, mirrorToStockIlf: readWorkspaceJson(studioDir).mirrorToStockIlf
      ?? true })` (same per-project mirror resolution Plan 06 already established — reuse it, do not hard-code
      true here). If `liveNetworkId` is non-null, ALSO calls `sendDespawnNode(mappingName, liveNetworkId)`
      (ROUND 3, R7 — `mappingName` is now a REAL declared parameter, not a free variable; if `mappingName` is
      null while `liveNetworkId` is somehow non-null, skip the despawn call and log a warning rather than
      calling `sendDespawnNode` with a null mapping name) — this is a best-effort call (wrap in try/catch; a
      failed despawn does not block the data removal, which has already succeeded by this point — matches D-02's
      "stock file never touched, removal inherently reversible" framing: the DATA removal is the durable
      contract, live despawn is an opportunistic bonus). **(C4)** As of this phase, `liveNetworkId` is ALWAYS
      passed as `null` by every caller in this codebase (WorldPanel.tsx, Task 2 below) — the function correctly
      implements the non-null branch and it is unit-tested here in isolation (proving the MECHANISM works),
      but it has zero live callers this phase ships. This is groundwork for a future plan, not a delivered
      end-to-end capability — state this in the test file's own top comment, not just in prose elsewhere.
    - An `addBackDecorationRow` sibling function (or reusing assembleDecorationEdit directly with kind='add')
      re-adds a previously-removed node via the SAME add path Plan 01/06 already exercise for ADD — Undo is
      NOT a bespoke code path. **(ROUND-3-REVIEW R7, MEDIUM, Codex+Sonnet — accepted, disclosed consequence,
      matching Plan 01's own R7 disclosure):** because `addNode` is strictly append-only (Plan 01), Undo
      re-adds the removed row at the END of its cell, not at its original position — this REORDERS the cell
      relative to where the row sat before Remove, shifting the effective rowIndex of every OTHER row in that
      cell that sits between the old and new position. This is an ACCEPTED tradeoff of D-01's "one code path
      for add and edit/undo" mandate (an insert-at-cell-row helper would be a second mutation path, which D-01
      explicitly rejects) — Undo restores the DECORATION (same template, same transform, same cell), not its
      exact numeric row position. Task 2's toast copy and this plan's `must_haves` disclose this scoping
      explicitly (see the Sonnet F11 8-second-window note above); Plan 15's sign-off ledger records it as a
      known, accepted characteristic of Undo, not a defect to fix later.
  </behavior>
  <action>
    Create removeUndoStore.ts mirroring deleteUndoStore.ts's `create<T>()` shape (scoped to decoration rows,
    NOT projects — do not import or extend deleteUndoStore.ts itself, this is a sibling store per PATTERNS.md),
    including the `undoErrors`/`setUndoError`/`clearUndoError` slice (ROUND 6, X4; ROUND 7, Z1 — per-entry keyed).
    Add `removeDecorationRow` (per its ROUND 3/R3/R7-revised signature above) and the re-add helper to
    decorationPersistOrchestrator.ts, reusing assembleDecorationEdit/readWorkspaceJson/hostCommand.sendDespawnNode
    exactly as specified. Write tests for both: removeUndoStore's push/restore round-trips a RemovedRowEntry
    exactly; `setUndoError(id, message)` sets `undoErrors.get(id)` to that exact `message` string WITHOUT
    disturbing any other id's entry, and `clearUndoError(id)` removes ONLY that id's entry from `undoErrors`
    WITHOUT disturbing any other id's entry (ROUND 6/X4; ROUND 7/Z1 — per-entry keyed, never a global clear);
    removeDecorationRow with liveNetworkId=null (THE ONLY VALUE ANY CURRENT CALLER EVER PASSES, per C4)
    performs the data removal and does NOT call sendDespawnNode (assert via spy); with a non-null liveNetworkId
    AND a non-null mappingName passed DIRECTLY IN A TEST (proving the mechanism works in isolation even though
    no current caller exercises it), it performs the removal AND calls sendDespawnNode with that exact
    (mappingName, liveNetworkId) pair; a non-null liveNetworkId with a NULL mappingName skips the despawn call
    and logs a warning instead of calling sendDespawnNode with a null mapping name (ROUND 3/R7); a
    sendDespawnNode failure (mocked to throw) does not prevent the function from returning its
    DecorationPersistResult (the data removal already committed); a `building` whose `buildingTemplateVfsPath`
    is `''` causes `removeDecorationRow` to throw the words-only "stock template path isn't known yet" error
    BEFORE calling `assembleDecorationEdit` (asserted via a spy showing `assembleDecorationEdit` was never
    called — ROUND 3/R3). **(ROUND 10, AA5; R9 review, BB2)** Also add `reset(): void` to `removeUndoStore.ts` (clears `pending`
    to `[]` and `undoErrors` to a new empty `Map`) and register the module-scope workspaceStore subscription
    per Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT (import `useWorkspaceStore` from `./workspaceStore`
    — one-directional, no cycle). Test both: seed both fields non-empty, call `reset()`, assert both are
    empty; and seed both non-empty, change `useWorkspaceStore`'s `studioDir` from one non-null value to
    another, assert this store is back to initial SYNCHRONOUSLY (immediately after the call returns, no
    render or effect flush needed); assert the initial null->'a' transition does NOT reset.
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- removeUndoStore decorationPersistOrchestrator</automated>
  </verify>
  <acceptance_criteria>
    Tests assert: data-only remove path never calls sendDespawnNode; a DIRECTLY-passed non-null liveNetworkId +
    mappingName (test-only, proving the mechanism) calls sendDespawnNode with the exact (mappingName,
    liveNetworkId) pair (ROUND 3/R7); a despawn failure is swallowed and logged, never thrown to the caller; a
    non-null liveNetworkId with a null mappingName never calls sendDespawnNode (ROUND 3/R7); removeUndoStore's
    restore() returns the exact removed IlfNode for re-adding; an empty `buildingTemplateVfsPath` throws BEFORE
    any `assembleDecorationEdit`/`readVfs` call, with a words-only message (ROUND 3/R3). removeUndoStore's
    `setUndoError`/`clearUndoError` are per-entry keyed: seeding TWO ids' errors via `setUndoError`, then calling
    `clearUndoError` on ONE id, asserts that id's entry is gone from `undoErrors` while the OTHER id's entry is
    UNCHANGED (ROUND 6/X4; ROUND 7/Z1 — the cross-entry-erasure regression test). **(ROUND 10, AA5; R9
    review, BB2)** `reset()` clears `pending` to `[]` and `undoErrors` to a new empty
    `Map` in one call (asserted by seeding both non-empty, calling `reset()`, and checking both are empty
    afterward); the module-scope subscription resets this store SYNCHRONOUSLY on a non-null->different
    `studioDir` change and NOT on the initial null->project open — the store-level half of the
    PROJECT-SWITCH RESET ORDERING CONTRACT (Plan 04 owns the canonical statement; Plan 14's Task 3 covers
    the component-local half).
  </acceptance_criteria>
  <done>removeDecorationRow implements D-02's data-only-guaranteed contract and D-04's live-despawn MECHANISM
  (unit-proven, currently unreachable by any real caller — C4's honest characterization, not a fabricated
  "opportunistic" claim); removeUndoStore holds the undo-eligible entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RemoveUndoToast.tsx (presentational) + World panel row Remove/Undo actions (data-only, always; ROUND 5: WorldPanel owns the whole Undo restore via resolveOverridePair(); ROUND 8: sticky-guard fold-deferral fix)</name>
  <files>packages/renderer/src/panels/world/RemoveUndoToast.tsx, packages/renderer/src/panels/world/RemoveUndoToast.test.tsx, packages/renderer/src/panels/world/WorldPanel.tsx, packages/renderer/src/panels/world/WorldPanel.test.tsx</files>
  <read_first>
    packages/renderer/src/panels/deploy/DeleteUndoToast.tsx (full file — **(ROUND 6, X5 — factual
    correction)** DeleteUndoToast takes ZERO props (`:48`) and is mounted globally in App.tsx; its OWN
    `handleUndo` (lines 96-111) calls `useDeleteUndoStore.getState().restore(entry.id)` DIRECTLY on the store
    — it does NOT delegate to an owner-passed prop. The precedent this ROUND 5 revision actually restores is
    narrower and more honest: DeleteUndoToast never resolves any WRITE-PATH dependency of its own (no
    `overrideDir`/`readVfs`/`studioDir`/`building`) — RemoveUndoToast matches that half exactly, but goes ONE
    STEP FURTHER, since ITS restore needs a resolved write path only `WorldPanel.tsx` has: it delegates the
    ENTIRE restore, not just the write path, via the `onUndo` prop. Corrected citations: pending-diff effect
    `:68-92`; 8s timer `:30`/`:61-64`; toast shell (delete variant) `:146-166`; `:96-101` is `handleUndo`
    specifically, which ALSO shows the catch-and-show-error pattern Task 2's `undoErrors` mechanism below
    PARTIALLY mirrors for X4 (the show-error half only — ROUND 9 explicitly does NOT mirror this same
    `handleUndo`'s `armTimer()` call at `:109`), even though — unlike DeleteUndoToast — the actual write
    attempt here happens in a DIFFERENT component. **(ROUND 8)** — note also, and do NOT copy verbatim: `:91`'s
    UNCONDITIONAL trailing `prevRef.current = pending` is the ONE line this component's copy must deviate from
    while its displayed entry is sticky; see below. **(ROUND 9)** — a SECOND deviation, also do NOT copy
    verbatim: `:61-64`'s `armTimer` (and `:109`'s unconditional call to it from `handleUndo`'s catch block) must
    NOT be mirrored while the sticky error is displayed; see below.)
    packages/renderer/src/state/removeUndoStore.ts (this plan's Task 1 output — restore(id) pops and returns
    the entry)
    packages/renderer/src/panels/world/WorldPanel.tsx (Plan 10's `resolveOverridePair()` — a forward
    output this Wave-4 task depends on by wave ordering (Plan 10 = wave 2, strictly earlier), not yet
    present in the repo as of this planning round, ROUND 7 seam-sweep Z4's class
    (ROUND 5/V6 — now ALSO resolves and returns `studioDir`) and `refreshTree()` — this task's Remove AND
    Undo handlers BOTH call `resolveOverridePair()`; ROUND 5, V1 — do not resolve overrideDir/readVfs/studioDir
    a second, independent way, and do not let RemoveUndoToast.tsx resolve them at all)
    packages/renderer/src/state/liveStore.ts (lines 27-31 — the `ConnectionStatus` union; `:26` is a comment
    line, not part of the union — ROUND 7/Z12 citation fix; `mappingName` exists ONLY on the `{ kind: 'attached'
    }` member — ROUND 5, V5)
  </read_first>
  <behavior>
    - **(ROUND 5, V1 — HIGH, Codex/Cursor/Opus, three independent angles)** RemoveUndoToast.tsx is PURELY
      presentational, matching `DeleteUndoToast.tsx:48`'s zero-props shape and `:68-92`'s pending-diff
      subscription structure exactly (ROUND 6/X5; ROUND 10/AA6 citation fix — `:96-101` is `handleUndo`, calling
      `restore()` directly on the store, NOT the subscription this line means to cite): it subscribes to
      `useRemoveUndoStore((s) => s.pending)`, diffs prev-vs-current each render (the SAME append=new-removal /
      removal=undo detection `DeleteUndoToast.tsx` already implements) to decide WHEN to show the "removed —
      Undo" toast vs. the brief "restored" confirmation, and shows an 8-second auto-dismissing toast on a new
      removal. It accepts a new REQUIRED prop `onUndo: (entry: RemovedRowEntry) => void` — the Undo button's
      click handler reads the CURRENT entry from `pending` (by the clicked toast's `id`) and calls
      `onUndo(entry)` — it does NOT call `restore(id)` itself, does NOT resolve
      `overrideDir`/`readVfs`/`studioDir`, and does NOT call any add-back helper. `RemoveUndoToast` has ZERO
      knowledge of `resolveOverridePair()`, `WorldEditorBuilding`, or the write path — exactly like
      `DeleteUndoToast.tsx` never resolves its own dependencies. This closes V1: the previous design gave
      `RemoveUndoToast` (a sibling file to `WorldPanel.tsx`) an inline add-back that needed
      `studioDir`/`overrideDir`/`readVfs`/`building`, but `resolveOverridePair()` is component-local to
      `WorldPanel.tsx` and unreachable from a sibling file — an executor following the old text would have had
      to invent a SECOND, unguarded resolution path, exactly the class of bug W3 closed for Remove. The
      "restored" confirmation toast is shown reactively when `pending` shrinks (i.e. once `WorldPanel.tsx`'s
      `onUndo` handler below actually calls `restore(entry.id)`, per the SAME pending-diff detection this
      component already runs for the append/remove case) — this component never needs to know WHY an entry
      left `pending`, only THAT it did.
    - **(ROUND 5, V1; ROUND 6, X3/X4/X6; ROUND 7, Z1/Z5 — HIGH, Opus/Sonnet, reordering + per-entry keying)**
      `WorldPanel.tsx` owns the ENTIRE Undo restore end-to-end, mirroring the SAME resolve-then-write discipline
      the Remove click handler (below) already follows: `onUndo(entry)` does NOT clear `undoErrors` up front (**R9 review, BB9** — the superseded clear-at-top
      design opened a clear->re-set window in which an interleaved React commit could observe the sticky
      guard transiently OFF, prematurely thaw, surface-and-fold a queued entry, then show a false 'restored';
      it survived only via React's automatic batching, an assumption ROUND 10/AA14 had to disclose as
      load-bearing. With no up-front clear, the sticky guard stays armed for the WHOLE retry and that
      assumption is no longer needed: failure branches OVERWRITE this entry's slot via `setUndoError(entry.id,
      ...)`, which needs no pre-clear, and the success branch clears the slot only AFTER the outcome is
      settled, below — still id-scoped, preserving Z1's cross-entry guarantee). **(ROUND 6, X6)**
      It THEN resolves the target building via `useWorldEditorStore.getState().tree.find((b) => b.buildingId ===
      entry.buildingId)`; if that lookup is `undefined` (the building fell out of the disk-scanned tree since
      the toast appeared), this is a WORDS-ONLY no-op exactly like the null-`resolveOverridePair()` case below:
      log the reason via `log()`, call `useRemoveUndoStore.getState().setUndoError(entry.id, <words-only reason
      naming the missing building>)` (ROUND 6, X4), and return WITHOUT calling `resolveOverridePair()`,
      the add-back helper, or `restore(entry.id)` — the entry stays in `pending`. Otherwise `onUndo` calls
      `resolveOverridePair()` (Plan 10's shared, null-safe helper — ROUND 5/V6 now also returns `studioDir`). If
      it returns `null` (no resolvable scan root — the SAME words-only disabled reasoning Remove already
      surfaces), this is ALSO a WORDS-ONLY no-op: log the reason via `log()`, **(ROUND 6, X4)** ALSO call
      `setUndoError(entry.id, <the same words-only reason>)`, and return WITHOUT calling the add-back helper or
      `restore(entry.id)` — the entry stays in `removeUndoStore.pending` exactly as it was, so the toast remains
      visible, now SHOWING the failure reason with NO dismiss timer running (ROUND 9 — the toast's own timer stays cleared while sticky; only the sticky state itself, never a re-armed timeout, keeps it visible) (X4 closes the previous silent-no-op gap).
      **(ROUND 7, Z1 — HIGH, the false-success fix)** Only once BOTH the building lookup and
      `resolveOverridePair()` succeed does `onUndo` call Task 1's add-back helper FIRST — with the FRESH
      `pair.studioDir`/`pair.overrideDir`/`pair.readVfs`, the resolved `building`, and `entry.removedNode`
      (already in scope from the `onUndo(entry)` argument — NOT from `restore()`'s return value) — and calls
      `restore(entry.id)` (popping the entry from `pending`) ONLY AFTER the add-back helper RETURNS
      SUCCESSFULLY, then `clearUndoError(entry.id)` (**R9 review, BB9** — settle-time cleanup of this
      entry's now-moot error slot, still scoped to this id only). This ordering is the root-cause fix for Z1's false-success finding: because the entry is
      NEVER removed from `pending` until restoration is CONFIRMED, a THROWING add-back can never trigger the
      pending-diff effect's "entry left pending → show restored" branch — the false "restored" toast Opus found
      is now structurally impossible on this branch, not merely made less likely. If the add-back helper THROWS
      (the entry is STILL in `pending` at this point, per the reordering above — it was never popped), catch it,
      surface the honest error via `log()`, **(ROUND 6, X4)** ALSO call `setUndoError(entry.id, <the caught
      error's message>)`, and do NOT call `restore(entry.id)` or `refreshTree()` — the entry remains safely in
      `pending` for a retry (a genuine improvement over the prior "already popped, unrecoverable" framing: since
      `restore()` is now called only after success, a failed add-back never loses the entry from `pending` at
      all). On SUCCESS: `restore(entry.id)` pops the entry, THEN **(ROUND 5, V2 groundwork — see Plan 14's own
      ROUND 5 revision; ROUND 6, X3 — scoped to content identity; ROUND 7, Z5 — MEDIUM, Sonnet, queue not a
      single ref)** PUSH `{ buildingId: entry.buildingId, cellName: entry.cellName, objectTemplateName:
      entry.removedNode.objectTemplateName }` onto the shared `suppressNextDiffRef` QUEUE:
      `suppressNextDiffRef.current = [...suppressNextDiffRef.current, { buildingId, cellName, objectTemplateName
      }]` (a `useRef<Array<{ buildingId: string; cellName: string; objectTemplateName: string }>>([])` declared
      in this component's body, co-located with `resolveOverridePair()`/`refreshTree()` — forward-looking
      groundwork this plan adds FOR Plan 14's "(NEW)" diff effect, which Plan 14 (later wave, same file) drains
      and clears; ROUND 7/Z5 — a QUEUE means a SECOND successful Undo before Plan 14's diff pass drains the
      first entry can never clobber it, both survive to be pre-credited) BEFORE calling the panel's shared
      `refreshTree()` — an Undo-restored row is a RESTORE, never a fresh placement, and Plan 14's
      content-identity diff must never mark IT "(NEW)" even on the very next refresh, while still correctly
      diffing and marking any OTHER, unrelated row that landed new in the same window (ROUND 6/X3 — the previous
      boolean form would have silently swallowed that unrelated row too). Undo re-adds the removed row via the
      append-only add path (D-01's one-code-path mandate), which reorders the cell relative to the row's
      original position — an accepted, disclosed consequence, not a defect (ROUND-3-REVIEW R7, unchanged by
      this revision).
    - **(ROUND 7, Z1; ROUND 8 — mechanism fix, checker blocker; ROUND 10 — AA1/AA2, re-deriving the guard as ONE
      branch that also performs the swap, and fixing panel-remount mount state)** `RemoveUndoToast`'s
      pending-diff effect (the append/removed detection it already runs, matching `DeleteUndoToast.tsx:68-92`'s
      structure) gains a sticky guard-and-swap branch AND a deviation from that structure's own trailing line,
      in this exact order:
      0. **(ROUND 10, AA2 — HIGH, Opus Y2)** On MOUNT, `toast`/`prevRef`/`wasStickyRef` are NOT unconditionally
         initialized to `{ kind: 'none' }`/`[]`/`false` the way a fresh, globally-mounted `DeleteUndoToast` could
         safely be — because this component is DELIBERATELY panel-scoped (mounted/unmounted with
         `WorldPanel.tsx`, ROUND 6/X5/Z17), it can remount mid-session (a tab navigation away and back) while
         `useRemoveUndoStore`'s `pending`/`undoErrors` (module-scope, session-durable) survive underneath it. A
         naive blank-slate mount default would diff the FULL current `pending` against an empty baseline on the
         very first post-mount run and could (a) show whichever entry is "newest" even if it is stale/unrelated,
         and, worse, (b) if the entry the toast WAS displaying before unmount has a live `undoErrors` entry (a
         sticky failure the user never resolved), silently supersede it with a DIFFERENT entry's plain "removed"
         toast — the exact forbidden wording (this plan's must_haves) on a FOURTH exit route (remount) the
         ROUND 9 enumeration (timer/dismiss/retry) never covered. Fix: `toast`'s initial value is COMPUTED from
         the store's CURRENT truth at mount time (a lazy initializer, not a post-mount effect): scan
         `useRemoveUndoStore.getState().pending` for entries whose id has a live entry in `undoErrors` —
         **(R9 review, BB22)** picking the LAST such entry in `pending`'s array order if more than one exists
         (deterministic under ANY state; the at-most-one invariant expected under this component's single-slot
         design is ASSERTED, not assumed: if the scan finds two or more, emit a `console.warn` naming every
         matching id, then proceed with the last). If found, the initial `toast` is
         `{ kind: 'error', entryId: that id, message: undoErrors.get(that id) }` (the sticky display is RESTORED
         across the remount, never lost), and **(R9 review, BB3 — the freeze-state reconstruction closing the
         fifth exit route)** `prevRef`'s initial value is `pending.slice(0, pending.findIndex((e) => e.id === thatId) + 1)` (R10
         review, CC16: `findIndex` by id, never `indexOf(that entry)` reference identity — the scanned entry
         does come from `pending` itself today, so reference identity happens to hold, but id-keying is the
         drift-proof form in this otherwise id-keyed design) —
         everything up to and INCLUDING the sticky entry, NOT the full `pending` array — reconstructing exactly
         the baseline the ROUND 8 fold-deferral had frozen before the unmount (no entry AFTER the sticky one
         can have been displayed, dismissed, or folded while the guard held: the guard blocks display switches,
         and only the displayed entry can be dismissed — so array positions after the sticky entry are
         precisely the freeze arrivals the fold-deferral was protecting), and `wasStickyRef` initializes to
         `true` (re-arming the thaw-priority rule for the eventual dismiss/retry, exactly as if the unmount
         never happened — the superseded blanket `prevRef = pending` + `wasStickyRef = false` seed folded every
         freeze arrival (C) in unshown AND disarmed the thaw priority, so resolving the sticky entry after such
         a remount produced an EMPTY diff and permanently lost C's undo affordance: a single, MOST-RECENT
         arrival, outside the AA3 carve-out). Otherwise (no sticky entry) the initial `toast` is `{ kind:
         'none' }` (no stale "removed"/"restored" toast is invented purely from a remount — closing Plan 15
         gap ledger item (g)'s "may or may not re-show" ambiguity into a deterministic "does not re-show
         unless sticky"), `prevRef`'s initial value is seeded from the FULL current `pending` at mount (never
         `[]`), so the first post-mount diff run sees `removed = []`/`added = []` — nothing looks newly
         appeared or newly resolved purely because the COMPONENT remounted; only a genuinely NEW removal or
         resolution occurring AFTER this mount will ever surface — and `wasStickyRef` initializes to `false`. This closes AA2 without hoisting any
         state into `worldEditorStore` (deferred, matching ROUND 6/X8's own precedent) — the fix is entirely a
         smarter LOCAL initializer reading the module-scope store's CURRENT truth once at mount, never a blank
         assumption.
      1. **(ROUND 10, AA1 — HIGH, Opus Y1)** On every run, it first checks whether the CURRENTLY-DISPLAYED entry
         (a) is still present in `pending` AND (b) has a live entry in `useRemoveUndoStore((s) => s.undoErrors)`
         for its id (i.e., it is a stuck, unresolved, FAILED Undo). **This guard condition is IDENTICAL to ROUND
         7/Z1's original — the defect a prior draft had was that this SAME branch never PERFORMED the
         content-swap ROUND 6/X4 specified as a separate instruction; because the guard becomes true on the
         VERY FIRST render after `setUndoError` fires (undoErrors having the entry is necessary and sufficient
         — independent of what `toast.kind` currently is), an early return BEFORE ever reaching the
         separately-described swap left the toast showing its plain "removed — Undo" copy forever, never
         erroring and never auto-dismissing (ROUND 9) — Opus Y1's exact repro.** THE FIX, applied in THIS SAME
         branch, not a separate effect or a later step: if BOTH (a) and (b) hold, (i) if the toast is not
         ALREADY showing that entry's exact error message, swap the toast's content to it (mirroring
         `DeleteUndoToast.tsx`'s own `undoError`-swap idiom, `:102-110`, but performed HERE rather than gated
         behind a guard that would otherwise block it); (ii) call `clearTimer()` and do NOT call `armTimer()`
         (ROUND 9 — see step 1b); (iii) set a component-local boolean ref (e.g. `wasStickyRef`) to `true`; (iv)
         `return` immediately — do NOT run the removed/added diff and do NOT run `DeleteUndoToast.tsx:91`'s
         unconditional trailing `prevRef.current = pending` fold this tick — this remains the ONE point this
         component's "near-verbatim copy" deliberately diverges from its precedent's fold, called out explicitly
         rather than left for an executor to silently reproduce the precedent's unconditional fold. Skipping the
         fold is what keeps a removal (e.g. a THIRD entry, C) that arrives DURING the sticky window from being
         silently absorbed into `prevRef`'s "already seen" set before it has ever been shown.
      1b. **(ROUND 9)** The toast's independent 8-second auto-dismiss timer is NEVER armed while the sticky
         guard (step 1) is active — this is now literally the SAME `clearTimer()`/no-`armTimer()` call step 1
         makes on every sticky tick (ii above), not a second, independent mechanism — a SECOND, disclosed
         deviation from `DeleteUndoToast.tsx`'s literal shape (whose own `armTimer` at `:61-64` and its
         unconditional call from `handleUndo`'s catch block at `:109` have no sticky-guard awareness at all).
         Without this, the sticky toast would vanish via its OWN inherited timeout after 8 seconds regardless of
         the fold-deferral fix, orphaning the entry's `undoErrors` slot (the timeout only calls `setToast`, never
         `clearUndoError`) with no remaining UI surface — a THIRD, undisclosed exit route this fix closes. The
         8-second re-arm idiom is preserved for this toast's non-sticky states (a fresh "removed" toast, a
         "restored" confirmation) exactly as `DeleteUndoToast.tsx` does — only the sticky error display itself
         never schedules a timeout.
      2. The effect's dependency array is `[pending, undoErrors, armTimer, toast]` — widened from
         `DeleteUndoToast.tsx`'s `[pending, armTimer]` so that (a) DISMISSING the sticky entry (which touches
         only `undoErrors`, not `pending`) re-triggers this SAME effect, exactly like a successful retry already
         does via its own `pending` change, and (b) the swap ITSELF (`setToast`, which changes `toast`)
         re-triggers the effect so step 1's idempotence check can observe the just-applied content and skip
         re-swapping rather than looping. No separate imperative "show the next entry" logic lives in the
         dismiss handler or in a distinct retry-handling code path — both routes converge on step 1's guard
         flipping to `false` (because `undoErrors` no longer names the displayed entry, or because the displayed
         entry left `pending`) and re-run this SAME body.
      3. When step 1's guard is `false`, the effect runs the ORIGINAL `removed`/`added` diff against `prevRef`'s
         (possibly stale, frozen-during-sticky) baseline. If `wasStickyRef` was `true` (read-and-reset it now)
         AND that diff finds a non-empty `added` set (entries that arrived during the freeze and were never
         folded, e.g. C), the newest such entry's "removed — Undo" toast is shown, taking priority over a bare
         "restored" confirmation for whatever entry just left `pending` on this SAME tick (e.g. a just-succeeded
         retry) — closing the gap on the retry-resolution route the SAME way as the dismiss route, per the
         checker's note that both exit routes needed this treatment. **(ROUND 9 — disclosed narrowing, AA3)** if
         `added` contains MORE THAN ONE entry (e.g., both C and D arrived during the same freeze), only the
         NEWEST (D) is shown — a deliberate, disclosed limitation of this toast's existing single-slot design
         (matching `DeleteUndoToast.tsx`'s own one-toast-at-a-time precedent), not a defect to fix here: the
         OTHER entries (e.g. C) already had their `.ilf` removal succeed and remain correctly recorded, but they
         do not get their own dedicated undo-toast turn once folded past — recoverable only via the manual Add
         flow, the SAME accepted-after-expiry framing as an ordinary 8-second-window lapse (this is ONE of D-03's
         TWO disclosed narrowings — the sticky-freeze case, Plan 15 gap ledger item (k); the other is the
         plain-remount toast loss, ledger item (g) — R10 review CC10 completes the two-narrowing set at this
         site; see this plan's must_haves, which carry both). Outside a
         freshly-thawed tick (`wasStickyRef` was already `false`), `removed` still wins over `added` exactly as
         `DeleteUndoToast.tsx` already does — this priority flip applies ONLY on the tick immediately following a
         sticky thaw, never generally.
      4. `prevRef.current = pending` runs at the END of every run that was NOT an early return in step 1 — this
         is now the ONLY place `pending` gets folded, meaning an entry that arrives during a sticky freeze is
         folded exactly once, on the SAME tick it is finally surfaced (or, if nothing surfaces because there is
         genuinely nothing unseen, the normal fold still happens so a later ordinary tick is not left comparing
         against a needlessly-stale baseline).
      The sticky entry itself stops being sticky (triggering step 2's re-run) when EITHER: the user clicks the
      toast's dismiss (✕) button while displaying it — this ALSO calls
      `useRemoveUndoStore.getState().clearUndoError(entry.id)` (an explicit acknowledgement that clears ONLY
      this entry's slot, never another's) and sets the displayed toast to `{ kind: 'none' }` itself (the
      dismiss handler does NOT scan `pending` or decide what shows next — the widened-dependency effect above
      does that reactively on its next run) — the entry remains in `pending`, simply no longer displayed,
      recoverable only via manually re-adding it through the 021-A Add flow (Plan 14) — **(ROUND 10, AA10 —
      MEDIUM, Opus Y6)** unlike `DeleteUndoToast`'s own precedent (whose second surface, ProjectListDialog's
      Restore button, gives a dismissed-but-still-pending entry another in-app path back), `RemoveUndoToast` has
      NO such second surface — once dismissed with nothing else queued, the entry is permanently un-surfaceable
      in this UI (it stays in the fold baseline, never in a future `removed` diff); OR the user retries Undo on it via `onUndo(entry)` — a SUCCESS removes
      it from `pending` (triggering step 3's diff, which per the priority rule above prefers surfacing any
      queued unseen entry over "A restored"; if nothing is queued, the normal "restored" toast shows exactly as
      ROUND 7 originally specified); a REPEAT FAILURE keeps the SAME sticky display shown with the new error text,
      WITHOUT ever arming a dismiss timer (ROUND 9 — step 1b's no-timer rule still applies; no exit — step 1's
      guard is still true). This guarantees a failed Undo's error is NEVER silently superseded by an unrelated
      Remove before the user has had a chance to see and act on it, is NEVER silently dismissed by its own
      inherited auto-dismiss timer while sticky (ROUND 9, step 1b), AND that a SINGLE removal which arrived
      during the sticky window is never permanently lost on EITHER exit route (if MORE THAN ONE arrives during
      the same freeze, only the newest surfaces its own toast — a disclosed narrowing, ROUND 9, not a defect) —
      closing the "surviving affordance for stuck pending entries" half of Z1, and the mechanism gap the
      checker found in it, without introducing a multi-toast stack.
    - **(ROUND 3, R4/R7; ROUND 4, W3; ROUND 5, V5/V6/V12)** WorldPanel.tsx: each decoration row gains a
      "Remove" action (icon/button, per-row, no confirm dialog — D-03). Its click handler calls Plan 10's
      shared, null-safe `resolveOverridePair()` helper (the SAME one `refreshTree()`, the mirror toggle, AND
      this task's own Undo handler above use — ROUND 4/W3, NEVER a second, independently-resolved
      `overrideDir`/`readVfs`/`meta`/`studioDir` pair, and NEVER a cached value from mount or a prior click,
      since `resolveOverridePair()` itself re-reads `readWorkspaceJson` fresh every call — ROUND-3-REVIEW R2).
      If `resolveOverridePair()` returns `null` (no resolvable scan root, INCLUDING the ROUND 5/V6 case where
      `studioDir` itself is null), the Remove action is a no-op that surfaces the same words-only disabled
      reasoning the live-strip already renders (defense-in-depth; the row action should already be visually
      disabled/absent in this state since the tree itself would be empty). Otherwise, it reads `mappingName` via
      **(ROUND 5, V5 — MEDIUM, Cursor)** the NARROWED status union: `const st = useLiveStore.getState().status;
      const mappingName = st.kind === 'attached' ? st.mappingName : null;` (`mappingName` exists ONLY on the
      `{ kind: 'attached' }` member of `liveStore.ts`'s `ConnectionStatus` — reading `st.mappingName`
      unconditionally is a real `tsc` error, the SAME class of bug the R8/M2 `clientLabel` fix in Plan 10
      already closed) — then calls `removeDecorationRow` with **(ROUND 5, V12 — LOW, Cursor)** the resolved
      `pair.studioDir` (Task 1's signature requires `studioDir` as its FIRST parameter — this call site now
      names it explicitly instead of omitting it), the row's building/cellName/rowIndex, the FRESHLY-resolved
      `pair.overrideDir`/`pair.readVfs`/`mappingName`, and **`liveNetworkId: null`, ALWAYS** (per C4 — this
      phase's `worldEditorStore.sessionOverlay` carries no node-id association to look up; do not invent one
      here or fabricate a lookup that doesn't exist) — then `useRemoveUndoStore.push(...)` with the removed
      node, then calls the panel's shared `refreshTree()` (Plan 10's helper — ROUND-3-REVIEW R2/R3 seam:
      `refreshTree()` re-reads `worldEditorBuildingTemplates` fresh AND, via Plan 04's widened `refresh()`,
      reconciles `sessionOverlay`/`selectedRowId` against the post-remove tree by content identity — this Remove
      action does NOT call `refresh()` directly with a hand-resolved argument pair; it calls the ONE shared
      `refreshTree()` this file's Plan-10 Task defines) so the row disappears from the disk-scanned view
      immediately (data removal already committed to disk by this point). If `removeDecorationRow` throws (e.g.
      ROUND 3/R3's "stock template path isn't known yet" case), catch it and surface the message via the
      existing `log()` service — never crash the row's click handler, and never push a `RemovedRowEntry` for a
      removal that didn't actually happen.
    - The row's status badge, for the data-only remove (which is EVERY remove this phase ships, per C4), reads
      "removed — reload scene to see it gone" until the next reload — this is rendered from the row's LAST
      KNOWN state before it vanished from the refreshed tree, i.e., surfaced via a toast/history entry rather
      than a lingering row (once removed from disk, the row is gone from the NEXT scan by definition — the
      "reload to see it gone" honesty message belongs in the toast/Activity-history text, not as a persistent
      row state).
  </behavior>
  <action>
    Create RemoveUndoToast.tsx as a near-verbatim structural copy of DeleteUndoToast.tsx (pending-diff
    useEffect, 8s timer, toast shell — `DeleteUndoToast.tsx:146-166`'s "delete"-variant shell EXACTLY, per the
    ROUND 6/X5 citation correction), scoped
    to `useRemoveUndoStore` and decoration-row copy, accepting the new REQUIRED prop `onUndo: (entry:
    RemovedRowEntry) => void` (ROUND 5/V1 — replacing the prior `onUndoComplete` design) — the Undo button's
    click handler calls ONLY `onUndo(entry)`; it does not call `restore()` or any add-back helper itself.
    **(ROUND 10, AA2 — HIGH, Opus Y2)** Initialize `toast`/`prevRef`/`wasStickyRef` per the ROUND 10/step-0 mount
    behavior described below — NOT unconditionally blank the way a fresh, globally-mounted `DeleteUndoToast`
    can safely be: derive `toast`'s initial value from whether the store ALREADY has a sticky entry (an id
    present in BOTH `pending` and `undoErrors`) at mount time — picking the LAST such entry and
    warn-asserting the at-most-one invariant (R9 review, BB22) — and seed `prevRef.current` per the step-0
    BB3 rule: to `pending.slice(0, pending.findIndex((e) => e.id === stickyEntry.id) + 1)` (id-keyed, R10 review CC16) with `wasStickyRef = true` when a sticky entry
    was found (reconstructing the frozen freeze baseline so unshown freeze arrivals still surface on thaw),
    else to the FULL current `pending` (never `[]`) with `wasStickyRef = false` — so a mid-session remount of
    this deliberately panel-scoped component never re-shows a stale plain-removal toast as if newly removed,
    never loses an already-sticky error display (closing AA2's fourth exit route: tab navigation away and
    back), and never folds an unshown freeze arrival into the seen baseline (closing BB3's fifth exit route).
    **(ROUND 7, Z1; ROUND 8 — mechanism fix, checker blocker; ROUND 10, AA1 — HIGH, Opus Y1, merging the swap
    INTO the guard it was previously gated behind)** Implement the sticky guard-and-swap and its fold-skip exit
    as a single deviation from the copied effect's own trailing line, not as an added branch bolted onto an
    otherwise-unchanged copy, and NOT as a separate subscription/effect: a prior draft's "ALSO subscribe to
    `useRemoveUndoStore((s) => s.undoErrors)`... swap the toast's content" instruction is SUPERSEDED here —
    describing the content-swap as a step separate from the guard is exactly what let an executor place the
    swap AFTER a guard that already returns early on that SAME `undoErrors`-has-an-entry condition, so the swap
    code was structurally unreachable on the very first tick after a failure (Opus Y1's repro: `setUndoError`
    makes the guard true immediately, before the toast has ever re-rendered with error content). The fix: widen
    the effect's dependency array from `DeleteUndoToast.tsx`'s `[pending, armTimer]` to `[pending, undoErrors,
    armTimer, toast]`; at the TOP of the effect, if the CURRENTLY-DISPLAYED entry still has a live `undoErrors`
    entry for its id AND is still present in `pending`: (a) if the toast is not ALREADY showing that exact error
    message (an idempotence check — re-running this branch on a LATER tick where the swap already happened must
    NOT call `setToast` again, which would otherwise re-trigger this same effect in a loop since `toast` is
    itself a dependency), call `setToast({ kind: 'error', entryId, message: undoErrors.get(entryId) })` —
    mirroring `DeleteUndoToast.tsx`'s own `undoError`-content-swap idiom (`:102-110`), performed HERE, inside the
    guard branch, not gated behind it; (b) call `clearTimer()` and do NOT call `armTimer()` (ROUND 9 — the
    sticky display's own dismiss timer is never scheduled, a deliberate deviation from `DeleteUndoToast.tsx`'s
    `armTimer`, `:61-64`, and its unconditional call from `handleUndo`'s catch block, `:109`, which have no
    sticky-guard awareness); (c) set a boolean ref (e.g. `wasStickyRef`) to `true`; (d) `return` immediately —
    do NOT run the removed/added diff and do NOT run the trailing `prevRef.current = pending` fold this tick
    (the ONE explicit deviation from `DeleteUndoToast.tsx:91`'s unconditional fold; unchanged from ROUND 8's
    original intent, just now co-located with the swap that makes it OBSERVABLE). When the guard is false, run
    the diff exactly as copied, EXCEPT: if `wasStickyRef` was set (read-and-reset it), and the diff finds a
    non-empty `added` set (entries whose ids are not in the frozen `prevRef`), show the newest such entry's toast
    in preference to a `removed`-triggered "restored" confirmation on this SAME tick (**ROUND 9 — if `added`
    contains more than one entry, e.g. both C and D arrived during the freeze, only the newest, D, is shown; a
    disclosed narrowing of the existing single-slot toast design, not a defect — C's `.ilf` removal already
    succeeded and is unaffected, but C loses its own dedicated undo-toast turn once folded past, recoverable
    only via the manual Add flow**) — otherwise (no freeze just ended, or nothing unseen) keep
    `DeleteUndoToast.tsx`'s original removed-before-added priority unchanged. Fold `prevRef.current = pending`
    at the end of every run that was not the early return. The dismiss (✕) handler, in addition to its existing
    `clearTimer()`, calls `useRemoveUndoStore.getState().clearUndoError(entry.id)` (scoped to only that entry)
    and sets the toast to `{ kind: 'none' }` itself — it does NOT scan `pending` or decide what shows next; the
    widened-dependency effect above does that reactively on its next run, exactly the SAME way a successful
    retry's `pending` change already triggers it, so no second, duplicated "what shows next" implementation
    exists between the dismiss handler and the retry path.
    Render it from WorldPanel.tsx (mounted once per WorldPanel instance — DELIBERATELY panel-scoped, NOT
    globally mounted the way `DeleteUndoToast` is in `App.tsx` per ROUND 6/X5's correction; this intentional
    panel-local scoping is the documented, accepted root cause of Plan 15's gap ledger item (g) for a PLAIN,
    non-sticky remount — ROUND 10/AA2 above closes the STICKY-display remount case specifically; ROUND 7/Z17
    fixes this line's previously-false "mounted at its own panel's top level" comparison, which contradicted
    this plan's own X5 correction elsewhere), passing a `handleUndo` function defined in WorldPanel.tsx as
    `onUndo`. **(ROUND 10, AA5 — HIGH, Sonnet L2)** ALSO add a component-scope
    `const studioDir = useWorkspaceStore((s) => s.studioDir);` selector (used ONLY to build this key — every
    OTHER read of `studioDir` in this file still goes through `resolveOverridePair()`'s imperative
    `getState()` form, ROUND 6/X1 — this hook subscription is a NEW, separate, deliberately narrow exception
    solely for React's `key` mechanism) and render `<RemoveUndoToast key={studioDir ?? 'no-project'} ... />` —
    a PROJECT SWITCH (a `studioDir` change) forces React to fully unmount-and-remount this component, reusing
    the SAME ROUND 10/AA2 mount-derived-initial-state path above rather than needing a bespoke project-switch
    code path of its own for this toast's local `toast`/`prevRef`/`wasStickyRef` state. This does NOT by itself
    reset `removeUndoStore`'s or `worldEditorStore`'s own module-scope state (a `key` change only remounts the
    COMPONENT, not the store) — the stores reset THEMSELVES, synchronously and BEFORE this remount renders,
    via their own module-scope workspaceStore subscriptions (Plan 04's PROJECT-SWITCH RESET ORDERING
    CONTRACT; this plan's Task 1 registers removeUndoStore's — R9 review, BB2: the superseded design, an
    effect in Plan 14 calling both resets, ran AFTER this key-remount's lazy initializers had already read
    the OLD project's `pending`, producing a false 'restored' toast in the new project). Plan 14's Task 3
    retains a `[studioDir]` effect ONLY for its OWN component-local `prevTreeRef`/`suppressNextDiffRef`
    reset, its pending-placement abort (Plan 08's HOST_CMD ACK PROTOCOL, aborted-switch), and the new
    project's first `refreshTree()` (R9 review, BB11).
    Implement `handleUndo(entry: RemovedRowEntry)` in WorldPanel.tsx per the ROUND 6 (X3/X4/X6) and
    ROUND 7 (Z1/Z5) behavior spec above: NO up-front `clearUndoError` call (**R9 review, BB9** — superseding both the ROUND 7 clear-first
    instruction AND the ROUND 10/AA14 disclosure that instruction forced: because nothing is cleared until
    the outcome is settled, the sticky guard cannot transiently open mid-retry no matter how many commits
    interleave, and the one-synchronous-turn assumption AA14 documented as load-bearing is now simply
    irrelevant — this adopts AA14's own named alternative, 'move clearUndoError to fire ONLY alongside the
    eventual success/failure outcome'); resolve
    `building` from the tree by `entry.buildingId` (undefined → words-only log + `setUndoError` +
    return, X6); `resolveOverridePair()` next (null → words-only log + `setUndoError`, no add-back call, no
    `restore()` call, entry stays in `pending`, X4); on success, call the add-back helper FIRST with
    `pair.studioDir`/`pair.overrideDir`/`pair.readVfs`/the resolved `building`/`entry.removedNode` (ROUND 7/Z1 —
    reordered), and ONLY once it returns successfully call `restore(entry.id)`, then `clearUndoError(entry.id)` (R9
    review, BB9 — settle-time cleanup, id-scoped), then PUSH `{ buildingId:
    entry.buildingId, cellName: entry.cellName, objectTemplateName: entry.removedNode.objectTemplateName }` onto
    `suppressNextDiffRef.current` (ROUND 7/Z5 — an array push, `suppressNextDiffRef.current = [
    ...suppressNextDiffRef.current, { ... } ]`, never an overwrite) BEFORE `refreshTree()` (ROUND 5/V2 groundwork
    for Plan 14, ROUND 6/X3 — identity-keyed, not a bare boolean; ROUND 7/Z5 — queued, not a single ref); if the
    add-back helper throws, catch, `log()`, ALSO `setUndoError(entry.id, <message>)` (X4), do NOT call
    `restore(entry.id)` (ROUND 7/Z1 — the entry was never popped, so it remains safely in `pending`), and skip
    `refreshTree()`. Declare `const suppressNextDiffRef = useRef<Array<{ buildingId: string; cellName: string;
    objectTemplateName: string }>>([]);` once in WorldPanel.tsx's component body, alongside
    `resolveOverridePair()`/`refreshTree()` (ROUND 7/Z5 — a queue, replacing the ROUND 6 single-nullable-ref form
    — a plain, undocumented-elsewhere component-local ref this plan introduces FOR Plan 14's later consumption —
    see Plan 14's own ROUND 7 revision for the reader). Add the per-row Remove action to WorldPanel.tsx's
    decoration rows per the
    behavior spec, resolving `overrideDir`/`readVfs`/`studioDir` via the SAME shared `resolveOverridePair()`
    Plan 10 defines (ROUND 4/W3 — never a second inline resolution) and `mappingName` via the ROUND 5/V5
    narrowed-status-union pattern above, ALWAYS passing `liveNetworkId: null` (no lookup, no fabricated
    association — C4), wrapping the call in a try/catch that logs a thrown `removeDecorationRow` error via
    `log()` instead of crashing (ROUND 3/R3), and record a `recordPersistResult` entry in
    `worldEditorStore.history` for the removal (`outcome: 'warn'`, `message: formatPersistMessage("removed —
    reload scene to see it gone", mirrorToStockIlf)` — **(ROUND-3-REVIEW R10a, MEDIUM, Opus)** routed through
    Plan 04's `formatPersistMessage` (D-10), the SAME single-implementation helper the base edit/add RESULT
    path (Plan 08) and Task 1's Remove already use elsewhere for this exact purpose — a mirror-OFF remove
    suffers the SAME hybrid-session invisibility as an edit, and D-10's full-detail promise applies to Remove
    too, not only to edit/add. `mirrorToStockIlf` here is the SAME `readWorkspaceJson(studioDir).
    mirrorToStockIlf ?? true` value this click handler already resolved to build `removeDecorationRow`'s deps —
    this is the ONLY outcome this phase's Remove ever produces, since the live-despawn branch is never
    exercised; do NOT also implement the 'ok'/"removed and despawned live" message variant described in an
    earlier draft of this plan, since no caller can ever trigger it).
    Extend WorldPanel.test.tsx: seed a tree with one decoration, click its Remove action, assert
    removeDecorationRow was called (spy) WITH `liveNetworkId: null` AND `studioDir` equal to the resolved
    pair's `studioDir` specifically (ROUND 5/V12), and the row disappears after refresh; a mocked
    `resolveOverridePair()` returning `null` makes the Remove action a no-op (assert `removeDecorationRow` is
    NEVER called — ROUND 4/W3); seed `useLiveStore`'s status as `{ kind: 'idle' }` and assert Remove passes
    `mappingName: null` (ROUND 5/V5), then seed `{ kind: 'attached', pid, mappingName: 'x' }` and assert Remove
    passes `mappingName: 'x'` exactly. Extend RemoveUndoToast.test.tsx (new file) mirroring
    DeleteUndoToast.test.tsx's existing test shape for the append/undo toast lifecycle, additionally asserting
    that clicking Undo calls the passed `onUndo` prop exactly once with the CORRECT `RemovedRowEntry` (ROUND
    5/V1) and does NOT call `restore()`/any write helper itself (this component has no access to either);
    **(ROUND 6, X4; ROUND 7, Z1)** seeding `useRemoveUndoStore`'s `undoErrors` with an entry for
    `<the displayed entry's id>` mapped to `'some words-only reason'` after the toast is showing that entry
    asserts the rendered toast text swaps to contain that EXACT message and NO dismiss timer is armed (ROUND 9 —
    fake-timer idiom, mirroring DeleteUndoToast.test.tsx's own timer test if one exists, but asserting the
    OPPOSITE outcome: the timer is cleared, never re-armed, while sticky); an `undoErrors` entry
    for a DIFFERENT entry's id does NOT change the currently-displayed toast's content (scoping assertion).
    **(ROUND 7, Z1 — sticky-error regression test)** seed `pending` with TWO entries (A, B); show A's toast; seed
    `undoErrors` with an entry for A's id (simulating a failed Undo on A); THEN seed a THIRD removal (C) added to
    `pending` — assert the toast STILL displays A's error content, NOT C's "removed" toast (the sticky guard);
    then dismiss A's toast — assert `clearUndoError(A.id)` was called (spy) and the toast NOW advances to show
    C's "removed" toast.
    **(ROUND 8 — the retry-resolution route's OWN version of the same regression test, since the checker's fix
    explicitly required BOTH exit routes to be covered, not only dismiss)** seed `pending` with entries A and B;
    show A's toast; seed `undoErrors` with an entry for A's id; THEN add a THIRD entry, C, to `pending` while
    A's error is still showing (the sticky guard keeps A displayed, per the test above); THEN, instead of
    dismissing, drive `onUndo(A)` through a SUCCESS path (mock the add-back helper to resolve, then call
    `useRemoveUndoStore.getState().restore(A.id)` and then `clearUndoError(A.id)` — mirroring what
    WorldPanel's real `handleUndo` does on success: restore, then settle-time clear, per R9 review BB9) — assert the toast advances to show C's "removed" toast
    (NOT a bare "A restored" confirmation), proving the SAME unseen-entry priority applies via the
    retry-resolution route, not only the dismiss route.
    **(ROUND 9 — the inherited-timer regression test, the literal repro the blocker's fix must close)** using
    fake timers: seed `pending` with entry A, show A's toast, seed `undoErrors` with an entry for A's id (the
    sticky error display) — then advance fake timers by `TOAST_MS` (8000ms) AND BEYOND (e.g. `TOAST_MS + 1`) —
    assert the rendered toast STILL displays A's error content (not `{ kind: 'none' }`),
    `useRemoveUndoStore.getState().pending` still contains A, and `useRemoveUndoStore.getState().undoErrors`
    still has A's entry — proving the sticky display is never silently dismissed by its own inherited
    auto-dismiss timer while unresolved.
    **(ROUND 9 — the disclosed-narrowing regression test)** seed `pending` with entry A; show A's toast; seed
    `undoErrors` with an entry for A's id (sticky); THEN add TWO further entries, C and D (in that order), to
    `pending` while A is still sticky; THEN resolve A (either dismiss or a successful retry, per the existing
    ROUND 7/ROUND 8 tests' idiom above) — assert the toast advances to show ONLY D's "removed" toast (never
    C's); this test does NOT assert C's removal was undone or lost from disk — only that C never gets its own
    displayed undo-toast turn, matching the disclosed narrowing this plan's must_haves now state explicitly.
    **(R9 review, BB16 — the unmount/remount test family the AA2 mount-derivation mechanism previously had
    ZERO coverage of; drive remounts by rerendering with a changed React `key`, or RTL `unmount()` + a fresh
    `render()`, so the lazy initializers actually re-run):** (1) *plain remount (ledger (g) determinism):*
    seed `pending` with one entry, render, let its toast show, unmount, remount — assert NO toast renders
    after the remount (the deterministic does-not-re-show behavior AA2 promised); (2) *sticky remount:* seed
    `pending` [A] + `undoErrors` {A}, render, unmount, remount — assert the toast renders A's EXACT error
    content and (fake timers) no dismiss timer is armed; (3) *freeze-remount repro (BB3 — the fifth exit
    route, now closed):* seed `pending` [A, C] + `undoErrors` {A} (C arrived during A's freeze), unmount,
    remount — assert the sticky A error shows; then dismiss A — assert `clearUndoError(A.id)` fires AND the
    toast ADVANCES to C's "removed — Undo" toast (the reconstructed `prevRef` baseline excludes C and the
    restored `wasStickyRef = true` re-arms the thaw priority; under the superseded blanket `prevRef = pending`
    seed this diff came back empty and C was permanently lost); repeat the same setup and resolve A via a
    SUCCESSFUL retry instead (mocked add-back resolves; `restore(A.id)` + `clearUndoError(A.id)` run) — assert
    the toast again advances to C, never a bare "A restored"; (4) *mount-pick determinism (BB22):* seed
    `pending` [A, B] with `undoErrors` naming BOTH (the broken-invariant state), mount — assert the toast
    shows B (the LAST matching entry in `pending` order) and a `console.warn` fired naming both ids; (5)
    *project-switch (BB2):* seed `useWorkspaceStore`'s `studioDir` to 'project-a', seed `pending` non-empty,
    render `WorldPanel`; change `studioDir` to 'project-b' (driving the store's own synchronous subscription
    reset AND the `key` remount in one act) — assert `removeUndoStore.getState().pending` is ALREADY `[]`
    when the new render commits, NO toast of ANY kind renders after the switch, and `queryByText(/restored/i)`
    is null at every point (the false-'A restored' race is structurally impossible under the
    reset-before-render contract).
    **(R9 review, BB9 — the premature-thaw regression the ROUND 8 suite could not express because it never
    touched `undoErrors` mid-retry):** seed `pending` [A, C] + `undoErrors` {A} (sticky, C queued during the
    freeze); drive `onUndo(A)` through a FAILURE path (mocked add-back throws) — assert that AFTER the retry
    the toast STILL shows A's (new) error text, C's "removed" copy never rendered at any intermediate commit,
    NO dismiss timer is armed, and `undoErrors.get(A.id)` was never observed empty between the click and the
    failure landing (the no-clear-at-top ordering).
    Extend WorldPanel.test.tsx further: a mocked `useWorldEditorStore` tree lookup that does NOT contain
    `entry.buildingId` when Undo is clicked calls `log()` with a words-only message naming the missing
    building AND `setUndoError(entry.id, ...)` (ROUND 6/X6), does NOT call `resolveOverridePair()`/the add-back
    helper/`restore()` (spies show zero calls to all three); a mocked `resolveOverridePair()` returning
    `null` when Undo is clicked calls `log()` with a words-only message, ALSO calls `setUndoError(entry.id,
    ...)` with that SAME words-only reason (ROUND 6/X4), does NOT call the add-back helper or `restore()` (assert
    via spies on both showing zero calls), and leaves the entry in `pending` (ROUND
    5/V1). **(ROUND 7, Z1 — the false-success regression test, the literal repro Y1 exists to close)** a mocked
    add-back helper that THROWS on Undo asserts: `restore()` is NEVER called (spy shows zero calls), the entry
    is STILL present in `useRemoveUndoStore.getState().pending` after the throw, `setUndoError(entry.id,
    <message>)` was called with the caught error's message, and `refreshTree()` was NEVER called — proving a
    failed add-back can never produce the "restored" toast (the false-success class this fix eliminates
    structurally, not just via a guard). A `resolveOverridePair()` returning a real pair on Undo calls the
    add-back helper BEFORE `restore(entry.id)` (asserted via a call-order spy — ROUND 7/Z1), with that pair's
    `studioDir`/`overrideDir`/`readVfs`/the resolved `building`, PUSHES `{ buildingId, cellName,
    objectTemplateName }` onto `suppressNextDiffRef.current` (asserted via the ROUND 6/X7 static grep gate below
    plus a runtime assertion that the array's length increases by exactly one per successful Undo — do not
    invent a runtime assertion for anything Plan 14's own consumption cannot yet make observable), and calls
    `refreshTree()` exactly once. **(ROUND 10, AA8 — MEDIUM, Opus Y4, replacing the ROUND 7/Z5-drafted "two rapid
    Undos before any drain occurs" test — unreachable through this phase's shipped single-slot toast UI: a
    second entry's Undo button cannot even render until the first entry's toast has already resolved (this
    component shows exactly one entry at a time), and a successful `handleUndo`'s own `refreshTree()` call
    triggers Plan 14's `[tree]`-keyed diff/drain effect before a second Undo's click could ever land — so two
    identity keys can never be independently observed sitting in the queue via any real, rendered user action)**
    the queue's append-only semantics (never an overwrite) are instead proven via a STATIC source-grep gate,
    mirroring the ROUND 6/X7 precedent already established for this SAME ref's other run-time-unobservable
    property: `grep -n "suppressNextDiffRef.current = \[\.\.\.suppressNextDiffRef.current" packages/renderer/src/panels/world/WorldPanel.tsx`
    matches, confirming the push is a spread-append (`[...prev, x]`), never an overwrite (`= x`) — the queue's
    structural capacity to hold multiple concurrent entries is verified even though a live 2-simultaneous-entry
    scenario is not independently exercisable through the shipped UI (disclosed; Plan 14's own equivalent
    consumer-side test is fixed the same way, see its own ROUND 10 revision).
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- RemoveUndoToast WorldPanel</automated>
  </verify>
  <acceptance_criteria>
    Tests assert: clicking Remove on a decoration row triggers removeDecorationRow with the correct row
    identity, a real `overrideDir`/`readVfs`/`studioDir` pair resolved via `resolveOverridePair()` (ROUND 3/R4;
    ROUND 4/W3; ROUND 5/V12), the seeded `mappingName` narrowed from `useLiveStore`'s status union (ROUND
    3/R7; ROUND 5/V5), AND `liveNetworkId: null` (never a fabricated non-null value) and no confirm dialog
    appears (query for any dialog/modal role returns none); a toast appears with an Undo action; clicking Undo
    calls the `onUndo` prop exactly once with the correct entry (ROUND 5/V1 — replacing the prior
    `onUndoComplete` assertion) — RemoveUndoToast.tsx itself never calls `restore()` or any write helper
    (asserted via spies showing zero such calls from THIS component); WorldPanel's `handleUndo` (tested
    directly via the rendered toast's Undo click) resolves `resolveOverridePair()` FIRST, calls `restore(id)`
    ONLY on a non-null result (a `null` result leaves the entry in `pending` — asserted via
    `useRemoveUndoStore.getState().pending` still containing it — and calls `log()`, never `restore()` — ROUND
    5/V1), then resolves `building` from the tree (undefined → the SAME words-only-no-op/`setUndoError`
    treatment, ROUND 6/X6), then the add-back helper, then PUSHES the restored row's identity key (`{ buildingId, cellName, objectTemplateName }`) onto the
    shared `suppressNextDiffRef` QUEUE (ROUND 6/X3 — never a bare boolean; ROUND 7/Z5 — never a single
    overwritable ref) before calling `refreshTree()` (ROUND 5/V2 groundwork), and — per ROUND 7/Z1 — the
    add-back helper is called BEFORE `restore(id)`, so `restore(id)` (and therefore this whole success branch)
    can only ever be reached once the add-back has ALREADY succeeded; **in the ORDINARY case only — no OTHER
    removal arrived during this entry's sticky window, and this resolution was not itself the "unseen queued
    entry takes priority" branch (ROUND 8/ROUND 9 — AA9: if an unseen removal WAS queued during a freeze, ITS
    OWN "removed — Undo" toast takes priority over a bare "restored" confirmation per the ROUND 8
    retry-resolution test above; a sticky display itself never carries a running timer at all, per ROUND 9)** —
    a confirmation toast appears and auto-dismisses after its timer
    (mirroring DeleteUndoToast's own timer test, if one exists — copy that test's fake-timer idiom); the
    recorded history entry for a removal is always `outcome: 'warn'` with a `formatPersistMessage("removed —
    reload scene to see it gone", mirrorToStockIlf)`-built message — a mirror-OFF removal's recorded message
    carries the D-10 suffix, a mirror-ON removal's does not (exact string match against
    `formatPersistMessage`'s own output, ROUND-3-REVIEW R10a) — never the unreachable 'ok'/despawned-live
    variant; a seeded row whose building has an empty `buildingTemplateVfsPath` shows the honest "not known
    yet" error via `log()` when Remove is clicked, and does NOT push a `RemovedRowEntry` or show the undo toast
    (ROUND 3/R3). **(ROUND-3-REVIEW R2; ROUND 4/W3)** clicking Remove resolves `overrideDir`/`readVfs` via
    `resolveOverridePair()`, which itself performs a FRESH `readWorkspaceJson` read at click time (asserted via
    a spy showing `readWorkspaceJson` is called AGAIN on a second Remove click, not reused from a cached
    value) and, on success, calls the SAME shared `refreshTree()` helper Plan 10's mount effect and mirror
    toggle use (asserted via a spy on `refreshTree`, not a direct `refresh()` call with a stale argument pair);
    a mocked `resolveOverridePair()` returning `null` results in ZERO `removeDecorationRow` calls (ROUND 4/W3
    — the Remove-side half of the shared-guard fix). **(ROUND 4/W2; ROUND 5/V1)** a Remove followed by an Undo
    within the 8-second window calls `refreshTree()` a SECOND time (once for the Remove, once for the Undo) —
    asserted via a call-count spy on `refreshTree`, not merely "the row reappears" — proving Undo's own refresh
    discipline, now owned end-to-end by WorldPanel (ROUND 5/V1) rather than Remove's.
    **(ROUND 6, X4; ROUND 7, Z1 — per-entry keyed, respecifying the collision the X4 must_have's mandated
    "near-verbatim copy" mechanism produced)** a failed Undo (null `resolveOverridePair()`, a missing building
    per X6, or a thrown add-back — the LATTER now provably never pops the entry from `pending` before failing,
    per the Z1 reordering fix above, so it can never present as a false 'restored' success) ALWAYS calls
    `useRemoveUndoStore.getState().setUndoError(entry.id, <message>)` in addition to `log()`, and a subsequent
    FAILED Undo attempt on THAT SAME entry OVERWRITES its `undoErrors` slot with the new message via
    `setUndoError` with NO pre-clear (**R9 review, BB9**: at NO point during a retry is the entry's
    `undoErrors` slot empty while the entry is still pending — asserted by checking the map immediately
    after driving the retry's failure; a SUCCESSFUL retry clears the slot only after `restore(entry.id)` has
    popped the entry); a subsequent Undo attempt on
    a DIFFERENT entry's `clearUndoError(otherId)` call does NOT remove THIS entry's `undoErrors` slot (the
    cross-entry-erasure regression test, ROUND 7/Z1); the rendered `RemoveUndoToast` visibly swaps to the error
    text WITHOUT arming its dismiss timer (ROUND 9 — `clearTimer()` only, never `armTimer()`, while sticky) when
    `undoErrors` has an entry for the id it is currently displaying (proving the toast is never silently
    invisible on a failed Undo — closing X4's HIGH finding);
    when a SECOND, unrelated removal is added to `pending` while the FIRST entry's error toast is still
    displayed and unresolved, the toast does NOT switch to the new removal (the ROUND 7/Z1 sticky-error guard —
    closing the "stuck entry silently superseded" finding) until the sticky entry is explicitly dismissed
    (which scopes `clearUndoError` to ONLY that entry) or successfully/repeatedly retried — **(ROUND 8)** — and
    once dismissed OR successfully retried, that unseen removal's OWN toast is what appears next (never
    silently dropped), on EITHER exit route, because the underlying effect defers folding `pending` into its
    seen-baseline until the sticky guard actually clears, rather than folding it unconditionally every tick as
    `DeleteUndoToast.tsx`'s copied structure would otherwise do (the ROUND 8 retry-resolution regression test
    above proves the retry-success half of this; the ROUND 7 dismiss test above proves the dismiss half). **(ROUND 9)** the sticky error toast is NEVER dismissed by its own inherited 8-second auto-dismiss timer while `undoErrors` still names it — advancing fake timers past `TOAST_MS` while sticky leaves the toast, the `pending` entry, and the `undoErrors` entry all unchanged (the inherited-timer regression test); when MORE THAN ONE removal (C, then D) arrives during a single sticky freeze, only the NEWEST (D) surfaces its own toast once the sticky entry resolves — C's own undo-toast turn is disclosed-and-accepted as lost (not a defect), while C's underlying `.ilf` removal remains correctly committed regardless (the disclosed-narrowing regression test).
    **(ROUND 6, X6)** a `handleUndo` call whose `entry.buildingId` is not
    found in the current tree logs a words-only reason naming the missing building, calls `setUndoError`, and
    calls NEITHER `resolveOverridePair()` NOR `restore()` NOR the add-back helper (spies show zero calls to
    all three). **(ROUND 6, X7 — static grep gate, since the ref has zero observable runtime consequence until
    Plan 14, wave 5, consumes it; ROUND 7, Z5 — grep pattern updated for the queue-push form)**
    `grep -n "suppressNextDiffRef.current = \[" packages/renderer/src/panels/world/WorldPanel.tsx`
    shows the queue-push assignment appearing textually BEFORE this same file's `refreshTree();` call inside
    `handleUndo` (a source-order check, not a runtime spy-sequence assertion this wave cannot make observable).
    **(ROUND 7, Z1 — the false-success runtime regression, now directly observable within THIS plan's own
    wave, unlike X7's ref-consumption check)** a mocked add-back helper that throws asserts, via a call-order
    spy: the add-back helper is invoked BEFORE `restore()`; `restore()` is NEVER called on this path; the entry
    remains in `useRemoveUndoStore.getState().pending`; and `refreshTree()` is NEVER called — together proving
    the false-'restored'-toast class is now structurally unreachable, not merely undertested.
  </acceptance_criteria>
  <done>Remove is a guarded, undo-able, no-confirm-dialog row action per D-02/D-03, fully wired into the World
  panel and covered by tests mirroring the proven DeleteUndoToast idiom — data-only ALWAYS, honestly (C4);
  reversible within the 8-second toast window (Sonnet F11) — both subject to the two disclosed D-03
  narrowings, (k) freeze multi-arrival and (g) plain-remount toast loss (R9 review, BB14); Remove AND Undo both resolve
  overrideDir/readVfs/studioDir through the SAME shared, null-safe `resolveOverridePair()` helper (ROUND 4/W3;
  ROUND 5/V6) and both trigger `refreshTree()` on completion, so the tree is never left stale after either half
  of the round trip (ROUND 4/W2). RemoveUndoToast.tsx is purely presentational — it owns NO write-path
  dependency resolution, matching DeleteUndoToast.tsx's own precedent (ROUND 6/X5 — corrected: DeleteUndoToast
  takes zero props and calls its own store directly; RemoveUndoToast goes one step further, delegating the
  ENTIRE restore via `onUndo` since it cannot even call `restore()` itself) — and WorldPanel.tsx owns the
  entire Undo restore end-to-end via a required `onUndo` prop, resolving the target building explicitly (ROUND
  6/X6) and closing the round's anchor finding that the previous design was unimplementable as written (ROUND
  5/V1). A failed Undo is never silently invisible — `removeUndoStore.undoErrors` (per-entry keyed, ROUND 7/Z1) gives the toast an
  observable signal to show the reason WITHOUT re-arming its auto-dismiss timer (ROUND 6/X4 for the
  show-the-reason half; ROUND 9 corrects the timer half — the sticky display's timer is never armed). The
  `suppressNextDiffRef` groundwork this plan adds now carries the restored row's identity key, not a bare
  boolean, so it can never swallow an unrelated new row when Plan 14 consumes it (ROUND 6/X3). The sticky-error
  guard's own fold-deferral (ROUND 8) means a SINGLE removal that arrives during the sticky window is never
  permanently lost on either exit route (dismiss or a successful retry); if MORE THAN ONE arrives during the
  same freeze, only the newest surfaces its own toast, a disclosed narrowing (ROUND 9), not a silent data loss —
  the underlying `.ilf` removal always succeeds regardless (full arithmetic per R9 review BB13: such a freeze
  strands the non-newest arrivals AND — if the sticky entry exits via dismiss — the sticky entry itself, i.e.
  two-plus entries left in `pending` with no remaining UI surface) — closing the plan-check blocker on Z1's "never
  silently superseded" guarantee. ROUND 9 additionally ensures the sticky display can never be silently
  dismissed by its own inherited timer while unresolved. R9: a remount DURING a freeze reconstructs the
  frozen baseline (sticky entry inclusive) and re-arms the thaw priority, so a single most-recent freeze
  arrival can no longer be silently lost to an unmount/remount (BB3); retries never pre-clear the sticky
  slot, so no interleaved commit can prematurely thaw the guard (BB9); the mount pick is deterministic with
  an asserted invariant (BB22).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI → decorationPersistOrchestrator (removeDecorationRow) → disk (+ an unreachable-this-phase live-despawn code path) | A single row-level UI click triggers a disk write (data removal); the conditional live-despawn branch exists but is never invoked with a non-null id by any caller this phase ships (C4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-13a | Repudiation | destructive remove with no confirm dialog | mitigate | D-03's explicit design: the undo toast (8s window, same precedent as the delete-project flow) is the guard, not a confirm dialog — the stock file is NEVER touched (D-02), so the removal is always reversible by re-adding the exact removed node WITHIN the toast window (Sonnet F11 — after the window, manual re-add via the Add flow is required, not automatic undo), bounding the real risk of a no-confirm action. **(ROUND 9 disclosed narrowing, AA3):** EXCEPT when a removal arrives during ANOTHER entry's sticky-error freeze and is not the most-recently-arrived one during that freeze (see must_haves and the ROUND 9 revision above) — that ONE entry's own undo-toast affordance is lost (its `.ilf` removal remains durable and reversible only via a manual re-add through the Add flow, never automatic Undo) — a rare, accepted narrowing of D-03's guard, disclosed here and in Plan 15's gap ledger item (k), not silently shipped as an unconditional guarantee — and the SECOND disclosed D-03 narrowing, the plain-remount toast loss (ledger item (g)), is likewise named in this plan's must_haves' EXCEPT clauses per R9 review BB8/BB14. |
| T-05.1-13b | Denial of Service | a failed live despawn blocking the data removal | mitigate | Explicit design (Task 1 behavior spec): sendDespawnNode is wrapped in try/catch and never blocks or reverts the already-committed data removal — the data path is the durable contract, live despawn is best-effort (and, per C4, currently unreachable in practice — this mitigation guards the mechanism's future activation, not a live risk today). |
| T-05.1-13c | Tampering | none new | accept | removeDecorationRow reuses Plan 01's already-hardened assembleDecorationEdit (sanitizeId, fail-closed resolution) with zero new path-construction logic. |
| T-05.1-13d | Denial of Service | Remove and Undo each resolving `overrideDir`/`readVfs`/`studioDir` a second, independent way (ROUND 4/W3; ROUND 5/V6) | mitigate | Both now call Plan 10's ONE shared, null-safe `resolveOverridePair()` helper (which itself now guards `studioDir` before `overrideDir`) — a `null` result is a no-op with no disk touch, never an unguarded `string | null` reaching a `string`-typed parameter. |
| T-05.1-13e | Repudiation | RemoveUndoToast (a sibling file) inventing a second, unguarded write-path resolution to implement Undo (the ROUND 4 design defect, ROUND 5/V1) | mitigate | RemoveUndoToast.tsx is now purely presentational (`onUndo` prop only, matching `DeleteUndoToast.tsx:146-166`'s shell / `:96-101`'s `handleUndo`, ROUND 6/X5 citation fix); WorldPanel.tsx owns the entire resolve-then-restore-then-refresh sequence via the SAME `resolveOverridePair()` every other write action in this file uses — no second resolution path exists to invent. |
| T-05.1-13f | Repudiation | a failed Undo producing zero user-visible feedback, presenting as a false success, or having its failure signal erased by an unrelated entry's Undo (ROUND 6, X4; ROUND 7, Z1); a removal arriving during a sticky freeze being silently and permanently folded away before it can ever be shown (ROUND 8) | mitigate | `removeUndoStore.undoErrors` (Task 1, per-entry-keyed Map) gives `RemoveUndoToast` an observable, per-entry signal, set on every `handleUndo` failure branch (null pair, missing building, thrown add-back) and cleared ONLY for the entry being retried — never a different entry's slot. The thrown-add-back branch can no longer present a false 'restored' toast because `restore(entry.id)` now runs only AFTER the add-back succeeds (ROUND 7/Z1 — the entry is never popped from `pending` on a failure, so the pending-diff effect never sees a spurious "left pending" event). A sticky-display guard keeps a failed entry's error toast visible until the user explicitly dismisses or retries it, so it can never be silently superseded by an unrelated new removal. **(ROUND 8)** The guard's own fold-deferral (skipping `prevRef.current = pending` while sticky, on both the dismiss and retry-success exit routes, and widening the effect's dependency array to also react to `undoErrors`, not only `pending`) is what makes "never silently superseded" also mean "never SILENTLY lost" — a naive near-verbatim copy of `DeleteUndoToast.tsx`'s unconditional trailing fold would have silently absorbed an unseen removal into the diff's baseline before it was ever shown, on either exit route. **(ROUND 9)** Two further gaps closed: (1) the sticky display's own inherited 8-second auto-dismiss timer (`DeleteUndoToast.tsx`'s `armTimer`, `:61-64`/`:109`) is NEVER armed while sticky — `clearTimer()` only, no `armTimer()` — so the toast cannot vanish via its own timeout regardless of the fold-deferral fix, which would otherwise orphan the entry's `undoErrors` slot with no remaining UI surface; (2) if MORE THAN ONE removal arrives during a single freeze, only the newest surfaces its own toast — a DISCLOSED narrowing (not a silent data loss; the earlier entries' `.ilf` removals already succeeded and remain committed) rather than a claim that every queued removal always gets its own toast turn. **(R9)** The mount initializer reconstructs the frozen baseline during a sticky freeze (BB3 — a remount can no longer fold an unshown freeze arrival into the seen set) and retries never pre-clear the sticky slot (BB9 — no interleaved commit can transiently open the guard), so neither a remount nor render timing can bypass the guard-and-fold discipline. |
</threat_model>

<verification>
`npm -w @swg/renderer run test -- removeUndoStore decorationPersistOrchestrator RemoveUndoToast WorldPanel`
green; `tsc --noEmit` clean for packages/renderer.
</verification>

<success_criteria>
Any decoration is removable from the World panel, guarded by an undo toast, no confirm dialog — EXCEPT the
ROUND 9 disclosed narrowing (AA3): a removal that arrives during ANOTHER entry's sticky-error freeze and is
NOT the most recent such arrival never gets its own undo-toast turn (see this plan's must_haves and Plan 15's
gap ledger item (k)) — and EXCEPT D-03's second disclosed narrowing, the plain-remount toast loss (ledger
item (g); R9 review BB8/BB14): navigating away from the World tab mid-window after a plain Remove forfeits
that toast's remaining window on return (R10 review, CC10 — this success-criteria site previously carried
only (k)'s EXCEPT clause while `:20`/`:22` correctly carried both); the data
removal is ALWAYS durable, and reversible within the 8-second toast window for every OTHER removal (Sonnet F11's scoped claim); the
live-despawn mechanism is implemented and unit-tested but honestly documented as unreachable by any caller
this phase ships (C4 — no fabricated "opportunistic" claim). SC4's REMOVE path is closed. ROUND 3:
`removeDecorationRow` sources its mandatory `buildingTemplateVfsPath` from the durable per-project map and
fails closed with words when it's unknown (R3); the caller builds a real `overrideDir`/`readVfs` (R4); the
despawn call site's `mappingName` is a real parameter, not an undeclared variable (R7). ROUND 4: Remove and
Undo both resolve `overrideDir`/`readVfs` through ONE shared, null-safe `resolveOverridePair()` helper, never a
second unguarded resolution (W3); Undo's re-add ALSO triggers `refreshTree()`, so the tree — and Plan 14's
content-identity "(NEW)" diff — never operates on stale data after a restore (W2). ROUND 5: RemoveUndoToast.tsx
is purely presentational and WorldPanel.tsx owns the entire Undo restore end-to-end via a required `onUndo`
prop, making D-03's undo guarantee actually implementable (V1 — the round's anchor finding); the Remove
click handler's `mappingName` read uses the narrowed status union (V5); `resolveOverridePair()` guards
`studioDir` and returns it as part of the resolved pair, closing the `removeDecorationRow(studioDir, ...)`
call site's previously-omitted first argument (V6/V12); Undo's refresh sets a shared `suppressNextDiffRef`
groundwork Plan 14's content-identity diff consumes, closing the delayed-misattribution class one layer
deeper than W2 alone did (V2 groundwork). ROUND 6: `suppressNextDiffRef` now carries the restored row's
identity key rather than a bare boolean, so it can pre-consume exactly the restored row without ever
swallowing an unrelated genuinely-new row landed in the same window (X3); a failed Undo is never silently
invisible — `removeUndoStore.undoErrors` (per-entry keyed, ROUND 7/Z1) drives the toast's error display (X4 — the
timer half as corrected by ROUND 9: never re-armed while sticky); the
precedent citation is factually corrected — DeleteUndoToast takes zero props and calls its own store
directly, and RemoveUndoToast's design goes one step further by delegating the entire restore (X5); the
add-back's `building` is explicitly resolved from the tree by `entry.buildingId`, failing closed when absent
(X6); the ref-set-before-refresh ordering assertion is demoted to a static grep gate, since it has zero
observable runtime consequence until Plan 14 lands as its consumer (X7); Undo's history-silence (no
`recordPersistResult` entry, unlike Remove) is a COSMETIC completeness gap, free to fix later — NOT
load-bearing for any other fix in this plan set (ROUND 7/Z6 corrects X13's previously-false "load-bearing"
rationale) — and the D-06 tension (an Undo add-back IS a persist by D-06's own wording) is now named
explicitly rather than left implicit (X13).
ROUND 7: the X4 "never invisible" must_have is respecified into three independently-true guarantees — a
failed Undo can never present as a false success (the add-back helper now runs BEFORE `restore()` pops the
entry, so a throw never leaves the pending-diff effect free to fire a spurious "restored" toast, closing
Opus Y1) — never silently erases a DIFFERENT entry's failure (per-entry-keyed `undoErrors` + a
`clearUndoError(id)` that is scoped, never global, closing Sonnet L1) — and never silently superseded by an
unrelated new removal while unresolved (the sticky-error toast guard) (Z1); `suppressNextDiffRef` becomes a
queue rather than a single ref — an append-only spread-push proven by the static grep gate, so no push can
ever overwrite a not-yet-drained entry (Z5; reworded per AA8 + R9 review BB15 — sequential Undos are the
only UI-reachable case); stale citations (DeleteUndoToast's shell/union line ranges, the
false "mounted at its own panel's top level" comparison) are corrected (Z10/Z11/Z12/Z17).
ROUND 8 (plan-check blocker fix): the Z1 sticky-error guard's own mechanism previously could not deliver its
stated "advances to the next queued removal" acceptance criterion, because a near-verbatim copy of
`DeleteUndoToast.tsx`'s unconditional trailing `prevRef.current = pending` fold would silently absorb a
removal arriving during the sticky window before it was ever shown, on both exit routes (dismiss and a
successful retry); the effect now defers that fold until the sticky guard actually clears (widening its
dependency array to also react to `undoErrors`, not only `pending`), and, on the tick immediately following a
thaw, prioritizes surfacing any unseen queued removal over a bare "restored" confirmation — closing the gap
on both exit routes, not only dismiss, as the checker's fix required.
ROUND 9 (revision — inherited-timer gap + disclosed narrowing found reviewing ROUND 8's fix against
`DeleteUndoToast.tsx` ground truth): the sticky error display's own auto-dismiss timer is now NEVER armed while
sticky (`clearTimer()` only — `DeleteUndoToast.tsx`'s `armTimer`, `:61-64`, and its unconditional call from
`handleUndo`'s catch block, `:109`, are deliberately not mirrored here), closing a THIRD, undisclosed exit route
where the sticky toast would otherwise silently self-dismiss 8 seconds after appearing, orphaning its
`undoErrors` slot; when MORE THAN ONE removal arrives during a single sticky freeze, only the most recent
surfaces its own undo toast on thaw — a disclosed, accepted narrowing of the existing single-slot toast design,
not a defect — earlier queued removals keep their already-successful data-only removal but must be manually
re-added via the Add flow to restore them, the same accepted-after-expiry framing Sonnet F11 already
established.
R9 (REVIEWS.md round 9): the project-switch reset moves into each store's own synchronous workspaceStore
subscription (Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT — reset-before-render), making the
false-'restored'-on-switch race structurally impossible rather than effect-ordering-dependent (BB2); a
remount during a sticky freeze reconstructs the frozen `prevRef` baseline and re-arms `wasStickyRef`,
closing the fifth exit route that silently lost a single most-recent freeze arrival (BB3), with the mount
pick deterministic and its invariant asserted (BB22); `handleUndo` never pre-clears the sticky slot,
retiring AA14's load-bearing one-synchronous-turn assumption entirely (BB9); the D-03 hedges now cover BOTH
disclosed narrowings at every restatement site including VALIDATION.md (BB8/BB14), with the
multi-queued-freeze arithmetic stated in full (BB13); residual 'concurrent Undo' phrasing is retired
(BB15); the AA2 mount mechanism gains a five-case unmount/remount regression family plus the BB9
premature-thaw test (BB16); and the two round-label errors are corrected with the citation convention
pinned — 'R9 review, BBn' for review items; in-plan ROUND labels are historical (BB17).
</success_criteria>

<output>
Create `.planning/phases/05.1-live-world-editor-productization/05.1-13-SUMMARY.md` when done
</output>
