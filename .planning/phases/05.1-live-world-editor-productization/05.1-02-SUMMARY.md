---
phase: 05.1-live-world-editor-productization
plan: 02
subsystem: infra
tags: [workspace-json, projectBinding, contracts, per-project-persistence, world-editor]

# Dependency graph
requires:
  - phase: 05.1-live-world-editor-productization Plan 01
    provides: decorationPersist.ts kind-aware (edit|add|remove) assembly that Plan 06's
      orchestrator will call with buildingTemplateVfsPath — the field this plan's durable
      map exists to backfill for the offline Remove path
provides:
  - WorkspaceBindingMeta.mirrorToStockIlf (D-08 per-project mirror-mode override)
  - WorkspaceBindingMeta.worldEditorBookmarks (D-07 saved World-panel teleport bookmarks)
  - WorkspaceBindingMeta.worldEditorBuildingTemplates (R3 durable stock buildingTemplateVfsPath
    map, keyed by sanitized building id)
  - Proven read/write/round-trip/backward-compat/atomicity coverage for all three fields via
    the existing updateWorkspaceMeta/readWorkspaceJson idiom
affects: [05.1-04 (offline Remove scan), 05.1-06 (orchestrator that populates the map on live
  capture), 05.1-10 (World panel bookmarks + mirror-mode UI), 05.1-13 (offline Remove reader)]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - packages/contracts/src/workspace.ts
    - packages/renderer/src/services/projectBinding.test.ts

key-decisions:
  - "No new persistence mechanism — all three fields are optional additions to the existing
    WorkspaceBindingMeta interface, written/read through the SAME atomic tmp+rename
    workspace.json idiom liveClientExe already established (same-day precedent, D-08/D-07)."
  - "worldEditorBuildingTemplates merge is the CALLER's responsibility (spread the previous
    read before adding a key) — updateWorkspaceMeta remains a shallow patch like every other
    field, per its existing contract; proven by a two-call sequential-merge test."
  - "readWorkspaceJson stays a pure passthrough — no default resolution at the read layer for
    mirrorToStockIlf (absent means 'true', but that default is resolved by the CALLER per
    D-08's contract, not injected here)."

patterns-established: []

requirements-completed: [pivot-driven]

# Metrics
duration: ~10min
completed: 2026-08-01
---

# Phase 05.1 Plan 02: Per-Project World Editor Persistence Surface Summary

**Extended `WorkspaceBindingMeta` with three optional fields (mirror-mode override, teleport
bookmarks, durable stock-building-template map) through the existing atomic `workspace.json`
idiom — zero new persistence code, proven by round-trip + backward-compat + atomicity tests.**

## Performance

- **Duration:** ~10 min (single session)
- **Started:** 2026-08-01T19:00Z (approx)
- **Completed:** 2026-08-01T19:04:17-05:00
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- `WorkspaceBindingMeta` now carries `mirrorToStockIlf?`, `worldEditorBookmarks?`, and
  `worldEditorBuildingTemplates?` — the exact three fields specified in this plan's ROUND 3
  revision (R3: the building-template map is a durable memory an OFFLINE Remove action cannot
  re-derive from override-dir bytes alone, since `deriveBuildingTemplate`'s DERV/base chunk
  names a generic shared base template, never the stock building's own VFS path).
- `@swg/contracts` rebuilt (`tsc`); `dist/workspace.d.ts` confirmed to expose all three new
  fields; `tsc --noEmit -p packages/contracts` clean.
- Five new test cases in `projectBinding.test.ts` prove: single-field round-trip
  (`mirrorToStockIlf`), array round-trip with no precision loss (`worldEditorBookmarks`),
  two-call merge-by-spread round-trip (`worldEditorBuildingTemplates`), backward-compat on a
  pre-existing file missing the new keys (no throw, fields simply `undefined`), and write
  atomicity (no `.tmp` file survives a successful `updateWorkspaceMeta` call).
- No new functions were added to `projectBinding.ts` — `writeWorkspaceJson` /
  `readWorkspaceJson` / `updateWorkspaceMeta` already handle any `WorkspaceBindingMeta` shape
  generically, exactly as the plan specified.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend WorkspaceBindingMeta (mirror toggle + bookmarks) and rebuild contracts** - `a5f9964` (feat)
2. **Task 2: projectBinding.ts read/write coverage for the two new fields** - `a352cb1` (test)

**Plan metadata:** (this commit)

_Note: Task 1 is `tdd="true"` but required no test-file change of its own — it's a pure type
addition consumed generically by the ALREADY-TDD'd `projectBinding.ts` helpers, so its
verification is the `tsc`/build gate plus Task 2's behavioral proof against the new shape._

## Files Created/Modified

- `packages/contracts/src/workspace.ts` - Added `mirrorToStockIlf?`, `worldEditorBookmarks?`,
  `worldEditorBuildingTemplates?` to `WorkspaceBindingMeta`, doc-commented in the same style as
  the existing `liveClientExe` precedent; contracts package rebuilt.
- `packages/renderer/src/services/projectBinding.test.ts` - Added a `describe`
  ('updateWorkspaceMeta — world editor fields (05.1-02)') with 5 new test cases (5a-5e)
  covering round-trip, array precision, map-merge, backward-compat, and write atomicity for the
  three new fields; added `updateWorkspaceMeta` to the existing import.

## Decisions Made

- No new persistence mechanism — reused `writeWorkspaceJson`/`readWorkspaceJson`/
  `updateWorkspaceMeta` verbatim, per the plan's explicit "no new file format, no new
  persistence mechanism" objective.
- `worldEditorBuildingTemplates` merging is the caller's job (shallow-patch contract preserved);
  proven with a sequential two-call test rather than adding deep-merge logic to
  `updateWorkspaceMeta`.
- `readWorkspaceJson` remains a pure passthrough with no default injection for
  `mirrorToStockIlf` — default resolution (`true` when absent) is deferred to the future caller
  (Plan 06/10), matching D-08's "read at persist time" contract stated in the plan.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched their `<action>`/`<behavior>`
specs precisely; no Rule 1-4 auto-fixes were needed.

## Issues Encountered

- The plan's verification commands were written as `npm -w @swg/contracts run build` /
  `npm -w @swg/renderer run test -- projectBinding`, but this repo is a pnpm workspace (no npm
  workspaces configured — confirmed via `pnpm-workspace.yaml` + npm's "No workspaces found"
  error). Ran the equivalent `pnpm --filter @swg/contracts run build` and
  `pnpm --filter @swg/renderer run test -- projectBinding` instead (same intent, correct tool
  for this repo's package manager) — not a deviation from the plan's *behavior*, just its
  literal command syntax.
- `@swg/contracts` has no `test` script (types-only package, no runtime code) — the plan's
  combined `build && test` verification command was satisfied by the build step plus a
  `tsc --noEmit` clean check and a direct `dist/workspace.d.ts` grep confirming the three new
  fields are present in the built output.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

The per-project persistence surface Plan 06 (orchestrator), Plan 10 (World panel UI), and
Plan 13 (offline Remove) all depend on is now settled, built, and tested:
- Plan 06 can call `updateWorkspaceMeta(studioDir, { worldEditorBuildingTemplates: { ...merge } })`
  on every successful live edit/add capture.
- Plan 10 can read/write `mirrorToStockIlf` and `worldEditorBookmarks` for the World panel UI.
- Plan 13's offline Remove can read `worldEditorBuildingTemplates[sanitizedBuildingId]` to
  recover a stock `buildingTemplateVfsPath` it cannot re-derive from override-dir bytes.

No blockers. Ready for the next plan in the wave.

---
*Phase: 05.1-live-world-editor-productization*
*Completed: 2026-08-01*

## Self-Check: PASSED

All claimed files and commits verified present:
- FOUND: packages/contracts/src/workspace.ts
- FOUND: packages/renderer/src/services/projectBinding.test.ts
- FOUND: .planning/phases/05.1-live-world-editor-productization/05.1-02-SUMMARY.md
- FOUND: a5f9964 (feat: extend WorkspaceBindingMeta)
- FOUND: a352cb1 (test: projectBinding round-trip coverage)
- FOUND: ed7cabc (docs: SUMMARY)
