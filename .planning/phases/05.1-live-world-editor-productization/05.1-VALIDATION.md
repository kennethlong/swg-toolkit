---
phase: 05.1
slug: live-world-editor-productization
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-31
revised: 2026-07-31
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Detailed per-criterion validation architecture lives in `05.1-RESEARCH.md` §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (renderer + packages) + native round-trip gate for `.ilf` |
| **Config file** | per-package `vitest.config.ts` (project convention) |
| **Quick run command** | `pnpm --filter <package> test` |
| **Full suite command** | `pnpm -r test` |
| **Estimated runtime** | ~60–120 seconds (renderer suite) |

---

## Sampling Rate

- **After every task commit:** Run the touched package's `pnpm --filter <pkg> test`
- **After every plan wave:** Run `pnpm -r test` + `tsc --noEmit`
- **Before `/gsd:verify-work`:** Full suite green + `.ilf` byte-exact round-trip gate green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

*Populated by the planner as tasks are defined. Every `.ilf` mutation (append/delete) task MUST
map to a byte-exact round-trip assertion on a real `.ilf`; every in-world behavior maps to a live
checkpoint (`checkpoint:human-verify`).*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01 | 0 | SC1 | T-05.1-01a/b | REBIND_REFUSED never leaks as a raw code | unit | `npm -w @swg/renderer run test -- decorationChannel` | ✅ extend | ⬜ pending |
| 01-T2 | 01 | 0 | SC4 | T-05.1-01a | addNode/removeNode fail-closed, byte-exact round-trip | unit | `npm -w @swg/renderer run test -- ilf` | ✅ extend | ⬜ pending |
| 01-T3 | 01 | 0 | SC4/SC5 | T-05.1-01a | kind-aware assembly, zero regression on proven edit path | unit | `npm -w @swg/renderer run test -- decorationPersist` | ✅ extend | ⬜ pending |
| 01-T4 | 01 | 0 | SC4 (ground-truth mandate) | T-05.1-01c | CORE-05 standing gate: .ilf registered with cited real-or-recipe fixture | round-trip (CORE-05) | `npx vitest run packages/harness/test/ilf-roundtrip.test.ts packages/harness/test/registry-coverage.test.ts` | 🆕 new | ⬜ pending |
| 02-T1 | 02 | 0 | SC3/D-08 | — | WorkspaceBindingMeta extension, atomic write preserved | unit | `npm -w @swg/contracts run build && npm -w @swg/contracts run test` | ✅ extend | ⬜ pending |
| 02-T2 | 02 | 0 | SC3/D-07 | — | projectBinding read/write round-trips the two new fields | unit | `npm -w @swg/renderer run test -- projectBinding` | ✅ extend | ⬜ pending |
| 03-T1 | 03 | 0 | SC4/D-01 | T-05.1-03c | CAPTURE_CELL_NAME fixed-length + truncation discipline | build (type-check) | `npm -w @swg/contracts run build` | n/a (contract-only) | ⬜ pending |
| 03-T2 | 03 | 0 | SC4/D-07/D-02 | T-05.1-03a/b | HOST_CMD fixed-size string slots, unknown-action fail-closed | build (type-check) | `npm -w @swg/contracts run build` | n/a (contract-only) | ⬜ pending |
| 03-T3 | 03 | 0 | D-10 | — | MIRROR_OFF bit purely additive, APPLY/ABORT unchanged | build (type-check) | `npm -w @swg/contracts run build` | n/a (contract-only) | ⬜ pending |
| 04-T1 | 04 | 0 | SC2/D-05 | T-05.1-04a | malformed edit_*.ilf isolates to one orphaned row, never aborts scan | unit | `npm -w @swg/renderer run test -- worldEditorScan` | 🆕 new | ⬜ pending |
| 04-T2 | 04 | 0 | SC2/D-06/D-12/D-10 | — | session-only history, badge logic, formatPersistMessage exact-string contract | unit | `npm -w @swg/renderer run test -- worldEditorStore` | 🆕 new | ⬜ pending |
| 05-T1 | 05 | 1 | SC1 | — | CONSULT-69 probe + raw-code line fully removed | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 05-T2 | 05 | 1 | SC1/D-10/D-11/D-12/D-13 | T-05.1-05a/c | F/G/R gated on WantCaptureKeyboard; strip never formats a raw code; D-10 mirror-off stash isolated to one line | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 06-T1 | 06 | 1 | SC3/D-08/D-09/D-10 | T-05.1-06a/b | mirror-off reconciled across ALL edited buildings on flip; MIRROR_OFF flag folded into the SAME writeRebind call | unit | `npm -w @swg/renderer run test -- decorationPersistOrchestrator decorationPersist` | ✅ extend | ⬜ pending |
| 07-T1 | 07 | 1 | SC4/D-07/D-02 | — | agent-side HOST_CMD read/ack | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 07-T2 | 07 | 1 | SC4/D-07/D-02 | — | host addon N-API export | build (N-API compile) | `npm -w @swg/live-inject run build` | n/a (build) | ⬜ pending |
| 08-T1 | 08 | 2 | SC4/D-07/D-02 | T-05.1-08a/b | HOST_CMD payloads are pure IPC, never path/shell input; monotonic epoch | unit | `npm -w @swg/renderer run test -- hostCommand` | 🆕 new | ⬜ pending |
| 08-T2 | 08 | 2 | SC4/D-07/D-02 | — | HOST_CMD result observed exactly once per epoch | unit | `npm -w @swg/renderer run test -- useChannelReader` | ✅ extend | ⬜ pending |
| 08-T3 | 08 | 2 | SC1/SC4/D-10/D-12 | T-05.1-08c | fresh edit/add persist RESULT recorded into worldEditorStore exactly once per epoch, words-not-codes message, failure sets hasFailureBadge | unit | `npm -w @swg/renderer run test -- useChannelReader` | 🆕 new | ⬜ pending |
| 09-T1 | 09 | 2 | SC4/D-02 | — | wsRemoveNode bound, scoped to WS-authored nodes only | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 09-T2 | 09 | 2 | SC4/D-07/D-02 | — | per-action HOST_CMD dispatch, unknown action fail-closed | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 10-T1 | 10 | 2 | SC2 | — | 5-place dockview registration, LAYOUT_VERSION guard | unit | `npm -w @swg/renderer run test -- WorkspaceShell workspace-config` | ✅ extend | ⬜ pending |
| 10-T2 | 10 | 2 | SC2/SC3/D-08 | T-05.1-10a/b | reconcileMirrorMode failure never crashes panel; D-08 disabled per-instance option present | component | `npm -w @swg/renderer run test -- WorldPanel` | 🆕 new | ⬜ pending |
| 11-T1 | 11 | 3 | SC1/SC2/D-06/D-10 | — | Activity accordion renders full message text, never truncated (D-10 App-side) | component | `npm -w @swg/renderer run test -- WorldPanel` | ✅ extend | ⬜ pending |
| 11-T2 | 11 | 3 | SC2/D-07 | T-05.1-11a/b | teleport coords parsed as numbers before channel write; no confirm-less accidental scene swap | component | `npm -w @swg/renderer run test -- WorldPanel` | ✅ extend | ⬜ pending |
| 12-T1 | 12 | 3 | SC4/D-01 | — | placement-mode ghost/reticle state | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 12-T2 | 12 | 3 | SC4/D-01 | — | click-to-spawn auto-arm, post-save temp-node cleanup (no double-spawn) | build (agent compile) | `cmake --build packages/live-inject/agent/build-agent --config Release` | n/a (build) | ⬜ pending |
| 13-T1 | 13 | 4 | SC4/D-02/D-04 | T-05.1-13b/c | despawn failure never blocks/reverts the already-committed data removal | unit | `npm -w @swg/renderer run test -- removeUndoStore decorationPersistOrchestrator` | 🆕 + ✅ extend | ⬜ pending |
| 13-T2 | 13 | 4 | SC4/D-02/D-03 | T-05.1-13a | no confirm dialog; undo toast is the sole guard | component | `npm -w @swg/renderer run test -- RemoveUndoToast WorldPanel` | 🆕 + ✅ extend | ⬜ pending |
| 14-T1 | 14 | 5 | SC4/D-01 | — | template search + thumbnail grid | component | `npm -w @swg/renderer run test -- AddDecorationModal` | 🆕 new | ⬜ pending |
| 14-T2 | 14 | 5 | SC4/D-01 | — | two-surface confirm; empty-cell degrade reads as words, not a code | component | `npm -w @swg/renderer run test -- WorldPanel` | ✅ extend | ⬜ pending |
| 15-T1 | 15 | 6 | D-04/A1 | T-05.1-15a | change-request handoff cites exact ground truth, never edits the sibling repo directly | shell check | `test -f ".planning/handoff/"*"-CHANGE-REQUEST-getContainingCellName.md"` | n/a (doc) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. Populated 2026-07-31 revision from the 15 plans'
own `<automated>` verify commands — one row per task as of that revision (32 rows). **Known-stale as of the
2026-08-01 round-5 replan:** the review-round replans grew the plan set to 47 automated tasks + 6 checkpoint
tasks (01-T5, 05-T3, 09-T3, 11-T3, 12-T3, 15-T2 — manual-only, tracked in the table below); tasks added after
2026-07-31 are not yet rowed here. The plans' own `<automated>` commands are the source of truth (round-5
plan-checker re-verified every automated task carries a scoped command); refresh this map at execute time.
Statuses flip to ✅/❌ as `/gsd:execute-phase` runs each task's verify command. Row 08-T3 added in the same-day follow-up revision that
closed the missing base edit/add persist RESULT → worldEditorStore wiring (Plan 08 Task 3).*

---

## Wave 0 Requirements

Wave 0 = Plans 01-04 (no dependencies, run in parallel). All four are covered by this plan set:

- [x] `ilf.test.ts` — extend existing byte-exact round-trip fixtures to cover row APPEND and row DELETE
      (Plan 01 Task 2)
- [x] `decorationChannel.test.ts` — add the missing `REBIND_REFUSED` label case (Plan 01 Task 1)
- [x] `decorationPersist.test.ts` — extend for kind-aware (edit/add/remove) assembly (Plan 01 Task 3)
- [x] `.ilf` registered in the CORE-05 standing gate (`packages/harness/fixtureRegistry.ts`) with a
      real-or-independently-encoded fixture — the project's ground-truth mandate, previously a gap (Plan 01
      Task 4, added in the 2026-07-31 revision closing the checker's Blocker 1)
- [x] `WorkspaceBindingMeta` extension + `projectBinding.ts` coverage for `mirrorToStockIlf`/bookmarks
      (Plan 02)
- [x] Wire-contract extensions (CAPTURE kind/cellName, LIVE_HOST_CMD region, REBIND_FLAGS.MIRROR_OFF) defined
      before any consumer touches them (Plan 03)
- [x] `worldEditorScan.ts`/`worldEditorStore.ts` — disk-scan tree + session-only history +
      `formatPersistMessage` (D-10) (Plan 04)

*Existing test infrastructure (vitest + the CORE-05 native round-trip harness) covers the rest; Wave 0
establishes every data/contract primitive later waves build against.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Hover→arm→move/rotate/persist from status-strip HUD | SC1 | Requires a live in-world client + agent inject | Attach to advertised client, hover a decoration, F-arm, G-move/R-rotate, Persist; confirm words-not-codes feedback |
| Rotation edit persists + reloads correctly | SC5 | Live-fire proof only (code support exists, no live proof yet) | Rotate a decoration, Persist, reload scene, confirm rotation survived byte-verified `.ilf` |
| ADD a new decoration end-to-end survives scene reload | SC4 | Live `wsAddObject` spawn + `.ilf` append + reload | 021-A wizard pick → ghost place → Persist → reload → new row present |
| REMOVE a decoration (live despawn vs data-only degrade) | SC4 | Live despawn only for this-session `wsAddObject` nodes; stock = data-only | Remove a session-added deco (live despawn) and a stock deco (reload-only), confirm both persist |
| World panel matches sketch 019-A element-for-element | SC2 | Visual observed/missing diff against sketch | Open World tab, diff every 019-A element (tree, detail card, accordions, mirror toggle, live strip, scene launcher) |
| D-10 mirror-off hybrid-session warning (both surfaces) | D-10 | Requires a live client + a project with mirror toggled off; the in-game strip variant and the App-side history line must be checked TOGETHER against the SAME persist | Toggle mirror OFF in the World panel, persist an edit; confirm the strip reads "saved (not visible here)" AND the Activity accordion carries the full "— mirror off — not visible on hybrid sessions until reload into an editor scene" detail (Plan 15 Task 2, step 10 — ROUND 7/Z9 citation fix; a later round moved this check from step 9 to step 10) |
| D-08 disabled per-instance scope control is visible | D-08 | Visual check against the locked decision text, not just the sketch (019-A itself lacks this element — a known sketch/decision gap resolved in the decision's favor) | Open the World tab's mirror-mode row; confirm "Per-instance (server repoint)" is VISIBLE, disabled, with a "coming later" hint, alongside the active "Per-template" option (Plan 10 Task 2, automated component-test-covered; this row is a visual spot-check only) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — 47/47 automated tasks carry an
      `<automated>` command (re-verified against the plans themselves by the round-5 plan-checker, 2026-08-01;
      the Per-Task Verification Map above predates the round 3–5 replan additions and needs a refresh pass at
      execute time); the 6 checkpoint tasks (01-T5, 05-T3, 09-T3, 11-T3, 12-T3, 15-T2) are
      `type="checkpoint:human-verify"` by design and are covered in the Manual-Only table instead, per the
      Nyquist rule's own exception for genuinely non-automatable in-world behavior.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — re-verified against all 15 plans
      by the round-5 plan-checker (2026-08-01): every checkpoint is preceded by automated tasks in its own
      plan, never 3 non-automated in a row anywhere in the set.
- [x] Wave 0 covers `.ilf` append/delete round-trip + REBIND_REFUSED label + the CORE-05 real-fixture
      registration (Plan 01 Tasks 1-4) — see Wave 0 Requirements above, all four Wave-0 plans checked.
- [x] No watch-mode flags — every `<automated>` command in the table above uses `run`/`test --`/`vitest run`,
      never `--watch`.
- [x] Feedback latency < 120s — every automated command is a scoped test/build run (`-- <module>` filters or a
      single-package build), consistent with the Sampling Rate section's per-task budget.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** signed off — 2026-07-31 revision (blockers B1-B3 and warnings W1-W2 from the checker's review
addressed; see the corresponding PLAN.md diffs for B1 → 05.1-01 Task 4/5, B2 → 05.1-03 Task 3 + 05.1-05 Task 2
+ 05.1-06 Task 1 + 05.1-04 Task 2 + 05.1-11 Task 1 + 05.1-15 checkpoint step 10 (ROUND 7/Z9: corrected from
step 9, which a later round's insertion shifted), B3 → 05.1-10 Task 2, W1 → this
document's RESEARCH.md companion, W2 → this document). **Same-day follow-up revision (2026-07-31):** closed
the one remaining blocker — no plan wired the base edit/add decoration persist RESULT into
`worldEditorStore.recordPersistResult`, breaking D-10's App-side mirror-off detail, D-12's attention badge, and
SC4's ADD two-surface confirm. Fixed via new 05.1-08 Task 3 (row 08-T3 above) plus a correction to 05.1-14 Task
2's behavior text, which had falsely claimed this wiring already existed.
