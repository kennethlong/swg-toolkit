---
phase: 05.1-live-world-editor-productization
plan: 14
type: execute
wave: 5
depends_on: ["05.1-13", "05.1-03", "05.1-12", "05.1-08", "05.1-04"]
files_modified:
  - packages/renderer/src/panels/world/AddDecorationModal.tsx
  - packages/renderer/src/panels/world/AddDecorationModal.test.tsx
  - packages/renderer/src/panels/world/WorldPanel.tsx
  - packages/renderer/src/panels/world/WorldPanel.test.tsx
autonomous: true
requirements: [pivot-driven]
must_haves:
  truths:
    - "A user can pick a decoration template from a searchable grid, hand it to the overlay for placement, and see both surfaces confirm once placed and persisted (SC4, 021-A)"
    - "The MVP cell-name resolution degrade (borrow an existing decoration's cellName from the target building) is real and honest — placement into a building with zero existing decorations is refused with words, never a silent failure (D-04)"
    - "The eligible-building set for ADD is scoped to buildings the World panel's disk-scan tree already tracks (i.e., previously-edited buildings with >=1 existing decoration) — NARROWER than RESEARCH.md's originally-recommended 'any building with >=1 decoration, including never-edited stock buildings' scope, because the tree (D-05) only scans the override dir. This narrowing is explicitly disclosed here and in Plan 15's sign-off, not silently shipped as if it were the wider scope (REVIEWS.md C6, Fable facet)"
    - "The freshly-added row is visually marked '(NEW)' in the World-panel tree until acknowledged/refreshed, matching 021-A Frame 3's two-surface confirm (Sonnet F9 / Cursor M5), keyed on CONTENT identity (buildingId+cellName+objectTemplateName), NOT a raw positional row id, so a reindex from Undo/Remove elsewhere in the tree can never be misread as a fresh placement (ROUND 4, W2 — HIGH, Sonnet) — EXCEPT the disclosed X8 case below (a mid-flow tab navigation/remount during the ADD round trip resetting the component-local diff baseline), where the marking can be silently swallowed on that ONE in-flight ADD; that exception is scoped, disclosed, and recorded in Plan 15's gap ledger item (h), never a license to skip the marking in any OTHER case (ROUND 7, Z2 — HIGH, Opus, applying the Plan 06 :17/:27 'EXCEPT the disclosed …' construction here so this line cannot be read as contradicting the X8 must_have below)"
    - "The '(NEW)' diff pass runs on EVERY refreshTree()-triggered tree update (mount, manual refresh, mirror toggle, Remove, Undo, ADD-confirm) — not only on worldEditorStore.history growth — so prevTreeRef is never stale by the time a LATER unrelated event diffs against it (ROUND 5, V2 — HIGH, Sonnet, closing the 'delayed, misattributed (NEW)' class)"
    - "Undo's own refreshTree() call consumes Plan 13's suppressNextDiffRef groundwork — now a QUEUE of IDENTITY KEYS (buildingId+cellName+objectTemplateName), not a boolean and not a single nullable ref — to pre-consume EXACTLY the restored row(s) from the normal count-map diff: the restored row itself is never marked '(NEW)' even at the instant it reappears, but the diff still RUNS (never a blanket skip) and still correctly marks any OTHER, unrelated row that lands new in the SAME refresh window, and a SECOND concurrent Undo's identity key is never clobbered by a first, not-yet-drained one (ROUND 5, V2; ROUND 6, X3 — HIGH, Sonnet, closing the class where a blanket suppress could permanently swallow an unrelated genuinely-new row; ROUND 7, Z5 — MEDIUM, Sonnet, closing the class where a second concurrent Undo could clobber the first's unconsumed identity key)"
    - "prevTreeRef starts at a null sentinel and the first diff pass after mount seeds it without marking ANY row '(NEW)'; a building absent from the previous snapshot contributes ZERO '(NEW)' rows (a whole-building first observation, never a placement) — closing the 'every row (NEW) on first mount / first edit in a building' defect (ROUND 5, V3 — HIGH, Opus)"
    - "The content-identity '(NEW)' marker (and its prevTreeRef/suppressNextDiffRef baseline) is component-local to WorldPanel.tsx, NOT hoisted into worldEditorStore — a mid-flow tab navigation that unmounts/remounts the panel during the 021-A ADD round trip (modal to place-in-game to return) resets prevTreeRef to null, so the null-sentinel first-pass rule (V3) can silently swallow that in-flight ADD's '(NEW)' marking on the remounted panel's first diff pass. This is a disclosed, accepted scope narrowing for this phase, not a hidden defect (ROUND 6, X8 — MEDIUM, Opus; recorded in Plan 15's gap ledger item (h))"
    - "The placement toast/UI copy explicitly warns the user to click only within the SAME room as the borrowed decoration — the residual same-building-wrong-cell risk Plan 12's building-id guard cannot close (REVIEWS.md C6, Codex/Sonnet facets)"
    - "The history-triggered refresh after a fresh ADD persist calls Plan 10's shared refreshTree() helper (fresh readWorkspaceJson read), never a mount-time-cached meta/overrideDir pair, so the freshly-added row's buildingTemplateVfsPath is populated, not silently empty (ROUND-3-REVIEW R2, HIGH, Cursor)"
    - "The placement-active toast never shows unconditionally: it is deferred until Plan 08's sendStartPlacement-returned epoch is correlated against a matching worldEditorStore.lastHostCommandResult, and shows an honest refusal message (never the false 'active' claim) when the agent silently refused per Plan 12's g_capArmed/g_placementActive exclusivity guards (REVIEWS.md ROUND 10, AA4 — HIGH, Sonnet L1)"
    - "A project switch (a studioDir change) resets worldEditorStore and removeUndoStore to their initial empty state, and resets this component's own prevTreeRef/suppressNextDiffRef, so Project A's Activity log/failure badge/sticky Undo-error toast/suppress-queue never leak into Project B's session (REVIEWS.md ROUND 10, AA5 — HIGH, Sonnet L2)"
  artifacts:
    - path: "packages/renderer/src/panels/world/AddDecorationModal.tsx"
      provides: "021-A Frame 1 — the wizard modal"
      contains: "Place in game"
  key_links:
    - from: "packages/renderer/src/panels/world/WorldPanel.tsx"
      to: "packages/renderer/src/services/hostCommand.ts"
      via: "the modal's 'Place in game ▸' calling sendStartPlacement with a cellName borrowed from the target building's existing tree data"
      pattern: "sendStartPlacement\\("
sketch_elements:
  # 021-A Frame 1 (Browse — app modal), the last un-built piece of the wizard:
  - "modal head '+ Add decoration to {building} ({cell})'"
  - "search input filtering the template grid"
  - "template thumbnail grid (thumb, name, path per tile, selectable)"
  - "modal footer: preview note text, Cancel button, 'Place in game ▸' primary button"
  # 021-A Frame 3 (Persisted — both surfaces), the confirm this plan wires:
  - "two-surface confirm: a toast/status line AND a new row appearing in the World list, marked distinctly as new — EXCEPT the disclosed X8 mid-flow-remount case (Plan 15 gap ledger item (h)), where the marking on that ONE in-flight ADD's row can be silently lost on remount; every OTHER ADD still marks the row (ROUND 7, Z2)"
---

<objective>
Build 021-A's Frame 1 (the "+ Add decoration…" wizard modal) and wire it to the placement flow Plan 12 (agent
ghost/click) and Plan 08 (`sendStartPlacement`) already built, completing the spawn-decoration flow's app-side
half. The modal is a search + thumbnail grid over the mounted VFS's decoration-shaped entries (reusing
`useTreStore`'s already-mounted `vfsEntries` — no new asset-discovery service), matching 016's wizard-modal
chrome per the sketch's own "016-style picker" framing, scoped to decorations only (not 016's full type/derive/
Core3-scaffold workflow, which is out of scope here).

The target `cellName` for the placement — the one genuine provider gap RESEARCH.md identifies (`
CellProperty::getCellName()` is inline/unadvertisable) — is resolved via the MVP degrade RESEARCH explicitly
recommends: the target building must already have at least one existing decoration in the tree (its
`cellName` is borrowed from that existing row's disk data, no live provider call needed); a building with zero
existing decorations refuses placement with an honest, words-not-codes message ("this building has no
decorations yet — add one to an existing building first" or similar), rather than silently failing or
guessing a cell name. The permanent fix (a real provider shim resolving any cell) is filed as a change-request
handoff in Plan 15, per the cross-repo protocol — this plan does NOT edit `../swg-client-v2`.

**CROSS-AI REVIEW REVISIONS (2026-08-01, C6 — HIGH, split across three reviewer facets):**
- **Fable's facet (eligible-building set narrower than RESEARCH's own recommendation):** `worldEditorStore.
  tree` is populated ENTIRELY from `worldEditorScan.ts`'s disk scan of the override dir (D-05) — buildings that
  have NEVER been edited (no `edit_*.ilf` on disk yet) are simply absent from the tree, even if their STOCK
  `.ilf` (readable offline via the mounted VFS) already has >=1 decoration and could, per RESEARCH's own
  stated recommendation, supply a borrowable cellName. Building the stock-`.ilf`-fallback lookup RESEARCH
  envisioned would require the World panel to also enumerate and let the user select never-edited buildings —
  a materially larger scope addition (a new building-discovery UI + a new stock-scan code path) than this
  targeted revision should absorb this late in the phase. Per this plan's own scope-reduction rules (a
  genuine "missing UI to select an unedited building" gap, not laziness), this revision EXPLICITLY DISCLOSES
  the narrowing rather than silently shipping it: ADD is available only for buildings ALREADY visible in the
  World panel's tree (i.e., previously-edited ones with >=1 decoration) — Task 2's behavior spec and this
  plan's `must_haves` say so in these exact words, and Plan 15's sign-off checklist records it as an observed
  scope note, not a silently-passed check.
- **Codex/Sonnet's facet (wrong-cell/wrong-room risk within the borrowed building):** Plan 12 now adds a
  building-ID cross-check guard (refusing a click in the WRONG building), but nothing can verify the click
  landed in the CORRECT ROOM within a multi-cell building (no advertised endpoint resolves a click's cell
  name — that is the exact gap the Plan 15 change-request exists to close). Task 2 below adds an explicit,
  visible warning to the placement-active toast copy: "place only in the SAME ROOM as the decoration you
  selected — this building may have other rooms the toolkit can't tell apart yet." This does not eliminate
  the risk (no code fix is possible without the provider shim) but makes it an INFORMED risk, not a silent
  one.
- **Sonnet F9 / Cursor M5:** the "(NEW)" row marker was named in this plan's `sketch_elements` frontmatter in
  the original version but never appeared in any task's actual behavior/action text or test assertions. Task 2
  below adds it explicitly.

**ROUND 4 REVISION (2026-08-01, REVIEWS.md W2 — HIGH, Sonnet):** the original Task 2 tracked "(NEW)" via a
raw positional-id Set diff ("row ids present in this session's history-triggered refresh but not seen in the
PREVIOUS refresh's row-id set"). Because `worldEditorRowId` embeds the numeric `rowIndex`, ANY reindex of a
cell — not just this plan's own ADD, but Plan 13's Undo re-adding a row at the END of its cell (D-01's
append-only mandate) — shifts every later same-cell row's id, and a raw before/after id-Set diff would then
misread the RESHUFFLED (but not actually new) rows as "(NEW)" too. Task 2 below replaces the positional-id
diff with a count-map walk over the whole tree that SHARES ONLY the content-identity KEY (buildingId+
cellName+objectTemplateName) and the duplicate-ambiguity posture Plan 04's `refresh()` already established for
`sessionOverlay` reconciliation — **(ROUND 5, V9 — MEDIUM, Opus+Fable, honest-framing fix)** this plan does
NOT literally reuse `refresh()`'s routine: Plan 04 walks SPARSE overlay entries with a positional-nearest
tiebreak, while this plan walks the FULL new tree against a per-building count map with a consumption-order
tiebreak — a different algorithm for a different purpose (detecting excess content vs. reconciling existing
status), sharing only the identity key and the "duplicates are ambiguous" posture. The count-map mechanism
itself is fully defined in THIS plan's Task 2 — nothing is imported from Plan 04 for this purpose, and no
plan claims otherwise. A row is "(NEW)" only when its CONTENT has no matching occurrence in the
immediately-prior tree snapshot for that building, never merely because its numeric position changed.

**ROUND 5 REVISION (2026-08-01, REVIEWS.md V2/V3/V9):**
- **V2 (HIGH, Sonnet):** the ROUND 4 diff/`prevTreeRef` update ran ONLY when `worldEditorStore.history` grew.
  Remove grows history; Undo (Plan 13) does not — its own `refreshTree()` call never triggered a diff pass, so
  `prevTreeRef` went stale the instant Undo restored a row. The NEXT unrelated history-growth event anywhere in
  the session then diffed against that stale snapshot and mislabeled the ALREADY-RESTORED row "(NEW)" — the
  exact class W2 was filed to close, reopened one layer down. Task 2 below keys the diff effect off the `tree`
  reference itself (`useEffect(..., [tree])`), so EVERY `refreshTree()`-triggered update — mount, manual
  refresh, mirror toggle, Remove, Undo, ADD-confirm — advances `prevTreeRef` uniformly; `prevTreeRef` can never
  go stale between refreshes again. Undo's OWN update is a SILENT re-baseline (via Plan 13's
  `suppressNextDiffRef` groundwork, consumed and cleared here): the diff pass advances `prevTreeRef` to the
  restored tree WITHOUT computing/marking any "(NEW)" row, since a restore is never a fresh placement even at
  the instant it reappears — closing V2 at BOTH the "later misattribution" layer AND the "Undo's own instant"
  layer a pure content-diff would otherwise still get wrong (content genuinely reappearing looks identical to
  content genuinely appearing, to a diff with no other signal).
- **V3 (HIGH, Opus):** the ROUND 4 spec had no stated initial value or first-pass rule for `prevTreeRef`, and
  directed "a building absent from the previous snapshot contributes an empty count map — ALL of its rows are
  then genuinely new." Two reachable sequences fell out of this: every World-tab mount (tree goes `[] →
  populated`, marking the WHOLE tree "(NEW)"), and the first persist in any building (the whole building
  appears at once per D-05's disk-scan scope, marking every stock row "(NEW)"). Task 2 below initializes
  `prevTreeRef` to a `null` sentinel; the FIRST diff pass after mount seeds `prevTreeRef` from the current tree
  and marks NOTHING "(NEW)"; and REVERSES the absent-building rule — a building absent from the previous
  snapshot now contributes ZERO "(NEW)" rows (a whole-building first observation is never a placement).

**ROUND 6 REVISION (2026-08-01, REVIEWS.md X3/X7/X8):**
- **X3 (HIGH, Sonnet):** the ROUND 5 suppress branch (V2) treated `suppressNextDiffRef.current` as a boolean
  and, when `true`, skipped the diff PASS ENTIRELY for the whole tree — silently re-baselining `prevTreeRef`
  with zero comparison. Because `refreshTree()` is an unscoped full-override-dir rescan and Plan 08's
  poll-tick-driven RESULT wiring can land a DIFFERENT building's brand-new row on disk in the SAME window as
  an Undo click, a row first observed during THAT suppressed pass was folded into `prevTreeRef` undiffed —
  no later pass could ever recover its "(NEW)" marking (a one-way, silent loss of the two-surface-confirm
  signal). Plan 13's own ROUND 6 revision changes `suppressNextDiffRef` from a boolean to an IDENTITY KEY (`{
  buildingId, cellName, objectTemplateName } | null`) carrying the restored row's content identity. This
  plan's suppress branch below now consumes that identity key to PRE-CONSUME exactly the restored row from the
  SAME normal count-map diff (branch 3) — the diff still RUNS every time, it is never skipped — so any OTHER,
  unrelated new row landed in the same window is still correctly diffed and marked "(NEW)"; only the restored
  row's own content-identical tuple is absorbed by the pre-added credit.
- **X7 (MEDIUM, Opus):** the ROUND 5 test plan asked for a test to "pre-set `suppressNextDiffRef.current`" —
  but the ref is a never-exported `useRef` local to `WorldPanel.tsx`; a test has no legitimate handle on it
  without inventing a test-only escape hatch. Task 2's tests below are rewritten to drive the REAL Undo flow
  through the RENDERED `RemoveUndoToast` (seed `removeUndoStore.pending`, render `WorldPanel`, click the
  toast's Undo button) so the ref is set and consumed by the ACTUAL `handleUndo`/diff-effect code path, and
  assert the OBSERVABLE consequence at the DOM level (zero "(NEW)" markers on the Undo-triggered refresh, then
  a SUBSEQUENT genuine ADD still renders a "(NEW)" marker on its own row — proving the suppression was
  correctly scoped and consumed, not left permanently stuck).
- **X8 (MEDIUM, Opus):** disclosed, not fixed this phase (see the new must_have above and Plan 15's gap ledger
  item (h)) — the "(NEW)" marker's baseline (`prevTreeRef`) is component-local to `WorldPanel.tsx`, so a
  mid-flow tab navigation that unmounts/remounts the panel during the 021-A ADD round trip resets it, and the
  null-sentinel first-pass rule (V3) then silently swallows that in-flight ADD's "(NEW)" marking on remount.
  Hoisting the "(NEW)" set + baseline into `worldEditorStore` (which would ALSO close ledger item (g), Plan
  15's tab-navigation RemoveUndoToast-visibility gap, since both share the same "component-local state lost on
  remount" root cause, and would compose naturally with X3's identity-keyed suppression above) is the
  permanent fix, but is deferred as a future-phase improvement rather than absorbed into this already-large
  replan; this revision's job is disclosure, not a silent narrowing. **(ROUND 7, Z2 — HIGH, Opus)** this
  disclosure was previously added as a new must_have WITHOUT amending the OLD unconditional must_have/
  sketch_elements/Plan-15-step-4 lines it supersedes — those three sites now carry an explicit "EXCEPT the
  disclosed X8 case" clause (see this plan's must_haves above and Plan 15's must_haves/step 4), closing the
  contradiction Opus's Y2 finding identified.

**ROUND 7 REVISION (2026-08-01, REVIEWS.md Z2/Z4/Z5):**
- **Z2 (HIGH, Opus Y2):** the X8 disclosure above was added as a NEW must_have without amending the three OLD
  absolute-reading sites it supersedes — this plan's own must_have at `:19` (unconditional "(NEW)" marking),
  its `sketch_elements` line (`:42`, the authoritative-UI-contract line per AGENTS.md), and Plan 15's must_have
  (`:14`) and checkpoint step 4 (the 021-A observed/missing diff). All three (four, counting Plan 15's step 4)
  now carry an explicit "EXCEPT the disclosed X8 case" clause, mirroring the Plan 06 `:17`/`:27` "EXCEPT the
  disclosed …" construction this phase already established for exactly this situation (D-09's `diskState:
  'uncertain'` carve-out) — an executor can no longer satisfy an unqualified reading of the old absolute while
  ignoring the new exception, since the exception is now named at every site the absolute is stated.
- **Z4 (HIGH, Cursor F1):** this plan's `read_first`/behavior text previously described Plan 08 Task 3's
  `recordPersistResult` wiring in `useChannelReader.ts` as "ALREADY-LANDED". Ground truth (re-verified this
  session): `grep -rn "recordPersistResult" packages/renderer/src` returns ZERO hits, and
  `useChannelReader.ts` has no `worldEditorStore`/`recordPersistResult` import at all — nothing in this repo has
  executed Plan 08 Task 3 yet (these are still-unexecuted plans, not landed code). The word "ALREADY-LANDED" is
  corrected below to describe this as a FORWARD OUTPUT Plan 08 Task 3 builds, guaranteed to exist by the time
  THIS plan's task executes purely by wave/dependency ordering (Plan 08 is wave 2; this plan is wave 5) — not
  because the code already exists in the tree today. To make that guarantee explicit rather than merely
  transitive (this plan's `depends_on` previously reached Plan 08 only via Plan 13's own `depends_on`), `"05.1-
  08"` is added directly to this plan's `depends_on` list in the frontmatter above.
- **Z5 (MEDIUM, Sonnet L2 — Plan 13's own fix, consumed here):** Plan 13's `suppressNextDiffRef` changes from a
  single nullable identity-key ref to a QUEUE (an array) so a second concurrent successful Undo can no longer
  clobber a first, not-yet-drained entry. This plan's suppress branch (below) is updated to DRAIN the whole
  queue each effect run (capturing and clearing the entire array, not a single value) and pre-credit EVERY
  identity key in it, grouped by building, rather than assuming at most one.

Purpose: close SC4's app-side ADD trigger and the two-surface confirm (021-A Frame 3), with the C6 risk
surfaced honestly rather than silently narrowed or left as an invisible landmine, and the "(NEW)" marker
immune to any reindex or stale-snapshot misattribution, not just this plan's own original ROUND 4 fix (ROUND
4, W2; ROUND 5, V2/V3).
Output: `AddDecorationModal.tsx`; the World panel's "+ Add decoration…" button wired to open it with a
resolved building/cell context; a visible confirm once the placement persists, including a content-identity-
keyed "(NEW)" row marker.

**ROUND 10 REVISION (2026-08-01, REVIEWS.md ROUND 8 crew — AA4/AA5/AA8):** a new Task 3 lands two fixes this
plan's original two tasks did not cover, plus a citation-honesty fix to Task 2's own test text:
- **AA4 (HIGH, Sonnet L1):** Task 2's placement toast fired unconditionally on `sendStartPlacement`, with no
  read of the agent's ack — but Plan 12 makes START_PLACEMENT a silent no-op refusal (result 0) when an edit
  is already armed elsewhere OR a placement is already active. Plan 08 built exactly the ack primitive
  (`sendStartPlacement` now returns its epoch; `worldEditorStore.lastHostCommandResult` reports the matching
  ack) this plan was flagged as free to "optionally pick up" and never did. Task 3 consumes it: the toast is
  now deferred until the ack correlates, and shows an honest refusal message on a silent no-op instead of a
  false "active" claim.
- **AA5 (HIGH, Sonnet L2):** no plan reset `worldEditorStore`/`removeUndoStore`/this component's own diff/
  suppress refs on a project switch — a real, already-built app capability. Task 3 adds the project-switch
  effect (paired with Plan 13's own `key`-prop remount fix for `RemoveUndoToast`'s component-local state).
- **AA8 (MEDIUM, Opus Y4):** Task 2's own ROUND 7/Z5 "two rapid Undos before any drain" regression test is
  unreachable through this phase's shipped single-slot toast UI (a second Undo button cannot render until the
  first entry's toast has resolved, and a successful Undo's own `refreshTree()` drains the queue before a
  second Undo could ever land) — Task 2's test is rewritten below to a reachable SEQUENTIAL two-Undo test
  instead, with Plan 13's own static grep gate as the proof of the underlying append-only mechanism.
This plan's `depends_on` now also lists `05.1-04` directly (Task 3 calls `worldEditorStore.reset()`/reads
`lastHostCommandResult`, both Plan 04 outputs — previously reached only transitively via Plan 13/08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-live-world-editor-productization/05.1-RESEARCH.md
@.planning/phases/05.1-live-world-editor-productization/05.1-PATTERNS.md
@.planning/phases/05.1-live-world-editor-productization/05.1-13-SUMMARY.md
@.planning/phases/05.1-live-world-editor-productization/05.1-12-SUMMARY.md
@.planning/phases/05.1-live-world-editor-productization/05.1-08-SUMMARY.md
@.planning/sketches/021-spawn-decoration-flow/README.md
@.planning/sketches/016-new-object-from-template/README.md
@.planning/phases/05.1-live-world-editor-productization/05.1-REVIEWS.md
</context>

<interfaces>
From packages/renderer/src/services/hostCommand.ts (Plan 08 output, reuse verbatim):
```typescript
export function sendStartPlacement(mappingName: string, decorationTemplate: string, cellName: string, buildingId: string): void;
```
From packages/renderer/src/services/worldEditorScan.ts (Plan 04 ROUND 3 output — the tree data this plan
borrows a cellName from; NOTE: this tree ONLY contains buildings with an existing edit_*.ilf on disk, per D-05
— never-edited/stock-only buildings are absent, per this revision's C6/Fable disclosure):
```typescript
export interface WorldEditorBuilding { buildingId: string; displayLabel: string; editedIlfPath: string; derivedTemplatePath: string; buildingTemplateVfsPath: string; decorations: { cellName: string; rowIndex: number; objectTemplateName: string; transform: number[] }[]; }
```
From packages/renderer/src/state/worldEditorStore.ts (Plan 04 ROUND 3 output — the shared row-id contract,
R12/MED-7; use these instead of any ad-hoc `selectedRowId` string parsing):
```typescript
export function worldEditorBuildingRowId(buildingId: string): string;
export function parseWorldEditorRowId(id: string): { kind: 'building'; buildingId: string } | { kind: 'decoration'; buildingId: string; cellName: string; rowIndex: number };
export function worldEditorRowId(buildingId: string, cellName: string, rowIndex: number): string;
```
From packages/renderer/src/state/treStore.ts (the mounted VFS entries this modal searches — reuse the SAME
store makeReadVfs already reads from in decorationPersistOrchestrator.ts):
```typescript
export const useTreStore: /* ...; vfsEntries: VfsEntry[]; ... */;
```
From packages/renderer/src/state/worldEditorStore.ts (Plan 04 output, extended by Plan 08 Task 3 — the
`history` growth this plan's refresh-on-growth effect subscribes to):
```typescript
export const useWorldEditorStore: /* ...; history: PersistHistoryEntry[]; tree: WorldEditorBuilding[]; ... */;
```
From packages/renderer/src/panels/world/WorldPanel.tsx (Plan 10/13 ROUND 4 output — the shared `refreshTree()`
helper this plan's history-triggered effect MUST call, never a hand-built `refresh()` argument pair; a
component-local function, not a module export):
```typescript
function refreshTree(): void;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AddDecorationModal.tsx — search + thumbnail grid (021-A Frame 1)</name>
  <files>packages/renderer/src/panels/world/AddDecorationModal.tsx, packages/renderer/src/panels/world/AddDecorationModal.test.tsx</files>
  <read_first>
    .planning/sketches/021-spawn-decoration-flow/index.html (lines 170-191, Frame 1's exact DOM: modal-head
    with building/cell context text, search input, tpl-grid of selectable tiles each with thumb/nm/pth, and
    the modal-foot's preview-note + Cancel + "Place in game ▸" primary button)
    packages/renderer/src/state/treStore.ts (vfsEntries shape — path/name fields to filter against)
  </read_first>
  <behavior>
    - Props: `buildingLabel: string`, `cellName: string`, `buildingId: string`, `onCancel: () => void`,
      `onPlace: (templatePath: string) => void` (the caller, WorldPanel.tsx, owns the actual
      sendStartPlacement call + modal-close sequencing — this component is presentation + selection only).
    - Modal head reads "+ Add decoration to {buildingLabel} ({cellName})" per 021-A's exact wording pattern.
    - Search input filters `useTreStore`'s `vfsEntries` by substring match against `path` (case-insensitive),
      scoped to a decoration-shaped prefix convention (paths containing `/furniture/` or `/tangible/` — match
      the SAME convention `.ilf`'s own `objectTemplateName` values already use, e.g.
      `object/tangible/furniture/...`) — do not attempt to enumerate ALL mounted assets unscoped (too broad,
      not what 021-A's "furniture templates" search implies).
    - Each result renders as a tile: a glyph/thumb placeholder, the template's basename (humanized), and its
      full VFS path — matching 021-A's tpl-grid tile shape. Selecting a tile highlights it (single-select).
    - Footer: a preview-note text ("preview loads from the mounted TRE" per the sketch, or a placeholder note
      if a live mesh-viewport preview integration is out of this task's scope — Claude's discretion, document
      the choice), a Cancel button calling `onCancel`, and a "Place in game ▸" primary button (disabled until
      a tile is selected) calling `onPlace(selectedTemplatePath)`.
  </behavior>
  <action>
    Create AddDecorationModal.tsx as a presentational modal component per the behavior spec, reading
    `useTreStore((s) => s.vfsEntries)` and filtering client-side. Write AddDecorationModal.test.tsx seeding a
    small vfsEntries fixture, asserting: the search input filters the grid to matching entries; selecting a
    tile enables "Place in game ▸"; clicking it calls onPlace with the selected entry's exact path; Cancel
    calls onCancel; the modal head shows the passed buildingLabel/cellName.
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- AddDecorationModal</automated>
  </verify>
  <acceptance_criteria>
    Test asserts search filtering narrows the visible tile count correctly; onPlace receives the exact
    selected template path; "Place in game ▸" is disabled with nothing selected.
  </acceptance_criteria>
  <done>AddDecorationModal.tsx matches 021-A Frame 1 element-for-element, fully covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: World panel wiring — open modal with resolved context (C6-disclosed scope), dispatch placement with a wrong-room warning (C6), confirm on both surfaces incl. a content-identity-keyed '(NEW)' marker with a tree-keyed trigger + null-sentinel seed (ROUND 5: V2/V3, closing the round-5 anchor's paired defect)</name>
  <files>packages/renderer/src/panels/world/WorldPanel.tsx, packages/renderer/src/panels/world/WorldPanel.test.tsx</files>
  <read_first>
    packages/renderer/src/panels/world/WorldPanel.tsx (Plan 10/11's "+ Add decoration…" footer stub — replace
    its toast-only onClick with the real modal-open flow; also Plan 10's `refreshTree()` — this plan's diff
    effect is keyed on the `tree` STATE VALUE this helper replaces, not on `history.length` — and Plan 13's
    `suppressNextDiffRef` (ROUND 5, V2 groundwork; ROUND 7, Z5 — now a queue) this task's diff effect drains and clears on Undo's refresh(es))
    packages/renderer/src/services/hostCommand.ts (sendStartPlacement)
    packages/renderer/src/hooks/useChannelReader.ts (the CURRENT, pre-Plan-08-Task-3 file — verified this
    session: zero `recordPersistResult`/`worldEditorStore` references exist here yet; Plan 08 Task 3 BUILDS the
    RESULT → `worldEditorStore.recordPersistResult` wiring this plan depends on, guaranteed to exist by the time
    THIS task executes via wave ordering (Plan 08 = wave 2, this plan = wave 5) AND this plan's own `depends_on`
    now lists `05.1-08` directly, per ROUND 7/Z4 — read Plan 08's own Task 3 spec, not this file's CURRENT
    state, to understand the wiring this plan's diff effect reacts to; this plan's diff effect itself no longer
    keys off `history.length` directly (ROUND 5, V2) but the tree update THAT wiring causes is still what the
    diff observes)
    packages/renderer/src/state/worldEditorStore.ts (Plan 04's `refresh()` — read ONLY for its
    buildingId+cellName+objectTemplateName identity-key convention and its duplicate-ambiguity tiebreak
    posture; ROUND 5, V9 — this plan's own count-map walk is a DIFFERENT algorithm for a different purpose, not
    a literal reuse of `refresh()`'s routine)
    .planning/sketches/021-spawn-decoration-flow/index.html (lines 206-219, Frame 3's two-surface confirm —
    overlay toast text style + the World-list's "(NEW)" marked row)
  </read_first>
  <behavior>
    - **(ROUND 3, R12/MED-7)** Clicking "+ Add decoration…" resolves a target building/cellName by calling
      `parseWorldEditorRowId(worldEditorStore.selectedRowId)` (Plan 04's shared discriminator — never a local
      colon-split) if a building or decoration row is currently selected in the tree: for `kind: 'decoration'`,
      use its exact `cellName`; for `kind: 'building'`, use the FIRST cellName among that building's existing
      decorations. **(C6, Fable facet — explicit scope disclosure):** the selectable set here is
      ENTIRELY `worldEditorStore.tree`, which per D-05 only contains previously-EDITED buildings — a
      never-edited stock building (even one whose STOCK `.ilf` already has decorations) is simply not
      selectable in this UI, because it doesn't appear in the tree at all. This is a NARROWER scope than
      RESEARCH.md originally recommended (which considered a stock-`.ilf` fallback lookup for unedited
      buildings) — the narrowing is intentional for this phase (avoiding a larger building-discovery UI
      addition) and is recorded in Plan 15's sign-off, not silently shipped. If the resolved building has ZERO
      existing decorations (`decorations.length === 0`) OR nothing is selected, the button is disabled with a
      hint tooltip/adjacent text reading "select a decorated building first — placing into a brand-new cell
      isn't supported yet" (the honest D-04 degrade message, words not a silent no-op).
    - When a valid context resolves and the user picks a template + clicks "Place in game ▸": close the
      modal, call `sendStartPlacement(mappingName, templatePath, resolvedCellName, resolvedBuildingId)`
      (requires an attached live session — the button/flow is disabled entirely when `useLiveStore` is not
      'attached', matching the Scene accordion's own live-gating precedent from Plan 11), and show a toast
      **(C6, Codex/Sonnet facet — wrong-room warning added)**: "Placement mode active in-game — place the
      object, then Persist. Click ONLY in the SAME ROOM as the decoration you selected in {resolvedCellName}
      — this building may have other rooms the toolkit can't distinguish yet." (the in-game half of the
      handoff; the in-game ghost/ack and the building-ID guard are Plan 12's territory — this toast's job is
      to make the RESIDUAL same-building-wrong-cell risk an INFORMED one, since no code fix for it exists yet).
    - Two-surface confirm (021-A Frame 3): once a subsequent CAPTURE/RESULT cycle lands with
      `capture.kind === 'add'` and a successful result, `worldEditorStore.history` grows by one entry — this
      is the poll loop's `useChannelReader.ts` → `worldEditorStore.recordPersistResult` wiring that
      **Plan 08 Task 3 builds** (added in the 2026-07-31 revision specifically to close this gap; before that
      revision, no plan actually dispatched the base edit/add RESULT into the store, so this plan's
      two-surface confirm had no real trigger). **(ROUND 7, Z4 — HIGH, Cursor)** As of THIS planning round,
      this wiring does NOT exist yet in `useChannelReader.ts` (verified: zero `recordPersistResult`
      references in the current source) — it is a FORWARD OUTPUT of Plan 08's own Task 3, not
      "already-landed" code, a prior draft of this plan incorrectly claimed. The guarantee that it exists
      by the time THIS task executes rests on wave/dependency ordering: Plan 08 is wave 2, strictly
      earlier than this plan's wave 5, and this plan's `depends_on` now lists `05.1-08` DIRECTLY
      (previously only reachable transitively via Plan 13's own `depends_on`) — making the contingency
      explicit rather than implicit. This plan's job
      is to (a) ensure a fresh refresh is triggered promptly after that history growth while the World tab is
      open — a small effect subscribing to the store's history length and, on growth, calling the SAME shared
      `refreshTree()` helper Plan 10's mount effect and mirror toggle define in this file (**ROUND-3-REVIEW
      R2, HIGH, Cursor** — do NOT reuse the `overrideDir`/`meta` pair Plan 10's mount effect resolved once at
      mount time: that snapshot PREDATES every persist this effect exists to react to, so `meta.
      worldEditorBuildingTemplates` would be missing the exact entry this ADD's own capture just wrote,
      silently defeating R3's detail-card/Remove groundwork for the freshly-added row; `refreshTree()` re-reads
      `readWorkspaceJson(studioDir)` FRESH on every call, closing this) — so the new row appears without
      requiring a manual reopen of the tab, AND (b) mark the newly-appeared row with a visible "(NEW)"
      badge/suffix in the tree via a SEPARATE effect keyed on the `tree` state value itself.
    - **(ROUND 5, V2 — HIGH, Sonnet; ROUND 5, V3 — HIGH, Opus)** The "(NEW)" diff is its OWN
      `useEffect(() => { ... }, [tree])` — keyed on the `worldEditorStore` `tree` STATE VALUE, NOT on
      `worldEditorStore.history.length` growth. This means EVERY `refreshTree()`-triggered tree replacement —
      mount, a manual refresh click, the mirror toggle, Plan 13's Remove, Plan 13's Undo, and this plan's own
      ADD-confirm — runs this SAME effect, closing V2's root cause (the ROUND 4 design keyed the diff on
      `history.length` growth alone; Remove grows history, but Undo does not, so Undo's own refresh never
      advanced `prevTreeRef`, leaving it stale until some LATER unrelated history-growth event diffed against
      it and misattributed the ALREADY-RESTORED row as fresh). Track two `useRef`s: `prevTreeRef =
      useRef<WorldEditorBuilding[] | null>(null)` (ROUND 5, V3 — a `null` SENTINEL, not an empty array, so the
      very first pass is distinguishable from "compared against an empty tree") and reuse Plan 13's
      `suppressNextDiffRef` (ROUND 7, Z5 — a `useRef<Array<{ buildingId: string; cellName: string;
      objectTemplateName: string }>>` QUEUE, replacing the ROUND 6 single-nullable-identity-key form — Plan 13
      already declared it in this SAME component; this plan does NOT redeclare it, it consumes the SAME ref
      instance). On
      every effect run: **(1) suppress branch, ROUND 5/V2; ROUND 6/X3 — scoped to content identity; ROUND 7/Z5
      — draining a QUEUE, not reading a single nullable value:** if `suppressNextDiffRef.current` is
      non-empty (one or more identity keys pushed by Plan 13's Undo handler(s) immediately before their own
      `refreshTree()` call), capture the WHOLE array locally as `suppressed` and IMMEDIATELY clear the queue
      (`suppressNextDiffRef.current = []`) — then proceed into the SAME building-lookup + count-map walk as
      branch (3) below for EVERY building in the new tree (the diff is NEVER skipped wholesale). The ONE
      difference: group `suppressed` by `buildingId` into a `Map<buildingId, Map<tuple, count>>` (a tuple may
      appear MORE THAN ONCE if two concurrent Undos restored content-identical rows — each occurrence adds
      ANOTHER credit for that tuple, never overwriting a prior one, ROUND 7/Z5's queue fix); when building the
      count map for a building present in this grouping (if that building IS present in `prevTreeRef.current`
      — the ordinary case), seed the map with ONE EXTRA credit PER occurrence of each
      `${cellName}::${objectTemplateName}` tuple in `suppressed` for that building, BEFORE walking that
      building's new decorations — i.e. `count = (count found in prevTreeRef.current) + (occurrences in
      suppressed for that tuple)`. This pre-added credit absorbs EXACTLY the restored row(s) (content-identical
      to what Remove took away) without marking any of them "(NEW)", while every OTHER row in that SAME
      building, and every row in every OTHER building, is diffed by the ORDINARY unmodified count map and marked
      "(NEW)" exactly as branch (3) already would — closing X3: the ROUND 5 boolean form would have silently
      swallowed any such unrelated new row landed in the same refresh window; this identity-scoped, queue-drained
      form cannot, because the diff still runs and every queued entry is credited (ROUND 7/Z5 closes the
      remaining "second concurrent Undo clobbers the first's unconsumed key" gap Sonnet L2 found in the ROUND 6
      single-ref form). (If a `suppressed` entry's `buildingId` is itself ABSENT from `prevTreeRef.current` — an
      edge case, e.g. the building's only observation was itself suppressed somehow — branch (2)'s reversed
      absent-building rule below already contributes zero "(NEW)" rows for that whole building, so no extra
      credit is needed or applied in that case.) **(2) first-pass
      branch, ROUND 5/V3:** else if
      `prevTreeRef.current === null` (no baseline yet — the very first render after mount, or the first-ever
      scan), seed `prevTreeRef.current = tree` and RETURN — mark NOTHING "(NEW)". This closes V3: without this
      seed rule, the tree's transition from `[]` (pre-mount) to populated (first scan) — or a building's first
      appearance after its first-ever plain EDIT (D-05's disk-scan scope: the WHOLE building surfaces at once,
      with all its pre-existing rows) — would otherwise diff against an implicit empty baseline and mark EVERY
      row "(NEW)". **(3) normal diff branch:** otherwise, for each building present in the NEW `tree`, find the
      matching building (by `buildingId`) in `prevTreeRef.current`; **(ROUND 5, V3 — reversed absent-building
      rule)** if NO matching building exists in `prevTreeRef.current` (a building that was not merely
      unchanged, but genuinely ABSENT from the prior snapshot — e.g. its first-ever edit landed since the last
      diff), treat this as a first OBSERVATION of that whole building, not a placement — contribute ZERO
      "(NEW)" rows for it (the exact reversal of the ROUND 4 spec's "empty count map, all rows are new" rule,
      which this revision supersedes). Otherwise, build a count map of `${cellName}::${objectTemplateName}`
      tuples from that matching building's decorations in `prevTreeRef.current`, walk the NEW tree's
      decorations for that building in `rowIndex` order decrementing the matching tuple's count each time one
      is consumed, and mark any decoration whose tuple's count is already exhausted (0 remaining, including
      tuples never present in the prior map) "(NEW)" via its NEW composite `worldEditorRowId(buildingId,
      cellName, rowIndex)` — this is DELIBERATELY the same duplicate-tiebreak posture Plan 04's `refresh()`
      documents as an accepted limitation for its own, different reconciliation purpose (ROUND 5, V9 — shared
      posture, not shared code): two decorations sharing template+cellName in the same building are
      content-ambiguous to any id scheme this phase ships, so a genuine ADD among duplicates may occasionally
      tag the "wrong" one of several content-identical rows as "(NEW)", which is no worse than Plan 04's own
      documented tiebreak and vastly better than a raw positional diff. At the END of every branch that reaches
      this point (suppress, first-pass, or normal diff), `prevTreeRef.current` is updated to the CURRENT
      `tree` — so the very next effect run always diffs against what THIS run just observed, never a
      multi-refresh-old snapshot. This closes ROUND 4/W2 AND ROUND 5/V2 together: a row's numeric position
      changing (from Undo's append-only re-add, or any future cause) can never by itself cause a "(NEW)"
      mislabel, whether observed on the SAME refresh that caused it (V2's Undo-instant case, handled by the
      suppress branch) or a LATER, unrelated one (V2's original stale-`prevTreeRef` case, closed by keying the
      effect on `tree` itself). Clearing an individual "(NEW)" marker either after a short timeout (matching
      the Persist strip's own ~2s discretion) or when the row is explicitly selected, Claude's discretion on
      which, documented in the component — this is orthogonal to the diff-pass mechanics above.
  </behavior>
  <action>
    Replace the Plan 11 footer stub's onClick with the modal-open flow described above (track modal-open
    state in local component state), render `<AddDecorationModal>` conditionally with the resolved
    buildingLabel/cellName/buildingId, wire its onPlace to sendStartPlacement + the wrong-room-warning toast +
    close. Add a small effect subscribing to `worldEditorStore.history.length` that calls the SAME shared
    `refreshTree()` on growth (unchanged from ROUND 4 — this is still the mechanism that makes a fresh ADD's
    persist actually appear in the tree; ROUND 5/V2 changes only the "(NEW)" MARKING mechanism below, not this
    refresh trigger). Implement the "(NEW)" marking as its OWN `useEffect(() => { ... }, [tree])` per the
    ROUND 5/V2/V3/ROUND 6/X3 behavior spec above: declare `const prevTreeRef = useRef<WorldEditorBuilding[] |
    null>(null);` in this component (a NEW ref, null-sentinel-initialized, ROUND 5/V3) and REUSE Plan 13's
already-declared `suppressNextDiffRef` (ROUND 7/Z5 — now a QUEUE/array, not a single identity key — do not
    redeclare it, same
    component, same file, Plan 13 lands first); implement the
    suppress branch (drain-the-whole-queue-then-group-by-building-then-pre-credit-then-diff, never a blanket
    skip), the first-pass
    branch, and the normal count-map diff branch exactly as specified, updating
    `prevTreeRef.current = tree` at the end of every branch. Extend WorldPanel.test.tsx: assert the Add button
    is disabled with the correct hint when no building/undecorated-building is selected; assert it opens the
    modal with the correct resolved context when a valid decoration row is selected; assert selecting a
    template and clicking Place calls sendStartPlacement with the resolved cellName/buildingId (spy/mock) and
    shows the toast containing BOTH "Placement mode active" AND the wrong-room warning text (C6); assert a
    history-length change (simulating Plan 08 Task 3's recordPersistResult having fired) calls the shared
    `refreshTree()` helper (spy — NOT a direct `refresh()` call with a hand-built argument pair, ROUND-3-REVIEW
    R2) AND that the row present after the refresh but content-absent from the PRIOR tree snapshot renders a
    "(NEW)" marker (Sonnet F9/Cursor M5). **(ROUND 4, W2)** ALSO assert the negative case: seed a tree, capture
    it as the "prior" snapshot, then simulate a refresh whose ONLY change is an EXISTING decoration's
    `rowIndex` shifting (same cellName+objectTemplateName, different rowIndex — the exact shape Plan 13's Undo
    produces) — assert NO row renders a "(NEW)" marker from this refresh (a pure reindex, zero content change,
    must never be mislabeled "(NEW)"); separately, seed a refresh that adds one genuinely new decoration
    ALONGSIDE an unrelated reindex of an existing row in the SAME building — assert ONLY the genuinely-new row
    is marked "(NEW)", the reindexed-but-not-new row is not. **(ROUND 6, X7 — MEDIUM, Opus, replacing the
    ROUND-5-drafted "pre-set `suppressNextDiffRef.current`" tests, which were unwritable: the ref is a
    never-exported component-local `useRef` with no legitimate test handle)** drive the REAL Undo flow through
    the RENDERED components instead of poking the ref directly: seed `removeUndoStore.pending` with one entry,
    render `WorldPanel` (which mounts `RemoveUndoToast`), let the diff effect baseline `prevTreeRef` once, then
    `fireEvent.click` the rendered toast's "Undo" button — this drives the REAL `handleUndo` (Plan 13), which
    pushes the entry's identity key onto `suppressNextDiffRef.current` and calls the REAL `refreshTree()` — assert
    the OBSERVABLE DOM consequence: ZERO "(NEW)" markers anywhere in the tree after this Undo-triggered refresh
    (X3's restored-row-suppression, proven through the real code path, not a poked ref). **(ROUND 6, X3 — NEW
    test, the literal repro this HIGH exists to close)** immediately after that same Undo click, simulate a
    SECOND, UNRELATED refresh whose new tree adds one genuinely-new decoration in a DIFFERENT building (or the
    SAME building, a different cell/template) ALONGSIDE the just-restored row still present — assert the
    genuinely-new row from THIS second refresh IS marked "(NEW)" (proving the suppression pre-consumed only the
    restored row's own tuple and did not blanket-skip the diff), while the restored row from the Undo click
    continues to show NO "(NEW)" marker (proving the pre-consumed credit persisted correctly into
    `prevTreeRef`). **(ROUND 5, V3 — NEW tests)** assert a FRESH
    mount over a tree seeded with 2 buildings/3 decorations renders ZERO "(NEW)" markers (the first-pass seed
    rule); assert a tree update where a BRAND-NEW building (absent from `prevTreeRef`) appears with 3
    pre-existing decorations renders ZERO "(NEW)" markers for that building (the reversed absent-building
    rule) — contrasted with a genuine same-building ADD (building ALREADY present in `prevTreeRef`, one new row
    added) which DOES mark that one row "(NEW)". **(ROUND 10, AA8 — MEDIUM, Opus Y4, replacing the ROUND 7/Z5-
    drafted "two rapid Undos before any drain" test — unreachable through this phase's shipped single-slot
    `RemoveUndoToast` UI: a second entry's Undo button cannot even render until the first entry's toast has
    already resolved, and a successful `handleUndo`'s own `refreshTree()` call triggers THIS SAME `[tree]`-keyed
    diff effect before a second Undo's click could ever land — so two identity keys can never be independently
    observed sitting in the queue via any real, rendered user action; Plan 13's own ROUND 10 revision proves the
    queue's append-only semantics via a static grep gate instead, for the same reason)** seed
    `removeUndoStore.pending` with TWO entries in DIFFERENT buildings; drive the REAL Undo flow (rendered toast
    clicks) SEQUENTIALLY — Undo the FIRST entry (its toast resolves, triggering a drain-and-diff that correctly
    shows zero "(NEW)" markers for it), THEN seed and Undo the SECOND entry the same way — assert BOTH
    Undo-restored rows individually show zero "(NEW)" markers after their OWN respective refresh, proving the
    identity-scoped suppression mechanism (Plan 13's ROUND 6/X3 fix) generalizes correctly across REPEATED use,
    not just a single Undo — this is the reachable, honest substitute for the unreachable "simultaneous" claim.
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- WorldPanel</automated>
  </verify>
  <acceptance_criteria>
    Tests assert: the Add button correctly gates on a decorated-building selection (D-04's honest degrade,
    scoped per C6's Fable-facet disclosure to tree-visible buildings only); a valid selection + template pick
    calls sendStartPlacement with the borrowed cellName, never a fabricated or empty one; the placement-active
    toast contains the wrong-room warning text (C6, Codex/Sonnet facet); the World panel refreshes
    automatically once a new persist result lands (i.e., once `worldEditorStore.history` grows by one entry
    via Plan 08 Task 3's wiring), without requiring a manual tab close/reopen; the freshly-appeared row after
    that refresh renders a "(NEW)" marker (Sonnet F9/Cursor M5), computed via content identity, not raw
    position (ROUND 4/W2) — proven by the negative reindex-only test producing ZERO "(NEW)" markers and the
    mixed reindex+genuine-add test marking ONLY the genuinely new row. **(ROUND 5, V2)** the diff effect is
    keyed on `tree`, not `history.length` (asserted by triggering a tree change with NO history growth — e.g.
    a manual refresh or a mirror-toggle-triggered `refreshTree()` — and observing `prevTreeRef` DOES advance,
    proven by a subsequent unrelated change no longer misattributing stale content); **(ROUND 6, X7 —
    demoted to a rendered, observable assertion)** driving a REAL Undo through the RENDERED `RemoveUndoToast`'s
    Undo button produces ZERO "(NEW)" markers for the restored row in the rendered DOM (never a poked-ref
    assertion); a LATER, unrelated tree update never retroactively marks that already-restored row "(NEW)" (the
    delayed-misattribution regression this HIGH exists to close). **(ROUND 6, X3)** a genuinely-new row landed
    in a SECOND, unrelated refresh immediately following that same Undo click IS marked "(NEW)" (proving the
    suppression pre-consumed only the restored row's own identity tuple and never blanket-skipped the diff —
    the literal repro X3 exists to close). **(ROUND 5, V3)** a fresh mount over a
    non-empty seeded tree produces ZERO "(NEW)" markers (first-pass seed rule); a brand-new building appearing
    whole (absent from `prevTreeRef`) produces ZERO "(NEW)" markers for its rows, while a genuine same-building
    ADD (building already tracked) DOES mark exactly the new row.
  </acceptance_criteria>
  <done>The "+ Add decoration…" flow is fully wired end-to-end from the app side, with an honest, words-based
  degrade for the one genuine provider gap this phase does not solve (with its actual scope — tree-visible
  buildings only — explicitly disclosed, C6) and a visible wrong-room warning for the residual same-building
  risk (C6); the newly-added row is marked "(NEW)" per the sketch, keyed on content identity so an unrelated
  reindex (Undo, or any future cause) can never trigger a false "(NEW)" (Sonnet F9/Cursor M5, ROUND 4/W2); the
  "(NEW)" diff now runs on EVERY tree update (not only history growth), so `prevTreeRef` never goes stale
  between refreshes (closing the delayed-misattribution class one layer deeper, ROUND 5/V2); Undo's own
  refresh pre-consumes EXACTLY the restored row's identity tuple(s) from Plan 13's `suppressNextDiffRef` QUEUE
  (ROUND 6/X3 — identity-keyed; ROUND 7/Z5 — a queue, draining potentially multiple concurrent Undos'
  entries, not a single overwritable ref), never marking the restored row(s) "(NEW)" while still correctly
  marking any OTHER, unrelated new row landed in the same window — proven through the REAL rendered Undo flow,
  not a poked ref (ROUND 6/X7); a null-sentinel first pass and a reversed absent-building rule
  ensure a fresh mount or a building's first observation never mass-marks its rows "(NEW)" (ROUND 5/V3); a
  mid-flow panel remount during the ADD round trip resetting the component-local baseline is a disclosed,
  accepted scope narrowing, not a hidden defect (ROUND 6/X8, Plan 15 gap ledger item (h)); the
  change-request for the permanent cell-name fix is filed in Plan 15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consume the START_PLACEMENT ack for an honest toast (AA4) + project-switch store/ref reset (AA5)</name>
  <files>packages/renderer/src/panels/world/WorldPanel.tsx, packages/renderer/src/panels/world/WorldPanel.test.tsx</files>
  <read_first>
    packages/renderer/src/services/hostCommand.ts (Plan 08's own ROUND 10 revision — sendStartPlacement now
    returns the epoch it used, `number` not `void`; parseHostCommandResult/describeHostCommandResult)
    packages/renderer/src/state/worldEditorStore.ts (Plan 04's own ROUND 10 revision — lastHostCommandResult/
    setLastHostCommandResult, and reset())
    packages/renderer/src/state/removeUndoStore.ts (Plan 13's own ROUND 10 revision — reset())
    packages/renderer/src/state/workspaceStore.ts (studioDir — the project-switch signal; ROUND 6/X1 — read
    reactively here via the hook-selector form, since this IS a render-time subscription, unlike
    resolveOverridePair()'s imperative getState() usage elsewhere in this file)
    packages/renderer/src/panels/world/WorldPanel.tsx (this plan's own Task 2 output — the placement toast
    call site: the "sendStartPlacement" call + "Placement mode active" toast; the `prevTreeRef`/
    `suppressNextDiffRef` refs this task must ALSO reset on a project switch)
  </read_first>
  <behavior>
    - **(ROUND 10, AA4 — HIGH, Sonnet L1)** `sendStartPlacement`'s call site no longer shows the "Placement
      mode active in-game..." toast SYNCHRONOUSLY at send time. It captures the returned epoch into a ref
      (`pendingPlacementEpochRef.current = epoch`) and a small effect, subscribing to
      `useWorldEditorStore((s) => s.lastHostCommandResult)`, fires when that value's `epoch` matches the ref's
      CURRENT value: on a match, it clears the ref (`= null`, so a later, unrelated HOST_CMD result never
      re-matches) and shows EXACTLY ONE of two toasts: `code === 1` -> the ORIGINAL "Placement mode active
      in-game — place the object, then Persist. Click ONLY in the SAME ROOM..." wrong-room-warning toast
      (Task 2's existing copy, unchanged); any OTHER code -> a NEW, honest refusal toast: "Placement request
      was refused — an edit or placement may already be active elsewhere. Cancel it, then try again." — never
      the false-positive "active" claim on a silent agent-side refusal (Plan 12's `g_capArmed`/
      `g_placementActive` exclusivity guards, both of which return result 0 with zero other signal).
    - **(ROUND 10, AA5 — HIGH, Sonnet L2)** A component-scope `const studioDir = useWorkspaceStore((s) =>
      s.studioDir);` selector (the SAME one Plan 13's own ROUND 10 revision adds for its `key` prop — declared
      ONCE in this file, reused by both) feeds a `useEffect` keyed on `[studioDir]` with a
      `useRef<string | null>` tracking the PREVIOUS value: on the FIRST run (mount), it just records the
      current value and does nothing else (a fresh mount is not a "switch"); on any SUBSEQUENT run where the
      value actually CHANGED, it calls `useWorldEditorStore.getState().reset()` (Plan 04's own ROUND 10
      addition) and `useRemoveUndoStore.getState().reset()` (Plan 13's own ROUND 10 addition), and ALSO resets
      this component's OWN `prevTreeRef.current = null` (re-arming the null-sentinel first-pass rule, ROUND
      5/V3, for the NEWLY-opened project's tree) and `suppressNextDiffRef.current = []` (discarding any stale,
      not-yet-drained identity keys from the OLD project) — all in response to the SAME `studioDir` change that
      also remounts `RemoveUndoToast` via its `key` prop (Plan 13's own ROUND 10 fix), so both the component
      state (via remount) and the module-scope store state (via `reset()`) are cleared together on a switch.
  </behavior>
  <action>
    In WorldPanel.tsx: change the placement click handler (Task 2's existing "+ Add decoration..." wizard's
    "Place in game" flow) to capture `sendStartPlacement`'s return value into a
    `const pendingPlacementEpochRef = useRef<number | null>(null);` component ref instead of discarding it, and
    remove the immediate, synchronous toast call from that click handler. Add a NEW `useEffect` depending on
    `useWorldEditorStore((s) => s.lastHostCommandResult)` that, when non-null AND its `epoch` equals
    `pendingPlacementEpochRef.current`, clears the ref and shows the appropriate toast per the behavior spec
    (success copy unchanged from Task 2; failure copy is new, words-only, per AA4). Add a component-scope
    `const studioDir = useWorkspaceStore((s) => s.studioDir);` selector (shared with Plan 13's `key` prop use)
    and a `useEffect([studioDir])` with a `useRef` tracking the previous value: skip on the first (mount) run;
    on any later run where the value differs, call `useWorldEditorStore.getState().reset()`,
    `useRemoveUndoStore.getState().reset()`, set `prevTreeRef.current = null`, and set
    `suppressNextDiffRef.current = []`. Extend WorldPanel.test.tsx: mock `sendStartPlacement` to return a fixed
    epoch (e.g. `7`), click "Place in game ▸", assert NO toast appears yet (deferred); then seed
    `useWorldEditorStore.setState({ lastHostCommandResult: { epoch: 7, code: 1 } })` and assert the ORIGINAL
    "Placement mode active" + wrong-room-warning toast NOW appears; separately, seed
    `{ epoch: 7, code: 0 }` instead and assert the NEW refusal toast appears with words only (no bare digit
    matching the input code); seed `{ epoch: 999, code: 1 }` (an unrelated epoch) after a placement send and
    assert NEITHER toast appears (the epoch-correlation guard, proving an unrelated HOST_CMD ack is never
    mistaken for this placement's own). For AA5: render `WorldPanel` with `useWorkspaceStore`'s `studioDir`
    seeded to `'project-a'`, seed `worldEditorStore.history`/`hasFailureBadge` and `removeUndoStore.pending`/
    `undoErrors` all non-default, then change `useWorkspaceStore`'s `studioDir` to `'project-b'` (re-render) —
    assert `worldEditorStore.getState().history`/`hasFailureBadge`/`sessionOverlay`/`tree`/`selectedRowId` and
    `removeUndoStore.getState().pending`/`undoErrors` are ALL back to their initial empty values (AA5); a
    mount with NO prior `studioDir` change does NOT call either store's `reset()` (spy shows zero calls on
    initial mount — this effect only fires on an ACTUAL switch, never on first render).
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- WorldPanel</automated>
  </verify>
  <acceptance_criteria>
    Tests assert: `sendStartPlacement`'s returned epoch is captured, not discarded; no placement toast renders
    until a matching `lastHostCommandResult` epoch arrives; a matching epoch with `code === 1` shows the
    original "active" + wrong-room-warning copy; a matching epoch with any other code shows a NEW, words-only
    refusal toast, never the false "active" claim; an UNRELATED epoch in `lastHostCommandResult` never triggers
    either toast (proving epoch correlation, not a bare "any ack arrived" trigger). A `studioDir` change
    (project switch) calls BOTH `worldEditorStore.reset()` and `removeUndoStore.reset()` exactly once each, and
    resets this component's own `prevTreeRef`/`suppressNextDiffRef` to their initial values; the SAME effect
    does NOT fire on initial mount (spy shows zero `reset()` calls before the first `studioDir` change).
  </acceptance_criteria>
  <done>A silently-refused placement request (Plan 12's exclusivity guards) never shows a false "active" toast
  — the toast now reflects the agent's REAL ack, correlated by epoch (AA4). A project switch clears
  worldEditorStore/removeUndoStore and this component's own diff/suppress refs, so Project A's Activity log,
  failure badge, sticky Undo-error toast, and suppress-queue never leak into Project B's session (AA5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI → hostCommand.ts → agent | The modal's confirmed selection triggers a live in-game placement-mode session from a UI click. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-14a | Tampering | template search string / selected path | mitigate | The selected template path is sourced ONLY from `useTreStore`'s already-mounted, already-trusted vfsEntries list (never free-text user input passed directly to sendStartPlacement) — a user can only select a template that genuinely exists in the mounted VFS. |
| T-05.1-14b | Information Disclosure | none material | accept | Local desktop tool browsing its own mounted asset set. |
| T-05.1-14c | Denial of Service / Repudiation | placing into an unresolvable cell (empty building) | mitigate | Explicit fail-closed UI gate (D-04's honest degrade, Task 2 behavior spec) — the button is disabled with a clear reason rather than allowing a placement attempt that would fail deep in the agent with no clear cause. |
| T-05.1-14d | Tampering | a click landing in the WRONG ROOM within the correct (guarded) building — residual C6 risk | mitigate (informational only, no code fix possible) | Plan 12's building-ID guard closes the wrong-BUILDING case; this plan's explicit wrong-room warning toast (C6) makes the residual same-building-multi-cell risk an INFORMED one — no advertised endpoint can verify cell identity yet (the exact gap Plan 15's change-request exists to close). |
| T-05.1-14e | Repudiation | a reindex (e.g. Plan 13's Undo) being misread as a fresh placement | mitigate | ROUND 4/W2: the "(NEW)" marker is keyed on content identity (buildingId+cellName+objectTemplateName count-diff), not raw row position, so a pure reindex with zero content change can never be mislabeled "(NEW)" — proven by this task's negative regression test. |
| T-05.1-14f | Repudiation | a STALE prevTreeRef letting a LATER unrelated refresh misattribute an already-restored row as "(NEW)" (ROUND 5, V2) | mitigate | The diff effect is keyed on the `tree` reference itself, not `history.length` growth, so EVERY refreshTree()-triggered update advances `prevTreeRef` — it can never go stale between refreshes; Undo's own update pre-consumes EXACTLY the restored row's identity tuple(s) (Plan 13's `suppressNextDiffRef` QUEUE, ROUND 6/X3 — identity-keyed; ROUND 7/Z5 — a queue, so a second concurrent Undo's entry is never clobbered) rather than skipping the whole diff, so it never marks the restored row(s) "(NEW)" while still correctly diffing every OTHER row. |
| T-05.1-14g | Repudiation | the WHOLE tree (or a whole newly-appeared building) rendering "(NEW)" on first mount / first edit (ROUND 5, V3) | mitigate | `prevTreeRef` starts at a `null` sentinel; the first diff pass seeds it and marks nothing; an absent-building contributes ZERO "(NEW)" rows rather than treating its whole row set as new. |
| T-05.1-14h | Repudiation | a blanket (boolean) suppress flag silently swallowing an UNRELATED genuinely-new row landed in the same refresh window as an Undo (ROUND 6, X3 — HIGH, Sonnet), or a second concurrent Undo clobbering a first, not-yet-drained suppress entry (ROUND 7, Z5 — MEDIUM, Sonnet) | mitigate | `suppressNextDiffRef` carries a QUEUE of the restored rows' identity keys, not a boolean and not a single overwritable ref; the suppress branch drains the WHOLE queue each run and runs the SAME normal count-map diff as every other pass, pre-crediting every queued tuple, so any other row in the same or a different building is diffed and marked "(NEW)" normally — proven by the ROUND 6 regression test (a genuinely-new row landed immediately after an Undo IS marked) and, per ROUND 10/AA8, by Plan 13's own static grep gate (proving the queue's append-only semantics structurally) plus a SEQUENTIAL two-Undo regression test here (proving the mechanism generalizes across repeated use) — the literal "two Undos observed simultaneously in the queue" scenario is not independently exercisable through this phase's single-slot toast UI, a disclosed correction to this row's prior "two concurrent Undos" phrasing. |
| T-05.1-14i | Repudiation | the "(NEW)" marker's component-local baseline (`prevTreeRef`) being reset by a mid-flow tab navigation/remount during the ADD round trip, silently degrading the two-surface confirm to one surface (ROUND 6, X8 — MEDIUM, Opus) | accept (disclosed, not fixed this phase) | Hoisting the marker/baseline into `worldEditorStore` is the permanent fix (would also close ledger item (g)'s class) but is deferred as a future-phase improvement; this phase discloses the narrowing explicitly in this plan's must_haves and Plan 15's gap ledger item (h) rather than shipping it silently. |
| T-05.1-14j | Repudiation | the placement-active toast showing unconditionally even when the agent silently refused START_PLACEMENT (Plan 12's `g_capArmed`/`g_placementActive` exclusivity, ROUND 10, AA4 — HIGH, Sonnet L1) | mitigate | Task 3 defers the toast until Plan 08's returned epoch correlates with `worldEditorStore.lastHostCommandResult`, then shows the honest outcome (active vs. refused) — never an unconditional "active" claim on a silent no-op. |
| T-05.1-14k | Repudiation | Project A's Activity log, failure badge, sticky Undo-error toast, and suppress-queue leaking into Project B's session across a project switch (ROUND 10, AA5 — HIGH, Sonnet L2) | mitigate | Task 3's `studioDir`-keyed effect calls `worldEditorStore.reset()`/`removeUndoStore.reset()` and clears this component's own `prevTreeRef`/`suppressNextDiffRef` on every ACTUAL switch (never on initial mount), paired with Plan 13's `key`-prop remount of `RemoveUndoToast`. |
</threat_model>

<verification>
`npm -w @swg/renderer run test -- AddDecorationModal WorldPanel` green; `tsc --noEmit` clean for
packages/renderer.
</verification>

<success_criteria>
021-A's Wizard Modal is fully built and wired; the ADD flow's app-side trigger, live-fire smoke test lands
in Plan 15's checkpoint; the one genuine provider gap is degraded honestly, not silently, and its ACTUAL scope
(tree-visible/previously-edited buildings only, narrower than RESEARCH's original recommendation) is disclosed
rather than silently shipped as the wider scope (C6). The freshly-added row is visibly marked "(NEW)" per the
sketch, keyed on content identity rather than raw position so a reindex from any cause (Plan 13's Undo
included) can never trigger a false positive (Sonnet F9/Cursor M5, ROUND 4/W2). ROUND 5: the diff runs on
EVERY tree update (not only history growth), so prevTreeRef never goes stale between refreshes and a LATER
unrelated event can never misattribute an already-restored row (V2); Undo's own refresh pre-consumes exactly
the restored row's identity tuple rather than blanket-skipping the diff (V2, refined ROUND 6/X3), never marking
it "(NEW)" while still correctly marking any OTHER unrelated new row landed in the same window; a null-sentinel
first pass and a reversed absent-building rule
close the "whole tree/building marked (NEW) on first observation" defect (V3); the count-map mechanism is
honestly framed as sharing only Plan 04's identity-key/tiebreak posture, not its literal routine (V9). ROUND 6:
the suppress branch is scoped to content identity so it can never permanently swallow an unrelated genuinely-new
row (X3 — the round's anchor HIGH for this plan); the "pre-set the ref" test plan is replaced with tests that
drive the real Undo flow through the rendered toast and assert observable DOM consequences (X7); a mid-flow
panel remount resetting the component-local "(NEW)" baseline is disclosed as an accepted scope narrowing, not a
hidden defect (X8). ROUND 7: the OLD unconditional "(NEW)" absolutes (this plan's must_have, its
sketch_elements line) now carry an explicit "EXCEPT the disclosed X8 case" clause matching Plan 15's
must_have/step 4, closing the contradiction the X8 disclosure otherwise left in force (Z2); the
"ALREADY-LANDED" claim about Plan 08 Task 3's `recordPersistResult` wiring is corrected to a forward
output this plan's `depends_on` now names directly (Z4); `suppressNextDiffRef` is drained as a queue,
grouped by building and tuple, so two concurrent Undos before a diff pass runs are both credited, never
one clobbering the other (Z5).
ROUND 10: the placement-active toast is now deferred to a real agent ack, correlated by the epoch
`sendStartPlacement` returns (Plan 08's own ROUND 10 revision) against `worldEditorStore.lastHostCommandResult`
— a silent no-op refusal (Plan 12's `g_capArmed`/`g_placementActive` exclusivity) now shows an honest refusal
message instead of an unconditional false "active" claim (AA4). A project switch (`studioDir` change) resets
`worldEditorStore`/`removeUndoStore` and this component's own `prevTreeRef`/`suppressNextDiffRef`, closing the
cross-project state-leak class no prior plan addressed (AA5). The ROUND 7/Z5 "two rapid Undos before any
drain" test is replaced with a reachable SEQUENTIAL two-Undo test, since the "simultaneous" scenario is not
independently exercisable through this phase's single-slot toast UI — Plan 13's own static grep gate proves
the underlying queue mechanism structurally (AA8).
</success_criteria>

<output>
Create `.planning/phases/05.1-live-world-editor-productization/05.1-14-SUMMARY.md` when done
</output>
