---
phase: 05.1-live-world-editor-productization
plan: 03
subsystem: live-injection
tags: [channel-protocol, shared-memory, seqlock, napi, cmake, contracts, decoration-persist]

# Dependency graph
requires:
  - phase: 05.1-live-world-editor-productization
    provides: "Plan 01 (decorationPersist.ts kind-aware assembly + cellName result field, sanitizeId), Plan 02 (WorkspaceBindingMeta.mirrorToStockIlf)"
provides:
  - "CAPTURE region kind/cellName extension (offsets 1308/1312) carrying ADD-identity (D-01) and a reused ARM_FAILED reason-string slot (C8), encoded agent-side end-to-end (small DecorationCapture struct + channelWriteCapture)"
  - "New unified LIVE_HOST_CMD region (offsets 1440-1863) — one seqlocked host->agent action-request slot for all six scene/placement/despawn actions, discriminated by LIVE_HOST_CMD_ACTION"
  - "LIVE_CHANNEL_TOTAL_SIZE 1864 consistently on all four touchpoints: contracts constant, channel.h struct, channel.cpp static_assert, channel_binding.cpp CHANNEL_BYTE_SIZE"
  - "REBIND_FLAGS.MIRROR_OFF (0x4) bit for D-10 hybrid-session status-strip groundwork"
affects: ["05.1-05 (agent reader, MIRROR_OFF branch)", "05.1-06 (host writer, mirrorToStockIlf->MIRROR_OFF)", "05.1-07 (channelReadHostCommand/channelWriteHostCommandResult implementation + writeHostCommand N-API binding)", "05.1-08 (renderer HOST_CMD ack protocol, hostCommand.ts)", "05.1-09 (agent-side HOST_CMD consumer)", "05.1-12 (overlay.cpp ADD capture path populates kind/cellName)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Interface-First groundwork: wire-contract byte layout landed as one plan, before any consumer plan, closing the class of build-break-from-stale-size-literal bugs the cross-AI review found (C1)"
    - "Non-contiguous seqlock-span fields (CAPTURE_KIND/CAPTURE_CELL_NAME at 1308/1312 vs the rest of CAPTURE at 400-1024): correctness enforced by physical placement inside the SAME critical section, not by offset adjacency"

key-files:
  created: []
  modified:
    - packages/contracts/src/live-inject.ts
    - packages/live-inject/agent/channel.h
    - packages/live-inject/agent/channel.cpp
    - packages/live-inject/src/channel_binding.cpp
    - packages/live-inject/test/soak.test.ts

key-decisions:
  - "channelWriteCapture's kind/cellName copy lands INSIDE the existing captureSeqCounter critical section (between the two InterlockedIncrement calls), not after it, despite being byte-non-contiguous with the rest of CAPTURE — per Opus's L6 finding, correctness depends entirely on seqlock span membership, not physical adjacency"
  - "LIVE_CHANNEL_TOTAL_SIZE and both C++ size literals (channel.cpp's static_assert, channel_binding.cpp's CHANNEL_BYTE_SIZE) are owned and bumped in THIS SAME plan alongside the struct growth — closes REVIEWS.md C1 BLOCKER (a struct grown in one plan, size literals left stale in files no later plan owned)"
  - "Task commits were reconstructed to be genuinely atomic per task (revert-then-reapply-per-task) rather than one combined commit, even though all edits were originally drafted together — Task 1's commit intentionally leaves channel.cpp's static_assert at the old value (1308), so the agent DLL does not compile until Task 2's commit lands within the same plan; this matches the plan's own stated Interface-First sequencing"
  - "soak.test.ts's two hardcoded byteLength===400 assertions (predating the decoration region) were fixed to read the live LIVE_CHANNEL_TOTAL_SIZE constant — a Rule 1 fix directly caused by this task's own mapping-size growth, bundled into Task 2's commit"

patterns-established:
  - "HOST_CMD region generalizes the existing REBIND/RESULT request/response idiom (seqlocked request + code-before-epoch published result) to one action-enum-discriminated slot instead of N separate channel regions"

requirements-completed: []  # requirements: [pivot-driven] in plan frontmatter is not a REQUIREMENTS.md ID (no match found) — no traceability entry to check off

# Metrics
duration: ~15min
completed: 2026-08-02
---

# Phase 05.1 Plan 03: Wire-Contract Extensions (CAPTURE kind/cellName + HOST_CMD region) Summary

**Grew the live-injection shared-memory channel from 1308 to 1864 bytes across all four size/assert touchpoints in lock-step (contracts, channel.h, channel.cpp static_assert, channel_binding.cpp CHANNEL_BYTE_SIZE), adding a CAPTURE_KIND/CAPTURE_CELL_NAME extension for ADD/arm-failure identity and a new unified LIVE_HOST_CMD region for six host->agent scene/placement/despawn actions — with the agent DLL and host N-API addon both rebuilt and verified compiling clean against the new struct.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-08-02T00:07:00Z (approx)
- **Completed:** 2026-08-02T00:20:22Z
- **Tasks:** 3
- **Files modified:** 5 (4 plan-scoped + 1 collateral test fix)

## Accomplishments

- `DecorationCapture` (contracts) and the small agent-side `DecorationCapture` struct (channel.h) both gained `kind`/`cellName` — the agent-side ENCODE path (`channelWriteCapture`) is real, not just declared, so Plan 12's overlay.cpp ADD-capture path has actual fields to populate
- New `LIVE_HOST_CMD_LAYOUT`/`LIVE_HOST_CMD_ACTION` region (contracts) and matching `LiveState` members + `HostCommand` struct + forward-declared `channelReadHostCommand`/`channelWriteHostCommandResult` (channel.h) — one region for all six actions (RELOAD_CURRENT_SCENE, LOAD_EDITOR_SCENE, TELEPORT, START_PLACEMENT, CANCEL_PLACEMENT, DESPAWN_NODE)
- `LIVE_CHANNEL_TOTAL_SIZE` / `sizeof(LiveState)` static_assert / `CHANNEL_BYTE_SIZE` all bumped from 1308 to 1864 together (closes REVIEWS.md C1 BLOCKER)
- `LIVE_DECORATION_REBIND_FLAGS.MIRROR_OFF` (0x4) added on both sides, ready for Plan 06 (writer)/Plan 05 (reader)
- Agent DLL (`cmake --build`, Release) and host N-API addon (`cmake-js build`) both rebuilt and confirmed compiling clean against the final 1864-byte struct; artifact mtimes verified newer than every edited source file after each rebuild

## Task Commits

Each task was committed atomically:

1. **Task 1: CAPTURE kind/cellName extension (ADD identity, D-01) — contract + agent-side ENCODE** - `1ab1119` (feat)
2. **Task 2: New LIVE_HOST_CMD region + total-size bump on ALL FOUR touchpoints** - `e1a0d47` (feat)
3. **Task 3: REBIND_FLAGS.MIRROR_OFF bit (D-10 in-game hybrid-session warning groundwork)** - `a807c72` (feat)

_Note: Task 1's commit intentionally leaves the C++ side non-compiling (static_assert still reads 1308 while the struct has already grown) — this is deliberate per-task sequencing within the plan; Task 2's commit is what makes the agent DLL compile again with the correct 1864-byte assertion, matching the plan's own explicit "Task 2 owns the C1 fix" framing._

## Files Created/Modified

- `packages/contracts/src/live-inject.ts` — `DecorationCapture.kind/cellName`, `LIVE_DECORATION_LAYOUT.CAPTURE_KIND/CAPTURE_CELL_NAME`, `LIVE_DECORATION_CAPTURE_KIND`, `LIVE_CHANNEL_TOTAL_SIZE=1864`, `LIVE_DECORATION_REBIND_FLAGS.MIRROR_OFF`, new `LIVE_HOST_CMD_LAYOUT`/`LIVE_HOST_CMD_ACTION`
- `packages/live-inject/agent/channel.h` — `LiveState.captureKind/captureCellName/hostCmd*`, small `DecorationCapture` struct gains `kind/cellName`, new `HostCommand` struct, `DECO_CAPTURE_KIND_*`/`HOST_CMD_ACTION_*`/`DECO_REBIND_FLAG_MIRROR_OFF` constants, forward-declared `channelReadHostCommand`/`channelWriteHostCommandResult`
- `packages/live-inject/agent/channel.cpp` — `static_assert(sizeof(LiveState)==1864, ...)`, offsetof asserts for every new field, `channelWriteCapture` extended to copy `kind`/`cellName` inside the existing seqlock span
- `packages/live-inject/src/channel_binding.cpp` — `CHANNEL_BYTE_SIZE=1864`
- `packages/live-inject/test/soak.test.ts` — two hardcoded `byteLength===400` assertions replaced with the live `LIVE_CHANNEL_TOTAL_SIZE` constant (collateral fix, Rule 1)

## Decisions Made

- Reconstructed genuinely atomic per-task commits (revert working tree to HEAD, then reapply each task's edit set in isolation) rather than one combined commit for all three tasks, even though the edits were originally drafted together in one pass — see key-decisions above for why Task 1's commit is intentionally left C++-non-compiling.
- `channelWriteCapture`'s new `kind`/`cellName` copy was placed inside the existing seqlock critical section per the plan's explicit Opus L6 citation, despite the offset gap (1308/1312 vs the CAPTURE region's 400-1024 span).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] soak.test.ts's stale hardcoded byteLength===400 assertions**
- **Found during:** Task 2 (total-size bump to 1864)
- **Issue:** `packages/live-inject/test/soak.test.ts` hardcoded `expect(opened.byteLength).toBe(400)` and `expect(view!.byteLength).toBe(400)` (predating even the original 1308-byte decoration-region growth). Bumping `CHANNEL_BYTE_SIZE`/`LIVE_CHANNEL_TOTAL_SIZE` to 1864 in this task broke both assertions (`expected 1864 to be 400`).
- **Fix:** Replaced both hardcoded `400` literals with the imported `LIVE_CHANNEL_TOTAL_SIZE` contracts constant, so the test tracks the live mapping size instead of a point-in-time snapshot. Left the pre-existing `LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length` constant (a distinct, intentionally-fixed 400-byte read/command-frame span) untouched and documented the distinction in a comment.
- **Files modified:** `packages/live-inject/test/soak.test.ts`
- **Verification:** `pnpm --filter @swg/live-inject run test` — 7 files / 50 tests, all passing.
- **Committed in:** `e1a0d47` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix, Rule 1)
**Impact on plan:** Necessary fallout of this task's own intentional size bump; no scope creep — no other test or source file referenced the stale 400/1308 literals (checked via repo-wide grep).

## Issues Encountered

None beyond the soak.test.ts fallout documented above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Both wire-contract files agree byte-for-byte on the ADD-identity CAPTURE extension (incl. the ARM_FAILED kind) and the new HOST_CMD region — Plan 05 (agent reader/status strip), Plan 06 (host rebind writer), Plan 07 (channelReadHostCommand/channelWriteHostCommandResult implementation + writeHostCommand N-API binding), Plan 08 (renderer HOST_CMD ack protocol), Plan 09 (agent-side HOST_CMD consumer), and Plan 12 (overlay.cpp ADD capture path) can all build against an already-agreed, already-compiling 1864-byte layout without any layout renegotiation.
- Verified live: `npm -w @swg/contracts run build`, the agent `cmake --build` (Release), and `npm -w @swg/live-inject run build` (host addon, via `cmake-js`) all succeed clean on the final committed state; `pnpm --filter @swg/live-inject run test` (50/50) and the renderer's `decorationChannel.test.ts` (8/8, uses `LIVE_CHANNEL_TOTAL_SIZE` for its synthetic buffer) both pass.
- No blockers for the next wave.

---
*Phase: 05.1-live-world-editor-productization*
*Completed: 2026-08-02*

## Self-Check: PASSED

All 5 modified files + SUMMARY.md confirmed present on disk; all 3 task commit hashes (1ab1119, e1a0d47, a807c72) confirmed present in `git log --oneline --all`.
