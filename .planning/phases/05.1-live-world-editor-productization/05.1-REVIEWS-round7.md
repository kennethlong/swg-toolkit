---
phase: 05.1
round: 7
reviewers: [codex, cursor, sonnet, opus, fable]
reviewed_at: 2026-08-01T16:17:11Z
plans_reviewed: [05.1-01-PLAN.md, 05.1-02-PLAN.md, 05.1-03-PLAN.md, 05.1-04-PLAN.md, 05.1-05-PLAN.md, 05.1-06-PLAN.md, 05.1-07-PLAN.md, 05.1-08-PLAN.md, 05.1-09-PLAN.md, 05.1-10-PLAN.md, 05.1-11-PLAN.md, 05.1-12-PLAN.md, 05.1-13-PLAN.md, 05.1-14-PLAN.md, 05.1-15-PLAN.md]
prior_rounds: [05.1-REVIEWS-round2.md, 05.1-REVIEWS-round3.md, 05.1-REVIEWS-round4.md, 05.1-REVIEWS-round5.md, 05.1-REVIEWS-round6.md]
replan_reviewed: 7cd2b59
---

# Cross-AI Plan Review — Phase 05.1 — Round 7 (post-X1–X13 convergence check)

**Angles (de-anchoring, non-overlapping):** Codex = seam-propagation tracer of every contract commit
`7cd2b59` touched, across all 15 plans · Cursor = line-level citation + wiring verification of the five
edited plans against the real `.ts`/`.tsx` files · Sonnet = lateral hunt for NEW failure modes the X-fixes'
interactions opened · Opus = locked-decision fidelity (D-01–D-14) + spec math + explicit rulings on the
X8/X13 "disclosed, not fixed" dispositions · Fable = fact-check of ONLY the claims commit `7cd2b59` added.
Task files: `.planning/research/CONSULT-05.1-R7-*.md`, raw outputs in the sibling `*.out` files.

**Verdicts:** Codex NOT CONVERGED (1 HIGH) · Cursor NOT CONVERGED (1 HIGH, 2 MED, 2 LOW) · Sonnet NOT
CONVERGED (1 HIGH, 1 MED) · Opus NOT CONVERGED (2 HIGH, 4 MED, 3 LOW) · Fable CONVERGED on axis
(30 new claims: 28 byte-accurate, 2 minor-drift, **0 fabricated — the zero-fabrication streak is restored**).

**Bottom line:** NOT CONVERGED — **4 distinct HIGHs** after cross-reviewer dedup (work-list Z1–Z18 below).
Z1 carries this round's convergence-from-divergence signature: Opus (decision-fidelity) and Sonnet
(lateral) independently proved the same X4 seam broken in two different ways — the "a failed Undo is
NEVER invisible" must_have is unsatisfiable as specified. The half-propagated-seam pattern also recurred
for a **fifth** consecutive round, twice: the X2 sweep missed a fourth restatement of the pre-hedge
absolute (Z3, Plan 10's acceptance criteria), and the X8 disclosure was bolted on while the contradicting
absolutes it supersedes were left in force (Z2) — the exact defect class X2 fixed, recurring inside the
commit that fixed it. The good news: X1/X2(core)/X12 are confirmed fully closed by two reviewers each,
all four round-6 wiring contracts are implementable as written, threat-register/gap-ledger/wave math is
exact, and the plans' factual layer is back to zero fabrications.

**Positive convergence (verified clean — do not re-open):**
- X1 closed (Codex seam trace + Cursor wiring check + Fable cites): `getState().studioDir` everywhere,
  hook-selector form survives only as an explicit prohibition; precedents byte-accurate.
- X2 closed in Plans 06/10 must_haves (Opus + Codex): all seven touched clauses carry the
  `diskState:'uncertain'` exception; no mutually exclusive must_haves pair remains anywhere (Codex;
  but see Opus Y1/Y2 for two satisfiability-level tensions Codex's wording-level trace did not count).
- The `suppressNextDiffRef` identity-key type is declared and consumed type-identically across
  Plans 13/14 (Cursor + Opus + Fable); the count-map pre-credit is multiset-correct for duplicate
  tuples (Sonnet); a failed Undo cannot leave a stale suppress key (Sonnet).
- Dependency graph acyclic, all 55 threat IDs unique and resolvable, gap-ledger (a)–(i) collision-free,
  step math consistent (Codex + Opus).
- Opus rulings: **X8 disposition LEGITIMATE conditional on Z2**; **X13 disposition LEGITIMATE but its
  rationale is false and must be rewritten (Z6)**.

---

## Work-list Z1–Z18 (deduped, severity-ordered)

| ID | Sev | Source(s) | Plan(s) | Defect |
|----|-----|-----------|---------|--------|
| Z1 | HIGH | Opus Y1 + Sonnet L1 (independent angles, same seam) | 13, 14 | X4's "a failed Undo is NEVER invisible" must_have (13:31) is unsatisfiable as specified. (a) Opus: the thrown-add-back branch calls `restore(entry.id)` FIRST, and the mandated "near-verbatim copy" of `DeleteUndoToast`'s pending-diff effect turns an entry leaving `pending` into an affirmative `'restored'`-variant toast (no entry id, no Undo button) — that failure branch renders a **false success confirmation**, with no specified effect ordering between the two store writes. (b) Sonnet: the single-slot toast shows an error only while the failed entry is the currently-displayed one, and `handleUndo` calls `clearUndoError()` unconditionally for ANY entry — so Remove/Undo on row B permanently erases row A's failure, leaving A stuck in `pending` with zero UI path to see or retry it. Fix must respecify the error-display contract (per-entry error keying or scoped clear; ordering/suppression so a failed add-back cannot present as 'restored'; a surviving affordance for stuck pending entries) or honestly narrow the must_have. |
| Z2 | HIGH | Opus Y2 | 14, 15 | X8's hedge was ADDED as a new must_have (14:23) without amending the old absolutes it supersedes: 14:19 still asserts unconditional "(NEW)" marking, 14:42's `sketch_elements` line ("marked distinctly as new" — the authoritative UI contract per AGENTS.md) has no caveat, and 15:14/step 4 still demand an unqualified 021-A diff. Same defect class as X2, recurring inside the commit that fixed X2. Apply the Plan 06:17/:27 "EXCEPT the disclosed …" construction to all three sites. |
| Z3 | HIGH | Codex R7-S1 | 10 | 10:578's ROUND-4/W1 acceptance assertion still says ANY non-empty `failures` result produces unconditional "no building's mirror was changed" wording — the fourth restatement the X2 sweep missed. Condition it on every entry carrying `diskState:'unchanged'`, deferring the `'uncertain'` case to the V7 hedged-wording contract. |
| Z4 | HIGH | Cursor F1 | 14 | 14:277-280/:312-318 treat Plan 08 Task 3's `recordPersistResult` wiring in `useChannelReader.ts` as "ALREADY-LANDED". Ground truth: zero `recordPersistResult` hits anywhere in `packages/renderer/src`; `useChannelReader.ts` has no `worldEditorStore` import and only logs on RESULT (:275-280). Relabel as Plan 08 forward output and make Plan 14's trigger contingent on it. |
| Z5 | MED | Sonnet L2 | 13, 14 | `suppressNextDiffRef` is a single scalar consumed by a deferred passive `useEffect` — a second successful `handleUndo` can clobber the first's unconsumed identity key before the diff pass flushes (no queue/guard specified), mislabeling the first restore "(NEW)". Specify a queue, an already-pending guard, or an explicit single-flight constraint on Undo. |
| Z6 | MED | Opus Y3 | 13, 15 | X13's recorded rationale is false: 13:219 calls the history asymmetry "LOAD-BEARING for Plan 14's V2 fix (silent re-baseline)" but V2's fix was precisely to STOP keying the diff on history growth, and X3 (same commit) deleted the skip-wholesale behavior. Rewrite 13:219 and 15:101/:307 as a cosmetic completeness gap that is free to fix later; also name the D-06 tension (an Undo add-back is a persist by D-06's wording) per the D-02 disclosure precedent. |
| Z7 | MED | Opus Y4 | 15 (06/10 context) | The `diskState:'uncertain'` carve-out qualifies LOCKED D-09 ("Disk always matches what the toggle claims — no stale mirrors") with no sign-off disclosure: no gap-ledger item, and step 13's maintainer CONTEXT.md annotation names only D-02. Add a ledger item (j) for T-05.1-06d and extend step 13 to cover D-09. |
| Z8 | MED | Opus Y5 | 15 | Step 8's X9 extension observes only what the superseded blanket-boolean design would ALSO produce; the observation that distinguishes X3's fix (an unrelated new row in the same window IS still marked "(NEW)") has no live step and 15:82-84 forecloses adding one. Add that check to step 8. |
| Z9 | MED | Opus Y6 | VALIDATION (15 context) | `05.1-VALIDATION.md:123`/`:149` still cite "Plan 15 step 9" for D-10's both-surfaces check; a later-round insertion moved it to step 10. Same dangling-cross-ref class as X12, half-swept. Fix both cites. |
| Z10 | MED | Cursor F2 | 13 | 13:266-267 (also :453, :147, :708) anchor the presentational/subscribe precedent on `DeleteUndoToast.tsx:96-101`, which is only the try/success half of `handleUndo`; the pending-diff subscription is the `useEffect` at :68-92. Re-anchor each cite to the span that carries the claimed behavior. |
| Z11 | LOW | Cursor F3 | 13 | The interfaces block still cites "toast shell at lines 118-218"; the delete-variant shell is :146-166 (restored variant :120-137). The plan's own ROUND-6 correction elsewhere is right; the interfaces block was missed. |
| Z12 | LOW | Cursor F4 | 13 | 13:448: `liveStore.ts:26-31` for the `ConnectionStatus` union — union is :27-31 (:26 is a comment). |
| Z13 | LOW | Cursor F5 + Fable #2 | 06 | 06:144/:323 call :203 a "comment"; :203 is a `logf(...)` trace string — the true comment block is :197-198. Substance exact; fix the label. |
| Z14 | LOW | Opus Y7 | 15 | X9's body flagged V4 as having no live step; only the "Fix:" line was applied. Step 11's two buildings never share a stock template — the exact condition V4's per-resolved-mirror-path grouping exists for. Require shared-stock-template buildings in step 11. |
| Z15 | LOW | Opus Y8 | 15 | 15:17's "explicitly recorded at sign-off" must_have still enumerates only C-era gaps; extend it to cover the ledger's newer entries (V8, V13, X8→(h), X13→(i), and Z7's (j)). |
| Z16 | LOW | Opus Y9 | 15 | 15:269-272 instructs the verifier to confirm unqualified "NO building's mirror was changed" wording. Benign in step 11's own all-`'unchanged'` scenario, but add the one-line qualifier so the verifier text matches 10:26's conditional contract. |
| Z17 | LOW | Fable #14 note | 13 | 13:565 (untouched pre-existing line) still says DeleteUndoToast "is mounted at its own panel's top level"; it is mounted globally in App.tsx (:27 import, :105 render) — contradicts the plan's own X5 correction. |
| Z18 | LOW | Fable #7 | 10 | 10:321/:479 describe `DeployDialog.tsx:139,185` as the "getState()-in-handlers idiom"; those two are useEffect-body reads (only :327 is a click handler). The load-bearing point (imperative, non-render context) holds — adjust the phrasing. |

---

## Codex Review (seam-propagation trace)

NOT CONVERGED

**Findings**

| ID | severity | file:line | what is stale/contradictory | evidence |
|---|---|---:|---|---|
| R7-S1 | HIGH | `.planning/phases/05.1-live-world-editor-productization/05.1-10-PLAN.md:578` | Stale `diskState` acceptance wording. This assertion still says any non-empty `failures` result produces unconditional "no building's mirror was changed" wording. Post-round-6 contract only allows that when all failures are `diskState: 'unchanged'`; any `'uncertain'` must hedge. | `ROUND 4/W1 assertion: a non-empty failures result produces a history-entry message stating no building's mirror was changed` |

**Per-Seam Trace**

`suppressNextDiffRef` boolean -> identity-key object

| reference | status |
|---|---|
| `05.1-13-PLAN.md:30` | OK: must-have says identity key, not blanket boolean. |
| `05.1-13-PLAN.md:55` | OK: seam contract check requires identity key. |
| `05.1-13-PLAN.md:153-159` | OK: includes old `current = true` only as round-5 history, then explicitly says round 6 refines it to identity key. |
| `05.1-13-PLAN.md:170-178` | OK: identifies boolean as old defect and replaces with `{ buildingId, cellName, objectTemplateName }`. |
| `05.1-13-PLAN.md:312-315` | OK: declares `useRef<{ buildingId; cellName; objectTemplateName } | null>`. |
| `05.1-13-PLAN.md:501-510`, `570-575`, `618-640`, `686-687`, `732-735` | OK: all current implementation/test wording uses restored-row identity key, not boolean. |
| `05.1-14-PLAN.md:21` | OK: must-have consumes identity key and says diff still runs. |
| `05.1-14-PLAN.md:129-140` | OK: old boolean behavior described as the fixed defect. |
| `05.1-14-PLAN.md:340-355` | OK: current algorithm consumes non-null identity key and pre-credits only that tuple. |
| `05.1-14-PLAN.md:421-435`, `477-479`, `505-507`, `523-526` | OK: tests/threats/success criteria reflect identity-key, non-blanket suppression. |
| `05.1-15-PLAN.md:82-89`, `237-245`, `361-364` | OK: sign-off checks identity-scoped suppression through observable Undo refresh behavior. |

`resolveOverridePair()` / `studioDir` store access

| reference | status |
|---|---|
| `05.1-04-PLAN.md:74`, `213-214`, `281` | OK: generic caller-owned `studioDir`/`readWorkspaceJson` notes; no stale hook-selector contract. |
| `05.1-08-PLAN.md:21`, `355-358`, `427-436`, `482-519` | OK: `useChannelReader.ts` threads `studioDir` via `useWorkspaceStore.getState().studioDir`; current source still lacks this, matching Plan 08 as future work. |
| `05.1-10-PLAN.md:28-31` | OK: must-haves require one shared helper, null guard, and imperative `getState()` access. |
| `05.1-10-PLAN.md:128-133`, `243-248`, `309-345`, `474-505`, `592-600`, `615-620`, `664-669` | OK: consistently guards nullable `studioDir` before `readWorkspaceJson`, returns it in the pair, and forbids hook-selector use inside the plain event-time helper. Ground truth: `workspaceStore.ts:34` is `studioDir: string | null`. |
| `05.1-13-PLAN.md:24`, `29`, `79`, `139-150`, `307-309`, `444-491`, `516-529`, `567-579`, `707-708`, `725-731` | OK: Plan 13 consumes Plan 10's helper and does not introduce a second resolution path. |
| `05.1-15-PLAN.md:188` | OK: checkpoint references the shared null-safe helper broadly; no stale pre-X1 hook behavior. |

`diskState: 'unchanged' | 'uncertain'` / absolute "unchanged" wording

| reference | status |
|---|---|
| `05.1-06-PLAN.md:17`, `27-29` | OK: must-haves explicitly carve out rollback double-fault as `diskState: 'uncertain'`. |
| `05.1-06-PLAN.md:150-154`, `294-356`, `376-391`, `442-443`, `520-522`, `566`, `591-592` | OK: function signature and behavior distinguish `'unchanged'` from `'uncertain'`. |
| `05.1-10-PLAN.md:26`, `30` | OK: must-haves split all-unchanged wording from hedged uncertain wording. |
| `05.1-10-PLAN.md:106-108` | OK historical round-4 prose, superseded immediately by V7 lines `135-141`. |
| `05.1-10-PLAN.md:135-141`, `378-388`, `514-516`, `601-605`, `665-672` | OK: current behavior/test/success criteria hedge when any failure is `'uncertain'`. |
| `05.1-10-PLAN.md:578-580` | STALE: verification assertion still applies unconditional "no building's mirror was changed" to any non-empty `failures`. |
| `05.1-15-PLAN.md:18`, `262-273`, `360-361` | OK: sign-off case is specifically mixed known/unknown validation failure, which is `diskState: 'unchanged'`; no rollback double-fault claim. |

`removeUndoStore.lastUndoError` / `setUndoError` / `clearUndoError`

| reference | status |
|---|---|
| `05.1-13-PLAN.md:31-32` | OK: must-haves require observable `lastUndoError` and missing-building fail-closed behavior. |
| `05.1-13-PLAN.md:180-190` | OK: X4 fix adds `{ id, message } | null`, `setUndoError`, `clearUndoError`, and toast timer re-arm. |
| `05.1-13-PLAN.md:473-480`, `557-573`, `604-618`, `659-667`, `685-686`, `709`, `736-737` | OK: implementation/test/threat wording consistently sets on every failed Undo branch and clears at next attempt. Ground truth for precedent is consistent: `DeleteUndoToast.tsx:96-111` catches undo failure, swaps toast content, and re-arms timer. |
| `05.1-15-PLAN.md:82-84`, `99-101`, `305-307`, `365` | OK: sign-off treats X4 as closed and records the separate accepted Undo-history asymmetry. |

**Must-Haves / Graph**

No mutually exclusive `must_haves` pair found across the 15 plans. The apparent tensions are scoped: Plan 14's first-pass/remount narrowing is explicitly accepted, and Plan 15 records it.

Dependency graph is acyclic and wave-ordered. All 15 `depends_on` references resolve, and every dependency points to an earlier wave.

---

## Cursor Review (line-level citation & wiring verification)

**Verdict: `NOT CONVERGED`**

One HIGH wiring claim is false against current renderer source; several MED/LOW citation defects remain in Plan 13's `DeleteUndoToast` line ranges and one stale interfaces block.

### Findings

| ID | Severity | plan file:line | cited claim | what the real file actually shows |
|----|----------|----------------|-------------|-----------------------------------|
| F1 | **HIGH** | `05.1-14-PLAN.md:277-280`, `:312-318` | Plan 08 Task 3's **`recordPersistResult` wiring is "ALREADY-LANDED"** in `useChannelReader.ts`; ADD confirm is triggered by `worldEditorStore.history` growth from that path | `useChannelReader.ts` has **no** `recordPersistResult`, **no** `useWorldEditorStore`, and **no** `worldEditorStore` import. On RESULT it only logs (`:275-280`). Grep across `packages/renderer/src` returns **zero** `recordPersistResult` hits. Plan 14's history-growth / two-surface-confirm trigger is **not wired today**. |
| F2 | **MED** | `05.1-13-PLAN.md:266-267` (also `:453`, `:147`, threat `:708`) | `DeleteUndoToast.tsx:96-101` is the precedent for presentational/subscribe behavior | `:96-101` is only the **start** of `handleUndo` (try + `restore()` + success comment). Pending-diff subscription is in the **`useEffect` at `:68-92`**, not `:96-101`. Using `:96-101` for "subscribes / presentational precedent exactly" names the **wrong span**. |
| F3 | **MED** | `05.1-13-PLAN.md:266-267` | "toast shell at lines **118-218**" | `:118` is `if (toast.kind === 'none') return null;` — not a shell. Delete-variant shell is `:146-166`; restored variant is `:120-137`. The plan's own ROUND-6 correction (`:146-166`) is accurate; the **interfaces block still cites 118-218** and was not updated. |
| F4 | **LOW** | `05.1-13-PLAN.md:448` | `liveStore.ts:26-31` — `ConnectionStatus` union; `mappingName` only on `'attached'` | Union is **`27-31`**; `:26` is a comment. `:30` is correct. |
| F5 | **LOW** | `05.1-06-PLAN.md:144`, `:323` | "`:203`'s own **comment** documents the per-template scope" | `:203` is a **`logf(...)` call**, not a comment. Per-template scope is documented in the comment block at **`197-198`** and the log string at `:203`. Substance is right; line kind is mislabeled. |

### Wiring feasibility (the four contract checks)

1. **`useWorkspaceStore.getState().studioDir` — feasible, precedent-accurate.** `workspaceStore.ts` exposes top-level `studioDir: string | null` (`:34`); `DeployDialog.tsx:139/:185/:327` are all real `getState().studioDir` reads. Plan 10's getState()-in-handler pattern is implementable exactly as written.
2. **`suppressNextDiffRef` identity key — feasible, names consistent across plans 13/14.** `IlfNode` carries `objectTemplateName`/`cellName` (`ilf.ts:24-28`); plans 13/14 agree on ref type and producer/consumer split. No cross-plan naming conflict.
3. **`RemoveUndoToast` error re-arm mirroring `DeleteUndoToast.tsx` — precedent exists and matches claimed behavior** (catch/content-swap/re-arm at `:102-110`). Plan 13's `:96-111`/`:102-110` citations accurate; the `:96-101`-only citations are not (F2). Architectural note (not a defect): `DeleteUndoToast` calls `restore()` inside the toast (`:96-98`); Plan 13 correctly requires `onUndo` delegation because undo needs `resolveOverridePair()` from `WorldPanel.tsx`.
4. **`useWorldEditorStore.getState().tree.find(...)` — not built yet; consistently specified** across plans 06/10/13/14 (`tree: WorldEditorBuilding[]`, `buildingId`, `recordArmFailure`, `history`, `selectedRowId`, `sessionOverlay`). Implementable once Plan 04 lands.

Other wiring notes (no additional HIGHs): Plan 06 `makeReadVfs` currently module-private at `decorationPersistOrchestrator.ts:102` (export is planned work — feasible); `DecorationPersistResult.cellName`, `formatPersistMessage`, `hostCommand.ts`/`sendStartPlacement`, and `WorldPanel.tsx`-local `resolveOverridePair()`/`refreshTree()` are all correctly labeled forward outputs — internally consistent.

**Citation tally:** 38 checked, 33 byte-accurate, 5 defective (F1–F5; F1 the only HIGH). Convergence fails on Plan 14's "already landed" `recordPersistResult` claim plus residual Plan 13 line-range drift contradicting the plan's own ROUND-6 corrections. Full byte-accurate check table in `.planning/research/CONSULT-05.1-R7-cursor-line-verify.out`.

---

## Sonnet Review (lateral / new-seam hunt)

CONVERGED: NO

| ID | severity | plan file:line | new failure mode | concrete trigger sequence |
|----|----------|-----------------|-------------------|----------------------------|
| L1 | HIGH | `05.1-13-PLAN.md:187,474,558` vs `DeleteUndoToast.tsx:79,85` | Round-6's X4 `lastUndoError` fix only helps while the failed entry is the toast's *currently-displayed* one. `RemoveUndoToast` is a "near-verbatim structural copy" reusing the single-slot pending-diff pattern (`added[added.length - 1]` / `removed[removed.length - 1]` — only the latest entry is ever rendered). Error display is gated on that slot ("when it names the CURRENTLY-DISPLAYED toast's entry", 13:187/:558), and `handleUndo` unconditionally wipes the error signal on every new attempt for ANY entry (13:471-474). Net: a failed Undo's error is visible only until the user touches any other row; the failed entry then sits silently, permanently stuck in `removeUndoStore.pending` with zero remaining UI path to discover or retry it — directly undercutting X4's stated goal (13:31). | 1. Remove row A. 2. Undo A fails (`resolveOverridePair()` null) → error toast for A, A stays in pending. 3. Remove row B → single-slot toast switches to B; A's error leaves the screen. 4. Undo B → `clearUndoError()` wipes A's error entirely. 5. A remains in pending forever, failure reason gone, no surviving affordance. |
| L2 | MEDIUM | `05.1-13-PLAN.md:174-179` vs `05.1-14-PLAN.md:331,345,400` | `suppressNextDiffRef` is a single scalar, not a queue, consumed only by a passive deferred `useEffect(..., [tree])` (NOT `useLayoutEffect`, no synchronous consumption; the "IMMEDIATELY clear the ref" step happens only when the effect eventually runs). Nothing prevents a SECOND successful `handleUndo` from overwriting the ref before the first `refreshTree()`-triggered diff pass has flushed and consumed it — no mutex, queue, or already-pending guard is specified in either plan. | 1. Undo A sets ref={A} + refreshTree(); passive diff effect not yet flushed. 2. Undo B (different row) sets ref={B}, clobbering A's unconsumed key. 3. Coalesced diff pass pre-credits only B; A's restored row is misdiagnosed "(NEW)". |

Also checked and found consistent (not findings): (a) the suppress ref is set only on `onUndo`'s SUCCESS path and `refreshTree()` likewise (13:479-500) — a failed Undo can never leave a stale suppress key; (b) the count-map pre-credit design is multiset-correct: it absorbs exactly one restored instance and still marks a genuinely concurrent duplicate placement "(NEW)" regardless of how many identical-tuple rows coexist (so long as one ref-set survives per diff pass — see L2); (c) hypothesized "last-decoration-removal makes a building vanish, breaking X6's lookup" is FALSE — `scanWorldEditorState` (05.1-04-PLAN.md:165-172) lists buildings by `edit_*.ilf` existence, not decoration count.

---

## Opus Review (locked-decision fidelity & spec math)

**NOT CONVERGED** (2 HIGH, 4 MED, 3 LOW on this angle)

| ID | Sev | Location | Violated decision / broken math | Evidence (quoted / measured) |
|----|-----|----------|--------------------------------|------------------------------|
| Y1 | HIGH | `05.1-13-PLAN.md:31` vs `:494`, `:551`, `:558` | New X4 must_have is unsatisfiable on one of the three branches it names; collides with the "structural copy of DeleteUndoToast" action. D-03 (undo toast is the *only* guard). | must_have `:31`: "A failed Undo (a null resolveOverridePair() result, a not-found building, **or a thrown add-back**) is NEVER invisible to the toast". Display rule `:558`: swap only when `lastUndoError` "names the id of the entry THIS toast is currently displaying". But the thrown-add-back branch runs `restore(entry.id)` FIRST — `:494` concedes "this path already lost the popped entry from `pending`". `:551` mandates a near-verbatim copy of the precedent, whose real bytes turn an entry *leaving* `pending` into an affirmative success toast with **no entry id** (`DeleteUndoToast.tsx:73` → `:76-83`, `'restored'` variant `:120-139`, no Undo button). On that branch the failure renders a **false "restored" confirmation**; both store writes land in one commit with no specified effect ordering. Two must_haves cannot both hold. |
| Y2 | HIGH | `05.1-14-PLAN.md:19` vs `:23`; also `:42` (`sketch_elements`), `05.1-15-PLAN.md:14` | X8's hedge was added as a NEW must_have without amending the OLD absolute it supersedes — the exact X2 defect class, recurring **inside the commit that fixed X2**. | `:19` (unamended): "The freshly-added row **is** visually marked '(NEW)' … matching 021-A Frame 3". `:23` (new): "…a mid-flow tab navigation … **can silently swallow that in-flight ADD's '(NEW)' marking**". Four lines apart; an executor can satisfy either reading. Contrast Plan 06 `:17`/`:27`, which carry explicit "EXCEPT the disclosed T-05.1-06d …" clauses. 14:42's `sketch_elements` line and 15:14 are likewise unqualified — and AGENTS.md makes sketch elements the authoritative UI contract. |
| Y3 | MED | `05.1-13-PLAN.md:219`; propagated to `05.1-15-PLAN.md:101`, `:307`, `05.1-13-PLAN.md:743` | X13's stated justification is factually false and self-contradicting. | `13:219`: the asymmetry is "LOAD-BEARING for Plan 14's V2 fix (Undo's refresh must be a **silent re-baseline**, not a history-growth event)". Plan 14 `:332-336` says the opposite is what V2 fixed (diff keyed on `tree`, NOT `history.length`), and X3 — same commit — deleted the skip-wholesale behavior ("the diff is NEVER skipped wholesale", 14:337+). The note then contradicts itself citing Opus's round-6 verification that a future history entry would NOT break the suppress mechanism. Risk: a future maintainer treats a non-existent coupling as a constraint. |
| Y4 | MED | `05.1-15-PLAN.md:276-311` (ledger a–i), `:312-315` (step 13); vs D-09 | A LOCKED decision is now qualified by plan text with **no** sign-off disclosure. | D-09 (CONTEXT `:89-91`): "Disk always matches what the toggle claims — no stale mirrors". Plans 06/10 carve the `diskState:'uncertain'` exception into it (`06:17`, `:27`, `:380`, `10:26`/`:30`). The phase's established treatment for exactly this (D-02's narrowing) is gap-ledger item **(e)** *plus* step 13's maintainer CONTEXT.md annotation; the D-09 carve-out has neither. |
| Y5 | MED | `05.1-15-PLAN.md:242-246`, `:82-84` | Step 8's X9 extension does not demonstrate what its own text claims; X3's distinguishing symptom has zero live coverage. | The step-8 observation ("no '(NEW)' badge after one more unrelated refresh") is produced identically by the superseded blanket-boolean design (which also advanced `prevTreeRef`). The observation that distinguishes X3's fix — an unrelated new row in the same window IS still marked — has no step, and `:82-84` forecloses one ("X3/X4 … CLOSED by real code fixes … no checkpoint change needed"). |
| Y6 | MED | `05.1-VALIDATION.md:123`, `:149` → `05.1-15-PLAN.md:248-260` | Stale cross-plan step cites for LOCKED D-10; same dangling-cross-ref class X12 fixed, half-swept. | Both cite "Plan 15 Task 2, step 9" for D-10's both-surfaces check; current step 9 (`:248-252`) is the SC1 spot check, the D-10 check is step 10 (`:254-260`). Measured: at `d487a9a` D-10 *was* step 9; a later insertion shifted it and the cites were never updated. D-10's only Manual-Only verification row now points at the wrong step. |
| Y7 | LOW | `05.1-15-PLAN.md:85-89`, `:262-273` | X9 answered only its "Fix:" line; the V4 half named in the finding body is silently dropped. | X9's body: "V4 has no live step". Step 11 exercises TWO buildings but never requires them to **share a stock template** — the exact condition V4's per-resolved-mirror-path grouping/rollback (`06:28`) exists for. Zero hits for shared/same-template/mirror-path in the verify steps. |
| Y8 | LOW | `05.1-15-PLAN.md:17` vs `:276-311` | The "explicitly recorded at sign-off" must_have was not extended with the ledger. | `:17` still enumerates only C-era gaps; the newer disclosures (V8/V13/X8/X13) rest on step text alone, not on a must_have. |
| Y9 | LOW | `05.1-15-PLAN.md:269-272` | Verifier-facing restatement of the pre-X2 absolute. | Step 11: confirm "wording that says NO building's mirror was changed (never a per-building mixed-outcome message)" — unqualified, whereas `10:26` now conditions that wording on every entry carrying `diskState: 'unchanged'`. Benign in step 11's own scenario; a fourth restatement the X2 sweep did not reach. |

**Verified clean on this angle (do not re-open):** X2 fully closed for Plans 06/10 must_haves (`06:17`, `:27`, `:365-372`, `:377-391`; `10:26` conditional, defers to `10:30`); X1 closed (`10` must_haves, `:309-316`, `:473-478` — hook-selector form survives only as prohibition); X12 closed (`T-05.1-15a` real at `15:336`); threat-register math exact (55 IDs, each exactly one register row; `T-05.1-13f`/`14h`/`14i`/`15a` unique, correctly cross-referenced, no collisions with `T-05.1-06a/b/c/d`); gap-ledger (a)–(i) each exactly once, in order, each tied to a named finding; step-count math consistent (13 steps: 1–11 verification, 12 ledger, 13 maintainer note; round 6 inserted no numbered step, so Y6 is inherited drift, not new); wave math acyclic (0{01-04} · 1{05,06,07} · 2{08,09,10} · 3{11,12} · 4{13} · 5{14} · 6{15}), every `depends_on` strictly earlier-wave; 13→14 `WorldPanel.tsx` seam correctly ordered, ref types verbatim-identical (`14:339-341` = `13:312`); D-01, D-02, D-04, D-05, D-07/D-07b, D-08, D-11, D-12, D-13, D-14 uncontradicted; D-03's mechanism-level reinterpretation (WorldPanel owns the add-back via `onUndo`) preserves the decision's substance and is documented at `13:169-175`.

**X8 disposition: LEGITIMATE — conditional on Y2 being fixed.** Disclosure is defensible on the merits: X8 breaks no numbered success criterion and no locked decision (SC4 requires only that the persisted `.ilf` gains a surviving row; SC2 is scoped to 019-A, not 021-A). Under the narrowing the row still appears on remount (`refreshTree()` runs) — only the "(NEW)" badge is lost, only on a mid-flow remount, and ledger (h) makes the verifier exercise that path and record observed behavior. The permanent fix (hoist baseline into `worldEditorStore`) composes with X3's identity key and also closes ledger (g), so deferring it as one coherent future change is proportionate, not evasive. What invalidates it currently is execution, not judgment: the disclosure coexists with its own contradicting absolutes (14:19, 14:42, 15:14/step 4). Amend those with the Plan 06 "EXCEPT …" construction and the disposition stands.

**X13 disposition: LEGITIMATE — but the stated rationale is false and must be rewritten (Y3).** No success criterion touches history completeness; D-06 defines history as a session-only play-by-play, rendered by Plan 11 `:127-128` as a most-recent-first chronological list, not a per-row status field; the recorded "removed — reload scene to see it gone" line remains historically true — the defect is an incomplete log, not a false claim about disk. The row itself is corrected on disk and in the tree. Ledger (i) gives it a real live check. LOW severity and "document, fix later" are right. Two caveats the replan must absorb: (1) rewrite the false "load-bearing" justification (13:219, 15:101/:307) as a cosmetic completeness gap, free to fix later; (2) name the D-06 tension (an Undo add-back is a persist by D-06's own words — "this session's persists/failures") per the D-02/(e)/step-13 precedent — the same omission Y4 flags for D-09.

---

## Fable Review (fact-check of NEW claims in commit 7cd2b59)

**Verdict: CONVERGED** (zero fabricated/defective claims among the lines commit 7cd2b59 added; 2 minor-drifts, both cosmetic phrasing, substance exact).

**30 claims checked: 28 byte-accurate, 2 minor-drift, 0 fabricated.** (Plus 1 not-checkable future-code claim, excluded from the tally.) Full 31-row claims ledger in `.planning/research/CONSULT-05.1-R7-fable-factcheck.out`. Highlights:

- All `decorationPersist.ts:199-203` / `workspaceStore.ts:34` / `DeleteUndoToast.tsx` (`:48`, `:68-92`, `:96-111`, `:102-110`, `:146-166`) / `useChannelReader.ts:272` cites byte-accurate.
- X2's seven amended clauses internally consistent across Plans 06/10; X12's "no STRIDE register pre-commit" claim verified against `git show 7cd2b59^`; step math (13 steps, ledger a–i) verified; round-5→6 attribution tags (V2/V4/V6/V7, X3–X7/X13 reviewer attributions) all match the archived review files.
- Minor-drift #2: 06:143-145 calls `decorationPersist.ts:203` a "comment" — content exact but it is a `logf(...)` trace string; the true comment is :197-198 (→ Z13).
- Minor-drift #7: 10:321/:479 call `DeployDialog.tsx:139,185` "handlers" — they are useEffect-body reads; only :327 is a handler. Load-bearing point (imperative getState outside render) holds (→ Z18).
- Out-of-scope note: untouched line 13:565 still says DeleteUndoToast "is mounted at its own panel's top level"; App.tsx mounts it globally (:27/:105) — contradicts the plan's own X5 correction (→ Z17).

---

## Consensus Summary

### Agreed strengths (2+ reviewers)
- All four round-6 wiring contracts (getState() studioDir, identity-key suppress ref, error re-arm precedent, tree lookup) are implementable exactly as written (Cursor, Codex, Fable).
- X1, X2 (core must_haves), and X12 are fully closed (Codex, Cursor, Opus, Fable).
- Structural math — waves, depends_on, threat IDs, gap-ledger letters, step counts — is exact (Codex, Opus).
- The factual layer is back to zero fabrications (Fable; Cursor's landed-file citation audit concurs at 33/38 byte-accurate with no fabrication-class defects).

### Agreed concerns (highest priority)
- **Z1** — the X4 "never invisible" contract fails two independent ways (Opus Y1 false-'restored' branch; Sonnet L1 single-slot + unconditional clear). Two reviewers, two non-overlapping angles, one seam: the round's strongest signal.
- **The half-propagated-seam pattern is now 5 rounds old and recurred inside the fix commit itself** (Opus Y2 on X8; Codex R7-S1 + Opus Y9 on X2's fourth restatement). The next replan must grep for every restatement of a superseded absolute — must_haves, sketch_elements, acceptance criteria, AND verifier-facing step text — before claiming a seam closed.

### Divergent views
- Codex found no mutually exclusive must_haves pair; Opus's Y1/Y2 argue two such tensions exist (13:31 vs the copy-mandated mechanism; 14:19 vs 14:23). The difference is depth of analysis, not contradiction — Codex traced wording consistency, Opus traced mechanism consequences. Treat Opus's stricter reading as governing for Z1/Z2.
- Sonnet L2 (ref clobber) assumes multi-row Undo bursts inside one flush window; no other reviewer examined timing. MED stands unless the replan specifies a single-flight constraint.

**Next:** `/gsd:plan-phase 5.1 --reviews` to close Z1–Z18.
