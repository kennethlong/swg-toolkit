---
phase: 05.1-live-world-editor-productization
plan: 15
type: execute
wave: 6
depends_on: ["05.1-14", "05.1-11", "05.1-05"]
files_modified:
  - .planning/handoff/2026-XX-XX-CHANGE-REQUEST-getContainingCellName.md
  - packages/renderer/src/panels/world/WorldPanel.tsx # checkpoint-verified surface (Task 2), not edited — R9 review, BB18
  - packages/live-inject/agent/overlay.cpp # checkpoint-verified surface (Task 2), not edited — R9 review, BB18
autonomous: false
requirements: [pivot-driven]
must_haves:
  truths:
    - "Rotation edits persist and reload correctly, live-verified with the same rigor the handoff's proven translate smoke test used (SC5)"
    - "The World panel matches sketch 019-A element-for-element, the HUD matches 020-A, and the spawn flow matches 021-A — including the '(NEW)' row marker on Frame 3 — verified by an observed/missing diff against each sketch (SC2), EXCEPT the disclosed X8 mid-flow-remount case (ledger item (h)), where that ONE in-flight ADD's '(NEW)' marking may be silently lost on remount; every OTHER ADD in the diff must still show it (ROUND 7, Z2)"
    - "The one genuine provider gap this phase does not solve (cell-name resolution for a brand-new placement) is filed as a change-request handoff, not silently left undocumented"
    - "D-10's mirror-off hybrid-session warning is live-verified in BOTH surfaces together: the in-game strip's short 'saved (not visible here)' variant AND the World panel's full-detail persist-result line (D-10)"
    - "Every documented-but-not-built gap from the 2026-08-01 cross-AI review (C6's narrower ADD-eligible-building scope, C8's un-wired arm-failure World-panel rendering if the store test alone isn't enough, C9's un-built per-action rich toasts, and the detail-card/Stage-to-project inert stubs), AND every later-round disclosed carve-out of a LOCKED decision or must_have (V8's per-project mirror-toggle block with no remedy named, V13's tab-navigation RemoveUndoToast-visibility gap, X8's mid-flow-remount '(NEW)'-marker narrowing, X13's Undo-history asymmetry, Z7's D-09 diskState:'uncertain' carve-out, the ROUND 9/AA3 D-03 multi-removal-during-a-sticky-freeze carve-out — ledger item (k), where an earlier queued removal can lose its own undo-toast turn while a later one is still sticky, stranding (per R9 review BB13) the non-newest arrivals AND the dismissed sticky entry itself, R10b's D-02 REMOVE-scope narrowing — ledger item (e) (R9 review, BB23: previously enumerated only in success_criteria, not here, so a verifier recording against this must_have was never prompted for it), AND the D-06 project-switch history-clearing narrowing — ledger item (l) (R9 review, BB21)) is EXPLICITLY RECORDED in this checkpoint's outcome — sign-off does not silently pass over a documented gap as if it were fixed (ROUND 7, Z15 — LOW, Opus Y8, extending this must_have beyond its original C-era-only enumeration to the ledger's newer entries; ROUND 10, AA3 extends it again to the D-03 carve-out)"
    - "A mixed known/unknown-building mirror-toggle flip proves reconcileMirrorMode's atomicity: the known building's mirror is untouched on disk and the persisted flag stays unmoved when any building in the SAME pass is unknown (ROUND 4, W1)"
    - "A Remove followed by an Undo within its 8-second window triggers a tree refresh and the restored row is NEVER mislabeled as a fresh '(NEW)' placement (ROUND 4, W2) — this must_have concerns the '(NEW)'-marker/refresh mechanics specifically and does not itself claim every Remove always retains its own undo-toast turn; see the ROUND 9/AA3 D-03 carve-out (ledger item (k)) for the one disclosed exception to the LATTER guarantee"
  artifacts:
    - path: ".planning/handoff/2026-XX-XX-CHANGE-REQUEST-getContainingCellName.md"
      provides: "the cross-repo change-request per AGENTS.md's provider/consumer separation protocol"
      contains: "getContainingCellName"
---

<objective>
Close the phase. Two things land here: (1) the ONE cross-repo deliverable this phase owes but must not build
directly — a change-request handoff for the permanent cell-name-resolution shim (D-04/A1), following the
EXACT protocol the `getContainingBuildingId` v25 shim already proved
(`.planning/handoff/2026-07-30-CHANGE-REQUEST-getContainingBuildingId.md` — the REAL filename, verified on
disk this session; a prior draft of this plan cited a nonexistent
`2026-07-30-toolkit-getContainingBuildingId-REQUEST.md`, corrected here per REVIEWS.md Fable L9), since
`CellProperty::getCellName()` is inline/unadvertisable for the same structural reason `getContainingBuildingId`
was; (2) a single blocking human-verify checkpoint that closes every remaining SC1-SC5 item this phase's
automated tasks could not prove on their own: SC5's rotation-persist live-fire, SC2's full three-sketch
observed/missing diff, D-07's owed provider-§4 editor-scene verify pass now that the World panel's Scene
accordion can trigger it directly, AND (ROUND 4) the two live-fire regressions the round-4 review's HIGH
findings (W1, W2) exist to close.

This plan does NOT edit `../swg-client-v2` — per AGENTS.md's cross-repo rule, the change-request goes into
this repo's `.planning/handoff/` inbox for the maintainer to relay, exactly as the v25 shim's request was
handled.

**CROSS-AI REVIEW REVISION (2026-08-01):** this phase's plan set carries several EXPLICITLY DOCUMENTED, NOT
fully closed gaps found by the cross-AI crew and addressed by targeted disclosure rather than a full fix (a
deliberate, proportionate choice recorded in each source plan's own revision note — see REVIEWS.md for the
full findings this section responds to):
- **C4:** Remove's live-despawn is a unit-tested mechanism with NO live caller this phase ships (Plan 13) —
  Remove is data-only in every real session.
- **C6 (Fable facet):** the ADD wizard's eligible-building set (Plan 14) is narrower than RESEARCH.md's
  original recommendation — only buildings ALREADY in the World panel's disk-scanned tree (previously edited)
  are selectable, not any building with a stock decoration.
- **C6 (Codex/Sonnet facets):** Plan 12's building-ID guard closes the wrong-BUILDING placement risk but NOT
  the wrong-ROOM-within-the-same-building risk — Plan 14 adds an explicit warning toast, not a code fix.
- **C8:** an arm-failure now reaches `worldEditorStore.recordArmFailure` (Plan 03/05/06), but no plan added a
  DEDICATED UI rendering for it beyond the shared Activity accordion (Plan 11) already displaying whatever
  `history` contains — this SHOULD surface correctly since `recordArmFailure` pushes into the same `history`
  array Task 2 below already diffs against the sketch, but this checkpoint explicitly re-verifies it live
  (see step 9 below) rather than assuming the store-level wiring alone proves the UI path.
- **C9:** rich, per-action-site HOST_CMD outcome toasts (Scene/despawn/placement each rendering their own
  words-only result) were NOT built — only a coarse words-only log line (Plan 08) exists at the generic layer.
- **Detail-card actions (Go to/Revert/Edit in game) and the footer's "Stage to project" button (Plan 10/11)**
  ship as documented, inert stubs — no plan closes them, and this checkpoint records them as an OBSERVED gap
  rather than silently skipping the check (Sonnet F5/Cursor L1).
This checkpoint's Task 2 step 12 (below) requires the human verifier to EXPLICITLY record each of these as
"observed gap, accepted" rather than let the sign-off pass silently over them.

**ROUND 4 REVISION (2026-08-01, REVIEWS.md W1/W2):** two NEW HIGH findings landed this round, both opened by
round-3's own fixes interacting with each other rather than by any fabrication (the round-4 review confirmed
zero fabrications in the round-3→round-4 replan — Fable's fact-check axis specifically, ROUND 5/V14 attribution
fix). Both are now CLOSED by targeted fixes in their source plans,
and this checkpoint's step 11 (new) and step 8 (extended) live-verify each fix rather than trusting the unit
tests alone, matching this phase's own "verify live, don't just trust the store wiring" precedent (see C8
above):
- **W1 (Plan 06):** `reconcileMirrorMode` is now transactional on DISK, not just on the persisted flag — a
  mixed known/unknown-building toggle-flip no longer moves the KNOWN building's mirror while the flag stays
  unpersisted. Step 11 below live-verifies this exact mixed-pass scenario.
- **W2 (Plans 13/14):** Undo's re-add now ALSO triggers a tree refresh (Plan 13), and the "(NEW)" row marker
  (Plan 14) is now keyed on content identity rather than raw position, so an Undo-driven reindex can never be
  mislabeled a fresh placement. Step 8 below is extended to live-verify both halves together.

**ROUND 6 REVISION (2026-08-01, REVIEWS.md X8/X9/X12/X13):** X3/X4 (the round's HIGHs touching Plans 13/14) are
CLOSED by real code fixes in their source plans (identity-scoped suppression; the `lastUndoError` signal — since
respecified into the per-entry-keyed `undoErrors` map by ROUND 7/Z1, see Plan 13's own revision), not
disclosed gaps — no checkpoint change needed for those beyond the existing steps. Two items land here instead:
- **X9 (MEDIUM, Opus):** the round-5 replan's checkpoint steps were byte-identical to round 4 despite round 5
  adding four new HIGHs — step 6 now ALSO confirms zero "(NEW)" markers on first opening the World tab this
  session (the V3 first-pass rule, live-verified), and step 8 now ALSO triggers one further, unrelated refresh
  after the Undo and confirms the restored row still shows no "(NEW)" badge (the delayed-misattribution class
  V2/X3 exist to close, live-verified rather than trusted from unit tests alone).
- **X12 (LOW, Opus):** this plan's `<threat_model>` previously had a Trust Boundaries table but NO STRIDE
  Threat Register at all, even though `05.1-VALIDATION.md:78` already cited `T-05.1-15a` as if it existed —
  added below, closing the dangling cross-reference.
Two more items are disclosure-only, recorded in step 12's gap ledger as items (h)/(i) rather than fixed this
phase (a deliberate, proportionate choice, matching this section's own C4/C6/C8/C9 precedent above; ROUND 7
adds ledger item (j) for D-09's disclosed carve-out, see the ROUND 7 REVISION section below):
- **X8 (MEDIUM, Opus):** Plan 14's content-identity "(NEW)" baseline is component-local to `WorldPanel.tsx`; a
  mid-flow tab navigation during the 021-A ADD round trip can reset it, silently degrading the two-surface
  confirm to one surface for that in-flight ADD. Hoisting the baseline into `worldEditorStore` is the
  permanent fix, deferred to a future phase.
- **X13 (LOW, Opus):** Undo records no `recordPersistResult` history entry (Remove does) — after an Undo, the
  Activity accordion's most recent entry for that row still reads whatever Remove last recorded, even though
  the row is back on disk. **(ROUND 7, Z6 — MEDIUM, Opus Y3)** Disclosed as an accepted, COSMETIC completeness
  gap, free to fix later (Plan 13's ROUND 7/Z6 correction: the previous "load-bearing for Plan 14's V2 fix"
  framing was FALSE — V2's actual fix keys the diff on tree content identity, not on history growth, so a
  future Undo history entry would not break Plan 14's suppress mechanism) — not a defect, and not something any
  OTHER fix in this plan set depends on. This also surfaces a D-06 tension: D-06 defines history as "this
  session's persists/failures," and an Undo add-back IS a persist by that literal wording, so recording none
  for it narrows D-06's own text — the SAME class of disclosure D-02's narrowing already required (ledger
  item (e)), now named explicitly rather than left implicit.

**ROUND 7 REVISION (2026-08-01, REVIEWS.md Z2/Z6/Z7/Z8/Z9/Z14/Z15/Z16):** Round 7's four HIGHs land in Plans
10/13/14 (Z1/Z3/Z4, see those plans' own ROUND 7 revisions); this plan absorbs the remaining MED/LOW
seam-propagation and citation fixes the round-7 crew found:
- **Z2 (HIGH, Opus Y2, propagated from Plan 14):** this plan's must_have (`:14`) and checkpoint step 4 now
  carry the same "EXCEPT the disclosed X8 case" clause Plan 14's must_have/sketch_elements gained, so the
  021-A observed/missing diff cannot be read as demanding the "(NEW)" marker on the ONE disclosed
  mid-flow-remount case.
- **Z6 (MEDIUM, Opus Y3):** the X13 rationale above and step 12's ledger item (i) are corrected — the
  Undo-history asymmetry is a cosmetic completeness gap, not load-bearing, and the D-06 tension is now named.
- **Z7 (MEDIUM, Opus Y4):** D-09 ("disk always matches what the toggle claims") is qualified by Plan 06's
  disclosed `diskState: 'uncertain'` carve-out with NO prior sign-off disclosure. A new gap-ledger item (j)
  and an extension to step 13's maintainer annotation close this the same way ledger item (e) already
  closes it for D-02.
- **Z8 (MEDIUM, Opus Y5):** step 8's X9 extension observed only what the superseded blanket-boolean design
  would ALSO produce; a new check — an unrelated new row in the SAME window IS still marked "(NEW)" — is
  added, the one observation that actually distinguishes X3's identity-scoped fix.
- **Z9 (MEDIUM, Opus Y6):** `05.1-VALIDATION.md`'s two "Plan 15 step 9" cites for D-10's both-surfaces check
  are stale (a later round moved this check to step 10); both are corrected in `05.1-VALIDATION.md` directly.
- **Z14 (LOW, Opus Y7):** step 11's two edited buildings never shared a stock template — the exact condition
  Plan 06's ROUND 5/V4 per-resolved-mirror-path grouping/rollback exists for. Step 11 now requires shared-
  stock-template buildings.
- **Z16 (LOW, Opus Y9):** step 11's "NO building's mirror was changed" wording is qualified to match Plan
  10's `:26` conditional contract (benign in this step's own all-`'unchanged'` scenario, but now stated
  explicitly rather than left as a fourth unqualified restatement).

**R9 REVISION (2026-08-01, REVIEWS.md round 9 — BB6/BB7/BB12/BB13/BB14/BB18/BB21/BB23; citation convention:
review items are cited as 'R9 review, BBn'):** `files_modified` now lists the two code files Task 2's
checkpoint exercises (BB18 — checkpoint-verified surfaces, not new edits); the gap-ledger enumeration in
this plan's must_have adds items (e) and the new (l) (BB23/BB21); item (k)'s arithmetic is corrected to the
full two-plus-stranded-entries accounting (BB13); step 13's maintainer annotation now also covers D-06 and
names D-03's second (plain-remount) narrowing (BB21/BB14); and steps 6/7 additionally live-verify the ack
protocol's timeout/durable-record/refuse-while-pending behavior (BB6/BB7/BB12, per Plan 08's HOST_CMD ACK
PROTOCOL). The round-9 structural fixes elsewhere (BB2's reset-before-render contract, BB3's freeze-baseline
reconstruction, BB9's no-pre-clear retry) RESTORE promised guarantees rather than narrowing them — no new
ledger items exist for them, deliberately.

Purpose: certify the phase's SC1-SC5 as actually, observably true — not merely "all tasks green" — AND ensure
every deliberately-not-fully-closed finding from the cross-AI review is recorded at sign-off, not silently
forgotten, AND live-verify the two round-4 HIGH fixes (W1, W2) rather than trusting their unit tests alone.
Output: the change-request handoff file (correct filename); a completed, human-approved verification
checkpoint covering every must-have this phase's automated tests could not reach, plus an explicit gap ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-live-world-editor-productization/05.1-RESEARCH.md
@.planning/phases/05.1-live-world-editor-productization/05.1-VALIDATION.md
@.planning/phases/05.1-live-world-editor-productization/05.1-REVIEWS.md
@.planning/handoff/2026-07-30-live-world-editor-decoration-persist.md
@.planning/handoff/2026-07-30-CHANGE-REQUEST-getContainingBuildingId.md
@.planning/sketches/019-world-editor-panel/README.md
@.planning/sketches/020-overlay-decoration-hud/README.md
@.planning/sketches/021-spawn-decoration-flow/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: File the cell-name-resolution change-request handoff (D-04/A1)</name>
  <files>.planning/handoff/2026-XX-XX-CHANGE-REQUEST-getContainingCellName.md</files>
  <read_first>
    .planning/handoff/2026-07-30-CHANGE-REQUEST-getContainingBuildingId.md (the exact precedent format/tone
    to mirror — a toolkit-mirror change-request document, not a direct edit to the provider repo; verified
    present at THIS exact filename on disk this session)
    .planning/phases/05.1-live-world-editor-productization/05.1-RESEARCH.md ("Open Questions" §1 — the full
    ground-truth citation: `CellProperty::getCellName()` inline at
    `sharedObject/src/shared/portal/CellProperty.h:249`; `PortalProperty::getCellNames()` exists non-inline
    but returns an ABI-unsafe `std::vector<const char*> const&`; `.ws` node addressing uses a numeric
    `cellIndex`, a DIFFERENT identity space than the `.ilf`'s string `cellName`)
  </read_first>
  <action>
    Write a new handoff file at `.planning/handoff/<today's date>-CHANGE-REQUEST-getContainingCellName.md`,
    modeled on the getContainingBuildingId precedent's structure (problem statement, ground-truth citations
    with exact file:line references, the shim options identified during research — a wrapped
    `getContainingCellName(Object*) -> char*` call vs. a POD out-buffer shim over `getCellNames()`'s
    index→name table, framed as a NEED for the provider session to design, not a prescribed signature, per
    A1's own stated uncertainty), the toolkit-side pre-wire/bind-by-name posture this phase used for every
    other provider dependency (D-04's default), and the interim MVP degrade this phase already shipped (Plan
    14: require the target cell to already contain at least one existing decoration; refuse placement into a
    genuinely empty cell with an honest message; Plan 12's building-ID cross-check reduces but does not
    eliminate the wrong-cell risk within a multi-cell building) so the provider session understands this is
    NOT a blocking request — it unblocks a currently-degraded case, not a currently-broken one.
  </action>
  <verify>
    <automated>test -f ".planning/handoff/"*"-CHANGE-REQUEST-getContainingCellName.md"</automated>
  </verify>
  <acceptance_criteria>
    The handoff file exists, cites the exact ground-truth file:line references RESEARCH.md already
    established (no re-derivation, no fabricated new claims), states the interim degrade already shipped
    (including Plan 12's building-ID guard as a partial, not full, mitigation), and follows the cross-repo
    protocol (toolkit-side mirror document, never a direct edit to `../swg-client-v2`).
  </acceptance_criteria>
  <done>The one genuine provider gap this phase leaves open is documented and filed for the maintainer's relay
  loop, not silently dropped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase sign-off checkpoint (incl. ROUND 4 W1/W2 live-verify + explicit gap ledger)</name>
  <files>packages/renderer/src/panels/world/WorldPanel.tsx, packages/live-inject/agent/overlay.cpp</files>
  <action>
    Pause for human verification. Claude has filed the cell-name-resolution change-request handoff (Task 1) and completed every automatable task across this phase's 14 prior plans. This checkpoint certifies SC1-SC5 as observably true against the running app + a live client, per the steps below, AND requires explicit recording of the cross-AI review's documented-but-not-fully-closed gaps.
  </action>
  <what-built>
    The complete productized Live World Editor: 020-A status-strip HUD (Plan 05), 019-A World panel (Plans
    09-11), mirror-mode toggle + reconcile (Plan 06/10, now transactional on disk per ROUND 4/W1), 021-A ADD
    flow end-to-end (Plans 03/07/08/09/12/14, "(NEW)" marker now content-identity-keyed per ROUND 4/W2),
    REMOVE with undo (Plan 13, data-only per C4, Undo now also refreshes the tree per ROUND 4/W2), and the
    already-shipped agent `-1`-refused fix + editor-scene tooling now reachable from the app (Plan 05/09/11).
    This revision closes the cross-AI review's two BLOCKERs (C1 channel-size ownership, C2 ADD kind/cellName
    encode+decode), every HIGH finding through round 3 (C3 studioDir threading, C4 honest Remove framing, C5
    mandatory real-.ilf fixture, C6 building-ID guard + explicit scope/risk disclosure), round 3's own HIGH
    seam fixes (R1-R13), round 4's two NEW HIGHs opened by those fixes (W1 — reconcileMirrorMode transactional
    on disk, not just the flag; W2 — Undo triggers refreshTree() + "(NEW)" keyed on content identity), round
    4's MEDIUM half-propagation fix (W3 — one shared resolveOverridePair() null-guard used everywhere), plus
    several MEDIUM findings (C7 single mirror resolution, C8 arm-failure wiring, C9 words-only HOST_CMD log
    line, C10 files_modified fix, C11 real-Electron smoke, C12 failure badge renderer, C13 documented
    synchronous-write invariant) and multiple lower-severity single-reviewer items (stale line-number
    citations through round 4/W5, "(NEW)" marker, D-07b hint, Sonnet F11 reversibility scoping, Sonnet F10
    re-entry guard, this plan's own handoff-filename fix).
  </what-built>
  <how-to-verify>
    **SC5 — rotation persist:**
    1. Hover a decoration, F-arm, R-rotate it (not just translate), Persist. Confirm "saved." Reload the
       scene (via the World panel's Scene accordion). Confirm the rotation survived, matching the handoff's
       proven translate smoke test but for a rotate-only move.

    **SC2 — sketch-parity observed/missing diff (do this for all three sketches, list any gap found):**
    2. Open `.planning/sketches/019-world-editor-panel/index.html` (Variant A) side-by-side with the real
       World tab. Walk every element from this phase's plan `sketch_elements` lists (tabstrip, live-strip,
       mirror toggle + hint + badge, edited-buildings section + count chip, building/decoration rows + status
       badges, detail card + its five kv fields + three action buttons, Activity accordion, Scene accordion
       + editor-scene/reload/bookmarks, footer's two buttons) and mark each observed/missing.
    3. Open `.planning/sketches/020-overlay-decoration-hud/index.html` (Variant A) side-by-side with the live
       in-game strip. Walk idle/hover/armed/saved/failed states, the Δ readout, Esc, hotkey hint — mark each
       observed/missing.
    4. Open `.planning/sketches/021-spawn-decoration-flow/index.html` (Variant A) side-by-side with a live
       add-decoration run. Walk all three frames (Browse modal, Place ghost/reticle, Persisted two-surface
       confirm, including the "(NEW)" row marker) — mark each observed/missing. **(ROUND 7, Z2)** The
       "(NEW)" marker is expected present on a normal, uninterrupted ADD; if you deliberately navigate away
       mid-flow (per ledger item (h)) and it is absent on that ONE run, that is the disclosed X8 exception,
       not a NEW gap to report here — a normal run missing the marker IS a gap.

    **D-07 — owed provider-§4 editor-scene verify pass, now from the app:**
    5. From the World panel's Scene accordion, click "Editor scene ▸ {terrain}". Confirm it loads the OFFLINE
       editor scene (matching the provider's canonical §4 visible-verify context) and a previously-persisted
       decoration edit is visible there — closing the handoff's originally-owed follow-up.

    **SC4 — ADD/REMOVE full round trip, reload-survive, incl. the C6 guards:**
    6. **(ROUND 6, X9 — MEDIUM, Opus)** Before your first ADD this session, confirm the World panel's tree
       shows ZERO rows marked "(NEW)" on first opening the World tab (the V3 first-pass seed rule, live-verified
       here rather than trusted from unit tests alone). Then repeat Plan 14's ADD flow end-to-end from the app
       (search → select → Place in game → click floor →
       Persist). Confirm the World panel shows the new row marked "(NEW)" (two-surface confirm) and it
       survives a scene reload. **(ROUND 10, AA4)** Confirm the placement toast now appears ONLY after the
       agent's real ack correlates (a brief delay is expected and normal, per Plan 08/14's own ROUND 10
       revision — the toast is deferred, not instant), and that it shows the wrong-room warning (C6). **(R9 review,
       BB6/BB12)** Separately confirm the timeout fallback: issue a placement request the game cannot answer
       (e.g. during a zoning/loading screen) and confirm that after ~10 seconds a words-only TIMEOUT toast
       appears AND a durable Activity entry + failure badge were recorded — never indefinite silence.
    7. Attempt a placement click in a DIFFERENT building than the one selected; confirm it is refused with a
       words-only "wrong building" message and nothing spawns (Plan 12's C6 guard). **(ROUND 10, AA4 — HIGH,
       Sonnet L1, the false-positive-toast live-check)** Separately: hover/arm an EXISTING decoration elsewhere
       (setting `g_capArmed`) or start a SECOND placement while one is already active, THEN attempt to open the
       Add wizard and click "Place in game ▸" — confirm the toast now reads the NEW honest refusal message
       ("Placement request was refused — an edit or placement may already be active elsewhere...") rather than
       the false "Placement mode active" claim a pre-ROUND-10 build would have shown regardless of the agent's
       silent no-op refusal; confirm nothing spawns — and confirm the refusal ALSO left a durable words-only
       Activity entry + failure badge (R9 review, BB12). Additionally, attempt a rapid second "Place in
       game ▸" before the first request's ack lands and confirm it is refused locally with the words-only
       "still waiting" toast, with NO second command sent (R9 review, BB7).
    8. **(ROUND-3-REVIEW R10b — reworded; a never-before-edited building isn't in the World-panel tree at all,
       per D-05's disk-scan scope, so "remove a stock decoration" as originally worded is unsatisfiable)** Remove
       an UNTOUCHED row (one never individually edited) inside an ALREADY-EDITED building via the World panel.
       Confirm the undo toast appears, no confirm dialog was shown, and after a scene reload the object is gone.
       Undo a DIFFERENT removal within its 8-second window and confirm the row reappears without a reload.
       Confirm the removal's history entry reads a `formatPersistMessage`-built message — mirror ON: plainly
       "removed — reload scene to see it gone"; mirror OFF: the same text plus D-10's hybrid-session suffix
       (ROUND-3-REVIEW R10a) — this is the ONLY outcome this phase produces, per C4 — do NOT expect a
       live-despawn variant; if one somehow appears, that is itself a finding to record, not a normal success).
       **(ROUND 4, W2 — Undo refresh + "(NEW)" mislabel regression)** Immediately after that Undo (within its
       8-second window, on a SEPARATE removal so it does not interfere with the FIRST removal's already-elapsed
       check above), confirm the World panel's tree refreshes automatically (no manual reopen/reload needed to
       see the restored row) and the restored row does NOT show a "(NEW)" badge — an Undo-restored row is a
       RESTORE, not a fresh placement, and must never be visually indistinguishable from step 6's genuine ADD
       confirmation. **(ROUND 6, X9 — MEDIUM, Opus, the delayed-misattribution live-check)** Then trigger ONE
       MORE, UNRELATED refresh (e.g. the live-strip's manual "refresh" control, or toggling the mirror switch)
       and confirm the SAME restored row STILL shows no "(NEW)" badge — proving the suppression correctly
       pre-consumed the restored row's identity tuple rather than leaving a stale baseline a later refresh could
       misattribute against. **(ROUND 7, Z8 — MEDIUM, Opus Y5, the distinguishing check X9's own live-verify
       text never added)** Before that "ONE MORE, UNRELATED refresh" in the prior sentence, first ADD one
       genuinely-new decoration to a DIFFERENT building (or a different cell/template in the SAME building)
       via Plan 14's ADD flow so that "unrelated refresh" actually carries new content, not just an inert
       re-scan — then confirm THAT new row IS marked "(NEW)" while the Undo-restored row from earlier in
       this step STILL shows no "(NEW)" badge — this is the one live observation that actually distinguishes
       X3's identity-scoped suppression fix from the superseded blanket-boolean design (which would ALSO have
       passed a purely-inert unrelated refresh, but would have silently swallowed a genuinely-new row landed
       in the same window — the exact defect X3 closed).

    **SC1 — words, never codes, one final spot check:**
    9. Deliberately provoke one failure case (e.g. attempt to arm an object with no resolvable building) and
       confirm NEITHER the in-game strip NOR the World panel ever displays a raw numeric result code anywhere
       in the UI. Confirm the failed ARM attempt appears as a words-only entry in the World panel's Activity
       accordion (C8's wiring — verify this LIVE, do not just trust the unit tests).

    **D-10 — mirror-off hybrid-session warning, BOTH surfaces:**
    10. In the World panel's mirror-mode toggle, switch mirror OFF. Persist an edit on any decoration. Confirm
       the in-game strip's saved state reads the SHORT variant "saved (not visible here)" — not plain "saved".
       Then open the World panel's Activity accordion and confirm the SAME persist's history line carries the
       FULL detail verbatim: "... — mirror off — not visible on hybrid sessions until reload into an editor
       scene". Switch mirror back ON and persist again; confirm the strip reads plain "saved" and the History
       line has no mirror-off suffix.

    **ROUND 4, W1 — mirror-reconcile atomicity, mixed known/unknown-building pass:**
    11. Edit TWO buildings live that SHARE THE SAME STOCK TEMPLATE (ROUND 7, Z14 — LOW, Opus Y7 — pick two
       placed instances of one layout, so both resolve to the SAME physical stock mirror path; this is the
       exact condition Plan 06's ROUND 5/V4 per-resolved-mirror-path grouping/rollback exists for, and no prior
       checkpoint step exercised it), so both appear in the World-panel tree with a known
       `buildingTemplateVfsPath`
       each, then simulate an "unknown" building by removing ONE of their two `worldEditorBuildingTemplates`
       entries from `workspace.json` by hand (or use a build that has a building edited before this phase's
       durable-map mechanism shipped) so `reconcileMirrorMode` sees one known + one unknown building in the
       SAME toggle-flip pass. Flip the mirror toggle. Confirm: the toggle visually REVERTS to its pre-flip
       position, the KNOWN building's mirror file on disk is UNCHANGED from its pre-flip state (compare file
       existence/mtime/bytes, not just a visual glance), and the Activity accordion shows a durable failure
       entry naming the unknown building with wording that says NO building's mirror was changed (never a
       per-building mixed-outcome message) — **(ROUND 7, Z16 — LOW, Opus Y9)** this unconditional wording is
       correct for THIS step's specific scenario because every failure entry here carries `diskState:
       'unchanged'` (a Phase-1 validation failure, per Plan 06's contract — never touches disk); it does NOT
       apply to a `diskState: 'uncertain'` rollback-double-fault case, which Plan 10's must_haves (`:26`/`:30`)
       hedge separately and this step does not exercise. This is the live proof that a mixed pass no longer
       moves ANY
       building's mirror on disk while the flag stays unmoved (ROUND 4/W1 — the D-09 stale-override class, one
       layer down at the disk level, is now closed), AND — because the two known buildings share one stock
       mirror file — that the shared-mirror-path grouping itself is exercised live, not just in Plan 06's unit
       tests (Z14).

    **Gap ledger — explicit recording, not a silent pass:**
    12. Record each of the following as "observed, accepted" (or "observed, NOT accepted — escalate") in your
        resume message: (a) Remove is data-only in this session, live-despawn never fired (C4, expected); (b)
        the "+ Add decoration…" flow only offered previously-edited buildings, not stock-only ones (C6/Fable,
        expected); (c) no rich per-action toast appeared for Scene reload/despawn beyond the generic log line
        (C9, expected); (d) the detail card's "Go to"/"Revert"/"Edit in game" buttons and the footer's "Stage
        to project" button did nothing beyond a stub toast, if clicked (Sonnet F5/Cursor L1, expected); (e)
        **(ROUND-3-REVIEW R10b)** REMOVE's actual eligible scope was narrower than D-02's literal "any decoration
        is removable, stock included" wording — in practice, only a row inside a building that ALREADY has an
        `edit_*.ilf` on disk AND whose stock template path was previously observed by a live capture
        (`worldEditorBuildingTemplates`, Plan 02/06) was actually removable this session; a genuinely
        never-edited building was not selectable in the tree at all (D-05's disk-scan scope), matching ADD's
        own (b) narrowing above (expected); (f) **(ROUND 5, V8 — MEDIUM, Opus)** a single building whose
        `buildingTemplateVfsPath` is unknown (and cannot be re-armed live — e.g. unreachable or a legacy
        `edit_*.ilf` predating the durable-map mechanism) blocks the mirror toggle for the WHOLE PROJECT, not
        just that building — confirm the Activity accordion's failure entry from step 11 names the concrete
        unblock (arm/persist it live once, OR delete its orphaned `edit_*.ilf` from
        `override dir/interiorlayout/toolkit/` if it cannot be reached live), not merely "unknown" with no
        remedy (expected, per Plan 06's ROUND 5/V8 error-message fix); (g) **(ROUND 5, V13 — LOW, Cursor;
        ROUND 10, AA2 — corrected, now deterministic)** if
        you navigate away from the World tab mid-undo-window after a PLAIN (non-sticky-error) Remove, the
        panel-scoped `RemoveUndoToast` unmounts with the tab, and returning to it before the 8-second window
        elapses does NOT re-show the toast (Plan 13's ROUND 10 mount-state fix seeds the component's baseline
        from the store's current `pending` at mount, so a plain pending entry is never mistaken for a fresh
        removal on remount) — this is now a DETERMINISTIC "does not re-show," not the previously-disclosed
        "may or may not" ambiguity; confirm this is what you observe. Separately, if the entry you navigated
        away from HAD an unresolved Undo error (sticky), confirm the toast DOES reappear showing that SAME
        error on return (the sticky display is restored from the store, never lost, per Plan 13's ROUND 10/AA2
        fix) — record which of the two cases you exercised and whether the observed behavior matched;
        (h) **(ROUND 6, X8 — MEDIUM, Opus)** if you navigate away from the World tab mid-flow during a 021-A
        ADD round trip (after clicking "Place in game" but before the persist confirms) and return before it
        finishes, confirm whether the "(NEW)" marker still appears once the persist completes — a remount
        resets the component-local diff baseline (`prevTreeRef`), which can silently swallow the "(NEW)"
        marking for that one row; this is a disclosed, accepted scope narrowing (hoisting the baseline into
        `worldEditorStore` is the permanent fix, deferred to a future phase), not a defect to fix this phase —
        record which behavior you actually observed; (i) **(ROUND 6, X13 — LOW, Opus; ROUND 7, Z6 — MEDIUM,
        Opus Y3, correcting the false "load-bearing" framing)** confirm the Activity
        accordion's most recent entry for the row you Undo'd in step 8 still reads whatever Remove last
        recorded (e.g. "removed — reload scene to see it gone") even though the row is now back on disk —
        Undo records no history entry of its own (a COSMETIC completeness gap, free to fix later — NOT
        load-bearing for Plan 14's suppress mechanism, which keys on tree content identity, not history growth;
        ROUND 7/Z6 corrects Plan 13's ROUND 6/X13 note, which had this backwards — and a disclosed D-06
        tension: an Undo add-back IS a persist by D-06's own "this session's persists/failures" wording, so
        recording none narrows that wording, the same class as D-02's narrowing per ledger item (e)), a
        disclosed, accepted stale-accordion consequence, not a defect to fix this phase; (j) **(ROUND 7, Z7 —
        MEDIUM, Opus Y4)** confirm the Activity accordion's failure entry from step 11 (the mixed
        known/unknown-building mirror-toggle pass) is the ONLY place this session where a mirror-toggle
        failure can carry `diskState: 'uncertain'` (Plan 06's disclosed `T-05.1-06d` rollback-double-fault
        residual) — this qualifies LOCKED decision D-09 ("Disk always matches what the toggle claims — no
        stale mirrors") with no prior sign-off disclosure; recording it here (and via step 13's maintainer
        annotation below, now naming D-02, D-03, AND D-09) closes that gap the SAME way ledger item (e) already
        closes it for D-02's narrowing; (k) **(ROUND 9 revision, Plan 13; ROUND 10, AA3 — HIGH, Opus Y3)** if
        you can arrange for a SECOND AND THIRD removal (C, then D) to land while an EARLIER entry's Undo error
        is still sticky (unresolved) on the World-panel's undo toast, confirm that C's OWN undo-toast turn is
        silently lost once the sticky freeze thaws (D's toast appears instead) — C's `.ilf` removal remains
        correctly committed to disk regardless, but C is NOT recoverable via Undo, only via a manual re-add
        through the Add flow. This is a rare, accepted narrowing of LOCKED decision D-03 ("Remove is guarded by
        an undo toast") — reachable only when an EARLIER Undo attempt has already failed and gone unresolved,
        not on an ordinary Remove/Undo sequence — disclosed here and via step 13's maintainer annotation, not
        silently left for a future reader to discover. **(R9 review, BB13 — full accounting)** Note ALSO that
        such a freeze leaves TWO OR MORE entries stranded in `pending` with no remaining UI surface: every
        non-newest freeze arrival (C) AND, if the sticky entry (A) exits via dismiss rather than a successful
        retry, A itself (per AA10, a dismissed entry has no second surface in this UI) — record the stranded
        count you observed (if this scenario is impractical to arrange live this
        session, record "not exercised — accepted as a code-reviewed, unit-tested disclosure" rather than
        silently skipping the line); (l) **(R9 review, BB21)** confirm that switching projects and switching
        BACK within one app run shows an EMPTY Activity history for the returned-to project —
        `worldEditorStore.reset()` clears `history` on every project switch (the AA5 leak fix, invoked by the
        store's own subscription per Plan 04's PROJECT-SWITCH RESET ORDERING CONTRACT), which narrows LOCKED
        decision D-06's literal 'clears on app restart' wording; a disclosed, accepted narrowing (recorded
        via step 13's maintainer annotation, now also covering D-06), not a defect.
        If ANY of these behaved DIFFERENTLY than described (better OR worse), record that explicitly — it
        changes the phase's actual state.
    13. (R11(b) close-out, REVIEWS.md Round 2; ROUND 7/Z7 extends this step to ALSO cover D-09; ROUND 10/AA3
        extends it to ALSO cover D-03) Maintainer
        action: annotate 05.1-CONTEXT.md's locked D-02
        wording — "Remove = the edited .ilf omits the row (+ live despawn)" — to note that live despawn is
        deferred groundwork this phase (unit-tested, zero live callers; see (a) above); ALSO annotate D-09's
        "no stale mirrors" wording to note the disclosed `diskState: 'uncertain'` rollback-double-fault
        carve-out (Plan 06's `T-05.1-06d`, ledger item (j) above) — a rare, accepted exception to "disk
        always matches what the toggle claims." ALSO annotate D-03's "guarded by an undo toast" wording to note
        the disclosed multi-removal-during-a-sticky-freeze carve-out (Plan 13's ROUND 9 revision, ledger item
        (k) above) — an EARLIER queued removal can lose its own undo-toast turn (though never its underlying
        data removal) when a LATER removal arrives while a PRIOR entry's Undo error is still unresolved — AND
        D-03's second disclosed narrowing, the plain-remount toast loss (ledger item (g); R9 review, BB14).
        ALSO annotate D-06's "clears on app restart" wording to note that history additionally clears on
        every project switch (ledger item (l); R9 review, BB21). Plans do not edit
        CONTEXT.md; this ledger line is the explicit flag REVIEWS.md R11(b) (and, for D-09, Z7; for D-03,
        ROUND 10/AA3 + R9 review BB14; for D-06, R9 review BB21) required.
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 verification checks (steps 1-11) hold AND the gap ledger (step 12)
  AND the maintainer annotation note (step 13) have been recorded — 13 numbered steps total, ROUND 4 revision
  of ROUND-3-REVIEW R13/Opus L5's count — or describe which check failed / which sketch element is missing /
  which gap-ledger item behaved unexpectedly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-repo (toolkit ↔ swg-client-v2) | This plan's only cross-repo artifact is a documentation handoff, never a direct code edit to the sibling repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-15a | Tampering | the change-request handoff file (Task 1) fabricating or mis-citing a ground-truth claim, or editing the sibling repo directly | mitigate | Task 1's action/acceptance_criteria require citing ONLY the exact ground-truth file:line references RESEARCH.md already established (no re-derivation, no new claims) and follow the cross-repo protocol verbatim (a toolkit-side mirror document in `.planning/handoff/`, never a direct edit to `../swg-client-v2`) — the SAME discipline `.planning/handoff/2026-07-30-CHANGE-REQUEST-getContainingBuildingId.md` already proved. (ROUND 6, X12 — LOW, Opus: this row was previously missing, though `05.1-VALIDATION.md:78` already cited it as if present — added here to close that dangling cross-reference.) |
</threat_model>

<verification>
The change-request handoff file exists at the CORRECT, verified-precedent-matching filename pattern and
cites exact ground-truth references; the blocking checkpoint's 11 verification steps all pass (steps 6/8 now
also live-verify the ROUND 6/X9 "(NEW)" zero-baseline and delayed-misattribution checks, step 8 now ALSO
live-verifies the ROUND 7/Z8 distinguishing unrelated-new-row check, and steps 6/7 now ALSO live-verify the
ROUND 10/AA4 deferred-toast-ack and false-positive-refusal checks plus the R9 timeout/durable-record/
refuse-while-pending checks, BB6/BB7/BB12), step 12's gap
ledger (now items a-l — ROUND 5/V8/V13, ROUND 6/X8/X13, ROUND 7/Z7, ROUND 9/AA3, plus R9 review BB21's (l)) is explicitly recorded (not silently
skipped), and step
13's maintainer-annotation note (now covering D-02, D-03, AND D-09, ROUND 7/Z7; ROUND 10/AA3) is recorded — 13 numbered steps
total (ROUND 5/V11 count fix; ROUND 6/7/10 add
ledger sub-items, not new numbered steps). The threat model's STRIDE register (ROUND 6/X12) carries the
T-05.1-15a row `05.1-VALIDATION.md:78` already cited.
</verification>

<success_criteria>
SC1-SC5 are all observably true, not merely "tasks green": rotation persists, both HUD and World panel are
words-only (including the C8 arm-failure path, live-verified), all three sketches are element-complete
(including the "(NEW)" marker) or gaps are explicitly recorded, ADD/REMOVE survive reload with the C6
building-ID guard demonstrated, and the owed editor-scene verify pass is closed from the app. The one
remaining provider gap is handed off (at the correct filename) not silently dropped. Every cross-AI-review
finding this phase deliberately did not fully close (C4, C6/Fable, C9's rich toasts, the inert stubs) is
explicitly recorded at sign-off, not silently passed over. Round-3-review findings deliberately accepted as
disclosed tradeoffs rather than code fixes (Undo's append-only reorder, R7; REMOVE's actual eligible scope
being narrower than D-02's literal wording, R10b) are recorded in the gap ledger's items (e) alongside R7's
note in Plan 13's own must_haves — sign-off does not silently pass over either. ROUND 4's two HIGH fixes are
live-verified, not merely unit-tested: reconcileMirrorMode's disk-level atomicity on a mixed known/unknown
pass (W1), and Undo's refresh + content-identity "(NEW)" keying surviving a real reindex (W2). ROUND 6: step 6
live-verifies zero "(NEW)" markers on first World-tab open this session (X9); step 8 live-verifies that a
SECOND, unrelated refresh after an Undo still shows no "(NEW)" badge on the restored row (X9's
delayed-misattribution check); the gap ledger's new items (h)/(i) record the mid-flow-remount "(NEW)" scope
narrowing (X8) and Undo's stale-Activity-accordion consequence (X13) explicitly, rather than leaving either
undisclosed. ROUND 7: the "(NEW)" observed/missing diff (step 4) and this plan's must_have (:14) carry the
same disclosed-X8-exception clause Plan 14 gained, so the diff cannot be misread as demanding the marker on
the ONE disclosed remount case (Z2); the X13 gap-ledger note and this plan's own rationale are corrected —
cosmetic, not load-bearing — and the D-06 tension is named (Z6); a new ledger item (j) and step 13
extension disclose D-09's diskState:'uncertain' carve-out the SAME way item (e) already discloses D-02's
narrowing (Z7); step 8 adds the ONE live check that actually distinguishes X3's identity-scoped
suppression from the superseded blanket-boolean design — an unrelated genuinely-new row landed in the
same window still shows "(NEW)" (Z8); 05.1-VALIDATION.md's stale "step 9" cites for D-10 are corrected to
06's per-mirror-path grouping/rollback exists for (Z14), with its "NO building's mirror was changed"
wording qualified to match Plan 10's conditional contract (Z16).
ROUND 10: steps 6/7 now ALSO live-verify AA4's fix — the placement-active toast is deferred to a real agent
ack (a brief delay is normal) and a silently-refused START_PLACEMENT (an edit armed elsewhere, or a placement
already active) now shows an honest refusal toast, never the unconditional false "active" claim a pre-fix
build would show regardless of the agent's outcome. A new gap-ledger item (k) and step 13 extension disclose
D-03's ROUND 9/AA3 multi-removal-during-a-sticky-freeze carve-out (an earlier queued removal can lose its own
undo-toast turn, though never its underlying data removal, while a later removal arrives during a prior
entry's still-unresolved sticky error) — the SAME "EXCEPT the disclosed …" treatment items (e)/(j) already
give D-02/D-09's own narrowings, now extended to D-03 (AA3); this plan's must_have (:17) enumeration and the
carve-out-adjacent must_have (:19) are updated so neither can be misread as demanding an unconditional
guarantee the phase does not, in this one disclosed case, actually deliver.
R9: the gap ledger enumerates items (a)-(l) — the must_have enumeration adds (e) (BB23) and the new D-06
switch-clear item (l) (BB21) — with (k)'s stranded-entry arithmetic stated in full (BB13); the maintainer
annotation covers D-02, D-03 (both narrowings), D-09, AND D-06; the sign-off checkpoint live-verifies the
ack protocol's timeout, durable-record, and refuse-while-pending guarantees (BB6/BB7/BB12); and
files_modified matches Task 2's verified surfaces (BB18).
</success_criteria>

<output>
Create `.planning/phases/05.1-live-world-editor-productization/05.1-15-SUMMARY.md` when done
</output>
