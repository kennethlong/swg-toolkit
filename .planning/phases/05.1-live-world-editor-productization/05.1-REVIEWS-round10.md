---
phase: 5.1
round: 10
reviewers: [codex, cursor, sonnet, opus, fable]
reviewed_at: 2026-08-01T21:17:09Z
plans_reviewed: [05.1-01-PLAN.md, 05.1-02-PLAN.md, 05.1-03-PLAN.md, 05.1-04-PLAN.md, 05.1-05-PLAN.md, 05.1-06-PLAN.md, 05.1-07-PLAN.md, 05.1-08-PLAN.md, 05.1-09-PLAN.md, 05.1-10-PLAN.md, 05.1-11-PLAN.md, 05.1-12-PLAN.md, 05.1-13-PLAN.md, 05.1-14-PLAN.md, 05.1-15-PLAN.md]
reviewed_commit: 751464a
---

# Cross-AI Plan Review — Phase 05.1 — Round 10 (post-BB1–BB23 convergence check)

Reviewed commit `751464a` (round-9 replan on the Fable planner: BB1–BB23 via two canonical specs —
Plan 08's HOST_CMD ACK PROTOCOL and Plan 04's reset-before-render ordering contract; BB4 premise
refuted against `channel_binding.cpp`). Five reviewers, non-overlapping angles as before. Task
files: `.planning/research/CONSULT-05.1-R10-*.md`, raw outputs `…-*.out`. All task files led with
LOCKED measured axioms (channel zero-fill; 218-line count) per the de-anchoring protocol — no
reviewer contradicted them and no falsified premise was re-derived.

**Verdicts:** Codex NOT CONVERGED (3 MED, 1 LOW) · Cursor NOT CONVERGED (1 HIGH, 2 MED, 2 LOW;
20+ MATCH) · Sonnet NOT CONVERGED (2 HIGH, 1 MED, 1 LOW) · Opus NOT CONVERGED (3 HIGH, 3 MED,
4 LOW; 9 dimensions CLEAN) · Fable NOT CONVERGED (4 FALSE, all LOW meta/citation; 24/29 verified).

**Overall: NOT CONVERGED — 6 HIGH, 6 MED, 6 LOW (unified work-list CC1–CC18).**

## Round-over-round signal — the pattern finally split in two

- **The reset-before-render redesign WORKED.** Opus formally derived that the BB2 "structurally
  impossible" absolute is *entailed* by zustand-5 synchronous notify + React 18/19 batching + the
  tearing de-opt — "the guarantee is stronger than the plan's own reasoning needs" — and that the
  BB3 slice-reconstruction and BB22 invariant are correct for every reachable ordering (CLEAN-2/3/4).
  Codex found zero stale hedges for the closed races. **This is the first round where a HIGH-class
  fix was proven closed rather than re-opened — the fix-spawns-seams streak is broken on that half.**
- **The ack-protocol redesign did NOT close.** All 6 HIGHs cluster on it, and they share one root
  (Opus's consensus note): the protocol was specified as a *list of six triggers*, not a *partition
  of a state space over a lifetime*. Missing: the component's own unmount, a precedence rule when
  two triggers land in one commit, latency-aware timeout math, and a switch-surviving durable surface.
- **Fact-check:** 24/29 verified; all 4 FALSE are meta/self-description errors, none touches a
  design decision. Both load-bearing ground-truth foundations of the round (channel zero-fill,
  zustand sync subscribe incl. `vanilla.d.ts:13` signature) re-verified independently by two
  reviewers each. One provenance-hygiene first: a round-9 REVIEWER error (BB19's "Plans 10/13
  already cite :27-31") was restated as plan fact unverified — reviewers are now also an
  injection channel, not just replans.

## Unified work-list (CC1–CC18)

| ID | Sev | Plans/files | Finding | Found by |
|----|-----|-------------|---------|----------|
| CC1 | HIGH | 08 (`:240-273`), 14 (`:684-706`, no cleanup effect) | **Unmount is a seventh transition the six exits don't partition — and the orphan timeout timer commits the exact BB5 defect.** WorldPanel is deliberately remountable mid-session (13:794; ledger (g)/(h) are about tab navigation), yet Plan 14 Task 3 specifies no `clearTimeout` cleanup and Plan 08 no unmount transition. Sequence: place (epoch 7, 10 s timer armed) → navigate off the World tab → refs die but the `setTimeout` survives → agent acks {7,1} (placement IS armed; ack dropped — no mounted consumer) → user switches to Project B (stores reset) → t=10 s the orphan callback fires and writes `outcome:'error'` + `hasFailureBadge=true` **into Project B's freshly-reset history** via the module-scope store. Cross-project misattribution resurrected through an unenumerated route. Fix: `aborted-unmount` transition (clear timer, no record) + cleanup effect. | Opus CC1 |
| CC2 | HIGH | 08 (`:253-273`), 14 (`:694`, `:697`, `:699-701`, slot shape `:684-685`) | **No simultaneity rule, and the prescribed hook declaration order resolves ack-vs-switch the WRONG way.** React runs passive effects in declaration order; Plan 14 declares ack-effect before `[studioDir]`-effect, so in a commit where both are live, resolution runs before abort. The slot carries no `studioDir`, so the ack effect can't detect the project changed. Sequence: pending in A → `openComplete` swaps studioDir (stores reset synchronously) → the overdue ~1 Hz poll timer fires BEFORE the passive-effect flush and writes A's refusal {7,0} into B's just-reset store → ack effect matches epoch 7 → refusal toast + `recordPersistResult` with A's buildingLabel land in B → switch effect then clears a consumed slot. No detach-vs-ack or ack-vs-timeout rule either. Fix: carry `studioDir` in the pending slot and compare (local enforcement), and state the precedence order (aborts win) in the canonical spec. | Opus CC2 |
| CC3 | HIGH | 08 (`:259-265`, `:305-315`), 14 (`:717-720`), 15 (no ledger item) | **`PLACEMENT_ACK_TIMEOUT_MS = 10_000` is not reconciled with the latency sources the spec itself cites — successes get recorded as durable failures, then compounded by an unhedged false refusal.** (a) The deadline bounds the *read*, not the *publish*: with the documented ~1 Hz throttle, an ack published at t=9.4 s is read at ≈10.4 s — a full poll interval is misclassified by construction. (b) The agent is silent during load/login/zoning (>10 s routinely; placing right after a teleport is the normal workflow): timeout records `'error'` → zoning ends → late ack {7,1} matches an empty slot, dropped (see CC7) → placement IS armed → user retries → `g_placementActive` refuses → SECOND durable `'error'` whose copy ("may already be active…") does not hedge — two false failure records + badge for a placement that worked. An undisclosed D-12 narrowing (the history "detail" is affirmatively false) with no must_have/threat-row/ledger item. Fix: latency-aware timeout (≥ worst-case read lag), hedge the copies, and a ledger item for whatever residual remains. | Opus CC3 |
| CC4 | HIGH | 04 (`:324`, `:365-367`, `:393-396`), 08 (`:256-265`), 11, 15 (item (l)), VALIDATION 04-T2 | **The "durable failure surface" isn't durable across the event most likely to follow a missed ack — and the disclosure is narrower than the mechanism.** `reset()` zeroes `history` AND `hasFailureBadge` on every studioDir change, including switch-away-and-back to the same project; Plan 08's own rationale says the durable record (not the toast) is "the guaranteed surface" precisely because the user is alt-tabbed. Ledger item (l) and all its restatement sites disclose only `history` — but `hasFailureBadge` is LOCKED D-12's contract (badge waits for the user), so the badge half is a SECOND undisclosed narrowing riding the same line. Plus one residual unhedged D-06 restatement at 04:324 ("session-only… cleared implicitly by… reload") under the round's own every-site standard. Fix options for the replan to weigh: per-project keyed history, or persist failure records across reset, or disclose the full mechanism (history + badge, incl. same-project round-trip) at every site with a D-12 ledger item. | Sonnet L1 + Opus CC5 + Fable FC-25 |
| CC5 | HIGH | 14 (`:656-659`, `:697-698`, `:720-721`), real `useChannelReader.ts:240-251` | **Detach-tick race: a genuinely-written ack can be discarded and replaced with a false "detached" durable record.** The poll loop's ~1000 ms-throttled pid-liveness check returns BEFORE that tick's `readChannelView`; if client exit is detected on the same tick the check fires (~3% window), an ack already sitting in the channel is never read, polling stops forever, and Plan 14's `aborted-detach` records "detached before acknowledged" even though the agent answered (possibly with success). No test exercises the real poll-loop ordering (the detach test sets liveStore status directly, bypassing it). Fix: read-then-liveness-check ordering in the plan's specified change to the poll loop (or one final read on the detach path) + a test against the real loop. | Sonnet L2 |
| CC6 | HIGH | 04 (`:100-101`, `:104-105`, `:133-135`) | **Plan 04's unrevised "ROUND 10" prose still specifies the SUPERSEDED design:** it names Plan 14's WorldPanel as the `reset()` caller (the R9 contract moved reset to module-scope subscriptions; Plan 14 Task 3 explicitly says "Do NOT call… reset()") and still enumerates a five-field reset (the contract is six incl. `lastHostCommandResult`). An executor reading Plan 04 top-to-bottom gets two contradictory specs for who resets and what. Same class as BB1: the fix landed in the new sections while an older revision block kept the retired mechanism. Fix: rewrite or supersede-mark the ROUND 10 block (04:100-135). | Cursor W01/F05 |
| CC7 | MED | 08 (spec silent), 14 (`:719-720` test prose only) | **The late-ack rule exists nowhere in the canonical spec** — the three cases (late ack vs empty slot / vs NEW pending slot / vs neither) are unpartitioned in Plan 08; only Plan 14's test text states a rule. Case (ii) is safe only via the never-reset send counter (08:330-332) — a consequence never derived in the protocol; case (i) is CC3's dropped success. An executor implementing "Plan 08 verbatim" (14:683) cannot obtain the rule from Plan 08. | Opus CC4 |
| CC8 | MED | 08 (`:25`, `:236-238`, `:310-312`), 14 (`:26`, `:632-680`, `:647-650`), Codex SP-3 | **The cite-never-paraphrase must_have is unsatisfiable against the plan's own ownership split, and the drift it forbids already exists:** Plans 14/09 legitimately restate the machine they implement (14 restates all six transitions; its must_have then claims "cites it, never paraphrases"), and the canonical refusal string differs — 08: "…active **in-game; cancel** it…" vs 14: "…active **elsewhere. Cancel** it…" (plus an action-block shorthand). Fix: scope the must_have to "restates no rule absent from 08 and no string differing from 08", canonicalize the strings character-identically, and drop Plan 14's literally-false "never paraphrases" clause. | Opus CC6 + Cursor A04/A05 + Codex SP-3 |
| CC9 | MED | 04 (`:376-386`), 13 (`:576-585`) | **The module-scope `useWorkspaceStore.subscribe(...)` pattern is first-of-its-kind in this renderer and ships with no lifecycle handling:** no unsubscribe/HMR dispose (orphaned listeners accumulate on dev edits — zero prior `.subscribe(`/`import.meta.hot` usage exists to copy), and it turns `useWorkspaceStore.setState(...)` in test `beforeEach` (the established convention, e.g. ProjectBindingBar.test.tsx) into a trigger of REAL resets, order-dependent across suites. Fix: HMR dispose + documented test-isolation recipe (e.g. exported unsubscribe or reset-silencing helper) in Plans 04/13. | Sonnet L3 |
| CC10 | MED | 13 (`:882`, `:1310-1314`) | D-03's TWO-narrowing set half-propagated inside Plan 13: `:882` still says "(k) is D-03's ONE disclosed narrowing", and the success-criteria site `:1310` carries only (k)'s EXCEPT clause, omitting (g) — while 13:20/:22 correctly carry both. | Codex SP-1/SP-2 |
| CC11 | MED | VALIDATION.md (`:76-84`) | VALIDATION.md has no row for Plan 14 Task 3 — the ack consumer implementing deferred toast, pending slot, timeout/detach, and switch-abort — and the table itself admits "tasks added after 2026-07-31 are not yet rowed here". Row the new/changed tasks (14-T3, and audit 08-T3, 13's new tests). | Codex SP-4 |
| CC12 | MED | 10 (`:126-127`, `:255`) | Stale citation: Plan 10 claims `useChannelReader.ts:173` is blank; it is the `templateName` TextDecoder decode line. (The `:272` clientLabel correction nearby is accurate.) | Cursor C12 |
| CC13 | LOW | 08 (`:246-252`) | "every transition below clears it — timeout included" is false over its own enumeration: local-refuse (BB7) explicitly leaves the slot untouched (a self-loop, not an exit). Reword the quantifier. | Opus CC7 |
| CC14 | LOW | 08 (`:288-291`) vs 09 (`:157-164`) | The disclosed "micro-residual" collision is unreachable under the plans' own rules (tracker rule is `==`, sender counts from 1; a same-instant send is epoch 1 ≠ stale 47). Delete or restate with a real sequence. | Opus CC8 |
| CC15 | LOW | 04 (`:376-382`, `:498-502`) | Reset-before-render's two unstated premises — no `flushSync` on the subscriber path, and both store modules evaluated (bare `subscribe` side effect surviving bundling) before the first switch — hold today but are unasserted/untested. Add a premise note + gate test. | Opus CC9 |
| CC16 | LOW | 13 (`:810`) | `pending.indexOf(entry)` is reference-identity in an otherwise id-keyed design; `findIndex(e => e.id === thatId)` is the drift-proof form. | Opus CC10 |
| CC17 | LOW | 08 (refuse-while-pending), 14 (`:640-641`, `:707`, `:714-716`) | No ambient pending signal: the "+ Add decoration…" trigger stays enabled during the ≤10 s pending window and the "still waiting" refusal names no request; user may have selected a different template between attempts. Minor UX, not correctness. | Sonnet L4 |
| CC18 | LOW | 04 (`:141`), 08 (`:117`, `:120-122`), 13 (`:378`) | Fact-check meta/self-description errors: (a) "Plans 10/13 already cite `:27-31`" — Plan 10 does not (a round-9 reviewer parenthetical restated unverified — provenance-hygiene: verify reviewer-supplied helper claims like any other); (b) Plan 13's BB17 narrative miscounts its own former mislabels ("two… and one… both corrected"); (c) Plan 08 self-describes "two tasks" (it has three) and "unchanged except one note" (Task 2 got a second edit); (d) Plan 08's "five consecutive rounds" attribution conflates the general pattern with the ack protocol's two-round history. | Fable FC-7/FC-15/FC-17/FC-18 |

## Rejected / no-action findings

None this round. (All five reviewers honored the LOCKED axioms; no finding contradicted measured
ground truth.)

## What was proven or propagated cleanly this round (do not re-litigate)

- **Reset-before-render is entailed, not asserted** (Opus CLEAN-2): zustand-5 synchronous notify
  (verified at `vanilla.js:6-13` / `vanilla.d.ts:13`) + React default batching (no render inside the
  listener loop; registration order not load-bearing) + `useSyncExternalStore` tearing de-opt
  (pre-set transition renders cannot commit mixed frames) ⇒ 13:31(a)'s restored absolute holds.
  CC15's premises are the only caveat, and they hold today.
- **BB3's slice-reconstruction correct for every reachable ordering**; **BB22's at-most-one-sticky
  invariant structurally true** (onUndo fully synchronous — zero async/await/Promise in Plan 13)
  (Opus CLEAN-3/4).
- **Epoch math composes** (monotonic-from-1 sender, `==` no-op rule, epoch-0 reset-and-skip, LOCKED
  zero-fill) — BB4 stays retired (Opus CLEAN-5; Fable FC-19).
- **Always-ack propagated completely** across Plans 09/12 incl. both guards and T-05.1-12c (Opus
  CLEAN-6; Cursor A03; Codex mechanics list).
- **Ack identifier/type chain character-consistent** at every hop (epoch return, store field/setter,
  10_000 constant, `code === 1`) — the residue is strings (CC8) and lifecycle (CC1/CC2), not wiring
  (Cursor A01/A02, chain table).
- **Retired AA14 clear-at-top survives nowhere as active behavior**; no stale hedge for the closed
  BB2/BB3 races anywhere (Codex).
- **The ledger set (a)–(l) intact and propagated**, step 13 names D-02/D-03/D-09/D-06; BB14's three
  sites closed; BB15 retired; BB17 mislabels corrected; new "R9 review, BBn" convention applied
  consistently (Opus CLEAN-7/8/9; Fable FC-13/14/16/21).
- **20+ real-source citations byte-verified MATCH** incl. `channel_binding.cpp:113-170`/`:153-154`,
  `workspaceStore.ts:79-86`/`:100-109`, zustand `5.0.14`, `useChannelReader.ts:114-118`/`:242-250`/`:285`,
  `channel.cpp:206-232`, `addon.cpp:46-78` (Cursor C01–C20; Fable FC-1…FC-6, FC-8).

## Per-reviewer summaries

### Codex (seam-propagation trace) — NOT CONVERGED
SP-1/SP-2 (MED) = CC10's half-propagated two-narrowing set. SP-3 = CC8's "cites, never paraphrases"
self-contradiction. SP-4 (MED) = CC11's missing VALIDATION rows. Confirms ack-protocol mechanics and
reset-ordering consistent at every named site, AA14 retired, no stale hedges.
Full output: `CONSULT-05.1-R10-codex-seam-trace.out`.

### Cursor (line-level citation & wiring) — NOT CONVERGED
W01 (HIGH) + F05 = CC6's stale Plan 04 ROUND 10 block. A04/A05 (MED/LOW) = CC8's string drift.
C12 (MED) = CC12. 20+ MATCHes on new citations; full ack-chain and reset-chain tables MATCH at the
revision layer. Full output: `CONSULT-05.1-R10-cursor-line-verify.out`.

### Sonnet (lateral new-seam hunt) — NOT CONVERGED
L1 (HIGH) = CC4's non-durable durable surface. L2 (HIGH) = CC5's detach-tick race (real poll-loop
code). L3 (MED) = CC9's subscribe lifecycle/test-isolation. L4 (LOW) = CC17. Six hunting grounds
swept clean incl. first-open harmlessness, timeout-timer throttle independence, no-deadlock across
exits, and untouched Plans 01–06. Full output: `CONSULT-05.1-R10-sonnet-lateral.out`.

### Opus (locked-decision fidelity & spec math) — NOT CONVERGED
CC1/CC2/CC3 (HIGH, all ack-protocol), CC4-facet/CC7/CC8-facet (MED), CC13–CC16 (LOW). Nine CLEAN
dimensions with derivations — the reset-before-render proof (CLEAN-2) is the round's headline.
Consensus note: the protocol needs a state × event table incl. unmount, a precedence order with
aborts winning, and `studioDir` in the slot. Full output: `CONSULT-05.1-R10-opus-spec-math.out`.

### Fable (fact-check of NEW claims, diff 8cfa11f..751464a) — NOT CONVERGED
29 claim groups: 24 VERIFIED (both ground-truth foundations re-verified; ledger letters; convention
consistency; BB-quotes verbatim), 4 FALSE (all LOW, = CC18 + the CC4 residual site), 1 UNVERIFIABLE.
Full output: `CONSULT-05.1-R10-fable-factcheck.out`.

## Consensus themes for the round-11 replan

1. **Finish the ack protocol the way the reset contract was finished: as a total specification.**
   Replace the six-trigger list with a state × event table over the full lifetime (states: idle,
   pending; events: ack(match/stale/late), timeout, detach, switch, UNMOUNT, second-place-attempt),
   with an explicit precedence order in which the abort family wins, `studioDir` carried in the
   pending slot, a latency-aware timeout (≥ one worst-case poll interval past the deadline, and
   hedged copy for the zoning case), and the late-ack rule stated IN Plan 08 (CC1, CC2, CC3, CC7,
   CC13). CC5's read-before-liveness ordering belongs to the same pass.
2. **Make the durable surface actually durable — and disclose what remains.** Decide: per-project
   history keying, failure-record persistence across reset, or full disclosure (history AND badge,
   D-12 ledger item, every restatement site incl. 04:324) (CC4).
3. **Harden the new module-scope subscribe pattern** (HMR dispose, test-isolation recipe, premise
   assertions) without weakening the proven ordering guarantee (CC9, CC15).
4. **String canonicalization + honest citation discipline** (CC8), the stale Plan 04 ROUND 10 block
   (CC6), the (k)+(g) completion (CC10), VALIDATION rows (CC11), and the mechanical LOWs
   (CC12–CC18).
