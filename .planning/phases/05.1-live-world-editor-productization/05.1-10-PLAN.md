---
phase: 05.1-live-world-editor-productization
plan: 10
type: execute
wave: 2
depends_on: ["05.1-02", "05.1-04", "05.1-06"]
files_modified:
  - packages/renderer/src/workspace/WorkspaceShell.tsx
  - packages/renderer/src/workspace/workspace-config.ts
  - packages/renderer/src/panels/world/WorldPanel.tsx
  - packages/renderer/src/panels/world/WorldPanel.test.tsx
autonomous: true
requirements: [pivot-driven]
must_haves:
  truths:
    - "The World panel matches sketch 019-A's building-tree spine element-for-element (SC2) for the tree/detail-card/mirror-toggle/live-strip elements this plan builds; Activity/Scene accordions and the footer land in Plan 11"
    - "The mirror-mode scope control shows a VISIBLE, DISABLED 'Per-instance (server repoint)' option with a 'coming later' hint next to the active 'Per-template' option — D-08 locked decision, not an omission (D-08)"
    - "The World tab's dockview title reflects hasFailureBadge (a visible marker, e.g. an appended glyph), and opening/activating the World tab calls acknowledgeFailures() — D-12's 'attention badge' contract has a REAL renderer, not just store-side logic with no UI consumer (REVIEWS.md C12)"
    - "The live-session strip shows a visible hint when the attached client's install path differs from the project's bound client (D-07b mismatch hint)"
    - "The mirror-mode toggle actually resolves a real overrideDir/readVfs pair and calls reconcileMirrorMode with them — not a call this plan describes but never shows how to construct (ROUND 3, R4)"
    - "Every refresh() call site (mount, manual refresh, mirror toggle) re-reads worldEditorBuildingTemplates fresh via one shared refreshTree() helper — never a stale mount-time meta value (ROUND-3-REVIEW R2, HIGH)"
    - "The detail card renders D-13's FULL 12-element before/after transform (not translation-only) so a rotate-only edit visibly differs before vs after, never reading as \"nothing changed\" (ROUND-3-REVIEW R4, HIGH — ties directly to SC5)"
    - "clientLabel is read via its own useLiveStore selector, never via liveStatus.clientLabel (which does not type-check) (ROUND-3-REVIEW R8/M2)"
    - "An unresolvable scan root (resolveScanRoot returns null — unbound project offline) renders a words-only disabled state and never calls refresh(), matching the Scene accordion's no-live-session precedent (ROUND-3-REVIEW R8/M3)"
    - "mirrorToStockIlf has exactly ONE persistence owner (reconcileMirrorMode) — the panel's toggle never independently calls updateWorkspaceMeta, closing the two-writers-two-moments D-09 risk (ROUND 3, R6)"
    - "A failed reconcileMirrorMode result is surfaced DURABLY — a worldEditorStore.history entry, visible in the Activity accordion for the rest of the session, not merely a transient log() line — and the toggle's visual state reverts to match what Plan 06 actually persisted; per Plan 06's ROUND 4/W1 transactional redesign, WHEN every failures entry carries diskState: 'unchanged' this failure is ATOMIC — the history entry says NO building's mirror was changed this call (validated-clean-or-rolled-back), never a mix of some-succeeded/some-failed wording (ROUND-3-REVIEW R6 + ROUND 4/W1, Plan-10 half) — the disclosed diskState: 'uncertain' case is a SEPARATE, hedged wording, deferred to the must_have below rather than claimed here (ROUND 5, V7; ROUND 6, X2)"
    - "The detail card's inert stub buttons (Go to / Revert / Edit in game) show an explicit, honest 'not yet wired' message when clicked — never the sketch mock's literal action-implying toast text ('Reverting row 3…', 'Row pinned to overlay gizmo') (ROUND 3, R11a)"
    - "The mirror toggle's onChange and (via Plan 13) the Remove AND Undo actions both resolve overrideDir/readVfs through ONE shared, null-safe resolveOverridePair() helper — the same null-guard refreshTree() already applies extends to every consumer that needs a real overrideDir, so `string | null` never silently flows into a `string`-typed dependency (ROUND 4, W3 — MEDIUM, Cursor)"
    - "resolveOverridePair() guards studioDir (workspaceStore.ts's string | null) BEFORE calling readWorkspaceJson(studioDir) — never an unguarded string | null feeding a string-typed dependency — and returns the confirmed-non-null studioDir as part of its resolved pair, so every consumer needing it (Plan 13's removeDecorationRow call) reads the SAME resolved value rather than re-selecting it independently (ROUND 5, V6 — MEDIUM, Cursor)"
    - "A reconcileMirrorMode failure whose diskState is 'uncertain' (Plan 06's disclosed rollback-double-fault residual, T-05.1-06d) produces a HEDGED history message naming which building(s) could not be verified, never the unconditional 'no buildings' mirrors were changed' wording used for every other (diskState:'unchanged') failure (ROUND 5, V7 — MEDIUM, Sonnet)"
    - "resolveOverridePair() reads studioDir via useWorkspaceStore.getState().studioDir (the imperative accessor), NEVER the useWorkspaceStore((s) => s.studioDir) hook-selector form — resolveOverridePair() is a plain function invoked from event handlers (the mirror toggle's onChange, and, via Plan 13, Remove/Undo), so a hook call inside it would throw 'Invalid hook call' on the first write action any user takes in this panel (ROUND 6, X1 — HIGH, three independent reviewers: Cursor/Fable/Opus)"
  artifacts:
    - path: "packages/renderer/src/panels/world/WorldPanel.tsx"
      provides: "the 019-A dockview World tab"
      contains: "Edited buildings"
  key_links:
    - from: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      to: "packages/renderer/src/panels/world/WorldPanel.tsx"
      via: "panelComponents['world'] registration"
      pattern: "world:\\s*WorldPanel"
    - from: "packages/renderer/src/panels/world/WorldPanel.tsx"
      to: "packages/renderer/src/state/worldEditorStore.ts"
      via: "useWorldEditorStore selector reads for the tree/selection; hasFailureBadge drives the dockview tab title; acknowledgeFailures() called on panel activation"
      pattern: "useWorldEditorStore\\("
sketch_elements:
  # 019-A elements this plan builds (the rest land in Plan 11):
  - "dockview World tab in the Inspect | Deploy | World tabstrip"
  - "live-session strip (dot, exe/pid, scene chip, refresh)"
  - "mirror-mode toggle row (switch, label, per-template/per-instance scope hint, PER-TEMPLATE badge)"
  - "scope control shows BOTH modes: 'Per-template (all instances of this layout)' active, AND 'Per-instance (server repoint)' as a VISIBLE, DISABLED option with a 'coming later' hint (D-08 — locked decision; NOT optional, NOT Claude's discretion)"
  - "'Edited buildings' collapsible section with a live count chip"
  - "building tree rows (icon, name, template/node-id subtext, status badge e.g. '3 SAVED' / '1 UNAPPLIED')"
  - "nested decoration rows under their building (indent, name, cell·row subtext, SAVED/ARMED badge)"
  - "selection detail card (Decoration / Cell·row / Position / Last persist / Files, with Go to / Revert / Edit in game actions)"
---

<objective>
Build sketch 019-A's spine: a real dockview `World` tab, registered per `WorkspaceShell.tsx`'s established
5-place contract (`panelComponents`, `STATIC_PANEL_IDS`, `PANEL_TITLES`, `PANEL_REOPEN_POSITIONS`, +
`LAYOUT_VERSION` bump in `workspace-config.ts`), rendering the building-first tree (buildings own the
hierarchy, decorations nest under their building), the mirror-mode toggle (wired to Plan 06's per-project
threading), the live-session strip (reading `liveStore` the same way `StatusBar.tsx` already does), and a
selection detail card. This plan does NOT build the Activity/Scene accordions or the footer's Add-decoration/
Stage buttons — those are Plan 11, kept separate to respect the 2-3 task budget and because they depend on
Plan 08's HOST_CMD wrapper this plan does not need.

**ROUND 3 REVISIONS (2026-08-01, REVIEWS.md R4/R5/R6/R8/R11a/MED-7/MED-11):**
- **R4 (HIGH, Opus):** the original Task 2 said the mirror toggle's onChange "calls reconcileMirrorMode" but
  never showed how `overrideDir`/`readVfs` — two of `reconcileMirrorMode`'s four required parameters — get
  built. Task 2 now explicitly resolves `overrideDir` via `resolveScanRoot` (Plan 04 ROUND 3, imported by name)
  and `readVfs` via `makeReadVfs` (Plan 06 ROUND 3, now exported), the SAME two helpers the refresh() flow
  already needs.
- **R6 (MEDIUM, Opus):** the original design had the toggle's onChange call BOTH `reconcileMirrorMode` (which
  itself calls `updateWorkspaceMeta`, per Plan 06) AND a second, separate `updateWorkspaceMeta` call from this
  panel — two writers, two moments, the exact divergence class D-09 exists to prevent. Fixed: this panel calls
  ONLY `reconcileMirrorMode`; it is the sole owner of persisting `mirrorToStockIlf`.
- **R8 (MEDIUM, Opus):** `reconcileMirrorMode` now returns `{ failures: [...] }` (Plan 06 ROUND 3) instead of
  `void`. The onChange handler consumes the RETURN VALUE (surfacing any per-building failures via `log()`) —
  the dead try/catch this plan originally wrapped a "never throws" function in is replaced with a real
  consumption of the structured result (the try/catch may still exist as defense-in-depth around an unexpected
  throw, but it is no longer the PRIMARY failure-reporting path).
- **R5 (HIGH, Sonnet):** the detail card's "Last persist" field now renders D-13's full readout — before/after
  transform + cell/row — from the matching history entry's new fields (Plan 04 ROUND 3), not just an outcome
  word.
- **R11a (MEDIUM, Sonnet):** the detail card's inert "Go to"/"Revert"/"Edit in game" stub buttons now show an
  explicit, HONEST "not yet wired — coming in a later phase" message on click (or equivalent wording), never
  the sketch mock's literal action-implying strings ("Reverting row 3 to stock transform…", "Row pinned to
  overlay gizmo") — those imply the action actually happened, which would be misleading for a stub.
- **MED-7 (Opus):** row selection now uses Plan 04's `worldEditorBuildingRowId`/`parseWorldEditorRowId`
  (ROUND 3) instead of ad-hoc string construction/parsing.
- **MED-11 (Opus):** `resolveScanRoot` is now called BY NAME, explicitly, from this panel's refresh flow (its
  revised signature per Plan 04 ROUND 3 takes an already-resolved `{ cfgPath, clientPath }` pair, which this
  panel builds from its own `readWorkspaceJson` call).

**CROSS-AI REVIEW REVISION (2026-08-01, C12 — MEDIUM, Cursor/Sonnet):** the original plan's Task 2 defined
`hasFailureBadge`/`acknowledgeFailures()` (Plan 04) as store-side logic but never actually RENDERED the badge
anywhere in the World tab, and never CALLED `acknowledgeFailures()` when the tab is opened — D-12's "failure
punt = badge + detail waiting" contract was half-delivered (the "waiting" half worked, the "badge" half did
not). Task 2 below adds both, reusing the `props.api.setTitle(...)` modified-dot idiom already established in
this repo (`DatatableGridEditor.tsx`/`StfStringsEditor.tsx`/`SidebarPanel.tsx`) and the per-panel
`props.api.onDidActiveChange`/`props.api.isActive` dockview API (verified present in this repo's
`dockview-core` version this session) for the activation hook.

**ROUND 4 REVISIONS (2026-08-01, REVIEWS.md W1/W3/W4/W5):**
- **W1 (HIGH, Sonnet — consumer-side propagation of Plan 06's transactional redesign):** Plan 06's
  `reconcileMirrorMode` is now atomic on DISK, not just on the flag (its own ROUND 4/W1 fix) — when it returns
  a non-empty `failures` array **whose entries ALL carry `diskState: 'unchanged'`** (the common case), EVERY
  building's mirror is provably unchanged (validated-clean-blocked) or has
  been rolled back to its pre-call state (mid-apply throw), never a mix of some-succeeded/some-failed.
  **(ROUND 10, AA7 — HIGH, Codex, byte-verified)** ANY entry carrying `diskState: 'uncertain'` (Plan 06's
  disclosed `T-05.1-06d` rollback-double-fault residual) is a HEDGED exception to this claim — see ROUND 5/V7
  below and this plan's must_haves `:26`/`:30`, which already condition this correctly; this ROUND 4 narrative
  bullet is now conditioned to match. Task 2's
  history-entry wording below is updated to say so explicitly, rather than implying a partial per-building
  outcome the underlying function no longer produces.
- **W3 (MEDIUM, Cursor):** the original Task 2 guarded `refresh()`'s call site against a `null` `overrideDir`
  (ROUND-3-REVIEW R8/M3) but the mirror toggle's onChange resolved `overrideDir`/`readVfs` inline with NO such
  guard before calling `makeReadVfs`/`reconcileMirrorMode` — a real `tsc` error waiting to happen the moment
  `resolveScanRoot` returns `null` (`string | null` flowing into `makeReadVfs(overrideDir: string)`). Task 2
  now extracts ONE shared `resolveOverridePair()` helper (defined once in this component, alongside
  `refreshTree()`) that carries the SAME null-guard everywhere an `overrideDir`/`readVfs` pair is needed —
  `refreshTree()` and the mirror toggle's onChange BOTH call it (Plan 13's Remove action, same file, later
  wave, calls it too — see Plan 13's own ROUND 4 revision).
- **W4 (LOW, Cursor — prose only, no behavior change):** the `liveStore.ts` interface citation below is
  unchanged in substance; see W5 for the actual citation fix.
- **W5 (LOW, Fable — citation fix):** the `clientLabel` accessor was cited as `useChannelReader.ts:173`; that
  line is blank. Verified this session: the real `getState()`-form read sits at `useChannelReader.ts:272`
  (`clientExe: useLiveStore.getState().clientLabel`); the true selector-idiom precedent this plan's own
  `useLiveStore((s) => s.clientLabel)` pattern matches is `LiveSyncClientCard.tsx:159`. Citation corrected
  below to both real locations.

**ROUND 5 REVISIONS (2026-08-01, REVIEWS.md V6/V7):**
- **V6 (MEDIUM, Cursor):** `resolveOverridePair()` was described as reading `readWorkspaceJson(studioDir)`
  without ever naming WHERE `studioDir` (workspaceStore.ts:34 — `string | null`) comes from or guarding it —
  the same unguarded-`string | null` class W3 already fixed for `overrideDir`, left open for `studioDir`.
  Task 2 below now names `useWorkspaceStore` explicitly, guards `!studioDir` FIRST (before any
  `readWorkspaceJson` call), and returns the confirmed studioDir as part of `resolveOverridePair()`'s resolved
  pair so Plan 13's Remove/Undo call sites (which need `studioDir` as `removeDecorationRow`'s first
  parameter) read it from the SAME pair instead of a second, independent selector.
- **V7 (MEDIUM, Sonnet):** Plan 06's disclosed `T-05.1-06d` rollback-double-fault residual (a rollback step
  that itself throws) means `reconcileMirrorMode`'s "no buildings' mirrors were changed" wording is provably
  false in that one disclosed case. Now that Plan 06 returns `diskState: 'unchanged' | 'uncertain'` per
  failure entry, this panel's history-entry message below checks it: an all-`'unchanged'` failure set keeps
  the existing all-or-nothing wording; ANY `'uncertain'` entry produces a hedged message instead ("mirror
  mode change blocked — most buildings' mirrors were not changed, but {uncertain building ids} could not be
  verified — check their mirror files manually: {reasons}").

**ROUND 7 REVISION (2026-08-01, REVIEWS.md Z3/Z18):**
- **Z3 (HIGH, Codex R7-S1):** Task 2's own test/acceptance text (the ROUND-4/W1 assertion) still asserted
  that ANY non-empty `failures` result produces the unconditional "no building's mirror was changed"
  wording — the FOURTH restatement of the pre-hedge absolute the ROUND 6/X2 sweep missed (the must_haves
  above, `:26`/`:30`, were already correctly conditioned; the test prose describing the SAME behavior was
  not). Both test-prose sites below are now conditioned identically to the must_haves: the unconditional
  wording applies ONLY when every `failures` entry carries `diskState: 'unchanged'`; any entry carrying
  `diskState: 'uncertain'` defers to the ROUND 5/V7 hedged-wording contract.
- **Z18 (LOW, Fable #7):** `DeployDialog.tsx:139,185,327` was cited as three examples of a "`getState()`-in-
  handlers idiom" — only `:327` (inside `handleDeploy`, a real click-handler callback) is literally a
  handler; `:139`/`:185` are imperative `useEffect`-body reads. The load-bearing point (`getState()`
  called imperatively outside a hook-selector context, not literally "inside a DOM event handler") holds
  for all three — the phrasing below is corrected so it no longer over-claims what `:139`/`:185` are.

Purpose: close the largest chunk of SC2 (019-A element-for-element parity) with the panel's data-bearing core
before the smaller accordion/footer additions land on top, AND make D-12's attention badge actually visible
(C12).
Output: `World` tab exists, shows the disk-scanned building tree with live-session overlay, mirror toggle
functions end-to-end through one shared null-safe resolution helper, selection shows a detail card, the tab
title reflects a failure badge and clears it on activation — verified by a component test enumerating each
019-A element present in this plan's scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-live-world-editor-productization/05.1-RESEARCH.md
@.planning/phases/05.1-live-world-editor-productization/05.1-PATTERNS.md
@.planning/phases/05.1-live-world-editor-productization/05.1-04-SUMMARY.md
@.planning/phases/05.1-live-world-editor-productization/05.1-06-SUMMARY.md
@.planning/sketches/019-world-editor-panel/README.md
@.planning/phases/05.1-live-world-editor-productization/05.1-REVIEWS.md
</context>

<interfaces>
From packages/renderer/src/workspace/WorkspaceShell.tsx (the registration contract's CURRENT 4-place shape
— Task 1 extends all four sites AND bumps LAYOUT_VERSION, which together are the "5-place" contract the
objective and Task 1's name refer to; R9 review, BB20 — follow verbatim):
```typescript
const panelComponents: Record<string, React.FunctionComponent<IDockviewPanelProps<any>>> = { /* ...existing..., deploy: DeployPanel */ };
const STATIC_PANEL_IDS = ['sidebar', 'viewport', 'inspector', 'data', 'live-inspector', 'deploy', 'vcs', 'console', 'log'] as const;
const PANEL_TITLES: Record<string, string> = { /* ...existing..., deploy: 'Deploy' */ };
const PANEL_REOPEN_POSITIONS: Record<string, { direction: string; referencePanel?: string }> = { /* ...existing..., deploy: { direction: 'within', referencePanel: 'inspector' } */ };
```
From packages/renderer/src/workspace/workspace-config.ts (bump + addPanel pattern, current LAYOUT_VERSION=4):
```typescript
export const LAYOUT_VERSION = 4 as const; // bump to 5 for 'world'
api.addPanel({ id: 'deploy', component: 'deploy', title: 'Deploy', position: { direction: 'within', referencePanel: 'inspector' }, initialWidth: 440 });
```
From packages/renderer/src/panels/editors/DatatableGridEditor.tsx (verified this session — the dockview tab
modified-dot idiom to reuse verbatim for the failure badge, C12):
```typescript
// Dockview tab modified-dot (05-08): mirrors SidebarPanel.tsx's api.setTitle precedent.
api?.setTitle(`${fileName} — Datatable${anyRowModified ? ' ●' : ''}`);
```
From node_modules/dockview-core's panelApi (verified this session — the per-panel activation hook, C12):
```typescript
readonly isActive: boolean;
readonly onDidActiveChange: Event<{ isActive: boolean }>;
```
From packages/renderer/src/state/worldEditorStore.ts (Plan 04 output, reuse verbatim):
```typescript
export const useWorldEditorStore: /* tree, selectedRowId, sessionOverlay, history, hasFailureBadge, acknowledgeFailures, refresh, select, ... */;
export function worldEditorRowId(buildingId: string, cellName: string, rowIndex: number): string;
```
From packages/renderer/src/services/worldEditorScan.ts (Plan 04 ROUND 3 output, WorldEditorBuilding shape +
resolveScanRoot):
```typescript
export interface WorldEditorBuilding { buildingId: string; displayLabel: string; editedIlfPath: string; derivedTemplatePath: string; buildingTemplateVfsPath: string; decorations: { cellName: string; rowIndex: number; objectTemplateName: string; transform: number[] }[]; }
export function resolveScanRoot(clientExe: string | null, offlineBinding: { cfgPath?: string; clientPath: string | null } | null): string | null;
export function scanWorldEditorState(overrideDir: string, buildingTemplates?: Record<string, string>): WorldEditorBuilding[];
```
From packages/renderer/src/services/decorationPersistOrchestrator.ts (Plan 06 ROUND 3/ROUND 4 output —
reconcileMirrorMode is now transactional on disk (ROUND 4/W1: a non-empty `failures` array whose entries ALL
carry `diskState: 'unchanged'` means NO building's mirror changed this call, never a partial mix — ROUND 10/AA7:
ANY entry carrying `diskState: 'uncertain'` is the disclosed, hedged exception, ROUND 5/V7 below), and
makeReadVfs is now exported; reuse both for the toggle's
onChange and for resolving `readVfs`):
```typescript
export function reconcileMirrorMode(studioDir: string, overrideDir: string, readVfs: (vfsPath: string) => Buffer, nextValue: boolean): { failures: { buildingId: string; error: string; diskState: 'unchanged' | 'uncertain' }[] };
export function makeReadVfs(overrideDir: string): (vfsPath: string) => Buffer;
```
From packages/renderer/src/state/worldEditorStore.ts (Plan 04 ROUND 3 output — the shared row-id contract,
MED-7):
```typescript
export function worldEditorBuildingRowId(buildingId: string): string;
export function parseWorldEditorRowId(id: string): { kind: 'building'; buildingId: string } | { kind: 'decoration'; buildingId: string; cellName: string; rowIndex: number };
```
From packages/renderer/src/shell/StatusBar.tsx (the live-store subscription idiom to mirror for the live
strip, lines 35-84):
```typescript
import { useLiveStore } from '../state/liveStore';
const liveStatus = useLiveStore((s) => s.status);
```
From packages/renderer/src/state/liveStore.ts (verified this session — ROUND-3-REVIEW R8/M2, Opus:
`liveStatus.kind === 'attached'` narrows to `{ kind: 'attached'; pid: number; mappingName: string }`
(`liveStore.ts:30`) — there is NO `clientLabel` field on that union member. `clientLabel` is a TOP-LEVEL store
field (`liveStore.ts:237`, set at `:394`) — read it via its OWN selector. Using `liveStatus.clientLabel`
directly is a `tsc` error, not a runtime nuance — do not write that access pattern anywhere in this plan.
**(ROUND 4, W5 — citation fix, Fable)** the selector-idiom precedent is `LiveSyncClientCard.tsx:159`
(`useLiveStore((s) => s.clientLabel)`); the `getState()`-form read this plan's own R3/Plan-06 code uses
elsewhere is `useChannelReader.ts:272` — `useChannelReader.ts:173` (an earlier draft's citation) is a blank
line, verified this session):
```typescript
const clientLabel = useLiveStore((s) => s.clientLabel);
```
From packages/renderer/src/services/projectBinding.ts (existing — WorkspaceBindingMeta.clientPath, for the
D-07b mismatch hint):
```typescript
export function readWorkspaceJson(studioDir: string): WorkspaceBindingMeta; // .clientPath: string | null
```
From packages/renderer/src/state/workspaceStore.ts (existing — the active project's studioDir, ROUND 5/V6 —
reuse verbatim, never re-derive; ROUND 6/X1 — inside `resolveOverridePair()`, an event-time-invoked plain
function, read it via `useWorkspaceStore.getState().studioDir`, NEVER the `useWorkspaceStore((s) =>
s.studioDir)` hook-selector form, which would throw "Invalid hook call" outside of render):
```typescript
export const useWorkspaceStore: /* ...; studioDir: string | null; ... */;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register the World panel (5-place dockview contract)</name>
  <files>packages/renderer/src/workspace/WorkspaceShell.tsx, packages/renderer/src/workspace/workspace-config.ts</files>
  <read_first>
    packages/renderer/src/workspace/WorkspaceShell.tsx (lines 55-154, the full registration block — import,
    panelComponents, STATIC_PANEL_IDS, PANEL_TITLES, PANEL_REOPEN_POSITIONS — this is the EXACT contract every
    field must be added to, per the file's own Pitfall 5 docstring warning)
    packages/renderer/src/workspace/workspace-config.ts (full file — LAYOUT_VERSION history comment convention
    at lines 37-42, buildInitialLayout's deploy/vcs addPanel calls at lines 129-141)
  </read_first>
  <action>
    In WorkspaceShell.tsx: import a placeholder `WorldPanel` (created fully in Task 2) from
    '../panels/world/WorldPanel'; add `world: WorldPanel` to `panelComponents`; add `'world'` to
    `STATIC_PANEL_IDS`; add `world: 'World'` to `PANEL_TITLES`; add `world: { direction: 'within',
    referencePanel: 'inspector' }` to `PANEL_REOPEN_POSITIONS` (docks alongside Inspect/Deploy/VCS, matching
    019-A's "Inspect | Deploy | World" tabstrip). In workspace-config.ts: bump `LAYOUT_VERSION` from 4 to 5,
    adding a doc-comment line in the same style as the existing history ("Bumped from 4 → 5 (05.1-10): added
    the 'world' panel — Live World Editor productization."); add an `api.addPanel({ id: 'world', component:
    'world', title: 'World', position: { direction: 'within', referencePanel: 'inspector' } })` call in
    `buildInitialLayout` immediately after the existing `vcs` addPanel call.
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- WorkspaceShell workspace-config</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "world:" packages/renderer/src/workspace/WorkspaceShell.tsx` shows all four registrations;
    `grep -n "LAYOUT_VERSION     = 5" packages/renderer/src/workspace/workspace-config.ts` matches; existing
    WorkspaceShell/workspace-config tests remain green (no regression to the version-guard/reopen-menu logic).
  </acceptance_criteria>
  <done>The World panel id is fully registered across all five required places; a returning user's saved
  layout is discarded and rebuilt with the World tab present (LAYOUT_VERSION guard).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorldPanel.tsx — tree, mirror toggle, live strip, detail card, failure badge (019-A core, C12, D-07b, ROUND 4 W1/W3)</name>
  <files>packages/renderer/src/panels/world/WorldPanel.tsx, packages/renderer/src/panels/world/WorldPanel.test.tsx</files>
  <read_first>
    .planning/sketches/019-world-editor-panel/index.html (lines 206-314, Variant A's full DOM — the tabstrip,
    live-strip, mirror-row with switch+hint+badge, "Edited buildings" collapsible section with count chip,
    building rows with icon/name/sub/badge, nested indented decoration rows, and the detail card's kv pairs +
    action buttons — match this structure and wording, not just its intent)
    packages/renderer/src/state/worldEditorStore.ts (Plan 04 — tree/selection/refresh/sessionOverlay/
    hasFailureBadge/acknowledgeFailures)
    packages/renderer/src/services/worldEditorScan.ts (Plan 04 — WorldEditorBuilding shape)
    packages/renderer/src/services/decorationPersistOrchestrator.ts (Plan 06 — reconcileMirrorMode, now
    transactional on disk per ROUND 4/W1)
    packages/renderer/src/services/projectBinding.ts (readWorkspaceJson/updateWorkspaceMeta — for the toggle's
    persisted state, reading the current mirrorToStockIlf value on mount, and reading clientPath for the D-07b
    mismatch hint)
    packages/renderer/src/shell/StatusBar.tsx (lines 35-84, the useLiveStore direct-subscription idiom for
    the live-session strip — dot/exe/pid/scene chip)
    packages/renderer/src/panels/editors/DatatableGridEditor.tsx (lines 294-298, the `props.api.setTitle`
    modified-dot idiom — the direct precedent for C12's failure-badge tab title)
  </read_first>
  <behavior>
    - **(ROUND 4, W3 — MEDIUM, Cursor; ROUND 5, V6 — MEDIUM, Cursor, studioDir guard; ROUND 6, X1 — HIGH,
      three independent reviewers: Cursor/Fable/Opus)** ONE shared
      `resolveOverridePair(): { overrideDir: string; readVfs: (vfsPath: string) => Buffer; meta:
      WorkspaceBindingMeta; studioDir: string } | null` helper is defined in this component's body (co-located
      with `refreshTree()`), carrying the SAME null-guard as `refreshTree()`'s own resolution. `resolveOverridePair()`
      is a PLAIN component-local function invoked from EVENT HANDLERS (the mirror toggle's `onChange`, and —
      later wave, same file — Plan 13's Remove click and `handleUndo`), never rendered directly in JSX. **(ROUND
      5, V6; ROUND 6, X1 — the fix)** It therefore FIRST reads `studioDir` via
      `useWorkspaceStore.getState().studioDir` (`workspaceStore.ts:34` — `string | null` — the imperative,
      non-hook accessor every Zustand store exposes, NEVER the `useWorkspaceStore((s) => s.studioDir)`
      hook-selector form: calling a React hook from inside a plain function invoked at event time throws
      "Invalid hook call" at runtime on the first click of every write action this panel offers — the SAME
      `getState()`-outside-render idiom this codebase already establishes: `DeployDialog.tsx:327` (inside
      `handleDeploy`, a genuine click-handler callback) plus `:139`/`:185` (imperative `useEffect`-body reads
      — not literally click handlers, but the SAME load-bearing point: `getState()` called imperatively
      outside a hook-selector context, ROUND 7/Z18 citation fix, Fable #7) and
      `useChannelReader.ts:272`, and the SAME form this SAME plan's own R2 clause below already requires for
      `readWorkspaceJson` — a hoisted/closed-over hook value cached across calls is explicitly foreclosed by
      that clause too, so `getState()` is the only form that satisfies both R2's freshness rule and React's
      rules of hooks at once) and returns `null` IMMEDIATELY — before calling `readWorkspaceJson` at all — when
      `!studioDir` (no project bound; the SAME unbound-offline words-only branch as the `overrideDir === null`
      case below; `readWorkspaceJson` requires a non-null `string` and was never guarded against this case
      before). Only once `studioDir` is confirmed non-null does it read `readWorkspaceJson(studioDir)` FRESH
      into `meta`, resolve `overrideDir` via `resolveScanRoot(liveStatus.kind === 'attached' ? clientLabel ??
      null : null, { cfgPath: meta.cfgPath, clientPath: meta.clientPath })`, and return `null` immediately (no
      `readVfs` constructed, no caller proceeds) when `overrideDir === null` — otherwise returns `{
      overrideDir, readVfs: makeReadVfs(overrideDir), meta, studioDir }` (the confirmed-non-null `studioDir` is
      now part of the returned pair, ROUND 5/V6 — every consumer needing BOTH, e.g. Plan 13's
      `removeDecorationRow(studioDir, overrideDir, ...)` call site for its Remove AND Undo actions, reads
      `studioDir` from THIS SAME resolved pair rather than re-selecting it independently). EVERY call site in
      this file that needs a real `overrideDir`/`readVfs`/`studioDir` — `refreshTree()`, the mirror toggle's
      onChange, and (Plan 13, later wave, same file) the Remove AND Undo actions — calls THIS helper, never
      re-resolving `overrideDir`/`readVfs`/`studioDir` inline a second way. This closes W3: the previous draft
      guarded `refreshTree()`'s resolution against a `null` `overrideDir` but left the mirror toggle's
      onChange resolving inline with no guard, so a `null` `overrideDir` (unbound/offline project) would have
      fed `string | null` into `makeReadVfs`/`reconcileMirrorMode`'s `string`-typed parameters — a real `tsc
      --noEmit` failure, not just a runtime nuance. It also closes V6: `studioDir` itself was never guarded
      before feeding `readWorkspaceJson`/`removeDecorationRow`'s `string`-typed parameters — the SAME class
      of bug W3 already fixed for `overrideDir`, now closed for `studioDir` too.
    - **(ROUND-3-REVIEW R2/R8-M3)** `refreshTree()` calls `resolveOverridePair()`; when it returns `null`,
      renders a words-only "no live session / project not bound to a client" disabled state and does NOT call
      `refresh()` at all (ROUND-3-REVIEW R8/M3 — a normal, non-error D-07b state, never an unhandled crash);
      otherwise calls `refresh(pair.overrideDir, pair.meta.worldEditorBuildingTemplates)` (Plan 04's widened
      signature — ROUND 3, R3 seam). Because `resolveOverridePair()` re-reads `readWorkspaceJson` FRESH on
      every call, `refresh()`'s SECOND argument is ALWAYS the freshly-read `meta.worldEditorBuildingTemplates`
      — never a value captured once at mount and reused later (ROUND-3-REVIEW R2, HIGH, Cursor: a stale
      mount-time `meta` would starve `refresh()` of any `worldEditorBuildingTemplates` entries a LATER live
      persist writes, leaving `buildingTemplateVfsPath` empty on the tree even after a real capture populated
      the durable map — defeating R3 end-to-end). `refreshTree()` is called on mount, whenever
      `liveStatus.kind` changes, and by the live-session strip's "refresh" control (019-A's sketch element of
      that name) on click, giving the user a manual re-scan affordance alongside the automatic mount/
      attach-change triggers.
    - Live-session strip: a dot + `{exe} · pid {pid}` + a scene chip when attached, PLUS the "refresh" control
      above; an idle/disconnected variant otherwise — subscribing to `useLiveStore` directly (no prop drilling),
      matching StatusBar.tsx's pattern, using the TWO separate selectors (`status` and `clientLabel`,
      ROUND-3-REVIEW R8/M2 — never `liveStatus.clientLabel`, which does not type-check). **(D-07b)** when
      attached, if the attached client's exe path (`clientLabel`) does NOT match (as a case-insensitive prefix,
      same comparison `resolveRunningClientOverrideDir` already uses) the project's bound
      `WorkspaceBindingMeta.clientPath`, the strip shows a visible hint (e.g. a small warning glyph +
      "attached client differs from this project's bound client" text) — the attached client still wins for
      scanning purposes (per D-07b's own rule), this is purely an informational hint, never a blocking gate.
    - **(ROUND 3, R4/R6; ROUND 4, W1/W3)** Mirror-mode toggle row: a switch reflecting the current project's
      `mirrorToStockIlf` (default true when absent); toggling it calls `resolveOverridePair()` (the SAME
      shared, null-guarded helper `refreshTree()` uses — ROUND 4/W3, never a second inline resolution). If it
      returns `null` (no real override dir resolvable), the toggle's onChange is a no-op that surfaces the
      same words-only disabled reasoning `refreshTree()` renders (the toggle itself should already be visually
      disabled in this state, per the live-strip's own disabled rendering — this is defense-in-depth, not the
      primary gate). Otherwise it calls `reconcileMirrorMode(studioDir, pair.overrideDir, pair.readVfs,
      nextValue)` — this is the ONLY place `mirrorToStockIlf` gets persisted (`reconcileMirrorMode` itself
      calls `updateWorkspaceMeta` internally, per Plan 06); this panel's onChange handler does NOT ALSO call
      `updateWorkspaceMeta` directly (R6 — single owner, closing the two-writers-two-moments risk D-09 exists
      to prevent). The handler consumes `reconcileMirrorMode`'s RETURNED `{ failures }` (R8) and, when
      non-empty: **(ROUND-3-REVIEW R6, Plan-10 half; ROUND 4/W1 wording update — durable, atomic failure
      surfacing; ROUND 5, V7 — hedged wording for the disclosed double-fault residual)** pushes a
      `recordPersistResult`-style entry into `worldEditorStore.history` (`outcome: 'error'`). **When every
      entry in `failures` carries `diskState: 'unchanged'`** (the common case — Plan 06's ROUND 4/W1
      transactional redesign guarantees this whenever no rollback itself throws), the message reads, e.g.,
      "mirror mode change blocked — no buildings' mirrors were changed: {building ids and reasons from
      `failures`}" — never implying a partial per-building outcome. **When ANY entry carries `diskState:
      'uncertain'`** (Plan 06's disclosed `T-05.1-06d` rollback-double-fault residual — ROUND 5, V7), the
      message instead reads, e.g., "mirror mode change blocked — most buildings' mirrors were not changed, but
      {the 'uncertain' building ids} could not be verified after a rollback failure — check their mirror files
      manually: {reasons}" — never the unconditional "no buildings' mirrors were changed" claim for THAT
      subset, since this function cannot prove it in this one disclosed case. AND visually reverts the switch
      to its PREVIOUS `mirrorToStockIlf` value (since the flag was NOT persisted on any failure — the toggle's
      visual state must not silently disagree with what actually landed on disk/settings). A `log('warn',
      'log', ...)` line is ALSO still emitted (console-visible trace), but the history entry (built from the
      RETURN VALUE) is the PRIMARY, durable failure surface — `log()` is always secondary. On completion (clean
      OR blocked/rolled-back), calls `refreshTree()` (ROUND-3-REVIEW R2) so the rendered tree reflects whatever
      the reconcile actually left on disk; a hint line reads "per-template: all buildings with this layout show
      the edit" when ON; a `PER-TEMPLATE` badge is shown (matching 019-A).
    - D-08 (LOCKED, not discretionary): the scope control shows BOTH modes side by side — "Per-template (all
      instances of this layout)" as the ACTIVE, clickable option (the switch above controls this one), and
      "Per-instance (server repoint)" as a VISIBLE but DISABLED option (greyed/aria-disabled) carrying a
      "coming later" hint (e.g. a small caption or tooltip reading "server-side per-instance repoint — coming
      later"). D-08's own text is explicit that the disabled option is itself the explanation for why
      per-template mode changes every instance of a layout — omitting the control entirely (as an earlier
      draft of this plan incorrectly did, mislabeling it "Claude's discretion to omit") is NOT an option; only
      the per-instance server-side REPOINT MECHANISM is out of scope (Deferred Ideas), not its disabled UI
      affordance. NOTE for the maintainer: sketch 019-A's index.html does not currently render this disabled
      option either — that is a sketch-vs-decision gap this plan resolves in the DECISION's favor (D-08's
      literal text), per AGENTS.md's rule that decisions and sketches are both source-of-truth and any
      discovered conflict must be surfaced, not silently resolved either way.
    - "Edited buildings" collapsible section header shows a live count chip (`tree.length`).
    - Each building row: an icon, `displayLabel`, a subtext of `derivedTemplatePath`'s basename + `· node
      {buildingId}`, and a status badge derived from that building's decorations' sessionOverlay state (e.g.
      "N SAVED" when all clean, "N UNAPPLIED" when the disk tree has rows the CURRENT session hasn't
      confirmed — Claude's discretion on the exact badge-derivation rule; document it in the component).
    - Each decoration row nests under its building (indented), showing `objectTemplateName`'s basename +
      `{cellName} · row {rowIndex}` and a SAVED/ARMED/FAILED badge from sessionOverlay, keyed by
      `worldEditorRowId(buildingId, cellName, rowIndex)` — NEVER a bare rowIndex (Pitfall 3 guard, carried
      through from Plan 04).
    - **(ROUND 3, MED-7)** Selecting a row (building or decoration) calls `select(rowId)` where `rowId` is built
      via `worldEditorBuildingRowId(buildingId)` for a building row or `worldEditorRowId(buildingId, cellName,
      rowIndex)` for a decoration row (Plan 04's shared helpers — never an ad-hoc string). Rendering the detail
      card reads `parseWorldEditorRowId(selectedRowId)` to discriminate which kind is selected, rather than a
      local colon-split. The detail card below the tree shows: Decoration (objectTemplateName), Cell / row,
      Position (from `transform`'s translation columns, formatted to 2 decimals), Last persist (from the most
      recent matching history entry, or "not yet persisted this session" when none), Files (editedIlfPath +
      derivedTemplatePath basenames + "mirror" when a stock-path mirror exists for that building).
      **(ROUND-3-REVIEW R4, HIGH, Opus — supersedes the round-2 "translation columns only" framing)** When a
      matching history entry carries `beforeTransform`/`afterTransform`/`cellName`/`rowIndex` (Plan 04 ROUND 3's
      widened `PersistHistoryEntry` — each transform is the FULL 12-element o2p array), the "Last persist" field
      renders the FULL D-13 readout: BOTH transforms' complete 12 elements (formatted to 2 decimals each, e.g.
      a monospace `before: [r0 r1 r2 r3 / r4 r5 r6 r7 / r8 r9 r10 r11]` block and the matching `after:` block —
      NOT translation-columns-only), plus cell/row. Rendering the FULL 12 elements (not a derived/interpreted
      subset) avoids introducing any new rotation-matrix-semantics claim not already grounded in this phase's
      RESEARCH.md/PATTERNS.md, while guaranteeing a rotate-only edit (which changes 9 of the 12 elements and
      leaves translation identical) renders a VISIBLY DIFFERENT before/after readout — closing R4's finding that
      a translation-only readout makes a rotate-only edit look like "nothing changed", directly undermining SC5.
      This is the FULL readout distinguishing the detail card's surface from the in-game strip's compact
      position-only Δ (Plan 05), per D-13's locked two-surface distinction. When the matching entry lacks this
      data (an older/incomplete entry, or none exists yet), fall back to the outcome-word-only rendering, never
      throw.
      throw. **(ROUND 3, R11a)** Detail-card action buttons ("Go to", "Revert", "Edit in game") render as
      present per 019-A but are INERT/stubbed in this plan; clicking any of them shows an explicit, HONEST
      message — e.g. "Go to: not yet wired — coming in a later phase" — via the existing `log()`/toast surface,
      NEVER the sketch mock's literal action-implying copy ("Reverting row 3 to stock transform…", "Row pinned
      to overlay gizmo"), which would misleadingly imply the click actually did something (Go to/Edit in game
      require a live session and are not this plan's scope; Revert requires a data mutation this plan does not
      implement) — the ELEMENTS must be present and visually correct per SC2's sketch-parity requirement even if
      their full behavior lands later; do not silently omit them, and do not borrow the sketch's success-implying
      copy for a button that does nothing.
    - **(C12) Failure badge:** a `useEffect` reacting to `useWorldEditorStore((s) => s.hasFailureBadge)` calls
      `props.api?.setTitle('World' + (hasFailureBadge ? ' ●' : ''))` (the SAME modified-dot idiom
      `DatatableGridEditor.tsx`/`StfStringsEditor.tsx` already establish for this repo's dockview tabs) —
      wrapped in a try/catch (`api unavailable in some test envs`, matching `SidebarPanel.tsx`'s own
      precedent). A SEPARATE `useEffect` subscribes to `props.api.onDidActiveChange` (per-panel dockview
      event) and calls `useWorldEditorStore.getState().acknowledgeFailures()` whenever `props.api.isActive`
      becomes true (mirrors D-12's "never steal focus... the badge is passive until the user looks" contract
      — acknowledging on ACTIVATION, not on mount, so a background-mounted-but-inactive World tab does not
      silently clear a badge the user never saw). **(ROUND-3-REVIEW R13, Sonnet LOW)** `onDidActiveChange`
      fires on a CHANGE of `isActive`, not on mount — if the World tab is ALREADY the active panel at the
      moment this component mounts (e.g. it was the last-focused tab in a restored layout), `isActive` starts
      `true` and the change-event never fires, so this effect could otherwise miss acknowledging a badge that
      was already visible when the tab first rendered. The same effect ALSO checks `props.api.isActive` once,
      synchronously, on its initial run (not only inside the event callback) — if already active at mount time
      with `hasFailureBadge` true, it acknowledges immediately, matching what a real activation would have
      done; this is a plain read of the existing `isActive` field already declared above, not a new API.
  </behavior>
  <action>
    Create WorldPanel.tsx as a dockview panel component (`React.FunctionComponent<IDockviewPanelProps<any>>`,
    matching every other panel's shape, e.g. DeployPanel). Read `useWorldEditorStore` selectors for tree/
    selection/sessionOverlay/history/hasFailureBadge; read `useLiveStore((s) => s.status)` for `liveStatus` AND
    `useLiveStore((s) => s.clientLabel)` for `clientLabel` as TWO SEPARATE selectors (ROUND-3-REVIEW R8/M2 —
    `clientLabel` is NOT a field on the `'attached'` status-union member; reading `liveStatus.clientLabel` is a
    `tsc` error).
    **(ROUND 4, W3, MEDIUM, Cursor; ROUND 5, V6, MEDIUM, Cursor; ROUND 6, X1, HIGH — Cursor/Fable/Opus)**
    Define ONE shared `resolveOverridePair()` function in this component's body (per the behavior spec above)
    — a PLAIN function invoked from event handlers, never a hook — that FIRST reads `studioDir` via
    `useWorkspaceStore.getState().studioDir` (the imperative accessor; NEVER the `useWorkspaceStore((s) =>
    s.studioDir)` hook-selector form — a hook call from inside this function throws "Invalid hook call" the
    moment any write action in this panel is clicked; matches the SAME `getState()`-outside-render idiom this
    codebase already establishes — `DeployDialog.tsx:327` is a genuine click-handler callback, `:139`/`:185`
    are imperative `useEffect`-body reads (ROUND 7/Z18 citation fix, Fable #7) — and `useChannelReader.ts:272`)
    and returns
    `null` immediately when `!studioDir` — never calling
    `readWorkspaceJson` with an unguarded `string | null` (ROUND 5/V6). Only then does it read
    `readWorkspaceJson(studioDir)` FRESH — never a value captured in an outer closure/ref from a previous
    render or a previous call — resolve `overrideDir` via `resolveScanRoot(liveStatus.kind === 'attached' ?
    clientLabel ?? null : null, { cfgPath: meta.cfgPath, clientPath: meta.clientPath })` (ROUND 3, MED-11), and
    return `null` immediately (constructing NO `readVfs`) when `overrideDir === null` — otherwise return `{
    overrideDir, readVfs: makeReadVfs(overrideDir), meta, studioDir }` (ROUND 5/V6 — `studioDir` is now part of
    the resolved pair). **(ROUND-3-REVIEW R2, HIGH, Cursor)** Define `refreshTree()` calling
    `resolveOverridePair()`: if it returns `null` (unbound project, missing studioDir, or fully offline with
    no cfgPath — a normal D-07b state, per `resolveScanRoot`'s ROUND-3-REVIEW R9 null-return contract, Plan
    04), do NOT call `refresh()` at all — instead leave the tree at its last-known value and render the SAME
    words-only "no live session / project not bound to a client" disabled state the Scene accordion (Plan 11)
    already establishes as this panel's precedent for an unresolvable scan root; otherwise calls
    `refresh(pair.overrideDir, pair.meta.worldEditorBuildingTemplates)` (Plan 04's widened signature — ROUND 3
    seam). `refreshTree()` is called from: a mount `useEffect` (re-running when `liveStatus.kind` changes, so
    attach/detach re-resolves the scan root); the live-session strip's "refresh" control (019-A's sketch element
    of that name, previously unwired — clicking it calls `refreshTree()` directly); **(R9 review, BB11)**
    Plan 14's project-switch `[studioDir]` effect (same file, later wave — a project switch changes neither
    `liveStatus.kind` nor the panel's mount, so without this third caller the newly-opened project's tree
    would sit empty until a manual refresh, contradicting D-05's disk-scan truth; that effect runs
    post-render, strictly AFTER the stores' synchronous reset per Plan 04's PROJECT-SWITCH RESET ORDERING
    CONTRACT); and is the SAME function
    Plans 13/14 (later waves, same file) MUST call for their own post-remove/post-undo / history-triggered
    refreshes — Plans 13/14 do NOT read `readWorkspaceJson`/`useWorkspaceStore` or resolve
    `overrideDir`/`studioDir` a second, independent way; they call this component's `refreshTree()` (and, for
    Plan 13's Remove AND Undo specifically, this component's `resolveOverridePair()` too — see Plan 13's own
    ROUND 5 revision). NO call site anywhere in this file (mount, live-strip refresh button, mirror toggle,
    Plan 13's post-remove/post-undo, Plan 14's diff effect) may cache `meta`/`overrideDir`/`studioDir` across
    renders/calls for reuse in a LATER refresh — each `resolveOverridePair()`/`refreshTree()` invocation
    re-reads `readWorkspaceJson` fresh (ROUND-3-REVIEW R2's core fix).
    Implement the mirror toggle's onChange to call `resolveOverridePair()` (ROUND 4/W3 — the SAME shared,
    null-guarded helper, never a second inline resolution); if it returns `null`, no-op (defense-in-depth — the
    toggle should already be visually disabled in this state); otherwise call ONLY
    `reconcileMirrorMode(studioDir, pair.overrideDir, pair.readVfs, nextValue)` (ROUND 3, R4/R6 — do NOT also
    call `updateWorkspaceMeta` from this panel; `reconcileMirrorMode` is the sole persistence owner), consuming
    its returned `{ failures }` (ROUND 3, R8) to surface any failures via the existing `log()` service —
    wrapped in a try/catch as defense-in-depth (do not let a reconcile failure crash the panel), but the RETURN
    VALUE (and the durable `worldEditorStore.history` entry built from it) is the PRIMARY failure surface, not
    the catch and not the `log()` line (ROUND 4/W4 wording alignment — matches Plan 06's own phrasing); the
    history-entry message reads an ALL-OR-NOTHING outcome ("no buildings' mirrors were changed", ROUND 4/W1)
    when every `failures` entry has `diskState: 'unchanged'`, and a HEDGED outcome naming the specific
    `'uncertain'` building ids (ROUND 5/V7) when any entry does not — never per-building success/failure mixed
    wording either way, since Plan 06's transactional redesign makes a true partial-success mix impossible; then
    calls `refreshTree()` so the panel's tree/mirror-derived display reflects the
    just-reconciled disk state immediately. Render the disabled
    "Per-instance (server repoint)" option (a disabled radio/button/label, per D-08 — visible, non-interactive,
    with its "coming later" hint text) ALONGSIDE the active Per-template switch, per the behavior spec above.
    Render the D-07b mismatch hint in the live-session strip per the behavior spec. Render the tree/detail-card
    structure per the behavior spec, matching 019-A's element set enumerated in this plan's frontmatter
    `sketch_elements`, using `worldEditorBuildingRowId`/`parseWorldEditorRowId`/`worldEditorRowId` for all row
    selection (ROUND 3, MED-7). Render the detail card's inert stub buttons with the honest "not yet wired"
    copy per R11a above. Add the two `useEffect`s for the C12 failure-badge tab title + activation-triggered
    `acknowledgeFailures()`. Write WorldPanel.test.tsx (React Testing Library, matching this repo's existing
    panel-test idiom — check DeployPanel.test.tsx or similar for the render/store-seeding pattern) asserting:
    the tabstrip renders 'World'; a seeded worldEditorStore.tree of 2 buildings (one with 2 decorations, one
    with 1) renders both building rows + all 3 decoration rows nested correctly; selecting a decoration row
    shows the detail card with its Decoration/Cell·row/Position/Files fields; toggling the mirror switch calls
    `resolveOverridePair()`/`reconcileMirrorMode` with the flipped value AND a real `overrideDir`/`readVfs` pair
    (asserted via a spy on `makeReadVfs`/`resolveScanRoot` being called, not just on `reconcileMirrorMode`
    itself — ROUND 3/R4) and
    does NOT call `updateWorkspaceMeta` directly from this component (ROUND 3/R6, asserted via a spy showing
    zero direct calls); a mocked `reconcileMirrorMode` returning a non-empty `failures` array where EVERY
    entry carries `diskState: 'unchanged'` results in a
    `log('warn', ...)` call naming the failing building(s) (ROUND 3/R8) AND a `recordPersistResult` call into
    `worldEditorStore.history` with `outcome: 'error'` and a message stating NO buildings' mirrors were changed
    (ROUND 4/W1 — never per-building mixed wording); a SEPARATE test where at least one `failures` entry
    carries `diskState: 'uncertain'` asserts the recorded message is the HEDGED wording naming the uncertain
    building id(s) instead (ROUND 5/V7 — never the unconditional "no buildings' mirrors were changed" claim
    for that mixed case, ROUND 7/Z3 — the fourth restatement the ROUND 6/X2 sweep missed), asserted on the
    STORE (durable), not only on the transient log spy (ROUND-3-REVIEW R6); the switch's rendered/checked state
    after that failed reconcile still matches the PRE-toggle value, not the attempted `nextValue` (ROUND-3-REVIEW
    R6); a mocked `resolveScanRoot` returning `null` makes the mirror toggle's onChange a no-op — asserted via
    spies showing `makeReadVfs`/`reconcileMirrorMode` are NEVER called (ROUND 4/W3); clicking a detail-card stub button
    ("Go to"/"Revert"/"Edit in game") shows the honest "not yet wired" message, never the sketch's literal
    action-implying toast text (ROUND 3/R11a); a history entry carrying before/after transforms + cell/row
    renders the FULL 12-element D-13 readout in the detail card (ROUND-3-REVIEW R4 — all 12 before/after
    elements, not translation-only), and an entry lacking that data falls back to the outcome-word-only
    rendering without throwing (ROUND 3/R5); **(ROUND-3-REVIEW R4)** a ROTATE-ONLY seeded history entry (same
    translation components in before/after, different rotation components) renders a detail-card string that is
    VISIBLY DIFFERENT before vs after (assert the rendered before-text !== the rendered after-text) — the
    regression test for R4's "rotate-only edit looks like nothing changed" finding; a manual re-scan via the
    live-strip's "refresh" control calls `refreshTree()`/`refresh()` with a FRESHLY-read
    `worldEditorBuildingTemplates` map (spy on `readWorkspaceJson`, asserted called AGAIN at click time, not
    reused from mount — ROUND-3-REVIEW R2); a `resolveScanRoot` returning `null` (seeded unbound project) renders
    the words-only "no live session / project not bound" state and does NOT call `refresh()` (ROUND-3-REVIEW
    R8/M3); the "Per-instance (server repoint)" option is
    present in the DOM, carries a disabled attribute/aria-disabled, and its "coming later" hint text is
    queryable; with `hasFailureBadge: true` seeded, a mocked `props.api.setTitle` is called with a string
    containing '●' (C12); simulating `props.api.onDidActiveChange`'s callback firing with `isActive: true`
    calls `acknowledgeFailures()` (spy/mock) exactly once (C12); with a seeded `useLiveStore` attached status
    whose clientLabel/exe path differs from a seeded `WorkspaceBindingMeta.clientPath`, the strip renders the
    D-07b mismatch hint text.
  </action>
  <verify>
    <automated>npm -w @swg/renderer run test -- WorldPanel</automated>
  </verify>
  <acceptance_criteria>
    Component test asserts, by exact text/role query, the presence of: the live-session strip, the mirror
    toggle row with its hint text, the "Edited buildings" section with a count chip matching the seeded
    fixture's building count, every seeded building AND decoration row (by name), and the detail card's five
    kv fields on selection. This is the observed/missing diff SC2 requires, encoded as an automated test
    rather than left to manual inspection alone. D-08 assertion: the "Per-instance (server repoint)" option is
    present and disabled (query returns a disabled control, clicking it produces no reconcileMirrorMode call),
    with its "coming later" hint text present. R4/R6/R8 assertion: the mirror toggle's onChange resolves a real
    `overrideDir`/`readVfs` via `resolveOverridePair()` and calls ONLY `reconcileMirrorMode` (never a direct
    `updateWorkspaceMeta`), and consumes its `{ failures }` return value. ROUND 4/W3 assertion: a `null`
    `resolveOverridePair()` result makes the mirror toggle's onChange a no-op (zero `makeReadVfs`/
    `reconcileMirrorMode` calls). ROUND 4/W1 assertion (ROUND 7/Z3 — conditioned, the fourth restatement the
    ROUND 6/X2 sweep missed): a `failures` result where EVERY entry carries `diskState: 'unchanged'` produces a
    history-entry message stating no building's mirror was changed (an all-or-nothing outcome), never
    per-building mixed wording; a `failures` result containing ANY `diskState: 'uncertain'` entry instead
    produces the ROUND 5/V7 hedged wording naming the uncertain building id(s), never this unconditional
    claim. R5/ROUND-3-REVIEW R4 assertion: the detail card renders D-13's
    FULL 12-element before/after readout + cell/row when the matching history entry carries it, AND a
    rotate-only entry's rendered before-text differs visibly from its after-text (the R4 regression test).
    R11a assertion: stub button clicks produce only the honest "not yet wired" text, never the sketch's
    success-implying copy. C12 assertion: `setTitle` is called with the modified-dot suffix when
    `hasFailureBadge` is true, and `acknowledgeFailures()` fires on panel activation, not on mount alone. D-07b
    assertion: an attached/bound client-path mismatch renders the visible hint text, using the separate
    `clientLabel` selector (ROUND-3-REVIEW R8/M2 — never `liveStatus.clientLabel`). ROUND-3-REVIEW R2
    assertion: a manual "refresh" click and the mirror toggle's onChange EACH independently call
    `readWorkspaceJson` at invocation time (asserted via a spy call-count that increases per trigger, not just
    once at mount) — proving no call site reuses a stale mount-time `meta`. ROUND-3-REVIEW R8/M3 assertion: a
    `null` `resolveScanRoot` result renders the disabled "no live session" state and never calls `refresh()`.
    **ROUND 5/V6 assertion:** a seeded `useWorkspaceStore` with `studioDir: null` (via
    `useWorkspaceStore.setState(...)`, matching this store's normal test-seeding idiom) makes
    `resolveOverridePair()` return `null` WITHOUT ever calling `readWorkspaceJson` (spy shows zero calls) —
    proving the studioDir guard runs before, not after, the read it protects; the mirror toggle's onChange and
    (via Plan 13) Remove/Undo are all no-ops in this state. **ROUND 6/X1 regression:** exercising the mirror
    toggle's onChange (or, via Plan 13, the Remove click / Undo) through React Testing Library's normal
    `fireEvent`/`userEvent` click path — i.e. `resolveOverridePair()` running from a REAL event handler, not a
    direct function call made outside a render — completes without throwing "Invalid hook call", proving
    `resolveOverridePair()` reads `studioDir` via `useWorkspaceStore.getState()`, never the hook-selector form.
    **ROUND 5/V7 assertion:** a mocked `reconcileMirrorMode` returning a `failures`
    array with one `diskState: 'unchanged'` entry and one `diskState: 'uncertain'` entry produces a HEDGED
    history message naming the `'uncertain'` building id specifically, never the unconditional "no buildings'
    mirrors were changed" wording; an all-`'unchanged'` `failures` array still produces the existing
    unconditional wording (no regression).
  </acceptance_criteria>
  <done>The World tab renders 019-A's tree/mirror-toggle/live-strip/detail-card spine, matching the sketch
  element-for-element for this plan's scope, proven by a component test enumerating each element. D-12's
  attention badge is REAL (tab title + activation-triggered acknowledge, C12 closed). D-07b's binding-mismatch
  hint is present. ROUND 3: the mirror toggle actually resolves and uses a real overrideDir/readVfs (R4),
  has exactly one persistence owner (R6), and surfaces reconcileMirrorMode's real failure list (R8); the detail
  card renders D-13's FULL 12-element before/after-transform + cell/row readout, proven visibly-different on a
  rotate-only edit (ROUND-3-REVIEW R4, superseding the round-2 translation-only framing) and its inert stub
  buttons show honest, non-misleading copy (R11a); row selection uses one shared id contract (MED-7).
  ROUND 5: `resolveOverridePair()` guards `studioDir` before it ever reaches `readWorkspaceJson`, and returns
  it as part of the resolved pair so Plan 13's Remove/Undo never re-select it independently (V6); a
  rollback-double-fault's `diskState: 'uncertain'` produces a hedged history message instead of a
  provably-false unconditional claim (V7). ROUND 6: `resolveOverridePair()` reads `studioDir` via
  `useWorkspaceStore.getState()`, never a hook-selector call from inside this event-invoked plain function,
  closing the "Invalid hook call" defect three independent reviewers found (X1). Every
  `refresh()` call site (mount, manual refresh, mirror toggle) re-reads `worldEditorBuildingTemplates` fresh via
  a shared `refreshTree()` helper built on top of ONE shared, null-safe `resolveOverridePair()` helper (ROUND-
  3-REVIEW R2, ROUND 4/W3) — never a stale mount-time value and never an unguarded `string | null` flowing into
  a `string`-typed dependency; a failed reconcile's history message correctly reflects Plan 06's now-atomic,
  all-or-nothing contract (ROUND 4/W1); `clientLabel` is
  read via its own selector, never `liveStatus.clientLabel` (ROUND-3-REVIEW R8/M2); an unresolvable scan root
  renders a words-only disabled state instead of crashing or silently no-op-ing (ROUND-3-REVIEW R8/M3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI → decorationPersistOrchestrator (reconcileMirrorMode) | The mirror toggle's onChange triggers a disk-writing reconcile pass across potentially many buildings, from a single UI click. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-10a | Denial of Service | reconcileMirrorMode failure crashing the panel | mitigate | The onChange handler wraps the call in try/catch and routes any failure through the existing `log()` service (behavior spec) — a blocked or rolled-back reconcile never crashes the World tab; Plan 06's reconcileMirrorMode itself already fails closed and rolls back per-building (ROUND 4/W1). |
| T-05.1-10b | Tampering | none material | accept | No new user-supplied path/string construction is introduced by this plan's rendering/selection logic — all writes route through Plan 06's already-hardened reconcileMirrorMode, resolved via ONE shared `resolveOverridePair()` guard (ROUND 4/W3). |
| T-05.1-10c | Denial of Service | `props.api.setTitle`/`onDidActiveChange` unavailable in a test/degraded environment | mitigate | Both calls are wrapped in try/catch (matching `SidebarPanel.tsx`'s own precedent) so a missing dockview API surface never crashes the panel render. |
</threat_model>

<verification>
`npm -w @swg/renderer run test -- WorkspaceShell workspace-config WorldPanel` green; `tsc --noEmit` clean for
packages/renderer.
</verification>

<success_criteria>
The World tab exists, is reachable via the Inspect|Deploy|World tabstrip, and renders the 019-A tree/mirror-
toggle/live-strip/detail-card spine element-for-element against real scanned data — closing the largest chunk
of SC2/SC3. D-12's attention badge has a real UI renderer (tab title + activation-triggered acknowledge, C12).
D-07b's binding-mismatch hint is visible in the live-session strip. ROUND-3-REVIEW: reconcileMirrorMode
calls are always re-fed a fresh worldEditorBuildingTemplates read via one shared refreshTree() helper (R2);
the detail card's D-13 readout survives a rotate-only edit visibly (R4); clientLabel never crashes tsc (R8/M2);
an unresolvable scan root degrades to a words-only disabled state (R8/M3). ROUND 4: refreshTree() and the
mirror toggle share ONE null-safe resolveOverridePair() helper, so a null scan root can never reach a
string-typed dependency unguarded (W3); a failed/blocked reconcile's history message correctly states an
all-or-nothing (never partial) outcome, matching Plan 06's transactional redesign (W1). ROUND 5:
resolveOverridePair() guards studioDir the SAME way it already guards overrideDir, and returns the confirmed
value so consumers never re-select it independently (V6); a reconcile failure's diskState:'uncertain' entries
(Plan 06's disclosed rollback-double-fault residual) produce a hedged history message rather than the
unconditional "no buildings changed" claim (V7). ROUND 6: resolveOverridePair() reads studioDir via
useWorkspaceStore.getState() rather than a hook selector, since it is a plain function invoked from event
handlers — the previous hook-selector form would have thrown "Invalid hook call" on the first write action any
user takes in this panel (X1); the history-entry wording for a failed reconcile now claims the all-or-nothing
outcome only when every failures entry carries diskState:'unchanged', deferring the hedged wording to the
disclosed 'uncertain' case rather than conflating the two (X2).
ROUND 7: the test-prose restating the ROUND 4/W1 assertion is conditioned to match the must_haves' already-
correct wording — a fourth restatement the ROUND 6/X2 sweep had missed (Z3); a citation calling
`DeployDialog.tsx:139/:185` "handlers" is corrected to "imperative useEffect-body reads", preserving the
load-bearing point (Z18).
</success_criteria>

<output>
Create `.planning/phases/05.1-live-world-editor-productization/05.1-10-SUMMARY.md` when done
</output>
