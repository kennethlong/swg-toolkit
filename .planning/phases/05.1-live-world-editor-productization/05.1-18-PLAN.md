---
phase: 05.1-live-world-editor-productization
plan: 18
type: execute
wave: 3
depends_on: ["05.1-09", "05.1-16"]
files_modified:
  - packages/live-inject/agent/rva_table.cpp
  - packages/live-inject/agent/overlay.cpp
autonomous: false
requirements: [pivot-driven]
inserted: 2026-08-02
inserted_reason: "Plan 11 builds D-07 teleport bookmarks on a teleport that lands the player at the right coordinates but leaves them parented to the WRONG CELL, so interiors render see-through. Bookmarking a spot inside a building you are decorating is the primary use case and is exactly the broken one."
revised: "2026-08-02 (rewritten after v27 shipped + live telemetry). Original draft was built on two wrong premises: that setParentCell was unavailable (it shipped in v27 the same day), and that the stale-player defect was the load-bearing problem (it has never reproduced under instrumentation). Weighting inverted accordingly."
execution_order_note: "Numbered 18 so the plan index parses a plain integer, but it is WAVE 3 and MUST run BEFORE 05.1-11."
user_setup:
  - service: local-swg-client
    why: "Cell parentage and portal rendering are only observable in a running client"
    dashboard_config:
      - task: "Rebuild the agent DLL and re-inject before verifying"
        location: "cmake --build packages/live-inject/agent/build-agent --config Release (node on PATH)"
must_haves:
  truths:
    - "Teleporting to a point inside a POB leaves the player parented to that POB's cell, and the interior renders solid from inside"
    - "Reparenting is only attempted where the destination cell can be RESOLVED; where it cannot, the behavior and the reason are stated honestly rather than guessed at"
    - "Every setParentCell call happens on the game thread -- the row is game-thread-only, and the agent's D-03 poll thread must never reach it"
    - "The player's parent cell is observable in the log on every teleport, so cell defects are measured rather than eyeballed"
    - "No claim is made that the stale-player-after-scene-change defect is fixed -- it has never been reproduced, and this plan only instruments it"
  artifacts:
    - path: "packages/live-inject/agent/rva_table.cpp"
      provides: "object::getParentCell, object::setParentCell, cellProperty::getWorldCellProperty bindings (v27)"
      contains: "setParentCell"
    - path: "packages/live-inject/agent/overlay.cpp"
      provides: "cell-aware teleport + parent-cell telemetry + scene-change write instrumentation"
---

# Plan 05.1-18 — Cell-aware teleport

<objective>
Make a teleport put the player in the right CELL, not just at the right coordinates. Today the write
lands perfectly and the player still renders the interior wrong, because nothing reparents them.

The provider shipped the missing capability as **v27** the same day the defect was characterised, so
this plan consumes it. It also adds cheap instrumentation for a second, unproven defect rather than
building machinery against something never observed.
</objective>

<evidence>
## LOCKED — measured live 2026-08-02, DebugView capture. Do NOT re-derive or contradict.

1. **Our write is CORRECT. This is not a transform bug.** Nine teleport clicks across two sessions, every
   one:
   ```
   overlay: teleport click    — player=2AF9B9F0 target=(3428.0, 8.0, -4788.0)
   overlay: teleport read-back — now=(3428.0, 8.0, -4788.0) wanted=(3428.0, 8.0, -4788.0)
   ```
   `now == wanted`, exactly, every time. Do not "fix" the write path.
2. **The player physically moves to the target and stays in the WRONG CELL.** Portal telemetry across a
   teleport from inside the cantina: position went `3437.24 → 3430.48 → 3428.32` (target X `3428.0`)
   while the probe continued to report **`cell=cantina`**. The player is at the doorway coordinates
   with the interior cell's portal set still active around them — which is what the maintainer
   described as *"moved forward to the wall in front of me, not to the exterior location."*
3. **28 `DOORHIT-WAKE` portal sweeps fire immediately after EACH teleport**, alternating
   `private_room ↔ cantina`, symmetric across both clicks. That is the `positionChanged` line-sweep
   (`CellProperty.cpp:150-176`) evaluating a teleport-length segment against the current cell's portal
   list. Normal walking produces occasional single `DOOR` lines; 28 in a burst is teleport-induced.
4. **After a `loadScene`, portal traversal stops entirely**: `[PortalCullProbe]` emitted **1095 lines
   before** the editor-scene load and **exactly 0 after**, across 22 seconds and 7 teleports until
   shutdown. The probe is `REPORT_LOG(true, …)` inside `RenderWorld`'s traversal — unconditional — so
   silence means the traversal is not executing. This is consistent with world-cell parentage after a
   scene load, and it is why neither interior nor exterior of the cantina drew.
5. **v27 SHIPPED the fix** (provider commit `b9363b5b0`): `object::setParentCell`
   (`engine_hookpoints.inc:447`) and `cellProperty::getWorldCellProperty` (`:448`).
   `object::getParentCell` was already advertised (`:178`). **We bind none of the three.**
6. **Provider's answers to the two questions we refused to guess at**, verified in their source:
   - **Transform: write `o2w`, do NOT convert.** `attachToObject_w` (`Object.cpp:1956`) derives o2p from
     the current world frame; `setTransform_o2w` (`:1450`) is itself cell-aware. Independently derived
     twice by the review crew — **this one is settled, three ways.**
   - **Ordering: write the transform FIRST, then reparent.** Their rationale — that reparent-first
     makes `cellChanged` observe a stale position — is **NOT the real reason** (see axiom 6b), but the
     conclusion is right.

6b. **ORDERING IS SETTLED, and the deciding fact is one everybody initially missed** (crew review,
    independent derivation): **`setParentCell` does NOT run the portal line-sweep at all.** It fires
    only `cellChanged(false)` (`Object.cpp:1408`), and `CellPropertyNamespace::Notification` overrides
    only `getPriority`/`positionChanged`/`positionAndRotationChanged` (`CellProperty.cpp:42-66`) — not
    `cellChanged`. **The sweep is a side effect of the TRANSFORM WRITE, and only the transform write.**

    So the real question is whether the sweep runs before or after our authoritative cell assignment:

    | Order | Sweep runs | If the sweep picks a cell |
    | --- | --- | --- |
    | **A — write, then reparent** | first, against the OLD cell | **unconditionally overwritten** by our trailing `setParentCell(C)`. Final cell is C in every case. Self-correcting. |
    | B — reparent, then write | **last** | **final and uncorrected** — a permanent, silent mis-parent, which is exactly the value `getContainingBuildingId` placement routing reads |

    Order A trades a higher-probability *transient* event for Order B's lower-probability *permanent*
    one. **On worst case Order A strictly dominates.**

    Two corollaries that retire earlier arguments — do not resurrect them:
    - The provider's stale-`cellChanged` rationale is **cosmetic**. `Object::cellChanged` is
      non-virtual (`Object.h:366`), so its observer set is closed and enumerable; all five re-derive
      from current state and **none caches a position**. The one that reads position at all
      (`Footprint::cellChanged`) is a no-op on the client for an interior destination.
    - The "six engine call sites reparent first" argument is **void**. Those sites bracket the
      **transform write only** — never `setParentCell` — which is itself direct confirmation that the
      sweep belongs to the write. Their cell-first choice is *conditional on suppression we do not
      have*, so it is not evidence for unsuppressed Order B.
7. **`object::setParentCell` is GAME-THREAD-ONLY** (`engine_advertise.cpp:1077`). The agent's D-03 poll
   thread (`agent_main.cpp:145`, a separate remote thread) must never reach it.
8. **A null cell FATALs the client.** `Object::setParentCell` `NOT_NULL`s its argument
   (`Object.cpp:1389`). The provider's shim returns `0` first, but the caller must still pass
   `getWorldCellProperty()` to mean "outside" — **never null**.

## LOCKED — the stale-player defect has NEVER been reproduced

9. Reported twice at the 05.1-16 checkpoint. **Two deliberate, instrumented reproduction attempts have
   now failed.** In the second, the player pointer *did* change across a genuine scene recreate
   (`2B018640 → 36C4EDD0`) — the precondition the hypothesised mechanism requires — and all seven
   subsequent writes landed exactly. The first click came 4.68 s after `loadScene`, past any plausible
   use-after-free window.
10. **CORRECTED — the defect is a transient NULL, not a use-after-free** (crew review, verified
    independently against source this session). An earlier draft cited `GameNetwork::startScene()`
    (`GameNetwork.cpp:480-505`), which does `delete gs` before the replacement is built. **That is the
    server-driven multiplayer zone path and the editor client does not execute it.** What our
    "Load editor scene" actually runs is `game::loadScene` → `Game::setScene(..., immediately=true)`,
    which skips the cutscene defer entirely, and:
    - `Game::cleanupScene()` (`Game.cpp:989-995`) is `ms_scene->quit(); _setScene(0);` — **quit, not
      delete**.
    - `Game::_setScene(Scene*)` (`Game.cpp:1865+`) assigns `ms_scene = newScene` and **never deletes the
      old scene** — it leaks it.

    So on our path `Game::getPlayer()` (`Game.cpp:2214-2217`, a `dynamic_cast` on `ms_scene`) goes
    **valid → null → valid-new**. It never dereferences freed memory. The window is created by
    **our own** two-frame `cleanupScene` → `loadScene` sequence (05.1-16), and a click landing in it
    gets a null player — already logged today at `overlay.cpp:1409`, and reported ZERO times in both
    repro attempts, consistent with narrowly missing a one-frame window rather than with the theory
    being wrong.

    **Consequence:** the fix, if confirmed, is trivial — refuse or retry the click during the gap
    instead of silently no-opping. Do NOT build pointer-staleness machinery for a null-return race.
11. **Consequence for this plan:** the earlier draft's N-tick stability gate is CUT. It gated the wrong
    trigger (`wsUnloadSnapshot`+`wsLoad` cannot reach `Game::ms_scene`), its tick counter was
    framerate-coupled against a window whose length is content-dependent, and it would have been built
    against a defect never once observed. Instrument, do not gate.

## BANNED framings — investigated and rejected; do not reintroduce

- ~~"`setParentCell` is unavailable / provider-blocked."~~ Shipped in v27 (axiom 5).
- ~~"Convert the target to cell-relative o2p before writing."~~ Falsified three ways (axiom 6).
- ~~"Use `object::isActive` to test whether the player pointer is live."~~ Calling it on freed memory is
  itself undefined behavior. It distinguishes "allocated but not in world" from "in world"; it is not a
  liveness test.
- ~~"Refuse player writes until the pointer is stable for N ticks."~~ Cut (axiom 11).

## NOT AVAILABLE — do not design around these; they are requested, not shipped

The engine's own teleport (`GroundScene.cpp:1492-1497`, verified) is
`setParentCell` → `setPortalTransitionsEnabled(false)` → write → `(true)` → `CollisionWorld::objectWarped`.
**Neither `CellProperty::setPortalTransitionsEnabled` nor `CollisionWorld::objectWarped` is advertised**,
and no child-object guard (`getAttachedTo` / `isChildObject`) is either — which matters because
`setParentCell` on a MOUNTED player silently corrupts its pose in Release builds (the `DEBUG_FATAL` at
`Object.cpp:1396` is `#if 0`'d out).

**Do NOT reach for `getParentCell` as a cheap mount proxy — it is actively the wrong tool, not merely
unavailable.** Mount attachment and cell attachment share the same `m_attachedToObject` field,
distinguished only by the `m_childObject` flag, and `getParentCell()` (`Object.cpp:1372-1383`)
deliberately walks *through* mount attachment to resolve the mount's ambient cell. A mounted and an
unmounted player therefore report the same parent cell **by design**. Downward raycasts and height
heuristics were also considered and rejected as unreliable for gating a silently-corrupting call.

All rows are asked for in the follow-up provider note. Until they land, this plan uses the derived
ordering (axiom 6b) and accepts the sweep churn.
</evidence>

<tasks>

<task type="auto">
  <name>Task 1: Bind the three cell rows and make parent cell observable</name>
  <files>packages/live-inject/agent/rva_table.cpp, packages/live-inject/agent/overlay.cpp</files>
  <action>
    Bind, following the existing row patterns:
    - `object::getParentCell` — non-virtual `CellProperty* Object::getParentCell() const`; typedef as an
      opaque `void*(__thiscall*)(void*)`, the `pGetNetworkId` idiom (`overlay.cpp:89`).
    - `object::setParentCell` — v27 shim, `int(__cdecl*)(void* object, void* cellProperty)`, 1 ok / 0 refused.
    - `cellProperty::getWorldCellProperty` — `void*(__cdecl*)()`.

    **Never dereference a `CellProperty*` agent-side.** It is an opaque handle passed straight back to
    the engine.

    Then add the measurement that has been missing: log the player's parent cell pointer alongside the
    existing teleport click trace, and whether it equals `getWorldCellProperty()`. This is the
    discriminator for axiom 4 — it turns "the building didn't draw" into a recorded fact about
    parentage.
  </action>
  <verify>Agent DLL compiles clean; all three rows resolve at runtime on the advertised client.</verify>
  <done>Cell state is bindable and observable.</done>
</task>

<task type="auto">
  <name>Task 2: Cell-aware teleport</name>
  <files>packages/live-inject/agent/overlay.cpp</files>
  <action>
    **Use the THREE-call sequence** (axiom 6b, crew-derived):

    ```
    setTransform_o2w(P);      // sweep runs here, against the OLD cell — any mis-parent is transient
    setParentCell(C);         // no sweep; unconditionally establishes C as the final cell
    setTransform_o2w(P);      // inert sweep (start == end), but runs the FULL notification list
    ```

    The third call is the part worth not cutting. A sweep HIT on the first call returns `false`, which
    aborts the rest of the ascending-priority notification list — skipping `TangibleNotification`
    (sphere tree) and `CollisionNotification` (spatial-DB reinsert), **neither of which `cellChanged`
    repairs**. Re-writing the same transform once the cell is final re-runs that list with the object
    at its correct cell and position. The re-run's sweep is inert because `start == end`
    (`CellProperty.cpp:131-132`), or at worst degenerates to a ~1e-7 segment that cannot penetrate a
    portal quad. This recovers most of what `CollisionWorld::objectWarped` would have given us and we
    do not have.

    Do not convert to o2p (axiom 6).

    **Resolving the destination cell.** There is no advertised "which cell contains world point P"
    accessor (requested, not shipped). So resolve only where we honestly can:
    - If the destination is derived from a PICK (`collideScreenRayObject` → `getParentCell` on the hit
      object), use that cell. This is the accurate path; prefer it wherever a pick exists.
    - **Coordinate-only teleport — CONDITIONAL, not unconditional.** Only pass `getWorldCellProperty()`
      when the player was **already in the world cell before the write**. If the player started INSIDE
      a cell, **skip the explicit reparent entirely** and let the engine's own portal auto-walk result
      stand.

      This distinction is load-bearing (crew review): an unconditional
      `setParentCell(getWorldCellProperty())` would override a cell the engine's sweep may have just
      resolved CORRECTLY — forcibly ejecting a player to the exterior when they teleport between two
      portal-adjacent rooms of the same building. That is a central editor workflow, and doing nothing
      would be strictly better than getting it wrong. "Skip the reparent" and "guess a cell from the
      player's current one" are different actions; only the second is banned.

    **Never pass null** (axiom 8) — `getWorldCellProperty()` is how "outside" is expressed.

    **Log when the auto-walk and our pick disagree.** Because the sweep can call `setParentCell` during
    our first write, our explicit call is not acting on virgin state. When the cell we are about to set
    differs from `getParentCell()` immediately after the write, log both — that is the doorway
    ambiguity made visible.

    **Game thread only** (axiom 7): both teleport call sites (`:1354-1410` local button, `:1024-1044`
    HOST_CMD) already run on the game thread. Do not add any reparent path reachable from the D-03 poll
    thread.

    Keep the existing read-back instrumentation (`a166f24`) intact and add a post-reparent parent-cell
    log line, so a failure is attributable to the write or the reparent.

    Update the honest UI note (`:1403`): it currently says interiors render wrong because the cell is
    not reparented. Once reparenting works for the pick path, the note must reflect what is actually
    still limited — the coordinate-only case — not keep claiming a blanket failure.
  </action>
  <verify>Agent DLL compiles clean. No reparent call is reachable from `agent_main.cpp`'s poll loop.</verify>
  <done>A pick-derived teleport into a POB parents the player to the right cell.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument the stale-player defect — do not gate it</name>
  <files>packages/live-inject/agent/overlay.cpp</files>
  <action>
    Per axioms 10 and 11, ship observation rather than a gate — and hunt the RIGHT signature. The
    corrected mechanism is a transient **null** `getPlayer()` inside our own `cleanupScene` →
    `loadScene` window, not a stale non-null pointer.

    **`getSceneId()` alone is NOT sufficient** and would produce a misleading correlation: it is keyed
    on the terrain appearance template name (`Game.cpp:1886+`), so reloading the SAME terrain leaves it
    unchanged while `ms_scene` is destroyed and rebuilt. A "pointer differed but scene id did not" case
    would be misread as *not* a scene boundary.

    Emit ONE structured record per player write containing:
    - a **scene-epoch counter** we own — a static bumped at our own `gameCleanupScene` / `gameLoadScene`
      call sites (`overlay.cpp:910-923`), which is the only signal that increments exactly once per
      scene swap;
    - the raw `getPlayer()` return **including when it is `0x0`** — log the null case as a field in the
      same record, not as a separate line, so post-hoc correlation is mechanical rather than manual;
    - the current `getSceneId()` when the player is non-null;
    - a frame/tick marker so a click can be placed inside or outside the swap window.

    That combination answers the actual question — *did this click land in the epoch-transition gap?* —
    from one capture, with no rebuild and no inference across loosely-coupled log lines.

    Explicitly DO NOT: add a tick counter, refuse writes, or claim in any surface that the defect is
    fixed. If a capture shows a click landing in the gap with a null player, the fix is trivial (refuse
    or retry, do not silently no-op) and belongs in a follow-up, not here.
  </action>
  <verify>A teleport log line carries pointer + scene id. No write is refused by this task.</verify>
  <done>The next occurrence will be diagnosable from the log alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live verification</name>
  <files>packages/live-inject/agent/overlay.cpp</files>
  <action>
    Pause for human verification. Rebuild the agent DLL, mtime-verify it, re-inject. Start DebugView
    FIRST and confirm capture with a probe string — a stale instance silently swallows output.
  </action>
  <how-to-verify>
    1. **The headline — the doorway test, both directions.** Stand inside the cantina. Pick a point
       INSIDE and teleport to it: the interior must render SOLID from inside, no see-through walls.
       Then pick a point OUTSIDE the door and teleport: the exterior must render correctly and the
       interior must not follow you out.
    2. **The measurement.** Confirm the new parent-cell log line shows a real cell (not the world cell)
       when you are inside, and the world cell when outside. This is the axiom-4 discriminator.
    3. **Baseline comparison.** Before this plan, teleporting inside left you at the target coordinates
       with `cell=cantina` and 28 door sweeps. Confirm the sweeps still fire (expected — suppression
       is not available) but the RENDERING is now correct.
    4. **Coordinate-only teleport.** The default target box teleports to the exterior front door. It
       must still work, and the UI note must accurately describe the interior-coordinate limitation.
    5. **Editor scene.** Load the editor scene, then teleport. Report whether the cantina now draws —
       axiom 4 predicts this improves, but it is NOT proven; if the portal probe stays silent, say so.
    6. **Do NOT teleport while mounted or in a vehicle.** No child-object guard is available and the
       engine's assertion is compiled out; a mounted reparent silently corrupts the pose. Note this in
       SUMMARY.md as an accepted, disclosed limitation pending the requested row.
    7. **Regression.** Reload, Editor scene, Insert-at-player, arm/gizmo on a decoration.
  </how-to-verify>
  <resume-signal>Type "approved" once 1-7 hold, or describe what failed. Pull the DebugView log on any
  failure — the click / read-back / parent-cell triple attributes it to the write, the reparent, or
  neither.</resume-signal>
</task>

</tasks>

<verification>
Agent DLL rebuilt by the executor and mtime-verified newer than both edited sources. All three v27-era
rows resolve. No reparent path reachable from the poll thread. Root aggregate suite (`npx vitest run`
from the repo root) stays green; two known parallel-load flakes noted in 05.1-17.
Blocking checkpoint's 7 steps pass.
</verification>

<notes>
HANDOFF TO PLAN 11 — what bookmarks must carry. **A `CellProperty*` is a single-frame value, not a
handle.** It dies with its building on any scene reload, and the address can be recycled — the same ABA
class as [[raw-pointer-identity-aba-across-process-boundary]]. Bookmarks must therefore persist the
**building `NetworkId` + cell index/name** and re-resolve the pointer at use time, never store the
pointer. Plan 11 should not describe bookmarks as cell-aware for coordinate-only destinations until the
resolver row lands.

STILL OPEN AFTER THIS PLAN, deliberately:
- **Four provider rows requested** — `setPortalTransitionsEnabled`, `CollisionWorld::objectWarped`, a
  child-object guard, and `ClientWorld::findClosestCellObjectFromWorldPosition`. The last one is what
  makes coordinate-only interior teleports resolvable, and it is also what placement routing needs.
- **The ordering question is CLOSED** (axiom 6b) — transform-first, because `setParentCell` does not
  sweep and therefore whichever call runs last determines the final cell. This supersedes an earlier
  draft that declined to resolve it; the trace was available and the punt was a mistake. The provider
  reaches the same conclusion from a rationale that does not survive tracing (their stale-`cellChanged`
  concern is cosmetic), so the ordering is right but the reasoning behind the contract comment is not —
  reported to them as data, not as a correction demand.
- **Collision position cache is stale under BOTH orderings** and this cannot be fixed by ordering.
  `CollisionProperty::m_lastTransform_w/_p` is written only via `storePosition()`, reached only from
  `objectWarped()` — the row we lack. `CollisionWorld::moveObject` compares against it. This is the
  strongest argument for the `CollisionWorld::objectWarped` request; the three-call sequence in Task 2
  recovers some but not all of it.
- **The stale-player defect** stays open as an instrumented watch item, not a fix.
- **SWGEmu legacy parity** for `setParentCell` (Utinni has the RVA) belongs to Milestone 9.
</notes>
