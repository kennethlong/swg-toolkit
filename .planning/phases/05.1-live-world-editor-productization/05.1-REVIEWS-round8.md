---
phase: 5.1
round: 8
reviewers: [codex, cursor, sonnet, opus, fable]
reviewed_at: 2026-08-01T18:13:23Z
plans_reviewed: [05.1-01-PLAN.md, 05.1-02-PLAN.md, 05.1-03-PLAN.md, 05.1-04-PLAN.md, 05.1-05-PLAN.md, 05.1-06-PLAN.md, 05.1-07-PLAN.md, 05.1-08-PLAN.md, 05.1-09-PLAN.md, 05.1-10-PLAN.md, 05.1-11-PLAN.md, 05.1-12-PLAN.md, 05.1-13-PLAN.md, 05.1-14-PLAN.md, 05.1-15-PLAN.md]
reviewed_commit: ba09958
---

# Cross-AI Plan Review — Phase 05.1 — Round 8 (post-Z1–Z18 convergence check)

Reviewed commit `ba09958` (round-7 replan: Z1–Z18 + checker-loop ROUND 8/9 revisions to Plan 13's
sticky-error toast contract). Five reviewers, non-overlapping angles: Codex (seam-propagation
trace), Cursor (line-level citation & wiring verification), Sonnet (lateral new-seam hunt), Opus
(locked-decision fidelity & spec math), Fable (fact-check of NEW claims in commit ba09958).
Task files: `.planning/research/CONSULT-05.1-R8-*.md`, raw outputs `…-*.out`.

**Verdicts:** Codex NOT CONVERGED (2 HIGH) · Cursor NOT CONVERGED (1 HIGH, 1 MED, 1 LOW) ·
Sonnet NOT CONVERGED (2 HIGH) · Opus NOT CONVERGED (3 HIGH, 3 MED, 2 LOW) · Fable NOT CONVERGED
(1 FALSE claim + 2 residuals; 35/37 claim groups verified).

**Bottom line:** NOT CONVERGED — **7 distinct HIGHs** after cross-reviewer dedup (work-list
AA1–AA17 below). The dominant pattern, now on its **4th consecutive round**, is half-propagation:
each fix lands at the reviewer-cited site while sibling restatements of the superseded absolute
survive (AA3, AA6, AA7, AA9, AA11, AA12 are all this class). Opus additionally found that the
checker-loop ROUND 8/9 revisions **introduced two new HIGH seams of their own** (AA1, AA2) — and
its cross-cutting note pinpoints the process cause: the sweeps grep *mechanism* keywords
("8-second", "re-arm", `TOAST_MS`) instead of *guarantee-restatement* sites (must_have
enumerations, gap ledgers, threat-model mitigations, success_criteria). Sonnet's two HIGHs (AA4,
AA5) are genuinely new seams no prior round touched. The zero-fabrication streak breaks on a
single one-word slip (AA15: "219-line" for the 218-line `DeleteUndoToast.tsx`).

**Verified sound this round (do not re-litigate):** Opus traced 11 guarantees end-to-end and found
the core ROUND 8 fold-deferral algebra producible on both exit routes (false-success impossibility,
cross-entry-erasure impossibility, both thaw routes, repeat-failure loop, effect-loop termination,
`wasStickyRef` idempotence, timer arithmetic, full wave/depends_on ordering, Plan 10's Z3-cited
sites, Plan 06's "EXCEPT" pair). Cursor: 27/29 citations byte-accurate; all 10 cross-plan symbols
correctly labeled forward outputs with valid wave ordering. Codex: Z1/Z2/Z4 propagated fully (site
lists in its section). Fable: every headline round-7 correction verified against real bytes.

## Work-list AA1–AA17 (deduped, severity-ordered)

| ID | Sev | Source | Plan(s) | Defect |
|----|-----|--------|---------|--------|
| AA1 | HIGH | Opus Y1 | 13 | The ROUND 9 error-swap is unreachable on its only reachable branch: the swap is specified via `DeleteUndoToast.tsx:102-110`'s imperative `setToast` idiom and must call `clearTimer()`, so it lives inside the pending-diff effect — but that effect's FIRST statement is the ROUND 8 sticky guard, which is already true on the first tick after `setUndoError` (the Z1 reorder means the entry never left `pending`). Early return fires before the swap ever runs → the toast keeps its plain "removed — Undo" copy, never shows the error, and (per ROUND 9) never auto-dismisses. Strictly worse than the X4 defect this chain exists to close; must_have 13:31(c) and success_criteria are unproducible. Fix: specify WHERE the swap lives such that it runs before/despite the guard (e.g. dedicated `undoErrors`-subscribing effect that performs the swap + `clearTimer()` and is ordered before the diff effect, with the guard keying off the already-swapped display), and add a regression test asserting the error text actually renders on first failure. |
| AA2 | HIGH | Opus Y2 | 13, 15 | Panel unmount/remount (tab navigation) is a FOURTH exit route the ROUND 9 enumeration (timer/dismiss/retry) never covers — and it's created by Plan 13's own deliberate panel-scoped mount (13:826-830). Component-local `toast`/`prevRef`/`wasStickyRef` die on unmount while `pending`/`undoErrors` survive; on remount the diff (`prev=[]` vs `pending=[A,C]`) shows C via `added[added.length-1]` — A's failure is "silently superseded by an unrelated Remove" (the literal wording 13:31(c) forbids) and A's `undoErrors` slot is orphaned with no UI surface, verbatim the condition ROUND 9 classified as a blocker on the timer route. Plan 15 ledger (g) covers only the plain-removal case and its "may or may not re-show" wording is false for the sticky case (remount deterministically shows C). Fix: either persist/restore the sticky display across remount (e.g. derive initial display from `undoErrors` on mount) or disclose the unmount route at 13:31 + ledger (g) with the sticky-case behavior stated accurately. |
| AA3 | HIGH | Opus Y3 | 13, 15, CONTEXT D-03 | ROUND 9's multi-removal narrowing creates removals guarded by NO undo toast at all (C-then-D-during-freeze: C never gets a toast or window), stated at exactly one site (13:31) while four sibling absolutes in Plan 13 (:20 "guarded by an undo toast", :22 "reversible via the 8-second undo toast window", T-05.1-13a :1041 "the undo toast is the guard … bounding the real risk of a no-confirm action", :1054-1057 "ALWAYS … reversible within the 8-second toast window") and Plan 15 (:17's carve-out enumeration, :19's unqualified restatement) still assert the opposite. This silently narrows LOCKED D-03 (CONTEXT :53-54 — the no-confirm decision is justified BY the toast guard) with no gap-ledger item and no step-13 maintainer annotation — the exact remedy Z2/Z7 established in the same commit. Also falsifies ROUND 9's own sweep claim (13:352-353). Fix: apply the full Z2/Z7 remedy — "EXCEPT the disclosed …" at every sibling site, a new Plan 15 ledger item, step-13 D-03 annotation, and add the ROUND 9 carve-out to Plan 15:17's enumeration. |
| AA4 | HIGH | Sonnet L1 | 14 (vs 12, 08) | The World panel's "Placement mode active in-game" toast fires unconditionally on `sendStartPlacement` (14:336-344, :504), but Plan 12's agent makes START_PLACEMENT a silent no-op refusal in two documented cases (`g_capArmed` edit armed elsewhere; `g_placementActive` re-entry). Plan 08 built the ack primitive (`parseHostCommandResult`/`describeHostCommandResult`) and flagged consuming it as a gap for Plans 11/13/14 to pick up (T-05.1-08d); Plan 14 never does (zero hits). The toast is affirmatively wrong, not merely un-rich: user gets "active", agent refused, clicks land nowhere with zero feedback. Plan 15's checkpoint never exercises this path. Fix: Plan 14 consumes the ack (or at minimum discloses the false-positive window as an accepted gap at the toast site + ledger, and Plan 15 adds a checkpoint probe for the armed-elsewhere refusal). |
| AA5 | HIGH | Sonnet L2 | 04, 13, 14 | No plan resets `worldEditorStore` (`history`/`hasFailureBadge`/`sessionOverlay`), `removeUndoStore` (`pending`/`undoErrors`), or Plan 14's component-local diff/suppress refs on a PROJECT SWITCH — a real, already-built app capability (`openProjectStore.ts` exists; `workspaceStore.close()` at workspaceStore.ts:100-109 touches none of these). Worse, Plan 04's acceptance test (04:391-407) REQUIRES `refresh()` to preserve `history`/`sessionOverlay` across any tree replacement, unscoped to same-project — so Project A's activity log/failure badge contractually leak into Project B, A's never-auto-dismissing sticky toast survives the switch, and retrying A's Undo against B's tree on a building-id collision would write A's removed node into B's `.ilf`. Fix: a project-switch reset contract (which stores/refs clear on project close/open), scope Plan 04's preservation test to same-project, and gate `handleUndo` on project identity. |
| AA6 | HIGH | Cursor C1 | 13 | 13:609's live Task 2 prose still attaches the wrong behavior to `DeleteUndoToast.tsx:96-101` ("purely presentational … it subscribes to `useRemoveUndoStore((s) => s.pending)`") — `:96-101` is the START of `handleUndo`, not the subscription (that's `:68-92`). The plan's own Z10 block (13:269-275) identifies and claims to fix exactly this, and says "Every citation below is corrected in place" — but :609 was missed. Same-class stale text also at :147 (AA11). Fix: correct :609 (and :147) to cite `:68-92` for the subscription/diff. |
| AA7 | HIGH | Codex R8-S1/S2 (byte-verified by orchestrator) | 10 | Two residual unconditioned diskState absolutes survive OUTSIDE the Z3-cited sites: ~:107 (W1 narrative bullet — "when it returns a non-empty `failures` array, EVERY building's mirror is provably unchanged … or has been rolled back … never a mix") and ~:221 (read_first/interfaces prose — "a non-empty `failures` means NO building's mirror changed this call, never a partial mix"). Post-round-6 contract: that wording is only allowed when every failure carries `diskState:'unchanged'`; any `'uncertain'` (rollback itself failed) must hedge. Fifth and sixth restatements of the same superseded absolute. Fix: condition both, matching must_haves 10:26/:30 and the Z3-fixed test prose. |
| AA8 | MED | Opus Y4 | 13, 14 | Both Z5 queue regression tests (13:923-928, 14:489-496) require two successful Undos on two different entries "before any drain" — unreachable through the shipped single-slot UI (second Undo button never renders: A stays folded in `prevRef` after B's restore), and `handleUndo`→`refreshTree()` triggers Plan 14's `[tree]`-keyed drain between clicks anyway; the asserted `suppressNextDiffRef` is a never-exported ref X7 already ruled has "no legitimate test handle" (14:141-148). Fix: replace with a writable test (direct store-level pushes asserting queue length, or the X7 static-grep gate), and drop the "before any drain" precondition. |
| AA9 | MED | Opus Y5 | 13 | 13:950-952 (pre-ROUND-8 acceptance text) still asserts unconditionally "a confirmation toast appears; the toast auto-dismisses after its timer" — contradicted in the SAME acceptance block by ROUND 8's thaw-priority criterion (:886-894, no bare "restored" on the retry route) and ROUND 9's no-timer-while-sticky criterion (:978-979). Falsifies ROUND 9's :348-349 "every restatement … updated" claim. Fix: qualify :950-952 to the non-sticky, nothing-queued case. |
| AA10 | MED | Opus Y6 | 13 | 13:728-734 justifies dismiss with "the entry remains in `pending` … recoverable by other means later" — no such means exists in this phase (the `DeleteUndoToast` precedent's second surface is ProjectListDialog's Restore button; `RemoveUndoToast` has no analog; after dismiss-with-nothing-queued, A is permanently un-surfaceable: in the fold baseline, never in `removed`). Fix: replace the phrase with the honest framing already used elsewhere (explicit acknowledgement; recovery = manual re-add via the Add flow). |
| AA11 | MED | Cursor C2 | 13 | 13:147 (ROUND 5/V1 narrative) repeats the same `:96-101`-as-subscription mis-attachment as AA6. Superseded by X5 (:205-208) but never marked, and misleads top-down readers. Fix with AA6. |
| AA12 | MED | Fable (Z6 half-propagation) | 13 | 13:224's ROUND 6/X13 note still carries the "LOAD-BEARING …" framing that Z6 declared FALSE — with no in-place SUPERSEDED marker, though the X4 note in the same commit received exactly such a marker. Fix: add the SUPERSEDED-by-Z6 marker. |
| AA13 | LOW | Opus Y7 | 13 | Plan 13 consumes Plan 10's `resolveOverridePair()`/`refreshTree()` on every write path but reaches Plan 10 only transitively (13→11→10) — the exact condition Z4 remedied for Plan 14 by adding a direct `depends_on` entry. Arithmetically harmless today (full wave audit clean); inconsistent with the standard the same commit set. Fix: add `"05.1-10"` to Plan 13's `depends_on`. |
| AA14 | LOW | Opus Y8 | 13 | The ROUND 8/9 mechanism silently assumes `clearUndoError` + paired writes land in one React batch and that Task 1's add-back stays synchronous; an interleaved render between clear and set reintroduces AA2's orphan outcome via the retry path. Cheap hardening: clear the entry's error only on SUCCESS (failure branch overwrites the slot anyway), or state the single-batch/synchronous constraint explicitly. |
| AA15 | LOW | Fable FC-10 | 13 | Fabricated one-word "correction": interfaces block now says "the full 219-line component"; `DeleteUndoToast.tsx` is 218 lines (pre-commit text was correct). Restore "218-line" or drop the count. Zero-fabrication streak broken by this word alone. |
| AA16 | LOW | Fable FC-34 + Opus cross-cutting note | 13 (process) | ROUND 9's sweep claim reads as grep-empty when grep returns five out-of-scope hits, and the sweeps target mechanism keywords rather than guarantee-restatement sites — the process root cause of AA3/AA9 (and the round-7 Z2/Z3 class). Next replan's sweep checklist should enumerate: must_have lists, sketch_elements, gap ledgers, threat-model mitigations, success_criteria, acceptance blocks, `<done>` text. |
| AA17 | LOW | Cursor W1 | 08 | 08:88-89 abbreviates the `useChannelReader.ts:270-273` call site (`{ mappingName, clientExe }` — real bytes pass `clientExe: useLiveStore.getState().clientLabel`). Diagnosis unaffected; tighten the cite. |

---

## Codex Review (seam-propagation trace)

NOT CONVERGED

| ID | severity | file:line | claim | evidence |
|---|---|---:|---|---|
| R8-S1 | HIGH | `05.1-10-PLAN.md:107` | Residual old diskState absolute: non-empty failures still imply every mirror is provably unchanged/rolled back. | "when it returns a non-empty `failures` array, EVERY building's mirror is provably unchanged … or has been rolled back" |
| R8-S2 | HIGH | `05.1-10-PLAN.md:221` | Same residual old diskState absolute in read-first/interface prose; not conditioned on all failures being `diskState:'unchanged'`. | "a non-empty `failures` means NO building's mirror changed this call" |

Propagated fully:

- Z1 undo-error display contract: checked Plan 13 at `:31`, `:320-345`, `:680-744`, `:910-989`, `:1015-1025`, `:1046`, `:1085-1110`; Plan 15 references at `:82-85` and Undo/`(NEW)` live checks at `:273-288`. The per-entry `undoErrors`, scoped `clearUndoError(id)`, add-back-before-`restore(id)`, sticky no-timer, deferred `prevRef` fold, and multi-removal narrowing are consistently stated. No Plan 14 undo-error restatement found beyond the Plan 13 suppression queue consumption.
- Z2 `(NEW)` hedge: checked Plan 14 must_have `:19`, sketch_elements `:42`, revision note `:164-171`, done text `:590-592`; Plan 15 must_have `:14`, checkpoint step 4 `:243-248`, ledger `:353-358`, done text `:439-441`. All carry the disclosed X8 mid-flow-remount exception.
- Z4 forward-output relabels: checked Plan 06 `:423-426`, Plan 08 `:410-411`, Plan 12 `:227-230`, Plan 13 `:421-428` and `:595-599`, Plan 14 `:172-180`, `:345-356`, `:593-594`. These are labeled as forward outputs or wave/dependency-ordering guarantees, except current-source facts are explicitly labeled as current facts where used, e.g. Plan 14 `:173-176` says current `recordPersistResult` hits are zero.

*(Orchestrator note: R8-S1/R8-S2 byte-verified against the plan file before inclusion — both quotes exact.)*

---

## Cursor Review (line-level citation & wiring verification)

**Verdict: `NOT CONVERGED`**

Round-7/8/9 citation fixes are mostly byte-accurate against real renderer source, but Plan 13 still attaches the wrong behavior to `DeleteUndoToast.tsx:96-101` in live Task 2 prose despite Z10 claiming in-place correction. Cross-plan symbol wiring is correctly labeled as forward outputs with valid wave ordering.

| ID | Severity | Plan file:line | Claim | Ground truth |
|----|----------|----------------|-------|--------------|
| C1 | HIGH | `05.1-13-PLAN.md:609` | RemoveUndoToast is "purely presentational, matching `DeleteUndoToast.tsx:96-101`'s own precedent exactly: it subscribes to `useRemoveUndoStore((s) => s.pending)`…" | `:96-101` is the start of `handleUndo` (`restore(entry.id)` + success comment), not the pending subscription. Subscription/diff is `:68-92`. Plan's own ROUND 7 Z10 block (`:269-275`) identifies and fixes this — but `:609` was not corrected, contradicting "Every citation below is corrected in place." |
| C2 | MED | `05.1-13-PLAN.md:147` | ROUND 5 V1 still says RemoveUndoToast is "purely presentational (the SAME `DeleteUndoToast.tsx:96-101` precedent…)" | Same mis-attachment. Superseded in ROUND 6 X5 (`:205-208`) and read_first (`:586-587`), but stale text remains and can mislead implementers reading top-down. |
| W1 | LOW | `05.1-08-PLAN.md:88-89` | C3 describes call site as `{ mappingName, clientExe }` | Abbreviated. Real bytes at `useChannelReader.ts:270-273` pass `{ mappingName, clientExe: useLiveStore.getState().clientLabel }`. Missing-`studioDir` diagnosis is still correct; cite is imprecise, not wrong-line. |

Wiring feasibility: `recordPersistResult` (No — correctly "Plan 08 Task 3 forward output", 08 w2 → 14 w5, depends_on now direct), `worldEditorStore` (No — "Plan 04 output", 04 w0 → consumers), `undoErrors`/`clearUndoError(id)` (No — Plan 13 Task 1 output), `suppressNextDiffRef` queue (No — 13 w4 → 14 w5, same file), `makeReadVfs` (private at `decorationPersistOrchestrator.ts:102`; Plan 06 export action, 06 w1 → 10/13), `resolveOverridePair()`/`refreshTree()` (No — Plan 10 output, 10 w2 → 13/14), `formatPersistMessage` (No — 08 w2 → consumers), `sendStartPlacement` (No — Plan 08 output, `hostCommand.ts` absent), `removeDecorationRow` (No — Plan 13 Task 1), `reconcileMirrorMode` (No — Plan 06 output, 06 w1 → 10 w2). No plan text in the ROUND-edited regions treats a nonexistent symbol as currently landed without a forward-output/wave-order qualifier. No consumer-before-producer ordering defect (`06→10→13→14→15`, `08→14` direct).

**Citation tally: 29 checked, 27 byte-accurate, 2 defective** (C1 HIGH, C2 MED). Full verified-accurate table in `.planning/research/CONSULT-05.1-R8-cursor-line-verify.out`.

---

## Sonnet Review (lateral / new-seam hunt)

CONVERGED: NO

| ID | severity | plan file:line(s) | new failure mode | concrete failure scenario |
|----|----------|--------------------|-------------------|----------------------------|
| L1 | HIGH | `05.1-14-PLAN.md:336-344`, `:504` vs `05.1-12-PLAN.md:206-214`, `05.1-08-PLAN.md:18,231-244,510` | The World panel's "Placement mode active in-game" toast fires unconditionally on `sendStartPlacement`, with no read of the agent's ack — but Plan 12 makes START_PLACEMENT a silent no-op refusal (result 0) in two of its own documented cases: an edit already armed elsewhere (`g_capArmed`), or placement already active (`g_placementActive`, Sonnet F10's own re-entry guard). Plan 08 built exactly the ack primitive to detect this (`parseHostCommandResult`/`describeHostCommandResult`) and explicitly flagged consuming it as an accepted gap for Plan 11/13/14 to "optionally pick up" (T-05.1-08d). Plan 14 never picks it up (zero hits for either function). This is worse than Plan 08's disclosed "no rich toast" gap — the toast shown is not merely un-rich, it's affirmatively wrong. Plan 15's checkpoint never exercises this case through the real UI. | Hover+arm a decoration elsewhere (g_capArmed=true) → open the Add wizard → Place in game → toast claims "active" → agent silently refuses (result 0) → user clicks the floor with zero feedback and no diagnostic path. Same false-positive on a fast double-click of "Place in game" itself. |
| L2 | HIGH | `packages/renderer/src/state/workspaceStore.ts:100-109` (real source) vs `05.1-04-PLAN.md:391-407`, `05.1-13-PLAN.md:319-353` | No plan specifies a reset for `worldEditorStore.history`/`hasFailureBadge`/`sessionOverlay`, `removeUndoStore.pending`/`undoErrors`, or Plan 14's component-local diff/suppress refs on a project switch — a real, already-built app capability (`openProjectStore.ts` exists; `workspaceStore.close()` touches none of these). Worse: Plan 04's own acceptance test requires `refresh()` to preserve `history`/`sessionOverlay` across any tree replacement, unscoped to "same project" — so the exact call WorldPanel makes on a project switch is contractually obligated to leak Project A's Activity log/failure badge into Project B. `removeUndoStore`'s sticky Undo-error toast (ROUND 9) never auto-dismisses, so it survives indefinitely across a switch too. | Fail an Undo + a persist in Project A (sticky error + failure badge set) → switch to Project B via Open Project → `refresh()` preserves A's history/badge into B's panel; A's stuck removal/sticky toast still renders over B; retrying Undo on it resolves against B's tree and, on a building-id collision between the two projects, would write A's removed node into B's `.ilf` file. |

Also checked and found consistent (not new findings): Plan 13's RemoveUndoToast diff effect vs Plan 14's `[tree]`-keyed diff effect are independent and don't newly interfere; Plan 12's agent-side exclusivity logic itself is sound (only the renderer's non-consumption of the ack is new); Plan 15's 13-step checkpoint ordering has no step depending on state an earlier step destroys; no new HUD/wizard focus-ownership conflict beyond L1. Searched and came up empty for prior-round mentions of "project switch," "workspace close," "already armed," "false toast," "ack…placement" (both findings genuinely new).

---

## Opus Review (locked-decision fidelity & spec math)

**NOT CONVERGED** (3 HIGH, 3 MED, 2 LOW on this angle)

| ID | Sev | plan file:line(s) | Colliding must_haves / unsatisfiable branch | Derivation (abridged — full stepwise traces in `.planning/research/CONSULT-05.1-R8-opus-spec-math.out`) |
|----|-----|-------------------|---------------------------------------------|------------|
| Y1 | HIGH | `05.1-13-PLAN.md:789-797` vs `:798-809` + `:676-688`; ground truth `DeleteUndoToast.tsx:102-110`, `:141-143` | must_have `:31`(c) "that entry's error toast stays displayed" vs ROUND 8's "at the TOP of the effect … `return` immediately". On the first tick after `setUndoError`, the guard is already true (the Z1 reorder means the entry never left `pending`), so the branch that would swap the toast content to the error message never runs. | The cited swap idiom (`:102-110`) is imperative `setToast`, render reads `undoError` off toast state (`:141`); ROUND 9 requires the swap to call `clearTimer()` — so it is effect-resident; the plan declares exactly ONE effect depending on `undoErrors`, whose first statement is the guard. Repro: `pending=[A]`, toast A, add-back throws → `setUndoError(A)` → guard true on first evaluation → early return → no swap. Toast keeps plain "removed — Undo" copy, never errors, never dismisses (ROUND 9 removed the timer). Strictly worse than X4; 13:31(c) and success_criteria unproducible on their only reachable branch. |
| Y2 | HIGH | `05.1-13-PLAN.md:31`, `:728-734`, `:826-830`; `05.1-15-PLAN.md:348-352` | Tab navigation is a FOURTH exit route: the deliberate panel-scoped mount (`:826-830`) destroys `toast`/`prevRef`/`wasStickyRef` while `pending`/`undoErrors` survive. | Remount: guard false (no displayed entry) → diff `[]` vs `[A,C]` → `added[added.length-1]` shows C. A's failure silently superseded by an unrelated Remove (the literal forbidden wording) and A's `undoErrors` slot orphaned with no UI surface — verbatim ROUND 9's own blocker condition on the timer route. Ledger (g) covers only the plain case; its "may or may not re-show" wording is false for the sticky case (deterministically shows C). |
| Y3 | HIGH | `05.1-13-PLAN.md:20`, `:22`, `:950-952`, `:1041`, `:1054-1057`; `05.1-15-PLAN.md:17`, `:19`, `:330-375`, `:378-385` | ROUND 9's narrowing creates removals with NO undo guard (C in C-then-D-during-freeze), disclosed at one site while 4 sibling absolutes in Plan 13 + Plan 15 `:17`/`:19` assert the opposite; silently narrows LOCKED D-03 (CONTEXT `:53-54` — no-confirm is justified BY the toast guard); no ledger item, no step-13 annotation (the Z2/Z7 remedy applied elsewhere in the same commit); makes Plan 15 `:17` unsatisfiable (the carve-out is absent from the enumeration a verifier must record against); ROUND 9's sweep claim (`:352-353`) falsified — the sweep grepped mechanism keywords, the sites needing update contain none of them. |
| Y4 | MED | `05.1-13-PLAN.md:923-928`; `05.1-14-PLAN.md:489-496` vs `:141-148` (X7), `05.1-13-PLAN.md:31` | Both Z5 queue regression tests unwritable: single-slot UI never renders a second Undo button (A folded in `prevRef` after B's restore); `handleUndo`→`refreshTree()` drains Plan 14's `[tree]`-keyed queue between clicks, so "before any drain" is unreachable; `suppressNextDiffRef` is X7's "never-exported ref with no legitimate test handle". |
| Y5 | MED | `05.1-13-PLAN.md:950-952` vs `:886-894`, `:689-699` | Same acceptance block asserts unconditionally "a confirmation toast appears; auto-dismisses after its timer" AND its negation on the ROUND 8 retry route (no bare "restored") AND the ROUND 9 sticky branch (no timer ever). Falsifies ROUND 9's "every restatement updated" claim. |
| Y6 | MED | `05.1-13-PLAN.md:728-734` | "recoverable by other means later" — no such means exists (precedent's second surface is ProjectListDialog Restore; no analog here; after dismiss-with-nothing-queued, the entry is permanently un-surfaceable: in the fold baseline, never in `removed`). |
| Y7 | LOW | `05.1-13-PLAN.md:6` vs `:597-602`, `:441-448` | Plan 13 consumes Plan 10 outputs on every write path but lacks a direct `depends_on` entry (reaches 10 only via 11) — the exact condition Z4 remedied for Plan 14. Full wave audit otherwise clean (all 15 plans enumerated). |
| Y8 | LOW | `05.1-13-PLAN.md:630-658`, `:729-733` | Unstated single-React-batch + synchronous add-back assumption; an interleaved render between `clearUndoError` and `setUndoError` reintroduces Y2's orphan outcome via the retry path. Cheap hardening: clear error only on success. |

Guarantees traced and found producible (do not re-litigate): Z1(a) false-success impossibility; Z1(b) cross-entry erasure impossibility; ROUND 8 fold-deferral on both dismiss and retry-success routes; ROUND 9 repeat-failure loop; effect-loop termination with `toast` in deps; `wasStickyRef` read-and-reset idempotence; timer arithmetic (TOAST_MS=8000); full wave/`depends_on` ordering across all 15 plans; Plan 10's Z3-cited sites genuinely conditioned; Plan 06 `:17`/`:27` "EXCEPT" pair mutually satisfiable.

Cross-cutting note: Y3/Y5/Y4 are one recurring failure mode, third consecutive round — a revision fixes the mechanism at the named site without re-diffing every other site asserting the superseded absolute. Process cause: sweeps grep mechanism keywords ("8-second", "re-arm", `TOAST_MS`) rather than guarantee-restatement sites (must_have enumerations, gap ledgers, threat-model mitigations, success_criteria).

---

## Fable Review (fact-check of NEW claims in commit ba09958)

**Verdict: NOT CONVERGED** — 1 FALSE claim among the lines commit ba09958 added (a fabricated "correction" of a previously-accurate line count). Everything else checked out, including every line-number citation fix, every "zero grep hits" claim, and every wave/depends_on assertion.

Method: `git diff ba09958^ ba09958` per file to isolate ADDED lines only; every checkable fact verified against actual bytes (components under `packages/renderer/src/`, plan frontmatter, `05.1-CONTEXT.md`, `05.1-REVIEWS-round7.md`, pre-commit blobs via `git show ba09958^:`), every claimed grep re-run.

**Totals: 37 claim groups checked — 35 VERIFIED, 1 FALSE, 1 UNVERIFIABLE.** Full FC-01…FC-37 table in `.planning/research/CONSULT-05.1-R8-fable-factcheck.out`.

- **FC-10 (FALSE):** the replan changed the previously-correct "218-line component" to "the full 219-line component" (`05.1-13-PLAN.md` interfaces block). `DeleteUndoToast.tsx` is 218 lines. One-word fix. **The zero-fabrication streak does NOT hold for ba09958** — by exactly this word.
- **FC-36 (UNVERIFIABLE):** the ROUND 8/9 checker-loop provenance has no on-disk artifact (internally coherent; all ground-truth premises verified).
- **FC-29 (VERIFIED with caveat):** Plan 13 `:224`'s X13 note still asserts the "LOAD-BEARING" framing Z6 declared FALSE, with no SUPERSEDED marker (the X4 note in the same commit got one) — residual internal contradiction.
- **FC-34 (VERIFIED with note):** ROUND 9's sweep claim reads as grep-empty; grep in fact returns five out-of-scope hits (conclusion holds, wording overstates).
- Notably verified: the `:197-198`/`:203` logf fix, the `:146-166`/`:120-137`/`:118` shell split, `:61-64`/`:109` timer cites, `:27-31` union, App.tsx `:27`/`:105` global mount, the DeployDialog useEffect-vs-handler reclassification, all five ALREADY-LANDED→forward-output relabels (grep-confirmed absent), all wave/depends_on arithmetic, and all Z1–Z18 reviewer attributions.

---

## Consensus Summary

### Agreed strengths (2+ reviewers)
- The core ROUND 8 fold-deferral mechanism is sound on both specified exit routes (Opus traced it end-to-end; the plan-checker's independent trace concurred pre-commit).
- Z1/Z2/Z4 propagated fully at the sites each reviewer checked (Codex site-list; Cursor wiring table; Fable FC-24/FC-25/FC-21).
- Forward-output labeling is now consistently honest: all 10 cross-plan symbols correctly labeled, wave ordering valid, zero consumer-before-producer defects (Cursor, Fable, Opus independently).
- Citation hygiene is near-fully repaired: 27/29 byte-accurate (Cursor), every headline round-7 correction verified against real bytes (Fable).

### Agreed concerns (highest priority)
1. **The half-propagation defect class is now the phase's dominant failure mode** (4th consecutive round; found independently this round by Codex on Plan 10, Cursor on Plan 13:609/:147, Opus on Plan 13/15 guarantee sites, Fable on Plan 13:224). The process fix (AA16) matters as much as the item fixes: sweep guarantee-restatement sites, not mechanism keywords.
2. **Plan 13's sticky-error contract still cannot deliver its own must_have** — two new HIGH seams (AA1 first-render unreachability; AA2 unmount route) introduced by the very revisions that closed the previous two. Opus, Sonnet, and the checker have now each falsified successive versions of this contract; consider whether the single-slot copied-precedent architecture is the wrong substrate for a per-entry error guarantee (e.g. derive display state from the store rather than a frozen ref baseline).
3. **The ROUND 9 narrowing silently narrows locked D-03** (AA3) — needs the maintainer-visible ledger/annotation treatment, not just an inline hedge.

### Divergent views
- Codex found Plan 10 residuals Opus's site-scoped check missed; Opus found Plan 10's Z3-cited sites clean — both correct, different sites (orchestrator byte-verified both quotes). Not a conflict; evidence the sweep, not the fixes, is the weak link.
- Sonnet's two HIGHs (AA4, AA5) are the only findings this round touching plans outside the Plan 13/15 blast radius — the wizard/ack seam (12↔14↔08) and the project-switch lifecycle (04↔13↔14) have had no reviewer attention across rounds 2–7 and may hold more.
- Fable alone treats the 218/219 slip as streak-breaking; all agree the impact is trivial but the process signal (a "correction" that invented a number) is worth the one-word fix.
