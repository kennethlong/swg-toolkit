---
phase: 03-live-injection-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/live-inject/CMakeLists.txt
  - packages/live-inject/cmake-js.json
  - packages/live-inject/package.json
  - packages/live-inject/src/addon.cpp
  - packages/live-inject/src/inject_binding.cpp
  - packages/live-inject/src/procmem_binding.cpp
  - packages/live-inject/src/channel_binding.cpp
  - packages/live-inject/agent/CMakeLists.txt
  - packages/live-inject/agent/agent_main.cpp
  - packages/live-inject/agent/resolve.h
  - packages/live-inject/agent/resolve.cpp
  - packages/live-inject/agent/rva_table.cpp
  - packages/live-inject/agent/sentinels.h
  - packages/live-inject/agent/sentinels.cpp
  - packages/live-inject/agent/channel.cpp
  - packages/live-inject/test/resolve.spec.ts
  - packages/live-inject/test/sentinels.spec.ts
  - packages/live-inject/test/channel-layout.spec.ts
  - packages/live-inject/test/handle.spec.ts
  - packages/contracts/src/live-inject.ts
  - packages/contracts/src/index.ts
  - packages/harness/fixtures-real/live/.gitkeep
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-04
  - LIVE-05

must_haves:
  truths:
    - "packages/live-inject exists in the pnpm workspace and pnpm install resolves it without error"
    - "contracts/live-inject.ts exports IPC message union, LIVE_CHANNEL_LAYOUT, ENGINE_ENDPOINT_NAMES, VerifiedObjectState — importable from @swg/contracts"
    - "4 test spec files exist and fail cleanly (RED — Wave 0 stubs) when pnpm --filter @swg/live-inject test is run"
    - "x86 MSVC toolset availability is confirmed or flagged as a blocking gap before any C++ compilation task"
  artifacts:
    - path: packages/live-inject/CMakeLists.txt
      provides: host x64 N-API addon build definition
    - path: packages/live-inject/agent/CMakeLists.txt
      provides: plain x86 Win32 DLL build definition (separate cmake invocation)
    - path: packages/contracts/src/live-inject.ts
      provides: LIVE_CHANNEL_LAYOUT, ENGINE_ENDPOINT_NAMES, VerifiedObjectState, LiveIpcMessage union
    - path: packages/live-inject/test/resolve.spec.ts
      provides: RED stub for name-keyed resolve unit tests
    - path: packages/live-inject/test/sentinels.spec.ts
      provides: RED stub for 4-sentinel predicate unit tests
    - path: packages/live-inject/test/channel-layout.spec.ts
      provides: RED stub for seqlock channel layout round-trip tests
    - path: packages/live-inject/test/handle.spec.ts
      provides: RED stub for OpenProcess handle lifecycle tests
  key_links:
    - from: packages/contracts/src/index.ts
      to: packages/contracts/src/live-inject.ts
      via: "export * from './live-inject.js'"
      pattern: "live-inject"
    - from: packages/live-inject/package.json
      to: pnpm-workspace.yaml packages glob
      via: "packages/*"
      pattern: "@swg/live-inject"
---

<objective>
Stand up the packages/live-inject/ package skeleton and all Wave-0 test infrastructure so
subsequent implementation plans have a clean foundation.

Purpose: Wave 0 — create RED tests and stubs before any implementation, so Plans 03-02 through
03-06 drive toward GREEN. Establish the contracts and build configuration that the entire phase
depends on.

Output: packages/live-inject/ directory with CMakeLists (host x64 + agent x86), stub C++ sources,
Vitest test stubs (RED), contracts/live-inject.ts with full type set, pnpm workspace integration,
and a Wave-0 environment note about the x86 MSVC toolset.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.claude\get-shit-done\workflows\execute-plan.md
@D:\Code\SWG-Toolkit\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-injection-foundation/03-CONTEXT.md
@.planning/phases/03-live-injection-foundation/03-GROUNDTRUTH-advertised-hooks.md
@.planning/phases/03-live-injection-foundation/03-RESEARCH.md
@.planning/phases/03-live-injection-foundation/03-PATTERNS.md

<interfaces>
<!-- Existing contracts to re-export alongside — packages/contracts/src/index.ts current shape -->
<!-- From packages/contracts/src/index.ts (read 2026-06-25): -->
export * from './ipc.js';
export * from './sab-layout.js';
export * from './opcodes.js';
export * from './tre.js';
export * from './iff.js';
export * from './mesh.js';
export * from './skeleton.js';
export * from './animation.js';
export * from './material.js';

<!-- From packages/native-core/CMakeLists.txt — copy this exact header block, change project name -->
<!-- cmake_minimum_required(VERSION 3.15) / project(swg_live_inject) / C++20 standard / -->
<!-- include_directories(${CMAKE_JS_INC}) / file(GLOB SOURCE_FILES "src/*.cpp") / -->
<!-- add_library SHARED / SUFFIX .node / target_link_libraries CMAKE_JS_LIB -->
<!-- target_include_directories: ${CMAKE_SOURCE_DIR}/node_modules/node-addon-api + -->
<!--   ${CMAKE_SOURCE_DIR}/../../node_modules/node-addon-api -->
<!-- target_compile_definitions: NAPI_DISABLE_CPP_EXCEPTIONS / NAPI_VERSION=8 / NAPI_EXPERIMENTAL -->
<!-- DO NOT add subdirectory(modules/core) — live-inject has no format-tower static lib -->

<!-- From packages/native-core/cmake-js.json — copy verbatim -->
<!-- { "generator": "Visual Studio 17 2022", "platform": "x64" } -->

<!-- From packages/native-core/package.json — shape to mirror -->
<!-- { "name": "@swg/native-core", "main": "index.js", scripts: build/rebuild/prebuild, -->
<!--   dependencies: node-addon-api ^8.8.0 / node-gyp-build ^4, -->
<!--   devDependencies: @electron/rebuild ^4 / cmake-js ^8 / prebuildify ^6 } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold — CMakeLists, build configs, C++ stubs, pnpm integration</name>
  <files>
    packages/live-inject/CMakeLists.txt,
    packages/live-inject/cmake-js.json,
    packages/live-inject/package.json,
    packages/live-inject/src/addon.cpp,
    packages/live-inject/src/inject_binding.cpp,
    packages/live-inject/src/procmem_binding.cpp,
    packages/live-inject/src/channel_binding.cpp,
    packages/live-inject/agent/CMakeLists.txt,
    packages/live-inject/agent/agent_main.cpp,
    packages/live-inject/agent/resolve.h,
    packages/live-inject/agent/resolve.cpp,
    packages/live-inject/agent/rva_table.cpp,
    packages/live-inject/agent/sentinels.h,
    packages/live-inject/agent/sentinels.cpp,
    packages/live-inject/agent/channel.cpp
  </files>
  <read_first>
    PATTERNS.md §packages/live-inject/CMakeLists.txt (analog: packages/native-core/CMakeLists.txt:1-55 — copy C++20 block, cmake-js include, glob, shared lib, SUFFIX .node, target_include_directories, compile definitions; omit add_subdirectory(modules/core) and swg_core link).
    PATTERNS.md §packages/live-inject/cmake-js.json (packages/native-core/cmake-js.json — copy verbatim, host is x64).
    PATTERNS.md §packages/live-inject/agent/CMakeLists.txt (partial analog: copy C++20 block, diverge to plain Win32 DLL — no cmake-js, no N-API, -A Win32, SUFFIX .dll; add arch-requirement comment).
    PATTERNS.md §packages/live-inject/src/addon.cpp (analog: packages/native-core/src/addon.cpp:1-194 — copy Init function shape + NODE_API_MODULE registration; forward-declare LaunchAndInject, AttachAndInject, Detach, OpenProcessHandle, CloseProcessHandle, ReadProcessRegion, IsProcessAlive, OpenChannel, CloseChannel, ReadChannelView).
    RESEARCH.md §Package layout (recommended project structure, §Standard Stack).
    PATTERNS.md §No Analog (agent_main.cpp — harvest from Utinni main.cpp:80-115 DllMain pattern).
    GROUNDTRUTH-advertised-hooks.md §Arch match (agent must be x86; host addon is host arch x64).
  </read_first>
  <action>
    Create the packages/live-inject/ directory tree. All C++ files in this task are STUBS only —
    correct declarations and includes, but empty/no-op bodies (implementations land in Plans 03-02
    through 03-05).

    packages/live-inject/CMakeLists.txt: Copy native-core/CMakeLists.txt:1-55 exactly, changing
    project name to swg_live_inject. Omit the add_subdirectory(modules/core) line and swg_core
    link — live-inject has no format-tower dependency. Add a comment: "host addon — x64 via cmake-js;
    agent DLL is a SEPARATE build in agent/ (plain Win32, -A Win32)."

    packages/live-inject/cmake-js.json: Copy native-core/cmake-js.json verbatim (x64, VS 17 2022).

    packages/live-inject/package.json: Mirror native-core shape. Name: @swg/live-inject.
    scripts: build (cmake-js build x64), rebuild, test (vitest run). Dependencies: node-addon-api ^8.8.0,
    node-gyp-build ^4. devDependencies: cmake-js ^8, vitest (workspace:*). Do NOT add prebuildify —
    this addon is not distributed; it runs in the dev checkout only.

    packages/live-inject/src/addon.cpp: Stub — includes napi.h, forward-declares all 10 functions,
    Init function registers them, NODE_API_MODULE(swg_live_inject, Init). Per PATTERNS.md §addon.cpp shape.

    packages/live-inject/src/inject_binding.cpp: Stub — includes napi.h + Windows.h + TlHelp32.h;
    declares LaunchAndInject, AttachAndInject, Detach returning env.Undefined() stubs.

    packages/live-inject/src/procmem_binding.cpp: Stub — declares OpenProcessHandle, CloseProcessHandle,
    ReadProcessRegion, IsProcessAlive returning stubs.

    packages/live-inject/src/channel_binding.cpp: Stub — declares OpenChannel, CloseChannel,
    ReadChannelView returning stubs. Add comment: "CHANNEL_BYTE_SIZE = 320 per LIVE_CHANNEL_LAYOUT.TOTAL_SIZE".

    packages/live-inject/agent/CMakeLists.txt: C++20 standard block (copy native-core pattern).
    Project name: swg_toolkit_agent. add_library(${PROJECT_NAME} SHARED ${AGENT_SOURCES}) with
    GLOB AGENT_SOURCES "*.cpp". SUFFIX ".dll", PREFIX "". NO cmake-js includes, NO node-addon-api,
    NO NAPI_* defines. Add comment: "# ARCH REQUIREMENT: This DLL must be x86 (Win32) to match
    the SWG client. Build separately: cmake -B build-agent -A Win32 -G 'Visual Studio 17 2022'
    then cmake --build build-agent --config Release. The host addon (../CMakeLists.txt) is x64."
    Add comment: "# x86 MSVC TOOLSET REQUIRED: Confirm 'MSVC v143 C++ x64/x86 build tools' is
    installed in VS 17 2022 before building. Missing = blocking. Check: vswhere -products *
    -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64".

    packages/live-inject/agent/agent_main.cpp: Stub — Windows.h include; DllMain doing only
    DisableThreadLibraryCalls; extern "C" __declspec(dllexport) DWORD WINAPI agent_init(LPVOID)
    returning 0. Include comment: "// TODO: full implementation in Plan 03-04 (agent_main task)".

    packages/live-inject/agent/resolve.h: Stub — EngineHookPoint/EngineHookPoints struct declarations
    (copy verbatim from GROUNDTRUTH §contract structs), ENGINE_HOOKPOINTS_VERSION 6. Declare
    resolveFromExe(), isAdvertisedClient(), lookupByName(). Include comment:
    "// Implementation in Plan 03-02".

    packages/live-inject/agent/resolve.cpp: Stub — #include "resolve.h", function bodies return
    false/nullptr.

    packages/live-inject/agent/rva_table.cpp: Stub — comment block naming the file, note
    "// Legacy known-RVA literals harvested from Utinni source — implemented in Plan 03-02".

    packages/live-inject/agent/sentinels.h: Stub — SentinelResult struct; checkTransform, checkNetworkId,
    checkTemplateName, checkLiveness, allSentinelsPassed declarations. TRANSFORM_BYTE_SIZE = 48 constexpr.

    packages/live-inject/agent/sentinels.cpp: Stub — bodies return {false,"not implemented"}.

    packages/live-inject/agent/channel.cpp: Stub — channelOpen/channelWrite/channelClose stubs.

    WAVE-0 ENVIRONMENT NOTE (add as a doc-comment in agent/CMakeLists.txt):
    Run the following from a Developer PowerShell to confirm x86 toolset presence before Plan 03-02:
    `vswhere -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -format list`
    A missing x86 toolset is a hard blocker for the agent build — install via VS Installer
    "MSVC v143 … x86/x64 build tools" component before proceeding. Document the result in
    the Plan 03-01 SUMMARY.
  </action>
  <verify>
    <automated>cd D:/Code/SWG-Toolkit && pnpm install --frozen-lockfile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    pnpm install resolves @swg/live-inject without error (workspace glob "packages/*" picks it up).
    All new files exist at expected paths. C++ stubs compile as stubs — not required yet.
    SUMMARY documents x86 toolset check result (present / missing-needs-install).
  </acceptance_criteria>
  <done>packages/live-inject/ skeleton in place; pnpm workspace recognizes @swg/live-inject; x86 toolset status documented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contracts (live-inject.ts) + Wave-0 RED test stubs</name>
  <files>
    packages/contracts/src/live-inject.ts,
    packages/contracts/src/index.ts,
    packages/live-inject/test/resolve.spec.ts,
    packages/live-inject/test/sentinels.spec.ts,
    packages/live-inject/test/channel-layout.spec.ts,
    packages/live-inject/test/handle.spec.ts,
    packages/harness/fixtures-real/live/.gitkeep
  </files>
  <read_first>
    PATTERNS.md §packages/contracts/src/live-inject.ts (analog: contracts/src/ipc.ts + sab-layout.ts —
    discriminated union shape ipc.ts:14-54; byte-layout constants sab-layout.ts:10-26; full content
    including LIVE_CHANNEL_LAYOUT, ENGINE_ENDPOINT_NAMES, VerifiedObjectState, LiveIpcMessage union).
    PATTERNS.md §packages/live-inject/test/resolve.spec.ts (analog: native-core/test/resolve-prebuild.test.ts:31-35).
    PATTERNS.md §packages/live-inject/test/sentinels.spec.ts (analog: harness/test/iff-roundtrip.test.ts:27-60).
    PATTERNS.md §packages/live-inject/test/channel-layout.spec.ts (seqlock round-trip shape).
    PATTERNS.md §packages/live-inject/test/handle.spec.ts (OpenProcess lifecycle mock shape).
    RESEARCH.md §Transform memory layout (48 bytes, NOT 64 — IPC doc is WRONG).
    RESEARCH.md §Read-Verify Endpoints table (endpoint names, RVAs, typedefs — for ENGINE_ENDPOINT_NAMES).
    RESEARCH.md §Validation Architecture §Wave 0 Gaps.
  </read_first>
  <behavior>
    - resolve.spec.ts RED: describe('name-keyed resolve', () => it('resolves by name', () => { expect(true).toBe(false) })) — fails because native binding not yet built
    - sentinels.spec.ts RED: describe('checkTransform', ...) and describe('checkNetworkId', ...) etc. — fail on import
    - channel-layout.spec.ts RED: describe('channel layout round-trip', ...) — imported LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length must equal 320; seqlock test fails until implementation
    - handle.spec.ts RED: describe('OpenProcess handle lifecycle', ...) — fails because binding stub returns undefined
    - contracts/live-inject.ts: LIVE_CHANNEL_LAYOUT.TRANSFORM.length === 48 (not 64); LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length === 320; ENGINE_ENDPOINT_NAMES.GET_TRANSFORM_O2W === 'object::getTransform_o2w'
  </behavior>
  <action>
    packages/contracts/src/live-inject.ts: Create the full type file per PATTERNS.md §live-inject.ts.

    IPC message union (discriminated, per ipc.ts:14-54 pattern): LiveAttachRequest (type:'live-attach',
    id, clientExe, agentDll), LiveAttachResponse (type:'live-attached', id, pid, mappingName),
    LiveAttachError (type:'live-attach-error', id, reason), LiveReadRequest (type:'live-read', id),
    LiveStateUpdate (type:'live-state', id, state: VerifiedObjectState|null). Export LiveIpcMessage union.

    LIVE_CHANNEL_LAYOUT byte constants (per PATTERNS.md §live-inject.ts §byte-layout, confirmed against
    RESEARCH §Transform memory layout): SEQ_COUNTER {offset:0, length:4}, TRANSFORM {offset:4, length:48}
    (12 floats, float[3][4] row-major — 48 bytes, NOT 64; the IPC doc's "64-byte 4x4 matrix" is WRONG for SWG),
    NETWORK_ID {offset:52, length:8} (uint64), TEMPLATE_NAME {offset:60, length:256} (null-terminated ASCII),
    LIVENESS {offset:316, length:4} (player_non_null|is_over|padding), TOTAL_SIZE {offset:0, length:320}.

    ENGINE_ENDPOINT_NAMES catalog (string keys for lookupByName — must match exactly what
    swg-client-v2 engine_advertise.cpp advertises per GROUNDTRUTH §contract):
    GET_TRANSFORM_O2W:'object::getTransform_o2w', GET_NETWORK_ID:'object::getNetworkId',
    GET_OBJECT_TEMPLATE_NAME:'object::getObjectTemplateName', GET_PLAYER:'game::getPlayer',
    G_RUNNING_FLAGS:'game::g_runningFlags', G_MAIN_LOOP_COUNTER:'game::g_mainLoopCounter'.

    VerifiedObjectState interface: networkId: bigint (uint64), templateName: string,
    transform: Float32Array (12 floats), playerAlive: boolean.

    packages/contracts/src/index.ts: Add export * from './live-inject.js'; after the existing
    export * from './material.js'; line. Do not modify any other line.

    Wave-0 test stubs — all 4 files must be RED (failing) stubs:

    packages/live-inject/test/resolve.spec.ts: import { describe, it, expect } from 'vitest'.
    Three describe blocks: 'name-keyed resolve — synthetic table', 'isAdvertisedClient detection',
    'version mismatch is soft'. Each has one it(..., () => { expect(true).toBe(false) }) stub.
    Add comment: "// RED STUB — implement in Plan 03-02 (make GREEN when resolve.cpp lands)".

    packages/live-inject/test/sentinels.spec.ts: import { describe, it, expect } from 'vitest'.
    Four describe blocks: checkTransform, checkNetworkId, checkTemplateName, checkLiveness.
    Each has pass-case and fail-case it() stubs as expect(true).toBe(false).
    Add comment: "// RED STUB — make GREEN in Plan 03-03".

    packages/live-inject/test/channel-layout.spec.ts: import { describe, it, expect } from 'vitest';
    import { LIVE_CHANNEL_LAYOUT } from '@swg/contracts'.
    One passing sanity check: it('TRANSFORM.length is 48 (not 64)', () => {
    expect(LIVE_CHANNEL_LAYOUT.TRANSFORM.length).toBe(48); }). This PASSES from day 1 (contracts exist).
    One RED stub: it('seqlock write→read round-trips LiveState values', () => { expect(true).toBe(false); }).

    packages/live-inject/test/handle.spec.ts: import { describe, it, expect } from 'vitest'.
    Three it() stubs (all RED): 'openProcessHandle returns handle on success',
    'closeProcessHandle is idempotent (double-close safe)', 'uses PROCESS_VM_READ flag'.
    Add comment: "// RED STUB — make GREEN in Plan 03-04".

    packages/harness/fixtures-real/live/.gitkeep: Empty file. This is where real client byte
    fixtures land (captured transform bytes, networkId, template string) during Plan 03-02/03-03
    execution. Matches the Phase-1 fixtures-real/ discipline (STATE.md:D-09 / D-10).
  </action>
  <verify>
    <automated>cd D:/Code/SWG-Toolkit && pnpm --filter @swg/contracts build 2>&1 | tail -3 && pnpm --filter @swg/live-inject test 2>&1 | grep -E "(FAIL|PASS|Tests)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    @swg/contracts builds without TypeScript error (live-inject.ts exports compile).
    LIVE_CHANNEL_LAYOUT.TRANSFORM.length === 48 confirmed by channel-layout.spec.ts PASS on that single check.
    All 4 test files exist; test run reports failures (RED) on the stub tests — NOT import errors.
    Grep gate: grep -c "TRANSFORM.*length.*48" packages/contracts/src/live-inject.ts gives 1.
    Grep gate: grep -v "^//" packages/contracts/src/live-inject.ts | grep -c "64" gives 0 (no 64-byte matrix anywhere).
  </acceptance_criteria>
  <done>contracts exports all Phase-3 types; 4 test spec stubs exist and are cleanly RED; channel-layout sanity check is GREEN; fixtures-real/live/ directory exists</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → build system | CMakeLists and package.json define what gets compiled; incorrect platform flags (x64 vs x86) would produce an unusable agent DLL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | inject target selection (later plans) | mitigate | ProductName identity check before any inject (planned in 03-05 per RESEARCH §Known Threats) |
| T-03-02 | Tampering | agent DLL path (later plans) | mitigate | agent DLL path hardened to install dir in 03-04/05; documented here as a reminder |
| T-03-05 | Elevation of Privilege | elevation model | accept | same-integrity inject requires no admin (RESEARCH A1); graceful degrade in D-08/03-06 if elevated access needed; auto-escalation explicitly excluded |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 3 (RESEARCH §Package Legitimacy Audit: all N/A); slopcheck is N/A |
</threat_model>

<verification>
After completing both tasks:
- pnpm install --frozen-lockfile succeeds
- pnpm --filter @swg/contracts build succeeds (live-inject.ts compiles)
- pnpm --filter @swg/live-inject test reports exactly 4 files, RED on stubs, GREEN on the TRANSFORM.length===48 sanity check
- grep -c "live-inject" packages/contracts/src/index.ts gives 1
- SUMMARY records x86 MSVC toolset check result
</verification>

<success_criteria>
Wave-0 scaffold complete: packages/live-inject package exists in pnpm workspace; contracts type
file compiles; 4 RED test stubs in place with correct describe/it shapes; TRANSFORM byte size
locked at 48 (not 64) verified by a passing test from day 1.
</success_criteria>

<output>
Create .planning/phases/03-live-injection-foundation/03-01-SUMMARY.md when done.
</output>
