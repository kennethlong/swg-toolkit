---
phase: 03-live-injection-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - packages/live-inject/agent/agent_main.cpp
  - packages/live-inject/src/procmem_binding.cpp
  - packages/live-inject/src/channel_binding.cpp
  - packages/live-inject/src/addon.cpp
  - packages/live-inject/test/handle.spec.ts
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-05

must_haves:
  truths:
    - "handle.spec.ts is GREEN: openProcessHandle → closeProcessHandle lifecycle, double-close idempotent"
    - "agent_main.cpp DllMain does ONLY DisableThreadLibraryCalls — no engine calls, no resolve (loader lock safety)"
    - "agent_init export resolves endpoints THEN opens the file-mapping THEN signals the ready event THEN enters the read-verify poll loop"
    - "channel_binding.cpp external ArrayBuffer holds a Napi::Reference to prevent GC dangling-pointer (RESEARCH Pitfall 5)"
    - "procmem_binding.cpp exports isAdvertisedClient gate — if false, sets file-patch mode (D-08 signal to renderer)"
    - "host addon builds successfully via cmake-js x64"
  artifacts:
    - path: packages/live-inject/agent/agent_main.cpp
      provides: "DllMain (DisableThreadLibraryCalls only) + agent_init export (remote-thread entry)"
      contains: agent_init
    - path: packages/live-inject/src/procmem_binding.cpp
      provides: "OpenProcessHandle (full flag set), CloseProcessHandle, ReadProcessRegion, IsProcessAlive, IsAdvertisedClient"
    - path: packages/live-inject/src/channel_binding.cpp
      provides: "OpenChannel (host-side CreateFileMapping), MapViewOfFile → external ArrayBuffer, Napi::Reference GC guard"
      contains: Napi::Reference
    - path: packages/live-inject/src/addon.cpp
      provides: all 12 binding functions registered in NODE_API_MODULE
  key_links:
    - from: packages/live-inject/src/channel_binding.cpp
      to: packages/live-inject/agent/channel.cpp
      via: "same kernel object: host creates (CreateFileMapping, INVALID_HANDLE_VALUE), agent opens (OpenFileMappingA)"
      pattern: "Local\\\\SwgToolkitLive_"
    - from: packages/live-inject/src/procmem_binding.cpp
      to: packages/renderer/src/state/liveStore.ts
      via: "IsAdvertisedClient() false → liveStore.setMode('file-patch', reason) via renderer IPC"
      pattern: "isAdvertisedClient"
---

<objective>
Implement agent_main.cpp (the injected DLL's entry point) and the host-side N-API addon bindings
for process-handle lifecycle, channel access, and file-patch mode detection. Make handle.spec.ts GREEN.

Purpose: This plan wires together the agent DLL (connecting resolve + sentinels + channel from Plans
03-02/03-03) and the host addon's memory-access and channel bindings. The inject orchestration
(LaunchAndInjectWorker / AttachAndInjectWorker) is Plan 03-05 — this plan handles everything else.

Output: Complete agent_main.cpp; procmem_binding.cpp; channel_binding.cpp; addon.cpp with all
exports; handle.spec.ts GREEN.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.claude\get-shit-done\workflows\execute-plan.md
@D:\Code\SWG-Toolkit\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-live-injection-foundation/03-CONTEXT.md
@.planning/phases/03-live-injection-foundation/03-GROUNDTRUTH-advertised-hooks.md
@.planning/phases/03-live-injection-foundation/03-RESEARCH.md
@.planning/phases/03-live-injection-foundation/03-PATTERNS.md
@.planning/phases/03-live-injection-foundation/03-02-SUMMARY.md
@.planning/phases/03-live-injection-foundation/03-03-SUMMARY.md

<interfaces>
<!-- agent_init shape — harvest from Utinni main.cpp:80-115 + PATTERNS.md §agent_main.cpp -->
// DllMain: ONLY DisableThreadLibraryCalls. No engine calls. Rationale: loader lock + CRT not ready.
BOOL APIENTRY DllMain(HMODULE hModule, DWORD reason, LPVOID) {
    if (reason == DLL_PROCESS_ATTACH) DisableThreadLibraryCalls(hModule);
    return TRUE;
}
// agent_init: remote-thread entry. lpReadyEventName is a C-string written into this process by the launcher.
extern "C" __declspec(dllexport) DWORD WINAPI agent_init(LPVOID lpReadyEventName);
// Sequence: 1. OpenEventA(name) 2. resolveFromExe() 3. channelOpen(mappingName) 4. SetEvent(hReady)
//           5. CloseHandle(hReady) 6. read-verify poll loop (forever or until channelClose)
// NOTE: the mapping name is passed alongside the ready event name (host writes both into agent's
// address space via the same VirtualAllocEx + WriteProcessMemory mechanism as the event name).

<!-- OpenProcess flag set — RESEARCH §Pitfall 6 (corrected, SC-1 is incomplete for inject) -->
// For attach-to-running + inject path:
PROCESS_CREATE_THREAD | PROCESS_QUERY_INFORMATION | PROCESS_VM_OPERATION | PROCESS_VM_READ | PROCESS_VM_WRITE
// For read-only RPM after injection is complete (if needed standalone):
PROCESS_VM_READ alone suffices.

<!-- channel_binding.cpp external ArrayBuffer GC guard — RESEARCH §Pitfall 5 -->
// CRITICAL: hold a Napi::Reference<Napi::ArrayBuffer> alongside the raw pointer.
// If V8 GC collects the ArrayBuffer, UnmapViewOfFile fires the finalizer,
// but if C++ still holds the raw Data() pointer, it is now dangling.
// Fix: store a persistent Napi::Reference on a per-channel state struct; clear on CloseChannel.
auto ab = Napi::ArrayBuffer::New(env, view, CHANNEL_BYTE_SIZE,
    [](Napi::BasicEnv, void* data) { UnmapViewOfFile(data); });
// store s_channelRef = Napi::Reference<Napi::ArrayBuffer>::New(ab, 1) in module-global state

<!-- sab.cpp GC-guard pattern from packages/native-core/src/sab.cpp (existing analog) -->
// Read packages/native-core/src/sab.cpp lines 19-36 for the Napi::Reference lifetime pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agent/agent_main.cpp — DllMain + agent_init remote-thread entry + read-verify poll loop</name>
  <files>
    packages/live-inject/agent/agent_main.cpp
  </files>
  <read_first>
    D:/Code/Utinni/Launcher/main.cpp:80-115 — remote thread init idiom: open ready event, fire utinni_init on separate thread, do NOT WaitForSingleObject on the infinite thread. Read verbatim.
    D:/Code/Utinni/UtinniCore/swg/endpoints.cpp — resolveFromExe() call site inside UtinniCore's init, to understand the init sequence.
    GROUNDTRUTH-advertised-hooks.md §Injection / attach idiom (Pattern 3: agent DLL shape).
    RESEARCH.md §Pattern 3 (Agent DLL shape) — DllMain nothing + agent_init export shape.
    RESEARCH.md §Anti-Patterns (engine work in DllMain = crash; do nothing there).
    PATTERNS.md §agent_main.cpp (DllMain stub, agent_init body, __declspec(dllexport) undecorated name).
    packages/live-inject/agent/resolve.h (resolveFromExe, isAdvertisedClient — from Plan 03-02)
    packages/live-inject/agent/sentinels.h (allSentinelsPassed, SentinelResult — from Plan 03-03)
    packages/live-inject/agent/channel.cpp (channelOpen, channelWrite, channelClose — from Plan 03-03)
  </read_first>
  <action>
    agent_main.cpp: Implement the two-function DLL entry following the EXACTLY-as-Utinni idiom:

    DllMain: ONLY DisableThreadLibraryCalls(hModule) when reason == DLL_PROCESS_ATTACH. Return TRUE.
    No other work. No engine calls. No resolve. No CreateThread. Nothing.
    Add a block comment explaining WHY (loader lock + CRT not initialized at DllMain time;
    cite main.cpp:80-115 rationale from GROUNDTRUTH).

    agent_init(LPVOID lpReadyEventName):
    The lpReadyEventName pointer points to a C-string in this process's address space. The launcher
    wrote BOTH the ready-event name AND the file-mapping name into the agent's address space via
    VirtualAllocEx + WriteProcessMemory. Pass the two strings as a small struct or null-delimited pair
    (see RESEARCH §Open Questions 2 / PATTERNS §Pattern 1 step 6 — the mapping name is passed the
    same way as the event name). Design: the launcher writes "Local\\SwgToolkitAgent_<pid>\0Local\\SwgToolkitLive_<pid>\0"
    as a single buffer; agent_init reads both null-terminated strings from the pointer.

    Sequence inside agent_init:
    1. Parse the two names from lpReadyEventName (event name + mapping name).
    2. HANDLE hReady = OpenEventA(EVENT_MODIFY_STATE, FALSE, eventName).
       If null: return 1 (launcher's WaitForSingleObject will timeout at 30s; acceptable error path).
    3. swg::endpoints::resolveFromExe() — fills fn-pointer slots from the advertised table (or leaves
       RVA literals if legacy path). Two-backend: isAdvertisedClient() true → name-keyed; false → RVA table.
    4. channelOpen(mappingName, sizeof(LiveState)). If false: signal ready anyway and return 2.
    5. SetEvent(hReady) — unblocks the launcher's WaitForSingleObject (main.cpp:337 pattern).
    6. CloseHandle(hReady). (No longer needed after signaling.)
    7. Enter the read-verify poll loop:
       - Get player Object*: call swg::game::getPlayer() (from resolved fn-pointer slot or RVA literal).
         If null: Sleep(100), continue.
       - Call swg::object::getTransform_o2w(player) → Transform* → read 12 floats into a local buffer.
       - Call swg::object::getNetworkId(player) → uint64 (advertised path) OR read struct offset (legacy).
       - Call swg::object::getObjectTemplateName(player) → const char* (advertised) OR getTemplateFilename (legacy).
       - Get liveness: isAdvertisedClient? call g_runningFlags/g_mainLoopCounter accessors : read direct globals.
       - Run all 4 sentinel checks. Compute loopCounterDelta by comparing to the previous counter value.
       - If allSentinelsPassed: populate LiveState, channelWrite(&state).
       - Sleep(33) (~30fps poll — low overhead, adequate for read-verify; Phase 5 raises this to 60fps).
    8. Return 0 when stopped (currently loops forever; Phase 5 adds a stop signal).

    CALLING CONVENTION: when calling engine functions through the resolved fn-pointer slots, the
    typedefs carry the calling convention (__thiscall or __cdecl). Use them exactly as declared
    in rva_table.cpp. Do NOT add intermediate __fastcall wrappers — the typedefs handle it.

    TWO-BACKEND pattern: the resolve and RVA tables from Plans 03-02/03-03 are the resolver.
    isAdvertisedClient() selects which fn-pointer slot is valid. For legacy: use the RVA literals
    in rva_table.cpp directly as the fn-pointer values. For advertised: use the slot values
    written by resolveFromExe() (which overwrites the RVA literal with the name-keyed address).
    Both paths use the SAME fn-pointer variables — resolveFromExe() either overwrites them or
    leaves them at the RVA defaults. This is exactly Utinni's design.
  </action>
  <verify>
    <automated>cmake -B packages/live-inject/agent/build-agent -A Win32 -G "Visual Studio 17 2022" -S packages/live-inject/agent 2>&1 | tail -5 && cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    Agent DLL compiles as x86 Win32 DLL without error (requires x86 MSVC toolset — see Wave-0 check in Plan 03-01).
    Source assertion: grep -c "DisableThreadLibraryCalls" packages/live-inject/agent/agent_main.cpp gives 1.
    Source assertion: grep -c "DllMain" packages/live-inject/agent/agent_main.cpp gives 1.
    Source assertion: grep -v "^//" packages/live-inject/agent/agent_main.cpp | grep -c "engine\|resolve\|GetProcAddress" | result == 0 for DllMain body (ensure DllMain is empty of engine work).
    Source assertion: grep -c "__declspec(dllexport)" packages/live-inject/agent/agent_main.cpp gives 1 (agent_init).
    Note: if x86 toolset is absent (Wave-0 blocker), document in SUMMARY and mark the cmake step as BLOCKED.
  </acceptance_criteria>
  <done>agent_main.cpp compiles as x86 DLL; DllMain is empty of engine work; agent_init polls and writes verified state to the channel</done>
</task>

<task type="auto">
  <name>Task 2: procmem_binding.cpp + channel_binding.cpp + addon.cpp — host handle lifecycle + channel access; handle.spec.ts GREEN</name>
  <files>
    packages/live-inject/src/procmem_binding.cpp,
    packages/live-inject/src/channel_binding.cpp,
    packages/live-inject/src/addon.cpp,
    packages/live-inject/test/handle.spec.ts
  </files>
  <read_first>
    packages/native-core/src/sab.cpp:19-36 — Napi::Reference pattern for GC-safe ArrayBuffer lifetime. Read these lines to understand the existing in-process analog.
    packages/native-core/src/sab-rw.cpp:29-67 — argument validation pattern + ArrayBuffer return pattern. Read for the N-API error-handling idiom.
    packages/native-core/src/tre_binding.cpp:183 — ArrayBuffer return one-liner. Read for the inline shape.
    packages/native-core/src/addon.cpp:133-194 — Init function + NODE_API_MODULE registration shape. Read for the exact exports registration pattern.
    PATTERNS.md §procmem_binding.cpp (OpenProcess full flag set from RESEARCH §Pitfall 6; sab-rw.cpp validate→extract→call→ArrayBuffer shape).
    PATTERNS.md §channel_binding.cpp (OpenChannel via OpenFileMappingA + MapViewOfFile + external ArrayBuffer with finalizer + Napi::Reference GC guard).
    PATTERNS.md §Shared Patterns §N-API error handling (try/catch shape from sab-rw.cpp:29-48 + tre_binding.cpp:68-109).
    RESEARCH.md §Pitfall 5 (dangling arrayBuffer.Data() pointer — hold Napi::Reference, CRITICAL).
    RESEARCH.md §Pitfall 6 (SC-1 handle flags insufficient for inject — use full set including PROCESS_CREATE_THREAD|PROCESS_QUERY_INFORMATION).
    RESEARCH.md §Architecture Patterns §channel_binding (host creates the file-mapping BEFORE inject, passes name to agent).
  </read_first>
  <action>
    procmem_binding.cpp:

    OpenProcessHandle(pid: number, forInject: boolean): Napi::Value
    - forInject=true: OpenProcess(PROCESS_CREATE_THREAD|PROCESS_QUERY_INFORMATION|PROCESS_VM_OPERATION|PROCESS_VM_READ|PROCESS_VM_WRITE, FALSE, pid)
    - forInject=false (read-only RPM): OpenProcess(PROCESS_VM_READ, FALSE, pid)
    - Handle lifecycle: store HANDLE in a module-global map keyed by a handle-id string (e.g. "swg:<pid>").
    - Return the handle-id string on success, throw Napi::Error on failure.
    - On failure: check GetLastError() — if ERROR_ACCESS_DENIED, message = "Requires same-integrity process or SeDebugPrivilege not held; toolkit running in file-patch mode" (per D-08 graceful degrade).
    - Also expose IsAdvertisedClient() result through a separate export, or include it in the
      OpenProcessHandle response: return {handleId: string, isAdvertisedClient: boolean} so
      the renderer can immediately set the mode indicator.

    CloseProcessHandle(handleId: string): Napi::Value
    - Lookup the handle-id in the map. If not found: return (idempotent — double-close safe).
    - CloseHandle(hProcess); remove from map; return undefined.

    ReadProcessRegion(handleId: string, addr: number, byteCount: number): Napi::Value
    - Lookup handle. If not found: throw.
    - Bounds-check byteCount (max 4096 for a single RPM call; reject larger reads).
    - ReadProcessMemory(hProcess, (LPCVOID)addr, buf.Data(), byteCount, nullptr).
    - Return Napi::ArrayBuffer (copy; RPM always copies).

    IsProcessAlive(handleId: string): Napi::Value — GetExitCodeProcess() → STILL_ACTIVE → true.

    channel_binding.cpp:

    OpenChannel(pid: number): Napi::Value
    - Construct the mapping name: "Local\\SwgToolkitLive_" + std::to_string(pid).
    - CreateFileMapping(INVALID_HANDLE_VALUE, nullptr, PAGE_READWRITE, 0, CHANNEL_BYTE_SIZE (320), name.c_str())
      — host CREATES the mapping (not opens it). The agent will OpenFileMappingA the same name.
    - MapViewOfFile(hMap, FILE_MAP_READ|FILE_MAP_WRITE, 0, 0, CHANNEL_BYTE_SIZE).
    - Wrap view as external ArrayBuffer with UnmapViewOfFile finalizer.
    - CRITICAL (RESEARCH Pitfall 5): hold s_channelRef = Napi::Reference<Napi::ArrayBuffer>::New(ab, 1)
      on a module-global per-channel state struct. Clear it in CloseChannel.
    - Return the ArrayBuffer to JS (the renderer reads from it via typed array views).

    CloseChannel(pid: number): Napi::Value
    - Unref and clear s_channelRef (this triggers UnmapViewOfFile when AB is collected).
    - CloseHandle(s_hMap); return undefined.

    ReadChannelView(pid: number): Napi::Value
    - Return the current ArrayBuffer if the channel is open, else null.

    addon.cpp:
    Register ALL 12 binding functions (launchAndInject, attachAndInject, detach from inject_binding.cpp
    stubs, plus the 7 functions above, plus any test-utility exports from Plans 03-02/03-03).
    NODE_API_MODULE(swg_live_inject, Init).

    handle.spec.ts: Rewrite from RED stubs to real tests using vi.mock() to mock the native binding:
    - 'openProcessHandle returns a handle string on success': mock returns {handleId:'swg:1234', isAdvertisedClient:true}
    - 'closeProcessHandle is idempotent (double-close does not throw)': mock, call close twice, no throw
    - 'openProcessHandle with the forInject=true flag uses PROCESS_VM_READ': verify via spy on the mock
    Pure vitest mock tests — no real process, no native code needed for this test.
  </action>
  <verify>
    <automated>pnpm --filter @swg/live-inject build 2>&1 | tail -3 && pnpm --filter @swg/live-inject test -- --reporter=verbose 2>&1 | grep -E "(handle|PASS|FAIL)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    Host addon builds (cmake-js x64) without error.
    handle.spec.ts: all tests GREEN.
    Source assertion: grep -c "Napi::Reference" packages/live-inject/src/channel_binding.cpp gives 1 (GC guard).
    Source assertion: grep -c "PROCESS_CREATE_THREAD" packages/live-inject/src/procmem_binding.cpp gives 1.
    Source assertion: grep -c "PROCESS_VM_READ" packages/live-inject/src/procmem_binding.cpp gives 1.
    Source assertion: grep -c "CreateFileMapping" packages/live-inject/src/channel_binding.cpp gives 1.
    Source assertion: grep -c "UnmapViewOfFile" packages/live-inject/src/channel_binding.cpp gives 1 (in finalizer).
    Source assertion: grep -v "^//" packages/live-inject/src/channel_binding.cpp | grep -c "SharedArrayBuffer" gives 0 (never use V8 SAB cross-process).
  </acceptance_criteria>
  <done>procmem + channel bindings complete and registered; handle.spec.ts GREEN; GC dangling-pointer guard in place; host addon builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host addon (renderer, x64) → SWG client process (x86) | All RPM reads cross this boundary; all injected code runs in the client |
| Host addon → OS file-mapping kernel object | CreateFileMapping creates it; agent opens it; neither can forge the other's data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | agent DLL load path | mitigate | The host constructs the agentDllPath from a fixed install-relative path (not user input); plan 03-05 inject path uses this; document in SUMMARY |
| T-03-04 | Info-disclosure / DoS | ReadProcessRegion over arbitrary addr | mitigate | Bounds-check byteCount (max 4096); ReadProcessMemory returns false on bad address — check return value, don't crash; treat result as untrusted before passing to sentinels |
| T-03-05 | Elevation of Privilege | OpenProcess fails with ACCESS_DENIED | mitigate | Return structured error with reason "Requires same-integrity process or SeDebugPrivilege" — do NOT auto-escalate; renderer sets file-patch mode on this error (D-08) |
| T-03-03 | Tampering | file-mapping name collision | mitigate | Name includes PID ("Local\\SwgToolkitLive_<pid>"); per-launch uniqueness; agent validates the mapping was opened before writing |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; N/A |
</threat_model>

<verification>
pnpm --filter @swg/live-inject build succeeds.
pnpm --filter @swg/live-inject test: all 4 spec files — resolve + sentinels + channel-layout + handle — are GREEN.
pnpm -r test: full suite green (no regressions in other packages).
</verification>

<success_criteria>
Agent DLL entry point complete and compiled (x86); host addon bindings for process-handle lifecycle
and channel access complete; all 4 test specs GREEN; Napi::Reference GC guard in channel_binding.cpp;
corrected handle flag set (PROCESS_CREATE_THREAD included) in procmem_binding.cpp.
</success_criteria>

<output>
Create .planning/phases/03-live-injection-foundation/03-04-SUMMARY.md when done.
</output>
