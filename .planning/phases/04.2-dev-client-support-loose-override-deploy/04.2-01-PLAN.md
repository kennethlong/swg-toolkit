---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/deploy.ts
  - packages/contracts/src/changeset.ts
  - packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg
  - packages/renderer/src/services/tocReader.test.ts
  - packages/renderer/src/services/looseOverrideDeploy.test.ts
  - packages/renderer/src/services/clientSearchOrder.test.ts
  - packages/renderer/src/services/clientLayout.test.ts
  - packages/harness/fixtures-real/toc/sku0_client_header.bin
autonomous: true
requirements: [CLIENT-02, TRE-05, DEPLOY-08]
must_haves:
  truths:
    - "'loose-override' is a valid DeployModel value in @swg/contracts"
    - "LooseDeployRecord interface exists in @swg/contracts with overrideDir + writtenFiles[]{virtualPath,destPath,preExisted,snapshotPath?} + skippedDeletes:string[]"
    - "SwgChangeset.deployRecord accepts CfgDeployRecord | LooseDeployRecord (widened union)"
    - "tocReader.test.ts exists and fails (module not found — RED gate for Plan 03)"
    - "looseOverrideDeploy.test.ts exists and fails (module not found — RED gate for Plan 04)"
    - "clientSearchOrder.test.ts includes test cases for quote-stripping, looseDirs, tocEntries, searchTreeRawPriorities (RED until Plan 02)"
    - "clientLayout.test.ts includes test cases for client.cfg row and treDirFromCfg flag (RED until Plan 02)"
    - "swg-client-v2-sharedfile.cfg fixture is checked in at packages/renderer/src/test/fixtures/"
    - "sku0_client_header.bin oracle fixture exists at packages/harness/fixtures-real/toc/ (2793 bytes — CI-runnable de-anchoring gate)"
  artifacts:
    - path: "packages/contracts/src/deploy.ts"
      provides: "LooseDeployRecord type + loose-override DeployModel member"
      exports: ["LooseDeployRecord", "DeployModel"]
      contains: "snapshotPath"
    - path: "packages/contracts/src/changeset.ts"
      provides: "SwgChangeset.deployRecord widened to CfgDeployRecord | LooseDeployRecord"
      contains: "LooseDeployRecord"
    - path: "packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg"
      provides: "Byte-exact text fixture of the live swg-client-v2 [SharedFile] cfg block"
      contains: "maxSearchPriority=12"
    - path: "packages/renderer/src/services/tocReader.test.ts"
      provides: "Wave-0 RED test gate for tocReader (Plan 03); test (c) encodes binary-search-vs-KEEP-FIRST divergence via 3-entry duplicate fixture"
      contains: "parseTocHeader"
    - path: "packages/renderer/src/services/looseOverrideDeploy.test.ts"
      provides: "Wave-0 RED test gate for looseOverrideDeploy (Plan 04)"
      contains: "resolveOverrideDir"
    - path: "packages/harness/fixtures-real/toc/sku0_client_header.bin"
      provides: "2793-byte CI oracle — sku0_client.toc header (36 bytes) + tree-name block (2757 bytes)"
      contains: "binary: magic 0x544F4320 / 0x30303031, numberOfTreeFiles=131 at offset 28"
  key_links:
    - from: "packages/contracts/src/changeset.ts"
      to: "packages/contracts/src/deploy.ts"
      via: "import LooseDeployRecord from deploy.js"
      pattern: "LooseDeployRecord"
---

## Phase Goal

**As a** modder/developer, **I want to** bind a decoupled swg-client-v2 install, browse its full 131-archive asset base, and deploy an edited texture directly into the client's override dir, **so that** I can reproduce the space-terminal retexture entirely within the toolkit with no manual file copying.

<objective>
Establish the type contracts and Wave-0 test scaffolding for Phase 04.2. Extends `@swg/contracts` with the `LooseDeployRecord` type (including `snapshotPath` for pre-existing file restoration and `skippedDeletes` for the delete-action banner) and `'loose-override'` deploy model; widens `SwgChangeset.deployRecord`; checks in the swg-client-v2 cfg text fixture; checks in the sku0_client.toc CI oracle slice; and writes RED test stubs for all four Wave-0 test files. No business logic is implemented here — Plans 02–05 turn each RED suite GREEN.

Purpose: All downstream plans depend on these types at compile time and the RED test files enforce the Wave-0 completeness gate. The checked-in .toc header slice lets tocReader's oracle assertions run in CI without the full 13.8 MB file.
Output: Updated contracts, cfg fixture, .toc header oracle, four test files (tocReader RED, looseOverrideDeploy RED, clientSearchOrder partially RED, clientLayout partially RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-RESEARCH.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-PATTERNS.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-VALIDATION.md

<interfaces>
From packages/contracts/src/deploy.ts (full file already read — the current state to extend):
```typescript
// Current DeployModel (line 52):
export type DeployModel = 'absolute-path' | 'hardlink-shadow' | 'patch-prepend' | 'shadow-base';

// Add 'loose-override' member.
// Add LooseDeployRecord interface after CfgInsertionRecord (lines 58-84).
```

From packages/contracts/src/changeset.ts (line 139):
```typescript
// Current SwgChangeset.deployRecord (line 139):
deployRecord?: CfgDeployRecord;

// Widen to:
deployRecord?: CfgDeployRecord | LooseDeployRecord;
```

LooseDeployRecord shape (per B3 + MED delete-banner findings from crew review 2026-06-28):
```typescript
export interface LooseDeployRecord {
  overrideDir: string;
  writtenFiles: Array<{
    virtualPath:   string;
    destPath:      string;
    preExisted:    boolean;
    /** Absolute path to snapshot of original bytes; present when preExisted===true.
     *  resetLoose RESTORES from snapshotPath (copy back + delete snapshot), does NOT skip. */
    snapshotPath?: string;
  }>;
  /** virtualPaths of staged 'delete' entries that were skipped (loose override cannot
   *  tombstone base-archive files). Surfaced as a visible banner in DeployDialog. */
  skippedDeletes: string[];
}
```

From 04.2-RESEARCH.md DEEPENED A5 — the live swg-client-v2 [SharedFile] block (use as text fixture verbatim):
maxSearchPriority=12
TOCTreePath="D:/Code/SWGSource Client v3.0/"
searchTOC_00_0="D:/Code/SWGSource Client v3.0/sku0_client.toc"
searchTOC_01_1="D:/Code/SWGSource Client v3.0/sku1_client.toc"
searchTOC_02_2="D:/Code/SWGSource Client v3.0/sku2_client.toc"
searchTOC_03_3="D:/Code/SWGSource Client v3.0/sku3_client.toc"
searchPath_00_5="D:/Code/swg-client-v2/stage/ilm_extract"
searchTree_00_7="D:/Code/SWGSource Client v3.0/disable_wayfar_dearic_snow.tre"
searchTree_00_8="D:/Code/SWGSource Client v3.0/swgsource_3.0.tre"
searchPath_00_9="D:/Code/SWGSource Client v3.0/"
searchPath_00_10="D:/Code/swg-client-v2/stage/override"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend contracts + write cfg text fixture</name>
  <read_first>
    - packages/contracts/src/deploy.ts (full — extend DeployModel union + add LooseDeployRecord after CfgInsertionRecord)
    - packages/contracts/src/changeset.ts:115-145 (SwgChangeset.deployRecord field to widen + import to add)
    - 04.2-PATTERNS.md §contracts/src/deploy.ts (doc-comment style)
    - 04.2-PATTERNS.md §contracts/src/changeset.ts (deployRecord union broadening)
  </read_first>
  <action>
    Extend `packages/contracts/src/deploy.ts`: add `'loose-override'` to the `DeployModel` union (between `'hardlink-shadow'` and `'patch-prepend'`); append the `LooseDeployRecord` interface after `CfgInsertionRecord` following the exact doc-comment style already in the file (cite DEPLOY-08 and looseOverrideDeploy.ts as source). `LooseDeployRecord` shape (from interfaces block above): `overrideDir: string`; `writtenFiles: Array<{virtualPath: string; destPath: string; preExisted: boolean; snapshotPath?: string}>` (per-file reset manifest — files where `preExisted===true` have their snapshot restored on reset, NOT skipped; files where `preExisted===false` are deleted); `skippedDeletes: string[]` (virtualPaths of staged 'delete' entries skipped because loose override cannot tombstone base-archive files — surfaced as a visible banner in DeployDialog). Re-export `LooseDeployRecord` via `packages/contracts/src/index.ts` if it is not already re-exported via the existing deploy.ts barrel.

    Extend `packages/contracts/src/changeset.ts`: add `import type { LooseDeployRecord } from './deploy.js'` at the top alongside the existing `CfgDeployRecord` import; widen `SwgChangeset.deployRecord` from `CfgDeployRecord` to `CfgDeployRecord | LooseDeployRecord`. Add a brief doc-comment noting that `resetLoose` uses the `LooseDeployRecord` branch while the existing `deactivatePatch`/`cfgActivator` path uses `CfgDeployRecord`.

    Write `packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` as a plain text file containing only the `[SharedFile]` section of the live swg-client-v2 `client.cfg` (verbatim from DEEPENED A5 in 04.2-RESEARCH.md — see interfaces above). The file is a byte-exact text oracle for the cfg-parsing tests in Plans 02 and 04. No other sections; no comments added.
  </action>
  <acceptance_criteria>
    - `pnpm -w tsc --noEmit` exits 0 with no new errors
    - `grep -c "loose-override" packages/contracts/src/deploy.ts` returns 1
    - `grep -c "LooseDeployRecord" packages/contracts/src/deploy.ts` returns at least 1
    - `grep -c "snapshotPath" packages/contracts/src/deploy.ts` returns at least 1
    - `grep -c "skippedDeletes" packages/contracts/src/deploy.ts` returns at least 1
    - `grep -c "LooseDeployRecord" packages/contracts/src/changeset.ts` returns at least 1
    - `grep -c "maxSearchPriority=12" packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` returns 1
    - `grep -c "searchPath_00_10" packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` returns 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Contracts compile clean with LooseDeployRecord exported (snapshotPath + skippedDeletes present); cfg fixture checked in with maxSearchPriority=12 and searchPath_00_10 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test stubs (tocReader, looseOverrideDeploy, extend clientSearchOrder + clientLayout) + check in .toc oracle fixture</name>
  <read_first>
    - 04.2-VALIDATION.md (per-task verification map — rows for TRE-05 + CLIENT-02 + DEPLOY-08; exact test behaviors to encode; note B3/H2/H1/path-safety rows added by crew review)
    - 04.2-PATTERNS.md §tocReader.test.ts (test structure with REAL_TOC skipIf + synthetic fixture pattern)
    - 04.2-PATTERNS.md §looseOverrideDeploy.test.ts (temp-dir structure + key test cases)
    - 04.2-PATTERNS.md §clientSearchOrder.test.ts — extend section (new cases to add inside existing describe block)
    - 04.2-PATTERNS.md §clientLayout.ts — KNOWN_LAYOUTS shape for the client.cfg row (what tests (e)+(f) check)
    - packages/renderer/src/services/clientSearchOrder.test.ts (full — append without modifying existing passing tests)
    - packages/renderer/src/services/clientLayout.test.ts (full — append after test (d))
    - packages/renderer/src/test/fixtures/fakeClientDir.ts (to know the buildFakeClientDir API for layout tests)
  </read_first>
  <action>
    First, create the CI oracle fixture: using Node.js `fs`, create `packages/harness/fixtures-real/toc/` directory (recursive mkdirSync, no-op if it already exists), then read bytes 0–2792 (inclusive, totalling 2793 bytes) from `D:/Code/SWGSource Client v3.0/sku0_client.toc` (confirmed present per RESEARCH.md §Environment Availability) and write them to `packages/harness/fixtures-real/toc/sku0_client_header.bin`. This slice is exactly the 36-byte header plus the 2757-byte tree-name block (sizeOfTreeFileNameBlock=2757 is at header offset 32 — 36 + 2757 = 2793); it contains only the magic header and null-terminated .tre name list, no asset payload. Do NOT copy the full 4.6 MB file.

    Create `packages/renderer/src/services/tocReader.test.ts`. Use `// @vitest-environment node` at the top. Import `parseTocHeader`, `readTocTreeNames`, and `readTocIndex` from `./tocReader` (module will NOT exist yet — imports fail → RED). Also import `crc32` from `./crc32` (will also NOT exist yet — RED; both resolve after Plan 03). Write five test cases inside a `describe('tocReader')` block: (a) CI oracle (no skipIf): resolve the fixture path as `path.resolve(__dirname, '../../../harness/fixtures-real/toc/sku0_client_header.bin')`; load with `fs.readFileSync`; pass the first 36 bytes to `parseTocHeader` and assert `numberOfFiles===193475`, `numberOfTreeFiles===131`, `sizeOfTOC===4643400`, magic validates as `' COT1000'`; also call `readTocTreeNames(fixturePath)` with the fixture file path and assert `length===131` and `[0]==='bottom.tre'`; (b) synthetic (no skipIf): construct a 36-byte `Buffer` (token `0x544F4320`, version `0x30303031`, compressor bytes 0, `numberOfFiles=193475` at offset 12, `sizeOfTOC=4643400` at 16, `numberOfTreeFiles=131` at 28, `sizeOfTreeFileNameBlock=2757` at 32) and assert `parseTocHeader` parses it correctly; (c) H1 synthetic multi-tre fixture — DEMONSTRATES binary-search divergence from KEEP FIRST (no skipIf, CI always runs): build a 3-entry binary TOC buffer with a TRUE DUPLICATE path to prove that KEEP FIRST returns the wrong treeFileIndex while binary search returns the correct one. Header: numberOfFiles=3, numberOfTreeFiles=3, tocCompressor=0, fileNameBlockCompressor=0. Tree names block: "patch_15_02.tre\0patch_24_client_00.tre\0extra_padding.tre\0" (3 NUL-terminated names; treeFileIndex=0→patch_15_02.tre, treeFileIndex=1→patch_24_client_00.tre, treeFileIndex=2→extra_padding.tre). Three `TableOfContentsEntry` records SORTED BY CRC ASCENDING — compute `sfCrc = crc32('string/ja/space/space_faction.stf')` and `padCrc = crc32('zzz_padding.txt')` at test runtime; add a test comment asserting `sfCrc < padCrc` (sort-order precondition; if this fails the fixture is malformed): entry[0]: treeFileIndex=1 (patch_24_client_00 analogue — LOWER array index; this is what KEEP FIRST / lowest-index scan would return → WRONG), crc=sfCrc, path="string/ja/space/space_faction.stf"; entry[1]: treeFileIndex=0 (patch_15_02 analogue — binary search lands here at mid=(0+2)/2=1 → CORRECT), crc=sfCrc, path="string/ja/space/space_faction.stf" (TRUE DUPLICATE of entry[0], same crc, same path); entry[2]: treeFileIndex=2, crc=padCrc, path="zzz_padding.txt". Name block contains both unique path strings NUL-terminated. Add test comment: "EXPECTED VALUE DERIVATION: binary search lo=0 hi=2 mid=1 → entry[1].crc==sfCrc, entry[1].name matches → found at mid=1 → treeFileIndex=0 (patch_15_02.tre). KEEP FIRST returns entry[0].treeFileIndex=1 → WRONG — this assertion catches a KEEP FIRST implementation." Call `readTocIndex(tmpFilePath)` and assert `resolve('string/ja/space/space_faction.stf')` returns `{treeFileIndex: 0, treName: 'patch_15_02.tre'}`; (d) optional real-file (skipIf): use `it.skipIf(!hasRealToc)` for a supplementary call against the full `D:/Code/SWGSource Client v3.0/sku0_client.toc` to assert `readTocIndex` resolves `'string/ja/space/space_faction.stf'` to `treeFileIndex===42` — this is the de-anchoring counterexample from GIVEN A8 and the byte-exact CRC-port gate (wrong CRC → wrong mid → this assertion fails); (e) bounds check (no skipIf): supply a 36-byte buffer with `sizeOfTreeFileNameBlock > 0` and verify `readTocTreeNames` throws when the name block would exceed the file size. All tests RED because `./tocReader` and `./crc32` do not exist yet.

    Create `packages/renderer/src/services/looseOverrideDeploy.test.ts`. Use `// @vitest-environment node`. Import `resolveOverrideDir`, `deployLoose`, `resetLoose` from `./looseOverrideDeploy` (does not exist → RED). Use a real temp-dir setup (`mkdtempSync`/`rmSync` in beforeEach/afterEach per PATTERNS.md §looseOverrideDeploy.test.ts). Write seven test cases: (a) `resolveOverrideDir` returns `searchPath_00_10` (priority 10 = highest) — NOT `_00_9` or `_00_5` — when given the swg-client-v2-sharedfile.cfg fixture path; (b) `deployLoose` writes a file at `<overrideDir>/<virtualPath>` from a mock `replacementFilePath`; (c) `deployLoose` rejects path-traversal virtualPaths (`'../outside.dds'`, `'..\\outside'`) by throwing; (d) `deployLoose` rejects drive-relative virtualPath `'C:foo'` by throwing (isVirtualPathSafe must reject `^[A-Za-z]:`); (e) confinement check: a virtualPath that resolves to `overrideDir_evil/file` (outside the override dir) is rejected (dest.startsWith(resolve(root)+sep) check, not just startsWith(root)); (f) B3 sha256 restore — create a pre-existing file at `<overrideDir>/texture/test.dds` with known bytes (sha256_A); deploy an edited version; then `resetLoose` the record; verify sha256(restored file) == sha256_A (file restored, not removed); (g) H2 cross-deploy prune — deploy `{A.dds, B.dds}` (prior record), then deploy `{A.dds}` with prior record passed as option; assert B.dds is gone after the second deploy, A.dds is present. All tests RED (module missing).

    Extend `packages/renderer/src/services/clientSearchOrder.test.ts`: append new test cases INSIDE the existing `describe('resolveClientMountOrder', ...)` block without modifying any existing passing tests. Add: (a) quote-stripping: a cfg with `searchTOC_00_0="path with spaces/sku0.toc"` should return the value without surrounding quotes in `tocEntries[0].tocFilePath`; (b) looseDirs ordered by priority DESC: a cfg with three searchPath entries at priorities 5, 9, 10 should yield `looseDirs` array ordered `[priority-10-path, priority-9-path, priority-5-path]` and `looseDirPriorities=[10,9,5]`; (c) searchTreeRawPriorities: a cfg with two searchTree entries at priorities 7 and 8 should yield `searchTreeRawPriorities=[8,7]` (parallel to `trePaths`); (d) regression: an existing searchTree-only cfg (no searchPath, no searchTOC) returns a `ClientMountOrder` whose `trePaths` and `priorities` are byte-identical to the result before this extension. These tests reference `looseDirs`, `tocEntries`, `searchTreeRawPriorities`, `looseDirPriorities` on the return value, which `resolveClientMountOrder` does not yet return → RED.

    Extend `packages/renderer/src/services/clientLayout.test.ts`: append two new test cases after test (d) in the existing `describe` block: (e) a directory with `client.cfg` present and ZERO `.tre` files should classify as `resolveLayout` returning `layout.release === 'swg-client-v2'` and `layout.treDirFromCfg === true`; (f) regression: tests (a) and (b) from the existing suite still pass with the new `KNOWN_LAYOUTS` (Infinity=`treSubdir:'Live'`, SWGEmu=`treSubdir:''`). Tests (e) and (f-new) go RED because `KNOWN_LAYOUTS` has no `client.cfg` row yet. Existing tests (a)–(d) remain GREEN.
  </action>
  <acceptance_criteria>
    - `tocReader.test.ts` exists: `grep -c "parseTocHeader" packages/renderer/src/services/tocReader.test.ts` returns at least 1
    - `grep -c "readTocIndex" packages/renderer/src/services/tocReader.test.ts` returns at least 1 (H1 counterexample test present)
    - `grep -c "patch_15_02" packages/renderer/src/services/tocReader.test.ts` returns at least 1 (space_faction.stf counterexample encoded)
    - `grep -c "crc32" packages/renderer/src/services/tocReader.test.ts` returns at least 1 (crc32 import present for fixture construction)
    - `looseOverrideDeploy.test.ts` exists: `grep -c "resolveOverrideDir" packages/renderer/src/services/looseOverrideDeploy.test.ts` returns at least 1
    - `grep -c "sha256\|createHash" packages/renderer/src/services/looseOverrideDeploy.test.ts` returns at least 1 (B3 restore test present)
    - `grep -c "priorRecord\|H2\|prune" packages/renderer/src/services/looseOverrideDeploy.test.ts` returns at least 1 (H2 prune test present)
    - tocReader tests are RED (module missing): `pnpm --filter @swg/renderer test -- --run src/services/tocReader.test.ts 2>&1` contains "Cannot find module" or similar import error
    - looseOverrideDeploy tests are RED: `pnpm --filter @swg/renderer test -- --run src/services/looseOverrideDeploy.test.ts 2>&1` contains "Cannot find module"
    - Existing clientSearchOrder tests still pass: `pnpm --filter @swg/renderer test -- --run src/services/clientSearchOrder.test.ts 2>&1` shows all ORIGINAL tests passing (new extended tests fail on looseDirs/tocEntries/searchTreeRawPriorities — expected RED)
    - Existing clientLayout tests (a)–(d) still pass; test (e) RED (no client.cfg row yet)
    - Oracle fixture created: `node -e "const b=require('fs').readFileSync('packages/harness/fixtures-real/toc/sku0_client_header.bin');process.exit(b.length===2793&&b.readUInt32LE(28)===131?0:1)"` exits 0
    - tocReader.test.ts references the fixture: `grep -c "sku0_client_header.bin" packages/renderer/src/services/tocReader.test.ts` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/clientLayout.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All six test files exist; existing suites still pass; new test cases are RED (including H1 3-entry duplicate fixture with binary-search derivation comment, B3 sha256 restore, H2 prune, path-safety cases); contracts compile clean; sku0_client_header.bin oracle checked in (2793 bytes, CI-runnable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts → renderer | Type contracts consumed by all services; shape errors caught at tsc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Tampering | looseOverrideDeploy.ts virtualPath join | mitigate | `isVirtualPathSafe` guard (tightened: rejects `^[A-Za-z]:`) + `dest.startsWith(resolve(root) + path.sep)` confinement check (trailing-sep form, fixes prefix-collision) — test cases (c),(d),(e) encode these requirements |
| T-04.2-02 | Tampering | looseOverrideDeploy.ts reset flow | mitigate | `preExisted` + `snapshotPath` in LooseDeployRecord; resetLoose RESTORES snapshots (not skips) for preExisted=true files — test case (f) encodes the sha256 restore invariant |
| T-04.2-03 | Tampering | resolveOverrideDir — wrong searchPath selected | mitigate | MAX-priority searchPath only; override-dir guard warns if priority > maxSearchPriority — test case (a) encodes the requirement |
| T-04.2-04 | DoS | tocReader.ts — malformed .toc header | mitigate | Bounds-check header fields against file length (planned in Plan 03; test case (e) encodes this) |
| T-04.2-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase — only TS source + contracts changes |
</threat_model>

<verification>
Full suite: `pnpm -w tsc --noEmit` green + all four test files exist + existing tests in clientLayout/clientSearchOrder remain passing + sku0_client_header.bin oracle present + `snapshotPath`/`skippedDeletes` in LooseDeployRecord.
</verification>

<success_criteria>
- `pnpm -w tsc --noEmit` exits 0 (contracts compile with new types)
- `LooseDeployRecord` exported from `@swg/contracts` with `snapshotPath?` and `skippedDeletes` fields
- `'loose-override'` in `DeployModel` union
- `SwgChangeset.deployRecord` is `CfgDeployRecord | LooseDeployRecord`
- `swg-client-v2-sharedfile.cfg` fixture present and contains `searchPath_00_10`
- tocReader.test.ts exists (RED — module missing); includes H1 3-entry duplicate fixture with `crc32` import, binary-search derivation comment, and assertion `treeFileIndex===0` (binary search result at mid=1, NOT the KEEP FIRST treeFileIndex=1)
- sku0_client_header.bin oracle committed at packages/harness/fixtures-real/toc/ (2793 bytes)
- looseOverrideDeploy.test.ts exists (RED — module missing); includes B3 sha256 restore test + H2 prune test + path-safety cases
- clientSearchOrder.test.ts extended with quote-strip + looseDirs + searchTreeRawPriorities + regression tests
- clientLayout.test.ts extended with client.cfg row test (RED) + regression assertions
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-01-SUMMARY.md` when done.
</output>
