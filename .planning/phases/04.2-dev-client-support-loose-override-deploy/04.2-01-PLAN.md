---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/deploy.ts
  - packages/contracts/src/changeset.ts
  - packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg
  - packages/renderer/src/services/tocReader.test.ts
  - packages/renderer/src/services/looseOverrideDeploy.test.ts
  - packages/renderer/src/services/clientSearchOrder.test.ts
  - packages/renderer/src/services/clientLayout.test.ts
autonomous: true
requirements: [CLIENT-02, TRE-05, DEPLOY-08]
must_haves:
  truths:
    - "'loose-override' is a valid DeployModel value in @swg/contracts"
    - "LooseDeployRecord interface exists in @swg/contracts with overrideDir + writtenFiles[]{virtualPath,destPath,preExisted}"
    - "SwgChangeset.deployRecord accepts CfgDeployRecord | LooseDeployRecord (widened union)"
    - "tocReader.test.ts exists and fails (module not found — RED gate for Plan 03)"
    - "looseOverrideDeploy.test.ts exists and fails (module not found — RED gate for Plan 04)"
    - "clientSearchOrder.test.ts includes test cases for quote-stripping, looseDirs, tocEntries (RED until Plan 02)"
    - "clientLayout.test.ts includes test cases for client.cfg row and treDirFromCfg flag (RED until Plan 02)"
    - "swg-client-v2-sharedfile.cfg fixture is checked in at packages/renderer/src/test/fixtures/"
  artifacts:
    - path: "packages/contracts/src/deploy.ts"
      provides: "LooseDeployRecord type + loose-override DeployModel member"
      exports: ["LooseDeployRecord", "DeployModel"]
    - path: "packages/contracts/src/changeset.ts"
      provides: "SwgChangeset.deployRecord widened to CfgDeployRecord | LooseDeployRecord"
      contains: "LooseDeployRecord"
    - path: "packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg"
      provides: "Byte-exact text fixture of the live swg-client-v2 [SharedFile] cfg block"
      contains: "maxSearchPriority=12"
    - path: "packages/renderer/src/services/tocReader.test.ts"
      provides: "Wave-0 RED test gate for tocReader (Plan 03)"
      contains: "parseTocHeader"
    - path: "packages/renderer/src/services/looseOverrideDeploy.test.ts"
      provides: "Wave-0 RED test gate for looseOverrideDeploy (Plan 04)"
      contains: "resolveOverrideDir"
  key_links:
    - from: "packages/contracts/src/changeset.ts"
      to: "packages/contracts/src/deploy.ts"
      via: "import LooseDeployRecord from deploy.js"
      pattern: "LooseDeployRecord"
---

## Phase Goal

**As a** modder/developer, **I want to** bind a decoupled swg-client-v2 install, browse its full 131-archive asset base, and deploy an edited texture directly into the client's override dir, **so that** I can reproduce the space-terminal retexture entirely within the toolkit with no manual file copying.

<objective>
Establish the type contracts and Wave-0 test scaffolding for Phase 04.2. Extends `@swg/contracts` with the `LooseDeployRecord` type and `'loose-override'` deploy model; widens `SwgChangeset.deployRecord`; checks in the swg-client-v2 cfg text fixture; and writes RED test stubs for all four Wave-0 test files. No business logic is implemented here — Plans 02–05 turn each RED suite GREEN.

Purpose: All downstream plans depend on these types at compile time and the RED test files enforce the Wave-0 completeness gate.
Output: Updated contracts, cfg fixture, four test files (tocReader RED, looseOverrideDeploy RED, clientSearchOrder partially RED, clientLayout partially RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-RESEARCH.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-PATTERNS.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-VALIDATION.md

<interfaces>
From packages/contracts/src/deploy.ts (full file already read — the current state to extend):
```typescript
// Current DeployModel (line 52):
export type DeployModel = 'absolute-path' | 'hardlink-shadow' | 'patch-prepend' | 'shadow-base';

// Add 'loose-override' member.
// Add LooseDeployRecord interface after CfgInsertionRecord (lines 58-84).
```

From packages/contracts/src/changeset.ts (line 139):
```typescript
// Current SwgChangeset.deployRecord (line 139):
deployRecord?: CfgDeployRecord;

// Widen to:
deployRecord?: CfgDeployRecord | LooseDeployRecord;
```

From 04.2-PATTERNS.md §contracts/src/deploy.ts — LooseDeployRecord shape:
```typescript
export interface LooseDeployRecord {
  overrideDir: string;
  writtenFiles: Array<{
    virtualPath: string;
    destPath:    string;
    preExisted:  boolean;
  }>;
}
```

From 04.2-RESEARCH.md DEEPENED A5 — the live swg-client-v2 [SharedFile] block (use as text fixture verbatim):
maxSearchPriority=12
TOCTreePath="D:/Code/SWGSource Client v3.0/"
searchTOC_00_0="D:/Code/SWGSource Client v3.0/sku0_client.toc"
searchTOC_01_1="D:/Code/SWGSource Client v3.0/sku1_client.toc"
searchTOC_02_2="D:/Code/SWGSource Client v3.0/sku2_client.toc"
searchTOC_03_3="D:/Code/SWGSource Client v3.0/sku3_client.toc"
searchPath_00_5="D:/Code/swg-client-v2/stage/ilm_extract"
searchTree_00_7="D:/Code/SWGSource Client v3.0/disable_wayfar_dearic_snow.tre"
searchTree_00_8="D:/Code/SWGSource Client v3.0/swgsource_3.0.tre"
searchPath_00_9="D:/Code/SWGSource Client v3.0/"
searchPath_00_10="D:/Code/swg-client-v2/stage/override"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend contracts + write cfg text fixture</name>
  <read_first>
    - packages/contracts/src/deploy.ts (full — extend DeployModel union + add LooseDeployRecord after CfgInsertionRecord)
    - packages/contracts/src/changeset.ts:115-145 (SwgChangeset.deployRecord field to widen + import to add)
    - 04.2-PATTERNS.md §contracts/src/deploy.ts (exact LooseDeployRecord shape + doc-comment style)
    - 04.2-PATTERNS.md §contracts/src/changeset.ts (deployRecord union broadening)
  </read_first>
  <action>
    Extend `packages/contracts/src/deploy.ts`: add `'loose-override'` to the `DeployModel` union (between `'hardlink-shadow'` and `'patch-prepend'`); append the `LooseDeployRecord` interface after `CfgInsertionRecord` following the exact doc-comment style already in the file (cite DEPLOY-08 and looseOverrideDeploy.ts as source). `LooseDeployRecord` has: `overrideDir: string` (absolute path to the override dir files were written into); `writtenFiles: Array<{virtualPath: string; destPath: string; preExisted: boolean}>` (per-file reset manifest — only files where `preExisted === false` are deleted on reset). Re-export `LooseDeployRecord` via `packages/contracts/src/index.ts` if it is not already re-exported via the existing deploy.ts barrel.

    Extend `packages/contracts/src/changeset.ts`: add `import type { LooseDeployRecord } from './deploy.js'` at the top alongside the existing `CfgDeployRecord` import; widen `SwgChangeset.deployRecord` from `CfgDeployRecord` to `CfgDeployRecord | LooseDeployRecord`. Add a brief doc-comment noting that `resetLoose` uses the `LooseDeployRecord` branch while the existing `deactivatePatch`/`cfgActivator` path uses `CfgDeployRecord`.

    Write `packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` as a plain text file containing only the `[SharedFile]` section of the live swg-client-v2 `client.cfg` (verbatim from DEEPENED A5 in 04.2-RESEARCH.md — see interfaces above). The file is a byte-exact text oracle for the cfg-parsing tests in Plans 02 and 04. No other sections; no comments added.
  </action>
  <acceptance_criteria>
    - `pnpm -w tsc --noEmit` exits 0 with no new errors
    - `grep -c "loose-override" packages/contracts/src/deploy.ts` returns 1
    - `grep -c "LooseDeployRecord" packages/contracts/src/deploy.ts` returns at least 1
    - `grep -c "LooseDeployRecord" packages/contracts/src/changeset.ts` returns at least 1
    - `grep -c "maxSearchPriority=12" packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` returns 1
    - `grep -c "searchPath_00_10" packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` returns 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Contracts compile clean with LooseDeployRecord exported; cfg fixture checked in with maxSearchPriority=12 and searchPath_00_10 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test stubs (tocReader, looseOverrideDeploy, extend clientSearchOrder + clientLayout)</name>
  <read_first>
    - 04.2-VALIDATION.md (per-task verification map — rows for TRE-05 + CLIENT-02 + DEPLOY-08; exact test behaviors to encode)
    - 04.2-PATTERNS.md §tocReader.test.ts (test structure with REAL_TOC skipIf + synthetic fixture pattern)
    - 04.2-PATTERNS.md §looseOverrideDeploy.test.ts (temp-dir structure + key test cases)
    - 04.2-PATTERNS.md §clientSearchOrder.test.ts — extend section (new cases to add inside existing describe block)
    - 04.2-PATTERNS.md §clientLayout.ts — KNOWN_LAYOUTS shape for the client.cfg row (what tests (e)+(f) check)
    - packages/renderer/src/services/clientSearchOrder.test.ts (full — append without modifying existing passing tests)
    - packages/renderer/src/services/clientLayout.test.ts (full — append after test (d))
    - packages/renderer/src/test/fixtures/fakeClientDir.ts (to know the buildFakeClientDir API for layout tests)
  </read_first>
  <action>
    Create `packages/renderer/src/services/tocReader.test.ts`. Use `// @vitest-environment node` at the top. Import `parseTocHeader` and `readTocTreeNames` from `./tocReader` (module will NOT exist yet — imports fail → RED). Write three test cases inside a `describe('tocReader')` block: (a) skipIf real-toc absent: `parseTocHeader` on `D:/Code/SWGSource Client v3.0/sku0_client.toc` returns `numberOfFiles===193475`, `numberOfTreeFiles===131`, `sizeOfTOC===4643400`, magic validates as `' COT1000'`; (b) synthetic: construct a 36-byte `Buffer` (token `0x544F4320`, version `0x30303031`, compressor bytes 0, `numberOfFiles=193475` at offset 12, `sizeOfTOC=4643400` at 16, `numberOfTreeFiles=131` at 28, `sizeOfTreeFileNameBlock=2757` at 32) and assert `parseTocHeader` parses it correctly; (c) skipIf absent: `readTocTreeNames` on the real .toc returns an array of length 131 whose first element equals `'bottom.tre'`. Use the `REAL_TOC` constant pattern from PATTERNS.md (hasRealToc skipIf guard). The file imports from `./tocReader` which does not exist → module resolution fails → all tests RED.

    Create `packages/renderer/src/services/looseOverrideDeploy.test.ts`. Use `// @vitest-environment node`. Import `resolveOverrideDir`, `deployLoose`, `resetLoose` from `./looseOverrideDeploy` (does not exist → RED). Use a real temp-dir setup (`mkdtempSync`/`rmSync` in beforeEach/afterEach per PATTERNS.md §looseOverrideDeploy.test.ts). Write five test cases: (a) `resolveOverrideDir` returns `searchPath_00_10` (priority 10 = highest) — NOT `_00_9` or `_00_5` — when given the swg-client-v2-sharedfile.cfg fixture path; (b) `deployLoose` writes a file at `<overrideDir>/<virtualPath>` from a mock `replacementFilePath`; (c) `deployLoose` rejects path-traversal virtualPaths (`'../outside.dds'`, `'..\\outside'`) by throwing; (d) `resetLoose` deletes only files whose `preExisted===false`; pre-existing files remain; (e) sha256 of a mock `client.cfg` file is byte-identical before and after `deployLoose`+`resetLoose` (the cfg is never touched). All tests RED (module missing).

    Extend `packages/renderer/src/services/clientSearchOrder.test.ts`: append new test cases INSIDE the existing `describe('resolveClientMountOrder', ...)` block without modifying any existing passing tests. Add: (a) quote-stripping: a cfg with `searchTOC_00_0="path with spaces/sku0.toc"` should return the value without surrounding quotes in the new `tocEntries[0].tocFilePath`; (b) looseDirs ordered by priority DESC: a cfg with three searchPath entries at priorities 5, 9, 10 should yield `looseDirs` array ordered `[priority-10-path, priority-9-path, priority-5-path]`; (c) regression: an existing searchTree-only cfg (no searchPath, no searchTOC) returns a `ClientMountOrder` whose `trePaths` and `priorities` are byte-identical to the result before this extension (pick one existing test's input and assert the same `trePaths`+`priorities`). These tests reference `looseDirs` and `tocEntries` on the return value, which `resolveClientMountOrder` does not yet return → tests fail at property access → RED.

    Extend `packages/renderer/src/services/clientLayout.test.ts`: append two new test cases after test (d) in the existing `describe` block: (e) a directory with `client.cfg` present and ZERO `.tre` files should classify as `resolveLayout` returning `layout.release === 'swg-client-v2'` and `layout.treDirFromCfg === true` (write `client.cfg` to `tmpDir`, no `.tre` files); (f) regression: tests (a) and (b) from the existing suite still pass with the new `KNOWN_LAYOUTS` (verify Infinity=`treSubdir:'Live'` and SWGEmu=`treSubdir:''` — these MUST NOT be broken). Tests (e) and (f-new) go RED because `KNOWN_LAYOUTS` has no `client.cfg` row yet. Existing tests (a)–(d) remain GREEN.
  </action>
  <acceptance_criteria>
    - `tocReader.test.ts` exists: `grep -c "parseTocHeader" packages/renderer/src/services/tocReader.test.ts` returns at least 1
    - `looseOverrideDeploy.test.ts` exists: `grep -c "resolveOverrideDir" packages/renderer/src/services/looseOverrideDeploy.test.ts` returns at least 1
    - tocReader tests are RED (module missing): `pnpm --filter @swg/renderer test -- --run src/services/tocReader.test.ts 2>&1` contains "Cannot find module" or similar import error
    - looseOverrideDeploy tests are RED: `pnpm --filter @swg/renderer test -- --run src/services/looseOverrideDeploy.test.ts 2>&1` contains "Cannot find module"
    - Existing clientSearchOrder tests still pass: `pnpm --filter @swg/renderer test -- --run src/services/clientSearchOrder.test.ts 2>&1` shows all ORIGINAL tests passing (new extended tests fail on looseDirs/tocEntries property access — that is expected RED)
    - Existing clientLayout tests (a)–(d) still pass: `pnpm --filter @swg/renderer test -- --run src/services/clientLayout.test.ts 2>&1` shows tests (a)–(d) GREEN; test (e) RED (no client.cfg row yet)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/clientLayout.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All six test files exist; existing suites still pass; new test cases are RED; contracts compile clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts → renderer | Type contracts consumed by all services; shape errors caught at tsc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Tampering | looseOverrideDeploy.ts virtualPath join | mitigate | `isVirtualPathSafe` guard before joining virtualPath to overrideDir (planned in Plan 04 — test case (c) written here encodes the requirement) |
| T-04.2-02 | Tampering | looseOverrideDeploy.ts reset flow | mitigate | `preExisted` flag in LooseDeployRecord; resetLoose only removes files it created (test case (d) encodes the requirement) |
| T-04.2-03 | Tampering | resolveOverrideDir — wrong searchPath selected | mitigate | MAX-priority searchPath only (test case (a) encodes the requirement; rejection at the wrong dir is verified by test) |
| T-04.2-04 | DoS | tocReader.ts — malformed .toc header | mitigate | Bounds-check header fields against file length (planned in Plan 03; test suite created here) |
| T-04.2-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase — only TS source + contracts changes |
</threat_model>

<verification>
Full suite: `pnpm -w tsc --noEmit` green + all four test files exist + existing tests in clientLayout/clientSearchOrder remain passing.
</verification>

<success_criteria>
- `pnpm -w tsc --noEmit` exits 0 (contracts compile with new types)
- `LooseDeployRecord` exported from `@swg/contracts`
- `'loose-override'` in `DeployModel` union
- `SwgChangeset.deployRecord` is `CfgDeployRecord | LooseDeployRecord`
- `swg-client-v2-sharedfile.cfg` fixture present and contains `searchPath_00_10`
- tocReader.test.ts exists (RED — module missing)
- looseOverrideDeploy.test.ts exists (RED — module missing)
- clientSearchOrder.test.ts extended with quote-strip + looseDirs + regression tests
- clientLayout.test.ts extended with client.cfg row test (RED) + regression assertions
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-01-SUMMARY.md` when done.
</output>
