---
phase: 04.2-dev-client-support-loose-override-deploy
plan: "02"
subsystem: renderer/services
tags:
  - client-detection
  - cfg-parsing
  - searchTOC
  - searchPath
  - loose-override
  - tdd-green
dependency_graph:
  requires:
    - "04.2-01 (ClientLayout.treDirFromCfg type + RED test stubs)"
  provides:
    - resolveLayout classifies swg-client-v2 (client.cfg, 0 local .tre) → release='swg-client-v2', treDirFromCfg=true
    - KNOWN_LAYOUTS extended with client.cfg row (last entry — T-04.2-06 probe order)
    - clientLocator.ts knownPaths includes swg-client-v2 at D:\Code\swg-client-v2\stage-x64
    - addManualClient opts{ cfgFileName, treDir } bypass resolveLayout for decoupled installs
    - parseSearchNodes (exported) — parses searchTree + searchTOC + searchPath + TOCTreePath
    - stripQuotes (exported) — T-04.2-07 quote-stripping for all cfg path values
    - resolveClientMountOrder two-arg form (cfgPath as file OR directory)
    - ClientMountOrder with looseDirs/looseDirPriorities/tocEntries/tocTreePaths/searchTreeRawPriorities/maxPriority
  affects:
    - packages/renderer/src/services/clientLayout.ts
    - packages/renderer/src/services/clientLocator.ts
    - packages/renderer/src/services/clientSearchOrder.ts
tech_stack:
  added: []
  patterns:
    - treDirFromCfg gate in resolveLayout (skip .tre check for decoupled installs)
    - two-arg cfgPath=file|dir detection via fs.statSync wrapped in try/catch
    - parallel raw-priority arrays (searchTreeRawPriorities, looseDirPriorities) for Plan 03 compaction
    - stripQuotes applied to all searchTOC/searchPath/TOCTreePath values before path use (T-04.2-07)
    - shared seen Set de-dup across all three families
key_files:
  created: []
  modified:
    - packages/renderer/src/services/clientLayout.ts
    - packages/renderer/src/services/clientLocator.ts
    - packages/renderer/src/services/clientSearchOrder.ts
decisions:
  - "client.cfg row is LAST in KNOWN_LAYOUTS — T-04.2-06: Infinity/SWGEmu rows (swgemu.cfg) checked first; mutual exclusion by cfgFile prevents mis-classification"
  - "resolveLayout treDirFromCfg branch skips existsSync(treDir) + readdirSync check entirely; existsSync(cfgPath) alone is the gate; no .tre co-location required"
  - "addManualClient opts.cfgFileName bypasses resolveLayout; opts.treDir forwarded to _detectTreVersion for version probe at external data dir"
  - "parseSearchNodes returns tocTreePaths alongside entries + maxPriority; Plan 03 uses tocTreePaths to prefix constituent .tre names when expanding TOC entries"
  - "resolveClientMountOrder returns null ONLY when ALL three families have zero entries after filtering; pure-searchTOC clients return non-null with empty trePaths"
  - "looseDirPriorities and searchTreeRawPriorities are raw cfg priorities (parallel arrays); Plan 03 uses them for unified cross-family priority compaction (B2 root fix)"
  - "two-arg resolveClientMountOrder: fs.statSync(cfgPath).isFile() detect; catch block treats unreadable/absent paths as file paths (parseSearchNodes handles missing)"
metrics:
  duration_minutes: 20
  completed_date: "2026-06-29"
  tasks: 2
  files_changed: 3
---

# Phase 04.2 Plan 02: Detection Slice — clientLayout client.cfg Row + Extended clientSearchOrder (looseDirs/TOC/searchTreeRawPriorities) Summary

Turned the Plan 01 RED tests GREEN for both clientLayout and clientSearchOrder; implements the full detection spine: swg-client-v2 style install classified by client.cfg alone (no co-located .tre), and cfg chain fully parsed for searchTree + searchTOC + searchPath entries with quote-stripping, parallel raw-priority arrays, and a two-arg resolveClientMountOrder that accepts a file path or directory.

## Task Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | 5f696fc | clientLayout client.cfg row + treDirFromCfg gate; clientLocator swg-client-v2 path + addManualClient opts |
| Task 2 | ad19216 | Extend clientSearchOrder — parseSearchNodes + full ClientMountOrder (looseDirs/tocEntries/searchTreeRawPriorities) |

## Deviations from Plan

None — plan executed exactly as written.

## Verification Results

### clientLayout.test.ts — 6/6 GREEN

| Test | Result |
|------|--------|
| (a) Infinity-style dir (Live/ + swgemu.cfg) → treSubdir 'Live' | PASS |
| (b) SWGEmu-style dir (root TREs + swgemu.cfg) → treSubdir '' | PASS |
| (c) unclassifiable dir (cfg present, no .tre) → null | PASS |
| (d) ClientLayout type usable for manual override (round-trip) | PASS |
| (e) client.cfg + ZERO .tre files → release='swg-client-v2', treDirFromCfg=true | PASS (was RED) |
| (f) regression: Infinity + SWGEmu layouts unchanged after KNOWN_LAYOUTS extension | PASS |

### clientSearchOrder.test.ts — 10/10 GREEN

| Test | Result |
|------|--------|
| orders by priority DESC | PASS |
| equal priority → later cfg line wins | PASS |
| equal priority → higher sku wins | PASS |
| excludes toolkit deploy cfg + skips missing files | PASS |
| drops entries above maxSearchPriority | PASS |
| returns null when no searchTree config | PASS |
| (ext-a) quote-stripping for searchTOC value | PASS (was RED) |
| (ext-b) looseDirs priority DESC + looseDirPriorities=[10,9,5] | PASS (was RED) |
| (ext-c) searchTreeRawPriorities=[8,7] for two searchTree entries | PASS (was RED) |
| (ext-d) regression: searchTree-only cfg same trePaths+priorities | PASS |

### Acceptance Criteria

- `grep -c "treDirFromCfg" clientLayout.ts` → 5 (≥2 required) ✓
- `grep -c "swg-client-v2" clientLocator.ts` → 8 (≥1 required) ✓
- `grep -c "stripQuotes" clientSearchOrder.ts` → 7 (≥2 required) ✓
- `grep -c "looseDirs" clientSearchOrder.ts` → 11 (≥1 required) ✓
- `grep -c "looseDirPriorities" clientSearchOrder.ts` → 7 (≥1 required) ✓
- `grep -c "searchTreeRawPriorities" clientSearchOrder.ts` → 9 (≥1 required) ✓
- `grep -c "tocEntries" clientSearchOrder.ts` → 9 (≥1 required) ✓
- `grep -c "maxPriority" clientSearchOrder.ts` → 12 (≥1 required) ✓
- `grep -c "isFile|resolveLayout" clientSearchOrder.ts` → 5 (≥1 required) ✓
- TypeScript: 0 errors on packages/renderer/tsconfig.json after contracts build ✓

### Success Criteria

- resolveLayout classifies swg-client-v2 (client.cfg, 0 local .tre) → release='swg-client-v2', treDirFromCfg=true ✓
- resolveLayout still classifies Infinity/SWGEmu identically (regression) ✓
- addManualClient accepts cfgFileName + treDir opts ✓
- resolveClientMountOrder(cfgPath, installRoot) accepts cfgPath as direct file path OR install directory ✓
- resolveClientMountOrder returns ClientMountOrder with all new fields ✓
- stripQuotes exported from clientSearchOrder.ts ✓
- All clientLayout.test.ts + clientSearchOrder.test.ts tests GREEN ✓
- tsc clean (packages/renderer) ✓

## Known Stubs

None — the new fields are fully implemented and tested; no placeholder values or TODO markers in business logic.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes introduced. All new file reads are existsSync/statSync/readFileSync (read-only), consistent with the existing T-04.2-05 mitigation.

## Self-Check: PASSED

Files modified exist:
- `packages/renderer/src/services/clientLayout.ts` ✓
- `packages/renderer/src/services/clientLocator.ts` ✓
- `packages/renderer/src/services/clientSearchOrder.ts` ✓

Commits exist:
- 5f696fc ✓
- ad19216 ✓
