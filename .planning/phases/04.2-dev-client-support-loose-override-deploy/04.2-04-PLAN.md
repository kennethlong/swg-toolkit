---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 04
type: execute
wave: 3
depends_on: ["04.2-01", "04.2-02"]
files_modified:
  - packages/renderer/src/services/looseOverrideDeploy.ts
  - packages/renderer/src/services/looseOverrideDeploy.test.ts
  - packages/renderer/src/services/changesetService.ts
autonomous: true
requirements: [DEPLOY-08]
must_haves:
  truths:
    - "resolveOverrideDir(cfgPath, installRoot) returns the MAX-priority searchPath — priority 10 ('stage/override'), NOT priority 9 ('SWGSource/') or priority 5 ('ilm_extract')"
    - "deployLoose writes <overrideDir>/<virtualPath> atomically (tmp+renameSync) for each add/modify entry"
    - "deployLoose records preExisted=true for files that already existed at destPath before writing"
    - "Path-traversal virtualPaths (e.g. '../outside.dds') are rejected by isVirtualPathSafe + resolve-prefix check"
    - "resetLoose deletes ONLY files where preExisted===false; pre-existing files and client.cfg are byte-untouched"
    - "sha256 of client.cfg is identical before and after deployLoose+resetLoose cycle"
    - "looseOverrideDeploy.test.ts all tests GREEN (was RED in Plan 01)"
    - "changesetService.updateChangesetDeployRecord accepts CfgDeployRecord | LooseDeployRecord"
  artifacts:
    - path: "packages/renderer/src/services/looseOverrideDeploy.ts"
      provides: "resolveOverrideDir + deployLoose + resetLoose"
      exports: ["resolveOverrideDir", "deployLoose", "resetLoose"]
      contains: "isVirtualPathSafe"
    - path: "packages/renderer/src/services/changesetService.ts"
      provides: "updateChangesetDeployRecord broadened to CfgDeployRecord | LooseDeployRecord"
      contains: "LooseDeployRecord"
  key_links:
    - from: "packages/renderer/src/services/looseOverrideDeploy.ts"
      to: "packages/renderer/src/services/clientSearchOrder.ts"
      via: "resolveClientMountOrder(cfgPath, installRoot)?.looseDirs[0]"
      pattern: "looseDirs"
    - from: "packages/renderer/src/services/looseOverrideDeploy.ts"
      to: "packages/renderer/src/services/pathSafety.ts"
      via: "isVirtualPathSafe(entry.virtualPath)"
      pattern: "isVirtualPathSafe"
---

<objective>
Build the loose-override deploy service: `resolveOverrideDir`, `deployLoose`, `resetLoose`. This is the core deploy branch for swg-client-v2 that writes loose files directly into the max-priority `searchPath` dir — NO `packPatch`, NO `cfgActivator`, NO `client.cfg` surgery. Broaden `changesetService.updateChangesetDeployRecord` to accept `LooseDeployRecord`.

Purpose: Spine step "deploy to override dir" — after this plan, the toolkit can deploy staged edits into `stage/override/` and reset them non-destructively.
Output: looseOverrideDeploy.ts (new), looseOverrideDeploy.test.ts (GREEN), changesetService.ts (broadened).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-RESEARCH.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-PATTERNS.md

<interfaces>
From packages/renderer/src/services/pathSafety.ts (M1 single source of truth):
```typescript
export function isVirtualPathSafe(vp: string): boolean
// Rejects: '..' segments, leading '/', drive-letter (e.g. 'C:'), path-traversal patterns.
```

From packages/renderer/src/services/packPatch.ts:29,85-89 (pathSafety import + use pattern):
```typescript
import { isVirtualPathSafe } from './pathSafety';
for (const entry of staged) {
  if (!isVirtualPathSafe(entry.virtualPath)) {
    throw new Error(`Invalid virtualPath: path traversal rejected — '${entry.virtualPath}'`);
  }
}
```

From packages/renderer/src/services/packPatch.ts:99-105 (replacementFilePath reading):
```typescript
if (!s.replacementFilePath) throw new Error(`Staged entry '${s.virtualPath}' has no source file`);
const data = new Uint8Array(fs.readFileSync(s.replacementFilePath));
```

From packages/renderer/src/services/cfgActivator.ts:226-230 (atomic BOM-free write pattern):
```typescript
const tmp = outputPath + '.tmp';
fs.writeFileSync(tmp, Buffer.from(patchBytes));
fs.renameSync(tmp, outputPath);
```

From Plan 02 — resolveClientMountOrder returns ClientMountOrder with looseDirs[]:
```typescript
import { resolveClientMountOrder } from './clientSearchOrder';
// looseDirs[0] = highest-priority searchPath dir (already sorted priority DESC)
function resolveOverrideDir(cfgPath: string, installRoot: string): string | null {
  return resolveClientMountOrder(cfgPath, installRoot)?.looseDirs[0] ?? null;
}
```

From Plan 01 — LooseDeployRecord type (in @swg/contracts):
```typescript
export interface LooseDeployRecord {
  overrideDir: string;
  writtenFiles: Array<{ virtualPath: string; destPath: string; preExisted: boolean }>;
}
```

From packages/renderer/src/services/changesetService.ts:449-475 (updateChangesetDeployRecord to broaden):
```typescript
export function updateChangesetDeployRecord(csId: string, record: CfgDeployRecord): void {
  // widen record type to CfgDeployRecord | LooseDeployRecord
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: looseOverrideDeploy.ts — resolveOverrideDir + deployLoose + resetLoose (GREEN)</name>
  <read_first>
    - 04.2-PATTERNS.md §looseOverrideDeploy.ts (full — imports, resolveOverrideDir, deployLoose, resetLoose implementation notes, key implementation notes at the bottom)
    - 04.2-RESEARCH.md §Capability 3 (deploy flow steps 1-4; pitfalls: wrong searchPath, path traversal, preExisted, delete semantics, idempotent re-deploy)
    - packages/renderer/src/services/looseOverrideDeploy.test.ts (the RED stub from Plan 01 — make it GREEN)
    - packages/renderer/src/services/pathSafety.ts (isVirtualPathSafe — import and use)
    - packages/renderer/src/services/cfgActivator.ts:226-230 (atomic write pattern to copy)
    - packages/renderer/src/services/packPatch.ts:85-105 (pathSafety import + replacementFilePath reading pattern)
  </read_first>
  <behavior>
    - resolveOverrideDir on swg-client-v2-sharedfile.cfg → returns path ending in 'stage/override' or 'stage\\override' (priority 10)
    - resolveOverrideDir on swg-client-v2-sharedfile.cfg → does NOT return the SWGSource root (priority 9) or ilm_extract (priority 5)
    - deployLoose with entry {virtualPath:'texture/test.dds', action:'add', replacementFilePath: <tmp file>} → file written at <overrideDir>/texture/test.dds
    - deployLoose writes atomically: uses <destPath>.tmp + renameSync
    - deployLoose records preExisted=true if destPath existed before the call; preExisted=false otherwise
    - deployLoose rejects virtualPath='../outside.dds' with Error containing "traversal"
    - deployLoose rejects virtualPath='C:/Windows/system32/evil.dll' with Error
    - resetLoose deletes only writtenFiles where preExisted===false; writtenFiles with preExisted===true are NOT deleted
    - resetLoose is non-fatal per file (catches unlinkSync errors and continues)
    - sha256(client.cfg bytes) is identical before and after a deployLoose+resetLoose cycle
    - 'delete' action in deployLoose: removes overrideDir file if it exists AND preExisted===false; else no-op + console.warn
  </behavior>
  <action>
    Create `packages/renderer/src/services/looseOverrideDeploy.ts`. Use `import path from 'path'` and `import fs from 'fs'` (static import form). Import `{ isVirtualPathSafe }` from `'./pathSafety'`; import `{ resolveClientMountOrder }` from `'./clientSearchOrder'`; import type `{ StagingEntry }` from `'@swg/contracts'`; import type `{ LooseDeployRecord }` from `'@swg/contracts'`. Do NOT import `packPatch`, `cfgActivator`, `ensureInclude`, `snapshotCfg`, or `activatePatch` — this is a new, independent branch (Pitfall 4 in RESEARCH.md).

    `export function resolveOverrideDir(cfgPath: string, installRoot: string): string | null`: calls `resolveClientMountOrder(cfgPath, installRoot)` and returns `order?.looseDirs[0] ?? null`. `looseDirs` is already sorted priority DESC (Plan 02), so index 0 is the max-priority entry.

    `export function deployLoose(entries: StagingEntry[], overrideDir: string): LooseDeployRecord`: Iterate entries:
    1. Skip entries with `action === 'delete'` after recording a `console.warn` (delete tombstoning in loose dirs is not supported per RESEARCH.md §Capability 3 §delete action semantics).
    2. For `add`/`modify`: call `isVirtualPathSafe(entry.virtualPath)`; if false throw `Error('Invalid virtualPath: path traversal rejected — '${entry.virtualPath}')`. Then additionally verify `path.resolve(overrideDir, entry.virtualPath).startsWith(path.resolve(overrideDir))` (second-level traversal check); if false throw `Error('virtualPath escapes overrideDir after resolution: ${entry.virtualPath}')`.
    3. Compute `destPath = path.join(overrideDir, entry.virtualPath)` (forward-slash virtualPath from VFS is cross-platform safe via path.join).
    4. Check `preExisted = fs.existsSync(destPath)`.
    5. Read source bytes: `const data = fs.readFileSync(entry.replacementFilePath!)` — throw if missing.
    6. Create parent dirs: `fs.mkdirSync(path.dirname(destPath), { recursive: true })`.
    7. Atomic write: `const tmp = destPath + '.tmp'; fs.writeFileSync(tmp, data); fs.renameSync(tmp, destPath)`.
    8. Push `{ virtualPath: entry.virtualPath, destPath, preExisted }` to `writtenFiles`.
    Return `{ overrideDir, writtenFiles }`.

    `export function resetLoose(record: LooseDeployRecord): void`: Iterate `record.writtenFiles` where `!preExisted`: try `fs.unlinkSync(destPath)`, catch per-file and log (non-fatal). Optionally clean up empty dirs created by this deploy using `fs.rmdirSync` (best-effort, catch silently).

    After writing, run the test suite to confirm GREEN.
  </action>
  <acceptance_criteria>
    - `grep -c "isVirtualPathSafe" packages/renderer/src/services/looseOverrideDeploy.ts` returns at least 1
    - `grep -c "resolveClientMountOrder" packages/renderer/src/services/looseOverrideDeploy.ts` returns at least 1
    - `grep -c "packPatch\|activatePatch\|cfgActivator\|snapshotCfg\|ensureInclude" packages/renderer/src/services/looseOverrideDeploy.ts` returns 0 (forbidden imports)
    - `pnpm --filter @swg/renderer test -- --run src/services/looseOverrideDeploy.test.ts 2>&1 | grep -E "passed|failed"` — all 5 test cases GREEN
    - resolveOverrideDir test: returns path containing 'stage/override' or 'stage\\override' (not 'SWGSource' or 'ilm_extract')
    - Path-traversal test: deployLoose throws for virtualPath='../outside.dds'
    - sha256 invariant test: client.cfg bytes unchanged before/after deploy+reset cycle
    - `pnpm -w tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/looseOverrideDeploy.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 looseOverrideDeploy tests GREEN; path-traversal rejected; preExisted flag correct; reset non-destructive.</done>
</task>

<task type="auto">
  <name>Task 2: changesetService — broaden updateChangesetDeployRecord type</name>
  <read_first>
    - packages/renderer/src/services/changesetService.ts:449-475 (updateChangesetDeployRecord function to edit)
    - 04.2-PATTERNS.md §changesetService.ts (exact line showing the broadened type + import)
    - packages/contracts/src/changeset.ts:115-145 (SwgChangeset.deployRecord — already widened in Plan 01)
  </read_first>
  <action>
    Edit `packages/renderer/src/services/changesetService.ts`: add `import type { LooseDeployRecord } from '@swg/contracts'` alongside the existing `CfgDeployRecord` import (or wherever `CfgDeployRecord` is imported from). Widen the `updateChangesetDeployRecord` function signature from `record: CfgDeployRecord` to `record: CfgDeployRecord | LooseDeployRecord`. No implementation change is needed — the function body calls `cs.deployRecord = record` which is already typed as `CfgDeployRecord | LooseDeployRecord` (from the `SwgChangeset` type widened in Plan 01). Verify `pnpm -w tsc --noEmit` is green.

    Also confirm the existing test suite for changesetService (if any) still passes after this change — the broadened type is backward-compatible.
  </action>
  <acceptance_criteria>
    - `grep -c "LooseDeployRecord" packages/renderer/src/services/changesetService.ts` returns at least 1
    - `pnpm -w tsc --noEmit` exits 0
    - Existing changeset tests still pass (regression): `pnpm --filter @swg/renderer test -- --run src/services/changeset.test.ts 2>&1 | grep -v "^$"` shows no new failures (if the file exists)
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>updateChangesetDeployRecord accepts CfgDeployRecord | LooseDeployRecord; tsc clean; existing changeset tests unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| virtualPath (from staged entries) → overrideDir join | Staged entry virtualPath could contain traversal sequences; must be validated before join |
| resolveOverrideDir — searchPath selection | Max-priority searchPath is the override dir; lower-priority dirs (data root, ilm_extract) must NOT be selected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Tampering | deployLoose — virtualPath path traversal | mitigate | `isVirtualPathSafe(vp)` (M1 shared validator, packPatch.ts:85 pattern) + second-level `resolve(overrideDir, vp).startsWith(resolve(overrideDir))` check; test case (c) encodes this requirement |
| T-04.2-02 | Tampering | resetLoose — clobbering pre-existing files | mitigate | `preExisted` flag in LooseDeployRecord; resetLoose only calls `unlinkSync` on writtenFiles where `preExisted===false`; test case (d) encodes this |
| T-04.2-03 | Tampering | resolveOverrideDir — wrong searchPath selected | mitigate | Returns `looseDirs[0]` — Plan 02 sorts looseDirs by priority DESC; test case (a) asserts priority-10 dir is selected over priority-9 and priority-5 |
| T-04.2-11 | Tampering | deployLoose — writing to shared TRE data root | mitigate | Downstream of T-04.2-03 mitigation: wrong dir never returned; also: Plan 05 surfaces resolvedOverrideDir for user confirmation before deploying |
| T-04.2-12 | DoS | deployLoose — staged entry with missing replacementFilePath | mitigate | Throws `Error('... has no source file')` before any fs write; no partial state left |
| T-04.2-SC | Tampering | npm installs | accept | No new package installs |
</threat_model>

<verification>
`pnpm --filter @swg/renderer test -- --run src/services/looseOverrideDeploy.test.ts` — all tests GREEN.
`pnpm -w tsc --noEmit` — exits 0.
`grep -c "packPatch\|activatePatch\|cfgActivator" packages/renderer/src/services/looseOverrideDeploy.ts` = 0 (forbidden imports absent).
</verification>

<success_criteria>
- resolveOverrideDir returns max-priority searchPath (priority 10 for swg-client-v2 fixture)
- deployLoose: atomic writes, preExisted flag, path-traversal rejection
- resetLoose: only removes self-created files; sha256 invariant holds
- changesetService accepts LooseDeployRecord
- All looseOverrideDeploy.test.ts tests GREEN
- tsc clean
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-04-SUMMARY.md` when done.
</output>
