---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 05
type: execute
wave: 4
depends_on: ["04.2-02", "04.2-04"]
files_modified:
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
autonomous: true
requirements: [DEPLOY-08, CLIENT-02]
must_haves:
  truths:
    - "A 'Loose override dir' radio appears in Section B alongside the two existing deploy model radios — same visual style (border, background, opacity, cursor)"
    - "When 'loose-override' is selected and a bound client is set, the resolved overrideDir is displayed below the radio (user can confirm before deploying)"
    - "H3: handleBrowse (line ~140) builds cfgRootPath using resolveLayout(folderPath)?.cfgFile — NOT the hardcoded literal 'swgemu.cfg'; a client.cfg install produces cfgRootPath ending in 'client.cfg'"
    - "H3 grep gate: the hardcoded path.join fallback at line ~140 no longer contains the literal string 'swgemu.cfg' in the cfgRootPath assignment"
    - "handleBrowse alert text is 'No swgemu.cfg or client.cfg found in this folder' (not the old single-cfg message)"
    - "handleDeploy for loose-override calls deployLoose(flattenedEntries, resolvedOverrideDir, { studioDir }) — NOT packPatch/activatePatch/snapshotCfg"
    - "Override-dir guard: handleDeploy calls resolveClientMountOrder to check looseDirPriorities[0] vs maxPriority; if looseDirs.length === 0 OR looseDirPriorities[0] > maxPriority, surfaces a visible error in the dialog and does NOT call deployLoose"
    - "handleReset for loose-override calls resetLoose(record as LooseDeployRecord) — NOT deactivatePatch/cfgActivator/restoreCfg"
    - "MED delete banner: if LooseDeployRecord.skippedDeletes.length > 0 after deploy, a visible warning banner is rendered in the 'done' phase UI listing the skipped virtual paths"
    - "Existing 'absolute-path' and 'hardlink-shadow' branches in handleDeploy/handleReset are UNCHANGED"
    - "pnpm -w tsc exits 0"
  artifacts:
    - path: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      provides: "loose-override radio + resolvedOverrideDir preview + H3 cfgFile fix + override-dir guard + delete-skip banner + handleDeploy/handleReset loose-override branches"
      contains: "loose-override"
  key_links:
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "packages/renderer/src/services/looseOverrideDeploy.ts"
      via: "deployLoose(entries, overrideDir, { studioDir }) + resetLoose + resolveOverrideDir imports"
      pattern: "deployLoose"
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "packages/renderer/src/services/clientLayout.ts"
      via: "resolveLayout in handleBrowse (H3 fix — replaces hardcoded swgemu.cfg)"
      pattern: "resolveLayout"
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "packages/renderer/src/services/clientSearchOrder.ts"
      via: "resolveClientMountOrder in handleDeploy override-dir guard (W1)"
      pattern: "looseDirPriorities"
---

<objective>
Wire the loose-override deploy mode into `DeployDialog.tsx` with all review fixes applied: H3 cfgFile fallback fix at line ~140 (the hardcoded `'swgemu.cfg'` literal replaced by `resolveLayout(folderPath)?.cfgFile ?? 'swgemu.cfg'`), override-dir guard in handleDeploy (W1 — checks looseDirPriorities[0] > maxPriority before calling deployLoose), delete-action skip banner (MED — surface `skippedDeletes` to the user), correct `deployLoose` call signature (now takes `opts: { studioDir }`), and the third radio + resolvedOverrideDir preview. The two existing deploy branches are not changed.

Purpose: Full deploy loop from UI. After this plan the user can select "Loose override dir", confirm the path, deploy, and reset. H3 prevents silent failure on `client.cfg` installs. The override-dir guard prevents a silent no-op when the override dir sits above maxSearchPriority (Opus Q3).
Output: DeployDialog.tsx updated; tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-RESEARCH.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-PATTERNS.md

<interfaces>
From 04.2-PATTERNS.md §DeployDialog.tsx — full pattern excerpts:
- Existing deployModel state (line 71): extend union to `'absolute-path' | 'hardlink-shadow' | 'loose-override'`
- Section B radio block (lines 679-759): copy the exact border/background/borderRadius/padding/cursor/opacity pattern for the third radio
- resolvedOverrideDir display: show below the radio when `deployModel === 'loose-override' && resolvedOverrideDir` in var(--font-mono) + var(--text-xs) + var(--color-info) style

H3 fix — handleBrowse line ~140 (confirmed by crew review):
  OLD (BUG): cfgRootPath: path.join(boundClientPath, 'swgemu.cfg')
  NEW (FIX): cfgRootPath: path.join(boundClientPath, resolveLayout(boundClientPath)?.cfgFile ?? 'swgemu.cfg')
  GREP GATE: after this task, `grep -c "'swgemu.cfg'" DeployDialog.tsx` at the line where cfgRootPath is built must return 0 (the literal is replaced by the dynamic lookup).
  NOTE: The fallback `?? 'swgemu.cfg'` is intentional — it preserves backward compat when resolveLayout returns null (e.g. path passed before user selects a folder). The alert text is updated separately.

From looseOverrideDeploy.ts (Plan 04 output — UPDATED signature):
```typescript
export function resolveOverrideDir(cfgPath: string, installRoot: string): string | null
export function deployLoose(
  entries:     StagingEntry[],
  overrideDir: string,
  opts: {
    studioDir:    string;          // REQUIRED — B3 snapshot storage
    priorRecord?: LooseDeployRecord; // H2 prune (optional — first deploy has no prior)
  }
): LooseDeployRecord
export function resetLoose(record: LooseDeployRecord): void
```

studioDir source (for deployLoose opts.studioDir):
  Read from useWorkspaceStore.getState().studioDir (or the equivalent Zustand accessor for the active studio/project dir). This is the same dir used by cfgActivator for cfg snapshots. Check the existing handleDeploy branches for the pattern.

From LooseDeployRecord (Plan 01 + Plan 04 output):
```typescript
export interface LooseDeployRecord {
  overrideDir: string;
  writtenFiles: Array<{
    virtualPath: string; destPath: string; preExisted: boolean; snapshotPath?: string;
  }>;
  skippedDeletes: string[];  // MED delete banner: non-empty means at least one delete entry was skipped
}
```

Override-dir guard (W1 — lives HERE in handleDeploy, not in deployLoose):
  After validating `resolvedOverrideDir != null`, call `resolveClientMountOrder(selectedClient!.cfgRootPath, selectedClient!.installPath)`.
  If `!order || order.looseDirs.length === 0 || order.looseDirPriorities[0] > order.maxPriority`:
    setPhase({ kind: 'error', step: 'activate',
      message: 'Override dir priority ' + (order?.looseDirPriorities[0] ?? 'unknown') +
               ' exceeds maxSearchPriority ' + (order?.maxPriority ?? 'unknown') +
               ' — engine would not load files at this priority',
      cfgRestored: false }) and return. Do NOT call deployLoose.
  Import `resolveClientMountOrder` from `'../../services/clientSearchOrder'`.

Delete-skip banner (MED — Sonnet finding):
  After a successful loose-override deploy, if record.skippedDeletes.length > 0, render a <div> banner inside the 'done' phase UI block.
  Style: var(--color-warning) border + background; icon ⚠ or equivalent; text:
    "Note: {N} 'delete' action(s) could not be applied (base-archive files cannot be removed by override). Skipped: {vp1}, {vp2}, ..."
  This surfaces the limitation to the user rather than silently ignoring it.

From clientLayout.ts (Plan 02 output):
```typescript
export function resolveLayout(installPath: string): ClientLayout | null
// ClientLayout.cfgFile: 'swgemu.cfg' | 'client.cfg'
```

From DeployDialog.tsx — bound client state (lines 68-75, 131-143, 164-175):
```typescript
// Line 70: selectedClient: DetectedClient | null
const [selectedClient, setSelectedClient] = useState<DetectedClient | null>(null);
// Lines 131-143: auto-populated from useWorkspaceStore.getState().clientPath on dialog open
// Lines 164-175: useEffect([selectedClient]) pattern for reactive updates
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeployDialog — loose-override radio UI + H3 cfgFile fix + resolvedOverrideDir preview + generalized handleBrowse</name>
  <read_first>
    - packages/renderer/src/panels/deploy/DeployDialog.tsx:61-250 (deployModel state, Section B radio pattern, handleBrowse lines ~130-250 — full section)
    - packages/renderer/src/panels/deploy/DeployDialog.tsx:128-160 (the bound-client auto-select block — WHERE THE H3 BUG IS at line ~140)
    - 04.2-PATTERNS.md §DeployDialog.tsx — radio block pattern (exact border/background/opacity/cursor JSX for the third radio)
    - 04.2-PATTERNS.md §DeployDialog.tsx — handleBrowse replacement (resolveLayout pattern)
    - 04.2-RESEARCH.md §Capability 3 §Why this is the right model (no cfg surgery needed for loose-override branch)
  </read_first>
  <action>
    Edit `packages/renderer/src/panels/deploy/DeployDialog.tsx`.

    1. Add imports at the top: `import { resolveLayout } from '../../services/clientLayout'`; `import { resolveOverrideDir, deployLoose, resetLoose } from '../../services/looseOverrideDeploy'`; `import { resolveClientMountOrder } from '../../services/clientSearchOrder'`; `import type { LooseDeployRecord } from '@swg/contracts'`.

    2. Extend `deployModel` state type to `'absolute-path' | 'hardlink-shadow' | 'loose-override'` (line ~71). Add state: `const [resolvedOverrideDir, setResolvedOverrideDir] = useState<string | null>(null)`.

    3. H3 FIX — find line ~140 where the bound-client auto-select block builds `cfgRootPath` using a hardcoded `'swgemu.cfg'` path (exact context: `cfgRootPath: path.join(boundClientPath, 'swgemu.cfg')` or equivalent). Replace the hardcoded string with: `cfgRootPath: path.join(boundClientPath, resolveLayout(boundClientPath)?.cfgFile ?? 'swgemu.cfg')`. This is a one-line fix. After the change, `grep -c "'swgemu.cfg'" DeployDialog.tsx` applied to the cfgRootPath assignment line must return 0.
       NOTE: also update the handleBrowse alert message from the old single-cfg form to "No swgemu.cfg or client.cfg found in this folder" (or the equivalent) so users of client.cfg installs get the correct diagnostic.

    4. In Section B (radio block, lines ~679-759), append a THIRD radio option using the EXACT same outer `<div>` wrapper + `<label>` + `<input type="radio" name="deployModel">` structure as the two existing radios. Match the border (accent when selected, border when not), background (accent-dim when selected, transparent when not), borderRadius, padding, cursor, and opacity CSS patterns verbatim from PATTERNS.md §DeployDialog.tsx §Section B radio block. Label: "Loose override dir" (bold, var(--text-sm), fontWeight:600). Sub-label: "writes files directly into the client's top-priority override dir — no cfg changes" (muted style). Below the radio, conditionally render the `resolvedOverrideDir` path when `deployModel === 'loose-override' && resolvedOverrideDir !== null` using `var(--font-mono)` + `var(--text-xs)` + `var(--color-info)` style.

    5. Add a `useEffect` with dependency array `[deployModel, selectedClient]`: when `deployModel === 'loose-override' && selectedClient !== null`, call `resolveOverrideDir(selectedClient.cfgRootPath, selectedClient.installPath)` and store in `resolvedOverrideDir`. Otherwise set `resolvedOverrideDir` to null. The dependency array MUST include both values — `selectedClient` is the DetectedClient state at line 70 (not a stale getState() call inside the effect).
  </action>
  <acceptance_criteria>
    - `grep -c "loose-override" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 2 (state type + radio onClick)
    - `grep -c "resolvedOverrideDir" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 2 (state + display)
    - `grep -c "resolveLayout" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1 (handleBrowse + auto-select H3 fix)
    - H3 grep gate: search DeployDialog.tsx for lines containing `cfgRootPath` and `swgemu.cfg` together on the same line — must return 0 matches: `grep -n "cfgRootPath.*swgemu\.cfg\|swgemu\.cfg.*cfgRootPath" packages/renderer/src/panels/deploy/DeployDialog.tsx | wc -l` == 0
    - `grep -c "client.cfg\|cfgFile" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1 (H3 fix present)
    - `grep -c "selectedClient" packages/renderer/src/panels/deploy/DeployDialog.tsx` appears in at least one useEffect dependency array (not isolated store read)
    - `pnpm -w tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>H3 cfgRootPath fixed (no hardcoded swgemu.cfg); third radio visible; resolvedOverrideDir state + display added; useEffect dependency on [deployModel, selectedClient]; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: DeployDialog — handleDeploy + handleReset loose-override branches (with override-dir guard) + delete-skip banner</name>
  <read_first>
    - packages/renderer/src/panels/deploy/DeployDialog.tsx:362-544 (existing handleDeploy absolute-path + hardlink-shadow branches + handleReset — understand structure before adding)
    - packages/renderer/src/panels/deploy/DeployDialog.tsx (full, focusing on how studioDir / active project dir is accessed — look for useWorkspaceStore pattern)
    - 04.2-PATTERNS.md §DeployDialog.tsx — handleDeploy branch pattern + handleReset branch pattern (exact structure to add)
    - 04.2-RESEARCH.md §Capability 3 §Pitfall 4 (do NOT call packPatch/activatePatch/cfgActivator from the loose-override branch)
    - 04.2-PATTERNS.md §LooseDeployRecord skippedDeletes banner (render pattern for the MED warning)
  </read_first>
  <action>
    Edit `packages/renderer/src/panels/deploy/DeployDialog.tsx`.

    In `handleDeploy`: add an `if (deployModel === 'loose-override')` branch AT THE TOP of the function (before the existing packPatch/activatePatch logic). Branch logic:
    1. Validate `resolvedOverrideDir != null`; if null, `setPhase({ kind: 'error', step: 'activate', message: 'No override dir resolved — bind a client with a searchPath override', cfgRestored: false })` and return.
    1b. Override-dir guard: call `const order = resolveClientMountOrder(selectedClient!.cfgRootPath, selectedClient!.installPath)`. If `!order || order.looseDirs.length === 0 || order.looseDirPriorities[0] > order.maxPriority`, call `setPhase({ kind: 'error', step: 'activate', message: 'Override dir priority ' + (order?.looseDirPriorities[0] ?? 'unknown') + ' exceeds maxSearchPriority ' + (order?.maxPriority ?? 'unknown') + ' — engine would not load files at this priority', cfgRestored: false })` and return. Do NOT call deployLoose. (`resolveClientMountOrder` was imported in Task 1 step 1.)
    2. Get `flattenedEntries` via the same `flatten(activeVersionId)` call the existing branches use (read the existing code to find the exact call site).
    3. Determine `studioDir`: use `useWorkspaceStore.getState().studioDir` (or the equivalent field for the active project/studio dir — read the existing handleDeploy code to find the pattern). This is passed as `opts.studioDir` to `deployLoose`.
    4. Read prior deploy record (H2): `const priorRecord = manifest?.deployRecord as LooseDeployRecord | undefined`. Pass as `opts.priorRecord` if present and is a `LooseDeployRecord` (check `'overrideDir' in priorRecord` as the discriminant).
    5. Call `const record = deployLoose(flattenedEntries, resolvedOverrideDir, { studioDir, priorRecord })`.
    6. Call `updateChangesetDeployRecord(manifest.activeVersionId!, record)` and `setDeployedVersion(manifest.activeVersionId!)`.
    7. Store `deployRecordRef.current = record` (or the equivalent ref/state the existing branches use).
    8. Call `setPhase({ kind: 'done', slot: 'override-dir', cfgPath: resolvedOverrideDir })`.
    9. Wrap steps 3-8 in try/catch: on Error, `setPhase({ kind: 'error', step: 'activate', message: e.message, cfgRestored: false })`.
    10. Return early — does NOT fall through to packPatch/activatePatch.

    In `handleReset`: add an `if (deployModel === 'loose-override')` branch:
    1. Get `rec` from `deployRecordRef.current` (or manifest — same as the existing branches).
    2. Call `resetLoose(rec as LooseDeployRecord)`.
    3. Clear `deployRecordRef.current` and call `setDeployedVersion(null)` + `setPhase({ kind: 'idle' })` (matching the existing reset branch pattern).
    4. Do NOT call deactivatePatch, restoreCfg, cfgActivator, or any cfg-surgery function.
    5. Return early.

    Delete-skip banner: in the 'done' phase UI block (the section rendered when `phase.kind === 'done'`), add a conditional block: if `phase.slot === 'override-dir'` AND `(deployRecordRef.current as LooseDeployRecord)?.skippedDeletes?.length > 0`, render a `<div>` with `var(--color-warning)` border + background styling containing the text: "Note: {N} delete action(s) could not be applied — base-archive files cannot be tombstoned by override. Skipped: {comma-joined list}." Use an `⚠` or equivalent icon prefix. The banner must be visible (not collapsed); it is informational, not blocking — the deploy is otherwise complete.

    Existing absolute-path and hardlink-shadow branches in BOTH handleDeploy and handleReset MUST remain byte-identical to pre-task state (additive only — no edits to existing branch code).
  </action>
  <acceptance_criteria>
    - `grep -c "deployLoose" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1
    - `grep -c "resetLoose" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1
    - `grep -c "studioDir" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1 (deployLoose opts.studioDir)
    - `grep -c "skippedDeletes" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1 (delete-skip banner)
    - Override-dir guard present: `grep -c "looseDirPriorities\[0\]\|looseDirs\.length" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1 (guard branch in handleDeploy loose-override)
    - Forbidden imports in the loose-override branch: `grep -n "packPatch\|activatePatch\|snapshotCfg\|restoreCfg\|deactivatePatch" packages/renderer/src/panels/deploy/DeployDialog.tsx` — these identifiers may exist for other branches; but the LOOSE-OVERRIDE branch code (lines added by this task) must contain NONE of them. Verify by reading the diff of lines added.
    - `pnpm -w tsc --noEmit` exits 0
    - Regression: `grep -c "absolute-path\|hardlink-shadow" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns the same count as before this task
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>handleDeploy loose-override: override-dir guard calls resolveClientMountOrder and surfaces error if priority exceeds maxPriority; then calls deployLoose(entries, dir, { studioDir }) only; handleReset calls resetLoose only; delete-skip banner renders when skippedDeletes is non-empty; existing branches unchanged; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → resolveOverrideDir | User sees the resolved override dir before deploying — this is the human confirmation gate |
| handleDeploy → deployLoose | The loose-override branch must not call cfg-surgery functions |
| H3 boundary | cfgRootPath must be resolved via resolveLayout, not hardcoded — affects what cfg is read for override dir resolution |
| Override-dir guard | Must fire BEFORE deployLoose so a priority-misconfigured override dir never silently no-ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-H3 | Spoofing | DeployDialog handleBrowse — hardcoded swgemu.cfg fallback | mitigate | Replace with resolveLayout(folderPath)?.cfgFile ?? 'swgemu.cfg'; grep gate in Task 1 acceptance_criteria verifies no residual literal |
| T-04.2-03 | Tampering | DeployDialog — wrong overrideDir confirmed | mitigate | resolvedOverrideDir displayed in UI for confirmation before deploy CTA; user sees exact absolute path |
| T-04.2-13 | Tampering | handleDeploy loose-override calling cfg-surgery path | mitigate | Branch returns early; acceptance criteria grep confirms forbidden identifiers not added to loose-override branch |
| T-04.2-15 | Tampering | handleDeploy — override dir above maxSearchPriority (Opus Q3) | mitigate | Override-dir guard: resolveClientMountOrder called; if looseDirPriorities[0] > maxPriority, surface error + return without calling deployLoose; acceptance_criteria grep gate asserts guard present |
| T-04.2-16 | Information | delete-skip banner — user unaware of unapplied deletes | mitigate | skippedDeletes banner renders visibly in the 'done' phase; user knows which paths were not removed |
| T-04.2-SC | Tampering | npm installs | accept | No new package installs |
</threat_model>

<verification>
`pnpm -w tsc --noEmit` — exits 0 after both tasks.
H3 grep gate: `grep -n "cfgRootPath.*swgemu\.cfg\|swgemu\.cfg.*cfgRootPath" packages/renderer/src/panels/deploy/DeployDialog.tsx | wc -l` == 0.
Override-dir guard: `grep -c "looseDirPriorities\[0\]\|looseDirs\.length" packages/renderer/src/panels/deploy/DeployDialog.tsx` >= 1.
Regression: absolute-path + hardlink-shadow branch strings unchanged.
</verification>

<success_criteria>
- H3: cfgRootPath uses resolveLayout(folderPath)?.cfgFile, no hardcoded 'swgemu.cfg' at line ~140
- H3 grep gate passes (0 matches for the combined literal pattern)
- 'Loose override dir' radio in Section B (additive, same style)
- resolvedOverrideDir preview below the radio
- useEffect dependency [deployModel, selectedClient]
- handleBrowse alert accepts both cfg types; alert text updated
- Override-dir guard: resolveClientMountOrder called in handleDeploy; error surfaced if looseDirPriorities[0] > maxPriority or looseDirs empty; deployLoose not called
- handleDeploy: deployLoose(entries, dir, { studioDir, priorRecord? }) + stores record
- handleReset: resetLoose only; no cfg surgery
- Delete-skip banner renders when skippedDeletes non-empty
- Existing branches unchanged; tsc clean
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-05-SUMMARY.md` when done.
</output>
