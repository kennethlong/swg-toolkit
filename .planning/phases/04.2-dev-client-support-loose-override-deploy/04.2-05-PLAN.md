---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 05
type: execute
wave: 4
depends_on: ["04.2-02", "04.2-04"]
files_modified:
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
autonomous: true
requirements: [DEPLOY-08, CLIENT-02]
must_haves:
  truths:
    - "A 'Loose override dir' radio appears in Section B alongside the two existing deploy model radios — same visual style (border, background, opacity, cursor)"
    - "When 'loose-override' is selected and a bound client is set, the resolved overrideDir is displayed below the radio (user can confirm before deploying)"
    - "handleBrowse accepts both swgemu.cfg and client.cfg installs via resolveLayout — no hardcoded 'swgemu.cfg' alert string"
    - "handleDeploy for loose-override calls deployLoose(flattenedEntries, resolvedOverrideDir) — NOT packPatch/activatePatch/snapshotCfg"
    - "handleReset for loose-override calls resetLoose(record as LooseDeployRecord) — NOT deactivatePatch/cfgActivator/restoreCfg"
    - "Existing 'absolute-path' and 'hardlink-shadow' branches in handleDeploy/handleReset are UNCHANGED"
    - "pnpm -w tsc exits 0"
  artifacts:
    - path: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      provides: "loose-override radio + resolved-dir preview + handleDeploy + handleReset loose-override branches; generalized handleBrowse"
      contains: "loose-override"
  key_links:
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "packages/renderer/src/services/looseOverrideDeploy.ts"
      via: "deployLoose + resetLoose + resolveOverrideDir imports"
      pattern: "deployLoose"
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "packages/renderer/src/services/clientLayout.ts"
      via: "resolveLayout in handleBrowse (replaces hardcoded swgemu.cfg)"
      pattern: "resolveLayout"
---

<objective>
Wire the loose-override deploy mode into `DeployDialog.tsx`: add the third radio option (additive, matching existing radio style), generalize `handleBrowse` to accept `client.cfg` via `resolveLayout`, add the `handleDeploy` and `handleReset` loose-override branches, and display the resolved override dir for user confirmation. The two existing deploy model branches (`absolute-path`, `hardlink-shadow`) are not changed.

Purpose: Spine step "full deploy loop from UI" — after this plan, the user can select "Loose override dir", confirm the resolved path, and deploy/reset without manual file copying.
Output: DeployDialog.tsx with third radio + logic branches; tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-RESEARCH.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-PATTERNS.md

<interfaces>
From 04.2-PATTERNS.md §DeployDialog.tsx — full pattern excerpts:
- Existing deployModel state (line 71): `useState<'absolute-path' | 'hardlink-shadow'>('absolute-path')` → extend union to include `'loose-override'`
- Section B radio block (lines 679-759): copy the exact `<div style={{ border: ..., background: ..., borderRadius: ..., padding: ..., cursor: ..., opacity: ... }}>` wrapper pattern from PATTERNS.md for the third radio
- resolvedOverrideDir display: show below the radio when `deployModel === 'loose-override' && resolvedOverrideDir` in `var(--font-mono)` + `var(--text-xs)` + `var(--color-info)` style
- handleBrowse (lines 213-248): replace hardcoded `path.join(folderPath, 'swgemu.cfg')` / `window.alert('No swgemu.cfg found')` with `resolveLayout(folderPath)`; alert "No swgemu.cfg or client.cfg found in this folder" on null
- handleDeploy loose-override branch (lines 362-413): skip packPatch/activatePatch/snapshotCfg entirely; call `deployLoose(flattenedEntries, resolvedOverrideDir!)` → returns LooseDeployRecord; call `updateChangesetDeployRecord(manifest.activeVersionId!, record)` and `setDeployedVersion`; `setPhase({ kind:'done', slot:'override-dir', cfgPath: resolvedOverrideDir! })`; catch → `setPhase({ kind:'error', ... cfgRestored:false })`
- handleReset loose-override branch (lines 493-544): `resetLoose(rec as LooseDeployRecord)` only; no cfg surgery

From looseOverrideDeploy.ts (Plan 04 output):
```typescript
export function resolveOverrideDir(cfgPath: string, installRoot: string): string | null
export function deployLoose(entries: StagingEntry[], overrideDir: string): LooseDeployRecord
export function resetLoose(record: LooseDeployRecord): void
```

From clientLayout.ts (Plan 02 output):
```typescript
export function resolveLayout(installPath: string): ClientLayout | null
// Returns layout.cfgFile = 'swgemu.cfg' or 'client.cfg'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeployDialog — loose-override radio UI + resolvedOverrideDir preview + generalize handleBrowse</name>
  <read_first>
    - packages/renderer/src/panels/deploy/DeployDialog.tsx:61-250 (deployModel state, Section B radio pattern, handleBrowse — full section to understand before editing)
    - 04.2-PATTERNS.md §DeployDialog.tsx — radio block pattern (exact border/background/opacity/cursor JSX to copy for the third radio)
    - 04.2-PATTERNS.md §DeployDialog.tsx — handleBrowse replacement (resolveLayout pattern replacing hardcoded swgemu.cfg)
    - 04.2-RESEARCH.md §Capability 3 §Why this is the right model (confirm no cfg surgery needed for this branch)
  </read_first>
  <action>
    Edit `packages/renderer/src/panels/deploy/DeployDialog.tsx`:

    1. Add imports at the top: `import { resolveLayout } from '../../services/clientLayout'`; `import { resolveOverrideDir, deployLoose, resetLoose } from '../../services/looseOverrideDeploy'`; `import type { LooseDeployRecord } from '@swg/contracts'`.

    2. Extend the `deployModel` state type to `'absolute-path' | 'hardlink-shadow' | 'loose-override'` (line ~71). Add a `resolvedOverrideDir` state: `const [resolvedOverrideDir, setResolvedOverrideDir] = useState<string | null>(null)`.

    3. In Section B (the radio block, lines ~679-759), append a THIRD radio option using the EXACT same outer `<div>` wrapper + `<label>` + `<input type="radio" name="deployModel">` structure as the two existing radios. Match the `border` (accent when selected, border when not), `background` (accent-dim when selected, transparent when not), `borderRadius`, `padding`, `cursor`, and `opacity` CSS patterns (copy from PATTERNS.md §DeployDialog.tsx §Section B radio block verbatim). Label: "Loose override dir" (bold, `var(--text-sm)`, `fontWeight:600`); sub-label: "writes files directly into the client's top-priority override dir — no cfg changes" (muted style). Below the radio, conditionally render the `resolvedOverrideDir` path when `deployModel === 'loose-override' && resolvedOverrideDir !== null` in `var(--font-mono)` + `var(--text-xs)` + `var(--color-info)` style (user confirmation of where files will be written).

    4. Add a `useEffect` (or inline in the radio `onClick`) that, when `deployModel` changes to `'loose-override'` and a bound client is available, calls `resolveOverrideDir(client.cfgRootPath, client.installPath)` and sets `resolvedOverrideDir`. Pattern: the bound client is already accessed in the existing dialog (check DeployDialog.tsx for how it reads the bound client from the store or props). If no client bound, set `resolvedOverrideDir` to null.

    5. Generalize `handleBrowse` (line ~213-248): replace the hardcoded `const cfgRootPath = path.join(folderPath, 'swgemu.cfg'); if (!fs.existsSync(cfgRootPath)) { window.alert('No swgemu.cfg found…'); return; }` with `const layout = resolveLayout(folderPath); if (!layout) { window.alert('No swgemu.cfg or client.cfg found in this folder'); return; } const cfgRootPath = path.join(folderPath, layout.cfgFile);`. The rest of handleBrowse (auto-detect + persist the client) is unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "loose-override" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 3 (state type + radio onClick + handleDeploy branch added in Task 2)
    - `grep -c "resolvedOverrideDir" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 2 (state + display)
    - `grep -c "resolveLayout" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1 (handleBrowse)
    - `grep -v "swgemu.cfg" packages/renderer/src/panels/deploy/DeployDialog.tsx | grep -c "swgemu"` = 0 (no hardcoded swgemu.cfg in alert strings — the literal alert text has been replaced)
    - `pnpm -w tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Third radio visible; resolvedOverrideDir state added; handleBrowse accepts both client.cfg and swgemu.cfg; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: DeployDialog — handleDeploy + handleReset loose-override branches</name>
  <read_first>
    - packages/renderer/src/panels/deploy/DeployDialog.tsx:362-544 (existing handleDeploy absolute-path + hardlink-shadow branches + handleReset — understand structure before adding the loose-override branch)
    - 04.2-PATTERNS.md §DeployDialog.tsx — handleDeploy branch pattern + handleReset branch pattern (exact structure to add)
    - 04.2-RESEARCH.md §Capability 3 §Pitfall 4 (do NOT call packPatch/activatePatch/cfgActivator from the loose-override branch)
  </read_first>
  <action>
    Edit `packages/renderer/src/panels/deploy/DeployDialog.tsx`:

    In `handleDeploy`: add a `if (deployModel === 'loose-override')` branch at the top of the function body (BEFORE the existing `packPatch`/`activatePatch` logic). This branch:
    1. Validates `resolvedOverrideDir != null`; if null, call `setPhase({ kind: 'error', step: 'activate', message: 'No override dir resolved — bind a client with a searchPath override', cfgRestored: false })` and return.
    2. Gets `flattenedEntries` via the same `flatten(activeVersionId)` call the existing branches use (examine DeployDialog.tsx to find the exact call site).
    3. Calls `deployLoose(flattenedEntries, resolvedOverrideDir)` → returns `LooseDeployRecord`.
    4. Stores the record in `deployRecordRef.current = record` (or the equivalent ref/state used by the existing branches for the deploy record).
    5. Calls `updateChangesetDeployRecord(manifest.activeVersionId!, record)` and `setDeployedVersion(manifest.activeVersionId!)` using the same pattern as the existing branches.
    6. Calls `setPhase({ kind: 'done', slot: 'override-dir', cfgPath: resolvedOverrideDir })`.
    7. Wraps in try/catch: on Error, `setPhase({ kind: 'error', step: 'activate', message: e.message, cfgRestored: false })`.
    8. Returns early after the branch — does NOT fall through to `packPatch`/`activatePatch`.

    In `handleReset`: add a `if (deployModel === 'loose-override')` branch. This branch:
    1. Gets the deploy record from `deployRecordRef.current` (or manifest) as `rec`.
    2. Calls `resetLoose(rec as LooseDeployRecord)`.
    3. Clears `deployRecordRef.current` and calls `setDeployedVersion(null)` / `setPhase({ kind: 'idle' })` (matching the pattern of the existing reset branches).
    4. Does NOT call `deactivatePatch`, `restoreCfg`, `cfgActivator`, or any cfg-surgery function.
    5. Returns early — does NOT fall through to the other reset branches.

    The existing `absolute-path` and `hardlink-shadow` branches in BOTH handleDeploy and handleReset MUST remain byte-identical to the pre-task state (additive only).

    Run `pnpm -w tsc --noEmit` to verify clean compilation.
  </action>
  <acceptance_criteria>
    - `grep -c "deployLoose" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1
    - `grep -c "resetLoose" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns at least 1
    - `grep -c "packPatch\|activatePatch\|snapshotCfg\|restoreCfg\|deactivatePatch" packages/renderer/src/panels/deploy/DeployDialog.tsx` — count should be IDENTICAL to the count before this task (these identifiers may exist for the OTHER branches, but no NEW calls were added in the loose-override branch)
    - `pnpm -w tsc --noEmit` exits 0
    - Regression: `grep -c "absolute-path\|hardlink-shadow" packages/renderer/src/panels/deploy/DeployDialog.tsx` returns the same count as before (existing branch strings unchanged)
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>handleDeploy loose-override branch calls deployLoose only; handleReset calls resetLoose only; existing branches unchanged; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → resolveOverrideDir | User sees the resolved override dir before deploying — this is the human confirmation gate |
| handleDeploy → deployLoose | The loose-override branch must not call cfg-surgery functions; the boundary is explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-03 | Tampering | DeployDialog — wrong overrideDir confirmed | mitigate | resolvedOverrideDir is displayed in the UI for user confirmation before the deploy CTA is clicked; the user sees the exact absolute path (T-04.2-03 mitigation layer 2) |
| T-04.2-13 | Tampering | handleDeploy loose-override calling cfg-surgery path | mitigate | Branch returns early; acceptance criteria grep confirms forbidden identifiers count unchanged (no new calls to packPatch/activatePatch/snapshotCfg in the loose-override branch) |
| T-04.2-14 | Spoofing | handleBrowse — accepting any folder as valid client | mitigate | resolveLayout performs layout probe (cfg + .tre or treDirFromCfg check); unknown folders return null → alert shown |
| T-04.2-SC | Tampering | npm installs | accept | No new package installs |
</threat_model>

<verification>
`pnpm -w tsc --noEmit` — exits 0 after both tasks.
Grep regression: existing handleDeploy/handleReset branches (absolute-path + hardlink-shadow) are byte-identical to before this plan.
</verification>

<success_criteria>
- 'Loose override dir' radio in Section B (additive, same style as existing two)
- resolvedOverrideDir state shown below the radio for user confirmation
- handleBrowse accepts both swgemu.cfg and client.cfg via resolveLayout
- handleDeploy loose-override: calls deployLoose only; no packPatch/cfgActivator
- handleReset loose-override: calls resetLoose only; no deactivatePatch/restoreCfg
- Existing absolute-path + hardlink-shadow branches unchanged
- tsc clean
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-05-SUMMARY.md` when done.
</output>
