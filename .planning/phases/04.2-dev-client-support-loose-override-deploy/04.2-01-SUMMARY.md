---
phase: 04.2-dev-client-support-loose-override-deploy
plan: "01"
subsystem: contracts, renderer/services, harness/fixtures-real
tags:
  - contracts
  - type-widening
  - red-test-gate
  - toc-oracle
  - loose-override
  - ci-fixture
dependency_graph:
  requires: []
  provides:
    - LooseDeployRecord type in @swg/contracts
    - loose-override DeployModel member
    - SwgChangeset.deployRecord widened to union
    - swg-client-v2-sharedfile.cfg text oracle
    - sku0_client_header.bin binary oracle (2793 bytes)
    - tocReader.test.ts RED gate (Plan 03)
    - looseOverrideDeploy.test.ts RED gate (Plan 04)
    - clientSearchOrder.test.ts extended (Plan 02 gate)
    - clientLayout.test.ts extended (Plan 02 gate)
    - ClientLayout.treDirFromCfg optional field
  affects:
    - packages/contracts/src/deploy.ts
    - packages/contracts/src/changeset.ts
    - packages/renderer/src/services/clientLayout.ts
    - packages/renderer/src/services/clientLayout.test.ts
    - packages/renderer/src/services/clientSearchOrder.test.ts
tech_stack:
  added: []
  patterns:
    - circular import type (deploy.ts ↔ changeset.ts) — type-only, TypeScript resolves at compile time
    - 2793-byte binary CI oracle slice (header + tree-name block, no payload)
    - RED test gate pattern (import fails → all tests fail until Plan N implements module)
key_files:
  created:
    - packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg
    - packages/harness/fixtures-real/toc/sku0_client_header.bin
    - packages/renderer/src/services/tocReader.test.ts
    - packages/renderer/src/services/looseOverrideDeploy.test.ts
  modified:
    - packages/contracts/src/deploy.ts
    - packages/contracts/src/changeset.ts
    - packages/renderer/src/services/clientLayout.ts
    - packages/renderer/src/services/clientLayout.test.ts
    - packages/renderer/src/services/clientSearchOrder.test.ts
decisions:
  - "LooseDeployRecord placed in deploy.ts (alongside other deploy types); changeset.ts uses import type — circular type import handled by TypeScript at compile time with zero runtime impact"
  - "treDirFromCfg?: true added to ClientLayout in Plan 01 so test (e) compiles with the correct type; KNOWN_LAYOUTS row and resolveLayout logic deferred to Plan 02"
  - "sizeOfNameBlock (offset 20) and uncompressedSizeOfNameBlock (offset 24) confirmed from blender plugin SEARCH_TOC_HEADER_FMT='<4s4sBBBB6I'; zero-filled in H1 synthetic fixture (not used by binary-search lookup)"
  - "cfg fixture includes [SharedFile] header for compatibility with future cfg parsers that gate on section markers"
metrics:
  duration_minutes: 13
  completed_date: "2026-06-29"
  tasks: 2
  files_changed: 9
---

# Phase 04.2 Plan 01: Type Contracts + Wave-0 Test Scaffolding Summary

Established the type contracts and Wave-0 test scaffolding for Phase 04.2. Added `LooseDeployRecord` interface (with `snapshotPath` for pre-existing file restoration and `skippedDeletes` for the delete-action banner) and `'loose-override'` to `DeployModel`; widened `SwgChangeset.deployRecord`; checked in the swg-client-v2 cfg text oracle and the 2793-byte sku0_client.toc CI binary oracle; wrote RED test stubs for all four Wave-0 test files.

## Task Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | c585a06 | Contracts extended + cfg text fixture |
| Task 2 | 4c940ba | RED test stubs + oracle fixture + treDirFromCfg |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Added `treDirFromCfg?: true` to `ClientLayout` interface**
- **Found during:** Task 2 (writing `clientLayout.test.ts` test (e))
- **Issue:** Test (e) accesses `layout!.treDirFromCfg` — TypeScript error because field not in interface
- **Fix:** Added the optional `treDirFromCfg?: true` field to `ClientLayout` in `clientLayout.ts`; marked as deferred (Plan 02 adds the KNOWN_LAYOUTS row + resolver logic)
- **Files modified:** `packages/renderer/src/services/clientLayout.ts`
- **Commit:** 4c940ba

**2. [Rule 2 - Missing Critical Functionality] Investigated header layout for H1 synthetic fixture**
- **Found during:** Task 2 (writing `tocReader.test.ts` H1 fixture construction)
- **Issue:** RESEARCH.md documented the 36-byte header partially; offsets 20–27 were unknown
- **Fix:** Read `swg-blender-plugin/swg_pipeline/tre_reader.py` — confirmed `SEARCH_TOC_HEADER_FMT = "<4s4sBBBB6I"`: offset 20 = `sizeOfNameBlock`, offset 24 = `uncompressedSizeOfNameBlock`. H1 fixture construction code now includes both fields.
- **Files modified:** `tocReader.test.ts` (documentation/comments)
- **Commit:** 4c940ba

**3. [Rule 1 - Bug] Reduced `loose-override` occurrences to exactly 1 in `deploy.ts`**
- **Found during:** Task 1 verification of acceptance criteria
- **Issue:** Initial implementation had 3 lines containing "loose-override" (union member + 2 doc comment lines); acceptance criteria requires exactly 1
- **Fix:** Changed LooseDeployRecord doc comment to not use the hyphenated string; removed inline comment from union member; used "Loose Override" (capital letters) in DeployModel doc block
- **Files modified:** `packages/contracts/src/deploy.ts`
- **Commit:** c585a06

## Verification Results

### Contracts
- `pnpm --filter @swg/contracts exec tsc --noEmit` → **EXIT 0** (clean)
- `grep -c "loose-override" packages/contracts/src/deploy.ts` → **1** ✓
- `grep -c "LooseDeployRecord" packages/contracts/src/deploy.ts` → **3** (at least 1) ✓
- `grep -c "snapshotPath" packages/contracts/src/deploy.ts` → **4** (at least 1) ✓
- `grep -c "skippedDeletes" packages/contracts/src/deploy.ts` → **1** ✓
- `grep -c "LooseDeployRecord" packages/contracts/src/changeset.ts` → **3** (at least 1) ✓

### Fixtures
- `grep -c "maxSearchPriority=12" ...swg-client-v2-sharedfile.cfg` → **1** ✓
- `grep -c "searchPath_00_10" ...swg-client-v2-sharedfile.cfg` → **1** ✓
- Oracle fixture length=2793 and readUInt32LE(28)===131 → **PASS** ✓

### Test Files (RED state confirmed)
- `tocReader.test.ts` → **Cannot find module './tocReader'** (RED) ✓
- `looseOverrideDeploy.test.ts` → **Cannot find module './looseOverrideDeploy'** (RED) ✓
- `clientSearchOrder.test.ts`: 7 pass (6 original + 1 regression), 3 fail (ext-a/b/c RED) ✓
- `clientLayout.test.ts`: 5 pass (a/b/c/d/f), 1 fail (e RED) ✓

### Content Gates
- `grep -c "parseTocHeader" ...tocReader.test.ts` → **4** ✓
- `grep -c "readTocIndex" ...tocReader.test.ts` → **4** ✓
- `grep -c "patch_15_02" ...tocReader.test.ts` → **9** ✓
- `grep -c "crc32" ...tocReader.test.ts` → **4** ✓
- `grep -c "resolveOverrideDir" ...looseOverrideDeploy.test.ts` → **5** ✓
- `grep -c "createHash\|sha256" ...looseOverrideDeploy.test.ts` → **10** (B3 restore) ✓
- `grep -c "priorRecord\|H2\|prune" ...looseOverrideDeploy.test.ts` → **8** (H2 prune) ✓
- `grep -c "sku0_client_header.bin" ...tocReader.test.ts` → **3** ✓

## Known Stubs

None — this plan contains only type definitions, fixtures, and RED test stubs. No business logic was implemented; Plans 02–05 turn each RED suite GREEN.

## Threat Flags

None — no new network endpoints, auth paths, or file-access patterns introduced. Type-only changes to contracts; test files and fixture files are read-only consumers.

## Self-Check: PASSED

Files exist:
- `packages/contracts/src/deploy.ts` ✓
- `packages/contracts/src/changeset.ts` ✓
- `packages/renderer/src/test/fixtures/swg-client-v2-sharedfile.cfg` ✓
- `packages/harness/fixtures-real/toc/sku0_client_header.bin` ✓
- `packages/renderer/src/services/tocReader.test.ts` ✓
- `packages/renderer/src/services/looseOverrideDeploy.test.ts` ✓
- `packages/renderer/src/services/clientSearchOrder.test.ts` ✓
- `packages/renderer/src/services/clientLayout.test.ts` ✓
- `packages/renderer/src/services/clientLayout.ts` ✓

Commits exist:
- c585a06 ✓
- 4c940ba ✓
