---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 06
type: execute
wave: 5
depends_on: ["04.2-03", "04.2-05"]
files_modified: []
autonomous: false
requirements: [CLIENT-02, TRE-05, DEPLOY-08]
must_haves:
  truths:
    - "Binding swg-client-v2 (stage-x64) detects the client.cfg layout and persists with the project"
    - "The TRE browser populates with the full 131-archive base; texture/ksk_all_spaceterminal.dds resolves and extracts (87,536 bytes DXT5)"
    - "The 'Loose override dir' radio shows 'D:/Code/swg-client-v2/stage/override' (or machine-equivalent) in the UI"
    - "Deploying the staged retextured .dds writes it to stage/override/texture/ksk_all_spaceterminal.dds"
    - "The retextured space terminal appears in-game after the deploy (matches the 2026-06-28 hand-proof)"
    - "client.cfg bytes are byte-identical before and after the deploy+reset cycle"
    - "Resetting removes only the toolkit-deployed file; the override dir's pre-existing contents are intact"
    - "SWGEmu/Infinity searchTree-only clients still mount and deploy exactly as before (regression)"
  artifacts:
    - path: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      provides: "UAT proof of loose-override deploy path end-to-end"
  key_links:
    - from: "toolkit UI (Deploy panel)"
      to: "D:/Code/swg-client-v2/stage/override/texture/"
      via: "deployLoose writes file at overrideDir/virtualPath"
      pattern: "ksk_all_spaceterminal"
---

<objective>
In-client UAT that proves the full Phase 04.2 spine: bind swg-client-v2, mount its searchTOC base, extract the space-terminal diffuse texture, stage an edit, deploy via "Loose override dir", confirm the change appears in-game, then reset and confirm clean removal. Also performs a regression smoke test on the SWGEmu/Infinity searchTree-only path.

Purpose: Proves success criteria 1-5 against the real swg-client-v2 install and a real running client — the same hand-proof that was verified manually on 2026-06-28, now reproducible end-to-end through the toolkit.
Output: UAT signed off; phase ready for /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-INPUTS.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full-suite test gate before UAT</name>
  <what-built>
    Plans 01-05 have implemented: contracts (LooseDeployRecord/DeployModel), client-cfg detection, searchTOC/searchPath mount extension, TOC reader, loose-override deploy service, and DeployDialog UI changes.
  </what-built>
  <how-to-verify>
    Run the full renderer test suite:
      `pnpm --filter @swg/renderer test -- --run 2>&1 | tail -30`

    Expected: all tests pass (including tocReader, looseOverrideDeploy, clientLayout, clientSearchOrder suites). Zero test failures. Any failure must be fixed before proceeding.

    Also run tsc:
      `pnpm -w tsc --noEmit 2>&1 | tail -10`

    Expected: exits 0, no errors.
  </how-to-verify>
  <resume-signal>Type "tests green" when all tests pass and tsc exits 0, or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-client UAT — space-terminal retexture E2E (success criterion 4)</name>
  <what-built>
    The full Phase 04.2 feature set: client.cfg detection, searchTOC mount, loose-override deploy, DeployDialog third radio.
  </what-built>
  <how-to-verify>
    Run the toolkit in development mode (`npm run dev` or `pnpm dev`). Perform the following steps in order:

    STEP 1 — Bind swg-client-v2 (SUCCESS CRITERION 1):
    - Open/create a project. In the Project Binding flow (wizard or ProjectBindingBar), browse to `D:\Code\swg-client-v2\stage-x64`.
    - Confirm: the detected layout shows "swg-client-v2", cfgFile="client.cfg", and persists with the project.

    STEP 2 — Verify full base mount (SUCCESS CRITERION 2):
    - After project opens, the TRE browser should populate. Wait for the mount to complete.
    - In the TRE browser search, search for `ksk_all_spaceterminal.dds`.
    - Confirm: the file resolves — at least one result shows `texture/ksk_all_spaceterminal.dds` with size 87,536 bytes (DXT5 format per INPUTS.md).
    - Confirm: the overall archive count shows 131+ archives from the searchTOC base.

    STEP 3 — Stage an edit:
    - Extract `texture/ksk_all_spaceterminal.dds` from the TRE browser (right-click → Extract or Extract→Add to staging).
    - Open the extracted .dds in any image editor; draw a visible mark (e.g. large X, bright color), save as DDS/DXT5 256×256.
    - Confirm: the edited file appears in the staging panel.
    - Save a version (name it "retexture-test") and set it as active.

    STEP 4 — Deploy via loose-override (SUCCESS CRITERION 3):
    - Open the Deploy dialog. Select "Loose override dir" radio.
    - Confirm: the resolved override dir displayed is `D:\Code\swg-client-v2\stage\override` (or machine-equivalent searchPath_00_10 value).
    - Note the sha256 of `D:\Code\swg-client-v2\stage-x64\client.cfg` before deploying.
    - Click Deploy. Confirm: no error; deploy completes.
    - Verify file on disk: `D:\Code\swg-client-v2\stage\override\texture\ksk_all_spaceterminal.dds` now exists.

    STEP 5 — In-game proof (SUCCESS CRITERION 4):
    - Launch `D:\Code\swg-client-v2\stage-x64\SwgClient_r.exe`. Log in or use /goto to reach the space terminal (in Mos Eisley cantina or any starport — object `object/tangible/terminal/shared_terminal_space.iff`).
    - Confirm: the space terminal displays the edited texture (visible mark from step 3).
    - This reproduces the 2026-06-28 hand-proof via the toolkit UI — zero manual file copying.

    STEP 6 — Reset and verify non-destructive (SUCCESS CRITERION 3 + reset):
    - Back in the toolkit, click Reset. Confirm: no error.
    - Verify: `D:\Code\swg-client-v2\stage\override\texture\ksk_all_spaceterminal.dds` is REMOVED.
    - Verify: sha256 of `D:\Code\swg-client-v2\stage-x64\client.cfg` matches the value noted before deploying (byte-untouched).

    STEP 7 — Regression (SUCCESS CRITERION 5):
    - Rebind project to a searchTree-only client (e.g. `D:\SWG Infinity\SWG Infinity`).
    - Confirm: mounts correctly (archives list, TRE browser populated); Deploy dialog shows "SWG Infinity" layout; deploy/reset work as before this phase.
  </how-to-verify>
  <resume-signal>Type "UAT passed" with a brief summary of each step result, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT — override dir selection | Human confirms the correct override dir is displayed before deploying |
| UAT — reset verification | Human verifies client.cfg is byte-untouched and only toolkit-deployed files were removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-03 | Tampering | Wrong override dir deployed to | verify | Step 4 of UAT: user confirms the displayed resolvedOverrideDir matches the expected `stage/override` path before clicking Deploy |
| T-04.2-02 | Tampering | Reset removes pre-existing files | verify | Step 6 of UAT: sha256 of client.cfg unchanged; only the toolkit-deployed file removed |
| T-04.2-SC | Tampering | npm installs | accept | No new package installs |
</threat_model>

<verification>
All 7 UAT steps completed with expected outcomes:
1. client.cfg layout detected + persists
2. 131-archive base mounted; ksk_all_spaceterminal.dds resolves at 87,536 bytes
3. Edit staged + version saved
4. Deploy: file present at stage/override/texture/ after deploy
5. In-game: retextured terminal visible
6. Reset: file removed; client.cfg sha256 unchanged
7. Regression: Infinity searchTree-only client unaffected
</verification>

<success_criteria>
- All Phase 04.2 success criteria verified in a real Electron instance against the real swg-client-v2 install
- Full test suite green (Task 1 gate)
- In-game retexture proof matches the 2026-06-28 hand-proof (no manual file copying needed)
- client.cfg byte-untouched after deploy+reset cycle
- SWGEmu/Infinity regression confirmed
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-06-SUMMARY.md` when done.
</output>
