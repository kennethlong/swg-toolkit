---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 06
type: execute
wave: 5
depends_on: ["04.2-03", "04.2-05"]
files_modified: []
autonomous: false
requirements: [CLIENT-02, TRE-05, DEPLOY-08]
must_haves:
  truths:
    - "Binding swg-client-v2 (stage-x64) detects the client.cfg layout and persists with the project"
    - "The TRE browser populates with the full 131-archive base; texture/ksk_all_spaceterminal.dds resolves and extracts (87,536 bytes DXT5)"
    - "The 'Loose override dir' radio shows 'D:/Code/swg-client-v2/stage/override' (or machine-equivalent) in the UI"
    - "Deploying the staged retextured .dds writes it to stage/override/texture/ksk_all_spaceterminal.dds"
    - "The retextured space terminal appears in-game after the deploy (matches the 2026-06-28 hand-proof)"
    - "client.cfg bytes are byte-identical before and after the deploy+reset cycle"
    - "B3: ksk_all_spaceterminal.dds existed in stage/override/texture/ before the deploy; after resetLoose, the file is RESTORED to its ORIGINAL CONTENTS (sha256 must match the pre-deploy value) — NOT removed, NOT empty"
    - "B3: a fresh toolkit-deployed file (one that did NOT pre-exist in stage/override) IS removed after reset"
    - "SWGEmu/Infinity searchTree-only clients still mount and deploy exactly as before (regression)"
  artifacts:
    - path: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      provides: "UAT proof of loose-override deploy path end-to-end"
  key_links:
    - from: "toolkit UI (Deploy panel)"
      to: "D:/Code/swg-client-v2/stage/override/texture/"
      via: "deployLoose writes file at overrideDir/virtualPath; resetLoose restores pre-existing originals"
      pattern: "ksk_all_spaceterminal"
---

<objective>
In-client UAT that proves the full Phase 04.2 spine: bind swg-client-v2, mount its searchTOC base, extract the space-terminal diffuse texture, stage an edit, deploy via "Loose override dir", confirm the change appears in-game, then reset and confirm ORIGINAL CONTENTS RESTORED (not removed) for the pre-existing `ksk_all_spaceterminal.dds` file (B3). Also tests that a brand-new toolkit-deployed file IS removed. Performs a regression smoke test on the SWGEmu/Infinity searchTree-only path.

Purpose: Proves success criteria 1-5 against the real swg-client-v2 install and a real running client — the same hand-proof verified manually on 2026-06-28, now reproducible end-to-end through the toolkit. Step 6 is the B3 acceptance test: sha256(file after reset) == sha256(file before deploy).
Output: UAT signed off; phase ready for /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-INPUTS.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full-suite test gate before UAT</name>
  <what-built>
    Plans 01-05 have implemented: contracts (LooseDeployRecord with snapshotPath + skippedDeletes / DeployModel), client-cfg detection (treDirFromCfg), searchTOC/searchPath mount extension (unified single-mount, B1/B2/B4), full TOC index reader (H1), loose-override deploy service (B3 snapshot/restore + H2 prune + path-safety hardening), and DeployDialog UI changes (H3 cfgFile fix + delete-skip banner).
  </what-built>
  <how-to-verify>
    Run the full renderer test suite:
      `pnpm --filter @swg/renderer test -- --run 2>&1 | tail -30`

    Expected: all tests pass (including tocReader, looseOverrideDeploy, clientLayout, clientSearchOrder, treAutoMount suites). Zero test failures. Any failure must be fixed before proceeding.

    Key assertions to spot-check in output:
    - tocReader tests: "(a) oracle CI" passes; "(c) H1 synthetic fixture" passes (space_faction.stf → treeFileIndex=0)
    - looseOverrideDeploy tests: B3 sha256 restore test passes; H2 prune test passes; path-safety tests (c)(d)(e) pass
    - treAutoMount tests: B1 (mountTrePaths once), B2 (tree@8 > toc@3), B4 (conditional isOverride) all pass

    Also run tsc:
      `pnpm -w tsc --noEmit 2>&1 | tail -10`

    Expected: exits 0, no errors.
  </how-to-verify>
  <resume-signal>Type "tests green" when all tests pass and tsc exits 0, or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-client UAT — space-terminal retexture E2E (success criteria 1-5)</name>
  <what-built>
    The full Phase 04.2 feature set: client.cfg detection, searchTOC unified mount, loose-override deploy, DeployDialog third radio, B3 snapshot/restore reset.
  </what-built>
  <how-to-verify>
    Run the toolkit in development mode (`npm run dev` or `pnpm dev`). Perform the following steps in order:

    STEP 1 — Bind swg-client-v2 (SUCCESS CRITERION 1):
    - Open/create a project. In the Project Binding flow, browse to `D:\Code\swg-client-v2\stage-x64`.
    - Confirm: detected layout shows "swg-client-v2", cfgFile="client.cfg", persists with the project.

    STEP 2 — Verify full base mount (SUCCESS CRITERION 2):
    - After project opens, wait for mount to complete.
    - In the TRE browser, search for `ksk_all_spaceterminal.dds`.
    - Confirm: file resolves — `texture/ksk_all_spaceterminal.dds` at 87,536 bytes (DXT5 per INPUTS.md).
    - Confirm: archive count shows 131+ archives from the searchTOC base.

    STEP 3 — Pre-deploy sha256 capture (B3 prerequisite):
    - Run: `certutil -hashfile "D:\Code\swg-client-v2\stage\override\texture\ksk_all_spaceterminal.dds" SHA256`
      (or `sha256sum` in Git Bash). Record this hash as SHA256_ORIGINAL.
    - This file already exists in the override dir (from the 2026-06-28 hand-proof).
    - Also record sha256 of `D:\Code\swg-client-v2\stage-x64\client.cfg` as SHA256_CFG.

    STEP 4 — Stage an edit:
    - Extract `texture/ksk_all_spaceterminal.dds` from the TRE browser (right-click → Extract+stage).
    - Open the extracted .dds in an image editor; draw a visible mark (large X, bright color), save as DDS/DXT5.
    - Confirm: edited file appears in staging panel. Save a version named "retexture-test"; set as active.

    STEP 5 — Deploy via loose-override (SUCCESS CRITERION 3):
    - Open Deploy dialog. Select "Loose override dir" radio.
    - Confirm: resolved override dir displayed is `D:\Code\swg-client-v2\stage\override` (searchPath_00_10).
    - Click Deploy. Confirm no error; deploy completes.
    - Verify on disk: `D:\Code\swg-client-v2\stage\override\texture\ksk_all_spaceterminal.dds` now contains the edited version.
    - Verify: a snapshot file exists at `<studioDir>/snapshots/texture/ksk_all_spaceterminal.dds` (B3 — toolkit saved the original).

    STEP 6 — In-game proof (SUCCESS CRITERION 4):
    - Launch `D:\Code\swg-client-v2\stage-x64\SwgClient_r.exe`. Travel to the space terminal.
    - Confirm: space terminal displays the edited texture (visible mark from step 4).
    - This reproduces the 2026-06-28 hand-proof via the toolkit UI — zero manual file copying.

    STEP 7 — Reset and verify B3 RESTORE (SUCCESS CRITERION 3 + B3):
    - Back in the toolkit, click Reset. Confirm no error.
    - Verify sha256 of `D:\Code\swg-client-v2\stage\override\texture\ksk_all_spaceterminal.dds`:
      Run: `certutil -hashfile "D:\Code\swg-client-v2\stage\override\texture\ksk_all_spaceterminal.dds" SHA256`
      EXPECTED: sha256 EQUALS SHA256_ORIGINAL (file RESTORED to original contents — NOT removed, NOT empty).
      FAILURE: if the file is absent or if sha256 != SHA256_ORIGINAL → B3 regression.
    - Verify sha256 of `D:\Code\swg-client-v2\stage-x64\client.cfg` == SHA256_CFG (byte-untouched).
    - Verify: the snapshot file `<studioDir>/snapshots/texture/ksk_all_spaceterminal.dds` has been deleted (cleanup after restore).

    STEP 8 — Verify new-file removal (B3 complement):
    - Create a brand-new staging entry for a virtual path that does NOT pre-exist in stage/override (e.g. `texture/test_new_toolkit.dds`). Deploy. Verify the file is created at `stage/override/texture/test_new_toolkit.dds`.
    - Reset. Verify the brand-new file IS REMOVED (it was preExisted=false → standard unlink behavior).

    STEP 9 — Regression (SUCCESS CRITERION 5):
    - Rebind project to a searchTree-only client (e.g. `D:\SWG Infinity\SWG Infinity`).
    - Confirm: mounts correctly; Deploy dialog shows "SWG Infinity" layout; deploy/reset work as before this phase.
  </how-to-verify>
  <resume-signal>Type "UAT passed" with sha256 values from steps 3 and 7 confirming they match, plus a brief summary of each step result. Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT — override dir selection | Human confirms the correct override dir is displayed before deploying |
| UAT — B3 reset verification | Human verifies sha256(after reset) == sha256(before deploy) — restore, not remove |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-03 | Tampering | Wrong override dir deployed to | verify | Step 5 of UAT: user confirms the displayed resolvedOverrideDir matches expected `stage/override` |
| T-04.2-02 | Tampering | B3 reset removes pre-existing file (previous bug) | verify | Step 7 of UAT: sha256 invariant; file RESTORED not absent |
| T-04.2-SC | Tampering | npm installs | accept | No new package installs |
</threat_model>

<verification>
All 9 UAT steps completed with expected outcomes:
1. client.cfg layout detected + persists
2. 131-archive base mounted; ksk_all_spaceterminal.dds resolves at 87,536 bytes
3. SHA256_ORIGINAL recorded before deploy
4. Edit staged + version saved
5. Deploy: file present + snapshot created at studioDir/snapshots/
6. In-game: retextured terminal visible
7. Reset B3: sha256(after reset) == SHA256_ORIGINAL; snapshot cleaned up; client.cfg sha256 unchanged
8. New-file reset: brand-new toolkit file IS removed (preExisted=false path)
9. Regression: Infinity searchTree-only client unaffected
</verification>

<success_criteria>
- All Phase 04.2 success criteria verified in a real Electron instance against the real swg-client-v2 install
- Full test suite green (Task 1 gate)
- B3 sha256 invariant: sha256(ksk_all_spaceterminal.dds after reset) == sha256(ksk_all_spaceterminal.dds before deploy)
- Snapshot file cleaned up after restore
- In-game retexture proof matches the 2026-06-28 hand-proof (no manual file copying needed)
- client.cfg byte-untouched after deploy+reset cycle
- Brand-new toolkit file removed after reset (preExisted=false path)
- SWGEmu/Infinity regression confirmed
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-06-SUMMARY.md` when done.
</output>
