---
phase: 04.2-dev-client-support-loose-override-deploy
plan: 03
type: execute
wave: 3
depends_on: ["04.2-01", "04.2-02"]
files_modified:
  - packages/renderer/src/services/crc32.ts
  - packages/renderer/src/services/crc32.test.ts
  - packages/renderer/src/services/tocReader.ts
  - packages/renderer/src/services/tocReader.test.ts
  - packages/renderer/src/services/treAutoMount.ts
  - packages/renderer/src/services/treAutoMount.test.ts
  - packages/renderer/src/services/treMount.ts
  - packages/renderer/src/state/treStore.ts
autonomous: true
requirements: [TRE-05]
must_haves:
  truths:
    - "parseTocHeader parses sku0_client_header.bin: numberOfFiles=193475, numberOfTreeFiles=131, sizeOfTOC=4643400 (CI-runnable against oracle — no real .toc needed)"
    - "readTocTreeNames on oracle: array length===131, result[0]==='bottom.tre'"
    - "readTocIndex H1 invariant (CI, no skipIf): synthetic 3-entry fixture resolves space_faction.stf to treeFileIndex===0 via engine-faithful binary search landing at mid=1 (NOT treeFileIndex===1, which KEEP FIRST / lowest-index heuristic would return from the first duplicate entry)"
    - "readTocIndex H1 oracle (skipIf real .toc absent): real sku0_client.toc resolves space_faction.stf to treeFileIndex===42 / patch_15_02.tre"
    - "Direct CRC32 oracle (W3, UNVERIFIED-pending-executor-confirm): crc32('appearance/mesh/thm_tato_imprv_building_s09_r0_mesh_l4.msh') === 3830594; independently CI-verifiable without real .toc; crc32.test.ts contains comment 'CONFIRM this value against native-core Crc.cpp before executing — byte-exact CRC-port gate'"
    - "autoMountClient calls mountTrePaths exactly ONCE (B1 fix)"
    - "autoMountClient calls mountComplete exactly ONCE (B1 fix)"
    - "swgsource_3.0.tre (searchTree@8) gets a higher compact mount priority than any sku3.toc-expanded .tre (toc@3) in the same unified list (B2 fix)"
    - "A searchPath@5 dir: injectLooseDirOverlay called with isOverride=false (5 < maxTreRawPriority=8) — B4 fix"
    - "A searchPath@10 dir: injectLooseDirOverlay called with isOverride=true (10 > maxTreRawPriority=8) — B4 fix"
    - "Missing .tre paths logged and skipped without throwing"
    - "mountComplete called ONCE after unified mountTrePaths; NOT called again after injectLooseDirOverlay"
  artifacts:
    - path: "packages/renderer/src/services/crc32.ts"
      provides: "crc32() TS helper — table-based port of packages/native-core/modules/core/tre/Crc.cpp (polynomial 0x04C11DB7, forward / MSB-first)"
      exports: ["crc32"]
    - path: "packages/renderer/src/services/crc32.test.ts"
      provides: "Direct CRC32 oracle test (W3): crc32(assetFilename) === 3830594 with CONFIRM comment; CI-runnable without real .toc"
      contains: "3830594"
    - path: "packages/renderer/src/services/tocReader.ts"
      provides: "parseTocHeader, readTocTreeNames, readTocIndex, TocIndex, TocIndexEntry, TocHeader"
      exports: ["parseTocHeader", "readTocTreeNames", "readTocIndex", "TocIndex", "TocIndexEntry", "TocHeader"]
      contains: "treeFileIndex"
    - path: "packages/renderer/src/services/treAutoMount.ts"
      provides: "Unified single-mount: ONE ordered list across all families; one mountTrePaths call; one mountComplete call"
      exports: ["autoMountClient"]
      contains: "searchTreeRawPriorities"
    - path: "packages/renderer/src/services/treAutoMount.test.ts"
      provides: "B1/B2/B4 cross-family mount tests"
      contains: "mountComplete"
    - path: "packages/renderer/src/services/treMount.ts"
      provides: "injectLooseDirOverlay helper (conditional isOverride)"
      exports: ["injectLooseDirOverlay"]
    - path: "packages/renderer/src/state/treStore.ts"
      provides: "appendLooseEntries action"
      contains: "appendLooseEntries"
  key_links:
    - from: "packages/renderer/src/services/treAutoMount.ts"
      to: "packages/renderer/src/services/tocReader.ts"
      via: "readTocTreeNames for .tre name expansion"
      pattern: "readTocTreeNames"
    - from: "packages/renderer/src/services/treAutoMount.ts"
      to: "packages/renderer/src/services/clientSearchOrder.ts"
      via: "resolveClientMountOrder — searchTreeRawPriorities + looseDirPriorities for unified compaction"
      pattern: "searchTreeRawPriorities"
    - from: "packages/renderer/src/services/treAutoMount.ts"
      to: "packages/renderer/src/state/treStore.ts"
      via: "mountTrePaths (once) + mountComplete (once) — B1 constraint"
      pattern: "mountTrePaths"
    - from: "packages/renderer/src/services/treMount.ts"
      to: "packages/renderer/src/state/treStore.ts"
      via: "appendLooseEntries action for loose VFS injection"
      pattern: "appendLooseEntries"
---

<objective>
Build the TOC reader with full-index support (H1) and rewrite `treAutoMount.ts` for the unified single-mount strategy (B1/B2/B4 root fix). Task 1 creates `crc32.ts` (port of Crc.cpp) and `crc32.test.ts` (direct W3 oracle) and `tocReader.ts` with `parseTocHeader`, `readTocTreeNames`, and `readTocIndex`; turns the five tocReader RED tests GREEN. Task 2 replaces `treAutoMount.ts` internals to build ONE ordered list across all three cfg families (searchTree + expanded TOC + loose dirs), sort by raw-priority DESC, assign a single compact sequence, call `mountTrePaths` exactly once, then `injectLooseDirOverlay` for each loose dir (with conditional `isOverride`), then `mountComplete` exactly once.

Purpose: Correctness heart of the mount pipeline. B1 (double-mount clobbers searchTree overlays) and B2 (compact sequence applied per-family losing raw priority context) both have the same root fix: one ordered list, one compact sequence, one call. B4 (unconditional isOverride) is fixed by comparing rawPriority against maxTreRawPriority. H1 (intra-TOC duplicate resolution) is covered by the full-index reader using engine-faithful CRC-keyed binary search — NOT a Map/KEEP FIRST heuristic, which diverges from the engine on the 3 duplicates in sku0. The W3 crc32 direct oracle in crc32.test.ts is CI-runnable and independently validates the CRC port without requiring the real .toc.
Output: crc32.ts (Crc.cpp port), crc32.test.ts (W3 direct oracle), tocReader.ts (parseTocHeader + readTocTreeNames + readTocIndex), treAutoMount.ts (unified mount), treAutoMount.test.ts (B1/B2/B4 tests), treMount.ts (injectLooseDirOverlay with conditional isOverride), treStore.ts (appendLooseEntries).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-RESEARCH.md
@.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-PATTERNS.md

<interfaces>
GROUND TRUTH — DO NOT RE-DERIVE (cited file:line):

SearchTOC::Header — TreeFile_SearchNode.h:270-284 (36 bytes LE):
  offset  0: uint32  magic          = 0x544F4320 (bytes " COT")
  offset  4: uint32  version        = 0x30303031 (bytes "1000")
  offset  8: uint8   tocCompressor
  offset  9: uint8   fileNameBlockCompressor
  offset 10: uint8   unusedOne
  offset 11: uint8   unusedTwo
  offset 12: uint32  numberOfFiles          (193475 for sku0)
  offset 16: uint32  sizeOfTOC              (4643400 for sku0 = 193475 * 24)
  offset 20: uint32  sizeOfNameBlock
  offset 24: uint32  uncompSizeOfNameBlock
  offset 28: uint32  numberOfTreeFiles      (131 for sku0)
  offset 32: uint32  sizeOfTreeFileNameBlock (2757 for sku0)
Validated by Cursor against real sku0_client.toc bytes.

TableOfContentsEntry — TreeFile_SearchNode.h:289-299 (24 bytes LE, struct '<BBHIIIII'):
  byte  0 (1B): compressor       (uint8)
  byte  1 (1B): unused           (uint8)
  byte  2 (2B): treeFileIndex    (uint16 LE — resolution key for H1)
  byte  4 (4B): crc              (uint32 LE)
  byte  8 (4B): fileNameOffset   (uint32 LE — ON DISK stores filename LENGTH; must be rebuilt)
  byte 12 (4B): offset           (uint32 LE)
  byte 16 (4B): length           (uint32 LE)
  byte 20 (4B): compressedLength (uint32 LE)
Verified: tre_reader.py:381 SEARCH_TOC_ENTRY_FMT = '<BBHIIIII'

TOC blob start:   byte offset 36 + sizeOfTreeFileNameBlock
Name block start: byte offset 36 + sizeOfTreeFileNameBlock + sizeOfTOC

fileNameOffset rebuild (tre_reader.py:385-389):
  fn_offset = 0
  for each entry i:
    entry[i].fileNameOffset = fn_offset
    fn_offset += (disk_value_for_entry_i) + 1  // disk value = filename LENGTH; add 1 for NUL

H1 invariant: ENGINE-FAITHFUL CRC-KEYED BINARY SEARCH (NOT a Map / KEEP FIRST heuristic).
  The on-disk TOC array is already sorted by (crc, name) ascending — do NOT re-sort it.
  resolve(virtualPath): normalize to lowercase + forward-slashes; compute crc = crc32(query)
  using the Crc.cpp algorithm (forward CRC-32, polynomial 0x04C11DB7 — port in crc32.ts, see
  action below). Binary-search the entry array replicating SearchTOC::localExists
  (TreeFile_SearchNode.cpp:783-836): compare entries[mid].crc to crc; on crc-greater set hi=mid-1;
  on crc-less set lo=mid+1; on crc-equal tiebreak by case-insensitive localeCompare of the stored
  entry name vs query; stop when found (return that mid's entry). Return
  {treeFileIndex: entries[mid].treeFileIndex, treName: treeNames[entries[mid].treeFileIndex] ?? ''}
  on found; undefined on not found.
  Counterexample proving KEEP FIRST / lowest-index is WRONG: in real sku0_client.toc the entry for
  space_faction.stf with treeFileIndex=54 (patch_24_client_00.tre) appears at a LOWER array index
  than the entry with treeFileIndex=42 (patch_15_02.tre). KEEP FIRST returns 54 — WRONG. The binary
  search at TreeFile_SearchNode.cpp:783-836 lands on treeFileIndex=42 — CORRECT.
  open() at :891-937 then reads payload from m_treeFiles[entry.treeFileIndex] — confirming
  treeFileIndex is the authoritative TRE selector. CRC port: crc32.ts must match Crc.cpp exactly;
  the real-.toc → treeFileIndex===42 oracle test is the byte-exact gate (wrong CRC → wrong mid →
  oracle fails).

From Plan 02 — ClientMountOrder (full shape):
```typescript
export interface ClientMountOrder {
  trePaths:                string[];
  priorities:              number[];   // compact — treAutoMount IGNORES, uses searchTreeRawPriorities
  searchTreeRawPriorities: number[];   // raw cfg priorities parallel to trePaths
  looseDirs:               string[];
  looseDirPriorities:      number[];   // raw cfg priorities parallel to looseDirs
  tocEntries:   Array<{ tocFilePath: string; priority: number }>;
  tocTreePaths: string[];
  maxPriority:  number;
}
```

MountNode (internal type in treAutoMount.ts — do NOT export):
```typescript
interface MountNode {
  kind:        'tre' | 'loose';
  path:        string;
  rawPriority: number;
  sku:         number;   // tie-break: sku DESC
  cfgIndex:    number;   // tie-break within same sku/priority
}
```

treStore mountComplete (B1 root cause — cited treStore.ts:159):
  Zustand full state REPLACE: set({ mountHandle, archives, vfsEntries, ... })
  → calling mountTrePaths a second time erases everything from the first call.
  FIX: call mountTrePaths EXACTLY ONCE.

TocIndex (new interface — to be created in tocReader.ts):
```typescript
export interface TocIndexEntry {
  treeFileIndex: number;
  treName:       string;
}
export interface TocIndex {
  resolve(virtualPath: string): TocIndexEntry | undefined;
  readonly size: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: crc32.ts (Crc.cpp port) + crc32.test.ts (W3 direct oracle) + tocReader.ts full-index reader (parseTocHeader + readTocTreeNames + readTocIndex / H1)</name>
  <read_first>
    - packages/native-core/modules/core/tre/Crc.cpp (FULL — harvest the table-generation loop and crcCalculate function; do NOT invent the polynomial; port exactly to crc32.ts)
    - ../swg-client-v2/.../TreeFile_SearchNode.cpp:783-836 (SearchTOC::localExists — the binary search algorithm to replicate for resolve(); cite this in the crc32 import and the binary search loop)
    - packages/renderer/src/services/tocReader.test.ts (the five RED test cases from Plan 01 Task 2 — implement to make all non-skipIf tests GREEN)
    - packages/harness/fixtures-real/toc/sku0_client_header.bin (2793-byte oracle — tests (a) and (b) use this; readTocTreeNames must work on this 2793-byte slice which contains ONLY header + tree-name block, NOT the TOC blob)
    - ../swg-client-v2/.../TreeFile_SearchNode.h:265-310 (ground truth — re-read for exact struct layout before coding)
    - ../swg-blender-plugin/swg_pipeline/tre_reader.py:330-413 (working Python reader — harvest fileNameOffset rebuild at lines 385-389)
    - 04.2-RESEARCH.md §GIVEN A8 (TableOfContentsEntry struct + space_faction.stf counterexample)
    - 04.2-PATTERNS.md §tocReader.ts (exact function signatures, Buffer usage, no stream API)
  </read_first>
  <behavior>
    - Direct CRC32 oracle (W3, UNVERIFIED-pending-executor-confirm): crc32('appearance/mesh/thm_tato_imprv_building_s09_r0_mesh_l4.msh') === 3830594; crc32.test.ts has code comment "CONFIRM this value against native-core Crc.cpp before executing — byte-exact CRC-port gate"; CI-runnable without real .toc
    - parseTocHeader(buf: Buffer) where buf has the first 36 bytes of sku0_client_header.bin → numberOfFiles===193475, numberOfTreeFiles===131, sizeOfTOC===4643400
    - parseTocHeader on synthetic 36-byte buffer with known fields → returns those fields exactly
    - parseTocHeader with bad magic → throws Error containing "magic"
    - readTocTreeNames(filePath) where filePath is sku0_client_header.bin (2793-byte oracle) → length===131; result[0]==='bottom.tre'
    - readTocTreeNames on buf shorter than 36 + sizeOfTreeFileNameBlock → throws (bounds check)
    - readTocIndex(filePath) on synthetic 3-entry fixture (test c from Plan 01): resolve('string/ja/space/space_faction.stf').treeFileIndex===0 — binary search mid=(0+2)/2=1 lands on the SECOND duplicate entry (treeFileIndex=0 / patch_15_02.tre), NOT the first duplicate (treeFileIndex=1 / patch_24 analogue) which KEEP FIRST / Map would return
    - readTocIndex(filePath) on real sku0_client.toc (skipIf): resolve('string/ja/space/space_faction.stf').treeFileIndex===42
  </behavior>
  <action>
    STEP A — Create `packages/renderer/src/services/crc32.ts`. Read Crc.cpp first (full). Port the 256-entry table-generation loop and the crcCalculate function exactly: polynomial 0x04C11DB7, init 0xFFFFFFFF, final XOR 0xFFFFFFFF, MSB-first (forward; NOT the reflected Ethernet CRC 0xEDB88320). Export `export function crc32(input: string): number`. Input is treated as Latin-1 / single-byte (each char's char code is the byte value, consistent with how the engine passes filenames to Crc::calculate). The real-.toc oracle test (d) asserts resolve('string/ja/space/space_faction.stf').treeFileIndex===42 — if the CRC port is wrong, binary search lands on the wrong mid and that assertion fails. Do NOT add any other exports or imports to crc32.ts itself.

    Alongside `packages/renderer/src/services/crc32.ts`, create `packages/renderer/src/services/crc32.test.ts`. Add a direct oracle test:
      `it('byte-exact CRC-port oracle', () => {`
      `  // CONFIRM this value against native-core Crc.cpp before executing — byte-exact CRC-port gate`
      `  expect(crc32('appearance/mesh/thm_tato_imprv_building_s09_r0_mesh_l4.msh')).toBe(3830594);`
      `});`
    Treat 3830594 as UNVERIFIED-pending-executor-confirm: the executor MUST verify the expected value by running Crc.cpp's crcCalculate on the same input before committing this test file. This test is CI-runnable (no file I/O) and independently validates the CRC port without requiring the real .toc.

    STEP B — Create `packages/renderer/src/services/tocReader.ts`. Node.js only (`// @vitest-environment node`-compatible). Use `import * as fs from 'fs'` and `import * as path from 'path'`. Import `crc32` from `./crc32` (created in Step A). Do NOT use stream API; all reads use `fs.readFileSync`. Use `(require('zlib') as {inflateSync:(buf:Buffer)=>Buffer}).inflateSync` for decompression (Node built-in, no new dep).

    Export `TocHeader` interface: all 10 fields from the header layout above (token, version, tocCompressor, fileNameBlockCompressor, unusedOne, unusedTwo, numberOfFiles, sizeOfTOC, sizeOfNameBlock, uncompSizeOfNameBlock, numberOfTreeFiles, sizeOfTreeFileNameBlock). Doc-comment cites `TreeFile_SearchNode.h:270-284` and `tre_reader.py:335-413`.

    Export `TocIndexEntry` and `TocIndex` interfaces (from interfaces block above).

    Implement `export function parseTocHeader(buf: Buffer): TocHeader`:
    - Bounds check: `if (buf.length < 36) throw new Error('Invalid .toc: header too short')`.
    - Read magic as two uint32LE at offsets 0 and 4. Validate: `token === 0x544F4320 && version === 0x30303031`; throw `Error('Invalid .toc magic: expected " COT1000", got 0x' + token.toString(16))` on mismatch.
    - Read remaining uint8 fields at offsets 8, 9, 10, 11 with `buf.readUInt8(offset)`.
    - Read uint32 fields at offsets 12, 16, 20, 24, 28, 32 with `buf.readUInt32LE(offset)`.
    - Return `TocHeader`.

    Implement `export function readTocTreeNames(filePath: string): string[]`:
    - `fs.readFileSync(filePath)` → full `Buffer`. Parse header (first 36 bytes). Bounds check: `if (buf.length < 36 + header.sizeOfTreeFileNameBlock) throw new Error('Malformed .toc: tree-name block exceeds file size')`.
    - Tree-name block: `buf.subarray(36, 36 + header.sizeOfTreeFileNameBlock)`. Scan NUL-terminated Latin-1 strings; collect until `header.numberOfTreeFiles` names are found. Return `string[]`.
    - NOTE: this function works on the 2793-byte oracle fixture (header + tree-name block only) because it never reads past offset 2793.

    Implement `export function readTocIndex(filePath: string): TocIndex`:
    - `fs.readFileSync(filePath)` → full `Buffer`. Call `parseTocHeader(buf.subarray(0, 36))` → `header`.
    - `tocBlobStart = 36 + header.sizeOfTreeFileNameBlock`.
    - Bounds check: `if (buf.length < tocBlobStart + header.sizeOfTOC) throw new Error('Malformed .toc: TOC blob exceeds file size')`.
    - Assert `header.sizeOfTOC === header.numberOfFiles * 24`; throw if mismatch.
    - `rawTocBlob = buf.subarray(tocBlobStart, tocBlobStart + header.sizeOfTOC)`.
    - If `header.tocCompressor !== 0`: `tocBlob = inflateSync(rawTocBlob)`. Else `tocBlob = rawTocBlob`.
    - `nameBlockStart = tocBlobStart + header.sizeOfTOC`.
    - `rawNameBlock = buf.subarray(nameBlockStart)`. If `header.fileNameBlockCompressor !== 0`: `nameBlock = inflateSync(rawNameBlock)`. Else `nameBlock = rawNameBlock`.
    - Get `treeNames = readTocTreeNames(filePath)`.
    - Parse entries into array preserving ON-DISK ORDER (which is already sorted by (crc, name) ascending — DO NOT re-sort): for each i in 0..header.numberOfFiles-1 collect `{ crc: uint32LE at i*24+4, treeFileIndex: uint16LE at i*24+2, fileNameLength: uint32LE at i*24+8, fileNameOffset: 0 (to be rebuilt) }`.
    - Rebuild fileNameOffset: `fn_offset=0`; for each entry i: `entries[i].fileNameOffset = fn_offset; fn_offset += entries[i].fileNameLength + 1` (per tre_reader.py:385-389).
    - ENGINE-FAITHFUL BINARY SEARCH — DO NOT build a Map or use KEEP FIRST. The resolve function replicates SearchTOC::localExists (TreeFile_SearchNode.cpp:783-836): normalize query `q = virtualPath.toLowerCase().replace(/\\/g, '/')`, compute `qCrc = crc32(q)`, then binary-search: `let lo=0, hi=entries.length-1`; while `lo<=hi`: `mid=(lo+hi)>>>1`; if `entries[mid].crc < qCrc`: `lo=mid+1`; elif `entries[mid].crc > qCrc`: `hi=mid-1`; else (crc equal): read name `n = nameBlock.subarray(entries[mid].fileNameOffset, entries[mid].fileNameOffset + entries[mid].fileNameLength).toString('latin1')`; `cmp = n.localeCompare(q, undefined, {sensitivity:'base'})`; if `cmp<0`: `lo=mid+1`; elif `cmp>0`: `hi=mid-1`; else `found=true, break`. Return `{treeFileIndex: entries[mid].treeFileIndex, treName: treeNames[entries[mid].treeFileIndex] ?? ''}` on found, else `undefined`.
    - Return `{ resolve: (vp: string) => ..., size: entries.length }`.

    Run: `pnpm --filter @swg/renderer test -- --run src/services/crc32.test.ts src/services/tocReader.test.ts`. crc32.test.ts oracle must pass; tocReader tests (a)(b)(c)(e) must pass; (d) may be skipIf.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer test -- --run src/services/tocReader.test.ts 2>&1 | grep -E "passed|failed"` — (a)(b)(c)(e) GREEN; (d) GREEN or skipped
    - Direct CRC32 oracle (W3): `grep -c "3830594" packages/renderer/src/services/crc32.test.ts` returns at least 1
    - Direct CRC32 oracle (W3): `grep -c "CONFIRM this value against native-core Crc.cpp" packages/renderer/src/services/crc32.test.ts` returns at least 1 (byte-exact CRC-port gate comment present)
    - `pnpm --filter @swg/renderer test -- --run src/services/crc32.test.ts 2>&1 | grep -E "passed|failed"` — oracle test GREEN (after executor verifies 3830594 against Crc.cpp)
    - H1 CI invariant: test (c) proves `resolve('string/ja/space/space_faction.stf').treeFileIndex === 0` on synthetic 3-entry fixture — binary search lands at mid=1 returning the second duplicate (treeFileIndex=0), NOT the first duplicate (treeFileIndex=1) that KEEP FIRST would return; proves CI catches a KEEP FIRST implementation without the real .toc
    - Oracle test (a): `parseTocHeader` on first 36 bytes of sku0_client_header.bin → numberOfFiles===193475, numberOfTreeFiles===131, sizeOfTOC===4643400
    - crc32.ts artifact: `grep -c "0x04C11DB7" packages/renderer/src/services/crc32.ts` returns at least 1 (correct polynomial present)
    - `grep -c "crc32" packages/renderer/src/services/tocReader.ts` returns at least 1 (crc32 import used in binary search)
    - `grep -c "readTocIndex" packages/renderer/src/services/tocReader.ts` returns at least 1
    - `grep -c "inflateSync" packages/renderer/src/services/tocReader.ts` returns at least 1
    - `grep -c "0x544F4320" packages/renderer/src/services/tocReader.ts` returns at least 1
    - Field name (I1 fix): `grep -c "fileNameBlockCompressor" packages/renderer/src/services/tocReader.ts` returns at least 1 (correct field name per TreeFile_SearchNode.h:275 offset 9)
    - `pnpm -w tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/tocReader.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>crc32.ts created (Crc.cpp port, polynomial 0x04C11DB7); crc32.test.ts created with W3 direct oracle (3830594, UNVERIFIED-pending-executor-confirm); tocReader tests (a)(b)(c)(e) GREEN; readTocIndex H1 invariant proven via binary search on CI synthetic fixture; fileNameBlockCompressor field name correct; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: treAutoMount unified single-mount (B1/B2/B4) + injectLooseDirOverlay + appendLooseEntries + treAutoMount.test.ts</name>
  <read_first>
    - packages/renderer/src/services/treAutoMount.ts (full — understand current structure to replace internals)
    - packages/renderer/src/state/treStore.ts:150-175 (mountComplete B1 root cause — full set({}) at line 159)
    - packages/renderer/src/services/treMount.ts:1-30, 132-171 (imports + mountTrePaths to add injectLooseDirOverlay alongside)
    - 04.2-PATTERNS.md §treAutoMount.ts (MountNode type, unified sort, single-call structure)
    - 04.2-RESEARCH.md §Capability 2 §Mount precedence (pitfall 6: double-mount; unified list semantics)
    - packages/renderer/src/services/clientSearchOrder.ts (Plan 02 output — full ClientMountOrder shape with new fields)
  </read_first>
  <behavior>
    - B1: mountTrePaths called exactly once per autoMountClient invocation
    - B1: mountComplete called exactly once per autoMountClient invocation
    - B2: with searchTree@8 and tocEntry@3 expanding to 2 paths: treNodes[0] (searchTree) gets compact priority 3, treNodes[1]/[2] (toc) get 2 and 1 — searchTree strictly higher
    - B4 isOverride=false: searchPath@5, maxTreRawPriority=8 → injectLooseDirOverlay called with {isOverride: false}
    - B4 isOverride=true: searchPath@10, maxTreRawPriority=8 → injectLooseDirOverlay called with {isOverride: true}
    - Missing .tre (existsSync returns false for a tree name): warned and skipped, no throw
  </behavior>
  <action>
    Replace internals of `packages/renderer/src/services/treAutoMount.ts`. Keep the exported function signature `autoMountClient(installPath: string): Promise<void>` unchanged. All prior implementation is replaced.

    Add imports: `import { readTocTreeNames } from './tocReader'`; keep existing imports for `resolveClientMountOrder`, `mountTrePaths`, `mountComplete`, `injectLooseDirOverlay` (to be added to treMount.ts in this same task). Add `import * as fs from 'fs'`; add `import * as path from 'path'`.

    Define internal interface `MountNode` (per interfaces block above — not exported).

    Implement internal `buildTreNodes(order: ClientMountOrder): MountNode[]`:
    1. searchTree family: `order.trePaths.map((p, i) => ({ kind: 'tre', path: p, rawPriority: order.searchTreeRawPriorities[i], sku: 0, cfgIndex: i }))`.
    2. tocEntry family: for each `order.tocEntries[t]` at index t, call `readTocTreeNames(tocEntries[t].tocFilePath)` → `treeNames`; for each name, try each prefix in `order.tocTreePaths`, `path.join(prefix, name)` → if `fs.existsSync`, push `{ kind: 'tre', path, rawPriority: order.tocEntries[t].priority, sku: t, cfgIndex: t }` and break to next name; if none exists, `console.warn('[treAutoMount] missing .tre:', name)` and skip.
    3. Sort ALL collected nodes: `(a, b) => (b.rawPriority - a.rawPriority) || (b.sku - a.sku) || (b.cfgIndex - a.cfgIndex)`.
    4. Return sorted nodes.

    Implement `autoMountClient(installPath: string): Promise<void>`:
    1. `const order = resolveClientMountOrder(installPath, installPath)`. If null, log warning and return.
    2. `const treNodes = buildTreNodes(order)`.
    3. `const compactPriorities = treNodes.map((_, i) => treNodes.length - i)`.
    4. `await mountTrePaths(treNodes.map(n => n.path), compactPriorities)` — EXACTLY ONE CALL (B1). Do NOT call mountTrePaths again.
    5. `const maxTreRawPriority = order.searchTreeRawPriorities.length > 0 ? Math.max(...order.searchTreeRawPriorities) : 0`.
    6. For each `order.looseDirs[i]`: `injectLooseDirOverlay(order.looseDirs[i], { isOverride: order.looseDirPriorities[i] > maxTreRawPriority })`.
    7. `mountComplete()` — EXACTLY ONE CALL (B1). Do NOT call mountComplete again anywhere in this function.

    Edit `packages/renderer/src/services/treMount.ts`: add export `function injectLooseDirOverlay(dir: string, opts: { isOverride: boolean }): void`. Implementation: recursively scan `dir` using `fs.readdirSync(dir, { withFileTypes: true })` + `path.join`; for each file build `VfsEntry` with `isOverride: opts.isOverride`; collect and call `useTreStore.getState().appendLooseEntries(entries)`. Wrap in try/catch; log error + return without throwing (consistent with autoMountClient error handling). `virtualPath` is the relative path from the dir root with forward-slash normalization and lowercasing.

    Edit `packages/renderer/src/state/treStore.ts`: add `appendLooseEntries(entries: VfsEntry[]): void` action to the Zustand store. Implementation: merge into existing `vfsEntries` by prepending new entries; de-dup by lowercased `path` (new entry wins). Do NOT replace archives or mountHandle. Follow the existing action pattern in the file.

    Create `packages/renderer/src/services/treAutoMount.test.ts`. Use `// @vitest-environment node` (or whatever env the existing treMount tests use). Mock `mountTrePaths`, `mountComplete`, `injectLooseDirOverlay` using `vi.mock`; mock `readTocTreeNames` to return a fixed list; mock `fs.existsSync` to return true for all paths. Write four test cases:
    (a) B1 test: ClientMountOrder with one searchTree .tre + one tocEntry expanding to 1 .tre → `mountTrePaths` called exactly 1 time (`toHaveBeenCalledTimes(1)`); `mountComplete` called exactly 1 time.
    (b) B2 test: ClientMountOrder with one searchTree@rawPriority=8 and one tocEntry@rawPriority=3 expanding to 2 .tre files → `mountTrePaths` receives the searchTree path as the FIRST element of the path array; the compact priority for the searchTree path (first element) is strictly greater than both toc path priorities (`compactPriorities[0] > compactPriorities[1]` and `compactPriorities[0] > compactPriorities[2]`). Assert by capturing the call arguments to `mountTrePaths`.
    (c) B4 isOverride=false: ClientMountOrder with searchTreeRawPriorities=[8] and looseDirPriorities=[5] → `injectLooseDirOverlay` called with second argument `{ isOverride: false }`.
    (d) B4 isOverride=true: ClientMountOrder with searchTreeRawPriorities=[8] and looseDirPriorities=[10] → `injectLooseDirOverlay` called with second argument `{ isOverride: true }`.

    Run: `pnpm --filter @swg/renderer test -- --run src/services/treAutoMount.test.ts`. All 4 tests must pass.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer test -- --run src/services/treAutoMount.test.ts 2>&1 | grep -E "passed|failed"` — 4/4 GREEN
    - B1 enforcement in source: `grep -c "mountTrePaths" packages/renderer/src/services/treAutoMount.ts` returns 1 (single call site)
    - B1 enforcement in source: `grep -c "mountComplete" packages/renderer/src/services/treAutoMount.ts` returns 1 (single call site)
    - B4 conditional in source: `grep -c "isOverride.*>.*maxTreRawPriority\|looseDirPriorities.*maxTre" packages/renderer/src/services/treAutoMount.ts` returns at least 1
    - `grep -c "injectLooseDirOverlay" packages/renderer/src/services/treMount.ts` returns at least 1
    - `grep -c "appendLooseEntries" packages/renderer/src/state/treStore.ts` returns at least 1
    - tocReader tests still GREEN: `pnpm --filter @swg/renderer test -- --run src/services/tocReader.test.ts 2>&1 | grep failed` returns nothing
    - `pnpm -w tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/treAutoMount.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>treAutoMount.test.ts 4/4 GREEN; mountTrePaths called once; searchTree@8 compact > toc@3 compact; B4 isOverride conditional; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .toc file → tocReader | Binary game data; malformed header or oversized sizeOfTOC can cause OOM/RangeError |
| TOCTreePath prefix → existsSync | User-controlled cfg path; read-only filesystem probe, no shell-out |
| compact priorities → mountTrePaths | Internal integer array; cross-family sort must be deterministic |
| loose dir → injectLooseDirOverlay | Filesystem scan; virtualPath must not escape the dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-08 | DoS | tocReader — malformed header (sizeOfTOC > fileSize) | mitigate | Bounds check: `if (tocBlobStart + sizeOfTOC > buf.length) throw Error('TOC blob exceeds file size')` |
| T-04.2-09 | DoS | tocReader — sizeOfTOC !== numberOfFiles × 24 | mitigate | Assert `sizeOfTOC === numberOfFiles * 24` before parsing; throw on mismatch |
| T-04.2-10 | Tampering | treAutoMount — double-mount store replace (B1) | mitigate | Single mountTrePaths call enforced structurally (grep count === 1); B1 test asserts toHaveBeenCalledTimes(1) |
| T-04.2-11 | Tampering | treAutoMount — priority compaction scramble (B2) | mitigate | Single unified sort before compact assignment; B2 test asserts searchTree@8 strictly above toc@3 |
| T-04.2-12 | Elevation | treAutoMount — searchPath@5 treated as override (B4) | mitigate | `isOverride: looseDirRawPriority > maxTreRawPriority`; B4 tests (c)(d) assert both branches |
| T-04.2-13 | Tampering | injectLooseDirOverlay — virtualPath traversal (dir scan) | accept | Virtual paths are computed from relative path within the scanned dir — cannot escape; no shell-out |
| T-04.2-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; zlib is Node built-in |
</threat_model>

<verification>
After Plan 03:
`pnpm --filter @swg/renderer test -- --run src/services/crc32.test.ts` — direct oracle test GREEN (after executor verifies 3830594).
`pnpm --filter @swg/renderer test -- --run src/services/tocReader.test.ts` — (a)(b)(c)(e) GREEN; (d) GREEN or skipped.
`pnpm --filter @swg/renderer test -- --run src/services/treAutoMount.test.ts` — 4/4 GREEN.
`pnpm --filter @swg/renderer test -- --run src/services/clientSearchOrder.test.ts` — still GREEN (Plan 02).
`pnpm -w tsc --noEmit` — exits 0.
</verification>

<success_criteria>
- parseTocHeader on sku0_client_header.bin oracle: numberOfFiles=193475, numberOfTreeFiles=131, sizeOfTOC=4643400
- crc32.ts: table-based port of Crc.cpp, polynomial 0x04C11DB7; the real-.toc → treeFileIndex===42 oracle test is the byte-exact CRC-port gate
- crc32.test.ts: direct oracle `crc32('appearance/mesh/thm_tato_imprv_building_s09_r0_mesh_l4.msh') === 3830594` (UNVERIFIED-pending-executor-confirm) with CONFIRM comment; CI-runnable without real .toc
- readTocIndex H1 CI invariant: synthetic 3-entry fixture resolves space_faction.stf to treeFileIndex=0 via binary search at mid=1 (NOT treeFileIndex=1 which KEEP FIRST returns from the lower-indexed duplicate)
- fileNameBlockCompressor field name used (not fileNameCompressor) per TreeFile_SearchNode.h:275
- treAutoMount calls mountTrePaths ONCE and mountComplete ONCE (B1)
- searchTree@8 gets strictly higher compact priority than toc@3 entries (B2)
- isOverride conditional on rawPriority vs maxTreRawPriority (B4)
- All tests GREEN; tsc clean
</success_criteria>

<output>
Create `.planning/phases/04.2-dev-client-support-loose-override-deploy/04.2-03-SUMMARY.md` when done.
</output>
