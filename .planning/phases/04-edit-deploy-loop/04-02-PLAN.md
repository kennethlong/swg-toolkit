---
phase: 04-edit-deploy-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
  - packages/renderer/src/panels/deploy/ActionBadge.tsx
  - packages/renderer/src/panels/deploy/StagingPanel.tsx
  - packages/renderer/src/shell/StatusBar.tsx
  - packages/renderer/src/workspace/workspace-config.ts
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "When no workspace is open, the staging panel renders the WorkspaceEntry empty state (Open Project… / New Project… buttons)."
    - "When a workspace is open with staged entries, StagingPanel renders each row with an ActionBadge (add/modify/delete) + virtual path (mono) + source info + remove button."
    - "Pack Patch / Deploy button in StagingPanel head is disabled when entries is empty; enabled when entries exist."
    - "StatusBar shows workspace name (mono), client-detected indicator, and a ⚠ badge when hasStaleDeployment is true."
    - "StagingPanel, ChangesetTimelinePanel (stub), and VcsPanel (stub) are registered in workspace-config.ts buildInitialLayout — NOT in WorkspaceShell.tsx."
  artifacts:
    - path: packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
      provides: Open Project / New Project empty-state; button styles defined LOCALLY (not imported from ExportDialog)
    - path: packages/renderer/src/panels/deploy/ActionBadge.tsx
      provides: StagingAction glyph+color+label badge (add/modify/delete)
    - path: packages/renderer/src/panels/deploy/StagingPanel.tsx
      provides: Virtualized staging list (ROW_HEIGHT=30, OVERSCAN=8), empty state, Deploy CTA
    - path: packages/renderer/src/shell/StatusBar.tsx
      provides: workspace name + client indicator + stale-deployment badge
    - path: packages/renderer/src/workspace/workspace-config.ts
      provides: staging/changesets/vcs panel IDs registered in buildInitialLayout (W3 fix)
  key_links:
    - from: packages/renderer/src/panels/deploy/StagingPanel.tsx
      to: packages/renderer/src/state/stagingStore.ts
      via: "useStagingStore((s) => s.entries)"
      pattern: "useStagingStore"
    - from: packages/renderer/src/workspace/workspace-config.ts
      to: packages/renderer/src/panels/deploy/StagingPanel.tsx
      via: "'staging': StagingPanel in panelComponents / addPanel"
      pattern: "'staging'"
---

## Phase Goal

**As a** SWG mod developer, **I want to** stage edited files in a project workspace and build a deployable `.tre` patch that activates via the client config, **so that** I can iterate on mod changes in-game, roll back to any prior state, and version my work safely via Git/LFS.

<objective>
Build the staging list UI and register the three new dockview panels. Fixes W1 (button styles must be local, not imported from ExportDialog) and W3 (wiring target is workspace-config.ts buildInitialLayout, not WorkspaceShell.tsx).

Purpose: After this plan, a user can open a workspace, see the staging panel, add entries, and hit Deploy. The virtualized list follows the VfsTree pattern so it scales to any number of entries.

Output: WorkspaceEntry (with LOCAL button styles — W1), ActionBadge, StagingPanel (virtualized, ROW_HEIGHT=30), StatusBar extensions (stale-deployment badge — W7), workspace-config.ts panel registrations (W3).
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.claude\get-shit-done\workflows\execute-plan.md
@D:\Code\SWG-Toolkit\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-edit-deploy-loop/04-CONTEXT.md
@.planning/phases/04-edit-deploy-loop/04-UI-SPEC.md
@.planning/phases/04-edit-deploy-loop/04-PATTERNS.md

<interfaces>
<!-- Zustand store selectors — use these exact signatures (from 04-01) -->
import { useStagingStore  } from '../../state/stagingStore';
import { useWorkspaceStore } from '../../state/workspaceStore';
import { useChangesetStore } from '../../state/changesetStore';

useStagingStore((s) => s.entries)              // StagingEntry[]
useStagingStore((s) => s.buildStatus)          // StagingBuildStatus
useStagingStore.getState().addEntry(e)         // imperative upsert
useStagingStore.getState().removeEntry(vp)     // imperative by virtualPath
useStagingStore.getState().beginBuild()

useWorkspaceStore((s) => s.workspaceName)      // string | null
useWorkspaceStore((s) => s.clientPath)         // string | null
useWorkspaceStore((s) => s.status.kind)        // 'idle'|'opening'|'ready'|'error'
useWorkspaceStore((s) => s.hasStaleDeployment) // boolean — W7

useChangesetStore((s) => s.manifest.activeVersionId)   // string | null (NEW graph model)
useChangesetStore((s) => s.manifest.deployedVersionId) // string | null (NEW — W10)

<!-- workspaceService -->
import { openWorkspace, createWorkspace } from '../../services/workspaceService';

<!-- VfsTree virtualization constants (mandatory, never change) -->
const ROW_HEIGHT = 30;  // matches VfsTree, InspectorPanel, HexInspector
const OVERSCAN   = 8;

<!-- W1 fix: button styles MUST be defined locally, NOT imported from ExportDialog.tsx -->
// primaryBtnStyle and secondaryBtnStyle are LOCAL const functions in WorkspaceEntry.tsx
// and in StagingPanel.tsx respectively — they are NOT exported from ExportDialog.
// (The cross-AI review found ExportDialog does not export these — importing them causes TS error.)
// Pattern to copy (style-only, no side effects):
const primaryBtnStyle = (): React.CSSProperties => ({
  background: 'var(--color-accent)',
  color: 'var(--color-text-on-accent)',
  border: 'none',
  borderRadius: 'var(--radius-sm)',
  padding: 'var(--space-2) var(--space-4)',
  fontFamily: 'var(--font-sans)',
  fontSize: 'var(--text-sm)',
  cursor: 'pointer',
});
const secondaryBtnStyle = (): React.CSSProperties => ({
  background: 'transparent',
  color: 'var(--color-text)',
  border: '1px solid var(--color-border)',
  borderRadius: 'var(--radius-sm)',
  padding: 'var(--space-2) var(--space-4)',
  fontFamily: 'var(--font-sans)',
  fontSize: 'var(--text-sm)',
  cursor: 'pointer',
});

<!-- packPatch + changesetService stubs (04-03/04-04 are Wave 2 parallel plans) -->
// StagingPanel imports these with stubs if files don't exist yet
import { packPatch, buildPatchName } from '../../services/packPatch';
import { sealVersion } from '../../services/changesetService';

<!-- W3 fix: dockview panel wiring target -->
// W3: buildInitialLayout lives in workspace-config.ts, NOT WorkspaceShell.tsx.
// WorkspaceShell.tsx owns panelComponents map but NOT buildInitialLayout addPanel calls.
// Read workspace-config.ts FIRST to find the exact location and existing addPanel calls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceEntry.tsx + ActionBadge.tsx + StatusBar extend + workspace-config.ts extend (W1/W3/W7)</name>
  <files>
    packages/renderer/src/panels/deploy/WorkspaceEntry.tsx,
    packages/renderer/src/panels/deploy/ActionBadge.tsx,
    packages/renderer/src/shell/StatusBar.tsx,
    packages/renderer/src/workspace/workspace-config.ts
  </files>
  <read_first>
    packages/renderer/src/panels/tre/VfsTree.tsx lines 127-148 — read the exact empty-state div structure, flex layout, color tokens. WorkspaceEntry mirrors this structure.
    packages/renderer/src/panels/viewport/ExportDialog.tsx — read the FULL file. Extract primaryBtnStyle and secondaryBtnStyle as LOCAL consts for WorkspaceEntry. Do NOT import them from ExportDialog (W1 fix: they are not exported).
    packages/renderer/src/shared/VerificationStatus.tsx lines 29-39 — read the VARIANT_CONFIG pattern. ActionBadge uses the same triple-encode pattern.
    packages/renderer/src/shell/StatusBar.tsx — read the FULL file. Find existing store selectors; add NEW selectors after. Find JSX section; add workspace/stale-deploy indicators.
    packages/renderer/src/workspace/workspace-config.ts — read the FULL file (W3 fix). Find buildInitialLayout function and the existing addPanel calls. Add new panel registrations here, NOT in WorkspaceShell.tsx.
    packages/renderer/src/workspace/WorkspaceShell.tsx — read the FULL file to find the panelComponents record. New panel components ARE registered here (in panelComponents) but the addPanel calls go in workspace-config.ts.
    04-UI-SPEC.md §Surface 1 (Workspace Entry) + §Surface 2 (ActionBadge color table) — read for exact copy strings, colors, glyph assignments.
    04-PATTERNS.md §ActionBadge.tsx — read the ACTION_CONFIG and render pattern exactly.
  </read_first>
  <action>
    packages/renderer/src/panels/deploy/WorkspaceEntry.tsx:
    Import React, useCallback, useState. Import openWorkspace, createWorkspace from '../../services/workspaceService'. Import useWorkspaceStore from '../../state/workspaceStore'.
    Define primaryBtnStyle and secondaryBtnStyle as LOCAL const functions (W1 fix — do NOT import from ExportDialog; ExportDialog does not export them). Follow the style shown in the interfaces block above.
    Render: centered flex-column div with 'No mod workspace open' heading (--text-md, weight 600, --color-text-muted), body text per UI-SPEC, and two buttons: [Open Project…] (primary, onClick = electron dialog.showOpenDialog({properties:['openDirectory']}) → openWorkspace(path)) and [New Project…] (secondary, onClick = dialog → createWorkspace(path)). Show AsyncProgress while status.kind === 'opening'. Import { dialog } from 'electron' via require('electron') for the folder picker.

    packages/renderer/src/panels/deploy/ActionBadge.tsx:
    Follow PATTERNS.md §ActionBadge.tsx exactly. Import React. Import StagingAction from '@swg/contracts'. Define ACTION_CONFIG: Record<StagingAction, {glyph, label, colorVar}>: add→('+','add','var(--color-info)'), modify→('~','modify','var(--color-text-muted)'), delete→('⊘','delete (tombstone)','var(--color-warn)'). Export ActionBadge component: span with aria-label=label, title=label, mono font-family, --text-xs, flexShrink 0. Triple-encoded: glyph (aria-hidden) + label text.

    packages/renderer/src/shell/StatusBar.tsx (extend):
    Read the existing file first to find exact locations. Add new imports after existing ones:
      import { useWorkspaceStore } from '../state/workspaceStore';
      import { useChangesetStore } from '../state/changesetStore';
    Add selectors inside component body after existing selectors:
      const workspaceName     = useWorkspaceStore((s) => s.workspaceName);
      const staleDeployment   = useWorkspaceStore((s) => s.hasStaleDeployment);
      const clientDetected    = useWorkspaceStore((s) => s.clientPath !== null);
      const deployedVersionId = useChangesetStore((s) => s.manifest.deployedVersionId);
    Add to JSX output AFTER existing live mode indicator:
      workspace name span (mono, --text-xs, --color-info when set, --color-text-faint when null)
      Dot
      client indicator (mono, --text-xs, clientDetected ? '● client' : '○ no client')
      {staleDeployment && <span style={{color:'var(--color-warn)', fontFamily:'var(--font-mono)', fontSize:'var(--text-xs)'}}>⚠ deployed patch missing from cfg</span>}

    packages/renderer/src/workspace/workspace-config.ts (W3 fix):
    Read the FULL file first. Find buildInitialLayout function (confirmed at line 56 per W3 review finding). Add three addPanel calls in that function (NOT in WorkspaceShell.tsx, NOT after a 'live-inspector' panel that doesn't exist here):
      api.addPanel({ id: 'staging', component: 'staging', title: 'Staging', position: { direction: 'left', referencePanel: 'sidebar' }, initialWidth: 260 });
      api.addPanel({ id: 'changesets', component: 'changesets', title: 'Changesets', position: { direction: 'below', referencePanel: 'staging' }, initialHeight: 200 });
      api.addPanel({ id: 'vcs', component: 'vcs', title: 'Version Control', position: { direction: 'below', referencePanel: 'changesets' }, initialHeight: 180 });
    Also add to WorkspaceShell.tsx panelComponents (NOT buildInitialLayout) — import and register:
      'staging':    StagingPanel,
      'changesets': ChangesetTimelinePanel,   (stub file from this plan)
      'vcs':        VcsPanel,                 (stub file from this plan)
    Write stub files for ChangesetTimelinePanel and VcsPanel if they do not exist (export default function returning empty div — real implementations come in 04-04b and 04-05).
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    pnpm --filter @swg/renderer exec tsc --noEmit exits 0.
    grep -c "primaryBtnStyle\|secondaryBtnStyle" packages/renderer/src/panels/deploy/WorkspaceEntry.tsx gives 2+ (defined locally).
    grep -c "from.*ExportDialog" packages/renderer/src/panels/deploy/WorkspaceEntry.tsx gives 0 (W1: NOT imported from ExportDialog).
    grep -c "workspaceName\|hasStaleDeployment" packages/renderer/src/shell/StatusBar.tsx gives 2+.
    grep -c "deployed patch missing\|staleDeployment\|stale" packages/renderer/src/shell/StatusBar.tsx gives 1+ (W7 stale badge).
    grep -c "'staging'\|\"staging\"" packages/renderer/src/workspace/workspace-config.ts gives 1+ (W3: in workspace-config.ts).
    grep -c "'changesets'\|\"changesets\"" packages/renderer/src/workspace/workspace-config.ts gives 1+ (W3).
    grep -c "ACTION_CONFIG" packages/renderer/src/panels/deploy/ActionBadge.tsx gives 1+.
    grep -c "aria-label\|aria-hidden" packages/renderer/src/panels/deploy/ActionBadge.tsx gives 2+.
    grep -c "Open Project\|New Project" packages/renderer/src/panels/deploy/WorkspaceEntry.tsx gives 2.
  </acceptance_criteria>
  <done>WorkspaceEntry has local button styles (W1 fix); workspace-config.ts receives addPanel calls (W3 fix); StatusBar shows stale-deployment badge (W7); ActionBadge triple-encodes add/modify/delete; stubs for ChangesetTimelinePanel and VcsPanel allow WorkspaceShell to compile.</done>
</task>

<task type="auto">
  <name>Task 2: StagingPanel.tsx — virtualized staging list with add/remove/Deploy</name>
  <files>
    packages/renderer/src/panels/deploy/StagingPanel.tsx
  </files>
  <read_first>
    packages/renderer/src/panels/tre/VfsTree.tsx — read lines 42-45 (ROW_HEIGHT + OVERSCAN), 67-113 (ResizeObserver + windowing math), 127-148 (empty state), 167-199 (virtualized list JSX), 276-310 (row onClick + keyboard + hover). Copy ALL virtualization logic verbatim.
    packages/renderer/src/panels/LiveInspectorPanel.tsx lines 34, 61-80 — panel head structure.
    packages/renderer/src/panels/viewport/ExportDialog.tsx lines 483-506 — copy primaryBtnStyle + secondaryBtnStyle as LOCAL consts (W1 rule applies here too).
    04-UI-SPEC.md §Surface 2 (Staging Panel) — read all sub-sections: panel head, row anatomy, footer, all 4 states.
    04-PATTERNS.md §StagingPanel.tsx — read the empty-state pattern and row selection style.
    packages/renderer/src/panels/deploy/ActionBadge.tsx — import from Task 1.
    packages/renderer/src/panels/deploy/WorkspaceEntry.tsx — import from Task 1.
    04-CONTEXT.md §D-04-02/03/07 — explicit staging list; entry = virtualPath + replacementFilePath + action; auto-seal on deploy (D-04-07).
  </read_first>
  <action>
    Create packages/renderer/src/panels/deploy/StagingPanel.tsx as a dockview panel component (accepts IDockviewPanelProps).

    W1 rule: define primaryBtnStyle and secondaryBtnStyle as LOCAL const functions — do NOT import from ExportDialog.

    Imports: React (useState, useCallback, useRef, useEffect, useMemo); IDockviewPanelProps from dockview; ActionBadge from './ActionBadge'; WorkspaceEntry from './WorkspaceEntry'; useStagingStore from '../../state/stagingStore'; useWorkspaceStore from '../../state/workspaceStore'; path from 'path'.

    Stub imports (04-03 and 04-04 will provide real implementations):
    - packages/renderer/src/services/packPatch.ts stub: export function packPatch(_: import('@swg/contracts').StagingEntry[], __: string): void {} + export function buildPatchName(_: string): string { return 'swgtoolkit_patch.tre'; }
    - packages/renderer/src/services/changesetService.ts stub: export async function sealVersion(_: any): Promise<void> {}

    Panel gate: if useWorkspaceStore status.kind !== 'ready', return <WorkspaceEntry />.

    Constants: ROW_HEIGHT = 30 (MANDATORY); OVERSCAN = 8.

    Virtualization: copy the ResizeObserver pattern exactly from VfsTree.tsx. Replace VfsEntry with StagingEntry.

    Panel head (height: var(--tabstrip-h), background: var(--color-header), borderBottom: 1px solid var(--color-border), display: flex, alignItems: center, paddingLeft/Right: var(--space-4)):
    Left: 'Staging' (--text-md, weight 600).
    Right: 'Add…' secondary button + 'Deploy…' primary button (disabled when entries.length === 0 or buildStatus.kind === 'building'; aria-disabled when disabled).

    'Deploy…' button opens a DeployDialog — import stub for now: { DeployDialog } from './DeployDialog' (stub: export function DeployDialog(_: any) { return null; }). The real DeployDialog comes in 04-06.

    Empty state: 'Nothing staged' heading + 'Extract a file and Add to patch, or drop in a replacement.' body (exact UI-SPEC copy).

    Row anatomy (30px fixed height): ActionBadge + virtual path (mono, flex 1, overflow ellipsis) + source info + remove button '×'. Path traversal guard: if virtualPath contains '..' or starts with '/' or '\': show '✕ invalid path' in --color-danger; do NOT add the entry if invalid. If replacementFilePath set but !fs.existsSync: show '⚠ source missing' in --color-warn.

    Footer: '{n} staged · {a} add · {m} modify · {d} delete' (mono, --text-xs, --color-text-faint).

    Drag-drop: onDrop on container → extract files[0].path → open 'Add file…' flow.
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    pnpm --filter @swg/renderer exec tsc --noEmit exits 0.
    grep -c "ROW_HEIGHT = 30\|ROW_HEIGHT=30" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 1.
    grep -c "OVERSCAN = 8\|OVERSCAN=8" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 1.
    grep -c "ResizeObserver" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 1+.
    grep -c "topPad\|bottomPad" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 2+.
    grep -c "ActionBadge" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 1+.
    grep -c "removeEntry\|Remove from patch" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 2+.
    grep -c "from.*ExportDialog" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 0 (W1: local styles only).
    grep -c "Nothing staged" packages/renderer/src/panels/deploy/StagingPanel.tsx gives 1 (exact UI-SPEC copy).
  </acceptance_criteria>
  <done>StagingPanel renders virtualized list (ROW_HEIGHT=30, ResizeObserver); Deploy opens DeployDialog; row remove non-destructive; path-traversal rejected inline; empty state shows correct UI-SPEC copy; local button styles (W1); workspace-config.ts wiring (W3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-dragged file path → staging entry virtualPath | Dragged filenames may contain path traversal sequences |
| staging list → packPatch call | The replacement file path in a StagingEntry is used directly in packPatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | StagingPanel virtualPath input | mitigate | Reject any virtualPath containing '..' or absolute separator before addEntry; show inline ✕ error |
| T-04-07 | Tampering | drag-drop replacement file path | mitigate | replacementFilePath is only READ (passed to packPatch which reads it); validate it is absolute before adding to staging |
| T-04-08 | Information Disclosure | 'source missing' check via fs.existsSync | accept | existsSync on local path; result shown only in user's own UI |
| T-04-SC | Tampering | npm/pip installs | mitigate | No new npm packages; slopcheck not required |
</threat_model>

<verification>
pnpm --filter @swg/renderer exec tsc --noEmit exits 0 after both tasks complete.
Manual smoke: load app → staging panel shows WorkspaceEntry empty state; open workspace → staging panel shows 'Nothing staged'; add a staging entry → row renders with ActionBadge + virtual path + remove button; StatusBar shows workspace name.
</verification>

<success_criteria>
StagingPanel renders with ROW_HEIGHT=30 virtualization pattern; button styles are LOCAL (W1); workspace-config.ts receives addPanel calls (W3); StatusBar shows stale-deployment badge (W7); WorkspaceEntry and ActionBadge created; all TS compiles clean.
</success_criteria>

<output>
Create .planning/phases/04-edit-deploy-loop/04-02-SUMMARY.md when done.
</output>
