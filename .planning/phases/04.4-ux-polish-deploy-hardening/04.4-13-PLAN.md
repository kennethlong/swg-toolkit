---
phase: 04.4-ux-polish-deploy-hardening
plan: 13
type: execute
wave: 2
depends_on: ["04.4-09"]
files_modified:
  - e2e/07-deploy-flow.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [e2e-deploy-flow-coverage]

# REVISION NOTE (--reviews pass, 2026-07-03):
#   - Added an early `window.__testHooks !== undefined` sanity assertion (Sonnet LOW) so a broken
#     test-hook plumbing failure surfaces immediately and clearly, rather than confusingly deep into
#     the scenario.

# REVISION NOTE (fact-check pass, 2026-07-03): CORRECTS AN INVERTED CLAIM injected by the prior
# --reviews pass (this note replaces a removed bullet that claimed `setLiveVersion` was "the shipped
# reconcile-to-version entry point" and `syncLiveToVersion` a "stale name from earlier STATE.md
# notes"). Verified by direct read of both files (2026-07-03): selecting a version invokes
# `syncLiveToVersion` (packages/renderer/src/services/syncLiveToVersion.ts) — the SHIPPED
# version-reconcile ENGINE, imported by `VersionHistoryBody.tsx`'s row-click handler, with its own
# `syncLiveToVersion.test.ts`. It performs the file-level reconcile (deployLoose/resetLoose/
# restoreCfg/activatePatch/deactivatePatch) and then calls `setLiveVersion` (changesetService.ts:465)
# internally, as its LAST step, purely to move the `activeVersionId`/`deployedVersionId` pointers.
# `syncLiveToVersion` is NOT a stale alias — it is the real, live, tested entry point; `setLiveVersion`
# is the pointer-move it performs at the end. The spec's mental model and assertions below reference
# this verified call relationship.

# REVISION NOTE (round 2, --reviews pass, 2026-07-03): Cursor MEDIUM — this spec's scenario exercises
# `buildCfgModelClient` only; `e2e/06-delete-flow.spec.ts` (04.4-14) is the only spec that exercises
# `buildLooseOverrideClient`, and only for the DELETE flow, not a full loose-override deploy/reconcile/
# revert loop. STATED EXPLICITLY (this was previously an implicit gap, not a documented decision):
# cfg-model-only deploy-loop E2E coverage IS SUFFICIENT for this phase's SC #2 ("closes the jsdom-green
# ≠ Electron-runs blind spot for the deploy panels") — the blind spot this phase's todo targets is
# panel-level wiring bugs (the three real Phase-4 UAT blockers that motivated this todo were ALL
# cfg-model-shaped), not a per-deploy-model matrix; `04.4-09`'s two fixture builders exist so EACH
# spec can pick the model that best fits its OWN scenario (this one: cfg, since Deploy-dialog UI is
# cfg/loose-agnostic at the panel level per 04.3's design), not so every spec must exercise both. A
# dedicated loose-override deploy-loop E2E spec (parameterizing this spec, or a sibling) is NOT added
# in this phase — recorded as a conscious, documented scope decision, not a silently-dropped ask.

# REVISION NOTE (round 3, maintainer ruling, 2026-07-03): MAINTAINER REJECTED round-2's cfg-model-only
# scoping. SC #2's own wording ("A real-Electron Playwright spec drives the full 04.3 deploy loop
# ... against a fixture client") does not qualify "a fixture client" to cfg-model specifically —
# round-2 narrowed an unqualified success criterion without an explicit ask to do so. Round-2's
# TECHNICAL reasoning (panel-level wiring bugs are deploy-model-agnostic; the three real Phase-4 UAT
# blockers were all cfg-model-shaped) is not disputed — it remains true and is preserved below as the
# rationale for why a SECOND scenario, not a full parameterized matrix, satisfies the ruling. FIXED:
# this spec file gains a SECOND `test.describe` block driving the IDENTICAL loop shape against
# `buildLooseOverrideClient` (04.4-09) — the same fixture builder `04.4-14`'s delete-flow spec already
# uses, but exercised here for the first time through the FULL deploy/reconcile/revert LOOP rather than
# just delete. Deploy/revert assertions in the new block check LOOSE-MODEL OBSERVABLE STATE (override
# files present in the client tree after deploy / reverted after revert), never cfg content — cfg
# sha256 assertions do not apply to a loose-override fixture. Kept in the SAME spec file (not a new
# file) — the round-3 ruling explicitly allows "same spec file with a second describe block", and
# `.github/workflows/ci.yml` already lists this one file, so Task 2 needs no further CI-list changes;
# this does mean the file's total wall-time in CI is roughly the sum of two independent cold Electron
# launches (each `test.describe` block runs its own `beforeAll`/`afterAll` lifecycle, mirroring
# 04.4-14's existing multi-scenario-in-one-file pattern for the SAME reason) — judged acceptable given
# this suite's existing per-spec cold-launch granularity, and cheaper than registering a whole second
# CI spec-list entry. `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-VALIDATION.md`'s 13-T1 row
# is updated and a 13-T3 row added for the new scenario.
must_haves:
  truths:
    - "A real-Electron Playwright spec drives the full 04.3 deploy loop (boot -> stage -> save version -> select/reconcile -> deploy -> revert) against the cfg-model fixture client, asserting zero console errors (D-10) — Scenario 1"
    - "The SAME spec drives the IDENTICAL loop against the loose-override fixture client (buildLooseOverrideClient, 04.4-09), asserting loose-model observable state (override files present after deploy / reverted after revert) rather than cfg content — Scenario 2 (round-3 maintainer ruling, reverses round-2's cfg-model-only scoping for SC #2)"
    - "The spec runs in CI's lean job (windows-latest) on every push"
  artifacts:
    - path: "e2e/07-deploy-flow.spec.ts"
      provides: "Real-Electron deploy-loop coverage (D-07 re-based scenario), BOTH cfg-model (Scenario 1) AND loose-override (Scenario 2, round-3) fixtures, in two test.describe blocks in one file"
  key_links:
    - from: "e2e/07-deploy-flow.spec.ts (Scenario 1)"
      to: "e2e/fixtures/cfgModelClient.ts"
      via: "buildCfgModelClient(tmpRoot) fixture setup in Scenario 1's beforeAll"
      pattern: "buildCfgModelClient"
    - from: "e2e/07-deploy-flow.spec.ts (Scenario 2, round-3)"
      to: "e2e/fixtures/looseOverrideClient.ts"
      via: "buildLooseOverrideClient(tmpRoot) fixture setup in Scenario 2's own beforeAll — the same builder 04.4-14 uses, exercised here for the full deploy loop rather than just delete"
      pattern: "buildLooseOverrideClient"
    - from: "e2e/07-deploy-flow.spec.ts"
      to: "window.__testHooks"
      via: "SWG_TEST_MODE-gated store reads + dialog-path stubbing, in BOTH scenarios"
      pattern: "__testHooks|test:set-stub-paths"
---

<objective>
Add the deploy-flow real-Electron Playwright spec (D-07) — the loop to drive, per CONTEXT.md's re-basing to the 04.3 model (select = silent reconcile via `syncLiveToVersion`, which internally calls `setLiveVersion` to move the live pointer as its last step; Deploy dialog only for NEW versions, not re-navigation): **boot (zero console errors) → non-workspace-folder UI feedback → new project → stage → save version → select/reconcile → deploy → revert.** This closes the "jsdom-green ≠ Electron-runs" blind spot for the deploy panels specifically (the exact panels that shipped 3 real Phase-4 UAT blockers under jsdom-only coverage).

**Scope decision (ROUND 3 — maintainer ruling, REVERSES round-2's cfg-model-only scoping; see revision note above): this spec now exercises BOTH deploy-model fixtures.** Round-2 argued cfg-model-only coverage was sufficient for SC #2 because the panel-level wiring bugs this todo targets are deploy-model-agnostic and the three real Phase-4 UAT bugs that motivated this todo were all cfg-model-shaped — that technical reasoning is not disputed, and is preserved below as the rationale for why a SECOND scenario (not a full parameterized cross-product) is the right shape for the fix. The maintainer rejected narrowing SC #2's own unqualified wording ("the full 04.3 deploy loop ... against a fixture client") to a single model without an explicit ask to do so. FIXED: a second `test.describe` block in this SAME spec file (`e2e/07-deploy-flow.spec.ts`) drives the IDENTICAL loop shape against `buildLooseOverrideClient` (04.4-09), asserting loose-model observable state (override files present/reverted on disk) rather than cfg content. `04.4-14`'s delete-flow spec still separately exercises `buildLooseOverrideClient`, but only for DELETE — this plan's Scenario 2 is the first LOOSE-model coverage of the full deploy/reconcile/revert LOOP itself.

Purpose: D-10's console-error gate (fail on `console.error`/`pageerror`, warnings allowed) is the hard proof bar — a spec that merely "runs" without this assertion does not satisfy the todo.
Output: `e2e/07-deploy-flow.spec.ts` with TWO scenarios (cfg-model, loose-override), appended to CI's lean job spec list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/todos/pending/e2e-deploy-flow-coverage.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-09-PLAN.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-14-PLAN.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md

<interfaces>
From e2e/04-workspace.spec.ts (the own-lifecycle pattern to copy for this new spec — launch, firstWindow,
test.describe.configure({timeout: 90_000}), beforeAll/afterAll):
  const app = await electron.launch({ executablePath: require('electron'), args: [REPO_ROOT, '--disable-gpu'] });
  const win = await app.firstWindow(); await win.waitForLoadState('domcontentloaded');

From e2e/fixtures/electron-helpers.ts:
  test.extend&lt;ElectronFixtures&gt;({...}) — shared fixture pattern for specs without bespoke setup

From e2e/fixtures/cfgModelClient.ts (04.4-09):
  buildCfgModelClient(tmpRoot): { installPath, cfgRootPath }

From e2e/fixtures/looseOverrideClient.ts (04.4-09, ROUND 3 — new to THIS plan, already used by 04.4-14
for the delete flow only):
  buildLooseOverrideClient(tmpRoot): { installPath: string; cfgPath: string; overrideDir: string }

From packages/renderer/src/testHooks.ts (04.4-09, SWG_TEST_MODE-gated, installed at MODULE SCOPE in
WorkspaceShell.tsx — available immediately on window load, not gated behind any panel interaction):
  window.__testHooks.getWorkspaceStatus() / getChangesetManifest() / listProjects() / setStubPaths(channel, paths)

From main.ts (04.4-09): ipcRenderer.invoke('test:set-stub-paths', 'workspace:pick-dir', [tmpProjectFolder])
  — via window.__testHooks.setStubPaths(...) — stubs the folder picker to return a caller-chosen tmp
  path instead of opening a real OS dialog.

From packages/renderer/src/services/syncLiveToVersion.ts (VERIFIED 2026-07-03 — the SHIPPED
version-reconcile ENGINE; imported by VersionHistoryBody.tsx's row-click handler):
  syncLiveToVersion(targetId: string | null, ctx: ReconcileCtx): Promise&lt;LiveReconcileResult&gt;
  — selecting a version calls this; it performs the file-level reconcile and then calls
  `setLiveVersion` (changesetService.ts:465) internally as its LAST step, which sets BOTH
  activeVersionId and deployedVersionId, so `manifest.deployedVersionId === savedId` after selection
  is still the correct "silent reconcile happened" assertion — the pointer move is the tail of
  syncLiveToVersion's call, not a separate, unrelated function.

From packages/contracts/src/deploy.ts (ROUND 3 — the shape Scenario 2's assertions read to determine
which files to check post-deploy/post-revert):
  LooseDeployRecord { overrideDir, writtenFiles: Array<{virtualPath, destPath, preExisted, snapshotPath?}>, skippedDeletes }
  — after deploy, `destPath` for each writtenFiles entry should exist on disk with the staged bytes;
  after revert (via syncLiveToVersion -> resetLoose), preExisted:true entries should be restored to
  their pre-existing bytes (sha256 match against a value seeded by the fixture) and preExisted:false
  entries should no longer exist.

From e2e/06-delete-flow.spec.ts (04.4-14 — the existing, only other spec exercising
buildLooseOverrideClient; ROUND 3: read for its Node-process sha256/fs verification pattern against
loose-override fixture files, which Scenario 2 below mirrors for the deploy-loop case instead of delete):
  independent `fs.readFileSync` + sha256 checks run in the Playwright TEST process, never trusting only
  in-app state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write e2e/07-deploy-flow.spec.ts (cfg-model scenario + loose-override scenario, round-3)</name>
  <files>e2e/07-deploy-flow.spec.ts</files>
  <read_first>
    e2e/04-workspace.spec.ts (full file — own-lifecycle launch pattern + console-error listener style used for existing checks)
    e2e/fixtures/electron-helpers.ts
    e2e/fixtures/cfgModelClient.ts (04.4-09)
    e2e/fixtures/looseOverrideClient.ts (04.4-09 — ROUND 3, new to this plan)
    e2e/06-delete-flow.spec.ts (04.4-14 — ROUND 3: the existing loose-override + Node-process sha256 verification pattern to mirror for Scenario 2)
    packages/renderer/src/testHooks.ts (04.4-09)
    packages/renderer/src/services/syncLiveToVersion.ts (syncLiveToVersion — the real reconcile ENGINE; it calls setLiveVersion in changesetService.ts internally as its final step to move the live pointer)
    packages/renderer/src/services/looseOverrideDeploy.ts (resetLoose / writtenFiles shape — ROUND 3, needed for Scenario 2's revert assertions)
    .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md (D-04..D-08 — select=silent reconcile via syncLiveToVersion (which internally calls setLiveVersion); Deploy dialog only for NEW versions — the model both scenarios' scenario must match, NOT the pre-04.3 Section-C-slot-preview sketch in the original todo)
  </read_first>
  <behavior>
    Scenario 1 (cfg-model) behavior is unchanged from round 2 (see Task 1 action below). Scenario 2
    (ROUND 3, NEW) behavior:
    - Same loop shape as Scenario 1, run against `buildLooseOverrideClient(tmpRoot)` instead of
      `buildCfgModelClient(tmpRoot)`, in its OWN `test.describe` block with its own Electron
      launch/beforeAll/afterAll lifecycle (mirrors Scenario 1's own-lifecycle pattern, not a shared
      Electron instance).
    - The `window.__testHooks !== undefined` sanity assertion runs immediately after window load, same
      as Scenario 1.
    - After staging a file and deploying a NEW version via the Deploy dialog, assert (via Node `fs`
      calls in the TEST process against `overrideDir`/`installPath` paths returned by
      `buildLooseOverrideClient`) that the staged file was actually written into the client's override
      tree — NOT a cfg sha256 check, which does not apply to this fixture.
    - Seed one pre-existing overridden file in the fixture before the app launches (mirroring
      04.4-14's pattern) so revert has something non-trivial to restore. After reverting (selecting
      Baseline or an earlier version, via `syncLiveToVersion`), assert the pre-existing file is
      restored to its ORIGINAL bytes (sha256 match, computed independently in the TEST process) and
      the toolkit-written (non-preexisting) file from the deploy step is removed from the client tree.
    - Zero console errors asserted throughout (D-10), identical gate to Scenario 1.
  </behavior>
  <action>
    SCENARIO 1 (cfg-model, unchanged from round 2): Launch the app with `SWG_TEST_MODE=1` in the Electron launch env (`electron.launch({ ..., env: { ...process.env, SWG_TEST_MODE: '1' } })`). Build a fixture client tree via `buildCfgModelClient(tmpRoot)` and a fresh empty project folder in `beforeAll`. Attach a `page.on('console', ...)`/`page.on('pageerror', ...)` listener FIRST (before any interaction) that collects entries and asserts zero `error`-level entries at the end of the test (D-10 — warnings allowed, errors fail). Immediately after the window loads, assert `await page.evaluate(() => (window as any).__testHooks !== undefined)` is true — a broken test-hook plumbing failure should surface here, not confusingly deep into the scenario. Drive the scenario: (1) assert the app boots with zero console errors; (2) use `window.__testHooks.setStubPaths('workspace:pick-dir', [nonWorkspaceTmpFolder])` then trigger "Open Project…" and assert a UI feedback element appears (not silent failure) for a folder that isn't yet a toolkit workspace; (3) create a New Project bound to the fixture cfg-model client; (4) stage a file via the staging panel's Add flow (stub `workspace:pick-file` similarly) and assert the staged row appears; (5) Save Version and assert the version graph shows the new node; (6) select/click the newly-saved version's row and assert (via `window.__testHooks.getChangesetManifest()`) that `deployedVersionId` now equals the saved version's id (silent reconcile via `syncLiveToVersion`, which internally calls `setLiveVersion` to move the pointer, D-07/04.3 model — no Deploy dialog for RE-navigation); (7) create ANOTHER new version and use the Deploy… dialog for that NEW version, asserting the dialog opens and a deploy completes; (8) revert (select Baseline or an earlier version) and assert the reconcile completes without opening the Deploy dialog. Assert zero console errors at the very end.

    SCENARIO 2 (loose-override, ROUND 3 — NEW, in a second `test.describe` block in this same file, own Electron lifecycle mirroring Scenario 1's): Launch the app the same way (`SWG_TEST_MODE=1`). In `beforeAll`, build the fixture via `buildLooseOverrideClient(tmpRoot)` and seed one pre-existing file at a path inside `overrideDir` (or the client's `installPath`, matching whatever `resetLoose`'s `preExisted:true` restore path expects — mirror 04.4-14's fixture-seeding approach exactly) with known bytes, recording its sha256 as the "pristine baseline" this scenario's revert step must reach. Assert `window.__testHooks !== undefined` immediately after window load, same as Scenario 1. Drive the IDENTICAL loop shape: boot (zero console errors) → non-workspace-folder UI feedback (same stub-path technique) → new project bound to the loose-override fixture → stage a file → Save Version → select/reconcile the new version (assert via `window.__testHooks.getChangesetManifest()` that `deployedVersionId` updates, same silent-reconcile assertion as Scenario 1) → create another new version and use the Deploy… dialog for it, THEN assert (via `fs.existsSync`/`fs.readFileSync` in the TEST process against `overrideDir`/`installPath`) that the staged file now exists in the client's override tree with the staged bytes → revert to Baseline/an earlier version and assert (again via direct `fs` checks in the TEST process, sha256-compared) that the seeded pre-existing file is back to its ORIGINAL bytes and the toolkit-written file from the deploy step no longer exists. Assert zero console errors at the very end of this scenario too.
  </action>
  <acceptance_criteria>
    - `pnpm exec playwright test --forbid-only e2e/07-deploy-flow.spec.ts` passes locally against a built renderer bundle, running BOTH `test.describe` blocks (cfg-model Scenario 1 + loose-override Scenario 2).
    - The spec fails if a `console.error`/`pageerror` fires at any point in EITHER scenario (verified by temporarily injecting a `console.error` call in a scratch branch and confirming the spec goes red, then reverting).
    - Each scenario's `window.__testHooks !== undefined` assertion runs BEFORE any scenario step (grep confirms its position near the top of each `test.describe` block's test body, right after window load).
    - Scenario 2's deploy/revert assertions check file bytes on disk via Node `fs`/`crypto` in the TEST process (never cfg content, never trusting only in-app state) — grep confirms no `sha256`/cfg-path assertion appears inside Scenario 2's block that references a `.cfg` file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the new spec in CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (the lean job's "E2E tests — non-packaged specs" step, lines 60-68)
  </read_first>
  <action>
    Append `e2e/07-deploy-flow.spec.ts` to the `pnpm playwright test --forbid-only` file list in the lean job's E2E step (the same step 04.4-09 inserted a build step ahead of). ROUND 3: no additional CI-list entry is needed for Scenario 2 — both scenarios live in this ONE spec file, so the existing single list entry already runs both `test.describe` blocks.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml`'s E2E step lists `e2e/07-deploy-flow.spec.ts` alongside the four existing specs.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test harness → SWG_TEST_MODE hooks | Test-only control surface; must never be reachable in a production launch (enforced by 04.4-09, verified here by exercising it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-22 | Tampering | Spec accidentally targets a real client/project directory instead of the fixture tree | mitigate | `buildCfgModelClient(tmpRoot)`/`buildLooseOverrideClient(tmpRoot)` (round-3: both fixtures) always use an isolated `os.tmpdir()` root per D-06 — never a real install |
</threat_model>

<verification>
`pnpm exec playwright test --forbid-only e2e/07-deploy-flow.spec.ts` green locally against a built bundle (both scenarios).
</verification>

<success_criteria>
Deploy-loop e2e coverage exists for BOTH the cfg-model (Scenario 1) and loose-override (Scenario 2, round-3) fixtures, satisfying SC #2's unqualified "against a fixture client" wording in full; each scenario asserts zero console errors, verifies test hooks are wired before relying on them, checks the model-appropriate observable state (cfg sha256 vs. loose file presence/reversion), and both run in CI via the existing single spec-list entry.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-13-SUMMARY.md` when done
</output>
