---
phase: 04.4-ux-polish-deploy-hardening
plan: 10
type: execute
wave: 2
depends_on: ["04.4-01", "04.4-02"]
files_modified:
  - packages/renderer/src/panels/deploy/DeleteProjectConfirmModal.tsx
  - packages/renderer/src/panels/deploy/DeleteUndoToast.tsx
  - packages/renderer/src/panels/deploy/ProjectListDialog.tsx
  - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
  - packages/renderer/src/App.tsx
autonomous: true
requirements: [delete-project-with-restore]

# REVISION NOTE (--reviews pass, 2026-07-03):
#   - Removed `packages/renderer/src/services/deleteProject.ts` from files_modified — Sonnet correctly
#     flagged this plan's frontmatter listed it but no task actually edits it (the service is fully
#     owned by 04.4-01; this plan only CALLS it).
#   - Undo semantics: this plan's toast/UI text was already consistent with the locked "restores
#     project bytes only, never re-deploys" contract (see 04.4-01's objective) — reiterated explicitly
#     below so 04.4-14's e2e spec assertions and this plan's UI copy stay in lockstep.
#   - Added an explicit `stopPropagation()` note to Task 2 (Codex LOW) — a kebab click must not also
#     fire the row's own onClick (which opens the project).

# REVISION NOTE (round 2, --reviews pass, 2026-07-03): Sonnet HIGH, VERIFIED by direct read of both
# component files in this pass — `ProjectListDialog.tsx:48-52` loads `projects` via
# `useEffect(() => {...}, [isOpen])` (re-fetches ONLY on the open/close transition) and
# `WorkspaceEntry.tsx:164-203` loads `recents` via `useEffect(() => {...}, [])` (loads ONCE, on mount,
# never again). Neither component re-fetches after a mutation while it stays mounted — meaning if a
# user deletes a project via the kebab WITHOUT closing `ProjectListDialog` (the exact "delete without
# closing the list" case sketch 017 is built around), the stale `projects` array keeps showing the
# just-deleted project as an ordinary, clickable row — the dimmed/struck-through row never appears —
# until the dialog is closed and reopened. This is THE single biggest risk to the phase's flagship
# visual feature actually looking right on screen. FIXED below (Task 2): both surfaces now subscribe
# directly to `useDeleteUndoStore`'s `pending` array and RE-DERIVE their displayed rows reactively
# (rather than manually calling `setProjects(listProjects())`/`setRecents(...)` after every mutation
# call site, which is easy to forget at a FUTURE delete call site too) — this makes the dimmed-row/
# disappearing-row behavior robust to ANY code path that deletes or restores a project, present or
# future, not just this plan's own kebab handler.
#
# Sonnet MEDIUM (no in-flight guard on Delete confirm button — a second click before the first
# `deleteProject()` call resolves could race two concurrent renames) — FIXED below (Task 1): the
# confirm modal now tracks an `isDeleting` state and disables the Delete button (+ shows a spinner/
# "Deleting…" label) for the duration of the call.
#
# Sonnet MEDIUM (sketch 017 element gap — global kebab-dismiss handlers) — VERIFIED by direct read of
# `.planning/sketches/017-delete-project-flow/index.html:547-551` (`document.addEventListener('click',
# () => closeKebabs())`) and `:580` (`document.addEventListener('keydown', e => { if (e.key ===
# 'Escape') { closeConfirm(); closeKebabs(); } })`) — these are real, distinct sketch elements with NO
# corresponding row in this plan's element table, which is exactly the "sketch element with no
# corresponding task" planning gap AGENTS.md calls out. FIXED below: added as element #17 and wired in
# Task 2. (The CONFIRM MODAL's own click-outside/Escape dismissal is NOT a new gap — VERIFIED
# `ProjectListDialog.tsx` itself already implements the click-backdrop + Escape-closes pattern
# generically at the dialog-shell level (lines 55-60), which `DeleteProjectConfirmModal` inherits by
# following the SAME established modal-chrome convention Task 1 already mirrors from `DeployDialog.tsx`
# — no separate fix needed for the confirm modal's own dismissal, only for the KEBAB MENU's dismissal,
# which is new in this phase and has no existing convention to inherit.)
#
# Sonnet MEDIUM (post-restore confirmation toast neither implemented nor declared out of scope) —
# VERIFIED by direct read of the sketch's `restoreProject()` (index.html:616-624): EVERY restore
# (Undo-from-toast AND Restore-from-dimmed-row) fires a SECOND toast — `"<name> restored ... all N
# versions back, byte-for-byte"` (plus the D-04a-disclaimed "deploy re-applied to client" clause when
# `p.deployed`, which this phase's toast copy must NEVER include, per the locked undo contract). FIXED
# below: added as element #17b (post-restore toast) and wired in Task 3 with D-04a-safe copy.
#
# Sonnet HIGH (server-push orphaning, 04.4-01 × 07/08) — 04.4-01's round-2 fix adds a read-only
# existence-check warning (not a remote reset) to `deleteProject`'s `restoreErrors`. This plan's
# confirm modal (Task 1) and toast (Task 3) already render `restoreErrors`-derived warnings generically
# — no NEW plumbing needed here; this note just confirms the wiring is generic enough to carry the new
# warning string without a code change (the ⚠ toast variant and modal warning line already key off
# "is `restoreErrors` non-empty", not off specific known messages).
must_haves:
  truths:
    - "A user can delete a project from BOTH the Welcome recents row AND the Open Project dialog row via a per-row kebab menu (D-01, D-02 — sketch 017 Variant B is the UI contract)"
    - "The confirm modal reads exactly what will happen, varying its numbered plan lines by the project's deploy model (D-03)"
    - "After delete, an undo toast appears for 8s, and the ProjectListDialog (only) shows the project as a dimmed, recoverable row until app close"
    - "Undo restores the parked project's bytes only — it does NOT re-deploy to the client (locked contract, identical to 04.4-01 and 04.4-14; the toast/undo-note copy must not imply a re-deploy happens)"
    - "A delete or restore is REFLECTED IMMEDIATELY in both row surfaces while they stay mounted — ProjectListDialog and WorkspaceEntry re-derive their displayed rows from useDeleteUndoStore's pending array reactively, not via a manually-called one-shot re-fetch (round-2 fix — the dimmed/disappearing-row behavior must not depend on the dialog being closed and reopened)"
    - "The Delete button in the confirm modal is disabled for the duration of the deleteProject() call (round-2 fix — no double-submit race)"
  artifacts:
    - path: "packages/renderer/src/panels/deploy/DeleteProjectConfirmModal.tsx"
      provides: "Shared descriptive confirm modal consumed by both ProjectListDialog and WorkspaceEntry kebabs; in-flight isDeleting guard on its Delete button"
    - path: "packages/renderer/src/panels/deploy/DeleteUndoToast.tsx"
      provides: "8s auto-dismiss toast with Undo button + ⚠ restore-failure variant + a second post-restore confirmation toast"
  key_links:
    - from: "ProjectListDialog.tsx / WorkspaceEntry.tsx kebab menus"
      to: "deleteProject.ts (from 04.4-01)"
      via: "Delete project… menu item -> DeleteProjectConfirmModal -> deleteProject(projectFolder)"
      pattern: "deleteProject\\("
    - from: "DeleteUndoToast.tsx"
      to: "deleteUndoStore.restore(id)"
      via: "Undo button click — restores parked project bytes only, no client-side call"
      pattern: "useDeleteUndoStore"
    - from: "ProjectListDialog.tsx / WorkspaceEntry.tsx"
      to: "useDeleteUndoStore((s) => s.pending)"
      via: "reactive subscription that re-derives displayed rows on every pending-array change, not a manual post-mutation re-fetch call (round-2 fix)"
      pattern: "useDeleteUndoStore\\(\\(s\\)"

# Sketch 017 (Variant B: kebab menu + inline dimmed rows) is the UI CONTRACT for this plan.
# Sketches 007/008 govern the pre-existing row idiom being extended (NOT replaced) — see the
# must_haves.truths above and the 17-element table below, copied verbatim per AGENTS.md's
# "sketches are the UI contract" rule. (Round 2: table extended to 17 rows — #17 kebab-dismiss
# handlers, #17b post-restore toast — closing two sketch-element gaps Sonnet found by direct
# read of index.html.)
---

<objective>
Build the UI half of `delete-project-with-restore` on top of the 04.4-01 service (`deleteProject`, `useDeleteUndoStore`) and the 04.4-02 log service: **sketch 017, Variant B** (the maintainer-approved winner) — a per-row **⋯ kebab menu** (Open · Reveal studio folder · Delete project…) on BOTH `WorkspaceEntry` recents rows and `ProjectListDialog` rows, a shared descriptive confirm modal with numbered plan lines that vary by deploy model, an 8s undo toast (with a ⚠ restore-failure variant and a second post-restore confirmation toast), and an inline dimmed/dashed/struck-through recoverable row rendered **only** in `ProjectListDialog`.

**Round-2 correctness fix (the single biggest risk to this plan's flagship visual feature, see revision note above):** both row surfaces now subscribe REACTIVELY to `useDeleteUndoStore`'s `pending` array (not a manual one-shot re-fetch after each mutation) so a delete/undo/restore reflects on screen immediately, even while the dialog stays open across the mutation — this is the literal scenario sketch 017 is built to demonstrate.

**Locked undo contract (identical wording in 04.4-01 and 04.4-14):** Undo restores the parked project's folders/changeset history byte-for-byte. Undo does **NOT** re-deploy or re-apply anything to the client. The toast and modal copy below (sketch 017's own wording — "the project and all its versions are parked, not destroyed") already matches this; do not add or imply any "client will be re-deployed on Undo" language, INCLUDING in the round-2-added post-restore toast (element #17b) — the sketch's own demo copy for that toast has a "deploy re-applied to client" clause that IS the one documented, intentional divergence (D-04a) — this plan's post-restore toast copy must OMIT that clause entirely, always.

Purpose: sketch 017 is the authoritative UI contract for the delete surface (sketch 007 does NOT govern this element — it governs the pre-existing row idiom being extended). Every element below must be present; a sketch element with no corresponding task is a planning gap per AGENTS.md.

Output: `DeleteProjectConfirmModal.tsx`, `DeleteUndoToast.tsx` (new), kebab menus (with global dismiss handlers) wired into both row surfaces, dimmed recoverable rows in `ProjectListDialog` only, reactive re-render on delete/undo in both surfaces, an in-flight guard on the Delete button, and a post-restore confirmation toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/sketches/017-delete-project-flow/README.md
@.planning/sketches/017-delete-project-flow/index.html
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-01-PLAN.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-02-PLAN.md

<interfaces>
From packages/renderer/src/services/deleteProject.ts (04.4-01):
  deleteProject(projectFolder: string): Promise&lt;{ restoreErrors: string[] }&gt;

From packages/renderer/src/state/deleteUndoStore.ts (04.4-01):
  useDeleteUndoStore — { pending: TrashEntry[]; push(entry); restore(id): void; clear(): void }
  TrashEntry = { id, projectName, entryDir, trashStudioPath, trashProjectPath, originalStudioDir, originalFolderPath, umbrellaMoveSkipped, restoreErrors, parkedAtISO }
  (round-3: entryDir is the .trash/<id>/ entry root — studio/ + project/ + .complete.json marker live under it; see 04.4-01)
  restore(id) moves parked bytes back to their original paths ONLY — no client-side call. (Round 2:
  restore() now THROWS if either destination path is already occupied — see 04.4-01's round-2 fix —
  this plan's Restore-button handler (Task 2) and Undo-button handler (Task 3) must both catch this
  and surface it as an inline/toast error rather than letting it propagate uncaught.)

From packages/renderer/src/services/logService.ts (04.4-02):
  log(level: 'info'|'warn'|'error', channel: 'log', message: string): void

From packages/renderer/src/panels/deploy/ProjectListDialog.tsx (current row structure — VERIFIED
2026-07-03: `projects` state loads via `useEffect(() => {...}, [isOpen])` — re-fetches ONLY on open/
close transition, never on a mutation while the dialog stays open. Round 2: this plan ADDS a reactive
`useDeleteUndoStore((s) => s.pending)` subscription so the rendered row list re-derives on every
pending-array change, not just on isOpen transitions — the exact div below gets a new trailing kebab
child AND the row-list render logic gets a `pending`-derived filter/overlay):
  &lt;div key={p.projectFolder} onClick={...} ...&gt;
    &lt;span&gt;◆&lt;/span&gt;
    &lt;div&gt;{name + sub}&lt;/div&gt;
    {p.kind === 'client' && &lt;span&gt;⛁ bound&lt;/span&gt;}
    {ago && &lt;span&gt;{ago}&lt;/span&gt;}
    &lt;!-- kebab button slots in here --&gt;
  &lt;/div&gt;

From packages/renderer/src/panels/deploy/WorkspaceEntry.tsx (the shared Row() component, lines 127-151 —
accepts pill/ago as trailing slot content; add an optional onKebab prop the same way. VERIFIED
2026-07-03: `recents` state loads via `useEffect(() => {...}, [])` — mount-only, never re-fetches.
Round 2: this plan ADDS a reactive `useDeleteUndoStore((s) => s.pending)` subscription so a just-
deleted recent row disappears immediately, without waiting for a remount):
  function Row({ ico, name, sub, pill, ago, onClick, title, disabled }: {...}): React.ReactElement

From packages/renderer/src/services/changesetService.ts:
  readManifest(studioDir) -> manifest; manifest.changesets[].deployRecord (shape determines the confirm
  modal's plan-line variant: 'overrideDir' in rec => loose-model line; else cfg-model line; no deployRecord
  on any changeset => "nothing to restore" line)

From packages/renderer/src/services/workspaceService.ts:
  getStudioDir(folderPath) — used to read the manifest for a NOT-open project

From .planning/sketches/017-delete-project-flow/index.html (round-2 citations, VERIFIED 2026-07-03):
  Line 547-551: `function closeKebabs() {...}` + `document.addEventListener('click', () => closeKebabs());`
    — clicking ANYWHERE outside an open kebab menu closes it (element #17).
  Line 580: `document.addEventListener('keydown', e => { if (e.key === 'Escape') { closeConfirm(); closeKebabs(); } });`
    — Escape closes BOTH the confirm modal and any open kebab menu (element #17, shared handler).
  Line 616-624: `function restoreProject(variant, id) {...}` fires a SECOND toast after every restore
    (Undo-from-toast OR Restore-from-dimmed-row): `"&lt;name&gt; restored ... all N versions back,
    byte-for-byte"` (element #17b) — the sketch's own copy has a disclaimed "deploy re-applied to
    client" clause for the deployed case; THIS PLAN'S VERSION OMITS THAT CLAUSE ALWAYS (D-04a).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeleteProjectConfirmModal.tsx — descriptive confirm with model-varying plan lines + in-flight guard</name>
  <files>packages/renderer/src/panels/deploy/DeleteProjectConfirmModal.tsx</files>
  <read_first>
    .planning/sketches/017-delete-project-flow/index.html (the confirm modal markup + `openConfirm()` JS — read the ACTUAL per-project plan-line branching logic, do not paraphrase)
    packages/renderer/src/panels/deploy/DeployDialog.tsx (overlay/panel/header/footer shell conventions to mirror — modal chrome, not content)
    packages/renderer/src/services/changesetService.ts (readManifest — how to determine deploy model for a project that may not be open)
  </read_first>
  <action>
    Create a shared modal component `DeleteProjectConfirmModal({ projectFolder, projectName, isCurrentlyOpen, onConfirm, onCancel }: Props)`. On open, read `readManifest(getStudioDir(projectFolder))`, find the carrier changeset's `deployRecord`, and render the numbered plan lines (element #5 below) matching the deploy-model dispatch already used by `deleteProject.ts`: `'overrideDir' in rec` → loose-model revert line (element #7); `rec` present with a cfg shape → cfg-model restore line naming the client + cfg file (element #6); no `deployRecord` on any changeset → "nothing to restore" line (element #8). ALWAYS render the "park the project + N saved versions" line (element #9, N = `manifest.changesets.length`) and the "remove from this list and Recent projects" line (element #10). When `isCurrentlyOpen` is true, render the amber warn-line (element #11) below the plan lines. Render the header (#3), intro line (#4), undo note (#12 — sketch copy: "the project and all its versions are parked, not destroyed"; do NOT add any wording implying the client is re-deployed on Undo), and Cancel/Delete footer buttons (#13) verbatim per the sketch's copy. **(ROUND 2 — in-flight guard, element unchanged but behavior hardened):** add an `isDeleting` boolean state; the Delete button's `onClick` sets `isDeleting = true`, calls `onConfirm()` (which the caller wires to `await deleteProject(...)`), and the button is `disabled={isDeleting}` with its label swapped to "Deleting…" for the duration — this closes the double-click/double-submit race a second click before the first call resolves would otherwise cause (two concurrent renames of the same studioDir, or two divergent trash entries). The Cancel button is also disabled while `isDeleting` is true (no cancelling out from under an in-flight delete).
  </action>
  <acceptance_criteria>
    - Manual check against the sketch: open `.planning/sketches/017-delete-project-flow/index.html` in a browser side-by-side with the built component for each of the sketch's three seed scenarios (cfg-model + currently-open, loose-model, never-deployed) and confirm the plan lines match element-for-element (elements #3-#13 below).
    - Component renders WITHOUT throwing when `readManifest` returns an empty manifest (never-deployed / brand-new project edge case).
    - Clicking Delete twice in rapid succession results in exactly ONE `deleteProject()` call (button is disabled after the first click; manual check or a component test simulating a double click).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Kebab menus (with global dismiss handlers) on both row surfaces + reactive re-render + inline dimmed recoverable row (ProjectListDialog only)</name>
  <files>packages/renderer/src/panels/deploy/ProjectListDialog.tsx, packages/renderer/src/panels/deploy/WorkspaceEntry.tsx</files>
  <read_first>
    .planning/sketches/017-delete-project-flow/index.html (kebab menu markup + `toggleKebab()`, `closeKebabs()`, the document-level click/Escape dismiss handlers at lines 547-551/580, `deletedInlineHtml()`, and the `render()` function that appends the dimmed row ONLY to the dialog element `dlg`, never to the Welcome `wel` element — element #16's nuance)
    packages/renderer/src/panels/deploy/ProjectListDialog.tsx (lines 36-52, the `projects`/`isOpen` load effect to make reactive; lines 136-175, the row map to extend)
    packages/renderer/src/panels/deploy/WorkspaceEntry.tsx (lines 155-203, the mount-only load effect to make reactive; lines 127-151, the Row() component to extend with an optional kebab prop)
    packages/renderer/src/state/deleteUndoStore.ts (04.4-01 — the `pending` array + round-2 occupied-destination throw from `restore()`)
  </read_first>
  <action>
    **Reactive re-render (ROUND 2 fix — apply FIRST, before the kebab wiring, since the kebab handlers below rely on it):** in BOTH `ProjectListDialog.tsx` and `WorkspaceEntry.tsx`, add `const pending = useDeleteUndoStore((s) => s.pending);` and derive the DISPLAYED row list from `[projects/recents, pending]` together (e.g. a `useMemo` keyed on both) rather than relying solely on the one-shot `listProjects()`/`getRecentProjects()` load effect to reflect deletions. Concretely: in `ProjectListDialog.tsx`, a project whose `folderPath` matches a `pending` entry's `originalFolderPath` renders as the dimmed row (element #15) INSTEAD of its normal row, even if the underlying `projects` array (loaded once per dialog-open) is stale; in `WorkspaceEntry.tsx`, a recent whose `folderPath` matches a `pending` entry's `originalFolderPath` is FILTERED OUT of the rendered list (element #16 — simply absent), even if the underlying `recents` array is stale. This makes the dimmed-row/disappearing-row behavior depend on `useDeleteUndoStore`'s state (always current, updated synchronously by `deleteProject`/`restore`) rather than on a manually-remembered re-fetch call at each mutation site — robust to this plan's OWN delete/restore handlers below AND to any future code path that mutates `pending`.

    Add a per-row ⋯ kebab button (element #1) to BOTH `ProjectListDialog`'s row map and `WorkspaceEntry`'s `Row()` component (add an optional `onOpen`/`onReveal`/`onDelete` prop triple, or a single `kebabActions` prop object, to `Row()` so `WorkspaceEntry`'s call sites pass handlers). **The kebab button's own `onClick` MUST call `e.stopPropagation()` before opening its menu** — both `ProjectListDialog`'s row and `WorkspaceEntry`'s `Row()` already have an `onClick` that opens the project; without `stopPropagation()`, clicking the kebab would also fire the row's open-project handler. The kebab menu (element #2) has three items: "Open" (same action as the existing row click), "Reveal studio folder" (opens the project's studio dir in the OS file explorer — use `require('electron').shell.openPath(getStudioDir(folder))`, Path B has direct access), a separator, and "Delete project…" (danger-styled) which opens `DeleteProjectConfirmModal` from Task 1.

    **(ROUND 2 — element #17, new) Global kebab-dismiss handlers:** exactly ONE `document.addEventListener('click', ...)` and ONE `document.addEventListener('keydown', ...)` pair, owned at a single shared point (e.g. a small `useKebabDismiss(closeAllKebabs)` hook used identically by both `ProjectListDialog.tsx` and `WorkspaceEntry.tsx`, or lifted into a tiny shared helper module if that avoids duplicating the effect twice) — clicking ANYWHERE outside an open kebab menu closes it (mirrors the sketch's `closeKebabs()` on a document-level click listener); pressing Escape closes any open kebab menu (mirrors the sketch's shared Escape handler, which ALSO closes the confirm modal — that half is already covered by `ProjectListDialog`'s existing Escape-closes-dialog convention plus `DeleteProjectConfirmModal`'s own chrome, per this plan's round-2 revision note — this task only needs to add the KEBAB half of that shared handler, since the modal-closing half already exists elsewhere).

    On confirm, call `deleteProject(projectFolder)`, then push a `TrashEntry` onto `useDeleteUndoStore` (already done inside `deleteProject.ts` per 04.4-01 — this task only needs to trigger the toast, see Task 3) and call `log('info', 'log', \`Deleted project ${projectName}\`)` (or `'warn'` if `restoreErrors.length > 0` — this generically covers the round-2 server-push-orphan warning from 04.4-01 too, no separate wiring needed) from 04.4-02's logService for D-14 instrumentation.

    In `ProjectListDialog.tsx` ONLY, render an inline dimmed row (element #15 — dashed border, struck-through name, "deleted just now · N versions parked" subtext, Restore button calling `useDeleteUndoStore.getState().restore(id)`) for every `pending` entry whose `originalFolderPath` matches a project no longer in `listProjects()`'s output (per the reactive derivation above). **(ROUND 2)** Wrap the Restore button's `restore(id)` call in a try/catch — since `restore()` now throws when a destination path is occupied (04.4-01's round-2 fix) — and surface a caught error inline on the dimmed row (e.g. a small red note under the subtext: "Could not restore — <message>") rather than letting it propagate uncaught; the entry remains dimmed/pending so the user can retry after resolving the conflict. In `WorkspaceEntry.tsx`, the deleted project's row simply disappears from the recents list via the reactive filter above — do NOT render a dimmed remnant there (element #16 — verified against the sketch's own `render()` behavior: the dimmed row is dialog-only).
  </action>
  <acceptance_criteria>
    - Both `ProjectListDialog` rows and `WorkspaceEntry` recents rows show a kebab button with Open / Reveal studio folder / — / Delete project… (danger-styled last item).
    - Clicking the kebab button does NOT also trigger the row's "open project" click handler (stopPropagation confirmed via manual check or a component test).
    - **(Round 2)** Deleting a project via the kebab WHILE `ProjectListDialog` stays open (no close/reopen) immediately shows the dimmed/dashed/struck-through row with a Restore button — verified this does NOT require closing and reopening the dialog.
    - **(Round 2)** Deleting a project from the Welcome screen recents list immediately removes its row, without a remount.
    - **(Round 2)** Clicking anywhere outside an open kebab menu closes it; pressing Escape while a kebab menu is open closes it.
    - After deleting a project via either surface, re-opening `ProjectListDialog` (or without reopening, per the round-2 fix above) shows the dimmed row; the SAME deleted project does NOT appear dimmed in the Welcome recents list (only absent).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: DeleteUndoToast.tsx + App.tsx mount + restore-failure variant + post-restore confirmation toast</name>
  <files>packages/renderer/src/panels/deploy/DeleteUndoToast.tsx, packages/renderer/src/App.tsx</files>
  <read_first>
    .planning/sketches/017-delete-project-flow/index.html (`#toast`, `.toast-bar`, `showToast()` — icon/name/status/sub-line/Undo/X/8s progress bar markup; `restoreProject()` at lines 616-624 — the POST-RESTORE second toast, element #17b)
    packages/renderer/src/App.tsx (current mount structure — the root render tree; this file is also edited by 04.4-01 Task 3 for the trash-purge effect — both edits are additive siblings, no conflict)
    packages/renderer/src/state/deleteUndoStore.ts (04.4-01 — round-2 occupied-destination throw from `restore()`)
  </read_first>
  <action>
    Create `DeleteUndoToast.tsx` — a global toast component subscribing to `useDeleteUndoStore((s) => s.pending)`, rendering the MOST RECENT entry as a toast (element #14): 🗑 icon (or ⚠ if `entry.restoreErrors.length > 0` — this generically covers the round-2 server-push-orphan warning from 04.4-01, no separate variant needed), bold project name + status text, gray sub-line (or amber if the ⚠ variant, showing the FIRST `restoreErrors` message), an Undo button calling `useDeleteUndoStore.getState().restore(entry.id)` and dismissing the toast, an X close button, and an 8s auto-dismiss with a visible progress bar (a CSS animation or a `setInterval`-driven width, matching the sketch's `.toast-bar`). **(ROUND 2)** Wrap the Undo button's `restore(id)` call in a try/catch (since `restore()` now throws on an occupied destination, per 04.4-01's round-2 fix) — on failure, keep the toast visible and swap its content to show the error (do not dismiss); on success, dismiss this toast and **(element #17b, round-2, new)** immediately show a SECOND, separate "restored" toast: bold project name + `"restored — all N versions back, byte-for-byte"` (N = the entry's changeset count), auto-dismissing the same way. **This second toast's copy MUST NEVER include any "deploy re-applied to client" or similar clause** — the sketch's own demo copy has that clause for the deployed-project case, and it is the ONE documented, intentional divergence from the sketch (D-04a); this plan's implementation omits it unconditionally, regardless of whether the restored project had a `deployRecord`. Mount `&lt;DeleteUndoToast /&gt;` once in `App.tsx` alongside the existing `&lt;ProjectListDialog /&gt;` render (a new sibling element in the root flex column, not nested inside any panel).
  </action>
  <acceptance_criteria>
    - After a delete, a toast appears within 1 render tick, auto-dismisses at 8s, and its Undo button restores the parked project bytes (verified by re-checking `listProjects()` includes it again after Undo) — no client-side assertion here (undo does not touch the client, per the locked contract).
    - Deleting a project whose `restoreErrors` is non-empty (simulate by forcing a missing snapshot in a manual test) shows the ⚠ variant with an amber sub-line, not the normal 🗑 variant.
    - **(Round 2)** After a successful Undo (or a Restore from the dimmed row), a SECOND "restored" toast appears with byte-for-byte copy and NEVER mentions a re-deploy, for both a never-deployed and a previously-deployed project.
    - **(Round 2)** Simulating an occupied restore destination (e.g. recreate a project at the original path before clicking Undo) keeps the original toast visible with an inline error, and does NOT show the "restored" toast.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kebab "Delete project…" → deleteProject service | User-triggered destructive action on real client cfg / deployed files — gated by an explicit confirm modal, never a single click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-18 | Repudiation | User forgets what delete will do before confirming | mitigate | Descriptive confirm modal (Task 1) enumerates EXACTLY what happens per deploy model — no generic "Are you sure?" |
| T-04.4-19 | Tampering (self) | Kebab "Reveal studio folder" opening an unexpected/unsanitized path | mitigate | `shell.openPath` is called with the SAME `getStudioDir(folder)` result already trusted elsewhere in the codebase — no new path construction |
| T-04.4-19b | Repudiation (round 2) | A double-click on Delete races two concurrent deleteProject() calls against the same project | mitigate | `isDeleting` in-flight guard disables the Delete button after the first click (Task 1) |
</threat_model>

## Sketch 017 (Variant B) — Element Table (copied verbatim, must_haves — every row below MUST map to a task above)

| # | Element | Surface(s) | Task |
|---|---------|-----------|------|
| 1 | Per-row kebab (⋯) button | WorkspaceEntry rows AND ProjectListDialog rows | Task 2 |
| 2 | Kebab menu items: Open · Reveal studio folder · separator · Delete project… (danger-styled) | Both | Task 2 |
| 3 | Confirm modal header "⚠ Delete project?" | Shared modal | Task 1 |
| 4 | Confirm intro line: "Delete **{name}**? Here's exactly what will happen:" | Shared modal | Task 1 |
| 5 | Numbered plan lines (ok-styled, green accent number) | Shared modal | Task 1 |
| 6 | Plan-line variant: cfg-model restore line | cfg-deployed projects | Task 1 |
| 7 | Plan-line variant: loose-model revert line | loose-deployed projects | Task 1 |
| 8 | Plan-line variant: never-deployed "nothing to restore" line | standalone/never-deployed | Task 1 |
| 9 | Plan line: "Park the project + all N saved versions in the session trash" | All | Task 1 |
| 10 | Plan line: "Remove it from this list and from Recent projects" | All | Task 1 |
| 11 | Amber warn-line: "currently open — workspace will close" | Only when deleting the open project | Task 1 |
| 12 | Undo note in modal body | Shared modal | Task 1 |
| 13 | Footer buttons: Cancel (ghost) / Delete project (danger), BOTH disabled while in-flight (round 2) | Shared modal | Task 1 |
| 14 | Toast: icon, name+status, sub-line, Undo, X, 8s progress bar | All | Task 3 |
| 15 | Inline dimmed row (`deleted-inline`): dashed border, struck-through, "deleted just now · N versions parked", Restore button | **ProjectListDialog ONLY** | Task 2 |
| 16 | Welcome recents row: on delete, row simply disappears (no dimmed remnant there) | WorkspaceEntry | Task 2 |
| 17 | (Round 2) Global kebab dismiss: click-outside closes any open kebab; Escape closes any open kebab | Both | Task 2 |
| 17b | (Round 2) Post-restore confirmation toast: "<name> restored — all N versions back, byte-for-byte" (NEVER a re-deploy clause) | All | Task 3 |

<verification>
`pnpm --filter @swg/renderer exec vitest run` full suite green. Manual walk of sketch 017's three seed scenarios (cfg-model+open, loose-model, never-deployed) against the built UI, INCLUDING the round-2 "delete without closing the dialog" and "kebab dismiss" checks.
</verification>

<success_criteria>
All 17(+1) sketch-017 elements present and mapped; delete/undo loop works end-to-end from both row surfaces, reflecting immediately even without a dialog remount; undo copy/behavior never implies a client re-deploy (including the new post-restore toast); the Delete button cannot be double-submitted.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-10-SUMMARY.md` when done
</output>
