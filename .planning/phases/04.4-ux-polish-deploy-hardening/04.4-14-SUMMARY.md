---
phase: 04.4-ux-polish-deploy-hardening
plan: 14
subsystem: testing
tags: [playwright, electron, e2e, delete, undo, deleteProject, deleteUndoStore, sha256]

# Dependency graph
requires:
  - phase: 04.4-ux-polish-deploy-hardening
    provides: "04.4-09's SWG_TEST_MODE hook surface (listProjects, getChangesetManifest) + fixture builders (cfgModelClient, looseOverrideClient); 04.4-10's delete/undo UI (DeleteProjectConfirmModal, DeleteUndoToast, ProjectListDialog kebab menu); 04.4-01's deleteProject/deleteUndoStore/resetDeploymentFromRecord service layer; 04.4-13's working test-hook infrastructure + build pattern"
provides:
  - "e2e/06-delete-flow.spec.ts — dedicated real-Electron coverage of delete -> confirm -> client-pristine assert -> undo -> restored, for BOTH cfg-model and loose-override deploy models"
  - "Direct-disk-seed fixture pattern for e2e specs that need an ALREADY-DEPLOYED project state without re-driving the full New-Project-wizard/stage/save/Deploy UI (reproduces workspaceService.ts's real path formulas verbatim)"
affects: [e2e-suite-broadly]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "e2e specs that need a pre-existing project state seed the studio dir + manifest.json + workspace.json directly via Node fs BEFORE electron.launch(), using path formulas reproduced verbatim from workspaceService.ts (appDataRoot/getStudiosRoot/getDefaultProjectFolder/getStudioDir) rather than importing the real service modules (which pull in zustand-store-coupled code not meant to run outside the renderer)"
    - "Locked undo contract verification pattern: assert a ground-truth byte/sha256 value BEFORE delete, again immediately after delete (proves restore), and a THIRD time after Undo (proves Undo does NOT re-deploy) — all three checks via Node fs/crypto in the TEST process, never trusting only window.__testHooks state"

key-files:
  created:
    - e2e/06-delete-flow.spec.ts
  modified:
    - .github/workflows/ci.yml

key-decisions:
  - "Seeded an already-deployed project directly on disk (studio dir + manifest.json + workspace.json + changeset files, matching the real CfgDeployRecord/LooseDeployRecord shapes) rather than re-driving the New-Project wizard -> stage -> save -> Deploy dialog sequence a second time — e2e/07-deploy-flow.spec.ts (04.4-13) already covers that path end-to-end; this keeps 06-delete-flow.spec.ts focused on delete/undo per the plan's own stated preference."
  - "Path formulas (appDataRoot/getStudiosRoot/getDefaultProjectFolder/getStudioDir) are duplicated verbatim in the spec file rather than imported from workspaceService.ts, since that module statically pulls in zustand stores and other renderer-coupled services not meant to execute in the plain Node/Playwright test process (matches the established e2e/07-deploy-flow.spec.ts pattern of never importing services, only fixtures)."

requirements-completed: [delete-project-with-restore, e2e-deploy-flow-coverage]

# Metrics
duration: ~35min
completed: 2026-07-04
---

# Phase 04.4 Plan 14: Delete-Flow E2E Coverage (cfg-model + loose-override) Summary

**A dedicated real-Electron Playwright spec drives delete -> confirm -> client-pristine assert -> undo -> restored against both deploy-model fixtures, independently verifying every restore claim via Node fs/crypto in the test process — all 14 assertions passed on the first run with zero product-code deviations.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-07-04T08:10:00Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- `e2e/06-delete-flow.spec.ts`: two `test.describe` blocks (cfg-model, loose-override), each seeding an already-deployed project directly on disk, then driving boot -> Open Project -> kebab menu -> Delete project… -> confirm modal -> Delete -> toast -> Undo -> post-restore toast, asserting the locked undo contract (project bytes restored via `window.__testHooks.listProjects()`; client cfg / loose files remain in their delete-restored state, never re-deployed) and zero console errors throughout (D-10).
- Registered the new spec in CI's lean job spec list, alongside the existing four specs plus 07-deploy-flow.spec.ts.
- Verified locally against a freshly-built renderer/main/preload bundle: **14/14 assertions green** on the first run — no bugs found, no auto-fixes needed (the 04.4-13 executor's prior fixes to `window.__testHooks` install, Vite `process.env` stubbing, and the Console/Log panel crash held up as genuine ground truth for this plan, exactly as that plan's SUMMARY predicted).

## Task Commits

1. **Task 1: Write e2e/06-delete-flow.spec.ts** — `a21ab0c` (feat)
2. **Task 2: Register the new spec in CI** — `4acbf7f` (feat)

## Files Created/Modified

- `e2e/06-delete-flow.spec.ts` — the new delete-flow spec (both scenarios)
- `.github/workflows/ci.yml` — lean job E2E step now lists the new spec (`e2e/06-delete-flow.spec.ts`), positioned alphabetically before `e2e/07-deploy-flow.spec.ts`

## Decisions Made

See `key-decisions` in frontmatter — summarized: (1) seeded an already-deployed project state directly on disk rather than re-driving the New-Project wizard, since 07-deploy-flow.spec.ts already covers that path; (2) duplicated workspaceService.ts's path formulas verbatim in the spec rather than importing the module, to avoid pulling zustand-store-coupled code into the plain Playwright test process.

## Deviations from Plan

None — plan executed exactly as written. Both scenarios' full 14-assertion suite passed on the first local run against a freshly-built bundle; no product-code bugs were found and no auto-fixes were required (Rules 1-3 did not trigger).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Delete/restore/undo now has dedicated, independently-verified real-Electron coverage for both deploy models (cfg-model and loose-override), closing the `delete-project-with-restore` todo's AC5 test matrix and the `e2e-deploy-flow-coverage` todo's D-09 item.
- This was the last plan in phase 04.4's 6-item UX-polish-deploy-hardening scope (delete-project-with-restore, e2e-deploy-flow-coverage, console/log tabs, statusbar mesh name, VFS override dim, viewport facing axis) — recommend a phase-completion review (`/gsd:verify-work 04.4`) before transitioning to Phase 5.
- No new blockers or concerns surfaced.

## Self-Check: PASSED

- `e2e/06-delete-flow.spec.ts` exists on disk (created) — confirmed via `pnpm exec playwright test --list`.
- `.github/workflows/ci.yml` lists `e2e/06-delete-flow.spec.ts` — confirmed via grep.
- Both task commits present in `git log`: `a21ab0c`, `4acbf7f`.
- Re-ran every task's `<acceptance_criteria>`:
  - Task 1: `pnpm exec playwright test --forbid-only e2e/06-delete-flow.spec.ts` — 14/14 PASSED (both scenarios). `grep -n "__testHooks !== undefined"` shows the assertion inside each scenario's `beforeAll`. `grep -n "listProjects("` confirms the concrete hook is used by name (not a DOM scrape).
  - Task 2: `.github/workflows/ci.yml`'s lean-job E2E step lists `e2e/06-delete-flow.spec.ts` alongside the other specs.
  - Plan-level verification: `pnpm exec playwright test --forbid-only e2e/06-delete-flow.spec.ts` green locally against a built bundle (both scenarios) — CONFIRMED.
- No residual test data left in the real `%LOCALAPPDATA%\swg-toolkit\studios`/`projects` dirs after the run (afterAll cleanup verified via directory listing).

---
*Phase: 04.4-ux-polish-deploy-hardening*
*Completed: 2026-07-04*
