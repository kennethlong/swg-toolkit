---
phase: 04.4-ux-polish-deploy-hardening
plan: 11
type: execute
wave: 2
depends_on: ["04.4-02"]
files_modified:
  - packages/backend/src/main.ts
  - packages/contracts/src/ipc.ts
  - packages/renderer/src/services/logService.ts
  - packages/renderer/src/services/treAutoMount.ts
  - packages/renderer/src/services/changesetService.ts
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
autonomous: true
requirements: [inapp-console-log-tabs-inactive]

must_haves:
  truths:
    - "Main-process events (dialog invocations, app lifecycle) reach the in-app Log tab, not just DevTools (D-13)"
    - "Mount, deploy, reconcile, and reset actions emit structured Log entries with levels on day one (D-14)"
  artifacts:
    - path: "packages/renderer/src/services/logService.ts"
      provides: "installMainLogForward() — subscribes to the main-process 'main-log' IPC channel"
  key_links:
    - from: "main.ts ipcMain handlers"
      to: "webContents.send('main-log', entry)"
      via: "one-way push channel, forwarded to the renderer's logStore"
      pattern: "main-log"
    - from: "treAutoMount.ts / changesetService.ts / DeployDialog.tsx"
      to: "logService.log('log', ...)"
      via: "structured app-event emission at existing call sites"
      pattern: "log\\('info'|log\\('warn'|log\\('error'"
---

<objective>
Complete D-12/D-13's "capture scope = renderer + main" by forwarding main-process events into the Log tab (04.4-02 built the renderer-side console-capture half), and instrument the deploy/reconcile/mount call sites this phase touches (D-14) so the Log tab is useful on day one, not an empty pane.

Purpose: "Console tab should mean I never need Ctrl+Shift+I" — this plan is the main-process half of that bar.
Output: a `main-log` one-way IPC channel + `installMainLogForward()` + structured log calls at 4-5 existing service call sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-02-PLAN.md

<interfaces>
From packages/renderer/src/services/logService.ts (04.4-02 — this plan ADDS installMainLogForward, does not touch installConsoleCapture):
  log(level, channel, message): void

From packages/backend/src/main.ts (current — the 3 dialog ipcMain.handle blocks + app.whenReady()):
  ipcMain.handle('tre:pick-archives', ...), ipcMain.handle('workspace:pick-dir', ...), ipcMain.handle('workspace:pick-file', ...)
  const win = new BrowserWindow(...) — `win.webContents.send(...)` is the push mechanism to use

From packages/renderer/src/services/treAutoMount.ts, changesetService.ts (sealVersion/setLiveVersion),
packages/renderer/src/panels/deploy/DeployDialog.tsx (handleDeploy/handleReset) — existing call sites
to instrument, NOT rewrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main-log IPC channel + installMainLogForward()</name>
  <files>packages/backend/src/main.ts, packages/contracts/src/ipc.ts, packages/renderer/src/services/logService.ts</files>
  <read_first>
    packages/backend/src/main.ts (the `win` BrowserWindow instance + the 3 existing ipcMain.handle blocks — add one `win.webContents.send('main-log', {...})` call inside each, on both success and failure paths)
    packages/renderer/src/services/logService.ts (04.4-02's installConsoleCapture — add a sibling installMainLogForward function, do not modify the console-wrap logic)
  </read_first>
  <action>
    In `main.ts`, add a small `function sendMainLog(level: 'info'|'warn'|'error', message: string): void { win.webContents.send('main-log', { level, message }); }` helper (defined after `win` is constructed), and call it from the 3 existing dialog `ipcMain.handle` blocks — e.g. `sendMainLog('info', 'workspace:pick-dir invoked')` before the dialog call, and `sendMainLog('warn', 'workspace:pick-dir cancelled')` when `result.canceled`. Also call `sendMainLog('info', 'app ready — COOP/COEP registered')` once inside `app.whenReady().then(...)` near the top. Add a `MainLogEvent { level: 'info'|'warn'|'error'; message: string }` type export to `ipc.ts` (a plain exported interface, not part of the request/response `IpcChannels` map since this is a one-way push, not an invoke channel). In `logService.ts`, add `export function installMainLogForward(): void { const { ipcRenderer } = require('electron'); ipcRenderer.on('main-log', (_event, entry: MainLogEvent) => log(entry.level, 'log', entry.message)); }` — call this once from the SAME mount point `installConsoleCapture()` is already called from (ConsolePanel.tsx's mount effect, per 04.4-02 Task 3) so both capture halves start together.
  </action>
  <acceptance_criteria>
    - Manual check: triggering the "Open Project" folder picker produces a new Log-tab entry originating from the main process (distinguishable from renderer-originated entries by content, e.g. "workspace:pick-dir invoked").
    - `pnpm --filter @swg/contracts exec tsc --noEmit` clean after adding `MainLogEvent`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instrument deploy/reconcile/mount call sites (D-14)</name>
  <files>packages/renderer/src/services/treAutoMount.ts, packages/renderer/src/services/changesetService.ts, packages/renderer/src/panels/deploy/DeployDialog.tsx</files>
  <read_first>
    packages/renderer/src/services/treAutoMount.ts (autoMountClient/autoMountTarget entry/exit points)
    packages/renderer/src/services/changesetService.ts (sealVersion success/N4-guard-throw paths; setLiveVersion)
    packages/renderer/src/panels/deploy/DeployDialog.tsx (handleDeploy success/error setPhase branches; handleReset success/error branches — lines ~600-740)
  </read_first>
  <action>
    Add `log('info', 'log', ...)` (or `'warn'`/`'error'` on failure branches) calls from `logService.ts` (04.4-02) at these existing points, without altering their control flow: `treAutoMount.ts` — on successful mount completion (`'Mounted &lt;N&gt; archives for &lt;client/target&gt;'`) and on a caught mount error (`'warn'`, since auto-mount errors are already swallowed non-fatally per existing behavior); `changesetService.ts` — inside `sealVersion` after `sealComplete` (`'Saved version: &lt;label&gt;'`) and inside `setLiveVersion` after the pointer write (`'Reconciled to version &lt;id&gt;'`); `DeployDialog.tsx` — inside `handleDeploy`'s success branch (`'Deployed &lt;patchName&gt; via &lt;model&gt;'`) and error branch (`'error'`, the caught message), and inside `handleReset`'s success/error branches (`'Reset deployment'` / `'error'`, the caught message).
  </action>
  <acceptance_criteria>
    - Manual check: mounting a project, saving a version, selecting/reconciling a version, and deploying all produce a corresponding Log-tab entry with the correct level.
    - No existing test in `treAutoMount.test.ts`/`changesetService.test.ts` (if present) regresses — the added log calls are side-effect-only, never altering return values or thrown errors.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main process → renderer via `main-log` IPC | One-way push of diagnostic strings; no untrusted external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-20 | Information Disclosure | Logged messages could include a full filesystem path | accept | Local single-user desktop tool; same class of info already visible in StatusBar/Inspector |
</threat_model>

<verification>
Manual walk: open dialog, save version, select version, deploy, reset — each produces a Log-tab entry. `pnpm test` full suite green (no regressions from the added log call sites).
</verification>

<success_criteria>
Log tab shows both main-process and instrumented app events with correct levels from day one.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-11-SUMMARY.md` when done
</output>
