---
phase: 04.4-ux-polish-deploy-hardening
plan: 11
type: execute
wave: 2
depends_on: ["04.4-01", "04.4-02", "04.4-09"]
files_modified:
  - packages/backend/src/main.ts
  - packages/contracts/src/ipc.ts
  - packages/renderer/src/services/logService.ts
  - packages/renderer/src/services/treAutoMount.ts
  - packages/renderer/src/services/changesetService.ts
  - packages/renderer/src/services/syncLiveToVersion.ts
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
autonomous: true
requirements: [inapp-console-log-tabs-inactive]

# REVISION NOTE (--reviews pass, 2026-07-03):
#   - FOLDED installMainLogForward INTO installConsoleCapture (both in logService.ts, the file this
#     plan already lists). Opus/Sonnet correctly flagged that calling `installMainLogForward()` "from
#     the same mount point installConsoleCapture() is already called from" required editing
#     ConsolePanel.tsx, which was NOT in this plan's files_modified — an unlisted-file edit. Fixed:
#     04.4-02 left an explicit insertion-point comment inside `installConsoleCapture()`'s body in
#     logService.ts; this plan's Task 1 adds the main-log-forward subscription AT that comment, inside
#     the SAME function, so the ONE existing module-scope call site (in ConsolePanel.tsx, owned by
#     04.4-02, untouched by this plan) now does both console-wrap AND main-log-forward. No new call
#     site, no ConsolePanel.tsx edit needed.
#   - depends_on now includes "04.4-01" (both plans edit `DeployDialog.tsx` — 01 extracts the restore
#     dispatch into deploymentReset.ts; this plan adds log() instrumentation calls to handleDeploy/
#     handleReset — sequential edits to the same file, correctly ordered since 01 already runs first
#     in Wave 1) and "04.4-09" (both plans edit `main.ts` — 09 adds the SWG_TEST_MODE dialog guards
#     first, this plan adds sendMainLog calls to the SAME 3 handlers second). Opus flagged the missing
#     04.4-09 dependency as a robustness gap even though wave ordering already made it safe; now it's
#     explicit in the dependency graph too.

# REVISION NOTE (fact-check pass, 2026-07-03): CORRECTS AN INVERTED CLAIM injected by the prior
# --reviews pass (the bullet directly above this one). That pass claimed `setLiveVersion` was "the
# shipped reconcile-to-version entry point" and that `syncLiveToVersion` was a stale alias that
# "doesn't exist" — this was FALSIFIED by a direct read of both files:
#   - `packages/renderer/src/services/syncLiveToVersion.ts` EXISTS and is the SHIPPED version-reconcile
#     ENGINE ("Version reconcile engine — Wave-1 implementation VER-02/VER-03/VER-04/VER-06/VER-09"),
#     with its own test file `syncLiveToVersion.test.ts`, imported by `DeployDialog.tsx`,
#     `VersionHistoryBody.tsx`, and `undoStore.ts`. It performs the actual file-level revert/apply
#     operations (deployLoose/resetLoose/restoreCfg/activatePatch/deactivatePatch, H4-dispatched on the
#     live vs. target version's deploy model).
#   - `setLiveVersion` (changesetService.ts:465) is only the manifest live-pointer setter — it writes
#     `activeVersionId`/`deployedVersionId` and re-materializes staging. It performs NO reconcile file
#     operations. `syncLiveToVersion` calls `setLiveVersion` internally, as its LAST step, purely to
#     move the pointers (see syncLiveToVersion.ts:189 noop path and :313 the main path).
#   - So: selecting a version invokes `syncLiveToVersion` (the engine), which performs the reconcile
#     work and then updates the live pointer via `setLiveVersion`. Task 2 below is corrected to
#     instrument `syncLiveToVersion` (in syncLiveToVersion.ts) for the reconcile-started/finished
#     events, since that is the only place the result data (`noop`, `model`) exists — NOT
#     `setLiveVersion`, which would either miss that data or produce a redundant, less-informative
#     duplicate log entry.

# REVISION NOTE (round 2, --reviews pass, 2026-07-03):
#   - Opus MEDIUM, VERIFIED true by re-reading 04.4-02's Task 1 test spec: `logService.test.ts` (from
#     04.4-02, NOT edited by this plan) calls `installConsoleCapture()` DIRECTLY (importing it from
#     `logService.ts`), bypassing `ConsolePanel.tsx`'s module-scope `process.env.VITEST` guard
#     entirely. This plan's Task 1 adds `require('electron')` INSIDE `installConsoleCapture()`'s body
#     — in a Vitest/Node environment (not real Electron), `require('electron')` returns a STRING (the
#     path to the electron binary), not an object, so `const { ipcRenderer } = require('electron')`
#     silently yields `ipcRenderer === undefined`, and `ipcRenderer.on(...)` THROWS. Running
#     `logService.test.ts` after this plan lands would therefore break, with no acceptance criterion
#     catching it. FIXED below (Task 1): the `require('electron')` call is now guarded INSIDE
#     `installConsoleCapture()` itself (not just at the ConsolePanel.tsx call site), and Task 1's
#     acceptance criteria now explicitly re-runs `logService.test.ts` to prove no regression.
#   - Cursor/Opus MEDIUM ("app ready" log likely lost — sent before the renderer's IPC listener
#     attaches; Electron does not queue `webContents.send` for a not-yet-listening renderer) — FIXED
#     below (Task 1): the one-time "app ready" log is now sent from `win.webContents.once('did-finish-
#     load', ...)` instead of directly inside `app.whenReady().then(...)` — by `did-finish-load`, the
#     renderer's initial synchronous module graph (including `ConsolePanel.tsx`'s module-scope
#     `installConsoleCapture()` call, which this plan's subscription now lives inside) has already run,
#     so the `main-log` listener is guaranteed attached before this specific log line is sent. This
#     does not guarantee every possible early main-process log is captured (a log sent before ANY
#     window exists still has nowhere to go) but closes the concrete, cited "lost app-ready log" case.
must_haves:
  truths:
    - "Main-process events (dialog invocations, app lifecycle) reach the in-app Log tab, not just DevTools (D-13)"
    - "Mount, deploy, reconcile, and reset actions emit structured Log entries with levels on day one (D-14)"
    - "Main-log forwarding starts from the SAME already-existing module-scope call site 04.4-02 created (installConsoleCapture in logService.ts) — no new, unlisted call site"
    - "The require('electron') seam added to installConsoleCapture() is guarded so 04.4-02's existing logService.test.ts (which calls installConsoleCapture() directly, outside any SWG_TEST_MODE/VITEST-guarded call site) does not regress (round-2 fix)"
    - "The one-time 'app ready' main-log line is sent after the renderer's main-log listener is guaranteed attached (did-finish-load), not immediately in app.whenReady() (round-2 fix — avoids a lost first log line)"
  artifacts:
    - path: "packages/renderer/src/services/logService.ts"
      provides: "installConsoleCapture() extended in-place to ALSO subscribe to the main-process 'main-log' IPC channel — one function, one call site, with a VITEST-safe guard around the require('electron') call"
  key_links:
    - from: "main.ts ipcMain handlers"
      to: "webContents.send('main-log', entry)"
      via: "one-way push channel, forwarded to the renderer's logStore; the one-time 'app ready' line waits for did-finish-load"
      pattern: "main-log"
    - from: "treAutoMount.ts / changesetService.ts (sealVersion) / syncLiveToVersion.ts (the reconcile engine) / DeployDialog.tsx"
      to: "logService.log('log', ...)"
      via: "structured app-event emission at existing call sites"
      pattern: "log\\('info'|log\\('warn'|log\\('error'"
---

<objective>
Complete D-12/D-13's "capture scope = renderer + main" by forwarding main-process events into the Log tab (04.4-02 built the renderer-side console-capture half AND left an explicit extension point inside `installConsoleCapture()` for this exact purpose), and instrument the deploy/reconcile/mount call sites this phase touches (D-14) so the Log tab is useful on day one, not an empty pane.

The main-log forward subscription is added INSIDE the existing `installConsoleCapture()` function body in `logService.ts` (at the insertion-point comment 04.4-02 left) — not as a new exported function with a new call site — so both capture halves start together from the ONE module-scope call already installed by 04.4-02 in `ConsolePanel.tsx`. **(Round 2)** The `require('electron')` call this adds is guarded so it is safe when `installConsoleCapture()` is called directly from a Vitest test (04.4-02's `logService.test.ts` does exactly this, without going through the `ConsolePanel.tsx` module-scope call site's own `VITEST` guard).

Purpose: "Console tab should mean I never need Ctrl+Shift+I" — this plan is the main-process half of that bar.
Output: a `main-log` one-way IPC channel + a main-log-forward subscription folded into `installConsoleCapture()` (VITEST-guarded) + structured log calls at 4-5 existing service call sites + a did-finish-load-deferred "app ready" log line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-02-PLAN.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-01-PLAN.md

<interfaces>
From packages/renderer/src/services/logService.ts (04.4-02 — this plan EXTENDS installConsoleCapture
in-place, at the comment 04.4-02 left: "// 04.4-11 extends this function with a main-process
log-forward subscription here" — do NOT add a second exported function):
  log(level, channel, message): void
  installConsoleCapture(): void  // module-scope-called once from ConsolePanel.tsx (04.4-02) — this
    plan adds a `require('electron').ipcRenderer.on('main-log', ...)` subscription INSIDE this
    function's body, guarded by the SAME `installed` idempotency flag that already prevents
    double-wrapping — so it only subscribes once regardless of re-evaluation. ROUND 2: also guarded
    against a non-Electron (Vitest/Node) call context — see Task 1's action.
  // VERIFIED 2026-07-03 (round 2): 04.4-02's own `logService.test.ts` calls `installConsoleCapture()`
  // DIRECTLY (not via ConsolePanel.tsx), so ConsolePanel.tsx's module-scope `process.env.VITEST` guard
  // does NOT protect this direct call. The guard added by THIS plan must live INSIDE
  // installConsoleCapture() itself.

From packages/backend/src/main.ts (current — the 3 dialog ipcMain.handle blocks + app.whenReady();
ALSO edited by 04.4-09 in the same Wave 1, before this plan's Wave-2 edit, per depends_on):
  ipcMain.handle('tre:pick-archives', ...), ipcMain.handle('workspace:pick-dir', ...), ipcMain.handle('workspace:pick-file', ...)
  const win = new BrowserWindow(...) — `win.webContents.send(...)` is the push mechanism to use
  win.webContents.once('did-finish-load', ...) — ROUND 2: the correct point to send the one-time
    "app ready" log, guaranteeing the renderer's initial module graph (and therefore its
    `ipcRenderer.on('main-log', ...)` subscription) has already run.

From packages/renderer/src/services/treAutoMount.ts, changesetService.ts (sealVersion — success/
N4-guard-throw paths), packages/renderer/src/services/syncLiveToVersion.ts (VERIFIED 2026-07-03 the
shipped version-reconcile ENGINE — `export async function syncLiveToVersion(targetId, ctx):
Promise<LiveReconcileResult>` with `LiveReconcileResult { liveVersionId, model: 'loose'|'cfg', record?,
noop: boolean }`; it calls `setLiveVersion` internally as its FINAL step to move the pointers —
`setLiveVersion` itself does no reconcile file work), packages/renderer/src/panels/deploy/DeployDialog.tsx
(handleDeploy/handleReset — ALSO edited by 04.4-01 in the same Wave 1, before this plan's Wave-2 edit,
per depends_on; 04.4-01 extracts the restore dispatch into `deploymentReset.ts` and calls it from
`handleReset` — this plan's log() calls wrap AROUND that call, not inside it) — existing call sites to
instrument, NOT rewrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main-log IPC channel + fold the VITEST-guarded forward-subscription into installConsoleCapture()</name>
  <files>packages/backend/src/main.ts, packages/contracts/src/ipc.ts, packages/renderer/src/services/logService.ts</files>
  <read_first>
    packages/backend/src/main.ts (the `win` BrowserWindow instance + the 3 existing ipcMain.handle blocks — add one `win.webContents.send('main-log', {...})` call inside each, on both success and failure paths; also read 04.4-09's SWG_TEST_MODE guard additions to these same 3 handlers, already landed by the time this task runs — add sendMainLog calls alongside, not in place of, those guards)
    packages/renderer/src/services/logService.ts (04.4-02's installConsoleCapture — find the insertion-point comment left there and add the main-log-forward subscription AT that exact spot, inside the same function body, under the same `installed` guard)
    packages/renderer/src/services/logService.test.ts (04.4-02's existing test — VERIFIED 2026-07-03 to call `installConsoleCapture()` directly, not via ConsolePanel.tsx's VITEST guard — this task's new require('electron') call must not break it)
  </read_first>
  <action>
    In `main.ts`, add a small `function sendMainLog(level: 'info'|'warn'|'error', message: string): void { win.webContents.send('main-log', { level, message }); }` helper (defined after `win` is constructed), and call it from the 3 existing dialog `ipcMain.handle` blocks — e.g. `sendMainLog('info', 'workspace:pick-dir invoked')` before the dialog call, and `sendMainLog('warn', 'workspace:pick-dir cancelled')` when `result.canceled`. **(ROUND 2 — moved off app.whenReady()):** call `sendMainLog('info', 'app ready — COOP/COEP registered')` from INSIDE `win.webContents.once('did-finish-load', () => { sendMainLog('info', 'app ready — COOP/COEP registered'); })` (registered once, right after `win` is created / `loadURL`/`loadFile` is called) instead of directly inside `app.whenReady().then(...)` — this guarantees the renderer's initial synchronous module evaluation (including `ConsolePanel.tsx`'s module-scope `installConsoleCapture()` call, which now includes the `main-log` subscription this task adds) has already completed before this specific log line is sent, closing the "renderer isn't listening yet" race Cursor/Opus flagged. Add a `MainLogEvent { level: 'info'|'warn'|'error'; message: string }` type export to `ipc.ts` (a plain exported interface, not part of the request/response `IpcChannels` map since this is a one-way push, not an invoke channel).
    In `logService.ts`, DO NOT create a new exported `installMainLogForward` function. Instead, edit the BODY of the existing `installConsoleCapture()` function: at the insertion-point comment 04.4-02 left, add a GUARDED subscription —
    `if (typeof process === 'undefined' || process.env['VITEST'] !== '1') { try { const { ipcRenderer } = require('electron') as { ipcRenderer: { on(channel: string, listener: (...args: unknown[]) => void): void } }; ipcRenderer?.on?.('main-log', (_event, entry: MainLogEvent) => log(entry.level, 'log', entry.message)); } catch { /* not running inside Electron (e.g. a Vitest/Node context) — no-op */ } }`
    — the `process.env.VITEST` check is the PRIMARY guard (matches 04.4-02's own idiom exactly, so it protects `logService.test.ts`'s DIRECT call to `installConsoleCapture()`, not just the ConsolePanel.tsx module-scope call site); the `try/catch` + optional-chaining (`ipcRenderer?.on?.`) is defense-in-depth for any OTHER non-Electron caller this function might ever have. This code runs exactly once per app session, inside the SAME `if (installed) return; installed = true;` guard that already protects the console-wrap logic, so it can never double-subscribe. No change is needed to `ConsolePanel.tsx` (04.4-02's module-scope call to `installConsoleCapture()` now transitively does both halves).
  </action>
  <acceptance_criteria>
    - Manual check: triggering the "Open Project" folder picker produces a new Log-tab entry originating from the main process (distinguishable from renderer-originated entries by content, e.g. "workspace:pick-dir invoked").
    - `grep -n "ipcRenderer" packages/renderer/src/services/logService.ts` shows the subscription INSIDE `installConsoleCapture`'s function body (same function as the console-wrap logic, not a separate exported function), guarded by a `process.env['VITEST']` check.
    - `grep -c "installConsoleCapture" packages/renderer/src/panels/ConsolePanel.tsx` is unchanged from what 04.4-02 committed (still exactly one module-scope call, no edits from this plan).
    - `pnpm --filter @swg/contracts exec tsc --noEmit` clean after adding `MainLogEvent`.
    - **(Round 2)** `pnpm --filter @swg/renderer exec vitest run logService.test.ts` — re-run 04.4-02's existing test suite AFTER this plan's edit and confirm it is STILL GREEN (proves the guarded `require('electron')` addition does not regress the test that calls `installConsoleCapture()` directly).
    - **(Round 2)** `grep -n "did-finish-load" packages/backend/src/main.ts` shows the one-time "app ready" log call is inside this event handler, not directly inside `app.whenReady().then(...)`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instrument deploy/reconcile/mount call sites (D-14)</name>
  <files>packages/renderer/src/services/treAutoMount.ts, packages/renderer/src/services/changesetService.ts, packages/renderer/src/services/syncLiveToVersion.ts, packages/renderer/src/panels/deploy/DeployDialog.tsx</files>
  <read_first>
    packages/renderer/src/services/treAutoMount.ts (autoMountClient/autoMountTarget entry/exit points)
    packages/renderer/src/services/changesetService.ts (sealVersion success/N4-guard-throw paths)
    packages/renderer/src/services/syncLiveToVersion.ts (the VERIFIED reconcile ENGINE — its two return
      points: the noop fast-path at the top, and the final return after apply/revert; `setLiveVersion`,
      called internally as its last step, is only the pointer move and is NOT separately instrumented
      here to avoid double-logging one reconcile as two Log-tab entries)
    packages/renderer/src/panels/deploy/DeployDialog.tsx (handleDeploy success/error setPhase branches; handleReset success/error branches — post-04.4-01, handleReset calls `resetDeploymentFromRecord` from `deploymentReset.ts` for the restore dispatch; this task's log() calls wrap that call, they do not need to re-instrument inside deploymentReset.ts itself)
  </read_first>
  <action>
    Add `log('info', 'log', ...)` (or `'warn'`/`'error'` on failure branches) calls from `logService.ts` (04.4-02) at these existing points, without altering their control flow: `treAutoMount.ts` — on successful mount completion (`'Mounted &lt;N&gt; archives for &lt;client/target&gt;'`) and on a caught mount error (`'warn'`, since auto-mount errors are already swallowed non-fatally per existing behavior); `changesetService.ts` — inside `sealVersion` after `sealComplete` (`'Saved version: &lt;label&gt;'`) only (do NOT add a log call to `setLiveVersion` — see below); `syncLiveToVersion.ts` — inside `syncLiveToVersion`, immediately before its noop-fast-path `return` (`'Reconciled to version &lt;targetId&gt; (&lt;liveModel&gt;, noop)'`) and immediately before its final `return` (`'Reconciled to version &lt;targetId&gt; (&lt;result model&gt;, N files)'`, using the computed `newRecord`/entries count for N when available) — this is the ONLY place the reconcile result data (`noop`, `model`) exists, since `syncLiveToVersion` calls `setLiveVersion` internally purely to move the pointers; `DeployDialog.tsx` — inside `handleDeploy`'s success branch (`'Deployed &lt;patchName&gt; via &lt;model&gt;'`) and error branch (`'error'`, the caught message), and inside `handleReset`'s success/error branches (`'Reset deployment'` / `'error'`, the caught message) — these wrap AROUND the `resetDeploymentFromRecord` call 04.4-01 introduced, not inside it. `DeployDialog.tsx`'s direct `setLiveVersion(...)` call (the post-file-work pointer move for a freshly-deployed NEW version, distinct from the reconcile-engine calls) is already covered by `handleDeploy`'s success-branch log call above — do not add a second, redundant log there.
  </action>
  <acceptance_criteria>
    - Manual check: mounting a project, saving a version, selecting/reconciling a version, and deploying all produce a corresponding Log-tab entry with the correct level.
    - No existing test in `treAutoMount.test.ts`/`changesetService.test.ts`/`syncLiveToVersion.test.ts` (if present) regresses — the added log calls are side-effect-only, never altering return values or thrown errors.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main process → renderer via `main-log` IPC | One-way push of diagnostic strings; no untrusted external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-20 | Information Disclosure | Logged messages could include a full filesystem path | accept | Local single-user desktop tool; same class of info already visible in StatusBar/Inspector |
</threat_model>

<verification>
Manual walk: open dialog, save version, select version, deploy, reset — each produces a Log-tab entry. `pnpm test` full suite green (no regressions from the added log call sites, INCLUDING 04.4-02's logService.test.ts re-run per Task 1's round-2 acceptance criterion).
</verification>

<success_criteria>
Log tab shows both main-process and instrumented app events with correct levels from day one, forwarded from the single existing capture call site (no new, unlisted mount point); the require('electron') addition is VITEST-safe (round-2 fix, verified by re-running 04.4-02's test); the first main-process log line is not lost to a listener-attachment race (round-2 fix).
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-11-SUMMARY.md` when done
</output>
