---
phase: 04.4-ux-polish-deploy-hardening
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - packages/backend/src/main.ts
  - packages/contracts/src/ipc.ts
  - packages/renderer/src/testHooks.ts
  - packages/renderer/src/workspace/WorkspaceShell.tsx
  - e2e/fixtures/cfgModelClient.ts
  - e2e/fixtures/looseOverrideClient.ts
autonomous: true
requirements: [e2e-deploy-flow-coverage]

must_haves:
  truths:
    - "CI's lean job builds the renderer/main/preload bundles BEFORE running Playwright specs (the verified gap is closed)"
    - "A SWG_TEST_MODE env-gated hook surface exists for dialog stubbing + store/deploy-state reads, and is INERT (no window.__testHooks, no dialog interception) when the env var is unset"
    - "Two reusable fixture client trees exist (cfg-model and loose-override) that new e2e specs build against — never a real install (D-06)"
  artifacts:
    - path: "packages/renderer/src/testHooks.ts"
      provides: "env-gated window.__testHooks surface (dialog-independent state reads/action triggers), MCP-shaped per D-08"
    - path: "e2e/fixtures/cfgModelClient.ts"
      provides: "buildCfgModelClient(tmpRoot) — minimal swgemu.cfg + searchTree layout fixture builder"
    - path: "e2e/fixtures/looseOverrideClient.ts"
      provides: "buildLooseOverrideClient(tmpRoot) — dev-client client.cfg + searchPath layout fixture builder"
  key_links:
    - from: ".github/workflows/ci.yml lean job"
      to: "vite.main.config.ts / vite.preload.config.ts / vite.renderer.config.ts"
      via: "a new build step inserted between 'Unit tests (vitest)' and 'Install Playwright browsers'"
      pattern: "vite build -c"
    - from: "main.ts dialog IPC handlers"
      to: "SWG_TEST_MODE canned-response map"
      via: "process.env.SWG_TEST_MODE gate, never active unless explicitly set"
      pattern: "SWG_TEST_MODE"
---

<objective>
Build the shared E2E test infrastructure that BOTH new deploy-flow and delete-flow specs (plans 13/14) depend on: (1) fix the VERIFIED CI gap — the lean job runs `playwright test` with NO prior build step, so `.vite/renderer/main_window/` (gitignored, `app://`-served) does not exist in a clean CI checkout; (2) a `SWG_TEST_MODE` env-gated hook surface (D-08) — dialog stubbing (Playwright cannot drive native OS dialogs) + store/deploy-state reads, designed as ONE coherent surface the future MCP server can reuse as-is, never shipped active; (3) two reusable fixture client trees (cfg-model + loose-override, D-06) — never a real install.

Purpose: closes the "jsdom-green ≠ Electron-runs" blind spot's PREREQUISITE gap before any new real-Electron spec can be trusted.
Output: CI build step; `main.ts` env-gated dialog stub + `testHooks.ts`; two fixture builders under `e2e/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/todos/pending/e2e-deploy-flow-coverage.md
@$HOME/.claude/get-shit-done/references/design-test-hook-points-for-harness.md

<interfaces>
Current .github/workflows/ci.yml lean job step order (lines 48-68 — the exact insertion point):
  "Install dependencies" (pnpm install) -> "contracts/ TypeScript check" -> "Unit tests (vitest)"
  -> [NEW STEP GOES HERE] -> "Install Playwright browsers" -> "E2E tests — non-packaged specs"

forge.config.ts VitePlugin build entries (the exact config files to invoke directly, bypassing the
slow full electron-forge package/make):
  packages/backend/src/main.ts    -> vite.main.config.ts
  packages/backend/src/preload.ts -> vite.preload.config.ts
  (renderer entry)                -> vite.renderer.config.ts

packages/backend/src/main.ts (current — 3 existing dialog IPC handlers to gate, verbatim locations):
  ipcMain.handle('tre:pick-archives', ...)   -- calls dialog.showOpenDialog
  ipcMain.handle('workspace:pick-dir', ...)  -- calls dialog.showOpenDialog
  ipcMain.handle('workspace:pick-file', ...) -- calls dialog.showOpenDialog

packages/contracts/src/ipc.ts IpcChannels map (existing pattern to extend, though the new test-only
channel is intentionally OUTSIDE the invoke/return-type contract since it is dev/test-only and gated):
  export interface IpcChannels { 'workspace:pick-dir': string[]; 'workspace:pick-file': string[]; 'tre:pick-archives': string[]; }

packages/renderer/src/workspace/WorkspaceShell.tsx — mount point for a one-line env-gated test-hook
install call (this file is NOT touched by any other plan in this phase — safe insertion point that
avoids the App.tsx file-overlap already used by plans 01/10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI build-before-E2E fix</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (lines 28-68, the full lean job)
    forge.config.ts (VitePlugin build entries + config file names)
  </read_first>
  <action>
    Insert a new step named "Build renderer/main/preload bundles (lean, non-packaged)" into the `lean` job between the existing "Unit tests (vitest)" step and "Install Playwright browsers" step, running the three underlying Vite builds directly (NOT `electron-forge package`, which the job's own design deliberately avoids for speed): `pnpm exec vite build -c vite.main.config.ts`, `pnpm exec vite build -c vite.preload.config.ts`, `pnpm exec vite build -c vite.renderer.config.ts` (three separate `run:` lines or one `run: |` block). This produces `.vite/build/main.js`, `.vite/build/preload.js`, and `.vite/renderer/main_window/` so the subsequent Playwright specs (served via `app://` per `main.ts`) load a real, current bundle.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` lean job has exactly one new step between "Unit tests (vitest)" and "Install Playwright browsers", running all three vite build commands.
    - Running the same three commands locally produces `.vite/build/main.js` and `.vite/renderer/main_window/index.html` on disk.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SWG_TEST_MODE hook surface — main.ts dialog stub + renderer testHooks.ts</name>
  <files>packages/backend/src/main.ts, packages/contracts/src/ipc.ts, packages/renderer/src/testHooks.ts, packages/renderer/src/workspace/WorkspaceShell.tsx</files>
  <read_first>
    packages/backend/src/main.ts (the 3 existing `ipcMain.handle` dialog blocks, exact current implementation)
    packages/contracts/src/ipc.ts (IpcChannels map + TypedIpcRenderer/TypedIpcHandler pattern)
    packages/renderer/src/state/workspaceStore.ts, changesetStore.ts (state the future MCP server + specs need to read)
    packages/renderer/src/workspace/WorkspaceShell.tsx (top of the component function, mount point for the install call)
    $HOME/.claude/get-shit-done/references/design-test-hook-points-for-harness.md
  </read_first>
  <action>
    In `main.ts`: add `const SWG_TEST_MODE = process.env['SWG_TEST_MODE'] === '1';` near the top of `app.whenReady().then(...)`. Add a `const testStubPaths = new Map&lt;string, string[]&gt;();` (main-process-local, in-memory). Register `ipcMain.handle('test:set-stub-paths', (_e, channel: string, paths: string[]) => { if (!SWG_TEST_MODE) return; testStubPaths.set(channel, paths); })` — this handler is registered UNCONDITIONALLY but is a no-op unless `SWG_TEST_MODE` is set (so registering it costs nothing in production and never activates). In each of the 3 existing dialog handlers, add a guard at the top: `if (SWG_TEST_MODE && testStubPaths.has('&lt;channel-name&gt;')) return testStubPaths.get('&lt;channel-name&gt;')!;` BEFORE the real `dialog.showOpenDialog` call — this lets a Playwright spec call `electronApp.evaluate(...)`-free stubbing via a normal `ipcRenderer.invoke('test:set-stub-paths', channel, paths)` from the renderer side instead. Add the `'test:set-stub-paths': void` entry to `IpcChannels` in `ipc.ts` for type-safety at call sites (args are channel:string + paths:string[] — note this channel takes extra args beyond the existing `defaultPath?` convention; extend `TypedIpcRenderer`'s `invoke` overload or add a second minimal typed helper specifically for this channel, whichever keeps `ipc.ts` internally consistent without breaking the existing 3 channels' typing).
    In `testHooks.ts` (new, renderer-side): `export function installTestHooks(): void { if (process.env['SWG_TEST_MODE'] !== '1') return; (window as any).__testHooks = { getWorkspaceStatus: () => useWorkspaceStore.getState().status, getChangesetManifest: () => useChangesetStore.getState().manifest, getDeployPhase: () => /* whatever DeployDialog's phase state exposes, or a minimal deployStore read if one exists */ null, setStubPaths: (channel: string, paths: string[]) => ipcRenderer.invoke('test:set-stub-paths', channel, paths) }; }` — start with this minimal, spec-driven set (D-08 "build only the hooks these specs need") and extend inline as plans 13/14 discover they need one more read; keep the object assignment itself gated identically to the main-process handler so BOTH halves are provably inert when `SWG_TEST_MODE` is unset. Call `installTestHooks()` once from a `useEffect` at the top of `WorkspaceShell.tsx`'s function body (this file has no other edits in this phase — a clean, single-owner mount point).
  </action>
  <acceptance_criteria>
    - With `SWG_TEST_MODE` unset (the default/production case), `window.__testHooks` is `undefined` (manual check: launch the app normally, `window.__testHooks === undefined` in the console) — the security_requirement's "must never ship active in production" is provably satisfied.
    - With `SWG_TEST_MODE=1` set, `window.__testHooks.getWorkspaceStatus()` returns the live workspace store status object.
    - `ipc.ts` still type-checks (`pnpm --filter @swg/contracts exec tsc --noEmit` clean) after adding the new channel.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fixture client trees (cfg-model + loose-override, D-06)</name>
  <files>e2e/fixtures/cfgModelClient.ts, e2e/fixtures/looseOverrideClient.ts</files>
  <read_first>
    e2e/fixtures/electron-helpers.ts (the existing fixtures dir's structural conventions)
    packages/renderer/src/services/clientLocator.ts (scanSharedFile — confirms what a parseable minimal swgemu.cfg needs: a `[SharedFile]` section)
    packages/renderer/src/services/looseOverrideDeploy.ts + clientSearchOrder.ts (searchPath/searchTOC dev-client layout expectations)
  </read_first>
  <action>
    Create `cfgModelClient.ts` exporting `buildCfgModelClient(tmpRoot: string): { installPath: string; cfgRootPath: string }` — writes a minimal client tree under `tmpRoot`: a root `swgemu.cfg` with a `[SharedFile]` section (`maxSearchPriority=60`, one `searchTree_00_10=some_base.tre` line) and a `Live/` subdirectory (empty is fine — no real .tre bytes needed for boot/dialog-stub/UI-feedback assertions per D-07's scenario scope). Create `looseOverrideClient.ts` exporting `buildLooseOverrideClient(tmpRoot: string): { installPath: string; cfgPath: string; overrideDir: string }` — writes a minimal `client.cfg` with `searchPath` entries (mirroring the swg-client-v2 dev-client layout: a `data/` loose dir referenced by the highest-priority `searchPath` line) under `tmpRoot`, with the override dir pre-created so `resolveOverrideDir`-family logic finds a real directory. Both builders MUST use a caller-supplied `tmpRoot` (never a hardcoded path) so specs can create/clean an isolated `os.tmpdir()`-based tree per test run — never touch a real install (D-06 explicit).
  </action>
  <acceptance_criteria>
    - Both fixture builders are pure functions (no global state) callable from a Playwright spec's `beforeAll`, each returning absolute paths that exist on disk after the call.
    - A quick manual/unit check: `scanSharedFile(buildCfgModelClient(tmp).cfgRootPath)` (from clientLocator.ts) parses the fixture without throwing and returns `maxSearchPriority === 60`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness (Playwright) → main process | SWG_TEST_MODE hook surface intercepts a real Electron API (dialog); must never be reachable outside an explicit test invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-17 | Elevation of Privilege | SWG_TEST_MODE hooks active in a shipped production build | mitigate | Both the main-process dialog gate and the renderer `window.__testHooks` assignment check `process.env.SWG_TEST_MODE === '1'` independently — inert by default; acceptance criteria in Task 2 requires a manual confirmation this is provably off |
| T-04.4-SC | Tampering | N/A — no new package installs this task | accept | No npm/pip/cargo install in scope |
</threat_model>

<verification>
Local `pnpm exec vite build -c vite.main.config.ts && pnpm exec vite build -c vite.preload.config.ts && pnpm exec vite build -c vite.renderer.config.ts` succeeds. `pnpm --filter @swg/contracts exec tsc --noEmit` clean. Manual: `window.__testHooks === undefined` with SWG_TEST_MODE unset.
</verification>

<success_criteria>
CI can build before E2E; SWG_TEST_MODE hooks exist and are provably inert by default; two fixture client trees are ready for plans 13/14.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-09-SUMMARY.md` when done
</output>
