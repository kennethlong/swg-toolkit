---
phase: 04.4-ux-polish-deploy-hardening
plan: 03
subsystem: ui
tags: [react, zustand, statusbar, vfs-tree, theming]

# Dependency graph
requires:
  - phase: 02-3d-mesh-viewport-mvp-proof
    provides: viewportStore (loadStatus/parsedMesh) and MeshShaderGroup.vertexCount contract
  - phase: 04.3-versioning-and-searchtoc-mount
    provides: StatusBar sketch-008 test coverage (S6/S7/P9) established in 04.3-09-PLAN.md
provides:
  - StatusBar mesh-name/vert-count chip live-bound to useViewportStore instead of hardcoded text
  - Legible non-override winning-archive label in VfsTree across all 5 themes
affects: [04.4-remaining-ux-polish-plans]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "StatusBar chips derive from Zustand store subscriptions (useXStore((s) => s.field)), never hardcoded/local literal state"

key-files:
  created: []
  modified:
    - packages/renderer/src/shell/StatusBar.tsx
    - packages/renderer/src/shell/StatusBar.test.tsx
    - packages/renderer/src/panels/tre/VfsTree.tsx

key-decisions:
  - "Extended the existing 258-line StatusBar.test.tsx in place (Edit, never Write) to preserve sketch-008 S6/S7/P9 coverage per the plan's round-3 revision note"
  - "vertCount is the sum of MeshShaderGroup.vertexCount across all shaderGroups for the currently shown LOD only — no per-LOD breakdown displayed (D-18)"

patterns-established:
  - "Pattern: StatusBar chips subscribe directly to domain stores (viewportStore, changesetStore, dockStateStore, clientScanStore) rather than hold local/hardcoded placeholder state"

requirements-completed: [statusbar-mesh-name-stale, vfs-override-archive-dim-too-dark]

# Metrics
duration: ~10min
completed: 2026-07-04
---

# Phase 04.4 Plan 03: StatusBar Live Mesh Chip + VfsTree Legibility Summary

**StatusBar mesh-name/vert-count chip now reads live from `useViewportStore` (loadStatus/parsedMesh) instead of rendering hardcoded `shared_landspeeder.msh · 4,812 verts`; VfsTree's non-override winning-archive label bumped from `--color-text-faint` to `--color-text-muted` for legibility across all 5 themes.**

## Performance

- **Duration:** ~10 min
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `StatusBar.tsx` subscribes to `useViewportStore((s) => s.loadStatus)` and `((s) => s.parsedMesh)`; the mesh chip shows the live filename and the `.toLocaleString()`-formatted sum of `MeshShaderGroup.vertexCount` across `parsedMesh.shaderGroups`, falling back to `'—'` for both fields when idle/loading/error or `parsedMesh` is null.
- Extended the existing sketch-008 `StatusBar.test.tsx` (10 pre-existing cases across 4 describe blocks) with 2 new cases (idle-state dashes, post-`loadComplete` filename+vert-count) — file was edited in place, never overwritten; original header comment and all prior cases survive unmodified.
- `VfsTree.tsx` non-override winning-archive pip color changed from `var(--color-text-faint)` to `var(--color-text-muted)`; the `entry.isOverride` true-branch (`var(--color-accent)`) is untouched. Confirmed `--color-text-muted` and `--color-text-faint` are distinct, meaningfully-lighter tokens in both `default.css` (#9a9a9a vs #6f6f6f) and `high-contrast.css` (#dcdce2 vs #aeaeb6).

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend the EXISTING StatusBar.test.tsx + wire mesh name/vert-count to useViewportStore** - `5c7c44b` (feat)
2. **Task 2: VfsTree non-override archive label — legibility token swap** - `5e03c80` (fix)

**Plan metadata:** (this commit, to follow)

## Files Created/Modified
- `packages/renderer/src/shell/StatusBar.tsx` - Added `useViewportStore` subscriptions (`loadStatus`, `parsedMesh`); derived `meshLabel`/`vertCount`; replaced hardcoded mesh-chip JSX
- `packages/renderer/src/shell/StatusBar.test.tsx` - Added 2 new test cases (idle-state dashes; post-`loadComplete` filename + vert-count) inside a new adjacent `describe` block; all 10 pre-existing cases untouched
- `packages/renderer/src/panels/tre/VfsTree.tsx` - Winning-archive pip non-override color: `--color-text-faint` → `--color-text-muted`

## Decisions Made
- Reused the plan's exact interface citations for `useViewportStore` (`loadStatus.kind === 'done' ? loadStatus.filename : ...`) and `MeshShaderGroup.vertexCount` (summed via `.reduce`) rather than re-deriving from scratch.
- Test stub for `parsedMesh` in the new test case uses a minimal partial object cast (`as unknown as MeshParseResult`) since only `shaderGroups[].vertexCount` is exercised by the component logic; `resolution` param stubbed similarly as `{} as AppearanceResolutionResult` since `loadComplete` only threads it into store state, unread by StatusBar.

## Deviations from Plan

None — plan executed exactly as written. One process note: Task 1 was marked `tdd="true"`; the test-file extension and the `StatusBar.tsx` implementation edit were verified together (ran `vitest run StatusBar.test.tsx` once, green, before committing) rather than as a strict separate RED-commit/GREEN-commit pair. The plan's own frontmatter type is `execute` (not `type: tdd`), so the plan-level RED/GREEN/REFACTOR gate-commit-sequence requirement does not apply here; documented for transparency since the task itself carried `tdd="true"`.

## Issues Encountered
- Initial edit of `StatusBar.tsx` added a code comment referencing the literal string `shared_landspeeder.msh` (for context), which caused the Task 1 acceptance-criteria grep (`grep -c "shared_landspeeder" ... returns 0`) to fail with count 1. Fixed by rewording the comment to avoid the literal string before committing; re-verified grep returns 0.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Both `statusbar-mesh-name-stale` and `vfs-override-archive-dim-too-dark` todos are resolved; full renderer suite green (297/297 tests, 39 files), typecheck clean.
- Manual-only check remaining per plan's `<verification>`: switch through all 5 themes in the running app and visually confirm the non-override archive label reads as legible (VALIDATION.md, not automatable — deferred to phase-level manual pass).
- Ready to proceed to the next plan in Phase 04.4 (remaining UX-polish todos: delete-project-with-restore, e2e deploy-flow coverage, viewport-default-facing-axis, etc.).

---
*Phase: 04.4-ux-polish-deploy-hardening*
*Completed: 2026-07-04*

## Self-Check: PASSED

- FOUND: packages/renderer/src/shell/StatusBar.tsx
- FOUND: packages/renderer/src/shell/StatusBar.test.tsx
- FOUND: packages/renderer/src/panels/tre/VfsTree.tsx
- FOUND: .planning/phases/04.4-ux-polish-deploy-hardening/04.4-03-SUMMARY.md
- FOUND commit: 5c7c44b (Task 1)
- FOUND commit: 5e03c80 (Task 2)
- FOUND commit: 416c01a (docs: summary)
