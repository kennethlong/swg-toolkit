---
phase: 04.4
slug: ux-polish-deploy-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-03
---

# Phase 04.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (unit/jsdom) + Playwright (real-Electron E2E) + native-core harness |
| **Config file** | `vitest.config.ts` / `playwright.config.ts` |
| **Quick run command** | `npm test` (vitest) |
| **Full suite command** | `npm test && npm run build && npx playwright test` (build-before-E2E per D-11) |
| **Estimated runtime** | ~60s unit; ~5–10 min full with E2E cold launches |

---

## Sampling Rate

- **After every task commit:** Run `npm test` (vitest quick pass)
- **After every plan wave:** Run full suite (build + Playwright real-Electron specs)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 600 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by planner) | — | — | todo-driven | — | — | — | — | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] CI build-before-E2E fix (`.github/workflows/ci.yml` lean job builds renderer bundle before `playwright test`) — the verified gap from RESEARCH.md; E2E sampling is meaningless until this lands
- [ ] E2E fixture client trees (cfg-model + loose-override, per D-06) under `e2e/fixtures/`
- [ ] SWG_TEST_MODE env-gated hook surface seams (D-08) so specs can stub dialogs and read deploy state

*Existing vitest + Playwright infrastructure covers the rest.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Viewport default facing matches SIE/in-game | viewport-default-facing-axis (D-16/D-17) | Correctness is visual parity with SIE; research found a documented false start (rotateY(π) showed mesh back) | Maintainer eyeballs protocol_droid_red + one more asymmetric mesh side-by-side with SIE before task close (autonomous:false checkpoint) |
| VfsTree dim-label legibility across 5 themes | vfs-override-archive-dim-too-dark | Legibility is perceptual; token change verified per-theme by eye | Switch all 5 themes; confirm non-override winning-archive label readable in each |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 600s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
