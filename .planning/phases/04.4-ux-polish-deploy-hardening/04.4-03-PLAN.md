---
phase: 04.4-ux-polish-deploy-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/shell/StatusBar.tsx
  - packages/renderer/src/shell/StatusBar.test.tsx
  - packages/renderer/src/panels/tre/VfsTree.tsx
autonomous: true
requirements: [statusbar-mesh-name-stale, vfs-override-archive-dim-too-dark]

# REVISION NOTE (--reviews pass, 2026-07-03): 2/4 reviewers (Sonnet, verified independently in this
# replan too) flagged that `packages/renderer/src/shell/StatusBar.test.tsx` ALREADY EXISTS (258 lines,
# from 04.3-09-PLAN.md Task 2 — sketch-008 S6/S7/P9 coverage: deployed-version label, dock-state chip,
# scan message — with a nontrivial `@swg/native-core`/Worker/SAB mocking harness). CONFIRMED via direct
# read 2026-07-03. The original Task 1 said "Create StatusBar.test.tsx (new — none exists)" — executing
# that literally with a fresh Write would DESTROY this existing coverage. Fixed below: Task 1 now reads
# the existing file first and EXTENDS it (Edit, never Write) with new test cases inside the SAME file.
must_haves:
  truths:
    - "The status bar mesh name + vert count update to match the currently rendered mesh on every viewport load (D-18)"
    - "The VfsTree non-override winning-archive label is legible (not deselected-looking) across all 5 themes"
    - "The existing sketch-008 StatusBar.test.tsx coverage (deployed-version label, dock-state chip, scan message) survives this change unmodified"
  artifacts:
    - path: "packages/renderer/src/shell/StatusBar.tsx"
      provides: "useViewportStore-bound mesh name/vert-count chip (replaces hardcoded shared_landspeeder.msh text)"
  key_links:
    - from: "StatusBar.tsx"
      to: "useViewportStore((s) => s.loadStatus / s.parsedMesh)"
      via: "Zustand subscription, no polling"
      pattern: "useViewportStore"
    - from: "VfsTree.tsx"
      to: "--color-text-muted token"
      via: "non-override winning-archive pip color"
      pattern: "color-text-muted"
---

<objective>
Two small, independent, from-scratch/one-line fixes bundled into one plan per context-budget sizing rules (each is individually &lt;10% context):

1. `statusbar-mesh-name-stale`: `StatusBar.tsx` currently renders LITERAL HARDCODED TEXT (`shared_landspeeder.msh · 4,812 verts`) — this is NOT a stale-binding bug, it is a from-scratch wire-up to `useViewportStore` (confirmed in RESEARCH.md — no subscription exists today). `StatusBar.test.tsx` ALREADY EXISTS (258 lines, sketch-008 coverage) — this plan EXTENDS it, never overwrites it.
2. `vfs-override-archive-dim-too-dark`: bump the non-override winning-archive label from `--color-text-faint` to `--color-text-muted` in `VfsTree.tsx` (confirmed safe across all 5 themes per RESEARCH.md's token audit — no new token needed).

Purpose: remove two long-standing cosmetic/functional papercuts flagged during Phase-2 checkpoint testing.
Output: a live-updating status bar mesh chip (existing test file extended, not replaced); a legible (not "deselected-looking") non-override archive label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/todos/pending/statusbar-mesh-name-stale.md
@.planning/todos/pending/vfs-override-archive-dim-too-dark.md

<interfaces>
From packages/renderer/src/state/viewportStore.ts (VERIFIED signature 2026-07-03):
  useViewportStore((s) => s.loadStatus)   // { kind: 'idle' } | { kind:'loading', filename } | { kind:'done', filename, mode } | { kind:'error', ... }
  useViewportStore((s) => s.parsedMesh)   // MeshParseResult | null
  loadComplete(filename, mode, resolution, isSkinned, parsedMesh?, parsedSkeleton?) — parsedMesh/parsedSkeleton
    are OPTIONAL trailing params; a test may call with 5 args and omit parsedSkeleton.

From packages/contracts/src/mesh.ts:
  MeshParseResult.shaderGroups: MeshShaderGroup[]
  MeshShaderGroup.vertexCount: number   // sum across shaderGroups for the TOTAL vert count of the shown LOD

From packages/renderer/src/shell/StatusBar.test.tsx (EXISTING, 258 lines — VERIFIED 2026-07-03, DO NOT
overwrite):
  Header comment: "Component tests for StatusBar — sketch 008 gaps S6 + S7 + P9 scan message."
  5 existing test cases: S7 'live: vN — label', S7 no stale 'deployed:' text, S6 '.sb-state' element,
  P9 scan message, KEEP diagnostic chips (addon/SAB/zero-copy).
  Mocking harness: `vi.mock('@swg/native-core', ...)` returning stub hello/allocateSab/writeSab/readSab;
  a stubbed global `Worker` class; `URL.createObjectURL` stub — installed in `beforeEach`/`afterEach`.
  Imports `StatusBar` AFTER the mocks (hoisted `vi.mock` pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the EXISTING StatusBar.test.tsx + wire mesh name/vert-count to useViewportStore</name>
  <files>packages/renderer/src/shell/StatusBar.tsx, packages/renderer/src/shell/StatusBar.test.tsx</files>
  <read_first>
    packages/renderer/src/shell/StatusBar.test.tsx (the FULL existing 258-line file — read it before touching; reuse its existing `vi.mock('@swg/native-core', ...)` + stubbed-Worker + beforeEach/afterEach harness for any new test cases; add new `it()` blocks inside the existing `describe` structure or a new adjacent `describe` block — do NOT replace the file)
    packages/renderer/src/shell/StatusBar.tsx (lines 76-93 for this file's OWN existing `useXStore((s) => s.field)` subscription idiom; lines 244-248 for the exact hardcoded JSX being replaced)
    packages/renderer/src/state/viewportStore.ts
    packages/contracts/src/mesh.ts (MeshShaderGroup.vertexCount field)
  </read_first>
  <behavior>
    - When `loadStatus.kind === 'done'`, the status bar shows `loadStatus.filename` and the SUM of `parsedMesh.shaderGroups[].vertexCount` (formatted with `.toLocaleString()`, matching the existing `4,812` comma style).
    - When `loadStatus.kind !== 'done'` (idle/loading/error) or `parsedMesh` is null, the chip shows `'—'` for both name and vert count (never the old hardcoded landspeeder values).
    - No LOD-index is displayed anywhere in this chip (D-18 — vert count is for the currently shown LOD only, not per-LOD breakdown).
    - All 5 EXISTING test cases in StatusBar.test.tsx (S7 label, S7 no-stale-text, S6 state element, P9 scan message, KEEP diagnostic chips) still pass unmodified.
  </behavior>
  <action>
    In `StatusBar.tsx`, add two subscriptions alongside the existing store reads (near the other `useXStore` lines): `const loadStatus = useViewportStore((s) => s.loadStatus);` and `const parsedMesh = useViewportStore((s) => s.parsedMesh);`. Derive `const meshLabel = loadStatus.kind === 'done' ? loadStatus.filename : '—';` and `const vertCount = parsedMesh ? parsedMesh.shaderGroups.reduce((sum, g) => sum + g.vertexCount, 0).toLocaleString() : '—';`. Replace the two hardcoded `&lt;span&gt;shared_landspeeder.msh&lt;/span&gt;` / `&lt;span&gt;4,812 verts&lt;/span&gt;` lines with `&lt;span&gt;{meshLabel}&lt;/span&gt;` and `&lt;span&gt;{vertCount} verts&lt;/span&gt;`.
    Add two NEW `it()` blocks to the EXISTING `packages/renderer/src/shell/StatusBar.test.tsx` (use the file's own established mocking harness — do not duplicate/replace it): (a) idle state renders '—' / '—' for the mesh chip; (b) after `useViewportStore.getState().loadComplete('foo.msh', 'leaf', resolutionStub, false, meshStub)` where `meshStub.shaderGroups = [{vertexCount: 100}, {vertexCount: 50}]` (a minimal partial `MeshParseResult`, cast as needed for the test), the component renders 'foo.msh' and '150 verts'. Use `Edit` (append inside the existing describe block or a new adjacent one), never `Write` — the file must retain its existing 5 test cases and header comment.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run StatusBar.test.tsx` green — all 5 PRE-EXISTING cases plus the 2 new cases (7 total) pass.
    - `grep -c "shared_landspeeder" packages/renderer/src/shell/StatusBar.tsx` returns 0.
    - `grep -c "sketch 008 gaps S6" packages/renderer/src/shell/StatusBar.test.tsx` returns 1 (the original header comment survives — confirms the file was extended, not overwritten).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: VfsTree non-override archive label — legibility token swap</name>
  <files>packages/renderer/src/panels/tre/VfsTree.tsx</files>
  <read_first>
    packages/renderer/src/panels/tre/VfsTree.tsx (line ~360-384 — the winning-archive pip span whose color is `entry.isOverride ? 'var(--color-accent)' : 'var(--color-text-faint)'`)
    packages/renderer/src/themes (all 5 theme .css files — confirm `--color-text-muted` is meaningfully lighter than `--color-text-faint` in each; RESEARCH.md already confirmed default.css/high-contrast.css define both distinctly and are safe — the other 3 themes inherit)
  </read_first>
  <action>
    In the winning-archive pip span, change the ternary's false-branch color from `'var(--color-text-faint)'` to `'var(--color-text-muted)'` — the `entry.isOverride ? 'var(--color-accent)' : ...` true-branch (cyan) is UNCHANGED. This is the only line to touch (per RESEARCH.md's exact citation and the todo's own "one-line style change" scoping).
  </action>
  <acceptance_criteria>
    - `grep -n "color-text-muted" packages/renderer/src/panels/tre/VfsTree.tsx` shows the winning-archive pip line using the new token; `entry.isOverride ? 'var(--color-accent)'` branch is untouched.
    - Manual-only (VALIDATION.md): switch through all 5 themes in the running app and confirm the non-override archive label reads as a legible authoritative label, not a disabled/deselected row.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure presentational/state-read changes; no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-07 | Information Disclosure | Status bar mesh filename could leak an internal asset path via the DOM | accept | Same information already shown in the Inspector panel; local desktop tool, no remote exposure |
</threat_model>

<verification>
`pnpm --filter @swg/renderer exec vitest run StatusBar.test.tsx` green (7 cases: 5 pre-existing + 2 new). `pnpm test` (full renderer suite) still green (no regressions in VfsTree tests if present).
</verification>

<success_criteria>
Status bar reflects the live mesh; existing sketch-008 StatusBar coverage is intact; VfsTree non-override label reads as legible across all 5 themes (manual-only check per VALIDATION.md).
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-03-SUMMARY.md` when done
</output>
</output>
