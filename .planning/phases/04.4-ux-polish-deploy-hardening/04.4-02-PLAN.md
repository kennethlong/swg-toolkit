---
phase: 04.4-ux-polish-deploy-hardening
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/services/logService.ts
  - packages/renderer/src/services/logService.test.ts
  - packages/renderer/src/state/logStore.ts
  - packages/renderer/src/panels/ConsolePanel.tsx
  - packages/renderer/src/panels/LogPanel.tsx
autonomous: true
requirements: [inapp-console-log-tabs-inactive]

must_haves:
  truths:
    - "The maintainer can read live console.* output in the in-app Console tab without opening DevTools (D-12)"
    - "The maintainer can read structured app events (mounts, deploys, reconciles, injections, restores) in the Log tab, color-coded by level (D-12)"
    - "The ring buffer caps at ~2000 entries and never grows unbounded (D-15)"
  artifacts:
    - path: "packages/renderer/src/services/logService.ts"
      provides: "installConsoleCapture() + log(level, channel, message) structured-event API"
    - path: "packages/renderer/src/state/logStore.ts"
      provides: "Ring-buffer Zustand store, ~2k cap, append/clear"
  key_links:
    - from: "ConsolePanel.tsx"
      to: "logStore entries filtered channel==='console'"
      via: "useLogStore((s) => s.entries.filter(...))"
      pattern: "useLogStore"
    - from: "LogPanel.tsx"
      to: "logStore entries filtered channel==='log'"
      via: "useLogStore((s) => s.entries.filter(...))"
      pattern: "useLogStore"
---

<objective>
Build the core logging service + ring-buffer store, and wire the ALREADY-REGISTERED (per RESEARCH.md — this is confirmed, not a stub-selection bug) Console/Log dockview panels to real data. Console = mirrored `console.*` + window errors (D-12 "dev stream"); Log = structured app events with levels (D-12 "structured app events"). D-15 keeps the feature set lean: ring buffer, autoscroll-with-pause, level color, Clear, copy-line — no search/filter yet.

Purpose: let the maintainer self-diagnose without Ctrl+Shift+I (the todo's explicit bar: "I never need DevTools for basic diagnosis").
Output: `logService.ts`, `logStore.ts`, real `ConsolePanel.tsx`/`LogPanel.tsx` content (Console/Log tab CONTENT changes only — dockview registration is untouched, per PATTERNS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/todos/pending/inapp-console-log-tabs-inactive.md

<interfaces>
From packages/renderer/src/state/viewportStore.ts (Zustand create&lt;T&gt;() pattern to mirror):
  export const useX = create&lt;XStore&gt;((set) => ({ ...state, action: (arg) => set({...}) }))

Current ConsolePanel.tsx / LogPanel.tsx (both stub content, already registered in WorkspaceShell.tsx
panelComponents map as ids 'console'/'log' — NOTHING to change there, only the two panel files' bodies):
  ConsolePanel: hardcoded `consoleSeedLines` array, no store.
  LogPanel: one hardcoded `&lt;div&gt;` line, no store.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): logStore + logService tests</name>
  <files>packages/renderer/src/services/logService.test.ts</files>
  <read_first>
    packages/renderer/src/state/viewportStore.ts
    packages/renderer/src/services/changesetService.test.ts (if present — vitest structure convention for this package)
  </read_first>
  <behavior>
    - logStore: `append(entry)` pushes a `LogEntry {ts, level, channel, message}`; after 2001 appends, `entries.length === 2000` (oldest dropped, FIFO cap via `.slice(-2000)`).
    - logStore: `clear()` empties `entries`.
    - logService.installConsoleCapture(): after calling it once, invoking `console.log('x')` / `console.warn('y')` / `console.error('z')` results in three new logStore entries with `channel: 'console'` and levels `'info'|'warn'|'error'` respectively, AND the original `console.*` still emits to the real console (capture wraps, does not silence).
    - logService.installConsoleCapture() is idempotent — calling it twice does not double-wrap (assert only ONE new entry appears per console.log call after a second install() call).
    - logService.log(level, channel, message): appends a structured entry with `channel: 'log'` for app-event emission (used by plan 11's instrumentation).
    - window 'error' and 'unhandledrejection' events (dispatched synthetically in the test) produce `channel:'console', level:'error'` entries once installConsoleCapture() has run.
  </behavior>
  <action>
    Create logService.test.ts importing (not yet existing) `installConsoleCapture`, `log` from './logService' and `useLogStore` from '../state/logStore'. Write the behavior cases above as vitest `it()` blocks (use `vi.spyOn(console, 'log')` etc. to assert the ORIGINAL console.* is still invoked, and dispatch real `ErrorEvent`/`PromiseRejectionEvent` via `window.dispatchEvent` for the window-error case). Run and confirm RED (module not found).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run logService.test.ts` fails RED (module does not exist).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement logStore.ts + logService.ts</name>
  <files>packages/renderer/src/state/logStore.ts, packages/renderer/src/services/logService.ts</files>
  <read_first>
    packages/renderer/src/state/viewportStore.ts
    packages/renderer/src/services/logService.test.ts (from Task 1)
  </read_first>
  <action>
    Implement `logStore.ts`: `interface LogEntry { ts: number; level: 'info'|'warn'|'error'; channel: 'console'|'log'; message: string }`; `useLogStore = create&lt;{ entries: LogEntry[]; append: (e: Omit&lt;LogEntry,'ts'&gt;) => void; clear: () => void }&gt;((set) => ({ entries: [], append: (e) => set((s) => ({ entries: [...s.entries, { ...e, ts: Date.now() }].slice(-2000) })), clear: () => set({ entries: [] }) }))`.
    Implement `logService.ts`: module-scope `let installed = false;` guard. `installConsoleCapture()`: if `installed` return; set `installed = true`; for each of `log`/`warn`/`error`, save the original `console[method]`, reassign `console[method] = (...args) => { original(...args); useLogStore.getState().append({ level: methodToLevel(method), channel: 'console', message: args.map(String).join(' ') }); }`. Also attach `window.addEventListener('error', (e) => useLogStore.getState().append({ level: 'error', channel: 'console', message: e.message }))` and `window.addEventListener('unhandledrejection', (e) => useLogStore.getState().append({ level: 'error', channel: 'console', message: String(e.reason) }))`. Export `log(level, channel, message): void` calling `useLogStore.getState().append({ level, channel, message })` directly (used by structured app-event call sites in plan 11 and, for delete-flow events, plan 10).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run logService.test.ts` all cases GREEN.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire ConsolePanel.tsx and LogPanel.tsx to the real store</name>
  <files>packages/renderer/src/panels/ConsolePanel.tsx, packages/renderer/src/panels/LogPanel.tsx</files>
  <read_first>
    packages/renderer/src/panels/ConsolePanel.tsx (current stub — replace consoleSeedLines with a live subscription, keep the existing outer div style block)
    packages/renderer/src/panels/LogPanel.tsx (current stub)
    packages/renderer/src/state/logStore.ts (from Task 2)
  </read_first>
  <action>
    ConsolePanel.tsx: subscribe `const entries = useLogStore((s) => s.entries.filter((e) => e.channel === 'console'))`; call `installConsoleCapture()` once via a `useEffect(() => { installConsoleCapture(); }, [])` at the top of the component (idempotent per Task 2, safe to call from every panel mount); render each entry with level-based color (`info` → `--color-text-muted`, `warn` → `--color-warn`, `error` → `--color-danger`), a monospace timestamp, autoscroll-to-bottom UNLESS the user has scrolled up (D-15 "pause-on-scroll-up" — track a `containerRef` and a `stickToBottom` boolean toggled by a scroll listener comparing `scrollTop+clientHeight` to `scrollHeight`), a Clear button calling `useLogStore.getState().clear()`, and a copy-line affordance (click a row to `navigator.clipboard.writeText(line)` — Path B renderer has full web-API access). LogPanel.tsx: identical structure but `filter((e) => e.channel === 'log')`, no `installConsoleCapture()` call (Console panel owns that single call site — LogPanel only reads).
  </action>
  <acceptance_criteria>
    - Component test or manual check: after `console.error('test')` fires anywhere in the app, ConsolePanel (when mounted) shows a new red-colored row containing "test".
    - Clear button in both panels empties the SAME shared store (clearing from one panel also empties the other, since both read `logStore.entries` filtered — confirms single source of truth).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console.* wrap → in-app UI | Application-internal log lines rendered in-app; no external/network input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-05 | Denial of Service | Unbounded log growth freezing the renderer | mitigate | Ring buffer hard-capped at 2000 via `.slice(-2000)` on every append |
| T-04.4-06 | Information Disclosure | Logged messages could include a full filesystem path or client cfg content | accept | Local single-user desktop tool; no remote transmission of log entries |
</threat_model>

<verification>
`pnpm --filter @swg/renderer exec vitest run logService.test.ts` green. Manual: open the app, switch to Console/Log tabs, confirm live entries appear and Clear works.
</verification>

<success_criteria>
Console/Log tabs show real, live, capped, level-colored entries — no more hardcoded seed lines.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-02-SUMMARY.md` when done
</output>
