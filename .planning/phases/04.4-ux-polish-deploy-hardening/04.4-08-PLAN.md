---
phase: 04.4-ux-polish-deploy-hardening
plan: 08
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/services/swgMainServerPush.ts
  - packages/renderer/src/services/swgMainServerPush.test.ts
  - docs/05-server-integration/swg-main-parity.md
autonomous: true
requirements: [product-thesis-shadow-sandbox-and-server-push]

# REVISION NOTE (--reviews pass, 2026-07-03): folds in the 4/4-reviewer MEDIUM cluster on server push.
#   - PATH CONTRACT resolved with a VERIFIED file location (Cursor/Opus flagged the prior plan's
#     `servercommon.cfg` relative-path guess as a hedge — "confirm the exact relative path"). VERIFIED
#     2026-07-03 by direct filesystem check of ../swg-main: `exe/shared/servercommon.cfg` EXISTS at
#     that exact relative path from the repo root. `serverConfig.path` (type 'swgsource-docker') =
#     the swg-main REPO ROOT (contains `exe/shared/servercommon.cfg` at that fixed relative path) —
#     this is now a cited fact, not a guess, and is stated identically in 04.4-12.
#   - PERSISTENCE (Codex HIGH, same finding as 04.4-07): pushSwgMainOverride now also persists its
#     record to `studioDir/serverPush.swgmain.json` so Reset survives an app restart.
#   - LIVENESS PRE-CHECK (Sonnet MEDIUM, same finding as 04.4-07): clear, named error if
#     servercommon.cfg is missing/unreadable at the resolved path.

# REVISION NOTE (mount-research pass, 2026-07-03 — see RESEARCH.md ADDENDUM "Server-Push Mounts &
# Reload Semantics"): folds in MEASURED ground truth + the completed searchPath source trace.
#   1. THE RUNNING SERVER IS A VIRTUALBOX VM, NOT DOCKER: the maintainer's swg-main runs in the
#      official SWGSource v3.0.2 VirtualBox VM (openSUSE, bridged NIC). The 'swgsource-docker'
#      serverConfig type literal stays (shipped contract) but is historical labeling. The LIVE
#      server tree is host-visible at `\\<vm-ip>\SWG\swg-main` via the VM's Samba share — VERIFIED
#      this session (default documented creds swg/swg): exe\shared\servercommon.cfg and all
#      data\sku.0 searchPath dirs are directly writable from Windows, and the live cfg is
#      line-identical to the D:\Code\swg-main checkout's. Users need a one-time SMB credential
#      (`cmdkey /add:<vm-ip> /user:swg ...` or `net use`) — the docs task's walkthrough covers it.
#      `serverConfig.path` may therefore be a UNC root — path handling must not assume drive letters.
#   2. PRIORITY RULE NOW SOURCE-VERIFIED — TIE-BREAK CORRECTED: higher searchPath<N> = searched
#      first = wins (TreeFile.cpp:285-308 descending sort; TreeFile::open first-match :712-715),
#      but within a tier the EARLIER-declared line wins (insert-after-last-match, :294-296) — the
#      round-1 "tie→later-added" assumption (inherited from client memory) was WRONG for the
#      server. `resolveSwgMainOverrideDir` returns the highest-numbered tier's FIRST-declared
#      value (for the real cfg: data/sku.0/sys.shared/compiled/game/). Task 1's bounded trace is
#      now a CONFIRM-THE-CITED-LINES step, not open research.
#   3. RELOAD SEMANTICS (source-traced): searchPath list is fixed at process start
#      (TreeFile::install, once, per process) and a SearchPath node is a LIVE disk passthrough
#      (TreeFile_SearchNode.cpp:142-150) → pushed loose files serve immediately for
#      not-yet-cached assets; already-loaded data needs console `reloadTable` /
#      `reloadServerTemplate` (broadcast to all game servers) or a per-process restart; NO
#      automatic change detection; new .tre archives can never hot-mount. The searchPath dir must
#      exist at BOOT (SearchPath ctor FATALs) — pushing into an EXISTING tier dir (this plan's
#      design) is correct; do not invent a new searchPath line.

# REVISION NOTE (round 2, --reviews pass, 2026-07-03):
#   - Codex/Cursor HIGH "server path not Windows-writable" concern (the Core3-flavor version of this
#     finding) does not apply to swg-main specifically (this plan's VirtualBox/Samba mount was already
#     ground-truthed in the mount-research pass above, BEFORE the round-2 review snapshot per STATE.md
#     commit 718d742) — re-confirmed in this pass, no further edit needed for this finding.
#   - MEDIUM (record-clear responsibility ambiguity, same cluster as 04.4-07/12) — LOCKED NOW for
#     parity with 04.4-07's now-explicit contract: `resetSwgMainOverride` (a thin wrapper of
#     `resetLoose`) is a PURE "undo the write" function — it does NOT call
#     `clearSwgMainPushRecordFile` itself. Clearing `serverPush.swgmain.json` after a successful
#     reset is EXCLUSIVELY 04.4-12's job (its Reset handler calls `resetSwgMainOverride(record)` THEN
#     `clearSwgMainPushRecordFile(studioDir)`, explicitly, in that order) — stated as a firm contract
#     below (Task 2's action), removing any ambiguity before 04.4-12 wires the caller.
must_haves:
  truths:
    - "A project bound to a serverConfig of type swgsource-docker can push staged loose files into the swg-main server's compiled-data search path with a non-destructive, restorable write (D-22, D-23 — supersedes the Phase-8 deferral for the push mechanism), and the record needed to reset it survives an app restart"
    - "swg-main's [SharedFile] searchPathN priority resolution is quick-verified against real source before the push targets a specific priority tier"
    - "resetSwgMainOverride is a pure undo-the-write function; clearing the persisted serverPush.swgmain.json record file is EXCLUSIVELY the caller's (04.4-12's) job — locked now, matching 04.4-07's identical contract (round-2 fix)"
    - "The push transport implements the D-24 mount ground truth: swg-main's live tree is reached via the VirtualBox VM Samba share, and the live-for-new-assets / reloadTable-reloadServerTemplate reload semantics are surfaced as mandatory post-push UI copy (D-24)"
  artifacts:
    - path: "packages/renderer/src/services/swgMainServerPush.ts"
      provides: "pushSwgMainOverride / resetSwgMainOverride — B3-snapshot loose-file write into the server's compiled-data dir, PLUS readSwgMainPushRecord/clearSwgMainPushRecordFile for cross-restart persistence"
  key_links:
    - from: "swgMainServerPush.ts"
      to: "looseOverrideDeploy.ts"
      via: "reuses the EXACT B3 snapshot-before-overwrite + isVirtualPathSafe/isConfinedToRoot pattern (ported, not reimplemented)"
      pattern: "isConfinedToRoot|isVirtualPathSafe"
    - from: "swgMainServerPush.ts"
      to: "exe/shared/servercommon.cfg"
      via: "serverConfig.path (swg-main REPO ROOT) + the fixed relative path 'exe/shared/servercommon.cfg', VERIFIED to exist at that path"
      pattern: "exe.*shared.*servercommon"
    - from: "swgMainServerPush.ts"
      to: "studioDir/serverPush.swgmain.json"
      via: "atomic persistence of the last push record; resetSwgMainOverride does NOT clear this file itself (round-2 locked contract — 04.4-12 does, matching 04.4-07's identical contract)"
      pattern: "serverPush\\.swgmain\\.json"
---

<objective>
D-22/D-23 (ground-truth AND implement — swg-main flavor, the SECOND server-push mechanism). Verified in RESEARCH.md: swg-main has **NO TRE-archive concept server-side at all** — `exe/shared/servercommon.cfg`'s `[SharedFile] searchPath0/1/2=` reads game data directly from loose compiled source directories. This is architecturally a **server-side `looseOverrideDeploy`**, NOT a TRE push — do not force it into the Core3 Lua-array shape (RESEARCH.md Pitfall 4: one shared "server deploy target" abstraction across both flavors is the wrong design).

**PATH CONTRACT (locked, identical in 04.4-12; VERIFIED 2026-07-03 via a direct filesystem check —
`D:/Code/swg-main/exe/shared/servercommon.cfg` exists at exactly that relative path):** `serverConfig.path`
(type `'swgsource-docker'`) = the swg-main REPO ROOT. The servercommon.cfg path is ALWAYS
`path.join(serverConfig.path, 'exe', 'shared', 'servercommon.cfg')` — this is a cited fact, not a guess;
callers (04.4-12) use this exact join.

**RESET-RECORD-CLEAR CONTRACT (round-2, locked, matching 04.4-07's identical contract):**
`resetSwgMainOverride(record)` undoes the loose-file write only (delegates to `resetLoose`) — it never
touches `serverPush.swgmain.json`. Clearing that persisted record file after a successful reset is
EXCLUSIVELY the CALLER's job (04.4-12's Reset handler calls `resetSwgMainOverride` then
`clearSwgMainPushRecordFile` explicitly, in that order).

Design note (Open Question 2 is now GROUND-TRUTHED — RESEARCH.md ADDENDUM): consume the already-captured `serverConfig.path` as a user-provided absolute path to the swg-main REPO ROOT — but the root the user must provide is the **LIVE server tree, as seen from Windows**: for the official SWGSource VirtualBox VM that is `\\<vm-ip>\SWG\swg-main` via the VM's Samba share (verified writable this session with the documented default creds). The sibling `D:\Code\swg-main` checkout is a source/build tree the RUNNING server does not read — valid for fixture-style testing, wrong as a live push target; the docs walkthrough (Task 2) says this explicitly. `serverConfig.path` may be a UNC root — no drive-letter assumptions anywhere in path handling (verify `isConfinedToRoot` holds for `\\ip\share\...` roots in the tests). **(Round 2: this mount-research ground-truthing already resolves the round-2 review's "server path not Windows-writable" HIGH-cluster concern; re-confirmed, no further change needed for that finding.)**

Docs note (planning revision — addresses cross-plan dependency): this plan's findings go into their OWN file, `docs/05-server-integration/swg-main-parity.md`, kept separate from `docs/05-server-integration/core3-parity.md` (owned by 04.4-07) -- this keeps 04.4-07 and 04.4-08 independently completable in Wave 1 with no shared-file write dependency, and mirrors the "two independent mechanisms, no shared abstraction" architecture decision for the two server flavors.

Purpose: complete D-22's "at least one server flavor" requirement with a genuinely different, correctly-modeled second mechanism (not a forced abstraction).
Output: `swgMainServerPush.ts` (with cross-restart persistence) + a new `docs/05-server-integration/swg-main-parity.md` doc containing the source-trace verification note resolving RESEARCH.md's Open Question 3 (searchPathN priority order).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/todos/pending/product-thesis-shadow-sandbox-and-server-push.md

<interfaces>
From ../swg-main/exe/shared/servercommon.cfg (VERIFIED 2026-07-03 to exist at exactly this relative
path from the swg-main repo root — NO .tre anywhere):
  [SharedFile]
  searchPath2=../../data/sku.0/sys.shared/compiled/game/
  searchPath2=../../data/sku.0/sys.server/compiled/game/
  searchPath1=../../data/sku.0/sys.shared/built/game/
  searchPath1=../../data/sku.0/sys.server/built/game/
  searchPath0=../../data/sku.0/sys.client/compiled/clientdata/
  Note: TWO searchPath2= lines with different values — multiple values stack at the same priority tier
  (same [SharedFile] multi-value-per-key shape the client-side scanSharedFile already parses).

From packages/renderer/src/services/looseOverrideDeploy.ts (the EXACT pattern to port, not reimplement):
  deployLoose(entries: StagingEntry[], overrideDir: string, opts?: {studioDir, priorRecord}): LooseDeployRecord
    — B3 pre-existing-file snapshot before overwrite; isVirtualPathSafe + isConfinedToRoot guards; atomic tmp+rename.
  resetLoose(record: LooseDeployRecord): void — restores preExisted files from snapshot, deletes non-preExisted ones.

From packages/renderer/src/services/pathSafety.ts:
  isVirtualPathSafe(vp), isConfinedToRoot(destAbs, rootAbs)

From packages/renderer/src/services/changesetService.ts:
  flatten(versionId, manifest, studioDir) -> StagingEntry[]   -- reuse to get the entries to push (same source
  the client-side loose-override deploy already uses; server push targets a DIFFERENT destination dir)
  writeManifest's atomic tmp+rename pattern (changesetService.ts:70-76) — mirror for serverPush.swgmain.json.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Verify searchPathN priority + write swgMainServerPush.test.ts against a fixture tree</name>
  <files>packages/renderer/src/services/swgMainServerPush.test.ts</files>
  <read_first>
    ../swg-main/exe/shared/servercommon.cfg
    packages/renderer/src/services/clientLocator.ts (scanSharedFile — the client-side [SharedFile] multi-value parser; confirm it can be reused/extended for the SAME section format on the server cfg, per RESEARCH.md's note that this is the same ConfigFile.cpp-family format)
    packages/renderer/src/services/looseOverrideDeploy.ts (deployLoose/resetLoose to mirror)
  </read_first>
  <behavior>
    - `resolveSwgMainOverrideDir(servercommonCfgPath)` parses `[SharedFile] searchPathN=` lines (reusing or extending `scanSharedFile`'s multi-value-per-key parsing) and returns the HIGHEST-numbered `searchPathN` tier's FIRST-declared value within that tier — the SOURCE-VERIFIED server rule (mount-research pass, 2026-07-03): priority DESC confirmed (`searchNodePriorityOrder` sorts descending, TreeFile.cpp:285-308; `TreeFile::open` takes the first hit, :712-715), tie within a tier → EARLIER-added wins (insert-after-last-match comment, :294-296). NOTE this tie-break CORRECTS the client-derived "tie→later-added" assumption; for the real cfg the winner is `../../data/sku.0/sys.shared/compiled/game/`. The test asserts the fixture's first-declared searchPath2 value wins.
    - Given a fixture `servercommon.cfg` (the exact content shown in this plan's context block) and a tmp directory tree mirroring the relative `../../data/sku.0/...` paths, `pushSwgMainOverride(servercommonCfgPath, studioDir, activeVersionId, manifest)` writes staged entries into the resolved highest-priority dir, snapshotting any pre-existing files (B3) exactly like `deployLoose`, AND persists the returned record to `studioDir/serverPush.swgmain.json` (atomic tmp+rename).
    - `readSwgMainPushRecord(studioDir)` returns the persisted record after a push, `null` if none (never throws).
    - `resetSwgMainOverride(record)` restores pre-existing files and removes toolkit-written ones — byte-for-byte identical to `resetLoose`'s contract (same record shape family). **(Round 2 — locked contract) `resetSwgMainOverride` does NOT call `clearSwgMainPushRecordFile` itself** — assert `serverPush.swgmain.json` STILL EXISTS on disk immediately after `resetSwgMainOverride(record)` returns; a separate, explicit call to `clearSwgMainPushRecordFile(studioDir)` is required to remove it.
    - A staged entry with `action: 'delete'` is recorded in `skippedDeletes` (cannot tombstone base files via loose override — same limitation as the client-side loose model) rather than throwing.
    - Given a `servercommonCfgPath` that does not exist, `pushSwgMainOverride` throws a clearly-named error identifying the missing path (e.g. `"swg-main servercommon.cfg not found at <path> — confirm serverConfig.path points at the swg-main repo root"`) rather than a raw ENOENT.
  </behavior>
  <action>
    The source trace is ALREADY DONE (mount-research pass, 2026-07-03 — RESEARCH.md ADDENDUM): the resolution rule lives in `D:\Code\swg-main\src\engine\shared\library\sharedFile\src\shared\TreeFile.cpp` — install-once at :84-149, descending priority sort at :285-308, first-match-wins walk at :712-715, tie→earlier-added at :294-296. Your task is CONFIRM-THE-CITED-LINES (open the file, verify each cited behavior is really there — do not re-derive from scratch, and do NOT regress to the old "tie→later-added" client assumption), then record the confirmation as a code comment at the top of the test file citing those file:line locations. Then write `swgMainServerPush.test.ts` covering the behaviors above (including the round-2 reset-vs-clear-record contract) against an isolated tmp fixture tree (never touching the real `../swg-main` checkout's actual data). Run and confirm RED.
  </action>
  <acceptance_criteria>
    - Test file exists with a source-trace citation comment (file:line or explicit "not present in checkout" note) resolving Open Question 3.
    - `pnpm --filter @swg/renderer exec vitest run swgMainServerPush.test.ts` fails RED (module not found).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement swgMainServerPush.ts</name>
  <files>packages/renderer/src/services/swgMainServerPush.ts, docs/05-server-integration/swg-main-parity.md</files>
  <read_first>
    packages/renderer/src/services/looseOverrideDeploy.ts (port this pattern near-verbatim)
    packages/renderer/src/services/clientLocator.ts (scanSharedFile — reuse for parsing servercommon.cfg's [SharedFile] section)
    packages/renderer/src/services/changesetService.ts (writeManifest atomic tmp+rename pattern — mirror for serverPush.swgmain.json)
    packages/renderer/src/services/swgMainServerPush.test.ts (from Task 1)
  </read_first>
  <action>
    Implement `resolveSwgMainOverrideDir(servercommonCfgPath: string): string | null` — verify the file exists first (liveness pre-check — throw the clearly-named error from Task 2's behavior list if not), then parse the `[SharedFile]` section (reuse `scanSharedFile`'s parsing logic if its shape is directly reusable for this cfg format, or a small dedicated parser if the section header/key format differs enough to warrant one — prefer reuse per Don't-Hand-Roll) and resolve to the winning highest-priority `searchPathN` directory per the Task 1 trace finding, resolved to an ABSOLUTE path relative to `path.dirname(servercommonCfgPath)`. Implement `pushSwgMainOverride(servercommonCfgPath, studioDir, activeVersionId, manifest): LooseDeployRecord` — call `flatten` to get entries, then port `deployLoose`'s body (B3 snapshot + isVirtualPathSafe/isConfinedToRoot guards + atomic tmp+rename + skippedDeletes) targeting `resolveSwgMainOverrideDir(...)` as the destination root instead of a client searchPath dir; after building the record, persist it via the SAME atomic tmp+rename pattern as `changesetService.writeManifest` to `path.join(studioDir, 'serverPush.swgmain.json')`. Implement `readSwgMainPushRecord(studioDir: string): LooseDeployRecord | null` (never-throws, mirrors `readManifest`'s defensive-parse style). Implement `resetSwgMainOverride(record: LooseDeployRecord): void` as a direct re-export or thin wrapper of `resetLoose` (the record shape and restore semantics are IDENTICAL — do not duplicate the restore logic, import and call `resetLoose` from `looseOverrideDeploy.ts`). **`resetSwgMainOverride` MUST NOT touch `serverPush.swgmain.json` — it is a PURE "undo the write" function** (round-2 locked contract, matching 04.4-07's identical `resetCore3TreOverride` contract). Implement `clearSwgMainPushRecordFile(studioDir: string): void` (best-effort unlink, never throws) as a SEPARATE export — `resetSwgMainOverride` never calls it; 04.4-12's Reset handler is the ONLY caller that invokes both, explicitly, in order. Write the Task 1 source-trace finding as a dated `## Verified: swg-main Server Push (2026-07-03)` section in a NEW file `docs/05-server-integration/swg-main-parity.md` -- independent of `core3-parity.md` (owned by 04.4-07 for the Core3 flavor; keeping the two server flavors' docs in separate files matches the "two independent mechanisms, no shared abstraction" decision and removes the same-wave same-file dependency between 04.4-07 and 04.4-08) -- confirming or correcting the searchPathN priority assumption for swg-main specifically (priority DESC + tie→earlier-declared, with the TreeFile.cpp citations), plus a brief description of the pushSwgMainOverride/resetSwgMainOverride mechanism AND the verified `exe/shared/servercommon.cfg` relative-path fact for that doc's own record. The doc MUST also include (from RESEARCH.md ADDENDUM): (i) a "Mounting the live swg-main (VirtualBox VM) from Windows" walkthrough — the official SWGSource VM exports a Samba share `SWG` whose `swg-main` subtree is the LIVE server tree (`\\<vm-ip>\SWG\swg-main`, default documented creds `swg`/`swg`; one-time `cmdkey /add:<vm-ip> /user:swg ...` or `net use` so Node fs can write the UNC; bridged-DHCP IP can drift — re-check the VM's IP if the share stops resolving; the `D:\Code\swg-main` checkout is a source/build tree, NOT the running server's data), and (ii) the RELOAD SEMANTICS a pusher must know — searchPaths are installed once per process at start; a loose SearchPath is a live disk passthrough so NEW/never-loaded assets serve immediately; already-loaded data needs console `reloadTable`/`reloadServerTemplate` (broadcast to all game servers) or a per-process restart; there is NO automatic change detection; `.tre` archives never hot-mount; a searchPath directory must exist at boot.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run swgMainServerPush.test.ts` all Task 1 cases GREEN.
    - `grep -c "resetLoose" packages/renderer/src/services/swgMainServerPush.ts` >= 1 (confirms reuse, not reimplementation, of the restore logic).
    - `grep -c "serverPush.swgmain.json" packages/renderer/src/services/swgMainServerPush.ts` >= 1 (confirms cross-restart persistence is wired).
    - `grep -n "clearSwgMainPushRecordFile" packages/renderer/src/services/swgMainServerPush.ts` shows it declared as a STANDALONE export, never called from inside `resetSwgMainOverride`'s own body.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolkit (Windows renderer) → external swg-main server data directory | Second deploy target outside the toolkit's own app-data root / bound client install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-15 | Tampering | Path traversal via a staged virtualPath escaping the resolved override dir | mitigate | Same `isVirtualPathSafe` + `isConfinedToRoot` guards ported verbatim from `looseOverrideDeploy.ts` |
| T-04.4-16 | Tampering | Writing to the wrong swg-main checkout / stale data root | accept | User-provided `serverConfig.path` (007 wizard) — same trust level as `clientPath`, no auto-discovery. A minimal liveness check (servercommon.cfg must exist, Task 2) catches the "pointed at nothing" case; confirming the pointed-at server is the RUNNING instance is covered by 04.4-15's real-server checkpoint (extracted from 04.4-12 in round 2). |
</threat_model>

<verification>
`pnpm --filter @swg/renderer exec vitest run swgMainServerPush.test.ts` green.
</verification>

<success_criteria>
swg-main server push works against a fixture servercommon.cfg tree, restore is byte-exact, the priority-order assumption is either confirmed or corrected with cited evidence, the push record survives an app restart via serverPush.swgmain.json, and the reset-vs-clear-record contract is locked and unambiguous (round-2, matching 04.4-07).
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-08-SUMMARY.md` when done
</output>
