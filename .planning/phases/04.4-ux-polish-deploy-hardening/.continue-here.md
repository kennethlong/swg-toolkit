---
context: phase
phase: 04.4-ux-polish-deploy-hardening
task: "plans 13/15 complete; 04.4-04 paused at task 3/3 (checkpoint); 04.4-15 not started (pure checkpoint plan)"
total_tasks: 15
status: blocked_on_human_checkpoints
last_updated: 2026-07-04T12:35:54.711Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure (or a live near-miss).
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: do-not-auto-approve-mandated-checkpoints — `workflow.auto_advance` is `true` in `.planning/config.json`, and execute-phase's auto-mode rule would auto-approve `human-verify` checkpoints. The two remaining gates (04.4-04 D-17 visual-vs-SIE; 04.4-15 D-22 real-server round-trip) are maintainer-MANDATED gates that exist precisely because automation cannot perform them. Structural mitigation: present both to the maintainer and wait; never pass `"approved"` to a continuation agent without a literal maintainer reply.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| auto-approve-of-mandated-human-gates | AUTO_MODE=true (persistent `auto_advance` pref, chain flag clear) would have rubber-stamped the D-17/D-22 checkpoints this session; orchestrator deliberately withheld auto-approval | blocking | Only resolve 04.4-04 task 3 and 04.4-15 task 1 from a literal maintainer response in chat; record which outcome/flavor in the SUMMARYs |
| root-vitest-env-mismatch | Renderer `.test.ts` files that touch `window`/DOM pass under `packages/renderer/vitest.config.ts` (jsdom) but FAIL under root `vitest.config.ts` (`environment: node`, include `packages/*/src/**/*.test.ts`). Manifested as 7 logService.test.ts failures at the wave-1 post-merge gate; fixed in `d80f7be` | advisory | Any new renderer `.test.ts` needing DOM gets a `@vitest-environment jsdom` docblock; always run root `npx vitest run` before declaring green. (`.test.tsx` files are safe — root include only matches `.test.ts`) |
| research-claim-backwards | 04.4-RESEARCH.md ADDENDUM's swg-main tie-break claim ("earlier-declared wins") was falsified by 04.4-08's mandatory re-derivation: `TreeFile.cpp` `lower_bound`+`insert` puts same-priority nodes BEFORE existing → LAST-declared wins | advisory | Ground-truth re-verification of cited lines stays mandatory before implementing from research docs (CLAUDE.md rule); auto-memory already corrected |

<current_state>
`/gsd-execute-phase 04.4` run, sequential mode (worktrees disabled in config). Waves 1 and 2 fully complete with post-merge build+test gates green (wave-1 gate caught and fixed a cross-plan jsdom env conflict, commit `d80f7be`). Wave 3: 04.4-14 complete; **04.4-04 paused at its task-3 D-17 checkpoint** (code tasks committed: `1e92aa8` RED, `ca9b419` GREEN extract of shared `orientation.ts`, `cab93e4` identity pin; SUMMARY.md deliberately NOT written — plan incomplete until checkpoint resolves); **04.4-15 not started** — it is a single `checkpoint:human-verify` task with `files_modified: []`, presented directly to the maintainer without spawning an executor. Both checkpoints were presented in chat; the session ended awaiting the maintainer's replies. Working tree clean; every completed plan has SUMMARY.md + STATE/ROADMAP updates (executors ran sequentially and self-updated tracking).
</current_state>

<completed_work>

Completed plans (all committed, SUMMARY.md present, spot-checked):
- 04.4-01: delete/restore service layer — `deleteProject.ts`, shared `deploymentReset.ts`, `deleteUndoStore.ts`, crash-safe trash markers, startup purge (`441f459`)
- 04.4-02: `logService` + `logStore` ring buffer (2000 cap); Console/Log panels live (`57b3a8e`)
- 04.4-03: StatusBar mesh chip live-wired; VfsTree winning-archive label legibility (`087c871`)
- 04.4-05: EFT FORM STAG fixed-function parsing; real (local-gated) + synthetic (CI) fixtures (`cb27887`)
- 04.4-06: TreCodec pluggable compression seam, payload + TOC/name-block (`70ad896`)
- 04.4-07: Core3 server-push service (WSL2 UNC, cross-restart record) + close-out audit + verified `core3-parity.md` section (`6c3c4f2`)
- 04.4-08: swg-main loose-override push service — with corrected last-declared-wins tie-break (`37af2ef`)
- 04.4-09: E2E infra — CI vite-builds-before-Playwright, SWG_TEST_MODE hook surface (`testHooks.ts`), cfg-model + loose-override fixture trees (`b1d44d3`)
- 04.4-10: delete UI, sketch 017 Variant B, all 17(+1) elements, 21 component tests (`303ffcb`)
- 04.4-11: main-process log IPC forwarding + deploy/reconcile/mount instrumentation (`499503a`)
- 04.4-12: Server Push section in VcsPanel, per-flavor dispatch, reset-then-clear-record contract (`f7e30e2`)
- 04.4-13: `e2e/07-deploy-flow.spec.ts` (13/13) — fixed 4 REAL product bugs along the way: testHooks never installing in real builds (additionalArguments fix), vite `process.env` stubs breaking LOCALAPPDATA resolution app-wide, ConsolePanel/LogPanel Zustand-selector infinite-loop blank-page boot crash, `syncLiveToVersion` wrong-cfg revert + loose-override fallback (`038fc12`)
- 04.4-14: `e2e/06-delete-flow.spec.ts` (14/14), CI-registered (`036e08f`)
- 04.4-04 tasks 1-2: shared `orientation.ts` (identity, history documented), both mesh views import it, 350/350 renderer tests green
</completed_work>

<remaining_work>

- 04.4-04 task 3: D-17 maintainer visual verify vs SIE → then a CONTINUATION agent (fresh, not resume; see next_action) records the outcome, writes `04.4-04-SUMMARY.md`, updates STATE/ROADMAP
- 04.4-15 task 1: D-22 real-server push→reset round-trip (one flavor) → then write `04.4-15-SUMMARY.md` naming the flavor + STATE/ROADMAP (no code changes; no executor needed)
- Phase close-out chain (execute-phase workflow, in order): `code_review_gate` (Skill gsd-code-review "04.4") → `close_parent_artifacts` (04.4 is a decimal phase — resolve parent phase 04 UAT gaps/debug sessions if present) → `regression_gate` → `schema_drift_gate` / `codebase_drift_gate` → `verify_phase_goal` (gsd-verifier agent, model sonnet) → `update_roadmap` (`gsd-sdk query phase.complete 04.4`) → offer_next
</remaining_work>

<decisions_made>

- Withheld auto-approval of D-17/D-22 checkpoints despite `auto_advance=true` — mandated gates need literal maintainer replies
- swg-main same-priority `searchPathN` tie-break corrected to last-declared-wins (04.4-08 re-derivation; RESEARCH ADDENDUM was backwards; auto-memory updated)
- `@vitest-environment jsdom` docblock is the fix pattern for renderer `.test.ts` DOM tests under the root node-env config
- 04.4-04 kept `SWG_ORIENTATION` identity rather than forcing an untested rotation guess (plan-sanctioned; camera-azimuth-only discrepancy counts as approved outcome and satisfies ROADMAP SC #5, superseding D-16's mechanism claim)
- Post-merge gate build command for this repo = the three vite builds (`vite.main` / `vite.preload` / `vite.renderer` configs), NOT `npm run build` (electron-forge make has no win32 make targets configured — pre-existing)
</decisions_made>

<blockers>
- 04.4-04 task 3: awaiting maintainer visual comparison (blocking, human-only)
- 04.4-15 task 1: awaiting maintainer real-server round-trip (blocking, human-only; needs live Core3 WSL2 or swg-main VM)
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable version of this state
2. `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-04-PLAN.md` — task 3 checkpoint spec + the two valid approve outcomes
3. `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-15-PLAN.md` — full how-to-verify steps (real server paths, reload semantics, record-file check)
4. `.planning/phases/04.4-ux-polish-deploy-hardening/deferred-items.md` — e2e/04-workspace pre-existing failures (likely root-caused by 04.4-13's Zustand fix — worth a re-run to confirm they're gone), NewProjectWizard layout-override UX gap, flaky root-suite note
5. `C:\Users\kenne\.claude\get-shit-done\workflows\execute-phase.md` — close-out chain steps (code_review_gate onward) and checkpoint continuation protocol (fresh agent + completed-tasks table, NOT resume)

## Infrastructure State
- Working tree: clean; branch `main`; branching_strategy none (commits go straight to main)
- Worktrees: disabled (`workflow.use_worktrees=false`) — sequential executor mode; executors self-update STATE/ROADMAP
- Vite bundles in `.vite/` are built (needed for Playwright); rebuild with the three `npx vite build -c vite.*.config.ts` if stale
- No servers/watchers left running by this session. Core3 (WSL2 Debian) and swg-main (VirtualBox VM) were NOT started — the 04.4-15 checkpoint needs the maintainer to start one
- Root test suite: 445 passed / 2 skipped (57 files) green at pause time; renderer suite 350/350

<context>
Orchestration model: `/gsd-execute-phase 04.4`, executor model sonnet, one gsd-executor subagent per plan run sequentially with spot-checks (SUMMARY exists, commits grep, no Self-Check: FAILED) after each. Per-wave post-merge gate = three vite builds + root `npm test`. The phase's two `autonomous: false` plans were deliberately sequenced last (04.4-04 stopped at its checkpoint with SUMMARY withheld; 04.4-15 presented straight from its PLAN.md). Both checkpoint prompts, with full verify steps, were already presented to the maintainer in the paused session — re-present them on resume if unanswered.
</context>

<next_action>
Start with: re-present the two checkpoints to the maintainer (04.4-04 D-17 visual vs SIE; 04.4-15 D-22 real-server round-trip — full steps in the two PLAN.md files). When 04.4-04 gets "approved" (+ which outcome), spawn a fresh CONTINUATION gsd-executor for plan 04.4-04 (per checkpoints/continuation protocol) with: completed-tasks table (1 RED `1e92aa8`, 1 GREEN `ca9b419`, 2 `cab93e4`), resume at task 3 with the user's response, instructions to record the outcome (incl. SC #5/D-16 reconciliation language if camera-azimuth-only), write 04.4-04-SUMMARY.md, update STATE/ROADMAP. When 04.4-15 gets "approved" (+ flavor), write 04.4-15-SUMMARY.md directly (no code). Then run the execute-phase close-out chain: code_review_gate → close_parent_artifacts (parent 04 UAT) → regression_gate → drift gates → verify_phase_goal → update_roadmap.
</next_action>
