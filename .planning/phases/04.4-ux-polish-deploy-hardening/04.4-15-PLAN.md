---
phase: 04.4-ux-polish-deploy-hardening
plan: 15
type: execute
wave: 3
depends_on: ["04.4-12"]
files_modified: []
autonomous: false
requirements: [product-thesis-shadow-sandbox-and-server-push]

# NEW PLAN (round 2, --reviews pass, 2026-07-03): extracted from 04.4-12's round-1 Task 3. Opus HIGH,
# verified against this project's wave-execution model: a blocking `autonomous:false` checkpoint
# sitting in Wave 2 stalls the Wave 2→3 barrier, delaying 04.4-14 (fully autonomous, Wave 3) starting
# until a maintainer manually approves a real-server round-trip 04.4-14 has no actual dependency on —
# the exact anti-pattern the roadmap's own Wave-1→Wave-3 move for 04.4-04 exists to avoid. This plan
# mirrors that same fix: the checkpoint moves to its own tail-wave plan, `depends_on: ["04.4-12"]`
# (needs the UI 04.4-12 built), sharing Wave 3 with 04, 14 (no file overlap with either), so neither is
# blocked waiting on a maintainer's real-server availability. `files_modified: []` because this plan
# makes NO code changes — it is purely a verification gate over what 04.4-07/08/12 already shipped.
must_haves:
  truths:
    - "The push→reset round-trip is confirmed AT LEAST ONCE against a real running Core3 or swg-main instance (not only a fixture tree) — D-22's 'working deploy target' bar"
    - "This checkpoint does not block 04.4-14 (Wave 3, no dependency on this plan) or 04.4-04 (Wave 3, no dependency on this plan) from starting — it shares the tail wave rather than gating the Wave 2→3 barrier (round-2 fix)"
  artifacts: []
  key_links:
    - from: "04.4-15 (this plan)"
      to: "04.4-12's VcsPanel Server Push UI"
      via: "depends_on — this plan verifies what 04.4-12 already built, makes no code changes itself"
      pattern: "N/A"
---

<objective>
D-22's "ship a WORKING server deploy target" bar, closed: a manual, blocking checkpoint that loads a project's pushed override into a REAL running Core3 or swg-main instance (not only 04.4-07/08's synthetic fixture trees, and not only 04.4-12's UI wired against those fixtures) and confirms it takes effect, then Resets and confirms it is gone. Mirrors the D-17 discipline already applied to the 04.4-04 viewport fix.

**Round-2 rationale (why this is its own plan, not folded into 04.4-12):** see 04.4-12's round-2 revision note. In short: a blocking checkpoint in Wave 2 would stall the Wave 2→3 barrier and delay 04.4-14 (fully autonomous) from starting on a maintainer-availability constraint it has no real dependency on. Extracting it into this Wave-3 plan (alongside 04 and 14, no file overlap with either) removes that stall while still mandating the real-server proof before the PHASE is considered closed.

Purpose: 04.4-07/08's automated tests and 04.4-12's UI wiring only prove the mechanism against synthetic fixture trees — this is the one and only point in the phase where a REAL server is required to call D-22 done.
Output: a maintainer-confirmed real-server push→reset round-trip (no code artifacts — this plan is a verification gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-07-PLAN.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-08-PLAN.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-12-PLAN.md

<interfaces>
From packages/renderer/src/panels/deploy/VcsPanel.tsx (04.4-12 — the Server Push section this
checkpoint exercises; no further edits made by this plan):
  Push to Server / Reset buttons, dispatching on serverConfig.type, disk-backed record rehydration.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real-server round-trip confirmation (D-22's "working deploy target" bar)</name>
  <files>N/A — verification only, no code changes</files>
  <action>Pause for human verification. 04.4-07/08's automated tests and 04.4-12's Tasks 1-2 only prove the push/reset mechanism against SYNTHETIC FIXTURE trees — never against an actually-running Core3 or swg-main instance. D-22 requires a "working" server deploy target, not only a file-write mechanism that looks correct. This checkpoint closes that gap for at least one server flavor, mirroring the D-17 discipline already applied to the viewport fix (04.4-04). This plan makes no code changes of its own — it is a pure verification gate over 04.4-07/08/12's already-shipped work.</action>
  <what-built>Nothing new — this checkpoint verifies the UI-reachable Push/Reset flow 04.4-12 built (for whichever server flavor, Core3 or swg-main, the maintainer has running), targeting the real `serverConfig.path` the maintainer's project is bound to.</what-built>
  <how-to-verify>
    1. Bind a project's `serverConfig` to a REAL server instance using the VERIFIED live paths (RESEARCH.md ADDENDUM — NOT the D:\Code source checkouts):
       - Core3: `serverConfig.path = \\wsl.localhost\Debian\home\kenny\workspace\Core3\MMOCoreORB\bin`. Start the server per the runbook (`D:\Code\Core3\wsl2\RUNBOOK.md`): `wsl -d Debian`, then `core3-start`; boot takes ~3-5 min.
       - swg-main: `serverConfig.path = \\<vm-ip>\SWG\swg-main` (SWGSource v3.0.2 VirtualBox VM must be running; check its current bridged IP — was 192.168.1.200 — and ensure a one-time SMB credential exists: `cmdkey /add:<vm-ip> /user:swg /pass:...`).
    2. Stage a small, easily-observable change (e.g. a distinctive datatable/string edit) and save a version.
    3. Click "Push to Server" in the VcsPanel's Server Push section (04.4-12), and confirm the per-flavor next-step guidance copy appears.
    4. Confirm the override takes effect on the RUNNING server, following that flavor's verified reload semantics:
       - Core3: RESTART IS REQUIRED (TreFiles is read once at boot) — `core3-stop` then `core3-start` (interactive terminal; may prompt for sudo per the runbook), then observe the change in-game or via server tooling. Do NOT expect a hot pickup.
       - swg-main: a never-loaded asset should serve immediately; an already-loaded datatable needs the server console's `reloadTable <name>` (or `reloadServerTemplate <name>` for templates), or a game-server restart.
    5. Click "Reset" and confirm the override is gone (the server's original data resolution order is restored) — for Core3 this again requires a restart to observe; verify against the RUNNING instance, not just the file's absence on disk. Also confirm `serverPush.{core3,swgmain}.json` is gone from `studioDir` after Reset (proves 04.4-12's explicit clear-record call actually ran).
  </how-to-verify>
  <resume-signal>Type "approved" once the push→reset round-trip is confirmed against a real running server instance (name which flavor was tested), or describe what failed to iterate on the push/reset service before closing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's Windows host → live Core3 (WSL2 UNC) / swg-main (VM Samba UNC) | Same boundary as 04.4-07/08/12; this plan adds no new I/O, only a real-instance confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-21b | Repudiation | The push→reset mechanism "looks correct" against fixtures but silently fails against a real server (e.g. wrong reload assumption, permission issue on the live UNC/Samba share) | mitigate | This blocking checkpoint is the ONE required real-server proof point before the phase is considered to satisfy D-22 |
</threat_model>

<verification>
Task 1's real-instance checkpoint approved (name the flavor tested in the SUMMARY).
</verification>

<success_criteria>
At least one server flavor's push→reset loop is confirmed against a REAL running instance, closing D-22's "working deploy target" bar without stalling 04.4-14's Wave-3 start (round-2 wave fix).
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-15-SUMMARY.md` when done
</output>
