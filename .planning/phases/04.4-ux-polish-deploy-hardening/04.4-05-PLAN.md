---
phase: 04.4-ux-polish-deploy-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/native-core/modules/core/formats/Effect.cpp
  - packages/native-core/modules/core/formats/Effect.h
  - packages/harness/scripts/extract-effect-fixtures.cjs
  - packages/harness/test/mesh-roundtrip.test.ts
autonomous: true
requirements: [eft-parser-completion]

must_haves:
  truths:
    - "parseEffect extracts sampler role maps for fixed-function (STAG) implementations, not just pixel-shader (PPSH) ones (D-19)"
    - "The a_envmask_specmap.eft CORE-05 byte-exact round-trip gate actually RUNS (not skips) on the maintainer's dev machine"
  artifacts:
    - path: "packages/native-core/modules/core/formats/Effect.cpp"
      provides: "parseStag helper + PASS-forms loop extended to walk FORM STAG siblings of FORM PPSH"
    - path: "packages/harness/scripts/extract-effect-fixtures.cjs"
      provides: "One-time extraction script mirroring extract-ans-fixtures.cjs, populating fixtures-real/effect/"
  key_links:
    - from: "Effect.cpp parseImpl PASS-forms loop"
      to: "FORM STAG (fixed-function texture stage)"
      via: "iff.enterForm/findForm walk parallel to the existing FORM PPSH path"
      pattern: "STAG"
---

<objective>
Close the two documented gaps in the `.eft` (FORM EFCT) shader-effect parser (landed in Phase 02-03, PPSH/PTXM path only):

1. **PTXM sampler→role extraction is ALREADY DONE** (verified 2026-07-03 in `Effect.cpp:135-207` — `parsePtxm`/`parsePpsh` exist and are correct per the todo's own citation). The REAL remaining gap is the **fixed-function `FORM STAG` path** — implementations with no pixel shader (no `FORM PPSH`) currently contribute ZERO samplers; `ShaderImplementationPassStage::load_0000/0001` (`ShaderImplementation.cpp:2430-2560`) shows each `FORM STAG` sibling carries a `m_textureTag` at a fixed field offset, one per fixed-function texture stage.
2. Activate the CORE-05 byte-exact fixture — the round-trip/sampler/blend assertions for `a_envmask_specmap.eft` ALREADY EXIST in `mesh-roundtrip.test.ts:1231-1309` but SKIP because the gitignored real fixture is absent on disk; extract it (and its companion `.sht`) from a real client install.

Purpose: parser-completeness (STAG) + CI-enforcement (fixture activation) for a format that already renders correctly visually (env-reflection works via `.sht` SLOT-tag binding today, independent of this gap).
Output: STAG parsing in `Effect.cpp`/`Effect.h`; a populated `fixtures-real/effect/` dir; the existing test assertions RUNNING (not skipping) locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/todos/pending/eft-parser-completion.md

<interfaces>
From ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/ShaderImplementation.cpp:
  ShaderImplementationPassStage::load_0000(Iff&) — FORM STAG, chunk 0000, sequential uint8/bool8 reads:
    colorOperation(u8), colorArgument0(u8)+complement(bool8)+alphaReplicate(bool8),
    colorArgument1(u8)+complement(bool8)+alphaReplicate(bool8),
    colorArgument2(u8)+complement(bool8)+alphaReplicate(bool8),
    alphaOperation(u8), alphaArgument0(u8)+complement(bool8), alphaArgument1(u8)+complement(bool8),
    alphaArgument2(u8)+complement(bool8), resultArgument(u8),
    THEN m_textureTag = uint32 (the sampler role source — same tag decode as PTXM's textureTag),
    THEN textureCoordinateSetTag(u32), textureAddressU/V/W(u8 each), mip/min/magFilter(u8 each),
    textureCoordinateGeneration(u8). load_0001 (chunk 0001) has the SAME leading field sequence
    before textureTag (confirmed by reading both — verify at implementation time, do not assume).
  ShaderImplementationPass: for a fixed-function pass, `m_fixedFunctionPipeline = new FixedFunctionPipeline(iff, true)`
    is read from the PASS version-form's DATA chunk, and AFTER `iff.exitChunk(TAG_DATA)`, `numberOfStages` FORM STAG
    entries are read as DIRECT SIBLINGS of (not children of) FORM PPSH within the same PASS version-form — i.e. a
    fixed-function PASS has FORM STAG children instead of a FORM PPSH child, at the SAME nesting level.

From packages/native-core/modules/core/formats/Effect.cpp (current — READ, do not re-derive from scratch):
  parsePtxm (lines 137-170) — the PTXM parsing pattern (version-branch dispatch + tagToRoleString) to MIRROR for STAG.
  parsePpsh (lines 171-207) — how PPSH sampler collection loops over PTXM children; the fixed-function branch needs
    an analogous parseStagStages(passVerForm) that loops FORM STAG children directly (no intermediate FORM PPSH/0001
    wrapper — STAG forms are direct PASS-version-form children).
  parseImpl PASS-forms loop (lines 317-340) — currently ONLY calls findForm(passVerForm, "PPSH"); this is the
    exact loop to extend with an else-branch (or additional walk) collecting FORM STAG children when no PPSH exists.
  tagToRoleString — existing helper (used by parsePtxm) that decodes a uint32 tag into an ASCII role string
    ("MAIN", "SPEC", "ENVM", etc.) — REUSE this for STAG's m_textureTag, do not reimplement tag decoding.

From packages/harness/scripts/extract-ans-fixtures.cjs (the exact script pattern to mirror for effects):
  mountTreMount(treFiles, priorities) -> handle; searchMount(handle, {text, mode}) -> entries;
  resolveEntry(handle, path) -> {winner, archiveIndex, entryIndex}; readMountEntry(handle, archiveIndex, entryIndex) -> bytes.

From packages/harness/test/mesh-roundtrip.test.ts:
  loadFixture(relPath) — reads packages/harness/fixtures-real/&lt;relPath&gt;, returns null (test SKIPS) if absent.
  describe('FORM EFCT (.eft) — shader effect (gap-closure 02-03)') at line 1231 — ALREADY has the round-trip +
  sampler-role + blend assertions wired against loadFixture('effect/a_envmask_specmap.eft') — do NOT rewrite these,
  only make the fixture file PRESENT on disk so they stop skipping.
  registerFormat('shader-efct', {...}) at line 686 — already gated on `if (efctBytes)`, no change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FORM STAG fixed-function sampler parsing</name>
  <files>packages/native-core/modules/core/formats/Effect.cpp, packages/native-core/modules/core/formats/Effect.h</files>
  <read_first>
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/ShaderImplementation.cpp (lines 2380-2560 for ShaderImplementationPassStage::load/load_0000/load_0001; lines 995-1120 for how a fixed-function PASS reads FixedFunctionPipeline then numberOfStages FORM STAG entries as direct PASS-version-form children)
    packages/native-core/modules/core/formats/Effect.cpp (lines 130-340 — parsePtxm/parsePpsh pattern + the PASS-forms loop to extend)
    packages/native-core/modules/core/formats/Effect.h (EffectSampler/EffectImpl struct shapes — no new fields needed, STAG samplers populate the SAME `samplers: std::vector&lt;EffectSampler&gt;` field as PTXM)
  </read_first>
  <action>
    Add a `static std::optional&lt;EffectSampler&gt; parseStag(const IffNode& stagForm, const uint8_t* srcData, uint32_t srcSize, int stageIndex)` mirroring `parsePtxm`'s version-branch-dispatch shape (chunk 0000 or 0001 inside the FORM STAG), sequentially reading the leading fields (colorOperation through resultArgument, per the field list in the read_first block) to advance the cursor to `m_textureTag`, then reading `textureTag = uint32` and returning `EffectSampler{ index: static_cast&lt;int8_t&gt;(stageIndex), role: tagToRoleString(textureTag) }` — `stageIndex` is the STAG's 0-based position among its sibling FORM STAG children (the fixed-function pipeline has no separate int8 index field like PTXM does; position IS the hardware stage index). In `parseImpl`'s PASS-forms loop (currently only `findForm(passVerForm, "PPSH")`), add an else-branch: when no `FORM PPSH` is found, iterate ALL direct `FORM STAG` children of `passVerForm` (via the same `for (const auto& child : passVerForm.children) if (child.isForm && strncmp(child.subType, "STAG", 4)==0)` pattern already used elsewhere in this file for FORM traversal), calling `parseStag` for each with its loop index, and push each returned sampler into `impl.samplers` (same field PPSH populates — downstream consumers need no changes). Update the Effect.h header comment's STRUCTURE block (already has a `-- OR -- FORM PFFP + FORM STAG` note at line 26-27) to reflect that STAG sampler extraction is now implemented, not just "walked."
  </action>
  <acceptance_criteria>
    - `packages/native-core` C++ build succeeds (cmake-js rebuild, no compile errors).
    - `grep -c "parseStag" packages/native-core/modules/core/formats/Effect.cpp` >= 2 (declaration + call site).
    - If a real or synthetic fixed-function `.eft` fixture is available (Task 3), `parseEffect` on it returns `samplers.length > 0` for the fixed-function IMPL (was `0` before this task).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract the real a_envmask_specmap.eft fixture + companion .sht</name>
  <files>packages/harness/scripts/extract-effect-fixtures.cjs</files>
  <read_first>
    packages/harness/scripts/extract-ans-fixtures.cjs (the full extraction script pattern — mount, search, resolve, read, write)
    packages/harness/test/mesh-roundtrip.test.ts (lines 137-151 loadFixture; lines 1231-1309 the EFCT describe block whose assertions this fixture activates)
  </read_first>
  <action>
    Create `extract-effect-fixtures.cjs` mirroring `extract-ans-fixtures.cjs`'s structure exactly: mount TRE archives from `D:/SWG Infinity/SWG Infinity/Live` (the same client root the existing script uses), search the mount for `.eft` entries matching `a_envmask_specmap` (per RESEARCH.md's citation "extracted from shader_02.tre"), resolve + read the winning entry's bytes, and write to `packages/harness/fixtures-real/effect/a_envmask_specmap.eft`. Also search for and extract its companion `.sht` shader-template file (the body-droid `.sht` already referenced by the "Bug-1 regression" describe block at `mesh-roundtrip.test.ts:1317+`, which reads `fixtures-real/shader/...` — confirm whether that fixture already exists on disk from a prior phase; if not, extract it too into `fixtures-real/shader/`). Run the script (`node packages/harness/scripts/extract-effect-fixtures.cjs`) against the real client install available in this environment.
  </action>
  <acceptance_criteria>
    - `packages/harness/fixtures-real/effect/a_envmask_specmap.eft` exists on disk after running the script.
    - `pnpm --filter @swg/harness exec vitest run mesh-roundtrip.test.ts -t "FORM EFCT"` — the three previously-skipping `it()` blocks (round-trip, sampler role, blend) now EXECUTE (no "SKIP:" console output) and PASS.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fixed-function STAG coverage — real or synthetic fixture</name>
  <files>packages/harness/test/mesh-roundtrip.test.ts, packages/harness/fixtureRegistry.ts</files>
  <read_first>
    packages/harness/test/mesh-roundtrip.test.ts (lines 1231-1309, the EFCT describe block, and the synthetic-fixture pattern used elsewhere in this file for DTLA/SKTM v0001 — "hand-crafted to match byte-exact oracle layout")
    packages/harness/fixtureRegistry.ts (registerFormat contract — loaderSource citation format required by the CORE-05 sweep)
  </read_first>
  <action>
    Search the mounted VFS (via the Task 2 extraction script or a quick ad-hoc mount) for a `.eft` file whose best IMPL has NO `FORM PPSH` (a genuinely fixed-function shader) among the client's shader archives. If one is found, extract it into `fixtures-real/effect/` and add a new `it()` case to the EFCT describe block asserting `parseEffect` returns `samplers.length > 0` with roles decoded via the new STAG path (mirroring the existing `a_envmask_specmap.eft` sampler-role test). If NO fixed-function `.eft` can be located within a reasonable search, hand-craft a MINIMAL synthetic FORM EFCT fixture (FORM EFCT → FORM 0001 → FORM IMPL → FORM 0002 → SCAP + DATA + FORM PASS → FORM 0009 → DATA(56B blend) + FORM PFFP + one FORM STAG chunk 0000 with a known textureTag) following this file's existing synthetic-fixture convention (e.g. the DTLA v0001 committed fixture at line 1076), and register it via `registerFormat` with a `loaderSource` citation to `ShaderImplementation.cpp:2430-2560` (satisfies the CORE-05 sweep's citation regex).
  </action>
  <acceptance_criteria>
    - At least one new or existing test case exercises the STAG code path added in Task 1 and asserts `samplers.length > 0` with a correctly-decoded role string (not "NIAM"/reversed).
    - `pnpm --filter @swg/harness exec vitest run` — full harness suite green, including the CORE-05 sweep test (`registry-coverage` or equivalent) which enforces every registered format has a cited fixture.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real client TRE archive → extraction script → repo-local gitignored fixture | Trusted local read of the maintainer's own installed client; never touches the network or commits real asset bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-09 | Tampering | Malformed/oversized .eft causing unbounded parse | accept | Same IFF traversal + bounds-checking discipline already applied to all other native-core parsers (T-01-03 style caps apply transitively) |
| T-04.4-SC | Tampering | N/A — no new package installs this task | accept | No npm/pip/cargo install in scope |
</threat_model>

<verification>
`pnpm --filter @swg/harness exec vitest run mesh-roundtrip.test.ts` green (EFCT block executing, not skipping). C++ build green.
</verification>

<success_criteria>
STAG sampler extraction implemented and covered by a passing test; the real a_envmask_specmap.eft fixture activates the previously-skipping CORE-05 gate.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-05-SUMMARY.md` when done
</output>
