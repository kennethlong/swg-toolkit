---
phase: 04.4-ux-polish-deploy-hardening
plan: 07
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/services/core3ServerPush.ts
  - packages/renderer/src/services/core3ServerPush.test.ts
  - docs/05-server-integration/core3-parity.md
autonomous: true
requirements: [product-thesis-shadow-sandbox-and-server-push]

# REVISION NOTE (--reviews pass, 2026-07-03): folds in the 4/4-reviewer MEDIUM cluster on server push.
#   - PATH CONTRACT frozen (Codex: "07 vs 12 disagree on Core3 path shape"): `serverConfig.path`
#     (type 'core3-wsl2') = the Core3 `MMOCoreORB/bin` directory (the root that DIRECTLY contains
#     `conf/`), matching the real repo layout confirmed by RESEARCH.md's citations
#     (`MMOCoreORB/bin/conf/config.lua`, `ConfigManager.cpp`, `DataArchiveStore.cpp` are all under
#     `MMOCoreORB/`). `pushCore3TreOverride`'s `confDir` PARAMETER is the ALREADY-RESOLVED conf
#     directory — callers (04.4-12's VcsPanel) compute `path.join(serverConfig.path, 'conf')` and pass
#     THAT in. This is now stated identically here and in 04.4-12 — no more guessing at the call site.
#   - PERSISTENCE (Codex HIGH: "reset records are local-only... lost if the app closes after a push"):
#     `pushCore3TreOverride` now also persists the returned record to
#     `studioDir/serverPush.core3.json` (atomic tmp+rename, same discipline as every other write in
#     this codebase) so 04.4-12's Reset button survives a panel remount or app restart, not just the
#     current React session.
#   - LIVENESS PRE-CHECK (Sonnet MEDIUM: "no sanity check before writing"): pushCore3TreOverride now
#     fails with a clear, named error (not a raw ENOENT) if `conf/config.lua` is missing/unreadable at
#     the resolved confDir, surfacing which path was checked.

# REVISION NOTE (fact-check pass, 2026-07-03): CORRECTS AN INVERTED CLAIM injected by the prior
# --reviews pass. That pass's Task 1 read_first/action text claimed `setLiveVersion` was "the SHIPPED
# function name" for the "reconcile-to-version invariant" and that `syncLiveToVersion` was "a stale
# alias" some notes refer to. Verified by direct read of both files (2026-07-03):
# `packages/renderer/src/services/syncLiveToVersion.ts` EXISTS and is the SHIPPED version-reconcile
# ENGINE (imported by DeployDialog.tsx, VersionHistoryBody.tsx, undoStore.ts; has its own
# syncLiveToVersion.test.ts). It performs the actual file-level revert/apply work and calls
# `setLiveVersion` (changesetService.ts:465) internally, as its LAST step, purely to move the
# activeVersionId/deployedVersionId pointers. Claim (e) below is corrected accordingly: the
# close-out audit verifies the reconcile-to-version ENGINE claim against `syncLiveToVersion`, not
# `setLiveVersion` alone.

# REVISION NOTE (mount-research pass, 2026-07-03 — see RESEARCH.md ADDENDUM "Server-Push Mounts &
# Reload Semantics"): folds in MEASURED ground truth from the maintainer's live Core3 + a full
# source trace of the config/TRE lifecycle. Four corrections to this plan's round-1 text:
#   1. LIVE TREE, NOT THE CHECKOUT: the running Core3 is `\\wsl.localhost\Debian\home\kenny\
#      workspace\Core3\MMOCoreORB` (Debian WSL2, ~/workspace/Core3). The sibling `D:\Code\Core3`
#      checkout is a source-reading copy the running server NEVER reads — the prior design note's
#      "e.g. the sibling D:\Code\Core3\MMOCoreORB\bin checkout" example was WRONG as a push target.
#      Node fs over the `\\wsl.localhost` UNC is verified working (auto-starts a stopped distro).
#   2. TREPATH RESOLUTION ORDER: `config-local.lua` OVERRIDES `config.lua` in the same Lua VM —
#      the maintainer's live config-local.lua sets `TrePath = "/mnt/d/SWGEmu-Client/SWGEmu"` while
#      config.lua still has the upstream default. Task 3's old step 4 ("read config.lua to find
#      Core3.TrePath") would extract the WRONG path; TrePath must be read from config-local.lua
#      first, falling back to config.lua, then translated to a host path (`/mnt/<x>/` → `<X>:\`,
#      other absolute → `\\wsl.localhost\<distro>\<path>`).
#   3. NEVER OVERWRITE A MOUNTED .tre (source-traced): Core3 reads TRE payload bytes from disk per
#      request against a boot-time index (TreeFileRecord.h:109-130) — overwriting an already-
#      mounted archive in place serves GARBAGE until restart. Push therefore writes a per-version
#      TOKENED filename and replaces the Lua line, never overwriting a possibly-mounted file.
#   4. RESTART REQUIRED, DOCUMENTED: TreFiles is consumed exactly once at boot (loadTres is
#      hard-guarded, DataArchiveStore.cpp:59-63); no command/REST/signal reloads it. The docs task
#      now documents this + the day-to-day runbook (D:\Code\Core3\wsl2\RUNBOOK.md), the verified
#      first-listed-wins duplicate precedence (prepend-at-1 confirmed correct; INVERSE of the
#      client searchTree rule), and Core3's v0005/0006-only TRE parsing (our packPatch v'5000' =
#      EERT5000 = v0005 → compatible).
must_haves:
  truths:
    - "The product-thesis close-out audit's five realized claims are re-verified against shipped code and documented (D-20)"
    - "The Core3 TRE search-path mechanism is verified against real source (config.lua/config-local.lua/ConfigManager.cpp/DataArchiveStore.cpp) and documented in docs/05-server-integration, replacing the AI-proposed caveat for that section (D-22)"
    - "A project bound to a serverConfig of type core3-wsl2 can push its override archive into the Core3 server's TRE search path with a non-destructive, restorable injection, and the record needed to reset it survives an app restart (not just the current session's React state)"
  artifacts:
    - path: "packages/renderer/src/services/core3ServerPush.ts"
      provides: "pushCore3TreOverride / resetCore3TreOverride — Lua-array injection into conf/config-local.lua, PLUS readCore3PushRecord/clearCore3PushRecordFile for cross-restart persistence"
    - path: "docs/05-server-integration/core3-parity.md"
      provides: "Verified (not AI-proposed) TRE search-path push mechanism section"
  key_links:
    - from: "core3ServerPush.ts"
      to: "conf/config-local.lua"
      via: "table.insert(Core3.TreFiles, 1, ...) — same-Lua-VM override point, ConfigManager.cpp:33-44"
      pattern: "table.insert\\(Core3.TreFiles"
    - from: "core3ServerPush.ts"
      to: "packPatch/flatten"
      via: "builds its OWN override .tre from the active version — does not depend on the CLIENT's deploy model"
      pattern: "packPatch|flatten\\("
    - from: "core3ServerPush.ts"
      to: "studioDir/serverPush.core3.json"
      via: "atomic persistence of the last push record, read back by 04.4-12's VcsPanel on mount"
      pattern: "serverPush\\.core3\\.json"
---

<objective>
D-20 (close-out audit) + D-22/D-23 (ground-truth AND implement, Core3 flavor). Verify the product-thesis's five "realized" claims against actually-shipped code, correct `docs/05-server-integration/core3-parity.md`'s AI-proposed caveat for the TRE search-path section with the RESEARCH.md-verified real mechanism, and ship a working, restorable Core3 server-push service whose reset record survives an app restart.

Design note (Open Question 1 is now GROUND-TRUTHED — RESEARCH.md ADDENDUM): the maintainer's own `007` project wizard ALREADY captures a user-provided `serverConfig.path` (type `'core3-wsl2'`, an absolute path — see `WorkspaceBindingMeta.serverConfig` in `packages/contracts/src/workspace.ts`, persisted per-project since Phase 4.1 as "capture-only"). This plan ACTS on that already-captured path — but the path the user must provide is the **LIVE server tree's `MMOCoreORB/bin`, as seen from Windows**, which for a WSL2 Core3 is a `\\wsl.localhost\<distro>\...` UNC (maintainer's: `\\wsl.localhost\Debian\home\kenny\workspace\Core3\MMOCoreORB\bin` — UNC `fs` writes verified working this session, and accessing the share auto-starts a stopped distro). The sibling `D:\Code\Core3` checkout is NOT a valid target (the running server never reads it) — the docs task's walkthrough says this explicitly. All path handling must treat UNC roots as first-class (no drive-letter assumptions).

**PATH CONTRACT (locked, identical in 04.4-12):** `serverConfig.path` = the Core3 `MMOCoreORB/bin` directory (contains `conf/` directly). `pushCore3TreOverride`'s `confDir` parameter is the CALLER-RESOLVED conf directory — i.e. callers pass `path.join(serverConfig.path, 'conf')`, never `serverConfig.path` itself. This function does not re-derive `confDir` from `serverConfig.path` internally.

Purpose: close the loop on "point at a working client → get a throwaway shadow" by extending the SAME thesis to the server side — the "server-side analog of cfgActivator" the maintainer described.
Output: `core3ServerPush.ts` (with cross-restart persistence) + updated `docs/05-server-integration/core3-parity.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/todos/pending/product-thesis-shadow-sandbox-and-server-push.md

<interfaces>
From packages/contracts/src/workspace.ts (ALREADY EXISTS — do not redeclare):
  WorkspaceInfo.serverConfig?: { type: 'core3-wsl2' | 'swgsource-docker'; path: string; hostPort: string }
  WorkspaceBindingMeta.serverConfig — same shape, persisted to .studio/workspace.json by projectBinding.initProject
  serverConfig.path (type 'core3-wsl2') = the MMOCoreORB/bin directory (root containing conf/) — LOCKED
  contract, identical in 04.4-12.

From ../Core3/MMOCoreORB/bin/conf/config.lua (verified, RESEARCH.md):
  Core3.TrePath = "&lt;path&gt;"
  Core3.TreFiles = { "default_patch.tre", ..., "bottom.tre" }   -- index 0 = highest priority
  (first-listed WINS duplicate virtual paths — hard-verified via TreeDirectory's NO_DUPLICATE
  insert plan; RESEARCH.md ADDENDUM. INVERSE of the client's searchTree priority rule.)

From the LIVE Core3 runtime (measured 2026-07-03, RESEARCH.md ADDENDUM — the real-world shape the
fixture tests model):
  live tree  = \\wsl.localhost\Debian\home\kenny\workspace\Core3\MMOCoreORB   (NOT D:\Code\Core3)
  config-local.lua EXISTS and overrides TrePath = "/mnt/d/SWGEmu-Client/SWGEmu"  (→ D:\SWGEmu-Client\SWGEmu)
  TreFiles/TrePath are consumed ONCE at boot (loadTres hard-guard) — every push needs a server
  restart to take effect (runbook: D:\Code\Core3\wsl2\RUNBOOK.md); Core3 parses TRE v0005/0006 only.

From ../Core3/MMOCoreORB/src/conf/ConfigManager.cpp:33-44 (verified):
  loadConfigData() runs conf/config.lua FIRST, then conf/config-local.lua SECOND IN THE SAME LUA VM
  (only if config-local.lua exists) — so config-local.lua can `table.insert` into the already-populated
  Core3.TreFiles table without redeclaring it.

From ../Core3/MMOCoreORB/src/templates/manager/DataArchiveStore.cpp:56-88 (verified):
  loadTres(path, treFilesToLoad) iterates treFilesToLoad IN ARRAY ORDER — index 0 is the highest priority
  (confirmed by the array's own contents: newest patch first, oldest base last).

From packages/renderer/src/services/cfgActivator.ts (the append-not-overwrite + atomic-write discipline to mirror):
  ensureInclude(rootCfgPath, includeFileName) — idempotency check + atomic tmp+rename write pattern.

From packages/renderer/src/services/changesetService.ts:
  readManifest(studioDir), flatten(versionId, manifest, studioDir) -> StagingEntry[], writeManifest(studioDir, manifest)
  (writeManifest's tmp+rename pattern — packages/renderer/src/services/changesetService.ts:70-76 — is the
  atomic-write pattern to mirror for the new serverPush.core3.json persistence)

From packages/renderer/src/services/packPatch.ts:
  packPatch(staged: StagingEntry[], outputPath: string): void  -- buildTre version='5000', atomic output
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close-out audit — verify realized claims + correct docs/05-server-integration</name>
  <files>docs/05-server-integration/core3-parity.md</files>
  <read_first>
    packages/renderer/src/services/workspaceService.ts (lazy shadow — no multi-GB copy; searchTree absolute-path default)
    packages/renderer/src/services/cfgActivator.ts (cfg snapshot/restore — snapshotCfg/restoreCfg)
    packages/renderer/src/services/looseOverrideDeploy.ts (B3 loose snapshot pattern)
    packages/renderer/src/services/changesetService.ts (seedBaseline — Baseline root node; setLiveVersion — the manifest live-pointer setter, verified 2026-07-03 at changesetService.ts:465)
    packages/renderer/src/services/syncLiveToVersion.ts (the SHIPPED version-reconcile ENGINE — verified 2026-07-03 to perform the actual file-level revert/apply operations per H4 dispatch, and to call setLiveVersion internally as its final step to move the pointers; verify the reconcile-to-version claim against THIS function, not setLiveVersion alone)
    docs/05-server-integration/core3-parity.md (current content, including the "AI-proposed... must be validated" caveat at the top)
    ../Core3/MMOCoreORB/bin/conf/config.lua, ../Core3/MMOCoreORB/src/conf/ConfigManager.cpp, ../Core3/MMOCoreORB/src/templates/manager/DataArchiveStore.cpp
  </read_first>
  <action>
    For each of the five product-thesis "realized" claims — (a) lazy/virtual shadow (no multi-GB copy, only deltas materialize), (b) cfg snapshot/restore (byte-pristine whole-file restore), (c) B3 loose-override snapshot pattern, (d) Baseline root changeset node, (e) reconcile-to-version engine (the shipped entry point is `syncLiveToVersion` in `syncLiveToVersion.ts` — the engine that performs the file-level revert/apply operations and calls `setLiveVersion` internally as its final step to move the activeVersionId/deployedVersionId pointers; verify against `syncLiveToVersion`, not `setLiveVersion` alone) — confirm each is present in the cited file and matches the thesis's description (read the file, do not assume from memory/STATE.md alone). Add a new `## Verified: Product-Thesis Close-Out (2026-07-03)` section to `docs/05-server-integration/core3-parity.md` documenting: the five confirmations with file:line citations, PLUS the Core3 TRE search-path push mechanism verified in this plan (config.lua `Core3.TreFiles` array + `config-local.lua` same-VM override via `ConfigManager.cpp:33-44` + `DataArchiveStore.cpp:56-88` load-order proof), PLUS a "Mounting the live Core3 (WSL2) from Windows" walkthrough sourced from the RESEARCH.md ADDENDUM: (i) the push target is the LIVE server tree's `MMOCoreORB/bin` via `\\wsl.localhost\<distro>\<path>` (enumerate distros with `WSL_UTF8=1 wsl.exe -l -q`; the `D:\Code\Core3` checkout is NOT read by the running server), (ii) `config-local.lua` overrides `config.lua` (TrePath resolution order), (iii) the Linux→host TrePath translation rule (`/mnt/<x>/` → `<X>:\`, other absolute → `\\wsl.localhost\<distro>\...`), (iv) RESTART REQUIRED semantics — TreFiles is read once at boot (`loadTres` hard-guard, DataArchiveStore.cpp:59-63), no runtime reload exists, and overwriting a mounted `.tre` in place serves garbage (TreeFileRecord.h:109-130 payload-per-request vs stale boot index) — hence the tokened-filename push design, (v) duplicate-path precedence is FIRST-listed-wins (prepend-at-index-1 confirmed; the INVERSE of the client's searchTree rule — cite SortedVector NO_DUPLICATE evidence), and (vi) Core3 parses only TRE v0005/0006 (tre3/TreeFile.cpp:53-86; our `packPatch` v'5000' = `EERT5000` = v0005, compatible) — replace the top-of-file "AI-proposed... must be validated" caveat's applicability to THIS section specifically (the caveat may remain accurate for other undocumented sections of the file, e.g. the REST deployment daemon design sketch — do not remove it wholesale, scope the correction to the TRE search-path subsection only). (swg-main flavor's findings are documented separately in `docs/05-server-integration/swg-main-parity.md`, owned by 04.4-08 -- this plan's docs edit does not depend on or wait for that plan.)
  </action>
  <acceptance_criteria>
    - `docs/05-server-integration/core3-parity.md` contains a new dated section citing real file:line evidence for all 5 close-out claims + the Core3 push mechanism.
    - The blanket "AI-proposed" caveat at the top of the file is either removed or explicitly scoped to exclude the newly-verified TRE search-path section.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED): core3ServerPush.test.ts against a fixture Core3-conf-like tree</name>
  <files>packages/renderer/src/services/core3ServerPush.test.ts</files>
  <read_first>
    packages/renderer/src/services/cfgActivator.ts (ensureInclude test conventions, if a cfgActivator.test.ts exists — mirror its fixture style)
    packages/renderer/src/services/changesetService.ts (flatten signature, writeManifest atomic pattern)
    packages/renderer/src/services/packPatch.ts (packPatch signature)
  </read_first>
  <behavior>
    - Given a tmp fixture dir with `<confDir>/config.lua` containing a `Core3.TreFiles = { "a.tre", "b.tre" }` table and NO `<confDir>/config-local.lua`, calling `pushCore3TreOverride(confDir, studioDir, activeVersionId, manifest, projectSlug)` creates `<confDir>/config-local.lua` containing exactly one `table.insert(Core3.TreFiles, 1, "swgtoolkit_&lt;projectSlug&gt;_&lt;versionToken&gt;.tre")` line (versionToken = a sanitized short token derived from `activeVersionId` — TOKENED so a re-push of different content never overwrites a `.tre` the running server may have mounted; see RESEARCH.md ADDENDUM: Core3 re-reads payload bytes per request against a stale boot-time index, so in-place overwrite serves garbage), copies a freshly-packed override `.tre` (built via `flatten`+`packPatch` from the active version) into the fixture's resolved TrePath, AND writes the returned record (including `treFileName`) to `studioDir/serverPush.core3.json` (atomic tmp+rename).
    - TrePath RESOLUTION ORDER (matches ConfigManager's same-VM override semantics): if `<confDir>/config-local.lua` exists and contains a `TrePath` assignment, THAT value wins; otherwise `Core3.TrePath` from config.lua. Test both: a fixture where config-local.lua pre-exists with its own `TrePath` (the maintainer's real shape) must push the archive into the config-local value's dir, not config.lua's.
    - `translateLinuxPathToHost(linuxPath, opts)` (exported pure helper): `/mnt/<x>/rest` → `<X>:\rest`; any other absolute Linux path → `\\wsl.localhost\<distro>\<path>` where `<distro>` comes from `opts.distro` or is derived from a `\\wsl.localhost\<distro>\...`-shaped `confDir`; a non-`/mnt` path with NO derivable distro throws a clearly-named error (not a silent wrong path). A TrePath that is ALREADY a Windows/UNC path passes through unchanged.
    - Calling `pushCore3TreOverride` a SECOND time with the SAME activeVersionId (idempotency) does not duplicate the `table.insert` line and reuses the same tokened filename; a second push with a DIFFERENT activeVersionId REPLACES the previous `table.insert` line (still exactly one toolkit line in the file), writes the NEW tokened `.tre`, best-effort unlinks the previous archive named in the prior record, and overwrites `serverPush.core3.json` with the latest record (not append).
    - `readCore3PushRecord(studioDir)` returns the persisted record after a push, and `null` if no push has occurred (never throws — mirrors `readManifest`'s never-throws style).
    - Calling `resetCore3TreOverride(record)` removes ONLY the injected `table.insert` line (line-surgery, mirroring `deactivatePatch`'s discipline) — if `config-local.lua` was toolkit-created (did not exist before the first push), it is deleted entirely instead; a config-local.lua that pre-existed with OTHER content (including its own `TrePath` line — the maintainer's real shape) is left otherwise intact. Reset ALSO best-effort unlinks the pushed archive (`record.treFileName` in the resolved TrePath dir) and calls `clearCore3PushRecordFile(studioDir)` to remove the persisted `serverPush.core3.json`.
    - Given a `confDir` with no `config.lua` present, `pushCore3TreOverride` throws a clearly-named error identifying the missing path (e.g. `"Core3 conf/config.lua not found at <confDir>/config.lua — confirm serverConfig.path points at the MMOCoreORB/bin directory"`) rather than a raw ENOENT.
    - A project slug / archive filename containing Lua-unsafe characters (`"`, `\`, newline) is escaped/sanitized before being embedded in the generated Lua line (V5-equivalent threat mitigation) — the resulting file remains valid Lua (parseable, no unescaped quote breaking out of the string literal).
  </behavior>
  <action>
    Create `core3ServerPush.test.ts` using an isolated tmp fixture directory (never touching the real `../Core3` checkout) with a synthetic `<confDir>/config.lua` + `<confDir>/config-local.lua` (absent initially) + a fake `TrePath` dir + a separate tmp `studioDir`, and a synthetic changeset manifest with one staged file entry, to exercise the six behaviors above. Run and confirm RED (module not found).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run core3ServerPush.test.ts` fails RED.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Implement core3ServerPush.ts</name>
  <files>packages/renderer/src/services/core3ServerPush.ts</files>
  <read_first>
    packages/renderer/src/services/cfgActivator.ts (ensureInclude atomic-write + deactivatePatch line-surgery patterns — port the SAME discipline, applied to a .lua file)
    packages/renderer/src/services/changesetService.ts (flatten, writeManifest's atomic tmp+rename pattern)
    packages/renderer/src/services/packPatch.ts (packPatch)
    packages/renderer/src/services/core3ServerPush.test.ts (from Task 2)
  </read_first>
  <action>
    Implement `pushCore3TreOverride(confDir: string, studioDir: string, activeVersionId: string, manifest: WorkspaceChangesetManifest, projectSlug: string): Core3PushRecord`: (1) verify `path.join(confDir, 'config.lua')` exists and is readable; if not, throw the clearly-named error described in Task 2's behavior list (liveness pre-check — the caller is given a specific, actionable message, not a raw fs exception); (2) sanitize `projectSlug` to a Lua-string-safe + filesystem-safe token (strip/escape `"`, `\`, control chars — reuse or mirror the sanitization already applied to project ids in `getStudioDir`); (3) derive `versionToken` (sanitized short token from `activeVersionId`) and the tokened archive name `swgtoolkit_${projectSlug}_${versionToken}.tre`, then `flatten(activeVersionId, manifest, studioDir)` and `packPatch(entries, path.join(studioDir, 'build', treFileName))` to build the override archive from THIS project's active version (independent of whatever deploy model the CLIENT side used); (4) resolve `Core3.TrePath` with config-local-first precedence — regex-extract from `config-local.lua` if present there, else from `config.lua` (ConfigManager runs both in the same Lua VM, config-local last, so its assignment wins — the maintainer's REAL config-local.lua overrides TrePath to `/mnt/d/SWGEmu-Client/SWGEmu`) — then map it through `translateLinuxPathToHost` (exported pure helper: `/mnt/<x>/rest` → `<X>:\rest`; other absolute Linux path → `\\wsl.localhost\<distro>\<path>` with distro from an options arg or derived from a `\\wsl.localhost`-shaped confDir; untranslatable → clearly-named error; already-Windows/UNC paths pass through); (5) copy the built `.tre` into that resolved host dir under the TOKENED name — NEVER overwrite a differently-named prior archive in place (a running Core3 re-reads payload bytes against a stale boot index; RESEARCH.md ADDENDUM); if a prior record names a DIFFERENT `treFileName`, best-effort unlink it after the new copy lands; (6) read (or create) `config-local.lua` in `confDir`; if it already contains a toolkit `table.insert(Core3.TreFiles, 1, "swgtoolkit_...")` line for this slug, REPLACE it with the new tokened line (same-version re-push = identical line, no-op), else append (idempotency check mirrors `ensureInclude`); use the SAME atomic tmp+rename write pattern as `cfgActivator.ts`. Build a `Core3PushRecord { confDir, configLocalPath, trePath (resolved host form), treFileName, insertedLine, wasConfigLocalCreatedByToolkit }`; (7) persist it via `fs.writeFileSync(tmpPath, JSON.stringify(record, null, 2), 'utf8'); fs.renameSync(tmpPath, path.join(studioDir, 'serverPush.core3.json'))` (same atomic discipline as `changesetService.writeManifest`); return the record. Implement `readCore3PushRecord(studioDir: string): Core3PushRecord | null` — never-throws, mirrors `readManifest`'s defensive-parse style (missing file or malformed JSON → `null`). Implement `resetCore3TreOverride(record: Core3PushRecord): void`: if `wasConfigLocalCreatedByToolkit`, `fs.unlinkSync(configLocalPath)`; else, targeted line-surgery removing ONLY the `insertedLine` (mirror `deactivatePatch`'s regex-line-removal + triple-newline collapse), atomic write; then best-effort unlink `path.join(record.trePath, record.treFileName)` (never throws — the file may already be gone or briefly locked by a running server). Implement `clearCore3PushRecordFile(studioDir: string): void` (best-effort unlink of `serverPush.core3.json`, never throws). `resetCore3TreOverride` should be callable standalone (04.4-12's Reset button calls it directly with the record it read via `readCore3PushRecord`); calling `clearCore3PushRecordFile` after a successful reset is the CALLER's responsibility (04.4-12), keeping `resetCore3TreOverride` a pure "undo the Lua/tre write" function — but it is ALSO acceptable and simpler for `resetCore3TreOverride` to call `clearCore3PushRecordFile(record's associated studioDir)` itself if the record shape carries `studioDir`; pick whichever keeps the record shape simplest and document the choice in a code comment.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run core3ServerPush.test.ts` all Task 2 cases GREEN.
    - `pnpm --filter @swg/renderer exec tsc --noEmit` clean.
    - `grep -c "serverPush.core3.json" packages/renderer/src/services/core3ServerPush.ts` >= 1 (confirms cross-restart persistence is wired, not just in-memory return).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolkit (Windows renderer) → external server conf directory (Core3 checkout, WSL2 or otherwise) | First deploy target OUTSIDE the toolkit's own app-data root or the bound client install — new territory per RESEARCH.md V12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-12 | Tampering | Lua injection via an unescaped project slug/archive filename embedded in config-local.lua | mitigate | Sanitize/escape `"`, `\`, control characters before embedding in the generated `table.insert` line (Task 3) |
| T-04.4-13 | Tampering | Writing to the wrong / stale Core3 checkout (confirms a different server than the one actually running) | accept | User-provided `serverConfig.path` (captured by the existing 007 wizard) is the same trust level as `clientPath` — the maintainer confirms which directory to point at; no auto-discovery attempted. A minimal liveness check (config.lua must exist/be readable, Task 3) catches the "pointed at nothing" case; confirming the pointed-at server is the RUNNING instance is out of scope for a file-write mechanism and is covered instead by 04.4-12's manual full-loop checkpoint. |
| T-04.4-14 | Repudiation | A failed push leaves a partially-written config-local.lua or a stale/missing serverPush.core3.json | mitigate | Atomic tmp+rename write for BOTH the Lua file and the JSON persistence record (mirrors cfgActivator.ts) — never a partial file on disk |
| T-04.4-14b | Tampering (data corruption) | Overwriting a `.tre` a RUNNING Core3 has mounted — payload bytes are re-read per request against the stale boot-time index, serving garbage until restart (source-traced, RESEARCH.md ADDENDUM) | mitigate | Per-version TOKENED archive filenames — a re-push with new content writes a NEW file + replaces the Lua line; the previously-named file is only best-effort unlinked, never rewritten in place |
</threat_model>

<verification>
`pnpm --filter @swg/renderer exec vitest run core3ServerPush.test.ts` green. Docs updated with verified findings.
</verification>

<success_criteria>
Close-out audit documented; Core3 server push works against a fixture conf tree with idempotent push + clean restore-on-reset; the push record survives an app restart via serverPush.core3.json.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-07-SUMMARY.md` when done
</output>
