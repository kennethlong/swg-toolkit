---
phase: 04.4-ux-polish-deploy-hardening
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - packages/renderer/src/panels/viewport/orientation.ts
  - packages/renderer/src/panels/viewport/orientation.test.ts
  - packages/renderer/src/panels/viewport/StaticMeshView.tsx
  - packages/renderer/src/panels/viewport/SkinnedMeshView.tsx
autonomous: false
requirements: [viewport-default-facing-axis]

# REVISION NOTE (--reviews pass, 2026-07-03): 4/4 reviewers flagged the same issue from different
# angles. VERIFIED 2026-07-03 by reading StaticMeshView.tsx:51-58 directly: the CURRENT code comment
# says, verbatim, "The 180° Y guess showed the model's BACK...0° shows its front. The residual
# left-vs-right difference from SIE is a default CAMERA-AZIMUTH preference, not a mesh rotation."
# The prior plan's Task 2 acceptance criteria HARD-REQUIRED a non-identity outcome ("must NOT be
# identity (0,0,0)"), which pre-judges the exact question D-17's human-verify checkpoint exists to
# answer, and risks burning the single mandated checkpoint cycle forcing a wrong rotation just to
# satisfy a non-identity assertion. Fixed below: Task 2's acceptance criteria no longer forbid
# identity; Task 3 explicitly allows "identity is already correct, SIE gap is camera-only" as a valid,
# non-failing outcome, with instructions to document that finding (not force a cosmetic rotation) if
# so. D-16's locked mechanism ("one fixed det+1 rotation... NOT a camera move") is still honored as
# the PRIMARY hypothesis to test first — this only changes what counts as an acceptable *outcome* of
# testing it, per the checkpoint's own purpose.
#
# WAVE CHANGE (Opus, verified against the roadmap's wave semantics): this plan's Task 3 is an
# `autonomous:false` blocking human-verify checkpoint. No Wave-2 plan in this phase depends on
# 04.4-04, so leaving it in Wave 1 risks stalling the whole Wave 1→2 barrier on a maintainer eyeball
# check unrelated to delete/console/e2e work. Moved to its own Wave 3 (same tail wave as 04.4-14,
# with which it shares no files) so all Wave-1/2 automated work completes first; the checkpoint runs
# whenever the maintainer is available, without blocking anything else in this phase.
must_haves:
  truths:
    - "A model's authored front faces the default camera like SIE/in-game (maintainer visually confirms — D-17 human checkpoint), OR the checkpoint confirms the residual gap is camera-azimuth-only (not a mesh rotation) and the todo is closed with that documented finding — either outcome is an acceptable resolution of this plan, not just the former"
    - "Winding/normals stay correct (determinant +1 — a rotation, never a mirror/scale)"
    - "Both StaticMeshView and SkinnedMeshView inherit the SAME orientation from ONE shared module (D-16)"
  artifacts:
    - path: "packages/renderer/src/panels/viewport/orientation.ts"
      provides: "Single SWG_ORIENTATION constant (THREE.Euler), imported by both mesh views — no duplicate copy"
  key_links:
    - from: "StaticMeshView.tsx"
      to: "orientation.ts SWG_ORIENTATION"
      via: "import (replaces the local duplicated const)"
      pattern: "from '\\./orientation"
    - from: "SkinnedMeshView.tsx"
      to: "orientation.ts SWG_ORIENTATION"
      via: "import (replaces the local duplicated const)"
      pattern: "from '\\./orientation"
---

<objective>
Extract the duplicated `SWG_ORIENTATION` constant (currently identity in BOTH `StaticMeshView.tsx` and `SkinnedMeshView.tsx`) into one shared module, then test a NEW rotation hypothesis — NOT the already-falsified `rotateY(Math.PI)` (which was tried in Phase 02-03, showed the mesh's back, and was reverted to identity; this history is documented in both files' comments and MUST NOT be repeated). Close with the maintainer's mandated human-verify checkpoint (D-17) comparing `protocol_droid_red` + one more asymmetric mesh against SIE side-by-side — and treat "identity was already correct; the gap is camera azimuth" as a legitimate, documented outcome of that checkpoint, not a failure to route around.

Purpose: match SIE/in-game default facing without touching geometry/winding (determinant +1 rotation only — the 02-05 export X-mirror is a SEPARATE concern, do not conflate).
Output: `orientation.ts` (single source of truth) + both view files importing it + a maintainer-confirmed resolution (either a new committed rotation, or a documented "identity confirmed correct, camera-azimuth is the residual gap" finding).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-CONTEXT.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-RESEARCH.md
@.planning/phases/04.4-ux-polish-deploy-hardening/04.4-PATTERNS.md
@.planning/todos/pending/viewport-default-facing-axis.md

<interfaces>
Current state (BOTH files today, VERIFIED 2026-07-03 by direct read — READ BOTH before touching):
  StaticMeshView.tsx:51-58   const SWG_ORIENTATION = new THREE.Euler(0, 0, 0);  // consumed at line 385: &lt;group rotation={SWG_ORIENTATION}&gt;
  SkinnedMeshView.tsx:74-77  const SWG_ORIENTATION = new THREE.Euler(0, 0, 0);  // consumed at line 941: &lt;group ref={meshGroupRef} rotation={SWG_ORIENTATION}&gt;
  Both files' comments document, VERBATIM (StaticMeshView.tsx:52-57): "SWG: forward=+Z, up=+Y.
  Three.js camera looks down -Z. A 180° Y rotation brings SWG's authored front (+Z) to face the
  default camera. Mesh is already correctly oriented (Y-up matches Three.js; geometry verified
  faithful). The 180° Y guess showed the model's BACK ('facing away'); 0° shows its front. The
  residual left-vs-right difference from SIE is a default CAMERA-AZIMUTH preference, not a mesh
  rotation — tracked in viewport-default-facing-axis.md. Identity here keeps winding/normals correct."
  This is an EXISTING, already-tested conclusion from a prior phase, not a fresh hypothesis — treat it
  as real evidence to weigh against D-16's locked "rotation only" mechanism choice, not as noise.

Ground truth (D-16): ../io_scene_swg_msh/io_scene_swg/__init__.py:332-355 — @orientation_helper(axis_forward='Z', axis_up='Y'),
  global_matrix = Matrix.Scale(1,4) @ axis_conversion(to_forward='Z', to_up='Y') — a BLENDER-axis remap (Blender default:
  -Y forward, +Z up) applied via mesh.transform(global_matrix) directly to raw SWG vertices. This is NOT a Three.js
  rotation transcribed verbatim (Three.js defaults: +Y up, camera looks down -Z) — the equivalent Three.js-space
  rotation must be derived mathematically or iterated empirically, not copied from the Blender Euler values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract SWG_ORIENTATION into a shared orientation.ts module (refactor only, no behavior change)</name>
  <files>packages/renderer/src/panels/viewport/orientation.ts, packages/renderer/src/panels/viewport/orientation.test.ts, packages/renderer/src/panels/viewport/StaticMeshView.tsx, packages/renderer/src/panels/viewport/SkinnedMeshView.tsx</files>
  <read_first>
    packages/renderer/src/panels/viewport/StaticMeshView.tsx (lines 1-70, esp. 51-58 + the group consumption at ~385)
    packages/renderer/src/panels/viewport/SkinnedMeshView.tsx (lines 60-90, esp. 74-77 + the group consumption at ~941)
    packages/renderer/src/services/pathSafety.ts (header comment — the "extracted verbatim, both callers import, do NOT inline a second copy" discipline to replicate)
  </read_first>
  <behavior>
    - orientation.test.ts: `SWG_ORIENTATION` is a `THREE.Euler`; converting it to a `THREE.Matrix4` and reading `.determinant()` (via `THREE.Matrix4.makeRotationFromEuler` composed) equals +1 (never a mirror), for BOTH the pre-fix (identity) and post-fix (Task 2) values — this test must remain green across the Task 2 edit, proving the fix never introduces a reflection.
  </behavior>
  <action>
    Create `orientation.ts` exporting `export const SWG_ORIENTATION = new THREE.Euler(0, 0, 0);` with a header comment carrying forward the full history from both view files VERBATIM (180°Y tried → showed the mesh's back → reverted → the residual-is-camera-azimuth conclusion already reached once → this phase's re-attempt under D-17) so a future reader never repeats the falsified hypothesis and understands the existing conclusion being re-tested. Remove the local `const SWG_ORIENTATION = new THREE.Euler(0, 0, 0);` from both `StaticMeshView.tsx` and `SkinnedMeshView.tsx`; replace with `import { SWG_ORIENTATION } from './orientation.js';`. The `&lt;group rotation={SWG_ORIENTATION}&gt;` consumption sites are UNCHANGED (same variable name, new source). This task is a pure refactor — the rendered result must be pixel-identical to before (still identity) until Task 2 changes the constant's value.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run orientation.test.ts` green (determinant === 1).
    - `grep -c "new THREE.Euler(0, 0, 0)" packages/renderer/src/panels/viewport/StaticMeshView.tsx packages/renderer/src/panels/viewport/SkinnedMeshView.tsx` returns 0 for both files (the local declarations are gone, only the import remains).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive and test a new SWG→Three.js facing-rotation candidate</name>
  <files>packages/renderer/src/panels/viewport/orientation.ts, packages/renderer/src/panels/viewport/orientation.test.ts</files>
  <read_first>
    ../io_scene_swg_msh/io_scene_swg/__init__.py (lines 320-360, axis_conversion call + orientation_helper decorator)
    packages/renderer/src/panels/viewport/orientation.ts (from Task 1 — the documented false-start history AND the existing "residual is camera-azimuth" conclusion)
  </read_first>
  <action>
    Derive the Three.js-equivalent rotation: SWG's authored axes are forward=+Z, up=+Y (same up-axis as Three.js — no Y/Z swap needed, unlike the Blender case where up must also convert). Since up already matches Three.js, and the already-tried 180°Y showed the mesh's BACK (meaning identity already shows the FRONT toward the default Three.js camera direction, per the existing code comment), treat this as a residual AZIMUTH offset rather than re-deriving a full axis remap: iterate candidate `rotateY` values other than 0 and π (e.g. π/2, -π/2) while keeping `rotateX`/`rotateZ` at 0 (preserves determinant +1). Update `SWG_ORIENTATION` in orientation.ts to whichever candidate best matches SIE's default view in a LOCAL manual check before the Task 3 checkpoint — this may take more than one edit-and-recompile iteration (budget for it, per RESEARCH.md Pitfall 2). If NO Y-only candidate visually improves on identity after reasonable iteration, leave `SWG_ORIENTATION` as identity `(0,0,0)` and proceed to Task 3 anyway — do NOT force a non-identity value just to satisfy a "must have changed something" expectation; the existing code comment's own conclusion (residual is camera-azimuth, not mesh rotation) is a legitimate outcome for Task 3 to confirm or refute directly with the maintainer. Keep the orientation.test.ts determinant assertion passing for every candidate tried, including if the final value remains identity.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @swg/renderer exec vitest run orientation.test.ts` remains green (determinant still +1) regardless of which value is finally committed.
    - The chosen rotation is NOT `new THREE.Euler(0, Math.PI, 0)` (the already-falsified 180°Y) — confirmed by reading the final committed constant. Identity IS an acceptable committed value if Task 3's checkpoint confirms the existing camera-azimuth conclusion rather than a mesh-rotation fix.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer visual confirmation vs SIE (D-17) — allows "identity is correct, gap is camera-only" as a valid resolution</name>
  <files>packages/renderer/src/panels/viewport/orientation.ts</files>
  <action>Pause for human verification. Claude has applied and automated-verified (determinant+1 unit test) a candidate rotation in Task 2 (which may be identity, if no Y-only candidate improved on it); the maintainer manually confirms visual parity with SIE per how-to-verify before this plan is considered done. Do NOT auto-approve — this is a blocking gate per D-17. Both a "new rotation confirmed correct" AND a "identity was already correct; SIE gap is a camera-azimuth preference, not a mesh issue — closing todo as verified, no further rotation change needed" outcome are valid closes for this task; do not treat the latter as a failure requiring further guessing.</action>
  <what-built>Applied and tested a candidate SWG→viewer facing-axis rotation (Task 2, via the shared orientation.ts module, inherited by both StaticMeshView and SkinnedMeshView) — which may be a new non-identity value, or a confirmed-as-correct identity, depending on what Task 2's local iteration found.</what-built>
  <how-to-verify>
    1. Launch the app (`pnpm start` or the dev workflow) and load `protocol_droid_red` in the viewport.
    2. Open the same asset in SIE (Sytner's IFF Editor) side-by-side.
    3. Confirm the model's authored front faces the default camera in BOTH tools the same way (same apparent yaw).
    4. Load one more ASYMMETRIC mesh (a mesh with a clear left/right detail difference) and repeat the comparison.
    5. If either mesh still faces a different direction than SIE AND the committed value is non-identity, report the observed direction (e.g. "faces left instead of right") so Task 2's candidate value can be iterated further in a follow-up commit within this same task before closing.
    6. If the committed value is identity and the ONLY remaining discrepancy from SIE is which way the camera starts orbiting from (not the mesh's own front-facing direction), confirm this explicitly — that is a valid "approved" outcome per D-16/D-17's own goal (match SIE's PERCEIVED default facing), and no further rotation change is needed; the SUMMARY should record this as the resolution.
  </how-to-verify>
  <resume-signal>Type "approved" once the visual result matches SIE's default facing (whether via a new rotation or a confirmed-correct identity), or describe the observed mismatch to iterate further.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure client-side Three.js scene-graph transform; no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-08 | Tampering (self — geometry integrity) | orientation.ts rotation value | mitigate | determinant+1 unit test (Task 1/2) prevents an accidental mirror/scale from silently flipping winding/normals |
</threat_model>

<verification>
`pnpm --filter @swg/renderer exec vitest run orientation.test.ts` green. Task 3 human-verify checkpoint approved (either resolution).
</verification>

<success_criteria>
Single shared orientation module; a candidate rotation was tested; maintainer confirms visual parity with SIE on two meshes OR confirms the existing camera-azimuth-only finding — either is a successful close.
</success_criteria>

<output>
Create `.planning/phases/04.4-ux-polish-deploy-hardening/04.4-04-SUMMARY.md` when done
</output>
</output>
