---
phase: 04.4-ux-polish-deploy-hardening
verified: 2026-07-06T22:40:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  # No prior VERIFICATION.md — this is initial verification. Performed AFTER the
  # post-execution code review (04.4-REVIEW.md: 1 critical + 11 warnings) and its
  # fixes (04.4-REVIEW-FIX.md: 12/12 fixed, commits 2a6bb01..383e0de). Current code
  # state (including those fixes) was verified, not the raw plan-execution state.
warnings:
  - "3 review-fix items lack dedicated automated assertions (WR-01 SCAP-LE, WR-05 partial-park restore, WR-06 ctx-omitted loose-revert). SCAP is ground-truth-verified against ShaderImplementation.cpp:375/435/494 (project gold standard); WR-05/06 are covered by passing suites but the specific edge path is not pinned by a dedicated test. Non-blocking; recommend adding multi-IMPL eft fixture + partial-park restore test + ctx-omitted loose-revert test."
  - "IN-05 (info, unfixed): docs/05-server-integration/core3-parity.md walkthrough step 2 not yet updated for the bare-global TrePath form. The mechanism itself (extractBareTrePath / resolveLinuxTrePath, commit d60b29f) IS implemented in core3ServerPush.ts and was proven live under the D-22 checkpoint. Doc-completeness only."
  - "2 pre-existing e2e/04-workspace.spec.ts failures (SidebarPanel title-update timing, Titlebar theme-select timeout) documented in deferred-items.md and confirmed unrelated to this phase via A/B test. NOT phase gaps — documentation confirmed present."
---

# Phase 04.4: UX Polish & Deploy Hardening — Verification Report

**Phase Goal:** Clear the UI/hardening todo debt while it's cheap — before Phase 5 builds on the freshly-reworked deploy surfaces. Eight todo items (six roadmap + two folded during discuss-phase).
**Verified:** 2026-07-06T22:40:00Z
**Status:** passed
**Re-verification:** No — initial verification, performed against the CURRENT code state (post code-review + 12 fixes, commits 2a6bb01..383e0de).

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User can delete a project behind explicit confirm; bound client byte-pristine afterward; studio + umbrella folders gone (parked, session-undoable) | ✓ VERIFIED | `deleteProject.ts` restores client FIRST via shared `resetDeploymentFromRecord` (`{cleanupArtifacts:false}`) — same dispatch as `DeployDialog.handleReset` (`restoreCfg`/`resetLoose`/`deactivatePatch` by record shape) — then parks studio+umbrella into `.trash/<id>/` with crash-safe `.complete.json` marker (never hard-`rm`). Kebab `⋯` → `DeleteProjectConfirmModal` → `handleDeleteConfirmed` → `deleteProject()` wired in BOTH `ProjectListDialog.tsx` and `WorkspaceEntry.tsx` (sketch-017 elements). Undo via `deleteUndoStore` (bytes-only, D-04a). `e2e/06` asserts client cfg restored to KNOWN pristine sha256. Suites: deleteProject 12/12, deleteUndoStore 2/2 (spot-checked green). |
| 2 | Playwright suite drives real Electron through full deploy loop (stage → save → select → deploy → revert), zero console errors, runs in CI | ✓ VERIFIED | `e2e/07-deploy-flow.spec.ts` (465 ln, 2 scenarios: cfg-model + loose-override): boots with zero console/pageerror (D-10 collector), New-Project wizard, stage → save version → select → Deploy dialog (moves `deployedVersionId`, writes client cfg) → revert to Baseline restoring pristine bytes. `.github/workflows/ci.yml`: build-before-E2E (3× `vite build`) + `shell: bash` (WR-02 fix) + spec list includes 06/07. Real renderer via `app://` (no jsdom). |
| 3 | Data panel Console/Log tabs selectable and show live app logs (no DevTools for basic diagnosis) | ✓ VERIFIED | `ConsolePanel.tsx` (channel `console`) + `LogPanel.tsx` (channel `log`) both consume shared `logStore`, registered in `WorkspaceShell.tsx` (`console`/`log` ids). `installConsoleCapture()` at module scope (boot-time, not tab-open) wraps `console.*` + window error/rejection + folds main-process `main-log` IPC forward. Structured `log()` emit sites in `treAutoMount` (mount), `syncLiveToVersion` (reconcile), `main.ts` (main push). logService 9/9 (spot-checked green). |
| 4 | Status bar mesh name/vert count updates on every viewport load | ✓ VERIFIED | `StatusBar.tsx` reactively subscribes `useViewportStore` `loadStatus`+`parsedMesh`; `meshLabel = loadStatus.filename`, `vertCount = Σ shaderGroups.vertexCount` (D-18, no LOD index). Store subscription → updates each `loadComplete`. StatusBar suite spot-checked green. |
| 5 | VfsTree non-override label legible across 5 themes; facing-axis satisfied via documented alternative close (D-17: mesh identity correct, residual = camera-azimuth-only, fixed in-task; ROADMAP treats this as satisfying) | ✓ VERIFIED | `VfsTree.tsx:373` non-override label = `--color-text-muted` (bumped from `--color-text-faint`, D-16 discretion token). `orientation.ts` `SWG_ORIENTATION = Euler(0,0,0)` (identity, det +1, no mirror). D-17 human-verify checkpoint APPROVED 2026-07-04. Camera-azimuth SIE-match fix present in `StaticMeshView.tsx:344-353` (default azimuth → -X/+Z quadrant matching SIE + OrbitControls re-target, commit a06b98a). orientation suite spot-checked green. |

**Score:** 5/5 truths verified

### Folded Ride-Along Items (part of the 8-item phase goal)

| Item | Status | Evidence |
|------|--------|----------|
| `eft-parser-completion` (D-19) | ✓ VERIFIED | `Effect.cpp` implements PTXM, PPSH, and STAG (fixed-function) sampler paths, ground-truth-cited (ShaderImplementation.cpp:2458-2651/3113-3181). CI-enforced synthetic FORM-STAG fixture `buildSyntheticStagEftFixture()` in `mesh-roundtrip.test.ts` (committed bytes, `it('CI-ENFORCED: ...')`, no client install) + local-only real-fixture (`a_envmask_specmap.eft`) asserting MAIN sampler role. (The pre-existing `contract-conformance.test.ts` R5 TODO is from Phase 2 commit 5b15cc6 — NOT introduced this phase — and is superseded by the new CI-enforced fixture.) |
| `product-thesis…` codec interface (D-21) | ✓ VERIFIED | `TreCodec.{h,cpp}` codec seam; `codecForCompressor`/`codecForBlockCompressor`/`codecForTreVersion` used in BOTH `TreArchive.cpp` (read: entry + TOC + name-block) and `TreBuilder.cpp` (write). Round-3 widened to cover TOC/name-block path. Static-zlib link preserved. tre-codec 8/8 (per REVIEW-FIX rebuild). |
| `product-thesis…` server push (D-22) | ✓ VERIFIED | `core3ServerPush.ts` (Lua-array `config-local.lua` injection + `extractBareTrePath` bare-global resolver, d60b29f) + `swgMainServerPush.ts` (`[SharedFile]` loose override). `VcsPanel.tsx` "Server Push" section dispatches on `serverConfig.type` → `pushCore3TreOverride`/`pushSwgMainOverride`, disk-backed record rehydration, reset-never-clears-record contract. Verified findings in `docs/05-server-integration/core3-parity.md` (869 ln) + `swg-main-parity.md`. D-22 real-server round-trip checkpoint APPROVED 2026-07-06 (core3-wsl2). core3ServerPush 14/14, swgMainServerPush (spot-checked green). |

### Required Artifacts (all Levels: exists / substantive / wired / data-flows)

| Artifact | Status | Notes |
|----------|--------|-------|
| `services/deleteProject.ts` (182 ln) | ✓ VERIFIED | restore-first + park; called from confirm modal on both surfaces |
| `services/deploymentReset.ts` (114 ln) | ✓ VERIFIED | shared dispatch, `cleanupArtifacts` opt-out; used by DeployDialog + deleteProject |
| `state/deleteUndoStore.ts` (194 ln) | ✓ VERIFIED | session-scoped park/restore; WR-05 partial-park fix applied |
| `panels/deploy/DeleteProjectConfirmModal.tsx` (304 ln) | ✓ VERIFIED | descriptive numbered plan lines; wired to deleteProject |
| `panels/deploy/{ProjectListDialog,WorkspaceEntry}.tsx` | ✓ VERIFIED | kebab + inline dimmed recoverable rows (sketch 017 elements) |
| `services/logService.ts` + `state/logStore.ts` | ✓ VERIFIED | module-scope capture + main-log IPC; WR-11 VITEST guard fix |
| `panels/{ConsolePanel,LogPanel}.tsx` | ✓ VERIFIED | registered in WorkspaceShell; useMemo filter (infinite-loop fix) |
| `shell/StatusBar.tsx` | ✓ VERIFIED | reactive mesh name/vert binding |
| `panels/tre/VfsTree.tsx` | ✓ VERIFIED | non-override label → `--color-text-muted` |
| `panels/viewport/orientation.ts` | ✓ VERIFIED | identity per D-17; single source of truth |
| `services/core3ServerPush.ts` + `swgMainServerPush.ts` | ✓ VERIFIED | wired into VcsPanel Server Push section |
| `native-core/.../Effect.cpp`, `TreCodec.*`, `TreArchive.cpp`, `TreBuilder.cpp` | ✓ VERIFIED | eft parser + codec seam; CR-01/WR-01/07/08/09/10 fixes applied, rebuilt clean |
| `e2e/{06-delete-flow,07-deploy-flow}.spec.ts` + fixtures | ✓ VERIFIED | real-Electron; in CI spec list |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Delete/undo/log suites | `vitest run deleteProject/deleteUndoStore/logService` | 3 files / 23 tests passed | ✓ PASS |
| Server-push/statusbar/orientation suites | `vitest run core3/swgMain/StatusBar/orientation` | 4 files / 37 tests passed | ✓ PASS |
| Full workspace suite (authoritative, per prompt + REVIEW-FIX) | `pnpm test` (post native rebuild, 2026-07-06) | 58 files, 449 passed / 2 skipped, 0 failures | ✓ PASS |

### Requirements Coverage

Phase is **todo-driven** — no new parent REQ-IDs. All 15 plans declare only todo-name requirements; all 8 todo items are covered by verified truths/artifacts above. `REQUIREMENTS.md` maps **no** parent REQ-ID to Phase 04.4 (confirmed — no orphaned requirements). STATE.md's TRE-05 / DEPLOY-01/02/03/08 / CLIENT-02 associations are prior-phase-complete requirements this phase *hardens* (via the e2e loop + delete restore + server push), not newly claimed — consistent, no gap.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| (phase-modified source) | TBD/FIXME/XXX debt markers | ℹ️ None | Scan of all phase-modified deliverables (deleteProject, deleteUndoStore, logService, core3/swgMain push, VcsPanel, Effect.cpp, TreArchive, Zlib, orientation, VfsTree, StatusBar, e2e specs) found ZERO debt markers. |
| `harness/contract-conformance.test.ts` | `TODO(R5)` samplers-may-be-[] | ℹ️ Info | Pre-existing from Phase 2 (commit 5b15cc6), NOT modified this phase — outside the debt-marker gate. Superseded by the new CI-enforced synthetic STAG fixture. |

### Human Verification Required

None outstanding. The phase's two designated human checkpoints are already approved by the maintainer:
- **D-17** (04.4-04 viewport orientation vs SIE) — approved 2026-07-04.
- **D-22** (04.4-15 real-server push round-trip vs live Core3 WSL2) — approved 2026-07-06 (the live run found + fixed the bare-global TrePath resolver defect, commit d60b29f).

The three "requires human verification" notes in 04.4-REVIEW-FIX.md (WR-01/05/06) are **test-coverage advisories**, not mandatory UAT gates: WR-01 (SCAP-LE) is verified against the project's #1 ground truth (client source `ShaderImplementation.cpp`), and WR-05/06 are covered by passing suites. Recorded as warnings (see frontmatter), non-blocking.

### Gaps Summary

No blocking gaps. All 5 ROADMAP success criteria are observably true in the current codebase; the three folded items (eft parser, codec interface, server push) are implemented and wired; both human checkpoints are approved; the full test suite is green (spot-checked: 60 tests across 7 phase suites passing). The code review's 12 critical/warning findings were all fixed on the current code and re-verified (native rebuild clean).

Three non-blocking warnings are recorded for follow-up (added test coverage for WR-01/05/06 edge paths, the IN-05 core3-parity.md doc note, and the two documented pre-existing e2e/04-workspace flakes). None obstruct Phase 5.

---

_Verified: 2026-07-06T22:40:00Z_
_Verifier: Claude (gsd-verifier)_
