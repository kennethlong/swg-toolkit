---
phase: 04.3-versioning-and-searchtoc-mount
plan: 12
type: execute
wave: 3
depends_on: ["04.3-10", "04.3-11"]
files_modified:
  - .planning/research/CONSULT-04.3-12-codex-tre-readpath.md
  - .planning/research/CONSULT-04.3-12-cursor-toc-bytemap.md
  - .planning/research/CONSULT-04.3-12-sonnet-lateral.md
  - .planning/research/CONSULT-04.3-12-opus-spec.md
  - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md
autonomous: true
requirements: [MOUNT-03, MOUNT-06]
must_haves:
  truths:
    - "The de-anchoring crew (Codex/Cursor/Sonnet/Opus) has cross-checked the Pillar B mount changes against real swg-client-v2 source + real .toc/.tre bytes + swg-blender-plugin/tre_reader.py, on NON-overlapping angles, BEFORE the combined UAT (D-16)."
    - "The native byte-exact v6000 test passes on a real swg-client-v2 v6000 archive (skipIf-absent), and the FULL vitest suite across packages is green."
    - "Any crew-found defect or byte-layout disagreement is reconciled against ground truth (real source + bytes), not crew consensus — and either fixed (loop back) or recorded with a maintainer-facing note."
  artifacts:
    - path: ".planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md"
      provides: "reconciled crew findings + byte-exact result + go/no-go for the UAT"
      min_lines: 20
  key_links:
    - from: "04.3-12-GATE-FINDINGS.md"
      to: "real swg-client-v2 source + real .toc/.tre bytes + tre_reader.py"
      via: "ground-truth reconciliation"
      pattern: "swg-client-v2|tre_reader"
---

<objective>
The D-16 verification gate (Wave 2). Before the combined in-client UAT, run the de-anchoring crew against
ground truth and confirm the native byte-exact v6000 test — the project's #1 constraint
("verify formats against ground truth"; AI consensus is NOT evidence). Pillar B is format-heavy and the
memory `feedback-crew-catches-what-plancheck-cannot` shows plan-check PASS ≠ byte-correct (04.2: 8 defects
found post-PASS).

This gates plans 10+11 (the mount changes) before the UAT. Lead the crew with measured ground truth as
LOCKED axioms; fan out on non-overlapping angles; convergence-from-divergence is the signal.

Purpose: catch any byte-layout / read-path defect in the searchTOC/v6000 mount before the in-client UAT.
Output: 4 crew briefs/outputs + a reconciled GATE-FINDINGS doc with a go/no-go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-10-SUMMARY.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-11-SUMMARY.md
@CLAUDE.md

<interfaces>
LOCKED ground-truth axioms (RESEARCH § Measured ground truth — hand to the crew as "do NOT re-derive"):
  - patch_sku3_24_shared_00.tre = EERT6000, numberOfFiles=0, 78 9c plain-zlib at byte 36 (NOT encrypted).
  - Master .toc entry 24B LE: compressor@0, treeFileIndex@2(u16), crc@4, fileNameLength@8, offset@12, length@16, compressedLength@20.
  - TRE TOC CRC-FIRST all versions; V6000 stride 32; size-first layout FALSIFIED — banned.
  - v6000 is DUAL-format: swg-source = plain zlib (READABLE); Restoration = encrypted (degrade). Per-payload, not version-tag.
Ground-truth sources (read access): ../swg-client-v2 (TreeFile.cpp / TreeFile_SearchNode.{h,cpp}), ../swg-blender-plugin/swg_pipeline/tre_reader.py + tre_decrypt.py, real bytes under D:/Code/SWGSource Client v3.0/.
Crew invocation (CLAUDE.md "Phone a friend"): Codex via codex.cmd exec (PATH+nodejs, stdin prompt); Cursor via cursor-agent.cmd -p --mode ask --trust; fresh Sonnet + fresh Opus via Agent tool. Write briefs to .planning/research/CONSULT-04.3-12-*.md, outputs to *.out.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-anchoring crew cross-check on non-overlapping angles</name>
  <read_first>
    - CLAUDE.md ("Phone a friend" crew invocation paths + "Ground truth beats consensus" de-anchoring protocol)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-10-SUMMARY.md + 04.3-11-SUMMARY.md (what the mount changes actually did)
    - packages/renderer/src/services/treAutoMount.ts + tocReader.ts (the as-merged read path)
    - packages/native-core/modules/core/tre/TreArchive.cpp (extractEntry/extractAt as-merged)
    - ../swg-blender-plugin/swg_pipeline/tre_reader.py (the working oracle reader)
  </read_first>
  <files>.planning/research/CONSULT-04.3-12-codex-tre-readpath.md, .planning/research/CONSULT-04.3-12-cursor-toc-bytemap.md, .planning/research/CONSULT-04.3-12-sonnet-lateral.md, .planning/research/CONSULT-04.3-12-opus-spec.md</files>
  <action>
    Write four crew briefs on NON-overlapping angles, each LEADING with the LOCKED ground-truth axioms (above) marked "do NOT contradict or re-derive" and the FALSIFIED size-first layout marked BANNED — hand neutral evidence (real source excerpts + the merged read-path), NEVER your hypothesis. Angles: (Codex) trace the as-merged master-.toc entry-sourcing + extractAt call graph vs swg-client-v2 SearchTOC::localExists + the container read path — does our offset/length/compressor sourcing match the engine? (Cursor) byte-map a real sku3 entry: master .toc descriptor → container .tre offset → inflate, vs tre_reader.read_tre_payload — do the bytes line up? (Sonnet) lateral: what could make patch_sku3_* still show 0 entries or a wrong winner after the change (dedup/Pitfall 4, TOCTreePath prefix, priority ties)? (Opus) spec/math: is the per-payload inflate-then-classify correct for BOTH swg-source (plain zlib) and Restoration (encrypted) without false-positives? Run them fire-and-continue per CLAUDE.md (Codex/Cursor by full path; Sonnet/Opus via Agent tool); capture each output to a `.out`. Reconcile: a productive SPLIT on independent angles that all confirm the bytes is the success signal; if any consultant contradicts the measured bytes, the BYTES win — investigate + fix in plan 10/11 (loop back) before proceeding.
  </action>
  <acceptance_criteria>
    - Four CONSULT briefs written, each leading with the locked axioms + banned falsified layout; four `.out` outputs captured.
    - Each angle is distinct (no two consultants given the same question) — convergence-from-divergence.
    - Any consultant-vs-bytes disagreement is resolved in favor of the measured bytes/real source, with the resolution recorded (and a fix looped back to plan 10/11 if a real defect).
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/research/CONSULT-04.3-12-*.md | wc -l | grep -q 4 && echo OK</automated>
  </verify>
  <done>Crew cross-checked the mount read path against ground truth on 4 non-overlapping angles; disagreements reconciled to the bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Native byte-exact v6000 test + full-suite gate + GATE-FINDINGS</name>
  <read_first>
    - packages/harness/test/tre-v6000-swgsource-byteexact.test.ts + packages/native-core/test/tre-extract-at-descriptor.test.ts (the byte-exact assertions)
    - packages/harness/fixtures-real/toc/ (the swg-source v6000 fixture from plan 03)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-VALIDATION.md (phase-gate sampling: full suite + D-16 before verify-work)
  </read_first>
  <files>.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md</files>
  <action>
    Run the native byte-exact v6000 test against a real swg-client-v2 / SWGSource v6000 archive (skipIf-absent) and the externally-supplied-descriptor extract test — both must PASS when the fixture is present. Run the FULL vitest suite across packages (`npm run test`) — must be green (MOUNT-06 regression incl.). Write `04.3-12-GATE-FINDINGS.md` reconciling: the four crew angles' verdicts, the byte-exact result (pass/skip + which real asset), any defects found + their fix (or a maintainer-facing note if a byte-layout question remains genuinely open), and an explicit GO / NO-GO for the combined UAT (plan 13). NO-GO if the byte-exact test fails on a present fixture or the crew surfaced an unreconciled byte disagreement.
  </action>
  <acceptance_criteria>
    - Native byte-exact v6000 test + descriptor extract test PASS (or skip cleanly when the real asset is absent) — never silently pass without the assertion.
    - Full `npm run test` green across packages (MOUNT-06 regression confirmed).
    - GATE-FINDINGS.md records the crew verdicts + byte-exact result + an explicit GO/NO-GO; NO-GO blocks the UAT until resolved.
  </acceptance_criteria>
  <verify>
    <automated>npm run test 2>&1 | tail -5 | grep -qiE "pass|✓" && test -f .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md && echo OK</automated>
  </verify>
  <done>Byte-exact v6000 confirmed + full suite green; GATE-FINDINGS records crew verdicts + GO/NO-GO for the UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| crew outputs → merge decision | External AI outputs inform the gate but do NOT override measured ground truth. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-12-01 | Tampering | false crew consensus re-deriving a wrong byte layout | mitigate | De-anchoring protocol: lead with locked measured axioms, ban the falsified layout, fan out on non-overlapping angles; bytes win over consensus (CLAUDE.md). |
| T-04.3-12-02 | Repudiation | shipping a byte-layout defect undetected (plan-check PASS ≠ byte-correct) | mitigate | Native byte-exact test on a REAL asset + crew cross-check before UAT (memory feedback-crew-catches-what-plancheck-cannot). |
| T-04.3-SC | Tampering | crew tools run untrusted | mitigate | Codex --sandbox read-only; Cursor --mode ask (read-only); no installs; outputs are advisory text only. |
</threat_model>

<verification>
- Four CONSULT briefs + outputs exist; byte-exact test pass/skip; full suite green; GATE-FINDINGS with GO/NO-GO.
- GO required before plan 13 (combined UAT).
</verification>

<success_criteria>
The searchTOC/v6000 mount is cross-checked against real source + real bytes on non-overlapping angles, the
native byte-exact v6000 test passes on a real asset, the full suite is green, and an explicit GO is recorded
for the combined in-client UAT.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-SUMMARY.md` when done.
</output>
