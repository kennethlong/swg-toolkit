---
phase: 04.3
slug: versioning-and-searchtoc-mount
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-29
---

# Phase 04.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `04.3-RESEARCH.md` § Validation Architecture (verified against package.json).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.9 (unit/integration) + Playwright 1.61.0 (E2E); native-core has its own cmake-js test target |
| **Config file** | per-package `vitest` config |
| **Quick run command** | `npm run test` (`vitest run`, relevant package) |
| **Full suite command** | `npm run test && npm run test:e2e` |
| **Estimated runtime** | ~60–120 seconds (unit); E2E longer |

---

## Sampling Rate

- **After every task commit:** Run `npm run test` (relevant package)
- **After every plan wave:** Run `vitest run` across packages
- **Before `/gsd:verify-work`:** Full suite green + the D-16 de-anchoring crew + byte-exact v6000 test, BEFORE the combined in-client UAT (D-03)
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

> Final task IDs assigned by the planner. Requirement IDs are the candidate set from RESEARCH.md
> (planner reconciles against REQUIREMENTS.md — TRE-05 parent exists for Pillar B; DEPLOY-03/08 for Pillar A).

| Requirement | Behavior | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|-------------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| VER-02 | `syncLiveToVersion` forward/back/forward idempotent; live bytes == version bytes; Baseline == stock | V5/V12 | writes route through `deployLoose`/`cfgActivator` (path-safe, B3 snapshot) | unit | `vitest run syncLiveToVersion` | ❌ W0 | ⬜ pending |
| VER-02 | loose AND cfg model both reconcile (sha256 match) | V12 | atomic tmp+rename; never clobber working cfg | unit | extends `looseOverrideDeploy.test.ts` / cfgActivator tests | partial | ⬜ pending |
| VER-05 | confirm fires only when uncommitted work would be discarded (D-07) | — | N/A | unit/component | RTL on VersionHistoryBody | ❌ W0 | ⬜ pending |
| VER-04 | session undo stack reverses a reconcile (D-06); invoked via Undo button / Ctrl+Z | — | N/A | unit | `vitest run undoStore` | ❌ W0 | ⬜ pending |
| GRAPH-01..08 | lane layout for no-branch / single-branch / deep-branch / branch-off-non-tip | — | N/A | unit | `vitest run laneLayout` | ❌ W0 | ⬜ pending |
| GRAPH render | built surface matches 002-A elements (A1–A13) | — | N/A | manual (UAT) + component snapshot | sketch-diff in combined UAT | manual | ⬜ pending |
| MOUNT-01/02 | searchTOC mount sources master-`.toc` entries; `patch_sku3_*` entryCount > 0; no double-count | V5 | offsets/lengths bounds-checked (subtraction form) | unit/integration | extends `treAutoMount.test.ts` / `tocReader.test.ts` | partial | ⬜ pending |
| MOUNT-03 | v6000 master-sourced payload extracts byte-identical to `tre_reader.read_tre_payload()` | V5 | zlib bomb cap (256MB) | native byte-exact, skipIf-absent | harness `assertRoundTrip` on swg-source v6000 fixture | ❌ W0 | ⬜ pending |
| MOUNT-04 | Restoration v6000 degrades to enumerate-only (no crash); swg-source v6000 reads | V5 | graceful-degradation, no fatal | native unit | extends native TRE tests | partial | ⬜ pending |
| MOUNT-06 | v5000 self-indexed + searchTree clients unaffected (regression) | — | N/A | unit/integration | existing `treAutoMount.test.ts` green | ✅ exists | ⬜ pending |
| MOUNT-07 | lazy searchPath: no flat VFS rows for loose dir; loose winner still resolves via `readVfsEntryBytes` | V12 | N/A | unit | extends `treAutoMount.test.ts` | partial | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `services/syncLiveToVersion.test.ts` (+ `syncLiveToVersion.ts`) — VER-02/03 idempotence, loose+cfg, Baseline restore-to-stock
- [ ] `panels/deploy/laneLayout.test.ts` (+ `laneLayout.ts`) — GRAPH lane assignment fixtures (no-branch / one-branch / deep / branch-off-non-tip)
- [ ] **swg-source v6000 byte-exact fixture** — extract a small `patch_sku3_*` payload + expected inflated bytes into `packages/harness/fixtures-real/toc/` (or `tre/`); skipIf-absent. *Currently only `sku0_client_header.bin` + Restoration v6000 archives exist — NO swg-source v6000 plain-zlib fixture is committed.* Source: `D:/Code/SWGSource Client v3.0/patch_sku3_24_shared_00.tre` + `sku3_client.toc`
- [ ] native extract test for the externally-supplied-entry-descriptor path (master-`.toc`-sourced extraction)
- [ ] `state/undoStore.test.ts` — session undo stack (D-06)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Branch-tree render matches sketch 002-A element-by-element | GRAPH-01..08 | Visual fidelity to the UI contract; sketch-diff is a human/visual judgment | Combined in-client UAT: open Deploy panel with a branched project; diff each A1–A13 element vs `002-version-graph-timeline/index.html` |
| Navigate tree = silent reconcile on a real client | VER-01..09 | Requires a running client + live filesystem deploy/revert | Combined UAT (autonomous:false) on BOTH a searchTree client (SWGEmu/Infinity) and the searchTOC dev client (swg-client-v2); D-03 |
| Full v6000 base readable in the mounted VFS | MOUNT-01..07 | Requires the real SWG-Source Client v3.0 data root | Combined UAT: mount swg-client-v2 stage cfg; confirm `patch_sku3_*` content browses + opens in viewer |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (5 items above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter (after Wave 0 stubs exist)

**Approval:** pending
