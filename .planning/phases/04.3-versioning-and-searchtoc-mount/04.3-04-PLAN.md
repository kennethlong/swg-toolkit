---
phase: 04.3-versioning-and-searchtoc-mount
plan: 04
type: execute
wave: 2
depends_on: ["04.3-02"]
files_modified:
  - packages/renderer/src/services/syncLiveToVersion.ts
  - packages/renderer/src/state/undoStore.ts
  - packages/renderer/src/services/changesetService.ts
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-06, VER-09]
must_haves:
  truths:
    - "syncLiveToVersion(targetId) makes the live override set EQUAL flatten(target) in ONE diff-and-apply op covering forward-apply AND backward-revert; idempotent forward→back→forward; Baseline = full restore-to-stock."
    - "Reconcile REVERTS the live deployment using the LIVE version's stored model and APPLIES the target using the TARGET version's stored model (H4 split, VER-01) — Baseline is revert-only; never a single-model dispatch on the target."
    - "Reconcile works for BOTH a loose-override client and an absolute/cfg client (VER-09)."
    - "Every reconcile pushes a session-undo snapshot BEFORE mutating; undo reverses it (VER-04)."
    - "A single 'live' pointer: setLiveVersion(id) moves activeVersionId + deployedVersionId together so they never diverge (VER-06); fields kept for migration safety."
  artifacts:
    - path: "packages/renderer/src/services/syncLiveToVersion.ts"
      provides: "the reconcile engine (loose path via deployLoose/resetLoose; cfg path via restoreCfg/activatePatch)"
      min_lines: 60
    - path: "packages/renderer/src/state/undoStore.ts"
      provides: "session undo stack implementation (push/undo/clear)"
    - path: "packages/renderer/src/services/changesetService.ts"
      provides: "setLiveVersion(id) atomic single-pointer mover"
      contains: "setLiveVersion"
  key_links:
    - from: "syncLiveToVersion.ts"
      to: "looseOverrideDeploy.deployLoose/resetLoose"
      via: "loose model reconcile"
      pattern: "deployLoose|resetLoose"
    - from: "syncLiveToVersion.ts"
      to: "cfgActivator.restoreCfg/activatePatch"
      via: "cfg model reconcile"
      pattern: "restoreCfg|activatePatch"
    - from: "syncLiveToVersion.ts"
      to: "undoStore.push"
      via: "snapshot before mutate"
      pattern: "push\\("
---

<objective>
Implement the Pillar A reconcile engine — the genuinely-new logic of this phase. `syncLiveToVersion`
is a thin diff-and-apply over the existing, tested deploy primitives (flatten / deployLoose / resetLoose
/ cfgActivator), backed by a session undo stack and a single "live" pointer (D-08).

Turns the Wave-0 RED tests (plan 02) GREEN. NO UI in this plan — VersionHistoryBody wiring is plan 06,
which imports these now-implemented modules (the Wave-0 stubs gave it the contract).

Purpose: navigating the version tree becomes deploy/revert (D-04) with one reversible operation.
Output: implemented syncLiveToVersion.ts, undoStore.ts, changesetService.setLiveVersion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/todos/pending/version-navigation-live-sync-deploy-model.md

<interfaces>
From changesetService.ts:
  flatten(versionId|null, manifest, studioDir): StagingEntry[]   // flatten(null)=[]; Baseline flattens to []
  flatEqual(a, b): boolean
  selectVersion(id|null): void      // moves manifest.activeVersionId
  setDeployedVersion(id|null): void // moves manifest.deployedVersionId
  setLiveVersion(id|null): void     // STUBBED in plan 02 — implement here (moves BOTH pointers; live === deployedVersionId)
  updateChangesetDeployRecord(versionId, record): void // persists a version's deployRecord (reuse — DeployDialog already calls it)
From looseOverrideDeploy.ts:
  deployLoose(entries, overrideDir, { studioDir, priorRecord }): LooseDeployRecord  // B3-snapshots + prunes priorRecord
  resetLoose(record): void                                                          // restores B3 / deletes toolkit files
  resolveOverrideDir(cfgPath, installRoot): string | null                           // looseDirs[0] (max priority) — VERIFIED string|null; throw clearly on null, never write to null
From cfgActivator.ts:
  activatePatch(...) / deactivatePatch(record) / restoreCfg(rootCfgPath, snapshotPath)
SwgChangeset.deployRecord: CfgDeployRecord | LooseDeployRecord  // shape discriminates the model
manifest pointers: activeVersionId (edited) + deployedVersionId (live) — VersionHistoryBody:66-67 computes 'stale' when they differ
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement syncLiveToVersion reconcile engine</name>
  <read_first>
    - packages/renderer/src/services/syncLiveToVersion.ts (the Wave-0 stub — implement its signature)
    - packages/renderer/src/services/changesetService.ts:139-225 (flatten + flatEqual — exact behavior)
    - packages/renderer/src/services/looseOverrideDeploy.ts:47-201 (deployLoose/resetLoose/resolveOverrideDir + B3 + priorRecord pruning)
    - packages/renderer/src/services/cfgActivator.ts:146-263 (activatePatch/deactivatePatch/restoreCfg)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (§ Pillar A — pseudocode + design decisions 1–4)
    - packages/renderer/src/services/syncLiveToVersion.test.ts (the RED cases this must satisfy)
  </read_first>
  <files>packages/renderer/src/services/syncLiveToVersion.ts</files>
  <action>
    Implement `syncLiveToVersion(targetId, ctx)`: compute `desired = flatten(targetId, manifest, studioDir)` and `live = flatten(ctx.liveVersionId, ...)`; if `flatEqual(desired, live)` return `{ noop: true }`. Push a snapshot to undoStore BEFORE mutating (Task 2 store). CRITICAL (H4 — dispatch on the RIGHT model): compute `revertModel = deployModelOf(LIVE version)` and `applyModel = deployModelOf(target)` SEPARATELY — do NOT dispatch the whole reconcile on the target's model (that breaks loose-live->Baseline: Baseline has no deployRecord -> 'cfg' -> restoreCfg never removes the loose override/ files). Step 1 REVERT the current live deployment using revertModel: loose -> `resetLoose(ctx.priorLiveLooseRecord)` (removes the loose override files); cfg -> `restoreCfg`/`deactivatePatch(priorCfgRecord)`. Step 2 APPLY the target using applyModel: loose -> `deployLoose(desired, resolveOverrideDir(ctx.cfgPath, ctx.installRoot), { studioDir, priorRecord: ctx.priorLiveLooseRecord })` (throw clearly if resolveOverrideDir returns null); cfg -> `activatePatch` for the target build. Baseline (desired empty) is REVERT-ONLY — run only Step 1 (revertModel), no apply (full restore-to-stock). SOURCE `ctx.priorLiveLooseRecord`/priorCfgRecord from the LIVE version's PERSISTED deployRecord (changesetService manifest), and after a successful apply PERSIST the returned record via `updateChangesetDeployRecord(targetId, record)` so the next hop's priorRecord is not stale (orphaned-file guard). Implement `deployModelOf` by inspecting `deployRecord` shape (LooseDeployRecord => 'loose', else 'cfg'); a Baseline/no-record target is the revert-only branch. Return `LiveReconcileResult` with the new record + applyModel. Do NOT inline any path-safety validator — deployLoose/cfgActivator already gate writes (V5). Route ALL writes through them (no direct fs writes here).
  </action>
  <acceptance_criteria>
    - All 6 RED cases in syncLiveToVersion.test.ts pass (incl. (f) loose-live->Baseline calls resetLoose, NOT restoreCfg): idempotent forward->back->forward; Baseline restore-to-stock; loose routes through deployLoose w/ priorRecord; cfg routes through restoreCfg+activatePatch; flatEqual no-op returns noop:true.
    - revert dispatches on the LIVE version's model and apply on the TARGET's model (H4) — a loose-live->Baseline reconcile calls resetLoose (loose files removed) and never restoreCfg; a cfg-live->loose-target reconcile deactivatePatch-es the live cfg before deployLoose (spies assert).
    - The returned record is persisted via updateChangesetDeployRecord(targetId, record); priorRecord is sourced from the live version's persisted deployRecord — forward->back->forward across TWO cycles leaves no orphaned override files (asserted).
    - No direct `fs.writeFile`/`fs.rm` in syncLiveToVersion.ts (grep -v '^\s*//' | grep -cE "fs\\.(write|rm|unlink)" == 0) — all mutation flows through deployLoose/resetLoose/cfgActivator.
    - `deployModelOf` returns 'loose' for a LooseDeployRecord and 'cfg' otherwise; a Baseline/no-record target is revert-only (unit-asserted).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/services/syncLiveToVersion.test.ts</automated>
  </verify>
  <done>syncLiveToVersion reconciles both models, idempotent, no-op fast path, undo snapshot taken; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session undo stack</name>
  <read_first>
    - packages/renderer/src/state/undoStore.ts (Wave-0 stub)
    - packages/renderer/src/state/undoStore.test.ts (RED LIFO/clear/session-only cases)
    - packages/renderer/src/state/changesetStore.ts (Zustand patterns)
  </read_first>
  <files>packages/renderer/src/state/undoStore.ts</files>
  <action>
    Implement `useUndoStore`: `push(snapshot)` appends; `undo()` pops + returns the last `ReconcileSnapshot` (LIFO); `clear()` empties; `canUndo` derived from stack length. Keep it session-scoped — plain in-memory Zustand, no persist middleware, no localStorage. The snapshot carries `{ priorLiveVersionId, priorDeployRecord?, takenAt }` so a caller can reverse a reconcile by re-running syncLiveToVersion to priorLiveVersionId.
  </action>
  <acceptance_criteria>
    - undoStore.test.ts passes: push→canUndo; undo LIFO + pop; clear; no persistence side effect.
    - No `persist(` / `localStorage` reference in undoStore.ts (grep -c == 0).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/state/undoStore.test.ts</automated>
  </verify>
  <done>Session undo stack implemented; LIFO + session-only verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single live pointer — changesetService.setLiveVersion</name>
  <read_first>
    - packages/renderer/src/services/changesetService.ts:398-434 (selectVersion + setDeployedVersion)
    - packages/renderer/src/panels/deploy/VersionHistoryBody.tsx:66-67 (the 'stale' computation D-08 retires — for context; do not edit this file here)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (design decision 2 — keep both fields, set together)
  </read_first>
  <files>packages/renderer/src/services/changesetService.ts</files>
  <action>
    Implement the plan-02 `setLiveVersion(id: string | null): void` STUB (it currently throws 'not implemented (W1-04)') so it moves BOTH `manifest.activeVersionId` and `manifest.deployedVersionId` to `id` atomically (one persist), invariantly equal (D-08). CANONICAL RULE (stated once, here): live === `deployedVersionId`; setLiveVersion is the ONLY mover of the live pointer; `selectVersion`/`setDeployedVersion` remain for storage compat but the navigate path (plan 06) AND the forward-deploy success path (plan 07) call setLiveVersion. Do NOT delete the two fields (migration risk per RESEARCH A5). Add a focused unit test asserting setLiveVersion sets both pointers equal and persists once.
  </action>
  <acceptance_criteria>
    - `setLiveVersion(x)` results in `manifest.activeVersionId === manifest.deployedVersionId === x` (unit-asserted).
    - Both fields still present in the persisted manifest shape (no field deletion) — existing changesetService tests stay green.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/services/changesetService 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>setLiveVersion moves both pointers together; fields preserved; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reconcile service → client filesystem | Reconcile writes/reverts loose override files + cfg edits on the user's real client. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-04-01 | Tampering | path traversal in reconcile writes (../, drive-letter) | mitigate | All writes route through deployLoose/cfgActivator which already call isVirtualPathSafe + isConfinedToRoot (looseOverrideDeploy.ts:102-118). No new validator inlined (V5). |
| T-04.3-04-02 | Tampering/availability | clobbering a working client cfg on revert | mitigate | cfg path uses restoreCfg byte-pristine restore + B3 snapshot; never `git commit -a`-style blanket write (V12). |
| T-04.3-04-03 | Tampering | losing the ability to revert (no undo) | mitigate | undo snapshot pushed BEFORE every mutation (D-06); session stack reverses. |
| T-04.3-04-04 | DoS | full re-deploy of a many-file version (A4) | accept | Per-file diff is a deferred optimization; reset-prior+deploy-target is correct and bounded by version size. |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; reuses existing services only. |
</threat_model>

<verification>
- `vitest run` (renderer) green for syncLiveToVersion, undoStore, changesetService.
- grep gates: no direct fs mutation in syncLiveToVersion.ts; no persist/localStorage in undoStore.ts.
</verification>

<success_criteria>
The reconcile engine reconciles to any version (forward/back) idempotently for both loose and cfg models,
restore-to-stock at Baseline, every reconcile undoable, single live pointer invariant. Wave-0 Pillar-A
tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-04-SUMMARY.md` when done.
</output>
