---
phase: 04.3-versioning-and-searchtoc-mount
plan: 12
status: complete
wave: 3
requirements: [MOUNT-03, MOUNT-06]
gate_verdict: GO
completed: 2026-06-30
---

# 04.3-12 SUMMARY — D-16 de-anchoring crew gate (Pillar B searchTOC/v6000)

## Objective
Before the combined in-client UAT, cross-check the Pillar B searchTOC/v6000 read path (plans 10/11)
against ground truth via the de-anchoring crew on non-overlapping angles + the native byte-exact test —
catch any byte-layout / read-path / integration defect that plan-check cannot. Output: 4 crew briefs +
outputs + a reconciled GATE-FINDINGS with an explicit GO/NO-GO.

## What happened
The gate did its job — it caught a **blocking integration defect before the UAT**.

**Task 1 — crew (4 non-overlapping angles, each led with LOCKED axioms + banned falsified layout):**
- **Codex** (call-graph): read path engine-faithful; field sourcing matches `SearchTOC` /
  `TreeFile_SearchNode`; flagged int32 narrowing (F-5) and that axiom-2 `compressor@0(u16)` is really
  `u8+u8` (our code matches the engine).
- **Cursor** (byte-map): real `appearance/lod/thm_must_droid_factory_exterior.lod` descriptor + container
  slice + inflate all **byte-identical** to the committed fixture; master-`.toc` sourcing confirmed
  necessary (oracle `read_tre_payload` KeyErrors on `numberOfFiles=0`). No layout disagreement.
- **Sonnet** (lateral): found F-2 (extraction unwired in UI — the blocker), F-3 (shadow/override
  double-count), F-6/F-7 (edge/benign).
- **Opus** (spec/math): per-payload classify sound for the axioms; F-4 (encrypted false-positive guard
  weaker than oracle). His specific content-CRC suggestion was later falsified against the bytes.

Convergence-from-divergence held: Cursor confirmed the happy-path bytes; Codex + Sonnet independently
reached the same root cause (`TreExtractDescriptor` / `TreMount::resolve` don't carry/consult external
master-.toc entries).

**Task 2 — byte-exact + full-suite + GATE-FINDINGS:** initial verdict **NO-GO** (F-2). Loop-back fixes
applied to plans 10/11 (F-2..F-5 + v6000 test reconciliation, 7 commits), then re-verified **GO**.

## Key findings + resolution
See `04.3-12-GATE-FINDINGS.md` (verdict: GO) for the full reconciled register. Highlights:
- **F-2 BLOCKING** (searchTOC extraction unwired in UI) — fixed end-to-end + integration test.
- **F-4** — the crew's proposed content-CRC check was **falsified against the bytes** (`desc.crc` is the
  path CRC-32, not content); the correct exact-length guard was applied. Bytes beat consensus.
- F-3/F-5 fixed; F-6/F-7 recorded as notes.

## Verification
- Root node suite: 376 passed / 2 skipped (50 files). Renderer jsdom: 258 passed (35 files).
- Byte-exact v6000 + descriptor-extract + F-2 integration: 7 passed / 2 skipped. SWG-Source plain-zlib
  extraction byte-identical (no regression). Native addon rebuilt; zlib still statically linked.

## Deferred (maintainer notes)
- **F-6** — `autoMountClient` passes only `tocTreePaths[0]`; multi-TOCTreePath installs unguarded (edge).
- **F-7** — external `length==0` tombstone flag diverges from engine `SearchTOC::exists()` (benign).
- The `crc` descriptor field is now threaded but only length-validated; a path-CRC cross-check could be
  added later if desired.

## Artifacts
- `.planning/research/CONSULT-04.3-12-{codex,cursor,sonnet,opus}-*.md` (briefs) + `.out` (outputs)
- `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md` (verdict: GO)

## Self-Check: PASSED
Crew ran on 4 non-overlapping angles vs real source + bytes + tre_reader.py; byte-exact test passes on the
real SWG-Source asset (skips when absent); full suite green across packages; every crew defect reconciled
to ground truth (fixed, not papered over — including falsifying one crew suggestion against the bytes);
explicit GO recorded for plan 13.
