---
phase: 04.3-versioning-and-searchtoc-mount
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/harness/fixtures-real/toc/README.md
  - packages/harness/scripts/extract-v6000-fixture.mjs
  - packages/harness/test/tre-v6000-swgsource-byteexact.test.ts
  - packages/native-core/test/tre-extract-at-descriptor.test.ts
  - packages/native-core/index.d.ts
  - packages/renderer/src/services/tocReader.resolveFull.test.ts
autonomous: true
requirements: [MOUNT-03, MOUNT-04, MOUNT-06, MOUNT-01]
must_haves:
  truths:
    - "A committed swg-source v6000 byte-exact fixture (small patch_sku3_* payload + expected inflated bytes) exists OR the extraction script can regenerate it from the real client; the test is skipIf-absent."
    - "A native test scaffold exists for the externally-supplied-entry-descriptor extract path (master-.toc-sourced extraction), RED until plan 10 lands."
    - "The native `extractMountAt(handle, archiveIndex, descriptor)` binding TYPING STUB + the master-.toc external-entries sourcing API (`mountTreMountWithToc`) TYPING STUB are frozen in native-core/index.d.ts (declaration only — runtime lands in plan 10) so plan 11 (Wave 1, parallel) compiles and mocks against the contract — mirrors plan 02's Pillar-A interface-first stubs (resolves checker W2; preserves D-02 parallelism, no plan-11 depends_on / wave cascade)."
    - "A tocReader.resolveFull RED test pins the need to surface offset/length/compressor/compressedLength AND to FILTER length==0 master-.toc tombstones (client localExists rejects them — deletions are not files), RESEARCH A2, RED until plan 11 lands."
  artifacts:
    - path: "packages/harness/scripts/extract-v6000-fixture.mjs"
      provides: "deterministic extraction of one sku3 v6000 payload + expected inflated bytes from the real client"
    - path: "packages/harness/test/tre-v6000-swgsource-byteexact.test.ts"
      provides: "skipIf-absent byte-exact assertion (MOUNT-03)"
      contains: "test.skipIf"
    - path: "packages/native-core/test/tre-extract-at-descriptor.test.ts"
      provides: "native extractMountAt(handle, archiveIndex, descriptor) RED scaffold (master-.toc-sourced extraction)"
    - path: "packages/native-core/index.d.ts"
      provides: "frozen extractMountAt(handle, archiveIndex, descriptor) typing stub + mountTreMountWithToc(paths, priorities, tocPath, tocTreePath) typing stub (declaration only — plan 10 implements runtime)"
      contains: "extractMountAt"
    - path: "packages/renderer/src/services/tocReader.resolveFull.test.ts"
      provides: "resolveFull descriptor RED test"
  key_links:
    - from: "tre-v6000-swgsource-byteexact.test.ts"
      to: "packages/harness/fixtures-real/toc/"
      via: "fixture load + skipIf when absent"
      pattern: "fixtures-real/toc"
---

<objective>
Wave 0 (Nyquist) fixtures + test scaffolds for Pillar B. Extract the one missing ground-truth fixture
(a swg-source v6000 plain-zlib payload + its expected inflated bytes) and stand up the RED tests that
plans 10 (native per-payload extract) and 11 (renderer master-.toc mount) will turn green.

Ground truth (LOCKED, do not re-derive — RESEARCH § Measured ground truth): `patch_sku3_24_shared_00.tre`
is `EERT6000`, numberOfFiles=0, payload `78 9c` plain-zlib begins at byte 36; its entries live in the
master `.toc` (`sku3_client.toc`, 1,158 entries). This is the fixture source.

Purpose: make MOUNT-03/06 byte-exact testable; close the VALIDATION.md Wave-0 Pillar-B gaps; freeze the
native `extractMountAt` + `mountTreMountWithToc` binding contracts so plan 11 runs parallel with plan 10 in Wave 1 (checker W2).
Output: 1 fixture + extraction script + 3 test scaffolds + the native extractMountAt + mountTreMountWithToc typing stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-VALIDATION.md

<interfaces>
From packages/renderer/src/services/tocReader.ts:
  export interface TocIndexEntry { treeFileIndex: number; treName: string }   // currently DISCARDS offset/len/comp
  export interface TocIndex { resolve(virtualPath): TocIndexEntry | undefined; readonly size: number }
  export function readTocIndex(filePath: string): TocIndex
  export function readTocTreeNames(filePath: string): string[]

Master .toc entry (24 bytes LE, VERIFIED tocReader.ts:228-251 + tre_reader.py:380-389):
  compressor:u8@0, unused:u8@1, treeFileIndex:u16@2, crc:u32@4, fileNameLength:u32@8, offset:u32@12, length:u32@16, compressedLength:u32@20
  Tombstone rule: a master-.toc entry with length==0 is a DELETION marker, not a file — resolveFull/resolve MUST skip it (client localExists rejects length==0; tre_reader treats it as absent).
CONTAINER LOCATOR (the H1 fix — offset alone names no file): the native read path is readMountEntry(handle, archiveIndex, entryIndex) -> opens mount.nodeAt(archiveIndex).path (tre_binding.cpp:543-547). So the new descriptor-based extract carries container identity via (handle, archiveIndex): extractMountAt(handle, archiveIndex, { offset, length, compressedLength, compressor }). The master-.toc-sourced ENTRIES reach the columnar VFS via mountTreMountWithToc (extends mountTreMount, index.d.ts:195) so empty-internal-TOC (numberOfFiles=0) v6000 containers contribute rows.

Reference reader (ground-truth oracle): ../swg-blender-plugin/swg_pipeline/tre_reader.py::read_tre_payload + tre_decrypt.py::try_read_tre_payload
Real client data root: D:/Code/SWGSource Client v3.0/ (sku3_client.toc + patch_sku3_24_shared_00.tre)
Existing fixtures dir: packages/harness/fixtures-real/ (currently has sku0_client_header.bin + Restoration v6000 archives only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract swg-source v6000 byte-exact fixture (script + committed fixture)</name>
  <read_first>
    - ../swg-blender-plugin/swg_pipeline/tre_reader.py (read_tre_payload — the byte-exact oracle: offset read + zlib inflate)
    - packages/renderer/src/services/tocReader.ts:196-315 (readTocIndex — to locate one sku3 entry's container/offset/len/comp/crc)
    - packages/harness/fixtures-real/toc/ or fixtures-real/tre/ (existing fixture layout + README conventions)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (§ Measured ground truth; Wave 0 Gaps bullet 3)
  </read_first>
  <files>packages/harness/scripts/extract-v6000-fixture.mjs, packages/harness/fixtures-real/toc/README.md, packages/harness/test/tre-v6000-swgsource-byteexact.test.ts</files>
  <action>
    Write `extract-v6000-fixture.mjs`: given `D:/Code/SWGSource Client v3.0/`, read `sku3_client.toc` via the same byte layout `tocReader` uses, pick ONE small entry whose container is a v6000 `patch_sku3_*` (compressor=2 zlib), read its compressed payload from the container `.tre` at `offset..offset+compressedLength`, and emit two committed fixture files into `packages/harness/fixtures-real/toc/`: `{fixture}.v6000.zlib.bin` (raw compressed bytes as stored) and `{fixture}.v6000.expected.bin` (the zlib-inflated bytes — cross-checked by inflating with node zlib AND noting the tre_reader.py value in the README). Also emit a small `{fixture}.v6000.descriptor.json` ({ container, offset, length, compressedLength, compressor, crc, virtualPath }). Document in `fixtures-real/toc/README.md` the source path, the de-anchoring note (v6000 is DUAL-format; this is the PLAIN-ZLIB swg-source variant, NOT Restoration-encrypted), and the regenerate command. Then write `tre-v6000-swgsource-byteexact.test.ts` using `test.skipIf(!fixtureExists)` that inflates `.v6000.zlib.bin` and asserts it equals `.v6000.expected.bin` byte-for-byte (this is the renderer/zlib sanity oracle; the NATIVE byte-exact assertion is Task in plan 10/12). Commit the fixture bytes (small — single small asset) per the gitignored-real-archives policy exception: this is a deliberately-extracted small fixture, not a full archive.
  </action>
  <acceptance_criteria>
    - Running the script against the real client (when present) produces the 3 fixture files deterministically; re-running yields byte-identical output.
    - `.v6000.descriptor.json` offset/length/compressedLength match the master `.toc` entry for the chosen path (cross-checked against tocReader's parse).
    - `tre-v6000-swgsource-byteexact.test.ts` PASSES when the fixture is present (node-zlib inflate == expected) and SKIPS cleanly when absent (CI without the client stays green).
    - README states the de-anchoring caveat (plain-zlib swg-source v6000) + regenerate command + ground-truth oracle (tre_reader.py).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/harness && npx vitest run test/tre-v6000-swgsource-byteexact.test.ts 2>&1 | grep -qE "passed|skipped" && echo OK</automated>
  </verify>
  <done>swg-source v6000 fixture committed (or regenerable) + skipIf-absent zlib-oracle test green/skip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Native extractMountAt + mountTreMountWithToc typing stubs + descriptor/resolveFull RED scaffolds</name>
  <read_first>
    - packages/native-core/modules/core/tre/TreArchive.cpp:302-335 (extractEntry — current internal-index-only path + the isEnumerateOnly refusal at :307)
    - packages/native-core/modules/core/tre/TreArchive.h (extract API surface)
    - packages/native-core/index.d.ts (JS-facing native binding types — where the extractAt declaration is added)
    - packages/renderer/src/services/tocReader.ts:64-93,196-320 (TocIndexEntry + resolve — what resolveFull must add)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (Pitfall 2; A2 resolveFull; the externally-supplied descriptor note at Fix 2 "Stride caveat")
  </read_first>
  <files>packages/native-core/index.d.ts, packages/native-core/test/tre-extract-at-descriptor.test.ts, packages/renderer/src/services/tocReader.resolveFull.test.ts</files>
  <action>
    FIRST, freeze the native binding CONTRACT (interface-first, mirroring plan 02's Pillar-A stubs — the checker-W2 fix that lets plan 11 run parallel with plan 10 in Wave 1). Add to `packages/native-core/index.d.ts`, DECLARATION ONLY (plan 10 implements the runtime; signatures must match): (1) `extractMountAt(handle: string, archiveIndex: number, descriptor: { offset: number; length: number; compressedLength: number; compressor: number }): ExtractAtResult` where `ExtractAtResult = { bytes?: ArrayBuffer; encrypted?: boolean }` — the container is identified by (handle, archiveIndex) exactly as the existing readMountEntry path opens mount.nodeAt(archiveIndex).path (tre_binding.cpp:543-547); offset alone is NOT a locator (the H1 redesign — the old locator-less `extractAt(offset,length,...)` is REMOVED). (2) `mountTreMountWithToc(paths: string[], priorities: number[], tocPath: string, tocTreePath: string): string` — extends mountTreMount (index.d.ts:195) so the master `.toc` supplies entries for empty-internal-TOC (numberOfFiles=0) v6000 containers through getMountEntriesColumnar (each external entry carries its container archiveIndex + descriptor). NO runtime here. This frozen typing is what plan 11 (Wave 1, parallel) compiles/mocks against; do NOT add `04.3-10` to plan 11's depends_on (would cascade waves 12/13). Then write `tre-extract-at-descriptor.test.ts`: a RED/skip test asserting the FUTURE native ability to extract a payload from an EXTERNALLY-SUPPLIED descriptor via `extractMountAt(handle, archiveIndex, descriptor)` against a `numberOfFiles=0` v6000 container — extraction that does NOT consult the container's (empty) internal TOC. Use the swg-source fixture from Task 1 (skipIf-absent): assert the inflated bytes equal `.v6000.expected.bin`. The runtime does not exist yet → `test.skipIf(!nativeHasExtractMountAt)` OR assert-throws so it is RED and turns green in plan 10. Write `tocReader.resolveFull.test.ts`: RED test pinning that `readTocIndex(...).resolveFull(virtualPath)` returns the full descriptor `{ treeFileIndex, treName, offset, length, compressedLength, compressor, crc }` (currently `resolve()` returns only `{treeFileIndex, treName}`) AND that a length==0 entry is FILTERED (resolveFull returns undefined for a tombstone path). Fails because resolveFull is unimplemented (plan 11).
  </action>
  <acceptance_criteria>
    - `native-core/index.d.ts` declares `extractMountAt(handle, archiveIndex, descriptor)` returning `ExtractAtResult { bytes?: ArrayBuffer; encrypted?: boolean }` AND `mountTreMountWithToc(paths, priorities, tocPath, tocTreePath)` (grep -c "extractMountAt" index.d.ts >= 1 AND grep -c "mountTreMountWithToc" >= 1) — declaration only; `tsc --noEmit` on packages consuming the binding type succeeds (plan 11 compiles/mocks against them). The locator-less `extractAt(offset,...)` signature is NOT present (grep -c "extractAt(offset" == 0).
    - `tre-extract-at-descriptor.test.ts` calls `extractMountAt(handle, archiveIndex, descriptor)` and is RED-or-skip (skips when fixture/runtime absent; fails when fixture present but runtime missing) — never silently passes.
    - `tocReader.resolveFull.test.ts` FAILS because `resolveFull` is not yet a method on the index — names the exact 7-field descriptor AND asserts a length==0 tombstone path resolves to undefined.
    - Neither test references internal-TOC lookup for the v6000 container (proves the master-sourced path).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/services/tocReader.resolveFull.test.ts 2>&1 | grep -qE "failed|FAIL" && echo RED-OK</automated>
  </verify>
  <done>extractMountAt + mountTreMountWithToc typing stubs frozen in index.d.ts (plan 11 compiles/mocks against them); native externally-supplied-descriptor extract test + resolveFull descriptor/tombstone test exist and are RED/skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| real client files → fixture extraction | Reads (never writes) the real SWGSource client; emits small committed fixtures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-03-01 | Tampering | extract-v6000-fixture.mjs reads real client | mitigate | Script is read-only on the client data root; writes only into packages/harness/fixtures-real/toc/. |
| T-04.3-03-02 | Information disclosure | committing real asset bytes | accept | Single SMALL extracted payload (per Phase-01 D-09/D-10 fixture policy); not a full archive dump. |
| T-04.3-03-03 | DoS | malformed .toc offsets during extraction | mitigate | Bounds-check offset/compressedLength against file size before read (mirror TreArchive T-01-02 subtraction form). |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; node built-in zlib only. |
</threat_model>

<verification>
- Byte-exact zlib-oracle test green when fixture present, skip when absent.
- Native + resolveFull tests RED (prove they exercise the not-yet-built paths).
</verification>

<success_criteria>
The missing swg-source v6000 fixture is extractable/committed; MOUNT-03 has a real test; the native
externally-supplied-descriptor path and tocReader.resolveFull have RED scaffolds for plans 10/11.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-03-SUMMARY.md` when done.
</output>
