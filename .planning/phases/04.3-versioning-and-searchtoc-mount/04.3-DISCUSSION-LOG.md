# Phase 04.3: Versioning Model & SearchTOC Mount Completion - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-29
**Phase:** 04.3-versioning-and-searchtoc-mount
**Areas discussed:** Phase shape & sequencing, Version-navigation UX & safety, Branch-tree rendering, Pillar B mount depth & risk

---

## Phase shape & sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| One phase, multi-plan | Keep A + B + polish in 04.3 as plans/waves | ✓ |
| Split Pillar B into 04.4 | Ship A's value loop fast, native mount on its own | |

| Option | Description | Selected |
|--------|-------------|----------|
| Pillar A first | Versioning value loop first, then mount | |
| Pillar B first | Full TRE base readable first | |
| Parallel waves | A (renderer) ∥ B (native), converge at UAT | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Split checkpoints | Separate versioning UAT + mount UAT | |
| One combined UAT | Single end UAT superseding 04.1-11 + 04.2-06 | ✓ |
| You decide | — | |

**User's choice:** One phase / parallel waves / one combined UAT.

---

## Version-navigation UX & safety

| Option | Description | Selected |
|--------|-------------|----------|
| Select + explicit action | Click=select; per-row hover actions reconcile | |
| Click = reconcile | Click a version immediately reconciles live | ✓ |
| Arm-then-apply | Click selects; sticky "Make live" button applies | |

| Option | Description | Selected |
|--------|-------------|----------|
| Per-reconcile confirm w/ diff | — | |
| Confirm once per session | — | |
| Silent + undo | No confirm; instant reconcile + session undo | ✓ |

**User's choice:** Click = reconcile; silent + undo. Pushed back on the divergence question →
clarified in plain text. Final model confirmed: clicking an existing version reconciles instantly &
silently via its stored deploy-model; the Deploy button is ONLY for publishing a new version forward;
per-row Deploy/Revert hover actions dropped, "Branch from here" kept; confirm only when uncommitted
changes would be discarded; selected ≡ live always (inspect via ▸-expand).
**Notes:** Maintainer dislikes any "second step" for navigating — the version tree is the deploy control.

---

## Branch-tree rendering

| Option | Description | Selected |
|--------|-------------|----------|
| Proper multi-lane tree | Lane algorithm, arbitrary branch depth | ✓ |
| Minimal (sketch 2-lane) | Just the shallow case the sketch draws | |

| Option | Description | Selected |
|--------|-------------|----------|
| Hand-rolled SVG | Compute positions + draw SVG ourselves | |
| Graph library | @gitgraph/react / d3 etc. | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| One 'live' marker + structure | Collapse active/deployed → one live node | ✓ |
| Keep sketch's two markers | Preserve active vs deployed | |

**User's choice:** Multi-lane tree; graph library; single live marker + structure.
**Notes:** Library choice carries a RESEARCH GATE (must render sketch 002-A faithfully — lane gutter
beside row content, theming, WIP stub — else fall back to hand-rolled SVG). Markers collapse because
selected ≡ live (from the version-navigation decision).

---

## Pillar B mount depth & risk

| Option | Description | Selected |
|--------|-------------|----------|
| Full: entries + extraction | Master-.toc sourcing + per-payload extraction | ✓ |
| Incremental: entries first | Enumerate now, defer extraction | |

| Option | Description | Selected |
|--------|-------------|----------|
| Overlay top dir + skip archives | Simpler over-enumeration fix | |
| Lazy searchPath lookups | On-demand resolution like the real client | ✓ |
| You decide | — | |

| Option | Description | Selected |
|--------|-------------|----------|
| Crew + byte-exact, skipIf-absent | — | ✓ |
| Crew only | — | |

**User's choice:** Full depth; lazy searchPath lookups; crew + byte-exact (skipIf-absent) gate.
**Notes:** Lazy lookups mean loose files aren't enumerated as VFS rows; override still wins at resolve
time; viewer reads loose winner from disk via readVfsEntryBytes.

## Claude's Discretion

- Plan breakdown / wave assignment within the one-phase parallel-waves frame.
- 007/008 polish prioritization within plans (all CONFIRMED gap-review rows in scope).
- VCS-tab placement (S3) — deferred to plan time, stays AMBIGUOUS.

## Deferred Ideas

- VCS-tab placement (S3): same-group 3rd tab vs separate dock region — decide at plan time.
- New-Project wizard form (P11): paginated stepper KEPT; all-steps-modal rework not pursued.
- `delete-project-with-restore`: out of scope for 04.3 (its undo-stack pattern informs D-06 only).
