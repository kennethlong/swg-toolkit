# Phase 04.3 — Versioning Model & SearchTOC Mount Completion (scope)

Pre-planning scope note (read by `/gsd:discuss-phase` / `/gsd:plan-phase`). Bundles the two big
reworks the 04.1/04.2 in-client UAT surfaced as blocking, gated by a sketch-fidelity review, then one
combined in-client UAT.

## Plan 01 (FIRST, gating) — Crew UI-vs-sketch gap review — ✅ DONE (2026-06-29)

**COMPLETE.** The cross-AI consult crew (Codex / Cursor / fresh Sonnet / fresh Opus) diffed the
as-built UI against sketches 002 / 005 / 006 / 007 / 008 element-by-element; findings reconciled +
self-verified against the real files.

➡️ **Authoritative output — every later plan in this phase MUST consume this as its `must_have`
source:** [`04.3-01-CREW-GAP-REVIEW.md`](04.3-01-CREW-GAP-REVIEW.md). It lists each CONFIRMED gap with
file:line evidence, the KEEP-lists (deliberate evolutions NOT to "fix"), and the maintainer's resolved
scope decisions. The planner/researcher must read it; do NOT re-run the crew.

Headline confirmed: the version history is a **flat list**, not the **Git-Graph Lanes** winner (002-A) —
missing the SVG graph (lanes, nodes, branch-from bezier, WIP stub), per-row Revert/Deploy/Branch actions,
state pips, status footer, with inverted node colors. Combined Deploy panel has the right skeleton but no
006-D collapsible sections. 007/008 mostly faithful with a cluster of chrome/affordance gaps.

Resolved scope (maintainer, 2026-06-29): **every CONFIRMED gap across 002/005/006/007/008 folds into
04.3 as a `must_have`**; New-Project wizard stays paginated (P11 = KEEP); VCS-tab placement (S3)
deferred to plan time. Crew briefs/outputs: `.planning/research/CONSULT-04.3-01-*`.

## Pillar A — Versioning rework

Full spec: [`../../todos/pending/version-navigation-live-sync-deploy-model.md`](../../todos/pending/version-navigation-live-sync-deploy-model.md). Couple the live client to the SELECTED version
(navigating the tree = deploy/revert): per-version deploy model (first-class, not the live radio);
`syncLiveToVersion` reconcile engine (forward apply / backward revert in one op) wired to selection behind
a confirm; B3 stock snapshot/restore across versions; deploy-model picker shown only for a new forward
deploy; first-class revert from any state (subsumes the reopen-Reset gap); visual branch-tree history
per sketches 002/005.

## Pillar B — SearchTOC / v6000 mount completion

Full spec: [`../../todos/pending/v6000-swg-source-plain-zlib-read-support.md`](../../todos/pending/v6000-swg-source-plain-zlib-read-support.md). Mount SearchTOC clients from the master `.toc` index (swg-client-v2's empty-internal-TOC
v6000 container `.tre` files contribute entries; payloads read by offset); v6000 enumerate-only becomes a
per-payload runtime check (plain zlib for SWG-Source, encrypted only for Restoration); fix loose-overlay
over-enumeration of the searchPath data root; loose-entry mesh viewport.

## Combined UAT (last plan, autonomous:false)

One in-client UAT that supersedes re-running 04.1-11 + 04.2-06 piecemeal: version-navigation deploy/revert
on both a searchTree client (SWGEmu/Infinity) and the searchTOC dev client (swg-client-v2), with the full
v6000 base readable and the branch-tree history rendering.

## Out of scope (separate future item)

`delete-project-with-restore` (project-lifecycle; its own todo).

## Constraints

- **Verify against ground truth** (CLAUDE.md/AGENTS.md prime directive): the v6000/searchTOC work is
  format-heavy — cite real `../swg-client-v2` source + real `.toc`/`.tre` bytes + the maintainer's working
  `swg-blender-plugin` reader; run the de-anchoring crew before merging the mount changes.
- Both 04.1 and 04.2 in-client UATs (04.1-11, 04.2-06) remain open; this phase's combined UAT closes them.
