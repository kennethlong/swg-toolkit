---
phase: 04.3-versioning-and-searchtoc-mount
plan: 05
type: execute
wave: 2
depends_on: ["04.3-02"]
files_modified:
  - packages/renderer/src/panels/deploy/laneLayout.ts
  - packages/renderer/src/panels/deploy/LaneGutter.tsx
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, GRAPH-05]
must_haves:
  truths:
    - "laneLayout assigns lane 0 to the first-child TRUNK chain (by TOPOLOGY/parentId, NOT branchSet) and the lowest free lane > 0 to each ADDITIONAL child, recycling freed lanes; every non-root node connects to its parentId (same-lane line / cross-lane bezier) so a continue-on-branch node connects to its REAL parent — single-parent rooted tree (no merges, D-11). Concurrently-active lanes get DISTINCT x; gutter widens past 2 lanes (no clamp-overlap)."
    - "LaneGutter renders the 90px SVG gutter beside (not fused with) the row content, with the exact 002-A primitives."
  artifacts:
    - path: "packages/renderer/src/panels/deploy/laneLayout.ts"
      provides: "pure lane-assignment + connector/node geometry over changesets"
      min_lines: 40
    - path: "packages/renderer/src/panels/deploy/LaneGutter.tsx"
      provides: "SVG lane gutter component (pure function of GraphLayout)"
      min_lines: 30
  sketch_diff:
    sketch: ".planning/sketches/002-version-graph-timeline/index.html (#variant-a, lines 341-381)"
    elements:
      - "GRAPH-02 / A2-A3: <svg> lane column width 90; vertical trunk <line> parent→child in lane 0 (stroke var(--color-border-soft), width 2)"
      - "GRAPH-03 / A4: cubic-bezier 'branch from vN' <path> M{parentX} {parentY} C {parentX+34} {parentY} {branchX} {midY} {branchX} {branchY} (the defining branch-diverges visual)"
      - "GRAPH-04 / A5: dashed WIP <line> stroke-dasharray='3 3' from y=0 to the live node, in the live node's lane, only when hasUncommittedWork"
      - "GRAPH-05 / A6+D-13: node templates — root/older gray hollow circle (r=6, fill var(--color-surface), stroke var(--color-text-faint)); LIVE node accent-filled (r=9, fill var(--color-accent)) + inner dot (r=4, fill var(--color-accent-text)); branch-point marker"
      - "node centers align to 52px rows: cy = 52*rowIndex + 26; lane x = 22 + 46*lane with DISTINCT x per concurrent lane (gutter widens past 2 lanes; rows oldest-first)"
---

<objective>
Build the hand-rolled branch-tree graph primitives (D-12 RESOLVED → hand-rolled SVG; no library clears
the gate). Two pure, independently-testable pieces: `laneLayout()` (geometry) and `<LaneGutter>` (SVG
render). Parameterizes the static sketch 002-A markup over the real changeset graph.

These are leaf components — plan 06 (VersionHistoryBody) composes them beside the row list. Turns the
Wave-0 laneLayout RED fixtures GREEN.

Purpose: the version history renders as the Git-Graph-Lanes winner (002-A), not a flat list — the
load-bearing UI gap that created this phase.
Output: implemented laneLayout.ts + LaneGutter.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md
@.planning/sketches/002-version-graph-timeline/index.html

<interfaces>
laneLayout.ts (from Wave-0 stub) — implement:
  export function laneLayout(changesets: SwgChangeset[], liveVersionId: string|null, hasUncommittedWork: boolean): GraphLayout
  GraphLayout = { rows: LaidRow[]; connectors: Connector[]; nodes: LaidNode[]; width: number; height: number }
  LaidRow = { id; rowIndex; lane; y }
  LaidNode = { id; cx; cy; kind: 'root'|'older'|'live'|'branch-point' }
  Connector = { kind: 'trunk'|'branch'|'wip'; d?: string; x1?;y1?;x2?;y2? }

Lanes by TOPOLOGY (parentId): trunk = first-child chain from root; an ADDITIONAL child ⇒ new lane. branchSet() (VersionHistoryBody.tsx:33-41, chronological) is a HINT for theming only — NOT the lane key (RESEARCH PRIMARY steps 3-8).
Geometry constants (002-A): rowY(r)=52*r+26 ; laneX(l)=22+46*l with DISTINCT x per concurrently-active lane (widen the gutter past 2 lanes; do NOT clamp-overlap) ; rows OLDEST-first (layout.rows is the single order source) ; r_gray=6, r_live=9, r_inner=4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement laneLayout pure function</name>
  <read_first>
    - packages/renderer/src/panels/deploy/laneLayout.ts (Wave-0 stub + types)
    - packages/renderer/src/panels/deploy/laneLayout.test.ts (the 5 RED fixtures to satisfy)
    - packages/renderer/src/panels/deploy/VersionHistoryBody.tsx:33-41 (branchSet detection to reuse)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (§ PRIMARY — lane-assignment algorithm steps 1–11)
  </read_first>
  <files>packages/renderer/src/panels/deploy/laneLayout.ts</files>
  <action>
    Implement the CORRECTED algorithm from RESEARCH § PRIMARY (lanes by TOPOLOGY, NOT branchSet): (1) order rows OLDEST-first by timestamp to MATCH VersionHistoryBody.tsx:70-72 (oldest-first — NOT newest-first); rowY=52*r+26; layout.rows is the single row-order source. (2) Walk rows; a node INHERITS its parent's lane iff it is its parent's FIRST-placed child (trunk = lane 0 = first-child chain from root). (3) Every ADDITIONAL child of a parent gets the lowest FREE lane > 0, freed once its subtree is fully emitted (past its last descendant's row); reuse freed lanes. Do NOT key lane membership on branchSet() (chronological) — a continue-on-branch node (v5 parent v4) MUST inherit v4's lane, not fall to lane 0. (4) Every non-root node emits ONE connector to its parentId: same-lane -> vertical trunk `<line>`; cross-lane -> cubic bezier `M {parentX} {parentY} C {parentX+34} {parentY} {branchX} {midY} {branchX} {branchY}`. wip dashed line from y=0 to the live node when hasUncommittedWork. (5) Emit nodes with kind: root (no parentId), live (id===liveVersionId), branch-point (additional child), else older; cx=laneX(lane), cy=rowY(rowIndex). laneX=22+46*lane with DISTINCT x per concurrently-active lane; width = max(90, 22+46*maxConcurrentLane+8) (widen past 2 lanes — do NOT clamp-overlap). Keep it a PURE function (no React, no DOM).
  </action>
  <acceptance_criteria>
    - laneLayout.test.ts all 6 fixtures pass (incl. continue-on-branch + distinct-x): no-branch (all lane 0, trunk only, height=52*n); branch-off-tip (lane 1 + branch connector to parent); branch-off-NON-tip (bezier `d` to the real parentId); continue-on-branch (v5 in v4's lane, same-lane line to v4, NOT a false trunk to v3); deep/wide tree (DISTINCT x per concurrent lane, width widens past 2 lanes via recycling); hasUncommittedWork (a 'wip' connector in live lane).
    - Pure: no `import ... react` and no `document.` in laneLayout.ts (grep -c == 0).
    - layout.rows is oldest-first (the single row-order source) — asserted.
    - Branch connector kind 'branch' has a `d` containing 'C ' (cubic bezier); concurrently-active lanes have distinct x (no clamp-overlap).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/laneLayout.test.ts</automated>
  </verify>
  <done>laneLayout implements bounded lane assignment + connectors + node kinds; all fixtures green; pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LaneGutter SVG component (002-A primitives)</name>
  <read_first>
    - .planning/sketches/002-version-graph-timeline/index.html:349-381 (the exact SVG: line/path/circle, CSS-var strokes, inner dot)
    - packages/renderer/src/panels/deploy/LaneGutter.tsx (Wave-0 stub + props)
    - packages/renderer/src/panels/deploy/laneLayout.ts (GraphLayout consumed)
  </read_first>
  <files>packages/renderer/src/panels/deploy/LaneGutter.tsx</files>
  <action>
    Implement `LaneGutter({ layout })` to render `<svg className="lane-svg" width={layout.width} height={layout.height}>` containing: trunk `<line>` per trunk connector (stroke var(--color-border-soft), width 2); branch `<path d=... stroke=accent-dim fill=none>` per branch connector; wip `<line stroke-dasharray="3 3">` per wip connector; and one `<circle>` per node using the 002-A templates — gray hollow (r=6, fill var(--color-surface), stroke var(--color-text-faint)) for root/older/branch-point, accent-filled (r=9, fill var(--color-accent)) + inner `<circle r=4 fill var(--color-accent-text)>` for the live node. Use CSS vars for ALL colors (no hardcoded hex — fixes A12 inverted colors). Add a component test (RTL/snapshot) asserting: an `<svg>` exists; a branch `<path>` with a 'C' bezier renders for a branched layout; the live node renders BOTH the r=9 outer and r=4 inner circle; the WIP dashed line appears only when the layout has a wip connector.
  </action>
  <acceptance_criteria>
    - Component renders one `<svg>` with width 90; node/connector counts match the GraphLayout.
    - Live node = two concentric circles (outer accent r=9 + inner r=4) — asserted in test.
    - No hardcoded color hex in LaneGutter.tsx (grep -v '^\s*//' | grep -ciE "#[0-9a-f]{3,6}" == 0); colors are CSS vars.
    - Branch-off-non-tip layout renders a `<path>` containing 'C' (cubic bezier) — sketch element A4.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/LaneGutter 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>LaneGutter renders the 002-A gutter (lanes, nodes incl. live double-circle, branch bezier, WIP dash) with CSS-var theming; component test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none) | Pure UI geometry + SVG render of in-renderer changeset data; no I/O, no untrusted input crosses a boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-05-01 | DoS | pathological deep/wide graph render | accept | Single-parent tree bounded by version count (tens, not thousands); lane recycling caps width; row list already virtualization-ready in plan 06. |
| T-04.3-SC | Tampering | npm installs | mitigate | No graph library installed (D-12 hand-rolled); zero new deps. |
</threat_model>

<verification>
- laneLayout fixtures + LaneGutter component tests green.
- Sketch-diff: each 002-A element in `sketch_diff` rendered by LaneGutter (verified element-by-element at the combined UAT, plan 13).
</verification>

<success_criteria>
The branch-tree graph primitives faithfully reproduce sketch 002-A's lane gutter, node templates,
branch-from bezier, and WIP stub — as pure, tested, library-free SVG ready for plan 06 to compose.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-05-SUMMARY.md` when done.
</output>
