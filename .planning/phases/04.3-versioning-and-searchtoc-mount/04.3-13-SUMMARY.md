---
phase: 04.3-versioning-and-searchtoc-mount
plan: 13
status: complete
wave: 4
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05, VER-06, VER-07, VER-08, VER-09, GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, GRAPH-05, GRAPH-06, GRAPH-07, GRAPH-08, MOUNT-01, MOUNT-02, MOUNT-03, MOUNT-04, MOUNT-05, MOUNT-06, MOUNT-07]
verdict: APPROVED
supersedes: [04.1-11, 04.2-06]
completed: 2026-07-03
---

# 04.3-13 SUMMARY — Combined in-client UAT (D-03) — APPROVED

## Objective
ONE real-Electron checkpoint on BOTH client classes (searchTree + searchTOC/loose-override) with the
full v6000 base mounted, the branch-tree history rendering, and every governing sketch surface diffed.
Supersedes 04.1-11 + 04.2-06 and closes Phase 04.3.

## What happened — LIVE UAT-ITERATION mode
The checkpoint did not run as a single scripted walkthrough. Instead the maintainer exercised the
full surface live across 2026-07-01..03, reporting defects as they surfaced; each was diagnosed
against ground truth (on-disk manifests/cfgs), fixed, and regression-tested in-session. This
UAT-as-iteration produced one MODEL change and a series of fixes:

**Model change (crew consult CONSULT-VG-01..04, convergence-from-divergence):**
Selection DECOUPLED from deployment — supersedes D-04/D-08. Click = `selectVersion` (pointer +
staging materialization, zero client mutation); the explicit Deploy CTA is the only client
reconcile; `activeVersionId`/`deployedVersionId` diverge until deploy. Codex proved the old
navigate=deploy path threw EISDIR (install dir passed as cfgPath) before the pointer moved;
Sonnet's model analysis ("not a close call"), Cursor + Opus independently derived the same
variable-height layout math. Requirements VER-03/04/06/08 + GRAPH-04/05/08 revised accordingly
(dated markers in 04.3-RESEARCH.md).

**Fixed during the UAT (all committed, 269 renderer tests green at close):**
- False "Discard unsaved changes?" after a clean save (dirty = flatEqual vs flatten(active), not length>0)
- Branch-parent bug (noop reconcile fast-path didn't move the pointer → Save parented wrong)
- Stale staging after navigation (setLiveVersion re-materializes the working set)
- "Working changes" header counts the uncommitted DELTA (dot clears after Save)
- Dead "Branch from here" removed (branch = select + Save); confirm-dialog buttons styled
- Selected-node accent ring + selected-row highlight; SVG nodes track ▸-expanded rows (variable-height laneLayout)
- Reset deployment: record-shape dispatch (not the untrusted radio), surfaced errors, persisted
  deployRecord cleared, cross-session Reset affordance
- Live badge ALWAYS readable: falls back to Baseline (= stock) when nothing deployed; footer +
  statusbar "stock" copy; Deploy CTA no-op state "vN is live ✓"
- Content-based client detection (find .tre dirs; all 4 distros + bonuses) + Scan-all-drives
- Window size/position/monitor persistence (backend/windowState.ts)

## Verdict
**APPROVED** — maintainer sign-off 2026-07-03 ("Already verified" / "pretty solid").
All 14 criteria pass (A1–A7 + A-P, B1–B2, C1–C5); every sketch element observed (S3 VCS-tab
ambiguity resolved by acceptance). B/C additionally backed by the 04.2/04.3 in-client iterations,
the D-16 gate (GO), and the byte-exact F-2 integration tests.

## Supersession
04.1-11 and 04.2-06 are CLOSED as superseded by this UAT (SUMMARY stubs written).

## Follow-ups carried forward
- todo `deploy-dialog-synclive-undo-wiring` — route DeployDialog through syncLiveToVersion
  (dedupe deploy logic; re-activate Undo after real deploys)
- todo `client-detection-and-layout-model` — detection half DONE this session; searchTOC
  mount/deploy half was completed by plans 10–11
- F-9 E2E persistence test remains quarantined (A-P manual probe passed → harness artifact)
