---
phase: 04.3-versioning-and-searchtoc-mount
plan: 13
type: execute
wave: 4
depends_on: ["04.3-04", "04.3-05", "04.3-06", "04.3-07", "04.3-08", "04.3-09", "04.3-10", "04.3-11", "04.3-12"]
files_modified:
  - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-13-UAT-RESULTS.md
autonomous: false
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05, VER-06, VER-07, VER-08, VER-09, GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, GRAPH-05, GRAPH-06, GRAPH-07, GRAPH-08, MOUNT-01, MOUNT-02, MOUNT-03, MOUNT-04, MOUNT-05, MOUNT-06, MOUNT-07]
must_haves:
  truths:
    - "On a searchTree client (SWGEmu/Infinity, absolute-path): navigating the version tree silently reconciles the live client (forward/back), Baseline restores to stock, an in-game change reflects the selected version."
    - "On the searchTOC dev client (swg-client-v2, loose-override): the same version-navigation deploy/revert works AND the full v6000 base mounts — patch_sku3_* content browses + opens in the viewer BYTE-EXACT (verified against tre_reader.py / the plan-03 fixture, not just non-zero entryCount)."
    - "The branch-tree version history renders the 002-A Git-Graph-Lanes surface (a branch off a non-tip version draws as a branch), and the 005/006/007/008 surfaces match their sketches element-by-element."
    - "This UAT supersedes the still-open 04.1-11 + 04.2-06 checkpoints (D-03)."
  artifacts:
    - path: ".planning/phases/04.3-versioning-and-searchtoc-mount/04.3-13-UAT-RESULTS.md"
      provides: "per-criterion pass/fail + sketch-diff observed/missing table + maintainer sign-off"
      min_lines: 30
  key_links:
    - from: "built Deploy panel"
      to: "sketch 002-A / 005 / 006 / 007 / 008"
      via: "element-by-element sketch-diff"
      pattern: "sketch"
---

<objective>
The combined in-client UAT (D-03) — ONE real-Electron checkpoint on BOTH a searchTree client
(SWGEmu/Infinity, absolute-path) AND the searchTOC dev client (swg-client-v2, loose-override), with the
full v6000 base mounted and the branch-tree history rendering. It supersedes re-running 04.1-11 + 04.2-06
piecemeal and closes Phase 04.3.

Claude automates everything first (build, full suite, launch, both clients prepared, a branched project
seeded), THEN the maintainer verifies the live behavior + diffs every governing sketch surface
element-by-element (AGENTS.md "sketches are the UI contract").

Purpose: prove navigate=deploy/revert on both client classes + full v6000 mount + sketch-faithful UI.
Output: UAT-RESULTS.md with per-criterion verdicts + sketch-diff + sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md
@.planning/sketches/002-version-graph-timeline/index.html
@.planning/sketches/005-deploy-inspect-tab/index.html
@.planning/sketches/006-combined-deploy-tab/index.html
@.planning/sketches/007-project-entry/index.html
@.planning/sketches/008-shell-composition/index.html

<interfaces>
Clients (Environment Availability, RESEARCH): searchTree = SWGEmu/Infinity (sibling dirs / D:/SWG Infinity, EERT5000, absolute searchTree). searchTOC dev = swg-client-v2 stage-x64/client.cfg → D:/Code/SWGSource Client v3.0/ (sku0/sku3 .toc + patch_sku3_* v6000).
Known assets: texture/ksk_all_spaceterminal.dds ∈ patch_11_0x.tre (v5000); sku3 v6000 content (collision floors, 344 dds, 294 msh, …).
Gate precondition: plan 12 GATE-FINDINGS must record GO.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-UAT readiness</name>
  <read_first>
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-12-GATE-FINDINGS.md (must say GO)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-VALIDATION.md (full-suite + E2E gate)
    - packages/renderer/src/services/treAutoMount.ts + syncLiveToVersion.ts (the merged behavior under test)
  </read_first>
  <files>.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-13-UAT-RESULTS.md</files>
  <action>
    Confirm the plan-12 gate recorded GO (abort with a clear message if NO-GO). Run the full suite (`npm run test`) + E2E hooks (`npm run test:e2e`) and record green. Build the app for real-Electron run. Prepare BOTH clients for the maintainer: verify the SWGEmu/Infinity searchTree client path + the swg-client-v2 stage-x64 client.cfg (searchTOC sku0–3 + searchPath) resolve; seed/confirm a project with a BRANCHED version history PROGRAMMATICALLY (script the manifest: trunk v1<-v2<-v3 plus a branch off a non-tip — e.g. v4 parent v2 and v5 parent v4, the continue-on-branch case) so the 002-A branch graph + lane topology have something to render (record in the skeleton that seeding was programmatic, not manual click-seeding). Write the skeleton of `04.3-13-UAT-RESULTS.md` with the criteria checklist + the sketch-diff table headers (one row per A1–A13 / D3–D17 / P1–P9 / S2–S8 element, columns: observed / missing). Do NOT mark anything passed yet — the maintainer fills the live verdicts.
  </action>
  <acceptance_criteria>
    - GATE-FINDINGS GO confirmed (else abort).
    - Full suite + E2E green (recorded in UAT-RESULTS skeleton).
    - Both client paths resolve; a branched project is seeded; UAT-RESULTS skeleton has the criteria + sketch-diff table ready for the maintainer.
  </acceptance_criteria>
  <verify>
    <automated>npm run test 2>&1 | tail -3 | grep -qiE "pass|✓" && test -f .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-13-UAT-RESULTS.md && echo READY</automated>
  </verify>
  <done>Gate GO confirmed; suite+E2E green; both clients + a branched project prepared; UAT-RESULTS skeleton staged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 04.3 fully integrated: version-navigation = silent reconcile (D-04) backed by a session undo stack
    + single live pointer (D-06/D-08), per-version deploy model (VER-01), the 002-A branch-tree graph,
    the 005/006 Deploy-tab chrome, the 007/008 polish, and the completed searchTOC/v6000 master-.toc mount
    with lazy searchPath. App is built and both clients are prepared. This supersedes 04.1-11 + 04.2-06.
  </what-built>
  <how-to-verify>
    Launch the app (real Electron). Run BOTH client classes and diff the UI against the sketches.

    A — Version navigation = deploy/revert (Pillar A), on the searchTree client (SWGEmu/Infinity):
      1. Open the branched project; click an older version → the live client SILENTLY reconciles (no dialog) and reflects that version (VER-03/VER-06).
      2. Make an uncommitted staging edit, then click another version → the "Discard unsaved changes and switch to vN?" confirm appears (VER-05); accept → reconciles; verify undo reverses it via the Undo button OR Ctrl+Z (VER-04 — the affordance added in plan 06).
      3. Click Baseline → client restores to STOCK (cfg byte-pristine) (VER-02).
      4. Click forward→back→forward → live bytes match each version (idempotent) (VER-02). Confirm an in-game-visible change tracks the selected version.
      5. Confirm: NO per-row Deploy/Revert buttons; "Branch from here" present (VER-08); deploy-model picker appears ONLY when deploying a NEW version forward, not when navigating (VER-07).

    B — Same navigation on the searchTOC dev client (swg-client-v2, loose-override):
      6. Repeat steps 1–4 on the loose-override client; loose deploy/revert reconciles correctly (VER-09); client.cfg byte-untouched. Then click Baseline on the loose client: CONFIRM the loose override/ files are REMOVED (restore-to-stock) AND client.cfg is byte-untouched (VER-02/VER-09 — the H4 loose-live→Baseline revert-only path).

    C — searchTOC / v6000 mount (Pillar B), on swg-client-v2:
      7. Mount the stage-x64 client.cfg; confirm patch_sku3_* containers show NON-ZERO entryCount (MOUNT-01) and the full base browses.
      8. Browse into sku3 v6000 content (e.g. a .dds/.msh) and Open in viewer → it extracts + renders BYTE-EXACT: a known patch_sku3_* entry opens byte-exact (cross-check against tre_reader.read_tre_payload / the plan-03 fixture), NOT merely a non-zero entryCount (MOUNT-03/04). Confirm the "enumerate-only (encrypted)" chip appears ONLY on genuinely-unreadable (Restoration) payloads, not swg-source v6000 (MOUNT-05).
      9. Confirm no thousands of bogus loose VFS rows from the searchPath data root (lazy resolution), yet a loose override still WINS + opens in the viewer (MOUNT-07). Confirm the v5000 space-terminal texture still resolves (MOUNT-06).

    D — Sketch-diff (AGENTS.md sketches-are-the-contract), element-by-element into UAT-RESULTS.md:
      10. 002-A: two-column lane gutter + rows (A1); SVG trunk lines (A2/A3); branch-from bezier for the non-tip branch (A4); dashed WIP stub when uncommitted (A5); node templates incl. live double-circle (A6/D-13); vN label + meta "date · N files · branch from vN" (A9/A10); status footer (A11); correct CSS-var colors (A12); is-active accent border on the LIVE row (A13).
      11. 005/006: collapsible sections + auto-hide splitter (D3/D4); section headers (D5); working-vs-saved split (D6); staging chrome (D7–D12); footer hint + full-width CTA (D13/D14); Deploy modal title + detected/not-found rows + recommended (D15–D17).
      12. 007: bound header name + pill chip (P1/P2); live Open Recent (P3); not-found rows (P4); first-run dim (P5); editable folders + Browse (P6/P7); scan statusbar (P9). 008: Deploy menu (S2); Inspect tab (S4); theme optgroups (S5); sb-state chip (S6); live: vN (S7); Datatable/Console/Log trio (S8). Mark each element observed / missing.

    Record every criterion (A–D) as pass/fail in `04.3-13-UAT-RESULTS.md`; for any sketch element marked missing, note it for a gap-closure plan.
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria + sketch surfaces pass, or list the failing criteria / missing sketch elements (each becomes a gap-closure must_have).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| toolkit → two real game clients | The UAT mutates real client installs (loose override + cfg) on the maintainer's machine. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-13-01 | Tampering/availability | UAT permanently alters a working client | mitigate | Reconcile is snapshot+restore (B3 / restoreCfg); Baseline restores to stock; never clobber a working cfg (client-deploy-design-tenets). Verify byte-pristine restore as a UAT step. |
| T-04.3-13-02 | Repudiation | shipping a sketch-divergent UI again (the bug that created this phase) | mitigate | Element-by-element sketch-diff into UAT-RESULTS (observed/missing) is a required gate; missing elements become gap-closure must_haves. |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; UAT runs the built app + tests only. |
</threat_model>

<verification>
- Pre-UAT: gate GO + full suite + E2E green.
- Human-verify: all A–D criteria pass on BOTH clients; every governing sketch element diffed (observed/missing).
- Sign-off recorded in UAT-RESULTS.md; supersedes 04.1-11 + 04.2-06.
</verification>

<success_criteria>
Navigate=deploy/revert proven on both a searchTree and a searchTOC client, the full v6000 base mounts and
browses/extracts, the branch-tree + all governing sketch surfaces match their contracts element-by-element,
and the maintainer signs off — closing Phase 04.3 and the two superseded UATs.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-13-SUMMARY.md` when done.
</output>
