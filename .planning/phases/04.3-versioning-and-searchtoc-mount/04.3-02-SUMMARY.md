---
phase: 04.3-versioning-and-searchtoc-mount
plan: "02"
subsystem: renderer/deploy
tags: [contracts, stubs, tdd, wave-0, lane-layout, undo, reconcile]
dependency_graph:
  requires: []
  provides:
    - syncLiveToVersion contract (VER-02/03 engine signature, Wave-1 plan 04)
    - deployModelOf discriminator (live)
    - ReconcileCtx / LiveReconcileResult types
    - setLiveVersion stub in changesetService (plan 06 compile contract)
    - useUndoStore / ReconcileSnapshot (session undo, VER-04)
    - useClientScanStore / setClientScanMessage / useClientScanStatus (P9 scan message)
    - laneLayout / GraphLayout / LaidRow / LaidNode / Connector (GRAPH-01..05)
    - LaneGutter stub component (GRAPH render placeholder)
  affects:
    - packages/renderer/src/services/syncLiveToVersion.ts
    - packages/renderer/src/services/syncLiveToVersion.test.ts
    - packages/renderer/src/services/changesetService.ts
    - packages/renderer/src/state/undoStore.ts
    - packages/renderer/src/state/undoStore.test.ts
    - packages/renderer/src/state/clientScanStore.ts
    - packages/renderer/src/state/clientScanStore.test.ts
    - packages/renderer/src/panels/deploy/laneLayout.ts
    - packages/renderer/src/panels/deploy/laneLayout.test.ts
    - packages/renderer/src/panels/deploy/LaneGutter.tsx
tech_stack:
  added: []
  patterns:
    - Wave-0 Nyquist scaffolding (contract stubs + RED tests lock interfaces before parallel Wave-1 execution)
    - Zustand session-scoped (no persist) stores for transient UI state
    - vi.mock module mocking for dependency isolation in service tests
    - Topology-based lane assignment (parentId, not branchSet chronology)
key_files:
  created:
    - packages/renderer/src/services/syncLiveToVersion.ts
    - packages/renderer/src/services/syncLiveToVersion.test.ts
    - packages/renderer/src/state/undoStore.ts
    - packages/renderer/src/state/undoStore.test.ts
    - packages/renderer/src/state/clientScanStore.ts
    - packages/renderer/src/state/clientScanStore.test.ts
    - packages/renderer/src/panels/deploy/laneLayout.ts
    - packages/renderer/src/panels/deploy/laneLayout.test.ts
    - packages/renderer/src/panels/deploy/LaneGutter.tsx
  modified:
    - packages/renderer/src/services/changesetService.ts (added setLiveVersion stub)
decisions:
  - "Wave-0 pattern: stub bodies throw/no-op so downstream plans compile while RED tests pin behavior"
  - "deployModelOf fully implemented as duck-type discriminator (overrideDir in record = loose)"
  - "LaneGutter stub renders empty SVG with correct width/height from GraphLayout"
  - "clientScanStore is session-scoped (no persist) to avoid bleeding scan state across projects"
metrics:
  duration: "~20 minutes"
  completed: "2026-06-30"
  tasks_completed: 3
  files_created: 9
  files_modified: 1
---

# Phase 04.3 Plan 02: Wave-0 Contract Stubs + RED Tests Summary

Wave-0 Nyquist scaffolding for Pillar A. Contract stubs with final exported signatures and RED tests for syncLiveToVersion reconcile engine, undoStore session-undo slice, and laneLayout branch-tree algorithm — plus clientScanStore P9 message and LaneGutter SVG stub.

## What Was Built

All 10 files from the plan delivered (9 created, 1 modified):

**Task 1 — syncLiveToVersion + RED test + changesetService stub:**
- `syncLiveToVersion.ts`: final signature `async function syncLiveToVersion(targetId, ctx: ReconcileCtx): Promise<LiveReconcileResult>` + `deployModelOf(cs)` (fully implemented) + `ReconcileCtx`/`LiveReconcileResult` types. Stub body throws `'not implemented (W1-04)'`.
- `syncLiveToVersion.test.ts`: 6 RED cases (a–f) using vi.mock'd dependencies — idempotent noop, Baseline restore-to-stock, loose/cfg model routing, flatEqual fast path, H4 loose-live→Baseline resetLoose. All 6 fail in RED; `deployModelOf` 3-case suite is always GREEN.
- `changesetService.ts`: added `setLiveVersion(id: string | null): void` stub (throws) for plan 06 compile contract.

**Task 2 — undoStore + clientScanStore + RED tests:**
- `undoStore.ts`: Zustand session slice `useUndoStore` with `stack: ReconcileSnapshot[]`, `canUndo`, `push/undo/clear` (all no-ops). `ReconcileSnapshot` type exported.
- `undoStore.test.ts`: 3 RED cases fail (push→canUndo, LIFO undo, clear empties); 1 session-only invariant always GREEN (no localStorage/persist).
- `clientScanStore.ts`: `useClientScanStore` + `useClientScanStatus()` hook + `setClientScanMessage()` function. Setter is no-op stub.
- `clientScanStore.test.ts`: 2 RED cases fail (set+read-back, set null clears); 1 initial-null invariant always GREEN.

**Task 3 — laneLayout + LaneGutter + RED test:**
- `laneLayout.ts`: `GraphLayout`/`LaidRow`/`LaidNode`/`Connector` types + `laneLayout()` stub returning `{ rows: [], connectors: [], nodes: [], width: 90, height: 0 }`.
- `laneLayout.test.ts`: 8 RED cases all fail against empty layout: no-branch linear, one branch (lane 1), branch-off-non-tip (with exact cubic-bezier `d` assertion), continue-on-branch (v5→v4 same lane, no false trunk), 3 concurrent lanes with DISTINCT x and width > 90, WIP connector, oldest-first row order, root/live node kinds.
- `LaneGutter.tsx`: stub renders `<svg width height>` with no children; accepts `{ layout: GraphLayout }`.

## Test Results

| Scope | Files | Tests |
|-------|-------|-------|
| New RED tests (expected failures) | 4 files | 19 failed (correct) |
| New always-GREEN invariants | same 4 | 5 passed |
| Existing tests (no regressions) | 18 files | 117 passed |
| TypeScript `--noEmit` | renderer | CLEAN |

All 4 new test files show RED (vitest grep matches "FAIL") against the stubs. Parallel Wave-1 plans (04/05/06/08/09) can now import from these modules without file races.

## Deviations from Plan

None — plan executed exactly as written. `deployModelOf` was explicitly described as fully-implementable (not a stub) and is GREEN in both phases.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. All stubs are in-process, throw/no-op only. Per plan threat model T-04.3-02-01: fixtures are synthetic objects, no real assets touched.

## Self-Check

**Created files exist:**
- `packages/renderer/src/services/syncLiveToVersion.ts` — FOUND
- `packages/renderer/src/services/syncLiveToVersion.test.ts` — FOUND
- `packages/renderer/src/state/undoStore.ts` — FOUND
- `packages/renderer/src/state/undoStore.test.ts` — FOUND
- `packages/renderer/src/state/clientScanStore.ts` — FOUND
- `packages/renderer/src/state/clientScanStore.test.ts` — FOUND
- `packages/renderer/src/panels/deploy/laneLayout.ts` — FOUND
- `packages/renderer/src/panels/deploy/laneLayout.test.ts` — FOUND
- `packages/renderer/src/panels/deploy/LaneGutter.tsx` — FOUND

**Commits:**
- `e8b9644` — test(04.3-02): syncLiveToVersion contract stub + RED test (Task 1)
- `ac728e8` — test(04.3-02): undoStore + clientScanStore stubs + RED tests (Task 2)
- `5ecd345` — test(04.3-02): laneLayout + LaneGutter stubs + RED test (Task 3)

## Self-Check: PASSED
