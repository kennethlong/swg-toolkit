---
phase: 04.3-versioning-and-searchtoc-mount
plan: 11
type: execute
wave: 2
depends_on: ["04.3-03"]
files_modified:
  - packages/renderer/src/services/tocReader.ts
  - packages/renderer/src/services/treAutoMount.ts
  - packages/renderer/src/services/treMount.ts
  - packages/renderer/src/services/openInViewer.ts
  - packages/renderer/src/services/readVfsEntryBytes.ts
  - packages/renderer/src/panels/tre/TreVfsBrowser.tsx
  - packages/renderer/src/panels/tre/MountedArchivesList.tsx
autonomous: true
requirements: [MOUNT-01, MOUNT-02, MOUNT-05, MOUNT-06, MOUNT-07]
must_haves:
  truths:
    - "For searchTOC clients, the mount sources its entry set from the master .toc index via the native mountTreMountWithToc API (plan 10), so TOC-indexed containers (incl. empty-internal-TOC v6000) contribute entries through the columnar bridge — patch_sku3_* shows non-zero entryCount (MOUNT-01)."
    - "No double-counting: the master .toc is the SINGLE index for searchTOC clients; internal TOCs are not also read for the same containers (MOUNT-02)."
    - "tocReader.resolveFull exposes the full entry descriptor (offset/length/compressedLength/compressor/crc) needed for extractMountAt, and FILTERS length==0 master-.toc tombstones (deletions are not files) (RESEARCH A2)."
    - "searchPath is resolved LAZILY on-demand via an explicit resolveLooseOverride() helper (override dirs highest-priority-first, then native mount) used by BOTH treAutoMount and viewer reads — no upfront enumeration of the data root (the readdir walk in treMount.ts injectLooseDirOverlay/collectLooseEntries is removed); loose files are NOT flat VFS rows but the override still wins at resolve time and reads via the shared readVfsEntryBytes service (D-15, MOUNT-07)."
    - "The MountedArchivesList 'enumerate-only (encrypted)' chip is driven by PER-ENTRY extract failure (extractMountAt encrypted:true), NOT the archive version tag — so swg-source v6000 is not mislabeled encrypted (MOUNT-05). Infinity/SWGEmu searchTree clients are unaffected (MOUNT-06)."
    - "readVfsEntryBytes is EXTRACTED from TreVfsBrowser.tsx (private :112) into a SHARED service packages/renderer/src/services/readVfsEntryBytes.ts; master-.toc-sourced reads route through native extractMountAt(handle, archiveIndex, descriptor); the TRE browser AND openInViewer both consume the shared service (H1 read-path wiring)."
  artifacts:
    - path: "packages/renderer/src/services/tocReader.ts"
      provides: "resolveFull() / extended TocIndexEntry carrying the full descriptor"
      contains: "resolveFull"
    - path: "packages/renderer/src/services/treAutoMount.ts"
      provides: "master-.toc entry sourcing for searchTOC clients (via native mountTreMountWithToc) + resolveLooseOverride helper"
    - path: "packages/renderer/src/services/treMount.ts"
      provides: "lazy searchPath — injectLooseDirOverlay/collectLooseEntries readdir walk removed; on-demand override resolve"
    - path: "packages/renderer/src/services/readVfsEntryBytes.ts"
      provides: "SHARED VFS byte reader (loose disk + native extractMountAt for master-.toc entries) — extracted from TreVfsBrowser"
      contains: "readVfsEntryBytes"
    - path: "packages/renderer/src/panels/tre/MountedArchivesList.tsx"
      provides: "enumerate-only chip driven by per-entry extract failure (not version tag)"
    - path: "packages/renderer/src/panels/tre/TreVfsBrowser.tsx"
      provides: "TRE browser routed through the shared readVfsEntryBytes service"
    - path: "packages/renderer/src/services/openInViewer.ts"
      provides: "viewer read path routed through shared readVfsEntryBytes (loose winners + master-.toc winners resolve)"
  key_links:
    - from: "treAutoMount.buildTreNodes"
      to: "tocReader.readTocIndex/resolveFull"
      via: "master-.toc entry sourcing (searchTOC clients)"
      pattern: "readTocIndex|resolveFull"
    - from: "openInViewer.ts"
      to: "readVfsEntryBytes"
      via: "loose-winner read path"
      pattern: "readVfsEntryBytes"
---

<objective>
Pillar B renderer change: source searchTOC mounts from the master `.toc` index (so empty-internal-TOC
v6000 containers contribute their entries) and make searchPath resolution lazy (no upfront enumeration
of the data root). This is the LOAD-BEARING fix — flipping native enumerate-only alone (plan 10) is
invisible without master-.toc entry sourcing (RESEARCH Pitfall 2).

Calls plan 10's native extractAt with the descriptors tocReader now exposes. Turns the plan-03
resolveFull RED test green. End-to-end byte-exact verified at the D-16 gate (plan 12).

Ground truth (LOCKED): master `.toc` = single authoritative index for searchTOC clients; CRC-keyed binary
search (H1, already in readTocIndex); TOCTreePath prefix on container names; priority-DESC + tombstones
already correct in treAutoMount/TreArchive::resolve.

Purpose: complete the searchTOC mount (TRE-05) — the 1,158-path sku3 set becomes browsable/extractable.
Output: tocReader.resolveFull, treAutoMount master-sourcing + lazy searchPath, openInViewer loose read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/todos/pending/v6000-swg-source-plain-zlib-read-support.md

<interfaces>
From tocReader.ts:
  TocIndexEntry { treeFileIndex; treName }   // EXTEND with offset/length/compressedLength/compressor/crc OR add resolveFull()
  TocIndex { resolve(virtualPath): TocIndexEntry|undefined; size }
  readTocIndex(filePath): TocIndex ; readTocTreeNames(filePath): string[]
  Master .toc entry (24B LE): compressor@0, treeFileIndex@2(u16), crc@4, fileNameLength@8, offset@12, length@16, compressedLength@20
From treAutoMount.ts:
  buildTreNodes (:72-103) — currently per-.tre internal-TOC sourcing via readTocTreeNames + mountTrePaths columnar bridge
  injectLooseDirOverlay (:176-180) — currently enumerates EVERY file under each searchPath dir (the over-enumeration bug)
  treAutoMount sorts rawPriority DESC then compact priorities (:106-111) — KEEP
From plan 10 native (frozen typings in plan 03): extractMountAt(handle, archiveIndex, descriptor) -> { bytes?: ArrayBuffer; encrypted?: boolean } (container located by mount.nodeAt(archiveIndex).path); mountTreMountWithToc(paths, priorities, tocPath, tocTreePath) -> handle (master-.toc columnar sourcing)
Shared read path: readVfsEntryBytes(entry, mountHandle) — CURRENTLY PRIVATE in TreVfsBrowser.tsx:112 (loose→disk via fs.readFileSync; archived→resolveChain + readMountEntry). EXTRACT to services/readVfsEntryBytes.ts and add the master-.toc branch (winnerArchiveIndex>=0 + external descriptor → extractMountAt). Tombstone master entries (length==0) resolve to null.
Perf: keep getMountEntriesColumnar off-thread columnar ArrayBuffer bridge (193k entries must not cross as JSON — CLAUDE.md binary-stays-binary).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tocReader.resolveFull — full entry descriptor</name>
  <read_first>
    - packages/renderer/src/services/tocReader.ts:64-93,196-320 (TocIndexEntry, resolve, readTocIndex internals — where offset/len/comp are parsed but discarded)
    - packages/renderer/src/services/tocReader.resolveFull.test.ts (the plan-03 RED test pinning the 7-field descriptor)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (A2 — resolveFull addition)
  </read_first>
  <files>packages/renderer/src/services/tocReader.ts</files>
  <action>
    Extend the index to surface the full per-entry descriptor: add `resolveFull(virtualPath): TocIndexEntryFull | undefined` returning `{ treeFileIndex, treName, offset, length, compressedLength, compressor, crc }` (keep the existing `resolve()` returning the slim shape for back-compat). The raw 24-byte parse already reads offset(@12)/length(@16)/compressedLength(@20)/compressor(@0)/crc(@4) — retain those fields on the internal entry instead of discarding them, and return them from resolveFull. FILTER length==0 tombstones: a master-.toc entry with length==0 is a DELETION marker (client localExists rejects it) — resolveFull (and resolve) return undefined for such a path; deletions are NOT files. Preserve the engine-faithful CRC binary search (H1) — resolveFull resolves the SAME winning entry as resolve, just with more fields.
  </action>
  <acceptance_criteria>
    - tocReader.resolveFull.test.ts passes: resolveFull returns the 7-field descriptor; offset/length/compressedLength match the master .toc bytes for a known path.
    - resolve() still returns the slim {treeFileIndex, treName} (back-compat) — existing tocReader.test.ts green.
    - resolveFull winner === resolve winner for the same path (H1 invariant preserved) — asserted.
    - A length==0 (tombstone) master-.toc entry resolves to undefined in BOTH resolve and resolveFull (deletions are not files) — asserted.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/services/tocReader 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>resolveFull exposes the full descriptor; H1 invariant + back-compat intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Master-.toc entry sourcing (native mountTreMountWithToc) + dedup + per-entry enumerate-only chip</name>
  <read_first>
    - packages/renderer/src/services/treAutoMount.ts (:72-103 buildTreNodes, :106-111 priority sort — KEEP)
    - packages/renderer/src/services/treAutoMount.test.ts (existing mount tests — regression guard MOUNT-06)
    - packages/renderer/src/services/tocReader.ts (readTocIndex/resolveFull from Task 1)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (Fix 1; Pitfall 2 + Pitfall 4; A1 dedup policy)
  </read_first>
  <files>packages/renderer/src/services/treAutoMount.ts, packages/renderer/src/panels/tre/MountedArchivesList.tsx</files>
  <action>
    For searchTOC clients ONLY: in buildTreNodes, mount via the native `mountTreMountWithToc(containerPaths, priorities, tocPath, tocTreePath)` (plan 10) so the master `.toc` supplies the entry set and empty-internal-TOC containers contribute entries through getMountEntriesColumnar (KEEP the off-thread columnar bridge — do NOT cross 193k entries as JSON; native does the columnar build). Apply the `TOCTreePath` prefix when resolving each container `.tre` path passed to the native API. Dedup (MOUNT-02/Pitfall 4): the master `.toc` is the SINGLE index for searchTOC clients — do NOT also read internal TOCs for the same containers (the CRC-binary-search index already dedups within the master). For non-searchTOC (searchTree: Infinity/SWGEmu) clients, leave the existing internal-TOC mount path untouched (MOUNT-06 regression guard). In `MountedArchivesList.tsx`, drive the "enumerate-only (encrypted)" chip from PER-ENTRY extract failure (extractMountAt returns encrypted:true on a real inflate failure), NOT the archive version tag — so swg-source v6000 is not mislabeled (MOUNT-05). Extend treAutoMount.test.ts: a searchTOC fixture sources entries from the master .toc (patch_sku3_* container contributes non-zero entries); no double-count; searchTree fixture unchanged.
  </action>
  <acceptance_criteria>
    - searchTOC mount sources entries from readTocIndex; a TOC-indexed v6000 container contributes non-zero entryCount (MOUNT-01) — test asserts entryCount > 0 for a sku3-style container fixture.
    - No double-count: a path that is both self-indexed and master-indexed appears once (MOUNT-02) — test asserts unique entry count.
    - searchTree (Infinity/SWGEmu) clients use the unchanged internal-TOC path (MOUNT-06) — existing treAutoMount.test.ts green.
    - Enumerate-only chip in MountedArchivesList.tsx is driven by the per-entry extractMountAt classification (MOUNT-05), not the version tag (grep MountedArchivesList: chip condition references the extract result / encrypted flag, not `=== 'v6000'` / version tag).
    - Columnar bridge retained (getMountEntriesColumnar still used; no JSON entry array crossing the bridge).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/services/treAutoMount 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>searchTOC mounts from the master .toc (v6000 containers contribute), no double-count, searchTree unaffected, chip on real failure only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lazy searchPath resolution + viewer reads via readVfsEntryBytes</name>
  <read_first>
    - packages/renderer/src/services/treMount.ts (:239-264 injectLooseDirOverlay/collectLooseEntries — the readdirSync upfront enumeration to remove; mountTrePaths)
    - packages/renderer/src/services/openInViewer.ts (viewer read path — route through readVfsEntryBytes)
    - packages/renderer/src/panels/tre/TreVfsBrowser.tsx (existing readVfsEntryBytes usage — the loose→disk pattern to reuse)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (Fix 3; Pitfall 5; D-15 consequence)
  </read_first>
  <files>packages/renderer/src/services/treMount.ts, packages/renderer/src/services/treAutoMount.ts, packages/renderer/src/services/readVfsEntryBytes.ts, packages/renderer/src/services/openInViewer.ts, packages/renderer/src/panels/tre/TreVfsBrowser.tsx</files>
  <action>
    FIRST, extract `readVfsEntryBytes` from TreVfsBrowser.tsx (private :112) into the SHARED service `packages/renderer/src/services/readVfsEntryBytes.ts`; add a master-.toc branch (an entry carrying an external descriptor → native `extractMountAt(handle, archiveIndex, descriptor)`) alongside the existing loose→disk and archived→readMountEntry branches; tombstone/null on failure. Re-point TreVfsBrowser.tsx to import from the shared service (no behavior change for it). THEN stop the upfront enumeration in `treMount.ts` `injectLooseDirOverlay`/`collectLooseEntries` (:239-264) — do NOT recursively `readdirSync` every file under each searchPath dir (which surfaced thousands of bogus loose VFS rows + the archives themselves for `searchPath_00_9="D:/Code/SWGSource Client v3.0/"`). Instead add an explicit `resolveLooseOverride(path)` helper that resolves loose overrides LAZILY at lookup time like the real client: check the searchPath override dirs (HIGHEST-priority first) for the requested path before falling to the native mount winner; the loose file WINS when present. Consequence to honor (D-15): loose files are NOT listed as flat VFS browser rows. Route the viewer "Open in viewer" read path (`openInViewer.ts`) AND the shared readVfsEntryBytes through `resolveLooseOverride` so a loose winner resolves there too (mesh/IFF readers read the loose bytes from disk). Extend treMount.test.ts/treAutoMount.test.ts: no flat VFS rows are emitted for a populated searchPath dir; a loose override still WINS at resolve time and reads via the shared readVfsEntryBytes.
  </action>
  <acceptance_criteria>
    - treMount.ts injectLooseDirOverlay/collectLooseEntries no longer recursively enumerates searchPath dirs (grep -v '^\s*//' packages/renderer/src/services/treMount.ts | grep -c "readdirSync" == 0); no flat loose VFS rows emitted (test asserts row count unchanged by a populated searchPath dir).
    - readVfsEntryBytes EXISTS as a shared service (grep -c "export.*readVfsEntryBytes" services/readVfsEntryBytes.ts >= 1) with a master-.toc branch calling extractMountAt; TreVfsBrowser.tsx imports it (no longer defines it privately).
    - A loose override still WINS at resolve time via resolveLooseOverride (MOUNT-07) — test asserts the loose path resolves over the native-mount entry.
    - openInViewer reads through the shared readVfsEntryBytes (grep -c readVfsEntryBytes in openInViewer.ts >= 1) — loose + master-.toc winners open in the viewer.
    - Mount perf not regressed (no upfront whole-data-root walk) — the over-enumeration is gone.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/services/treMount src/services/treAutoMount 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>readVfsEntryBytes is a shared service routing master-.toc reads through extractMountAt; searchPath is lazy (no flat rows, override still wins); TRE browser + viewer read loose/master winners via the shared service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| master .toc / .tre bytes → renderer mount | Untrusted index/archive bytes parsed in the renderer; offsets/lengths feed native extractAt. |
| searchPath dir → lazy resolve | User client's override dir read on-demand at lookup time. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-11-01 | Tampering/DoS | malformed .toc offsets/lengths → OOB in native extract | mitigate | resolveFull descriptors are bounds-checked in native extractAt (subtraction form, plan 10 T-04.3-10-01); renderer passes them through, native is the guard (V5). |
| T-04.3-11-02 | DoS | over-enumeration of the data root (thousands of junk rows / perf) | mitigate | Lazy searchPath resolution removes the upfront walk (D-15, MOUNT-07) — the over-enumeration fix. |
| T-04.3-11-03 | Tampering | loose override path traversal | mitigate | Loose reads go through readVfsEntryBytes / existing path-confinement; no new unvalidated fs walk. |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; reuses tocReader/readVfsEntryBytes/native binding. |
</threat_model>

<verification>
- treAutoMount + tocReader tests green (master sourcing, dedup, lazy searchPath, regression).
- D-16 gate (plan 12) confirms a real swg-client-v2 mount: patch_sku3_* browse + open-in-viewer end-to-end.
</verification>

<success_criteria>
searchTOC clients mount the FULL base from the master .toc (empty-internal-TOC v6000 containers contribute
entries), no double-count, searchPath is lazy (no junk rows, override still wins), viewer reads loose
winners — completing TRE-05; searchTree clients unaffected.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-11-SUMMARY.md` when done.
</output>
