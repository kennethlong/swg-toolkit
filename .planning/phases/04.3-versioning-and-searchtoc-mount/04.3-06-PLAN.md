---
phase: 04.3-versioning-and-searchtoc-mount
plan: 06
type: execute
wave: 2
depends_on: ["04.3-02"]
files_modified:
  - packages/renderer/src/panels/deploy/VersionHistoryBody.tsx
autonomous: true
requirements: [VER-03, VER-04, VER-05, VER-08, VER-06, GRAPH-01, GRAPH-06, GRAPH-07, GRAPH-08]
must_haves:
  truths:
    - "Clicking a version row SILENTLY reconciles the live client to it via syncLiveToVersion (D-04) — the version tree IS the deploy control; no preview/Deploy two-step. The Baseline node click reconciles to STOCK via syncLiveToVersion(BASELINE_ID), NOT selectVersion (H2 restore-to-stock)."
    - "An unsaved-changes confirm fires ONLY when uncommitted working changes would be discarded by navigating away (D-07); clean navigation is silent."
    - "The stale-deployment banner is removed (D-08: selected ≡ live, single pointer); per-row Deploy/Revert hover actions are NOT added (D-09)."
    - "'Branch from here' is kept but sets the branch-from parentId INTENT for the NEXT save only — it does NOT reconcile or navigate, so the D-07 confirm does not apply and the live client is unchanged (D-09)."
    - "A concrete Undo affordance (an Undo button/menu item AND a Ctrl+Z handler) calls useUndoStore.undo() and re-runs syncLiveToVersion(prior) — VER-04 is performable; rapid row/Baseline clicks are GUARDED while a reconcile is async in-flight (single isReconciling flag)."
    - "VersionHistoryBody renders the two-column graph (LaneGutter beside the row list), re-themed rows, status footer, and WIP stub state."
  artifacts:
    - path: "packages/renderer/src/panels/deploy/VersionHistoryBody.tsx"
      provides: "graph-composed, reconcile-wired version history"
      contains: "syncLiveToVersion"
  sketch_diff:
    sketch: ".planning/sketches/002-version-graph-timeline/index.html (#variant-a, lines 311-469)"
    elements:
      - "GRAPH-01 / A1: two-column .graph-container — 90px lane-col (<LaneGutter/>) BESIDE .graph-rows-col (independently-laid-out React rows)"
      - "GRAPH-08 / A13: 52px graph-row sizing, row hover background, is-active accent LEFT-border on the live row (not branch rows)"
      - "GRAPH-06 / A9: vN mono label column (.ver-label)"
      - "GRAPH-06 / A10: meta line 'date · N files · branch from vN' (file count + branch provenance, not raw timestamp/'N deltas')"
      - "GRAPH-07 / A11: bottom status footer 'N versions · N branches · live: vN — label'"
      - "VER-08 / A8: state pip collapses to ONE 'live' marker (D-13) + 'root' label; NO active/deployed split"
      - "VER-08: keep 'Branch from here' row action; DROP per-row Deploy/Revert (D-09)"
      - "KEEP (not gaps): Baseline node, per-row ▸ delta expansion, save-version affordance"
---

<objective>
Rework VersionHistoryBody to BE the deploy control: compose the branch-tree graph (plan 05's LaneGutter
+ laneLayout) beside re-themed rows, wire row-click → syncLiveToVersion (plan 04) behind the D-07
unsaved-changes confirm, retire the stale banner (D-08), keep "Branch from here", drop per-row
Deploy/Revert (D-09), and add the status footer + WIP-stub state.

This owns ONLY VersionHistoryBody.tsx. It imports syncLiveToVersion / useUndoStore / setLiveVersion
(plan 04) and laneLayout / LaneGutter (plan 05) — all available as Wave-0 contracts (plan 02) and
implemented in parallel this wave. Component tests mock the service so this plan is order-independent.

Purpose: close the load-bearing 002-A gap (flat list → Git-Graph Lanes) AND make navigation = deploy.
Output: reworked VersionHistoryBody.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md
@.planning/sketches/002-version-graph-timeline/index.html

<interfaces>
From plan 04 (services/state):
  syncLiveToVersion(targetId, ctx): Promise<LiveReconcileResult>
  useUndoStore (push/undo/canUndo)
  changesetService.setLiveVersion(id)
  changesetService.flatten / flatEqual  (to detect uncommitted working changes for D-07)
From plan 05 (graph):
  laneLayout(changesets, liveVersionId, hasUncommittedWork): GraphLayout
  <LaneGutter layout={GraphLayout} />
Current file: VersionHistoryBody.tsx — :33-41 branchSet(); :66-67 stale calc (REMOVE); :87 single-column (REPLACE w/ two-column); :103-147 Baseline node (KEEP); :174-218 row render + ▸ delta expansion (KEEP expansion; re-theme rows); :181-182 node colors (REMOVE inline, use LaneGutter)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-column graph layout + re-themed rows + footer + WIP stub</name>
  <read_first>
    - packages/renderer/src/panels/deploy/VersionHistoryBody.tsx (FULL — the surface being reworked; note KEEP items: Baseline node, ▸ delta expansion)
    - .planning/sketches/002-version-graph-timeline/index.html:311-469 (two-column container, row content, meta line, status footer)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (rows A1,A8,A9,A10,A11,A12,A13 + KEEP list)
    - packages/renderer/src/panels/deploy/laneLayout.ts + LaneGutter.tsx (consume)
  </read_first>
  <files>packages/renderer/src/panels/deploy/VersionHistoryBody.tsx</files>
  <action>
    Replace the single flex column (`:87`) with a `.graph-container` two-column layout: left `.lane-col` rendering `<LaneGutter layout={laneLayout(changesets, liveVersionId, hasUncommittedWork)} />` (90px), right `.graph-rows-col` rendering the existing row list at 52px row height aligned to the gutter node centers. Re-theme rows per A8/A9/A10/A12/A13: `.ver-label` mono vN column; meta line `date · N files · branch from vN` (compute file count from the changeset deltas; show "branch from vN" only for branch nodes via branchSet/parentId; vN = 1-based OLDEST-first ordinal, RESEARCH PRIMARY step 13 — the SAME derivation feeds the footer "live: vN" and plan 09's statusbar so the numbers agree); collapse the state pip to ONE "live" marker on the live row (D-13) + "root" label on the root; apply `is-active` accent LEFT-border to the LIVE row only (not branch rows — fixes A13). Remove inline node `●` colors (`:181-182`) — nodes now live in LaneGutter. Add the bottom status footer (A11): `N versions · N branches · live: vN — label`. Drive the WIP stub by passing `hasUncommittedWork` (computed in Task 2) into laneLayout. KEEP the Baseline node and the ▸ per-row delta expansion. Component test: renders two columns; footer shows correct counts; live row has the accent left-border; meta line shows file count + branch provenance.
  </action>
  <acceptance_criteria>
    - `.graph-container` renders `<LaneGutter>` (an `<svg>`) BESIDE the row list (two columns) — grep VersionHistoryBody for `LaneGutter` and `graph-rows` both present.
    - Status footer renders `N versions · N branches · live: …` (A11) — asserted in component test.
    - Meta line shows "N files" + "branch from vN" for a branch node (A10), not "N deltas"/raw timestamp.
    - is-active accent left-border applies to the LIVE row only (A13) — asserted.
    - Inline hardcoded node color hex removed from VersionHistoryBody (grep -v '^\s*//' | grep -c "#22c55e" == 0).
    - ▸ delta expansion + Baseline node still present (KEEP) — existing rows still expand.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/VersionHistoryBody 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>VersionHistoryBody renders the two-column Git-Graph-Lanes surface with re-themed rows, footer, WIP stub; KEEP items preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Row-click → silent reconcile, D-07 confirm, retire stale banner, keep Branch-from-here</name>
  <read_first>
    - packages/renderer/src/panels/deploy/VersionHistoryBody.tsx:66-67 (stale calc to REMOVE) + :92-99 (stale banner to REMOVE) + :107-110 and :129-132 (Baseline node onClick — currently selectVersion(BASELINE_ID), rewire to syncLiveToVersion)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (design decisions 2 + 4 — single pointer + D-07 confirm via flatEqual(currentStaging, flatten(live)))
    - packages/renderer/src/services/syncLiveToVersion.ts + state/undoStore.ts (plan 04 contracts)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md (D-04, D-07, D-08, D-09)
  </read_first>
  <files>packages/renderer/src/panels/deploy/VersionHistoryBody.tsx</files>
  <action>
    Change row click from `selectVersion` (preview) to: compute `hasUncommittedWork = !flatEqual(currentStaging, flatten(liveVersionId))` ONCE per render (not twice); if true, show the D-07 confirm ("Discard unsaved changes and switch to vN?") — ONLY then; if false, proceed SILENTLY. On proceed, call `syncLiveToVersion(targetId, ctx)` then `setLiveVersion(targetId)` (the reconcile + undoStore.push happen inside the service). ALSO rewire the Baseline node onClick handlers (VersionHistoryBody.tsx:107-110 and :129-132) from `selectVersion(BASELINE_ID)` to the same reconcile path -> `syncLiveToVersion(BASELINE_ID, ctx)` + `setLiveVersion(BASELINE_ID)` (else "restore to stock" never fires — H2). GUARD the in-flight reconcile race: a single `isReconciling` flag disables/ignores row + Baseline clicks while a reconcile promise is pending (rapid navigation must not queue concurrent reconciles that stomp B3 snapshots). Add a concrete UNDO affordance (H5): an "Undo" button/menu item AND a Ctrl+Z key handler that calls `useUndoStore.undo()` and, if a snapshot is returned, re-runs `syncLiveToVersion(snapshot.priorLiveVersionId, ctx)` (VER-04 performable). Specify "Branch from here" (D-09): it sets the branch-from parentId INTENT for the NEXT save (next sealVersion parentId) WITHOUT reconciling or navigating — the D-07 confirm does NOT apply and the live client is unchanged. Remove the `stale` computation (`:66-67`) and the stale-deployment banner (`:92-99`) — D-08 makes selected ≡ live. Keep the "Branch from here" row action (D-09); do NOT add per-row Deploy/Revert hover actions (D-09 dropped — sketch A7's Deploy/Revert buttons are intentionally omitted). Component test (mock syncLiveToVersion): clean-state click calls syncLiveToVersion with NO confirm; dirty-state click shows confirm and only calls on accept; Baseline click calls syncLiveToVersion(BASELINE_ID) not selectVersion; a click during an in-flight reconcile is ignored; Ctrl+Z/Undo re-runs syncLiveToVersion to the prior version; "Branch from here" does NOT call syncLiveToVersion; stale banner absent from DOM.
  </action>
  <acceptance_criteria>
    - Clean-working-set row click calls `syncLiveToVersion` directly with no confirm dialog (mock asserted).
    - Dirty-working-set row click renders the D-07 confirm; accepting calls syncLiveToVersion, cancelling does not.
    - The Baseline node onClick calls syncLiveToVersion(BASELINE_ID) (restore-to-stock), NOT selectVersion (grep VersionHistoryBody: the Baseline onClick references syncLiveToVersion, not selectVersion(BASELINE_ID)).
    - An Undo affordance (button + Ctrl+Z) calls useUndoStore.undo() and re-runs syncLiveToVersion to the prior version (VER-04) — asserted.
    - A click during an in-flight reconcile is ignored (isReconciling guard) — asserted.
    - "Branch from here" sets the next-save parentId intent without reconciling/navigating (no syncLiveToVersion call on that action) — asserted.
    - Stale banner removed: grep VersionHistoryBody for the old stale-banner copy/`stale` flag returns 0 in non-comment lines.
    - "Branch from here" action present; NO "Deploy vN"/"Revert here" buttons added (grep -c "Revert here" == 0, "Deploy v" per-row == 0).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/VersionHistoryBody 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Row click = silent reconcile (confirm only on unsaved work); stale banner gone; Branch-from-here kept; per-row Deploy/Revert not added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| row click → reconcile service | User action triggers a real client deploy/revert via syncLiveToVersion (boundary owned by plan 04). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-06-01 | Tampering/availability | accidental destructive navigation (discarding unsaved work) | mitigate | D-07 confirm fires when uncommitted work would be lost; reconcile is undoable (undoStore, plan 04). |
| T-04.3-06-02 | Repudiation | user unaware client was mutated by a click | mitigate | Single live pointer + status footer "live: vN" makes the live state always visible (D-08). |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; UI-only. |
</threat_model>

<verification>
- VersionHistoryBody component tests green (two-column render, footer, confirm logic).
- Sketch-diff A1/A8/A9/A10/A11/A12/A13 verified element-by-element at the combined UAT (plan 13).
</verification>

<success_criteria>
The version history is the Git-Graph-Lanes surface where clicking a version silently reconciles the live
client (confirm only on unsaved work), the stale concept is gone, Branch-from-here is kept, per-row
Deploy/Revert are intentionally absent.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-06-SUMMARY.md` when done.
</output>
