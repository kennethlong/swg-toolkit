---
phase: 04.3-versioning-and-searchtoc-mount
plan: 09
type: execute
wave: 1
depends_on: ["04.3-02"]
files_modified:
  - packages/renderer/src/shell/Titlebar.tsx
  - packages/renderer/src/shell/StatusBar.tsx
  - packages/renderer/src/workspace/workspace-config.ts
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04, SHELL-05, SHELL-06]
must_haves:
  truths:
    - "The shell matches sketch 008: a 'Deploy' titlebar menu item; the right-dock asset tab labeled 'Inspect'; theme <select> grouped into Accent/Accessibility optgroups; statusbar gains an sb-state chip and a 'live: vN' indicator; the bottom pane is a Datatable/Console/Log tab trio."
    - "S3 (VCS-tab placement) is NOT changed — it stays AMBIGUOUS/deferred per the maintainer; the 3-tab group is left as-is and the open question is re-surfaced in the SUMMARY."
  artifacts:
    - path: "packages/renderer/src/shell/Titlebar.tsx"
      provides: "Deploy menu item + theme optgroups"
    - path: "packages/renderer/src/shell/StatusBar.tsx"
      provides: "sb-state chip + live: vN indicator"
    - path: "packages/renderer/src/workspace/workspace-config.ts"
      provides: "Inspect tab label + Datatable/Console/Log bottom-pane trio"
  sketch_diff:
    sketch: ".planning/sketches/008-shell-composition/index.html"
    elements:
      - "SHELL-01 / S2: 'Deploy' titlebar menu item (current menu = File/Edit/View/Asset/Window/Help, no Deploy)"
      - "SHELL-02 / S4: right-dock asset tab label 'Inspect' (currently 'Inspector')"
      - "SHELL-03 / S5: theme <select> Accent / Accessibility optgroups (currently flat option list)"
      - "SHELL-04 / S6: statusbar 'sb-state' chip ('Deploy active · dock 440px')"
      - "SHELL-05 / S7: statusbar 'live: vN' indicator (currently hashed 'deployed: <id.slice(0,8)>')"
      - "SHELL-06 / S8: bottom pane = Datatable / Console / Log tab trio (currently single 'Data' panel)"
      - "P10 (PLAUSIBLE re-verify, gap review 007): the asset/welcome tab label 'Assets' → 'Welcome' — re-verify vs the sketch; relabel if confirmed, else record disposition in SUMMARY. (Lives in workspace-config.ts = this plan's scope, not plan 08's.)"
      - "DEFERRED — S3: VCS-tab placement stays AMBIGUOUS; do NOT move VCS; re-surface in SUMMARY"
      - "KEEP (not gaps): auto-widen 440/290 contract, Layout ▾ toolbar (Reset/Reopen), diagnostic statusbar chips, window-control circles"
---

<objective>
Close the sketch-008 shell-composition gaps (S2, S4–S8) — all chrome/copy/structure against the cited
sketch elements. Explicitly DO NOT resolve S3 (VCS-tab placement): it stays AMBIGUOUS per the maintainer;
leave the 3-tab group as-is and re-surface the open question.

Aligns the statusbar "live: vN" with D-08's single live pointer (replacing the hashed "deployed: <id>").

Purpose: the shell matches its UI contract; the live indicator speaks the new single-pointer model.
Output: reworked Titlebar.tsx, StatusBar.tsx, workspace-config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md
@.planning/sketches/008-shell-composition/index.html

<interfaces>
Current evidence (gap review):
  Titlebar.tsx:79 (menu File/Edit/View/Asset/Window/Help — no Deploy), :113-143 (flat theme option list)
  StatusBar.tsx:322-335 (shows 'deployed: <id.slice(0,8)>' — replace with 'live: vN')
  workspace-config.ts:92 (tab 'Inspector' → 'Inspect'), :99-104 (single 'Data' panel → Datatable/Console/Log trio), :113,120-123 (3-tab inspector group incl. Version Control — DO NOT CHANGE: S3 deferred)
Live pointer source: changesetService manifest single live pointer (plan 04 setLiveVersion) — statusbar reads the live version label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Titlebar — Deploy menu item + theme optgroups</name>
  <read_first>
    - packages/renderer/src/shell/Titlebar.tsx (:79 menu, :113-143 theme select)
    - .planning/sketches/008-shell-composition/index.html (titlebar menu + theme groups)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (S2,S5 + KEEP)
  </read_first>
  <files>packages/renderer/src/shell/Titlebar.tsx</files>
  <action>
    S2: add a "Deploy" titlebar menu item (between Asset and Window per the sketch ordering) whose actions surface the Deploy panel (focus/open the Deploy dock tab). S5: group the theme `<select>` options into `<optgroup label="Accent">` and `<optgroup label="Accessibility">` per the sketch (currently a flat list :113-143). Keep the existing window-control circles + Layout toolbar (KEEP).
  </action>
  <acceptance_criteria>
    - Titlebar menu includes a "Deploy" item (grep -c '>Deploy<' or menu-item label == 1).
    - Theme select renders two `<optgroup>`s (Accent, Accessibility) — component test asserts optgroup labels.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/shell/Titlebar 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Deploy menu item + theme optgroups present per sketch 008.</done>
</task>

<task type="auto">
  <name>Task 2: StatusBar sb-state chip + 'live: vN' indicator</name>
  <read_first>
    - packages/renderer/src/shell/StatusBar.tsx (:322-335 'deployed: <id>' — replace)
    - .planning/sketches/008-shell-composition/index.html (sb-state chip + live indicator)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md (D-08 single live pointer) + .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (PRIMARY step 13 — vN ordinal convention) + packages/renderer/src/state/clientScanStore.ts (useClientScanStatus from plan 02)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (S6,S7)
  </read_first>
  <files>packages/renderer/src/shell/StatusBar.tsx</files>
  <action>
    S6: add an `sb-state` chip showing the active dock state ("Deploy active · dock 440px") sourced from the workspace/dock state. S7: replace the hashed "deployed: <id.slice(0,8)>" (:322-335) with a "live: vN — label" indicator reading the single live pointer (`manifest.deployedVersionId` — the canonical live pointer per D-08) and deriving vN via the 1-based OLDEST-first ordinal convention (RESEARCH PRIMARY step 13 — the SAME derivation as plan 06's footer/meta line, so the numbers agree). Wire the first-run scan message from the shared `clientScanStore` (useClientScanStatus, written by plan 08 P9) into the statusbar. Keep the existing diagnostic chips (addon/SAB/zero-copy/mount/vfs/COI/pnpm) (KEEP).
  </action>
  <acceptance_criteria>
    - Statusbar shows "live: vN — label" reading manifest.deployedVersionId (canonical) with the oldest-first vN ordinal (matches plan 06's footer) — component test asserts the live label renders (grep -c 'deployed:' in non-comment lines == 0).
    - The first-run scan message is read from the shared clientScanStore (useClientScanStatus) — the same store plan 08 writes — asserted.
    - sb-state chip renders the dock-active state (S6).
    - Existing diagnostic chips still present (KEEP) — not removed.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/shell/StatusBar 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Statusbar shows sb-state chip + 'live: vN' aligned to the single live pointer.</done>
</task>

<task type="auto">
  <name>Task 3: workspace-config — 'Inspect' tab + Datatable/Console/Log trio; P10 re-verify; S3 left as-is</name>
  <read_first>
    - packages/renderer/src/workspace/workspace-config.ts (:92 'Inspector', :99-104 single Data panel, :113/:120-123 3-tab group — DO NOT move VCS)
    - .planning/sketches/008-shell-composition/index.html (tab labels + bottom-pane trio)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (S4,S8 + S3 DEFERRED decision + P10 PLAUSIBLE row)
    - .planning/sketches/007-project-entry/index.html (P10 — the welcome/assets tab label the sketch specifies)
    - packages/renderer/src/workspace/workspace-config.test.ts (LAYOUT_VERSION + panel-id guard conventions)
  </read_first>
  <files>packages/renderer/src/workspace/workspace-config.ts</files>
  <action>
    S4: rename the asset tab label "Inspector" → "Inspect" (:92). S8: change the single "Data" panel (:99-104) into a Datatable / Console / Log tab trio (3 panels in the bottom-pane group) — wire the inactive Console/Log tabs per the folded `inapp-console-log-tabs-inactive` item (lower priority; at minimum render the three tabs with Datatable active, Console/Log as present-but-stub panes). If panel IDs change, bump LAYOUT_VERSION with the existing migration guard (per the established :LAYOUT_VERSION pattern). P10 (PLAUSIBLE re-verify): check sketch 007 for the welcome/assets tab label — the build's tab reads "Assets" while the sketch is reported to label it "Welcome". If the sketch confirms "Welcome", relabel the tab here (pure copy change). If the sketch does NOT clearly say "Welcome" (or "Assets" is the intended label), leave it and record the disposition in the SUMMARY — do not silently drop it. S3: DO NOT move or regroup Version Control — leave the 3-tab inspector group exactly as-is (:113,120-123); record in the SUMMARY that S3 remains AMBIGUOUS and is re-surfaced for a maintainer decision (same-group 3rd tab vs separate dock region).
  </action>
  <acceptance_criteria>
    - Asset tab label is "Inspect" (S4) — grep -c "'Inspector'" in the tab def == 0; "Inspect" present.
    - Bottom pane defines Datatable + Console + Log panels (S8) — three panel ids present.
    - LAYOUT_VERSION bumped + migration guard intact if ids changed (workspace-config.test.ts green).
    - P10 dispositioned: EITHER the welcome/assets tab is relabeled "Welcome" (sketch-confirmed), OR the SUMMARY records why it was kept "Assets" — never silently dropped.
    - Version Control grouping UNCHANGED (S3) — the 3-tab inspector group (:113,120-123) byte-unchanged except unrelated edits; SUMMARY re-surfaces S3.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/workspace/workspace-config 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>'Inspect' tab + Datatable/Console/Log trio landed; P10 dispositioned (relabeled or recorded); VCS placement left as-is; S3 re-surfaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Shell chrome/config only; no I/O or untrusted input crosses a boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-09-01 | Availability | layout-id change soft-bricks a saved layout | mitigate | LAYOUT_VERSION bump + existing migration guard (workspace-config.test.ts) — established pattern from 04.1-05. |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; shell config/markup only. |
</threat_model>

<verification>
- Component/config tests green for Titlebar, StatusBar, workspace-config.
- Sketch-diff S2/S4/S5/S6/S7/S8 verified at the combined UAT (plan 13); S3 left as-is and re-surfaced.
</verification>

<success_criteria>
The shell matches sketch 008 (Deploy menu, Inspect tab, theme optgroups, sb-state chip, live: vN,
Datatable/Console/Log trio); the live indicator reflects the single live pointer; VCS placement is not
silently moved.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-09-SUMMARY.md` when done.
</output>
