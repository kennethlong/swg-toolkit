---
phase: "04.3"
plan: "08"
subsystem: "deploy-panel / project-entry"
tags: [ui, sketch-007, project-entry, binding-bar, wizard, scan-message, dim-hook]
dependency_graph:
  requires: ["04.3-02"]   # clientScanStore RED tests created here; this plan makes them GREEN
  provides: ["04.3-09"]   # StatusBar reads clientScanStore written here
  affects:  ["packages/renderer/src/panels/deploy", "packages/renderer/src/state", "packages/renderer/src/services"]
tech_stack:
  added: []
  patterns: ["Zustand real setter replacing stub no-op", "stable dock-group ID constants", "split host+port fields"]
key_files:
  created:
    - packages/renderer/src/panels/deploy/ProjectBindingBar.test.tsx
    - packages/renderer/src/panels/deploy/WorkspaceEntry.test.tsx
    - packages/renderer/src/state/useNoProjectDim.test.ts
    - packages/renderer/src/panels/deploy/NewProjectWizard.test.tsx
  modified:
    - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx
    - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
    - packages/renderer/src/state/useNoProjectDim.ts
    - packages/renderer/src/state/clientScanStore.ts
    - packages/renderer/src/services/clientLocator.ts
    - packages/renderer/src/panels/deploy/NewProjectWizard.tsx
decisions:
  - "P5 stable dock-group IDs: DIM_GROUP_BOTTOM_PANE = 'bottom-pane-group' (NOT panel label 'Data') — survives plan-09 split into Datatable/Console/Log"
  - "P9 scan message format: 'detected: {found} ✓ · {missing} ✗' joined with · separator"
  - "getKnownClientPaths() Rule 2 addition: clientLocator.ts exports known install paths to support P4 not-found diff"
metrics:
  duration_minutes: 20
  completed_date: "2026-06-30"
  tasks_completed: 3
  files_changed: 10
---

# Phase 04.3 Plan 08: Project Entry Front-Door Fidelity (Sketch 007) Summary

JWT auth with refresh rotation using jose library — wait, wrong template. Real one-liner:

**Project-entry front-door fidelity gaps P1–P9+P12 closed: bound header with project name + pill chip, live Open Recent, not-found client rows, group-based dim hook, scan message, editable wizard folders, split host:port, × close.**

## What Was Built

### Task 1 — ProjectBindingBar.tsx (P1, P2, P3)

- **P1 project name in bound header**: proj-bar row renders `◆ {wsInfo.projectName ?? workspaceName}` when a workspace is bound (clientPath non-null). Uses `var(--color-accent)` for the ◆ glyph.
- **P2 pill chip**: replaced plain chip with `<span>` styled as pill (`border-radius: var(--radius-full, 999px)`, `background: var(--color-accent-dim)`, `border: 1px solid var(--color-accent-line)`). Shows `⛁ {clientName}` + optional `· EERT{treVersion}` suffix.
- **P3 live Open Recent**: `getRecentProjects()` loaded in `useEffect`; when list non-empty, dropdown renders clickable `role="menuitem"` entries. Clicking opens workspace via `openWorkspace()`. Falls back to aria-disabled static "Open Recent" when list is empty.
- **Tests**: 5/5 pass (P1 name, P2 pill, P3 live recents, no-project state, D-13 sub-row preserved).

### Task 2 — WorkspaceEntry.tsx + useNoProjectDim.ts + clientScanStore.ts + clientLocator.ts (P4, P5, P9)

- **P4 not-found rows**: `WorkspaceEntry` diffs `detectClients()` results against `getKnownClientPaths()`. Missing installs render as disabled rows with red `✗ not found` pill (`rgba(224,88,79,.12)` bg, `rgba(224,88,79,.40)` border).
- **P5 group-based dim**: `useNoProjectDimForGroup(groupId)` exported from `useNoProjectDim.ts` with stable constants `DIM_GROUP_VIEWPORT`, `DIM_GROUP_INSPECTOR`, `DIM_GROUP_BOTTOM_PANE`. NOT tied to panel label "Data" — survives plan-09 trio split.
- **P9 scan message**: after `detectClients()` in `WorkspaceEntry` useEffect, writes `"detected: SWG Infinity ✓ · SWGEmu ✗"` format to `clientScanStore` via `setClientScanMessage`.
- **clientScanStore RED → GREEN**: changed Wave-0 no-op setter `(_msg) => {}` → real Zustand setter `(msg) => set({ message: msg })`. Makes plan-02's 2 RED tests GREEN without changing public signatures.
- **Rule 2 deviation**: added `getKnownClientPaths(): KnownClientPath[]` to `clientLocator.ts` — necessary for P4 missing-client diff. Not in original files_modified but required for correctness.
- **Tests**: 14/14 pass (5 WorkspaceEntry + 9 useNoProjectDim).

### Task 3 — NewProjectWizard.tsx (P4 wizard, P6, P7, P8, P12)

- **P12 × close button**: `<button aria-label="Close wizard" title="Close" onClick={onClose}>×</button>` in dialog-header alongside step counter. Disabled during busy state. Confirmed from sketch 007 line 745-746.
- **P6 editable project folder**: replaced read-only "Stored in" hint with an editable `<input aria-label="Project folder">` + `⊕ Browse…` button (`title="Browse for project folder"`). Browse invokes `workspace:pick-dir` IPC and sets folder state.
- **P4 wizard not-found rows**: Step 2 `BindClient` section computes `missingClients` diff from `getKnownClientPaths()`. Renders disabled rows with red pill for each missing known install.
- **P7 Browse server**: Step 3 server folder row gets Browse button (`aria-label="Browse for server folder"`) beside the path input.
- **P8 split host:port**: replaced single `hostPort` state with `host` (default `127.0.0.1`) + `port` (default `44463`). Rendered as two-column grid (`gridTemplateColumns: '1fr 84px'`). Synced to `serverConfig` as `hostPort: '${host}:${port}'`. Confirmed from sketch 007 CSS `.hostport { grid-template-columns: 1fr 84px }`.
- **Tests**: 8/8 pass (all P4/P6/P7/P8/P12 elements verified).

## Sketch 007 Element Diff

Governing sketch: `.planning/sketches/007-project-entry/index.html`

| Element | Sketch variant | Status |
|---------|---------------|--------|
| P1: project name with ◆ in bound header | 007-A | PRESENT |
| P2: bound-client pill chip (radius-full + accent colors) | 007-A | PRESENT |
| P3: live Open Recent links in dropdown | 007-A | PRESENT |
| P4: ✗ not found rows in WorkspaceEntry | 007-B | PRESENT |
| P4: ✗ not found rows in wizard Step 2 | 007-C | PRESENT |
| P5: group-based dim hook (stable dock-group IDs) | 007-B | PRESENT |
| P6: editable project folder input + Browse button | 007-C Step 1 | PRESENT |
| P7: Browse button for server folder in Step 3 | 007-C Step 3 | PRESENT |
| P8: split host + port inputs (two-column grid) | 007-C Step 3 `.hostport` | PRESENT |
| P9: scan message written to clientScanStore | 007-B status bar (plan 09) | PRESENT |
| P12: × close button in wizard dialog header | 007-C | PRESENT |

All P1–P9 + P12 elements confirmed present. No sketch element missing.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Added `getKnownClientPaths()` to clientLocator.ts**
- **Found during:** Task 2 (P4 implementation)
- **Issue:** `detectClients()` only returns FOUND clients. P4 requires diff against a set of KNOWN clients to compute the "not found" rows. No function existed to get the known client set.
- **Fix:** Added `KnownClientPath` interface and `getKnownClientPaths()` returning the two canonical SWG install paths. Without this, P4 could not be implemented.
- **Files modified:** `packages/renderer/src/services/clientLocator.ts`
- **Commit:** b2cbf9d

### Test Fixes (not in plan)

**1. [Rule 1 - Bug] `toBeDisabled` jest-dom matcher not in vitest setup**
- `NewProjectWizard.test.tsx` initially used `.not.toBeDisabled()` — jest-dom matcher not available without import. Fixed to use `(button as HTMLButtonElement).disabled === false` via native DOM check.

**2. [Rule 1 - Bug] `getByText(/STEP 2/i)` ambiguous**
- Both "Step 2 of 4" header counter and "STEP 2 — BIND CLIENT" body heading matched the regex. Fixed navigation helpers to use `getAllByText` and assert `length > 0`.

## Wave Constraint: clientScanStore RED → GREEN

Plan 02 created `clientScanStore.ts` with a stub no-op setter and 2 RED tests.
This plan made the setter real (`(msg) => set({ message: msg })`), turning those 2 tests GREEN.
Public interface unchanged: `message: string | null`, `setMessage(msg: string | null): void`.

## Known Stubs

None. All implemented features have live wired data paths:
- P3 recents: reads from `getRecentProjects()` (persistent store)
- P4 not-found: diffs against `getKnownClientPaths()` (real known installs)
- P9 message: written to `clientScanStore.setMessage()` after `detectClients()` scan
- P8 host:port: synced to `serverConfig.hostPort` on wizard finish

## Threat Flags

No new network endpoints, auth paths, or trust-boundary schema changes introduced.

## Self-Check: PASSED

### Files verified present:
- `packages/renderer/src/panels/deploy/ProjectBindingBar.test.tsx` — FOUND
- `packages/renderer/src/panels/deploy/WorkspaceEntry.test.tsx` — FOUND
- `packages/renderer/src/state/useNoProjectDim.test.ts` — FOUND
- `packages/renderer/src/panels/deploy/NewProjectWizard.test.tsx` — FOUND

### Commits verified:
- `cdc1809` — Task 1: ProjectBindingBar P1/P2/P3
- `b2cbf9d` — Task 2: WorkspaceEntry P4/P5/P9 + clientScanStore + clientLocator
- `93ec139` — Task 3: NewProjectWizard P4/P6/P7/P8/P12

### Tests: 22 new pass, 0 regressions (pre-existing 2 RED in tocReader stay RED until plan 11)
### TypeScript: `tsc --noEmit` clean (0 errors)
