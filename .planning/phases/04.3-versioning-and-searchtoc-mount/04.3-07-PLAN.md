---
phase: 04.3-versioning-and-searchtoc-mount
plan: 07
type: execute
wave: 1
depends_on: ["04.3-02"]
files_modified:
  - packages/renderer/src/panels/deploy/DeployPanel.tsx
  - packages/renderer/src/panels/deploy/StagingPanelBody.tsx
  - packages/renderer/src/panels/deploy/ActionBadge.tsx
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
autonomous: true
requirements: [DEPLOYUI-01, DEPLOYUI-02, DEPLOYUI-03, DEPLOYUI-04, DEPLOYUI-05, DEPLOYUI-06, DEPLOYUI-07, DEPLOYUI-08, DEPLOYUI-09, DEPLOYUI-10, DEPLOYUI-11, DEPLOYUI-12, DEPLOYUI-13, DEPLOYUI-14, DEPLOYUI-15, VER-07, VER-06]
must_haves:
  truths:
    - "The Deploy panel has collapsible Working-changes / Version-history sections with the splitter auto-hidden when a section is collapsed (006-D contract)."
    - "The staging surface gains the 005/006 chrome: section headers, working-vs-saved split, row selection, ActionBadge pill, changed-vs-base dot, kB payload footer, hover-only ×, row context-menu parity."
    - "The Deploy modal is forward-deploy only: model picker shown ONLY for a new forward deploy (VER-07/D-10), modal titled 'Deploy vN — label', detected/not-found client rows, '(recommended)' model label."
  artifacts:
    - path: "packages/renderer/src/panels/deploy/DeployPanel.tsx"
      provides: "collapsible sections + auto-hide splitter + section headers + footer hint + full-width CTA"
    - path: "packages/renderer/src/panels/deploy/StagingPanelBody.tsx"
      provides: "staging chrome (selection, working-vs-saved split, dot, kB footer, hover-× , context menu)"
    - path: "packages/renderer/src/panels/deploy/ActionBadge.tsx"
      provides: "pill-styled badge + 'modify' label"
    - path: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      provides: "forward-deploy-only picker scope + modal title + detected/not-found rows + recommended label"
  sketch_diff:
    sketch: ".planning/sketches/005-deploy-inspect-tab/index.html + 006-combined-deploy-tab/index.html"
    elements:
      - "DEPLOYUI-01 / D3: collapsible sections (▾ caret + collapse state) for Working changes & Version history"
      - "DEPLOYUI-02 / D4: splitter auto-hidden when a section is collapsed"
      - "DEPLOYUI-03 / D5: section headers '● Working changes (uncommitted)' + file count; 'Version History'"
      - "DEPLOYUI-04 / D6: working-vs-saved split — 'Based on vN' divider + dimmed 'Saved in vN' rows"
      - "DEPLOYUI-05 / D7: staging row selection (click → accent bg + inset left bar)"
      - "DEPLOYUI-06 / D8: ActionBadge as pill (bg+border), label 'modify'"
      - "DEPLOYUI-07 / D9: changed-vs-base ●/○ dot ('identical to base')"
      - "DEPLOYUI-08 / D10: staging footer payload total '… · NNN kB payload'"
      - "DEPLOYUI-09 / D11: hover-only × remove (opacity 0→1)"
      - "DEPLOYUI-10 / D12: row context menu parity (editor / viewport / reveal-in-TRE / compare-to-base)"
      - "DEPLOYUI-11 / D13: deploy footer instructional hint ('collapse a section · expand a version…')"
      - "DEPLOYUI-12 / D14: Deploy CTA full-width"
      - "DEPLOYUI-13 / D15: Deploy modal title 'Deploy vN — label'"
      - "DEPLOYUI-14 / D16: 'detected' badge + greyed 'not found' client rows in modal"
      - "DEPLOYUI-15 / D17: '(recommended)' label on preferred deploy model"
      - "VER-07: deploy-model picker shown ONLY for a new forward deploy; hidden when navigating/reverting"
      - "KEEP (not gaps): renamed deploy-model set (Absolute/Loose/Advanced), build/activate progress, Reset flow, unsaved-changes prompt, config-slot preview, drag-drop staging, empty-state card, invalid-path warnings"
---

<objective>
Land the 005/006 Deploy-tab composition gaps: collapsible sections + auto-hide splitter (006-D), the
full staging chrome, and the forward-deploy-only Deploy modal (VER-07/D-10). Mostly CSS/markup/state
fidelity against the cited sketch elements — but the picker-scope change is real behavior.

Disjoint from VersionHistoryBody (plan 06 owns that); DeployPanel composes it but only wraps it in a
collapsible section header here.

Purpose: the combined Deploy surface matches sketches 005-B/006-D element-by-element; the Deploy modal
is unambiguously "publish a new version forward" (not a navigation step).
Output: reworked DeployPanel.tsx, StagingPanelBody.tsx, ActionBadge.tsx, DeployDialog.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/sketches/005-deploy-inspect-tab/index.html
@.planning/sketches/006-combined-deploy-tab/index.html

<interfaces>
Current files (gap-review evidence):
  DeployPanel.tsx:187,196-207,209,248-261 — sections, splitter, CTA (right-aligned now)
  StagingPanelBody.tsx:644 (aria-selected hardcoded false), :779-799 (× always visible), :680-705 (context menu = Open in viewer + Unstage only), :839 (footer no kB)
  ActionBadge.tsx:21-58 (inline glyph+text, no pill)
  DeployDialog.tsx:144-159 (auto-selects client silently), :707-708 (static "Deploy patch" title)
VER-07 source: RESEARCH design decision 3 — picker governs ONLY a NEW forward deploy (DeployDialog); reverting/navigating reads the version's stored deployRecord (handled in plan 04 — no picker).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeployPanel collapsible sections + auto-hide splitter + headers + footer hint + full-width CTA</name>
  <read_first>
    - packages/renderer/src/panels/deploy/DeployPanel.tsx (FULL — splitter :196-207, CTA :248-261, section mounts :187/:209)
    - .planning/sketches/006-combined-deploy-tab/index.html (winner D — collapse+splitter contract)
    - .planning/sketches/005-deploy-inspect-tab/index.html (section headers, footer hint)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (D3,D4,D5,D13,D14 + KEEP list)
  </read_first>
  <files>packages/renderer/src/panels/deploy/DeployPanel.tsx</files>
  <action>
    Add collapse state for the two sections (Working changes / Version history) with a ▾/▸ caret toggle in each section header (D5: "● Working changes (uncommitted)" + file count; "Version History"). When EITHER section is collapsed, auto-hide the draggable splitter (D4 — 006-D contract: a collapsed section takes its header height only, the other fills). Add the deploy footer instructional hint (D13: "collapse a section · expand a version…"). Make the Deploy CTA full-width (D14) keeping the existing "Deploy {versionLabel}…" label (an accepted improvement). Do NOT modify VersionHistoryBody internals (plan 06 owns it) — only wrap it in the collapsible section.
  </action>
  <acceptance_criteria>
    - Each section header renders a caret that toggles a collapse state; collapsing hides that section body.
    - When a section is collapsed, the splitter is not rendered/inert (D4) — asserted in component test.
    - Section headers show "Working changes" (+ uncommitted file count) and "Version History" (D5).
    - Footer hint text present (D13); Deploy CTA spans full width (D14) — class/style asserted.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/DeployPanel 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Collapsible sections + auto-hide splitter + headers + footer hint + full-width CTA match 006-D/005.</done>
</task>

<task type="auto">
  <name>Task 2: Staging chrome — selection, working-vs-saved split, ActionBadge pill, dot, kB footer, hover-×, context menu</name>
  <read_first>
    - packages/renderer/src/panels/deploy/StagingPanelBody.tsx (FULL — :644 selection, :779-799 ×, :680-705 menu, :839 footer)
    - packages/renderer/src/panels/deploy/ActionBadge.tsx (:21-58 — pill restyle + 'modify')
    - .planning/sketches/005-deploy-inspect-tab/index.html (staging rows, dot, kB footer, context menu items)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (D6,D7,D8,D9,D10,D11,D12 + KEEP list)
  </read_first>
  <files>packages/renderer/src/panels/deploy/StagingPanelBody.tsx, packages/renderer/src/panels/deploy/ActionBadge.tsx</files>
  <action>
    StagingPanelBody: (D7) real row selection — click sets selected row → accent bg + inset left bar; wire `aria-selected` (currently hardcoded false :644). (D6) working-vs-saved split: a "Based on vN — … files from previous version below" divider with dimmed "Saved in vN" rows beneath the uncommitted working rows. (D9) compute a changed-vs-base ●/○ dot per row ("identical to base" when the staged bytes match the version's base). (D10) staging footer payload total "… · NNN kB payload" (sum staged file sizes). (D11) hover-only × remove (opacity 0→1 on row hover; remove always-visible :779-799). (D12) row context-menu parity: add editor / viewport / reveal-in-TRE / compare-to-base alongside existing Open-in-viewer/Unstage. Mark each item's DISPOSITION explicitly (do NOT ship enabled no-ops): reveal-in-TRE + open-in-viewer/viewport wire to existing handlers (FUNCTIONAL); compare-to-base + open-in-editor — if the compare/editor handler does not yet exist, ship them DISABLED/greyed with a 'coming soon' tooltip and record the disposition in the SUMMARY. ActionBadge: restyle as a pill (bg+border) and use label "modify" for the modify action (D8). Preserve KEEP items (drag-drop staging, empty-state card, invalid-path/source-missing warnings).
  </action>
  <acceptance_criteria>
    - Clicking a staging row selects it (aria-selected=true + accent bg) — component test asserts (no longer hardcoded false).
    - Working-vs-saved divider + dimmed saved rows render when the version has a base (D6).
    - ActionBadge renders a pill (has bg + border class) and shows "modify" for modify action (D8).
    - Staging footer shows a "NNN kB payload" total (D10); × is hidden until row hover (D11) — asserted.
    - Context menu includes reveal-in-TRE + compare-to-base entries (D12); compare-to-base/open-in-editor are FUNCTIONAL or explicitly DISABLED (not enabled no-ops), disposition recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/StagingPanelBody 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Staging surface matches 005/006 chrome; ActionBadge is a pill; KEEP items preserved.</done>
</task>

<task type="auto">
  <name>Task 3: DeployDialog — forward-deploy-only picker (VER-07), modal title, detected/not-found rows, recommended label</name>
  <read_first>
    - packages/renderer/src/panels/deploy/DeployDialog.tsx (:144-159 silent auto-select, :707-708 static title, :87/:162-176/:722+ W7 staleWarning to REMOVE, :448/:498/:566 setDeployedVersion(activeVersionId) -> change to setLiveVersion)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (design decision 3 — picker scope; D15/D16/D17)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-CONTEXT.md (D-10, D-08 single live pointer)
    - .planning/sketches/006-combined-deploy-tab/index.html (modal title, client rows, recommended)
  </read_first>
  <files>packages/renderer/src/panels/deploy/DeployDialog.tsx</files>
  <action>
    Scope the deploy-model picker (VER-07/D-10): show the absolute/loose/Advanced picker ONLY when the dialog is invoked for a NEW forward deploy (staging a freshly-saved version's first deploy); when invoked for navigate/revert, the picker is hidden/disabled (the version's stored deployRecord drives it — plan 04 reconcile, no dialog). Set the modal title to "Deploy vN — label" (D15) from the target version. Render the client rows with a "detected" badge for the bound/found client and greyed "not found" rows for unavailable clients (D16) instead of silently auto-selecting (:144-159). Add a "(recommended)" label on the preferred deploy model for the detected client layout (D17). H2 single-live-pointer: in the DeployDialog SUCCESS handler (which currently calls `setDeployedVersion(manifest.activeVersionId!)` at :448/:498/:566 after a forward deploy), call `setLiveVersion(newVersionId)` instead so `activeVersionId === deployedVersionId === newVersionId` immediately after deploy (no stale divergence — the D-08 invariant). REMOVE DeployDialog's own W7 stale-divergence logic — the `staleWarning` state (:87), the activeVersionId!==deployedVersionId compute (:162-176), and the banner render (:722+) — superseded by the single live pointer (the same concept plan 06 removes from VersionHistoryBody); do NOT keep a second stale calc.
  </action>
  <acceptance_criteria>
    - Picker is rendered for the new-forward-deploy invocation and absent for a navigate/revert invocation (VER-07) — component test with both modes.
    - Modal title reads "Deploy vN — <label>" (D15) — asserted.
    - Detected client shows a "detected" badge; an unavailable client renders greyed "not found" (D16).
    - Preferred model shows "(recommended)" (D17).
    - After a forward deploy, the success handler calls setLiveVersion(newVersionId) so activeVersionId === deployedVersionId === newVersionId (mock asserted); DeployDialog's W7 stale banner + stale calc are removed (grep -c "staleWarning" == 0).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/DeployDialog 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Deploy modal is forward-deploy-only with scoped picker, version-titled, detected/not-found rows, recommended label; success handler calls setLiveVersion (single pointer); DeployDialog stale banner removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deploy modal → forward deploy write | The modal's confirm triggers a forward deploy write (boundary mitigations owned by the deploy primitives / plan 04). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-07-01 | Tampering | wrong deploy model applied via stale picker | mitigate | Picker scoped to forward deploy only (VER-07); navigate/revert uses the version's stored deployRecord (no picker), removing the reopen-resets-to-absolute bug. |
| T-04.3-07-02 | Spoofing/UX | deploying to a not-found/unintended client | mitigate | Detected/not-found rows (D16) make the target explicit; no silent auto-select. |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; UI/markup + state only. |
</threat_model>

<verification>
- Component tests green for DeployPanel, StagingPanelBody, DeployDialog.
- Sketch-diff D3–D17 verified element-by-element at the combined UAT (plan 13).
</verification>

<success_criteria>
The combined Deploy surface matches 005-B/006-D (collapsible sections, auto-hide splitter, full staging
chrome) and the Deploy modal is unambiguously forward-deploy-only with scoped picker + version title +
detected/not-found rows.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-07-SUMMARY.md` when done.
</output>
