---
phase: 04.3-versioning-and-searchtoc-mount
plan: 10
type: execute
wave: 2
depends_on: ["04.3-03"]
files_modified:
  - packages/native-core/modules/core/tre/TreVersion.h
  - packages/native-core/modules/core/tre/TreArchive.cpp
  - packages/native-core/modules/core/tre/TreArchive.h
  - packages/native-core/modules/core/tre/TreMount.h
  - packages/native-core/modules/core/tre/TreMount.cpp
  - packages/native-core/src/tre_binding.cpp
  - packages/native-core/index.d.ts
autonomous: true
requirements: [MOUNT-03, MOUNT-04, MOUNT-06]
must_haves:
  truths:
    - "v6000 isEnumerateOnly is no longer a blanket version-tag refusal — extraction attempts the inflate and classifies a payload as encrypted/unreadable ONLY on real zlib failure (D-14, MOUNT-04)."
    - "A swg-source v6000 plain-zlib payload extracts byte-identically to tre_reader.read_tre_payload() (MOUNT-03, native byte-exact)."
    - "Restoration v6000 (genuinely encrypted) degrades gracefully to enumerate-only WITHOUT crashing."
    - "The native extract path accepts an EXTERNALLY-SUPPLIED entry descriptor via extractMountAt(handle, archiveIndex, { offset, length, compressedLength, compressor }) — the container is located by (handle, archiveIndex) -> mount.nodeAt(archiveIndex).path (the same lookup readMountEntry uses, tre_binding.cpp:543-547), NOT by a locator-less offset — so master-.toc-sourced payloads in a numberOfFiles=0 container can be extracted (RESEARCH Fix 2 stride caveat; H1 redesign)."
    - "mountTreMountWithToc(paths, priorities, tocPath, tocTreePath) sources the master .toc so empty-internal-TOC (numberOfFiles=0) v6000 containers contribute entries through the columnar bridge (getMountEntriesColumnar) — the native API the renderer (plan 11) drives to make patch_sku3_* show non-zero entryCount (H1 columnar gap)."
  artifacts:
    - path: "packages/native-core/modules/core/tre/TreArchive.cpp"
      provides: "try-inflate-classify extractEntry + extractAt(descriptor, stream) [externally-supplied descriptor]"
    - path: "packages/native-core/modules/core/tre/TreVersion.h"
      provides: "isEnumerateOnly no longer blanket-refuses V6000 (per-payload at the caller)"
    - path: "packages/native-core/src/tre_binding.cpp"
      provides: "JS binding for extractMountAt(handle, archiveIndex, descriptor) + mountTreMountWithToc + classification result surfaced to renderer"
    - path: "packages/native-core/index.d.ts"
      provides: "extractMountAt + mountTreMountWithToc typings + enumerate-only/encrypted result shape"
  key_links:
    - from: "TreArchive.cpp::extractEntry"
      to: "Zlib inflate"
      via: "try inflate, classify on failure"
      pattern: "inflate|treInflate"
    - from: "tre_binding.cpp"
      to: "TreArchive::extractAt (via mount.nodeAt(archiveIndex).path)"
      via: "extractMountAt externally-supplied descriptor extraction"
      pattern: "extractMountAt"
---

<objective>
Pillar B native-core change: make v6000 enumerate-only a PER-PAYLOAD runtime classification (try the
inflate; classify encrypted only on real failure) and add an `extractMountAt(handle, archiveIndex, descriptor)` path that extracts from an externally-supplied
entry descriptor (offset/len/compressedLength/compressor), locating the container via mount.nodeAt(archiveIndex).path
(tre_binding.cpp:543-547), plus a `mountTreMountWithToc` mount API that sources the master `.toc` — both required
because master-.toc-sourced v6000 entries live in a `numberOfFiles=0` container with an EMPTY internal TOC.

Turns the Wave-0 native RED test (plan 03) GREEN. The renderer wiring that supplies the descriptors is
plan 11. Both are verified end-to-end at the D-16 gate (plan 12).

Ground truth (LOCKED — RESEARCH § Measured ground truth, do not re-derive): `EERT6000`, numberOfFiles=0,
`78 9c` plain-zlib at byte 36; CRC-first TOC all versions; V6000 stride 32. The byte layout is verified —
do NOT reinstate the FALSIFIED size-first layout.

Purpose: unlock the swg-source v6000 / empty-internal-TOC content set (1,158 sku3 paths) for extraction.
Output: native per-payload extraction + extractMountAt + mountTreMountWithToc + binding/typings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/todos/pending/v6000-swg-source-plain-zlib-read-support.md

<interfaces>
From TreVersion.h:
  recordStride(V6000)=32, others 24 ; isCrcFirst()=true all ; isEnumerateOnly(v){ return v==V6000; }  // ← change
From TreArchive.cpp:302-335 extractEntry(idx, stream):
  refuses if isEnumerateOnly(m_version) [:307-311]; bounds-checks offset (T-01-02 subtraction form :326-329);
  readLen = compressor!=0 ? compressedLength : length ; zlib bomb cap ZLIB_MAX_BLOCK=256MB (T-01-03)
Reference oracle: ../swg-blender-plugin/swg_pipeline/tre_reader.py::read_tre_payload (zlib.decompress; on zlib.error → decrypt path),
  tre_decrypt.py::try_read_tre_payload (the try-inflate-then-classify pattern to mirror)
Wave-0 fixtures (plan 03): packages/harness/fixtures-real/toc/{fixture}.v6000.{zlib.bin,expected.bin,descriptor.json}
Wave-0 test (plan 03): packages/native-core/test/tre-extract-at-descriptor.test.ts (RED → green here); frozen plan-03 typings: extractMountAt(handle, archiveIndex, descriptor)->{bytes?,encrypted?} + mountTreMountWithToc(paths, priorities, tocPath, tocTreePath)
NOTE: native addon statically links zlib (Electron renderer crash otherwise — memory electron-native-addon-no-host-zlib); keep static link.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-payload enumerate-only (try-inflate-classify)</name>
  <read_first>
    - packages/native-core/modules/core/tre/TreVersion.h:111-126 (isEnumerateOnly blanket V6000 refusal)
    - packages/native-core/modules/core/tre/TreArchive.cpp:302-335 (extractEntry refusal + bounds + readLen + bomb cap)
    - ../swg-blender-plugin/swg_pipeline/tre_decrypt.py (try_read_tre_payload — the inflate-then-classify oracle)
    - ../swg-blender-plugin/swg_pipeline/tre_reader.py (read_tre_payload — zlib.decompress, zlib.error fallback)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (Fix 2; Pitfall 3 — Restoration vs swg-source v6000)
  </read_first>
  <files>packages/native-core/modules/core/tre/TreVersion.h, packages/native-core/modules/core/tre/TreArchive.cpp</files>
  <action>
    Remove the version-tag short-circuit: change `extractEntry` so it no longer refuses outright when `isEnumerateOnly(m_version)` (drop the :307-311 throw). Instead, for a compressor≠0 entry, ATTEMPT the inflate (existing zlib path with the 256MB bomb cap); on a real zlib failure, return/surface an "encrypted/unreadable" classification (a result the binding maps to the UI enumerate-only chip) — do NOT crash or fatal (Restoration v6000 must degrade gracefully, MOUNT-04). Keep `isEnumerateOnly` as a helper but make the EXTRACT decision per-payload (the caller/inflate-result decides), per RESEARCH. Preserve all bounds checks (T-01-02 subtraction form) and the bomb cap (T-01-03). Update/extend the native TRE tests: swg-source v6000 fixture inflates byte-exact; an encrypted (Restoration) v6000 payload returns the classification without throwing.
  </action>
  <acceptance_criteria>
    - `extractEntry` no longer throws solely because `m_version==V6000` — the :307-311 unconditional refusal is gone (grep -v '^\s*//' TreArchive.cpp | grep -c "enumerate-only (v6000 encrypted payload)" == 0).
    - swg-source v6000 fixture (plan 03) extracts byte-identical to `.v6000.expected.bin` (native test, skipIf-absent) — MOUNT-03.
    - A Restoration v6000 (encrypted) payload returns an encrypted/unreadable classification WITHOUT crashing (native test asserts no throw/fatal) — MOUNT-04.
    - Bounds + bomb caps unchanged (existing native TRE tests green) — MOUNT-06 regression.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/native-core && npm run build && npm test 2>&1 | grep -qiE "pass|ok" && echo OK</automated>
  </verify>
  <done>v6000 extraction is per-payload classify; swg-source reads byte-exact; Restoration degrades gracefully; no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractMountAt(externally-supplied descriptor) + mountTreMountWithToc + binding/typings</name>
  <read_first>
    - packages/native-core/modules/core/tre/TreArchive.cpp:302-335 + TreArchive.h (extract API surface)
    - packages/native-core/src/tre_binding.cpp (existing extract/mount binding patterns)
    - packages/native-core/index.d.ts (JS-facing native types)
    - packages/native-core/test/tre-extract-at-descriptor.test.ts (the RED scaffold from plan 03)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (Fix 2 "Stride caveat" — master-sourced extraction needs an external descriptor, not an internal index)
  </read_first>
  <files>packages/native-core/modules/core/tre/TreArchive.cpp, packages/native-core/modules/core/tre/TreArchive.h, packages/native-core/modules/core/tre/TreMount.h, packages/native-core/modules/core/tre/TreMount.cpp, packages/native-core/src/tre_binding.cpp, packages/native-core/index.d.ts</files>
  <action>
    Add TWO native runtime pieces matching the plan-03 frozen typings. (A) `extractMountAt(handle, archiveIndex, descriptor)` in tre_binding.cpp: resolve the container via `mount.nodeAt(archiveIndex).path` (the SAME lookup readMountEntry uses, tre_binding.cpp:543-547), open a FileInputStream on it, and extract from the EXTERNALLY-SUPPLIED descriptor (offset/length/compressedLength/compressor) via a new `TreArchive::extractAt(descriptor, stream)` helper (h+cpp) — NOT keyed off the container's internal `m_entries` (empty for numberOfFiles=0). Reuse Task 1's try-inflate-classify + bounds checks (offset cap vs stream length, subtraction form, T-01-02) + bomb cap (T-01-03); return a zero-copy ArrayBuffer payload (binary-stays-binary) or the encrypted classification. (B) `mountTreMountWithToc(paths, priorities, tocPath, tocTreePath)` on the mount surface (TreMount.h/.cpp + tre_binding.cpp): extend mountTreMount (index.d.ts:195) to ALSO ingest the master `.toc` so empty-internal-TOC containers contribute entries through getMountEntriesColumnar — each external entry carrying its container archiveIndex + descriptor so resolveChain/readMountEntry/extractMountAt can read it (this is the H1 "no native API to supply external master entries through the columnar bridge" fix). The `index.d.ts` declarations for `extractMountAt` + `mountTreMountWithToc` + the result shape were ALREADY frozen by plan 03 (Wave 0) — do NOT re-declare them; CONFIRM the runtime here matches those existing typings exactly (the new code lands in the `.cpp`/`.h`, not the `.d.ts`; only adjust `index.d.ts` if the plan-03 stub turns out to mismatch the implemented shape). Turn the plan-03 `tre-extract-at-descriptor.test.ts` green using the swg-source v6000 fixture descriptor via extractMountAt.
  </action>
  <acceptance_criteria>
    - `extractMountAt(handle, archiveIndex, descriptor)` is callable from JS via tre_binding (resolving the container via mount.nodeAt(archiveIndex).path); `TreArchive::extractAt(descriptor, stream)` exists (h+cpp); the runtime matches the plan-03-frozen `index.d.ts` declarations for extractMountAt + mountTreMountWithToc + the result shape (no duplicate re-declaration; `tsc --noEmit` clean — no redeclare error).
    - `mountTreMountWithToc(paths, priorities, tocPath, tocTreePath)` makes an empty-internal-TOC v6000 container contribute entries through getMountEntriesColumnar (the H1 columnar-bridge gap) — native test asserts non-zero entries from a numberOfFiles=0 container.
    - `tre-extract-at-descriptor.test.ts` passes (skipIf-absent): the master-sourced v6000 payload extracts byte-exact via extractMountAt's external descriptor, WITHOUT consulting the empty internal TOC.
    - extractMountAt/extractAt bounds-check offset/compressedLength against stream length (subtraction form) — a malformed descriptor throws, not OOB-reads (unit asserted).
    - Payload crosses the bridge as an ArrayBuffer (zero-copy), not JSON.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/native-core && npm run build && npx vitest run test/tre-extract-at-descriptor.test.ts 2>&1 | grep -qiE "passed|skipped" && echo OK</automated>
  </verify>
  <done>extractMountAt extracts from an external descriptor (container via mount.nodeAt path) byte-exact + bounds-safe; mountTreMountWithToc sources external master entries through the columnar bridge; binding + typings shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| on-disk .tre bytes → native parser/inflate | Untrusted archive bytes (offsets/lengths/compressed streams) are read + inflated in C++. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-10-01 | Tampering/DoS | OOB read via malformed offset/length (incl. external descriptor) | mitigate | Bounds-check offset > streamLen − readLen (subtraction form, T-01-02) on BOTH extractEntry and extractAt before any read (V5). |
| T-04.3-10-02 | DoS | zlib decompression bomb | mitigate | ZLIB_MAX_BLOCK 256MB cap (T-01-03) retained on the inflate path. |
| T-04.3-10-03 | DoS/availability | crash on encrypted Restoration v6000 | mitigate | Try-inflate-classify returns an encrypted result; never fatal (MOUNT-04 graceful degradation). |
| T-04.3-10-04 | Tampering | renderer crash from borrowing host zlib | mitigate | Native addon keeps zlib STATICALLY linked (memory electron-native-addon-no-host-zlib) — do not link host zlib. |
| T-04.3-SC | Tampering | npm/native deps | mitigate | No new packages; C++ change against existing statically-linked zlib. RESEARCH has no package-install task → no legitimacy gate needed. |
</threat_model>

<verification>
- `npm run build` (native-core) + native tests green; swg-source v6000 byte-exact (skipIf) + Restoration graceful.
- D-16 gate (plan 12) re-runs the byte-exact test against real bytes + the crew cross-check before UAT.
</verification>

<success_criteria>
v6000 extraction is per-payload (swg-source reads byte-exact; Restoration degrades), and master-.toc-sourced
payloads in empty-internal-TOC containers extract via an externally-supplied descriptor — all bounds-safe,
zlib statically linked, no v5000/searchTree regression.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-10-SUMMARY.md` when done.
</output>
