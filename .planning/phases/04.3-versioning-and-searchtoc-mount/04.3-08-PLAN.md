---
phase: 04.3-versioning-and-searchtoc-mount
plan: 08
type: execute
wave: 2
depends_on: ["04.3-02"]
files_modified:
  - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx
  - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
  - packages/renderer/src/panels/deploy/NewProjectWizard.tsx
  - packages/renderer/src/state/useNoProjectDim.ts
autonomous: true
requirements: [PROJUI-01, PROJUI-02, PROJUI-03, PROJUI-04, PROJUI-05, PROJUI-06, PROJUI-07, PROJUI-08, PROJUI-09]
must_haves:
  truths:
    - "The project-entry / front-door surfaces match sketch 007: bound header shows project name + pill-styled client chip; Open Recent is live; not-found client rows render; first-run dims the rest of the workspace; project + server folders are editable with Browse."
    - "PLAUSIBLE rows P8 (split host:port) and P12 (header × close) are RE-VERIFIED against sketch 007 at execution and included only if the sketch confirms them; P11 paginated wizard is KEEP (not changed)."
  artifacts:
    - path: "packages/renderer/src/panels/deploy/ProjectBindingBar.tsx"
      provides: "project name in bound header + pill client chip + live Open Recent links"
    - path: "packages/renderer/src/panels/deploy/WorkspaceEntry.tsx"
      provides: "not-found client rows + first-run welcome wiring"
    - path: "packages/renderer/src/panels/deploy/NewProjectWizard.tsx"
      provides: "editable project folder + Browse (step 1), server-folder Browse (step 3), conditional split host:port + header ×"
    - path: "packages/renderer/src/state/useNoProjectDim.ts"
      provides: "first-run dim of Viewport/Inspector/Data panels"
  sketch_diff:
    sketch: ".planning/sketches/007-project-entry/index.html"
    elements:
      - "PROJUI-01 / P1: project name in bound header ('◆ projectName') — active-project confirmation"
      - "PROJUI-02 / P2: bound-client chip styled as pill (accent-dim bg, accent-line border, radius-full)"
      - "PROJUI-03 / P3: 'Open Recent' → live recent-project links (wire services/recentProjects.ts; currently permanently-disabled static item)"
      - "PROJUI-04 / P4: not-found / disabled client row ('✗ not found') in welcome + wizard"
      - "PROJUI-05 / P5: first-run dims Viewport/Inspector/Data to opacity ~.32"
      - "PROJUI-06 / P6: project-folder editable + Browse (Step 1) (currently read-only 'Stored in …' hint)"
      - "PROJUI-07 / P7: server-folder Browse (Step 3) (currently text input only)"
      - "PROJUI-08 / P8 (PLAUSIBLE — re-verify vs sketch): Host:Port as two split fields"
      - "PROJUI-09 / P9: first-run statusbar scan message ('detected: … ✓ · … ✗')"
      - "KEEP (not gaps): D-13 binding-details sub-row, manual layout override, Back button, 'Skip — no client', error/loading states, in-app ProjectListDialog, paginated Back/Next wizard (P11 KEEP)"
---

<objective>
Close the sketch-007 front-door fidelity gaps (P1–P9). Mostly chrome/copy plus three real affordances:
live Open Recent (P3), editable project/server folders with Browse (P6/P7), and the first-run dim (P5).
Re-verify the PLAUSIBLE rows (P8 split host:port, P12 header ×) against the sketch markup before
implementing; honor P11 KEEP (paginated wizard stays).

Disjoint files from the deploy panel (plan 07) and shell (plan 09).

Purpose: the project-binding front door matches its UI contract element-by-element.
Output: reworked ProjectBindingBar / WorkspaceEntry / NewProjectWizard + first-run dim hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md
@.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md
@.planning/sketches/007-project-entry/index.html

<interfaces>
Current evidence (gap review):
  ProjectBindingBar.tsx:224-231 (Open Recent permanently disabled), :262-277 (plain mono chip, not pill)
  NewProjectWizard.tsx:377-393 (read-only 'Stored in …'), :883-895 (single combined host:port input), :265-321 (paginated stepper — KEEP)
  WorkspaceEntry.tsx (only detected installs rendered; no not-found row)
Existing service to wire: packages/renderer/src/services/recentProjects.ts (recent-project list source for P3)
First-run dim hook exists: packages/renderer/src/state/useNoProjectDim.ts (extend to cover Viewport/Inspector/Data at ~.32 opacity)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bound header project name + pill client chip + live Open Recent</name>
  <read_first>
    - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx (:224-231 Open Recent, :262-277 chip)
    - packages/renderer/src/services/recentProjects.ts (recent-list API to wire P3)
    - .planning/sketches/007-project-entry/index.html (bound header + chip + Open Recent markup)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (P1,P2,P3 + KEEP list)
  </read_first>
  <files>packages/renderer/src/panels/deploy/ProjectBindingBar.tsx</files>
  <action>
    P1: render the active project name in the bound header ("◆ projectName") for active-project confirmation. P2: restyle the bound-client chip as a pill (accent-dim bg, accent-line border, radius-full) — replace the plain mono span (:262-277). P3: replace the permanently-disabled static "Open Recent" item (:224-231) with live links sourced from `recentProjects.ts` (clicking opens that project); show an empty/disabled state only when the recent list is genuinely empty. Preserve the D-13 binding-details sub-row (KEEP).
  </action>
  <acceptance_criteria>
    - Bound header shows the project name (P1) — component test asserts the name renders when a project is bound.
    - Client chip has pill styling (radius-full + accent border classes) (P2).
    - Open Recent renders live links from recentProjects (P3); not a hardcoded disabled item (grep -c 'disabled' on that item == 0 when recents exist).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/ProjectBindingBar 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Bound header names the project, chip is a pill, Open Recent is live.</done>
</task>

<task type="auto">
  <name>Task 2: Not-found client rows + first-run dim + statusbar scan message</name>
  <read_first>
    - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx (welcome client list)
    - packages/renderer/src/state/useNoProjectDim.ts (first-run dim hook to extend)
    - .planning/sketches/007-project-entry/index.html (not-found row '✗ not found', dimmed panels, statusbar scan)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-01-CREW-GAP-REVIEW.md (P4,P5,P9)
  </read_first>
  <files>packages/renderer/src/panels/deploy/WorkspaceEntry.tsx, packages/renderer/src/state/useNoProjectDim.ts</files>
  <action>
    P4: render greyed "✗ not found" client rows for configured-but-missing installs in the welcome list (not only detected installs). P5: extend `useNoProjectDim` so on first run the Viewport/Inspector/Data panels render at ~.32 opacity (the welcome/front-door is the focus) — dim by GROUP id (the dock group), NOT the literal panel id "Data" (which goes stale after plan 09 splits Data into Datatable/Console/Log; a panel-id match would break the bottom-pane dim). P9: WRITE the first-run statusbar scan message ("detected: … ✓ · … ✗") to the shared `clientScanStore` (setClientScanMessage — the Wave-0 stub from plan 02) which plan 09's statusbar reads — do NOT edit StatusBar.tsx in this plan.
  </action>
  <acceptance_criteria>
    - Not-found client rows render greyed/disabled (P4) — component test with a missing install.
    - First-run state dims the dock GROUPS (not the panel id "Data") to ~.32 opacity (P5) — hook returns the dim flag keyed by group id; asserted to survive the plan-09 Data->trio split.
    - The first-run scan message is WRITTEN to the shared clientScanStore (setClientScanMessage, P9) without touching StatusBar.tsx — asserted.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/WorkspaceEntry src/state/useNoProjectDim 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Not-found rows + first-run dim + scan-message source in place.</done>
</task>

<task type="auto">
  <name>Task 3: Editable project/server folders + Browse; re-verify PLAUSIBLE P8/P12</name>
  <read_first>
    - packages/renderer/src/panels/deploy/NewProjectWizard.tsx (:377-393 read-only project folder, :883-895 host:port, :265-321 paginated stepper KEEP)
    - .planning/sketches/007-project-entry/index.html (Step 1 project folder + Browse; Step 3 server folder + Browse; host:port field shape; wizard header ×)
    - .planning/phases/04.3-versioning-and-searchtoc-mount/04.3-RESEARCH.md (§4 — P8/P10/P12/P13 re-verification guidance)
  </read_first>
  <files>packages/renderer/src/panels/deploy/NewProjectWizard.tsx</files>
  <action>
    P6: make the Step-1 project folder editable with a Browse button (replace the read-only "Stored in …" hint :377-393). P7: add a Browse button to the Step-3 server folder (currently text-only). RE-VERIFY against sketch 007: P8 — if the sketch shows Host and Port as TWO split fields, split the combined input (:883-895); if the sketch shows one field, leave it and note "P8 not confirmed" in the SUMMARY. P12 — if the sketch shows a header × close, add it (alongside backdrop/Esc); else skip and note. Do NOT change the paginated Back/Next stepper (P11 = KEEP). Fold the cheap P13 copy fixes only where the sketch text differs (note each in SUMMARY). P4 (wizard half): ALSO render the greyed "✗ not found" client rows in the wizard's client-selection step (NewProjectWizard.tsx), not only the welcome list — the P4 gap applies to BOTH surfaces.
  </action>
  <acceptance_criteria>
    - Step-1 project folder is an editable input with a Browse button (P6); Step-3 server folder has Browse (P7).
    - The wizard's client-selection step renders greyed "✗ not found" rows for missing installs (P4 wizard half) — component test asserts.
    - P8/P12 decisions recorded in the SUMMARY with the sketch line that confirmed/denied each (no silent guess).
    - Paginated stepper unchanged (P11 KEEP) — grep confirms the stepper markup (:265-321 structure) intact.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/renderer && npx vitest run src/panels/deploy/NewProjectWizard 2>&1 | grep -qE "passed" && echo OK</automated>
  </verify>
  <done>Project + server folders editable with Browse; PLAUSIBLE rows re-verified against the sketch and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browse dialog → folder path input | User-picked folder paths feed project/server config (validated by existing binding/locator services). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-08-01 | Tampering | malformed/unsafe folder path from Browse | mitigate | Paths flow through existing projectBinding/clientLocator validation (reuse, no new validator); UI only collects the path. |
| T-04.3-08-02 | Information disclosure | Open Recent leaking arbitrary paths | accept | Recent list is the user's own projects (local trusted tool); no cross-user surface. |
| T-04.3-SC | Tampering | npm installs | mitigate | No installs; UI/markup + existing recentProjects service. |
</threat_model>

<verification>
- Component tests green for the three surfaces + dim hook.
- Sketch-diff P1–P9 verified element-by-element at the combined UAT (plan 13); P8/P12 verdicts in SUMMARY.
</verification>

<success_criteria>
The project front door matches sketch 007 (named bound header, pill chip, live Open Recent, not-found
rows, first-run dim, editable folders with Browse); PLAUSIBLE rows resolved against the sketch; paginated
wizard preserved.
</success_criteria>

<output>
Create `.planning/phases/04.3-versioning-and-searchtoc-mount/04.3-08-SUMMARY.md` when done.
</output>
