---
phase: 01-core-engine-iff-tre-verification-harness
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/native-core/modules/core/iff/Iff.h
  - packages/native-core/modules/core/iff/Iff.cpp
  - packages/native-core/src/iff_binding.cpp
  - packages/native-core/src/addon.cpp
  - packages/native-core/index.d.ts
  - packages/contracts/src/iff.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/opcodes.ts
  - packages/harness/test/iff-parse.test.ts
  - packages/harness/test/iff-roundtrip.test.ts
  - packages/harness/fixtures/iff/README.md
  - packages/renderer/src/panels/DataPanel.tsx
  - packages/renderer/src/panels/InspectorPanel.tsx
  - packages/renderer/src/panels/iff/IffStructureTree.tsx
  - packages/renderer/src/panels/iff/HexInspector.tsx
  - packages/renderer/src/shared/VerificationStatus.tsx
  - packages/renderer/src/state/iffStore.ts
autonomous: false
requirements: [CORE-03, CORE-04]
must_haves:
  truths:
    - "System parses an arbitrary real IFF (big-endian FORM/chunk) into a navigable tree with tag/declared-length/absolute-offset/kind/subType per node"
    - "A CLEAN (unedited) container re-emits its FULL declared byte span verbatim — including any interior padding/gaps within the declared length — so a clean parse->serialize round-trip is byte-exact even for files with interior pad bytes; this is the load-bearing fidelity guarantee, distinct from the trailing-bytes feature"
    - "Any bytes after the last top-level block surface as an explicit trailing-bytes node — NEW TOOLKIT BEHAVIOR (the client's calculateRawDataSize zeroes/ignores trailing non-IFF data, it does not preserve it), never silently dropped"
    - "On WRITE the serializer emits NO EA-IFF-85 pad byte (SWG no-pad quirk); on READ it TOLERATES a detected single 0x00 pad (consumes it only when present), never requires one — relabelled from the prior incorrect 'no pad ever'"
    - "An edited IFF structure serializes back byte-exact via hybrid-DOM verbatim re-emit (clean node writes its captured slice; only a dirty node reserializes), proven by assertRoundTrip on every committed IFF fixture incl. an odd-chunk-no-pad fixture and a gapped-FORM fixture"
    - "User can view the parsed FORM/chunk tree (Structure tab) and the selected leaf's bytes in an offset|hex|ascii inspector (Hex tab); selecting a node highlights its byte range"
    - "Malformed IFF (bad block length, non-printable FourCC, child-end > parent-end) is rejected cleanly without crashing the renderer"
    - "Implements CONTEXT decisions D-06 (generic IFF FORM/chunk tree viewer), D-07 (structure tree tag/size/offset + raw hex/ascii inspector pane — the SIE-successor baseline) and D-08 (read-only this phase — no per-format typed decode, no in-UI IFF edit/Save affordance)"
  artifacts:
    - path: "packages/native-core/modules/core/iff/Iff.cpp"
      provides: "big-endian FORM/chunk walk + validate + hybrid-DOM byte-exact serialize (clean span verbatim) + trailing-bytes (toolkit invention)"
      min_lines: 90
      contains: "FORM"
    - path: "packages/contracts/src/iff.ts"
      provides: "IffNode (tag, length, byteOffset, kind, subType, children) + trailing-bytes + round-trip status"
      min_lines: 25
    - path: "packages/renderer/src/panels/iff/IffStructureTree.tsx"
      provides: "FORM/chunk tree w/ tag/kind/size/offset columns + trailing-bytes node + status footer"
      min_lines: 50
    - path: "packages/renderer/src/panels/iff/HexInspector.tsx"
      provides: "virtualized offset|hex|ascii grid w/ selected-range highlight"
      min_lines: 50
  key_links:
    - from: "packages/renderer/src/panels/iff/IffStructureTree.tsx"
      to: "@swg/native-core"
      via: "Path B require (parseIff)"
      pattern: "require\\(.@swg/native-core"
    - from: "packages/harness/test/iff-roundtrip.test.ts"
      to: "packages/native-core/modules/core/iff/Iff.cpp"
      via: "assertRoundTrip(parseIff, serializeIff, fixtureBytes)"
      pattern: "assertRoundTrip"
---

<objective>
Deliver the IFF read+write slice: parse an arbitrary real IFF (FORM/chunk) into a navigable tree the UI displays (Structure tab) with a hex/ASCII inspector (Hex tab), and serialize an edited structure back **byte-exact** via the harness gate. This closes CORE-03 (parse) and CORE-04 (byte-exact serialize) — the second format registered in the standing-gate registry.

Purpose: After this plan, a real user can select an IFF file in the VFS browser and navigate its FORM/chunk structure with a raw hex inspector (the SIE-successor baseline, D-07) — and the byte-exact write path is proven by tests (D-08: no in-UI editing). The load-bearing fidelity guarantee is that a CLEAN container re-emits its full declared byte span verbatim (incl. interior padding), proven on a gapped-FORM fixture.
Output: native `Iff` parse+serialize (engine-free C++20), `iff_binding`, `contracts/iff.ts`, `IffStructureTree` + `HexInspector` renderer components (Surfaces 2 & 3), committed IFF fixtures (incl. gapped-FORM), and the IFF parse + byte-exact round-trip harness tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-iff-tre-verification-harness/01-CONTEXT.md
@.planning/phases/01-core-engine-iff-tre-verification-harness/01-RESEARCH.md
@.planning/phases/01-core-engine-iff-tre-verification-harness/01-PATTERNS.md
@.planning/phases/01-core-engine-iff-tre-verification-harness/01-UI-SPEC.md
@.planning/phases/01-core-engine-iff-tre-verification-harness/01-01-SUMMARY.md
@.planning/phases/01-core-engine-iff-tre-verification-harness/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (harness + lib skeleton + binding pattern) and Plan 02 (VFS browser select -> open file). -->
From packages/harness/assertRoundTrip.ts: `assertRoundTrip(parse, serialize, fixtureBytes)` throws `round-trip FAIL @ 0x{offset}` on mismatch.
From packages/harness/fixtureRegistry.ts: `registerFormat(id, { parse, serialize, fixtures: [{name, bytes, loaderSource}] })`; the sweep enforces fixtures + loaderSource.
From packages/native-core/modules/core/io/IInputStream.h: `MemoryInputStream` (harness + zero-copy ArrayBuffer path).
From packages/native-core/src/addon.cpp: forward-declare + `exports.Set`; keep all prior exports (hello/sab/TRE), add `parseIff`/`serializeIff`/`getChunkBytes`.
From packages/contracts/src/index.ts: add `export * from './iff.js'`.
From packages/renderer/src/panels/DataPanel.tsx + InspectorPanel.tsx: the BOTTOM data pane gets a "Structure" tab (IFF tree) and a "Hex" tab; VfsTree file-select (Plan 02) triggers parseIff.
</interfaces>

<ground_truth>
<!-- VERIFIED against real client/Utinni source (NOT the AI-distilled docs). DO NOT re-derive. -->
- IFF is BIG-ENDIAN: tags + lengths byte-swapped via ntohl on read / htonl on write (Iff.cpp:508-555 read, :637-644,713 write). 'FORM' reads as readable ASCII; length is MSB-first.
- Block framing: [4-byte Tag][4-byte BE uint32 length][payload]. Leaf length = payload bytes (NOT incl the 8-byte header).
- FORM container: 'FORM' · BE_u32(innerLen) · subTypeTag · children; innerLen INCLUDES the 4-byte sub-type tag (Iff.cpp:643 writes length+sizeof(Tag); :1144 reads getLength-sizeof(Tag)). FORM header = 12 bytes.
- Container discriminator: a block is a FORM iff first tag == TAG_FORM (Iff.cpp:1076-1095). Generic SIE-successor viewer ALSO recognizes LIST and `CAT ` (trailing space) as containers; PROP is a LEAF (OPEN-3 RESOLVED).
- **PAD (relabelled — the prior "NO PAD EVER" was wrong):** WRITE emits NO pad byte (IffWriter.cs:141 emits none); READ TOLERATES a detected single 0x00 pad on an odd-length chunk — it consumes the trailing byte ONLY when it is actually 0x00, otherwise leaves it as the next chunk's TypeID (IffReader.cs:307-327). So: "write no pad; read tolerates a detected pad," NOT "no pad ever." Adding a pad on write fails byte-exact.
- Byte-exact serialize: hybrid-DOM verbatim re-emit — a CLEAN node writes its captured source slice verbatim; only a dirty node reserializes (IffWriter.cs:98-187). Iff::write dumps the buffer verbatim (Iff.cpp:419-429). **Load-bearing guarantee: a clean container re-emits its FULL declared byte span verbatim, including any interior padding/gaps within the declared length** — the captured slice spans the whole declared length, so interior gaps survive (proven by the gapped-FORM fixture).
- **Trailing-bytes node is NEW TOOLKIT BEHAVIOR, not ported:** the client's `calculateRawDataSize` (Iff.cpp:63-84) only computes the used IFF size and ASSUMES trailing non-IFF data is zeroed (`-TF- this assumes any extra non-iff data ... has been zeroed out`, :69) — it does NOT surface or preserve a trailing-bytes node. We ADD an explicit trailing-bytes node for bytes after the last top-level block (zero unexplained trailing bytes); label it a toolkit invention in the port comment. NOTE it only catches bytes after the last TOP-LEVEL block — INTERIOR gaps are handled by the clean-span-verbatim guarantee above, not by this node.
- Gapped FORM (declared length > children span): a legal case (Opus LOW). Decide explicitly reject-vs-roundtrip — the chosen behavior is the clean-span-verbatim round-trip (the captured slice spans the full declared length, so the interior gap is preserved); a fixture proves it.
- Security caps (Utinni IffReader.cs): per-chunk <= 64 MB + reject childEnd > parentEnd (:174-195); reject non-0x20-0x7E FourCC bytes (:149-158).
</ground_truth>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine-free C++20 Iff parse + byte-exact serialize + binding + contracts (CORE-03, CORE-04)</name>
  <files>packages/native-core/modules/core/iff/Iff.h, packages/native-core/modules/core/iff/Iff.cpp, packages/native-core/src/iff_binding.cpp, packages/native-core/src/addon.cpp, packages/native-core/index.d.ts, packages/contracts/src/iff.ts, packages/contracts/src/index.ts, packages/contracts/src/opcodes.ts</files>
  <read_first>
    - packages/native-core/modules/core/io/IInputStream.h (MemoryInputStream the parser reads through) — from Plan 01
    - packages/native-core/src/sab-rw.cpp:29-67 (binding validate->bounds->return) + src/sab.cpp:35 (ArrayBuffer/SAB alloc)
    - packages/native-core/src/addon.cpp (forward-declare + exports.Set; keep prior exports)
    - ../swg-client-v2/src/engine/shared/library/sharedFile/src/shared/Iff.cpp:56-84 (calculateRawDataSize — **assumes trailing data zeroed; does NOT surface a trailing-bytes node — the trailing node is OUR invention**), :419-429 (verbatim write), :508-555 (BE read via ntohl), :637-644,697,713 (htonl write, FORM len + sizeof(Tag)), :1076-1095 (FORM discriminator), :1132-1310 (walk) — PRIMARY port source
    - ../Utinni/UtinniCoreDotNet/Formats/Iff/IffReader.cs:307-327 (**no-pad DETECT — consume a trailing 0x00 only when present, never require**), :150-158 (FourCC reject), :174-195 (nested-overflow reject) and IffWriter.cs:98-187 (hybrid-DOM verbatim re-emit), :141 (emit NO pad)
    - packages/contracts/src/ipc.ts (discriminated-union + provenance-comment style)
    - .planning/phases/01-core-engine-iff-tre-verification-harness/01-PATTERNS.md § "Ground-Truth Port Map" (Iff row) + § "contracts/src/iff.ts"
  </read_first>
  <behavior>
    - Test "iff parse": parse a synthetic nested IFF -> a tree whose root FORM has the correct subType, declared length, absolute byteOffset, and child list; a leaf chunk has kind 'leaf' and correct length/offset; LIST and `CAT ` are recognized as containers, PROP as leaf.
    - Test "iff roundtrip clean-span verbatim": assertRoundTrip(parseIff, serializeIff, bytes) is byte-exact on (a) the nested fixture, (b) the odd-chunk-no-pad fixture (no pad byte inserted on write), and (c) a GAPPED-FORM fixture whose FORM declared length EXCEEDS its children span (interior gap) — the clean tree re-emits the full declared span verbatim, preserving the interior gap byte-for-byte.
    - Test "read tolerates pad": a fixture with a genuine single 0x00 pad after an odd-length chunk parses correctly (the pad is consumed, not mistaken for a chunk); a fixture WITHOUT the pad parses correctly (the next TypeID is not eaten).
    - Test trailing bytes: a fixture with bytes after the last top-level block yields an explicit trailing-bytes node carrying {count, offset}; round-trip preserves them. (Comment/spec note: this is the toolkit-invented node, not a ported client behavior.)
    - Test malformed: a truncated / bad-length / non-printable-FourCC / child-end>parent-end fixture is rejected cleanly (throws, no crash).
  </behavior>
  <action>
    Implement `modules/core/iff/Iff.{h,cpp}` as engine-free C++20. Parse: hold the whole file as one buffer + a stack of frames `{start, length, used}` (port Iff.cpp:56-72); read tags/lengths big-endian via an `ntohl`-style `readBe32` (port :508-555); discriminate a FORM by first-tag == 'FORM' (port :1076-1095) and ALSO treat `LIST`/`CAT ` as containers for the generic viewer, PROP as leaf (OPEN-3); for a FORM, child span = `getLength - sizeof(Tag)` (the innerLen INCLUDES the 4-byte subtype, :1144); build an `IffNode { tag, declaredLength, byteOffset(abs header offset), kind('form'|'leaf'), subType?, children?, capturedSlice }`; **capture each node's exact source byte slice spanning its FULL declared length** (hybrid-DOM) so a clean re-emit reproduces interior padding/gaps verbatim. **Pad handling (relabelled): DETECT-don't-assume — on an odd-length chunk consume a trailing byte ONLY when it is 0x00, else leave it (IffReader.cs:307-327); never require a pad.** Surface bytes after the last TOP-LEVEL block as an explicit trailing-bytes node — **mark this in the header comment as NEW TOOLKIT BEHAVIOR (the client's calculateRawDataSize at Iff.cpp:63-84 only zeroes/ignores trailing data, it does NOT surface a node)**; interior gaps are NOT this node's job (they ride along in the clean-span captured slice). For a gapped FORM (declared length > children span), the decided behavior is clean-span-verbatim round-trip (preserve the interior gap), NOT reject. Serialize: hybrid-DOM verbatim re-emit — a clean node writes `capturedSlice` verbatim (full declared span); a dirty node reserializes as `BE_u32(tag) · BE_u32(len) · [subType for container] · children/payload` with NO pad byte (port IffWriter.cs:98-187, :141); ancestor-invalidation so a clean ancestor never overwrites a dirty subtree. Apply security caps from <ground_truth> (per-chunk <= 64 MB; reject childEnd > parentEnd; reject non-0x20-0x7E FourCC). Header comment cites the `swg-client-v2 file:line` ported AND flags the trailing-bytes node as a toolkit invention. Implement `iff_binding.cpp` THIN: `parseIff(bytes)` -> IffNode tree (structure as contracts-typed JSON, NEVER the payload bytes as JSON), `serializeIff(tree)` -> zero-copy ArrayBuffer, `getChunkBytes(idx)` -> zero-copy ArrayBuffer slice; wrap large parse/serialize in `Napi::AsyncWorker` holding a `Napi::Reference` to the input buffer (Pitfall 6). Register exports in `addon.cpp` + `index.d.ts`; add IFF opcodes at the `opcodes.ts` stub. Create `contracts/src/iff.ts`: `IffNode { tag: string; length: number; byteOffset: number; kind: 'form'|'leaf'; subType?: string; children?: IffNode[] }`, `IffTrailingBytes { offset; count }`, `IffRoundTripStatus { passed: boolean; failOffset?: number }`. Add `export * from './iff.js'` to `contracts/src/index.ts`.
  </action>
  <verify>
    <automated>pnpm vitest run -t "iff parse" && pnpm vitest run -t "iff roundtrip"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run -t "iff roundtrip"` byte-exact green on every committed IFF fixture INCLUDING odd-chunk-no-pad (no pad byte inserted) AND the gapped-FORM fixture (interior gap preserved by clean-span verbatim re-emit) — the byte-exact round-trip gate (CORE-04).
    - `pnpm vitest run -t "iff parse"` green: nested tree with correct tag/length/byteOffset/kind/subType; LIST/`CAT ` containers, PROP leaf; trailing-bytes node present when bytes follow the last top-level block.
    - `pnpm vitest run -t "read tolerates pad"` green: a genuine 0x00 pad is consumed; an absent pad does not eat the next TypeID.
    - Malformed fixtures rejected cleanly (behavior: `expect(() => parseIff(badBytes)).toThrow()` — no process crash under `pool: 'forks'`).
    - Source assertion: `Iff.cpp` reads/writes big-endian (grep for an `ntohl`/`htonl`-style BE swap; no little-endian tag/length read); emits NO pad byte on write (no `+1` odd-length pad write) but tolerates a detected 0x00 pad on read; the header comment cites its `Iff.cpp`/`IffReader.cs` `file:line` AND labels the trailing-bytes node a toolkit invention (grep for "toolkit" / "not ported" near the trailing-bytes handling).
    - Standing gate: the `iff` format is registered with fixtures + loaderSource citations; sweep (`-t "registry coverage"`) passes with TWO formats (tre, iff).
    - Security: per-chunk 64 MB cap + childEnd>parentEnd reject + non-printable-FourCC reject present (source review).
  </acceptance_criteria>
  <done>Engine-free Iff lib parses BE FORM/chunk trees; a clean container re-emits its full declared span verbatim (interior gaps preserved, proven on a gapped-FORM fixture); WRITE emits no pad while READ tolerates a detected 0x00 pad; the trailing-bytes node is labelled a toolkit invention; serializes byte-exact (hybrid-DOM), proven by assertRoundTrip; binding returns structure-as-JSON + payloads zero-copy; iff registered in the standing-gate registry.</done>
</task>

<task type="auto">
  <name>Task 2: IFF Structure tree + Hex/ASCII inspector UI (CORE-03, Surfaces 2 & 3)</name>
  <files>packages/renderer/src/panels/DataPanel.tsx, packages/renderer/src/panels/InspectorPanel.tsx, packages/renderer/src/panels/iff/IffStructureTree.tsx, packages/renderer/src/panels/iff/HexInspector.tsx, packages/renderer/src/shared/VerificationStatus.tsx, packages/renderer/src/state/iffStore.ts</files>
  <read_first>
    - .planning/phases/01-core-engine-iff-tre-verification-harness/01-UI-SPEC.md § "Surface 2 — IFF FORM/Chunk Tree Viewer" + § "Surface 3 — Hex/ASCII Inspector Pane" + § "Accessibility Contract" + § "Copywriting Contract" + § "Component Inventory for Planner"
    - packages/renderer/src/panels/DataPanel.tsx + InspectorPanel.tsx (BOTTOM data pane tab host + panel chrome, var(--color-*)/var(--space-*), addon-require pattern)
    - packages/renderer/src/panels/tre/VfsTree.tsx (Plan 02 — file-select event that triggers parseIff) and treStore.ts
    - .planning/sketches/MANIFEST.md and .planning/sketches/themes/*.css (LOCKED tokens: 3px grid, mono --text-xs mandate for bytes/offsets, hex row height 18px, --color-accent-dim selected-range highlight)
    - packages/contracts/src/iff.ts (IffNode + trailing-bytes + round-trip status types) — from Task 1
  </read_first>
  <action>
    Add a **Structure** tab and a **Hex** tab to the BOTTOM data pane (extend `DataPanel.tsx`/`InspectorPanel.tsx` tab host). `IffStructureTree.tsx` (Surface 2): when a VFS file is selected (Plan 02 event) call native `parseIff` and render the FORM/chunk tree — each row has columns Tag (4-char mono, e.g. `FORM`, `DERV`), Kind (`form`/`leaf`, forms show `FORM:{subType}`), Size (hex + decimal mono, note FORM length includes the 4-byte subtype), Offset (abs `0x…` mono); 16px indent/level; selected row = 2px accent left border + `--color-accent-dim`; container chevron rotates 0.15s. Append an explicit `⟨trailing bytes⟩` node (`--color-warn`, `▴`, aria-label "Unexplained trailing bytes") showing `{n} bytes @ 0x{offset}` when present; otherwise a footer `0 trailing bytes ✓` (accent mono). Footer round-trip status line: `byte-exact ✓` (accent) or `round-trip FAIL @ 0x{offset}` (danger) — DISPLAY ONLY (computed by harness/native; D-08 no in-UI edit/save). `HexInspector.tsx` (Surface 3): virtualized `offset │ hex │ ascii` grid, mono `--text-xs`, 18px row height; 8-hex-digit offset gutter on `--color-header`; 16 bytes/row grouped 8+8; ASCII gutter with non-printable as `·`; sticky `00 01 … 0F` ruler; selecting an IFF node highlights its header/payload byte range with `--color-accent-dim` across hex+ascii; hover a hex byte cross-highlights the paired ascii char. Fetch bytes zero-copy via `getChunkBytes` (ArrayBuffer). `VerificationStatus.tsx`: glyph+color+caption status pill (`✓`/`✕`/`≈`/`▴` + token + mono caption) — triple-encoded per Accessibility Rule 1. `iffStore.ts`: Zustand store for the parsed tree, selected node, selected byte range, round-trip/trailing status. Use ONLY locked tokens; state never color-alone; every icon-only control gets `aria-label` + `title`; exact UI-SPEC copy strings ("No structure loaded", "Select an IFF file in the Assets panel", "No bytes to inspect", parse-error copy with `@ 0x{offset}` + "Open raw bytes"). Path B addon access via `require('@swg/native-core')`. NO editable fields, NO Save affordance (D-08).
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer run typecheck && pnpm --filter @swg/renderer run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `IffStructureTree.tsx` calls `require('@swg/native-core')` `parseIff`; renders Tag/Kind/Size/Offset columns; appends the `⟨trailing bytes⟩` node + footer status line; consumes `contracts/src/iff.ts` `IffNode` (no `any`).
    - Source assertion: NO editable input / Save button exists in the IFF surfaces (D-08) — grep for `<input`/`Save` in the iff/ components returns none (other than read-only hex selection).
    - Source assertion: `HexInspector.tsx` renders a virtualized grid (only visible rows in the DOM), 18px row height, mono `--text-xs`, selected-range `--color-accent-dim` highlight; no arbitrary px spacing outside chrome-dimension exceptions.
    - Behavior: selecting an IFF node updates the selected byte range in the store and the HexInspector highlights it (state-link assertion in a renderer unit test or a documented manual step in the checkpoint).
    - Behavior: `pnpm --filter @swg/renderer run typecheck` and `lint` pass.
    - Copy + a11y: exact UI-SPEC strings; every icon-only control has `aria-label` + `title`; status is glyph+color+caption (never color alone).
  </acceptance_criteria>
  <done>The data pane hosts a Structure tab (FORM/chunk tree + trailing-bytes node + round-trip status footer) and a Hex tab (virtualized offset|hex|ascii with selected-range highlight); selecting a node highlights its bytes; read-only (D-08), token-compliant, accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the IFF Structure tree + Hex inspector</name>
  <action>Pause for human verification. Claude has automated the full slice (native parse/serialize + UI); the user manually confirms the visual/interactive behavior per how-to-verify before the phase proceeds. Do NOT auto-approve — this is a blocking gate.</action>
  <what-built>The IFF viewer (Surfaces 2 & 3): selecting an IFF file in the VFS browser parses it into a navigable FORM/chunk Structure tree and shows the selected chunk's raw bytes in an offset|hex|ascii inspector — the SIE-successor baseline. The byte-exact write path (clean-span verbatim, incl. interior gaps) is proven by the harness (no in-UI editing this phase).</what-built>
  <how-to-verify>
    1. Run the app (`pnpm dev`). Mount a real archive (from Plan 02) and select a real `.iff`-framed file (e.g. a datatable `.iff` or a `.msh`) in the VFS tree.
    2. Confirm the BOTTOM data pane **Structure** tab shows a FORM/chunk tree with Tag / Kind / Size (hex+decimal) / Offset columns; expand a FORM and confirm children nest at 16px/level.
    3. Confirm the footer shows `0 trailing bytes ✓` (or an explicit `⟨trailing bytes⟩` node if the file has trailing bytes) and a `byte-exact ✓` round-trip status for a known-good file.
    4. Click a leaf chunk; confirm the **Hex** tab shows its bytes as `offset │ hex │ ascii`, and the selected node's byte range is highlighted in accent; hover a hex byte and confirm the paired ASCII char cross-highlights.
    5. Select a deliberately non-IFF file; confirm a clean parse-error message (`Could not parse … @ 0x{offset}` + "Open raw bytes") — the renderer does NOT crash.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looked wrong (mis-parsed tree, wrong offsets, missing trailing-bytes node, hex misalignment, a crash on malformed input).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arbitrary IFF file -> native parser | Third-party / malformed IFF bytes cross into the parser; a crash takes the renderer (Path B) |
| native ArrayBuffer -> HexInspector | Chunk bytes cross zero-copy; large chunks must not block or DoS the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | IFF nested-chunk overflow (childEnd > parentEnd) | mitigate | Reject `declaredEnd > parentEnd` for nested chunks before recursing (IffReader.cs:185-195); malformed fixture proves clean rejection |
| T-01-11 | Tampering | Non-printable / garbage FourCC | mitigate | Reject tag bytes outside 0x20-0x7E (IffReader.cs:150-158) |
| T-01-12 | Denial of Service | Oversized chunk length (alloc attack) | mitigate | Per-chunk cap <= 64 MB; subtraction-form bound on declared length vs remaining buffer (IffReader.cs:174-195) |
| T-01-13 | Tampering | Byte-exact corruption via wrong pad handling | mitigate | DETECT-don't-assume pad on read (consume a 0x00 only when present, IffReader.cs:307-327); emit NO pad on write (IffWriter.cs:141); clean-span verbatim re-emit (incl. interior gaps) for unedited nodes — assertRoundTrip on odd-chunk-no-pad + gapped-FORM fixtures is the regression guard |
| T-01-14 | Denial of Service | Multi-MB hex render blocking the UI | mitigate | Virtualized hex grid (only visible rows in DOM); large parse on Napi::AsyncWorker |
</threat_model>

<verification>
- `pnpm vitest run -t "iff parse"` / `-t "iff roundtrip"` byte-exact green (incl odd-chunk-no-pad + gapped-FORM + malformed-rejected) / `-t "read tolerates pad"`.
- `pnpm vitest run -t "registry coverage"` green with TWO registered formats (tre, iff) — the standing gate now covers both.
- `pnpm --filter @swg/renderer run typecheck && lint` green.
- Human-verify checkpoint: real IFF parsed into the Structure tree + hex inspection + clean malformed handling.
</verification>

<success_criteria>
- System parses an arbitrary real IFF into a navigable tree with zero unexplained trailing bytes (CORE-03); the trailing-bytes node is correctly labelled a toolkit invention.
- A CLEAN container re-emits its full declared byte span verbatim (incl. interior padding/gaps), and an edited IFF structure serializes back byte-exact (write no-pad / read tolerates-pad, hybrid-DOM verbatim), proven by assertRoundTrip on all committed fixtures incl. odd-chunk-no-pad and gapped-FORM (CORE-04).
- The user can browse the FORM/chunk tree and inspect selected-chunk bytes in offset|hex|ascii (D-06/D-07).
- Malformed IFF is rejected cleanly without crashing the renderer.
</success_criteria>

<output>
Create `.planning/phases/01-core-engine-iff-tre-verification-harness/01-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
