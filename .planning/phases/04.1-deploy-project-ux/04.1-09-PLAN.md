---
phase: 04.1-deploy-project-ux
plan: 09
type: execute
wave: 7
depends_on: ["04.1-02", "04.1-04"]
files_modified:
  - packages/renderer/src/services/clientLayout.ts
  - packages/renderer/src/services/clientLocator.ts
  - packages/renderer/src/panels/deploy/NewProjectWizard.tsx
  - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx
  - packages/renderer/src/services/clientLayout.test.ts
autonomous: true
requirements: [PROJ-01, DEPLOY-06]
must_haves:
  truths:
    - "A release-pattern table classifies a browsed client folder (cfg filename + TRE dir + maxSearchPriority), with a manual override the user can enter and that persists with the binding"
    - "The hardcoded swgemu.cfg / Live/ assumptions are routed through the clientLayout resolver instead of being duplicated across services"
    - "Detected layout is surfaced for confirm/correct in the wizard bind step AND in the ProjectBindingBar project-binding group (the single canonical home for the detected-layout + local-server rows)"
  artifacts:
    - path: "packages/renderer/src/services/clientLayout.ts"
      provides: "KNOWN_LAYOUTS table + resolveLayout + manual override resolution (D-13)"
      exports: ["resolveLayout", "KNOWN_LAYOUTS"]
  key_links:
    - from: "packages/renderer/src/services/clientLocator.ts"
      to: "packages/renderer/src/services/clientLayout.ts"
      via: "resolveLayout replaces hardcoded cfg/TRE-dir"
      pattern: "resolveLayout"
---

<objective>
Generalize client-layout detection (D-13): a small release-pattern table + resolver that classifies a browsed client folder (cfg filename varies `swgemu.cfg`/`client.cfg`; TRE dir varies `Live/`/root; `maxSearchPriority` varies Infinity=60/SWGEmu=27) with a persisted manual override when auto-detect can't classify — and route the hardcoded `swgemu.cfg`/`Live/` assumptions scattered across `clientLocator` (and surfaced in the wizard/binding UI) through that resolver.

Purpose: support real-world client layouts beyond the two measured ones without re-hardcoding.
Output: `clientLayout.ts` + `clientLocator` generalization + manual-override UI in the wizard + ProjectBindingBar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-RESEARCH.md
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-UI-SPEC.md

<interfaces>
From plan 01: `WorkspaceInfo.cfgPath/treDir/pattern`. From plan 02: `projectBinding.ts` consumes `clientLocator` — this plan refactors `clientLocator.ts`, so it depends on plan 02 to land on plan 02's final version. From plan 04: NewProjectWizard bind step + ProjectBindingBar.
Hardcode sites (verified, RESEARCH §Pattern 3): clientLocator.ts:186-207 (`swgemu.cfg` + known paths), :249 (registry join), :282 (addManualClient), :308-310 (`Live/` + EERT). Table seed: SWG Infinity (swgemu.cfg, Live/, 60); SWGEmu (swgemu.cfg, root, 27). [ASSUMED A1] client.cfg + other releases reported but not measured → manual override covers them.
`chooseSlot` (clientLocator.ts:157-164) still computes the real free slot from the live chain scan — maxSearchPriority is informational/confirmation only.
M4 — surface disambiguation: UI-SPEC Surface 3 describes a "Project-binding group" for the detected-layout + local-server rows. This plan declares the `ProjectBindingBar` (created in plan 04, mounted in the Assets head) to BE that project-binding group — it is the single canonical home for those rows. Do NOT also add them to a separate inspector accordion (avoid two homes). files_modified accordingly lists `ProjectBindingBar.tsx` (not a separate inspector component).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — clientLayout resolver + manual-override tests</name>
  <read_first>
    - 04.1-VALIDATION.md D-13 row (release-pattern table + manual override)
    - packages/renderer/test/fixtures/fakeClientDir.ts (build Infinity-style Live/ and SWGEmu-style root layouts)
    - 04.1-PATTERNS.md §clientLayout.ts (KNOWN_LAYOUTS + resolveLayout)
  </read_first>
  <action>
    Create `packages/renderer/src/services/clientLayout.test.ts` using the fixture to build (a) a Live/ + swgemu.cfg dir → `resolveLayout` returns the Infinity-style layout (treSubdir 'Live'), (b) a root-tre + swgemu.cfg dir → SWGEmu-style (treSubdir ''), (c) an unclassifiable dir (e.g. cfg present but no .tre, or a `client.cfg`-only dir) → `resolveLayout` returns null so the caller shows manual override, and (d) a manual-override object (user-entered cfgFile + treSubdir) is accepted and round-trips. RED.
  </action>
  <acceptance_criteria>
    - tests exist and fail (resolveLayout absent)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/clientLayout.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Detection-table behavior pinned by failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clientLayout table + resolver; route clientLocator hardcodes through it</name>
  <read_first>
    - packages/renderer/src/services/clientLocator.ts:186-207, :249, :282, :308-310 (hardcodes to route)
    - 04.1-PATTERNS.md §clientLayout.ts (table + resolver + execFile registry pattern) + §clientLocator.ts (self-modify)
    - 04.1-RESEARCH.md §Pattern 3 (table seed + resolver behavior; maxSearchPriority informational only)
  </read_first>
  <behavior>
    - `resolveLayout(installPath)` matches a known layout (cfg present + TRE dir has .tre) or returns null.
    - manual override (user-entered cfgFile + treSubdir) is honored over auto-detect and persisted with the binding.
    - clientLocator no longer hardcodes `swgemu.cfg`/`Live/` — it consults `resolveLayout` (falling back to the table's defaults).
  </behavior>
  <action>
    Create `clientLayout.ts` with `KNOWN_LAYOUTS` (Infinity + SWGEmu seeds, with a comment that `client.cfg`/others are manual-override per A1) and `resolveLayout(installPath)` per 04.1-PATTERNS.md §clientLayout.ts. Refactor `clientLocator.ts` to consult `resolveLayout` for cfg filename + TRE dir instead of the inline `swgemu.cfg`/`Live/` literals at :186-207/:282/:308-310 (keep `chooseSlot`/chain-scan untouched — maxSearchPriority stays informational). All shell-outs keep `execFile` arg arrays (no string interpolation). Make Task-1 tests GREEN. Keep `projectBinding.detectFolderKind` working (it may now call `resolveLayout`).
  </action>
  <acceptance_criteria>
    - resolveLayout classifies Infinity/SWGEmu and returns null for unclassifiable; manual override honored
    - clientLocator routes through resolveLayout (no new `swgemu.cfg`/`Live/` literals)
    - clientLayout.test.ts green; existing clientLocator-dependent tests still green
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/clientLayout.test.ts test/cfgScan.test.ts && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Layout detection table + manual override land; hardcodes removed.</done>
</task>

<task type="auto">
  <name>Task 3: Surface detected layout + manual override in wizard + ProjectBindingBar group</name>
  <read_first>
    - packages/renderer/src/panels/deploy/NewProjectWizard.tsx (bind-client step from plan 04)
    - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx (the project-binding group from plan 04 — the canonical home per M4)
    - 04.1-UI-SPEC.md Surface 3 (show detected cfg path/filename + TRE dir + matched pattern + maxSearchPriority; manual-override entry; unconfirmed-directory warning card) + Copywriting Contract
  </read_first>
  <action>
    M4 — put the detected-layout + (read-only captured) local-server rows in ONE home: the `ProjectBindingBar` project-binding group (created in plan 04), and the wizard's Bind-client step. Do NOT introduce a second inspector-accordion copy. In the wizard's Bind-client step and in `ProjectBindingBar`, show what `resolveLayout` detected (cfg path + filename, TRE dir, matched release pattern, maxSearchPriority) for confirm/correct, and add a manual-override entry (user types the actual cfg path/name + TRE dir) that persists into `WorkspaceInfo.cfgPath/treDir/pattern` via the binding. When `resolveLayout` returns null, show the `--color-warn` unconfirmed-directory card ("Is this a client install?" → Yes treat as client / No non-client project per Surface 3; "Yes treat as client" forces kind:'client' per plan 04 M6). Inline styles + CSS vars; triple-encode the warning (glyph+border+text).
  </action>
  <acceptance_criteria>
    - wizard + ProjectBindingBar group display detected layout and accept a persisted manual override (single home — no duplicate inspector accordion)
    - unclassifiable folder shows the warn card with the Surface-3 branches
    - `pnpm -w tsc --noEmit` green; `pnpm --filter @swg/renderer test -- --run src/panels/deploy` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit && pnpm --filter @swg/renderer test -- --run src/panels/deploy 2>&1 | tail -10</automated>
  </verify>
  <done>Detected layout confirmable + overridable in the wizard + ProjectBindingBar group; persists with the binding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-entered cfg/TRE-dir override → binding | free-text-ish path override persisted and later used for cfg writes/mount |
| registry/reg query → detection | reg shell-out for install discovery |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-21 | Tampering | shell-out injection (`reg`) | mitigate | `execFile` with arg arrays only (clientLocator.ts:241) — never string interpolation |
| T-04.1-22 | Tampering | manual cfg/TRE-dir override path | mitigate | Override paths resolved + existence-checked before use; cfg writes still go through atomic cfgActivator confined to the client cfg + .studio |
| T-04.1-23 | Spoofing | misclassified non-client folder | mitigate | resolveLayout null → user-confirmed unconfirmed-directory branch; deploy disabled for non-client |
| T-04.1-SC | Tampering | package installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `vitest run src/services/clientLayout.test.ts test/cfgScan.test.ts` green
- `pnpm test` full unit green
- `pnpm -w tsc --noEmit` green
</verification>

<success_criteria>
D-13 met: release-pattern detection + persisted manual override; no re-hardcoded cfg/TRE-dir assumptions; detected layout surfaced for confirmation in ONE canonical project-binding group (ProjectBindingBar) + the wizard.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-09-SUMMARY.md` when done
</output>
