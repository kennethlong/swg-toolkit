---
phase: 04.1-deploy-project-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/workspace.ts
  - packages/contracts/src/changeset.ts
  - packages/renderer/test/fixtures/fakeClientDir.ts
  - packages/renderer/package.json
autonomous: true
requirements: [PROJ-01, DEPLOY-05]
must_haves:
  truths:
    - "WorkspaceInfo carries a `kind` discriminator and optional binding fields the binding service and DeployPanel read"
    - "`BASELINE_ID` is exported once from `@swg/contracts` so plan 03 (VersionHistoryBody) and plan 06 (seedBaseline) reference one source of truth"
    - "A reusable fake-client-dir fixture builds a cfg + Live/*.tre EERT header set for every detection/binding/deploy test"
    - "Renderer and root run a single hoisted vitest version (no split 2.x/4.x)"
  artifacts:
    - path: "packages/contracts/src/workspace.ts"
      provides: "WorkspaceInfo.kind + cfgPath/treDir/pattern/serverConfig fields"
      contains: "kind"
    - path: "packages/contracts/src/changeset.ts"
      provides: "exported BASELINE_ID baseline-changeset id constant"
      contains: "BASELINE_ID"
    - path: "packages/renderer/test/fixtures/fakeClientDir.ts"
      provides: "buildFakeClientDir(base) test helper"
      exports: ["buildFakeClientDir"]
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/workspace.ts"
      via: "barrel re-export"
      pattern: "workspace"
---

<objective>
Lay the interface-first foundation every later plan builds against: extend the `WorkspaceInfo` contract with the project-binding fields (D-10), export the shared `BASELINE_ID` baseline-changeset id constant from `@swg/contracts` (so plan 03's VersionHistoryBody and plan 06's seedBaseline import one source — W2 fix), create the shared fake-client-dir test fixture used across all Wave-0 detection/binding/deploy tests, and resolve the split vitest version so the whole phase runs one toolchain.

Purpose: downstream plans import `WorkspaceInfo.kind`, `BASELINE_ID`, and the fixture directly — no contract churn, no per-plan constant duplication, no per-plan fixture duplication.
Output: extended `workspace.ts` contract, `BASELINE_ID` in `changeset.ts`, `fakeClientDir.ts` fixture, aligned vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-deploy-project-ux/04.1-CONTEXT.md
@.planning/phases/04.1-deploy-project-ux/04.1-RESEARCH.md
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-VALIDATION.md

<interfaces>
Current WorkspaceInfo (contracts/src/workspace.ts:19-28) has: folderPath, studioDir, workspaceName, clientPath (always null today). This plan adds the binding discriminator + resolved-layout fields.
Current changeset contract (contracts/src/changeset.ts) defines `SwgChangeset` ({id, parentId, label, timestamp, sealedBy, deltas}) + the manifest. This plan adds the `BASELINE_ID` constant (the canonical baseline-changeset id) alongside it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WorkspaceInfo contract + export BASELINE_ID (D-10/D-13/D-01)</name>
  <read_first>
    - packages/contracts/src/workspace.ts (the interface to extend, lines 19-28)
    - packages/contracts/src/changeset.ts (SwgChangeset shape — add BASELINE_ID next to it)
    - packages/contracts/src/index.ts (barrel — confirm workspace.js + changeset.js are re-exported)
    - 04.1-PATTERNS.md §`packages/contracts/src/workspace.ts` (the exact field shape to add)
    - 04.1-CONTEXT.md D-10 (kind + clientPath persist), D-13 (cfgPath/treDir/pattern), D-01 (serverConfig capture-only)
  </read_first>
  <action>
    Add to `WorkspaceInfo`: required `kind: 'client' | 'tre-set' | 'mod-project'` (per D-10); optional `cfgPath?: string`, `treDir?: string`, `pattern?: string` (resolved layout per D-13); optional `serverConfig?: { type: 'core3-wsl2' | 'swgsource-docker'; path: string; hostPort: string }` (capture-only per D-01 — no push fields). Keep `clientPath: string | null` as-is. Export a `WorkspaceBindingMeta` type (the persisted subset: `{ kind; clientPath: string | null; cfgPath?; treDir?; pattern?; serverConfig? }`) that `projectBinding` will read/write to `.studio/workspace.json`. Do not widen any existing field's type. Do not add `deployModelKind` (deferred idea — out of scope). Then, in `contracts/src/changeset.ts`, export `export const BASELINE_ID = 'baseline'` (the canonical baseline-changeset id) so plan 03 (VersionHistoryBody) and plan 06 (seedBaseline) both import it from `@swg/contracts` rather than redeclaring it — confirm it is re-exported via the contracts barrel.
  </action>
  <acceptance_criteria>
    - `WorkspaceInfo.kind` is a required union of the three literals; `serverConfig` matches D-01 shape exactly (type/path/hostPort, no push fields)
    - `WorkspaceBindingMeta` is exported from `@swg/contracts`
    - `BASELINE_ID` (value `'baseline'`) is exported from `@swg/contracts` (via the barrel)
    - `pnpm -w tsc --noEmit` passes (no consumer of WorkspaceInfo breaks — existing constructors will be updated in their own plans, so add `kind` as required only if all current constructors are updated; otherwise stage as required and let plan 02/06 set it — confirm tsc is green by making the new constructors the responsibility of 02/06 OR temporarily default — see note)
  </acceptance_criteria>
  <action_note>If making `kind` required breaks the two current `openComplete`/`createWorkspace` constructors before plan 02/06 update them, add `kind` as required AND update those two call sites here to pass `kind: 'mod-project'` as a safe default so tsc stays green; plans 02/06 then set the real value. Prefer this over an optional `kind`.</action_note>
  <verify>
    <automated>pnpm -w tsc --noEmit</automated>
  </verify>
  <done>WorkspaceInfo + WorkspaceBindingMeta + BASELINE_ID compile and are exported; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Shared fake-client-dir fixture + vitest version alignment</name>
  <read_first>
    - 04.1-PATTERNS.md §Wave 0 Test Files → "File-exist fixture pattern" (buildFakeClientDir shape, EERT5000 header bytes)
    - packages/renderer/test/cfgActivator.test.ts (real-temp-dir test style — no mock-fs)
    - packages/renderer/package.json + root package.json (vitest version pins: renderer ^2.0.0 vs root 4.1.9 — RESEARCH §Validation Architecture flag)
  </read_first>
  <action>
    Create `packages/renderer/test/fixtures/fakeClientDir.ts` exporting `buildFakeClientDir(base: string, opts?: { treSubdir?: string; cfgFile?: string; maxSearchPriority?: number })`: makes `base`, writes the cfg file (default `swgemu.cfg`) containing a `maxSearchPriority=<n>` line and a minimal `.include` reference chain, creates the TRE dir (default `Live/`, or root when `treSubdir:''`), and writes 2-3 dummy `.tre` files whose first 8 bytes are the EERT5000 header (`Buffer.from('EERT5000')`). Return the resolved `{ cfgPath, treDir, trePaths }`. Use only `node:fs`/`node:path`/`node:os` (real temp dirs). Then align vitest: set the renderer package's vitest devDependency to match the root `4.1.9` (single hoisted version per memory `[Phase 03] vitest workspace`), and confirm `packages/renderer/vitest.config.ts` still resolves.
  </action>
  <acceptance_criteria>
    - `buildFakeClientDir` produces a folder that `fs.existsSync(cfgPath)` is true and the TRE dir contains `.tre` files beginning with the 8 ASCII bytes `EERT5000`
    - renderer + root resolve the same vitest major; `pnpm --filter @swg/renderer test` starts (no version-resolution error)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run --reporter=dot 2>&1 | head -20</automated>
  </verify>
  <done>Fixture exists and is importable; vitest single-versioned and runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test fixture → filesystem | fixture writes under an OS temp dir only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | `buildFakeClientDir` base path | mitigate | Callers pass `os.tmpdir()`-rooted paths; fixture never writes to a client install or repo source |
| T-04.1-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO new packages (RESEARCH Package Legitimacy Audit: none); vitest re-pin is a version alignment of an already-present dep, not a new install |
</threat_model>

<verification>
- `pnpm -w tsc --noEmit` green
- `pnpm --filter @swg/renderer test` boots on a single vitest version
- Fixture importable from both `src/services/*.test.ts` and `test/*.test.ts`
</verification>

<success_criteria>
Contract + BASELINE_ID + fixture + toolchain ready so plans 02-11 build against a stable interface, a single baseline-id constant, and a shared fixture.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-01-SUMMARY.md` when done
</output>
