---
phase: 04.1-deploy-project-ux
plan: 04
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx
  - packages/renderer/src/panels/deploy/NewProjectWizard.tsx
  - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
  - packages/renderer/src/panels/tre/TreVfsBrowser.tsx
autonomous: true
requirements: [PROJ-01]
must_haves:
  truths:
    - "The Assets panel-head shows a ＋ Project ▾ split-button beside Mount Archive…, and a bound-client chip (⛁ <client> · EERT<version>) when bound"
    - "A New-Project wizard walks name/location → bind client (detected radios + Browse + non-client branch) → optional local-server CAPTURE-ONLY → seed assets, and calls projectBinding.initProject"
    - "First-run empty state lists detected clients with ✓ ready / ✗ not found pills and project/open/mount actions"
    - "A project with no bound client is first-class (deploy-to-client disabled)"
    - "For a client binding the base TREs ALWAYS auto-mount; the step-4 seed choice only controls optional staging pre-population (not mounting)"
  artifacts:
    - path: "packages/renderer/src/panels/deploy/NewProjectWizard.tsx"
      provides: "4-step project creation stepper (007-C)"
      min_lines: 80
    - path: "packages/renderer/src/panels/deploy/ProjectBindingBar.tsx"
      provides: "＋ Project front-door controls + bound-client chip"
  key_links:
    - from: "packages/renderer/src/panels/deploy/NewProjectWizard.tsx"
      to: "packages/renderer/src/services/projectBinding.ts"
      via: "initProject on finish"
      pattern: "initProject"
    - from: "packages/renderer/src/panels/deploy/WorkspaceEntry.tsx"
      to: "packages/renderer/src/services/clientLocator.ts"
      via: "detectClients() for the first-run list"
      pattern: "detectClients"
---

<objective>
Build the PROJ-01 project front door (sketch 007): the persistent `＋ Project ▾` control + bound-client chip in the Assets panel-head, the New-Project wizard (name/location → bind client with detected-install radios + Browse + the unconfirmed/non-client branch → optional local-server capture-only per D-01 → seed assets), and the first-run welcome takeover listing detected clients with ready/not-found pills.

Purpose: a discoverable workflow front door that drives `projectBinding.initProject`.
Output: `ProjectBindingBar.tsx`, `NewProjectWizard.tsx`, `WorkspaceEntry.tsx` first-run, Assets-head wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-UI-SPEC.md
@.planning/sketches/007-project-binding-front-door/

<interfaces>
From plan 02: `projectBinding.initProject(folder)`, `detectFolderKind`, `WorkspaceBindingMeta`. NOTE (M5): plan 02 `initProject` auto-mounts base TREs UNCONDITIONALLY for a client-kind binding — the wizard does NOT pass a skip-mount flag; "start empty" in step 4 governs only optional staging pre-population, never mounting.
Reuse: `clientLocator.detectClients()` (clientLocator.ts:180); IPC `workspace:pick-dir` → `string[]` (backend/src/main.ts:234-263).
Modal/overlay analog: StagingPanel.tsx:480-585 (VirtualPathModal); IPC bridge: WorkspaceEntry.tsx:26-30; async+error: WorkspaceEntry.tsx:73-99,167-181.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectBindingBar — ＋ Project control + bound-client chip in Assets head</name>
  <read_first>
    - packages/renderer/src/panels/deploy/StagingPanel.tsx:380-421 (panel-head bar layout) + :61-86 (button styles)
    - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx:73-99 (async action + error pattern)
    - packages/renderer/src/panels/tre/TreVfsBrowser.tsx (Assets panel-head where Mount Archive… lives — mount point for the bar)
    - 04.1-UI-SPEC.md Surface 2 (007-A header control + bound-state bar) + Accessibility Rule 1
    - 04.1-PATTERNS.md §ProjectBindingBar.tsx
  </read_first>
  <action>
    Create `ProjectBindingBar.tsx`: a panel-head bar (reuse the StagingPanel.tsx:380-421 layout + local `primaryBtnStyle`/`secondaryBtnStyle`) with a `＋ Project ▾` split-button (menu: New · Open… · Open Recent), the existing `Mount Archive…` secondary button, and — when `workspaceStore.clientPath` is set — a mono bound-client chip `⛁ <clientName> · EERT<treVersion>` (glyph + text, not color-only). `New` opens the wizard (task 2); `Open…` calls `ipcRenderer.invoke('workspace:pick-dir')` then `projectBinding.initProject(path)`; errors via the WorkspaceEntry async/error pattern. Mount this bar into the Assets panel-head in `TreVfsBrowser.tsx` beside `Mount Archive…` (do not remove the existing manual mount button). Use inline `require('electron')` IPC cast (centralized in plan 10).
  </action>
  <acceptance_criteria>
    - `ProjectBindingBar` renders ＋ Project + Mount Archive…; chip appears only when clientPath set
    - mounted in the Assets head without removing Mount Archive…
    - `pnpm -w tsc --noEmit` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Front-door control + bound-client chip live in the Assets head.</done>
</task>

<task type="auto">
  <name>Task 2: NewProjectWizard — 4-step stepper (incl. capture-only server + non-client branch)</name>
  <read_first>
    - packages/renderer/src/panels/deploy/StagingPanel.tsx:507-522 (overlay), :547-551 (keyboard), :569-584 (button row)
    - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx:26-30 (IPC), :167-181 (inline error)
    - 04.1-UI-SPEC.md Surface 2 (007-C steps) + Surface 3 (unconfirmed/non-client branch) + Copywriting Contract (local-server prompt, unconfirmed-directory prompt, no-client hint)
    - 04.1-CONTEXT.md D-01 (server CAPTURE-ONLY — type/path/host:port, NO push) and WorkspaceInfo.serverConfig shape (contract from plan 01)
  </read_first>
  <action>
    Create `NewProjectWizard.tsx`: a single-column stepper modal (overlay pattern from StagingPanel.tsx:507-522, `role="dialog" aria-modal`, Esc cancels, backdrop-click cancels) with 4 steps: (1) Name & location (folder via `workspace:pick-dir`); (2) Bind client — render `detectClients()` results as radio rows + `Browse for client folder…`, plus the unconfirmed-directory branch ("Is this a client install?" → Yes, treat as client / No, non-client project per Surface 3 copy). M6: a "Yes, treat as client" choice MUST force `kind:'client'` for that folder, overriding `detectFolderKind` (which would otherwise classify an unclassified folder as tre-set/mod-project) — the user override wins and the binding persists `kind:'client'`; (3) Local server (optional) — "Are you running a local server for this project?" Yes reveals type (Core3/WSL2 · SWG Source/Docker), path, and host:port (placeholder `127.0.0.1:44463`) — CAPTURE ONLY, stored into `serverConfig`, NO push UI (D-01); (4) Seed assets — bound client's TRE set (recommended) vs start empty. M5: for a client binding the base TREs ALWAYS mount via initProject regardless of this choice; "start empty" here only means do NOT pre-populate the staging list — it never disables mounting. On finish call `projectBinding.initProject(folder)` (passing the chosen kind/server capture through the persisted meta). A "No, non-client project" choice yields `kind:'mod-project'`, `clientPath:null`. Inline styles + CSS vars only. The full release-pattern manual override (cfg name/TRE-dir typing) is added in plan 09 (D-13) — this step provides the basic bind + non-client branch only.
  </action>
  <acceptance_criteria>
    - Wizard advances through all 4 steps; step 3 captures server type/path/host:port with NO push control
    - "Yes, treat as client" forces `kind:'client'` (M6); non-client branch produces a mod-project binding (clientPath:null); client branch calls initProject and auto-mounts
    - step-4 "start empty" does NOT suppress the client TRE auto-mount (M5); it only skips staging pre-population
    - modal a11y present (role=dialog, Esc, backdrop cancel, autofocus)
    - `pnpm -w tsc --noEmit` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Wizard creates client and non-client projects; "treat as client" forces client kind; TREs always mount for a client; server captured (not pushed).</done>
</task>

<task type="auto">
  <name>Task 3: WorkspaceEntry first-run welcome (detected clients + pills + copy)</name>
  <read_first>
    - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx (full — heading/body copy at :152/:157, button row :191-208, error :167-181)
    - 04.1-UI-SPEC.md Surface 2 (007-B first-run) + Copywriting Contract (empty heading/body, detected-client pills) + Accessibility Rule 1
    - 04.1-PATTERNS.md §WorkspaceEntry.tsx (detected-clients list + pill snippet)
  </read_first>
  <action>
    Edit `WorkspaceEntry.tsx`: change the empty heading to `No project open` and body to `Open or create a project, or pick a client install to start a mod.` (Copywriting Contract). Add an auto-scan "Detected clients" list from `clientLocator.detectClients()` rendered as clickable rows with `✓ ready` / `✗ not found` pills (glyph + border + label — never color-only); clicking a ready client calls `projectBinding.initProject(installPath)`. Surface the `＋ Project ▾` control (New opens the wizard; Open… picks a folder) and `Mount Archive…`. Keep the inline error pattern. Do not introduce a Tailwind utility layer.
  </action>
  <acceptance_criteria>
    - first-run shows `No project open` + detected-clients list with triple-encoded ready/not-found pills
    - clicking a ready client binds + auto-mounts (initProject)
    - `pnpm -w tsc --noEmit` green; existing WorkspaceEntry consumers (DeployPanel gate) still compile
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit && pnpm --filter @swg/renderer test -- --run src/panels/deploy 2>&1 | tail -10</automated>
  </verify>
  <done>First-run welcome drives binding from detected clients; copy matches UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user folder/file picks → projectBinding | OS-dialog paths flow into detection + persistence |
| captured server config → workspace.json | host:port/path stored locally, never executed in this phase |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-07 | Tampering | wizard folder/cfg inputs | mitigate | All paths come from `workspace:pick-dir`/`pick-file` IPC (main-process dialog), not free-text shell strings; persisted via projectBinding atomic write |
| T-04.1-08 | Elevation | captured serverConfig | accept | Capture-only (D-01) — no connection/exec made this phase; host:port stored as opaque string, surfaced read-only |
| T-04.1-09 | Spoofing | "treat as client" override | mitigate | User-confirmed via the unconfirmed-directory branch; deploy stays disabled for non-client kind |
| T-04.1-SC | Tampering | package installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `pnpm -w tsc --noEmit` green
- `pnpm --filter @swg/renderer test -- --run src/panels/deploy` green (DeployPanel non-client gating still passes)
- Real-Electron behavior validated in the phase UAT (plan 11) — jsdom cannot exercise IPC dialogs
</verification>

<success_criteria>
PROJ-01 front door usable: ＋ Project / wizard / first-run all drive `initProject`; non-client projects first-class; "treat as client" forces client kind; client TREs always mount; server captured only.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-04-SUMMARY.md` when done
</output>
