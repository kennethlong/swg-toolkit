---
phase: 04.1-deploy-project-ux
plan: 07
type: execute
wave: 6
depends_on: ["04.1-06", "04.1-03"]
files_modified:
  - packages/renderer/src/services/shadowBaseService.ts
  - packages/renderer/src/services/cfgActivator.ts
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
  - packages/renderer/src/services/shadowBaseService.test.ts
  - packages/renderer/test/cfgActivator.test.ts
autonomous: true
requirements: [DEPLOY-06]
must_haves:
  truths:
    - "The hardlink full-shadow path uses fs.link on a same-NTFS-volume work dir and falls back to copyFile only on EXDEV (multi-GB copy retired as default)"
    - "The ROOT client cfg (the one that receives the .include) is snapshotted into .studio BEFORE any mutation, and Reset = whole-file restore of that snapshot (which removes the .include + any maxSearchPriority bump) for ALL deploy models"
    - "Reset (the existing done-phase Reset button, rewired) leaves the client BYTE-PRISTINE: root cfg restored whole-file, .include gone, toolkit cfg + patch .tre removed"
    - "A mid-deploy failure auto-rolls-back via restoreCfg on BOTH the absolute-path and hardlink-shadow paths"
    - "Selecting Baseline and deploying performs reset-to-stock (restoreCfg), not packPatch — the flatten-empty error is bypassed for Baseline"
    - "No duplicate [SharedFile] header after two consecutive deploys (single idempotent header owner)"
    - "The absolute-path override is the default deploy model and writes a whitespace-free searchTree value"
  artifacts:
    - path: "packages/renderer/src/services/cfgActivator.ts"
      provides: "snapshotCfg(rootCfg)/restoreCfg whole-file + idempotent [SharedFile] + backup-into-.studio"
      exports: ["snapshotCfg", "restoreCfg"]
    - path: "packages/renderer/src/services/shadowBaseService.ts"
      provides: "fs.link hardlink path + same-volume guard + EXDEV→copyFile fallback"
      contains: "link"
  key_links:
    - from: "packages/renderer/src/services/shadowBaseService.ts"
      to: "fs.promises.link"
      via: "hardlink materialize w/ Stats.dev same-volume check"
      pattern: "\\.link\\("
    - from: "packages/renderer/src/services/cfgActivator.ts"
      to: ".studio/snapshots"
      via: "whole-file ROOT cfg snapshot + restore"
      pattern: "snapshot"
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "cfgActivator.restoreCfg"
      via: "Reset + auto-rollback + Deploy-Baseline reset-to-stock"
      pattern: "restoreCfg"
---

<objective>
Land the zero-risk shadow re-architecture (DEPLOY-06): swap `shadowBaseService`'s multi-GB copy loop for `fs.link` hardlinks with a `Stats.dev` same-volume guard and EXDEV→copyFile fallback (D-02); add `snapshotCfg`/`restoreCfg` whole-file cfg snapshot+restore of the ROOT cfg into `.studio` and relocate all cfg backups out of the client install (D-06/D-07); make whole-file `restoreCfg` the PRIMARY Reset path for ALL models (removing the `.include` + maxSearchPriority bumps so the client is byte-pristine); make `[SharedFile]` a single idempotent header owner (Pitfall 4); set the absolute-path override as the default deploy model with a whitespace-free `searchTree` value (D-05); route a Baseline deploy to reset-to-stock (H1); auto-roll-back on mid-deploy failure (H5/M9); and load the deploy record cross-session for Reset after re-open (M8).

D-09 note (pluggable TRE codec interface): honored as a NO-OP this phase — the existing TRE read/pack engine is reused as-is, no byte-level codec work is performed, and the pluggable-codec extension point is deliberately deferred (no encrypted/proprietary variant in scope here). This is a recorded acknowledgment of the locked decision, not a silent drop.

Purpose: the original client can never be permanently broken; reset is a whole-file restore that makes the client byte-pristine.
Output: hardlink path, ROOT-cfg snapshot/restore, dup-header fix, deploy-model radio default, Baseline-reset routing, auto-rollback, cross-session Reset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-RESEARCH.md
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-UI-SPEC.md

<interfaces>
LOCKED ground-truth (cite, do NOT re-derive): absolute `searchTree` paths accepted verbatim → CreateFile, no base-dir prepend, priorities high→low (swg-client-v2 TreeFile.cpp:130-138/:360-372, TreeFile_SearchNode.cpp:249-264, OsFile.cpp:86); ConfigFile truncates a searchTree value at first whitespace.

VERIFIED CURRENT BEHAVIOR (de-anchored against real source — these are the facts the fixes correct):
- DeployDialog.tsx:70 — `deployModel` state is currently `'patch-prepend' | 'shadow-base'` (NOT yet `absolute-path`/`hardlink-shadow`); the cfg-include model is `patch-prepend`. This plan renames the default to `absolute-path` per D-05 (keep `hardlink-shadow` as the opt-in shadow model).
- DeployDialog.tsx:97 — on open, `deployRecordRef.current = null` (in-memory only) → Reset is unavailable after dialog close/reopen unless the record is reloaded from the manifest (M8).
- DeployDialog.tsx:264 — `if (flattenedEntries.length === 0)` hard-errors. For Baseline, `flatten(BASELINE_ID)=[]` → this currently blocks "Deploy Baseline" (H1).
- DeployDialog.tsx:350-355 — creates `swgtoolkit.cfg` writing `'[SharedFile]\r\n'` (should be EMPTY — Pitfall 4); :366 `ensureInclude(selectedClient.cfgRootPath, 'swgtoolkit.cfg')` appends `.include` to the ROOT cfg.
- DeployDialog.tsx:316-323 — shadow-path catch sets `cfgRestored:false` and does NOT roll back (H5/M9).
- DeployDialog.tsx:397-422 — a "Reset deployment" button ALREADY EXISTS (rendered at :687-719) wired to `handleReset`. `handleReset` currently does LINE-SURGERY (`deactivatePatch` at :409) + `fs.unlinkSync(patch)` — it does NOT remove the `.include`, does NOT restore the root cfg whole-file. ⚠ DE-ANCHOR: the fix is to REWIRE the existing `handleReset`, NOT to add a new button.
- DeployDialog.tsx:729 — the error message falsely claims "The .cfg was restored from backup." while only line-surgery happened (H5(5) — fix the copy to match reality).
- cfgActivator.ts:58-74 `ensureInclude` (appends `.include` to root cfg, idempotent); :100-146 `activatePatch` (backs up the TOOLKIT cfg, not the root); :168-189 `deactivatePatch` (line-surgery only — never removes `.include`, never whole-file restores).
- shadowBaseService.ts:260 (checkFreeDisk), :273-280 (copy loop), :237-242 (W4 containment), :305-306 (.shadow.bak next to cfg).
fs.link → CreateHardLinkW; throws EXDEV cross-volume; Stats.dev == volume serial on Windows (RESEARCH §Pattern 1, cited Node docs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — shadow hardlink + ROOT-cfg snapshot/restore/dup-header/absolute-path tests</name>
  <read_first>
    - 04.1-VALIDATION.md DEPLOY-06 rows (hardlink+EXDEV fallback; snapshot byte-for-byte; no dup header; whitespace-free searchTree)
    - packages/renderer/test/cfgActivator.test.ts (existing real-temp-dir cfg tests to extend)
    - 04.1-PATTERNS.md §Wave 0 Test Files (fs temp-dir pattern) + §shadowBaseService.ts / §cfgActivator.ts
  </read_first>
  <action>
    Create `packages/renderer/src/services/shadowBaseService.test.ts`: mock `node:fs`/`fs.promises` so `link` succeeds when a `sameVolume` stub returns true (assert `fs.promises.link` called, `copyFile` NOT called), and so `link` throws `{code:'EXDEV'}` when cross-volume (assert fallback to `copyFile`; assert `checkFreeDisk` runs only on the copy fallback, not the hardlink path — M9). Extend `packages/renderer/test/cfgActivator.test.ts` (real temp dirs): (a) `snapshotCfg(rootCfgPath, studioDir)` writes a copy of the ROOT cfg into `.studio/snapshots`; after `ensureInclude` appends `.include` + a maxSearchPriority bump, `restoreCfg` reproduces the original ROOT cfg byte-for-byte (read both, assert Buffer.equals) AND the restored file no longer contains the `.include` line or the bump (byte-pristine); (b) running the activate path twice yields exactly ONE `[SharedFile]` header (grep count, filtering comment lines per grep hygiene); (c) the absolute-path default writes a `searchTree` value containing no whitespace. RED.
  </action>
  <acceptance_criteria>
    - both test files exist and fail for missing `snapshotCfg`/`restoreCfg` whole-file semantics + hardlink behavior + checkFreeDisk-on-fallback-only
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/shadowBaseService.test.ts test/cfgActivator.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>DEPLOY-06 fs behaviors (incl. byte-pristine restore) pinned by failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cfgActivator — ROOT-cfg snapshot/whole-file restore + idempotent [SharedFile] + backup relocation</name>
  <read_first>
    - packages/renderer/src/services/cfgActivator.ts:58-74 (ensureInclude — appends .include to root cfg), :100-146 (activatePatch incl. backup :107-108 + [SharedFile] :118-128 + atomic write :133-135), :168-189 (deactivatePatch line-surgery)
    - 04.1-PATTERNS.md §cfgActivator.ts (backup relocation, idempotent-header, snapshotCfg/restoreCfg snippets)
    - 04.1-CONTEXT.md D-07 (whole-file restore is the primary reset path; client stays pristine)
  </read_first>
  <behavior>
    - `snapshotCfg(rootCfgPath, studioDir)` copies the ROOT cfg (the file `ensureInclude` mutates — e.g. swgemu.cfg) into `.studio/snapshots/<basename>.bak`, returns the snapshot path. Idempotent per deploy: snapshot is taken once, BEFORE the first mutation, and not overwritten by a later deploy that would capture an already-mutated cfg.
    - `restoreCfg(rootCfgPath, snapshotPath)` whole-file restores the ROOT cfg via atomic tmp+rename; output Buffer-equals the original (removes the `.include` line AND any maxSearchPriority bump because the whole pre-mutation file is written back).
    - `activatePatch` adds `[SharedFile]` only if not already present (idempotent); a second deploy adds no second header.
    - backups/snapshots are written into `.studio`, never next to the client cfg.
  </behavior>
  <action>
    Edit `cfgActivator.ts`: relocate the backup write (:107-108) into `.studio/snapshots` (whitespace-free path from plan 06 getStudioDir); make the `[SharedFile]` block (:118-128) idempotent (`needsHeader = !existing.includes('[SharedFile]')`); add exported `snapshotCfg(rootCfgPath, studioDir)` and `restoreCfg(rootCfgPath, snapshotPath)` modeled on the atomic write at :133-135 (BOM-free utf8 / copyFileSync). `snapshotCfg` MUST target the ROOT cfg (the one `ensureInclude` edits), not the toolkit-owned `swgtoolkit.cfg`. `restoreCfg` is whole-file copy-back (the primary reset path per D-07). Keep the existing BOM-free atomic write and never write `user.cfg`/`options.cfg`. Make Task-1 cfg tests GREEN.
  </action>
  <acceptance_criteria>
    - `snapshotCfg`/`restoreCfg` reproduce the ROOT cfg byte-for-byte and the restored file has no `.include`/bump residue; exactly one `[SharedFile]` after two deploys; backups live under `.studio`
    - absolute-path default writes a whitespace-free `searchTree` value
    - cfgActivator.test.ts green
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run test/cfgActivator.test.ts && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>ROOT-cfg snapshot + whole-file restore + idempotent header + relocated backup all GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shadowBaseService hardlink path + DeployDialog model/default/reset/rollback/Baseline-reset/cross-session</name>
  <read_first>
    - packages/renderer/src/services/shadowBaseService.ts:260 (checkFreeDisk), :273-280 (copy loop), :237-242 (W4 containment), :305-306 (.shadow.bak)
    - packages/renderer/src/panels/deploy/DeployDialog.tsx:70 (deployModel), :91-118 (open effect; :97 ref clear), :263-272 (flatten + length-0 error), :294-325 (shadow path + catch), :350-366 (toolkit cfg create + ensureInclude), :397-422 (handleReset), :687-719 (existing Reset button + confirm), :723-731 (error message)
    - 04.1-PATTERNS.md §shadowBaseService.ts (sameVolume + link loop + skip-checkFreeDisk + backup relocation) + §DeployDialog.tsx (deployModel radio + empty-file create)
    - 04.1-UI-SPEC.md Surface 6 (deploy model: absolute-path default selected; hardlink opt-in with disk/same-volume caution) + Copywriting (Reset = Cancel secondary + Reset danger-outline)
    - 04.1-RESEARCH.md §Pattern 1 (EXDEV fallback ladder; Assumption A4 = copyFile fallback)
  </read_first>
  <behavior>
    - hardlink path: `sameVolume(liveDir, destParent)` via Stats.dev; if true use `fs.promises.link` per file; on EXDEV fall back to `copyFile`; `checkFreeDisk` skipped for the hardlink path, applied only for the copy fallback (M9).
    - DeployDialog default model = `absolute-path` (no copy); `hardlink-shadow` is opt-in and shows the disk/same-volume caution.
    - toolkit cfg created empty; `cfgActivator.activatePatch` is the sole `[SharedFile]` writer.
    - `ensureInclude` is called for the active deploy model on BOTH the absolute-path and hardlink-shadow paths (M9 — the shadow path must not skip it).
    - Before the first cfg mutation, `snapshotCfg(rootCfgPath, studioDir)` captures the pristine ROOT cfg.
    - Reset (the existing :687-719 button, via rewired `handleReset`) = `restoreCfg(rootCfgPath, snapshotPath)` (whole-file, removes .include + bumps) + delete the toolkit `swgtoolkit.cfg` + delete the deployed patch .tre + remove the built patch from `.studio/build` + clear the manifest deploy record — for ALL models. Client ends byte-pristine.
    - Mid-deploy failure on EITHER path → auto-rollback via `restoreCfg`; set `cfgRestored:true` only when restore actually ran; the error copy matches what happened (H5(5)).
    - Deploy with the active version = Baseline (`activeVersionId === BASELINE_ID`, or flatten empty on a valid version) → bypass the :264 length-0 error and route to the reset-to-stock path (`restoreCfg` + cleanup) instead of packPatch (H1).
    - Cross-session: on dialog open, if no in-memory `deployRecordRef`, load the persisted deploy record + snapshot path from the manifest/`.studio` so Reset works after re-open (M8).
  </behavior>
  <action>
    Edit `shadowBaseService.ts`: add `sameVolume(a,b)` (Stats.dev compare) and replace the copy loop (:273-280) with the hardlink-then-EXDEV-fallback loop (RESEARCH §Pattern 1); guard `checkFreeDisk` (:260) to run only when `!canLink` (M9); relocate `.shadow.bak` (:305-306) into `.studio/snapshots`; keep the W4 containment guard (:237-242) and validate dest containment before each `fs.link` (hardlink-escape mitigation).
    Edit `DeployDialog.tsx`:
    (1) set `deployModel` default to `'absolute-path'` (D-05) with `'hardlink-shadow'` opt-in radio (Surface 6 caution copy + `--color-warn` disk note); change the toolkit-cfg create at :354 from `'[SharedFile]\r\n'` to an EMPTY file (Pitfall 4 — let activatePatch own the header).
    (2) Before the first cfg mutation in `handleDeploy`, call `snapshotCfg(selectedClient.cfgRootPath, studioDir)` and persist the snapshot path with the deploy record; ensure `ensureInclude` is called for the active model on BOTH paths (M9 — add it to the shadow branch if missing).
    (3) H1: when `manifest.activeVersionId === BASELINE_ID` (import BASELINE_ID from `@swg/contracts`) — or `flattenedEntries.length === 0` on an otherwise valid version — do NOT throw the :264 error; instead route to the reset-to-stock path: `restoreCfg` + delete toolkit cfg + delete any deployed patch + clear deploy record, then set phase done ("reset to stock"). "Deploy Baseline" = reset.
    (4) H5: REWIRE the existing `handleReset` (:397-422 — the button at :687-719 already calls it; do NOT add a new button) to call `restoreCfg(rootCfgPath, snapshotPath)` as the PRIMARY reset for ALL models, then delete the toolkit `swgtoolkit.cfg`, delete the deployed patch .tre, remove the built patch from `.studio/build`, and clear the manifest deploy record. Replace the line-surgery-only path. (`deactivatePatch` may remain as a defensive secondary, but whole-file restore is primary.)
    (5) H5/M9 auto-rollback: in the error catches on BOTH the absolute-path (:381-389) and shadow (:316-323) paths, call `restoreCfg` and set `cfgRestored:true` only if restore ran; fix the misleading message at :729 to state what actually happened (e.g. "The original .cfg was restored." only when it was).
    (6) M8: in the open effect (:91-118), if `deployRecordRef.current` is null, load the persisted deploy record (incl. snapshot path) from the manifest/`.studio` so Reset is available after re-open.
    Do NOT change `buildTre`/pack or the flatten(active) deploy target for non-Baseline versions. Make Task-1 shadow tests GREEN.
  </action>
  <acceptance_criteria>
    - hardlink path calls `fs.link` (not copyFile) on same-volume; EXDEV → copyFile fallback; `checkFreeDisk` only on the copy fallback (shadowBaseService.test.ts green)
    - DeployDialog default model is absolute-path; hardlink is opt-in with caution; toolkit cfg created empty → single [SharedFile] (with task 2)
    - Reset (rewired handleReset) restores root cfg whole-file (.include + bumps gone), deletes toolkit cfg + patch + built patch, clears deploy record → client byte-pristine
    - mid-deploy failure on either path auto-rolls-back via restoreCfg; message matches reality
    - "Deploy Baseline" routes to reset-to-stock (no length-0 error)
    - Reset works after dialog reopen (record loaded from manifest)
    - `pnpm test` full unit green
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/shadowBaseService.test.ts test/cfgActivator.test.ts && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Hardlink/absolute-path models work; whole-file Reset makes client byte-pristine; auto-rollback + Baseline-reset + cross-session Reset wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renderer fs → client cfg | the only mutation of the client install is the cfg edit; everything else stays in .studio |
| hardlink src/dest → NTFS volume | base TREs hardlinked into a work dir must stay contained on the same volume |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-15 | Tampering | corrupting client cfg on failed write | mitigate | ROOT-cfg whole-file snapshot taken before first mutation; atomic tmp+rename; mid-deploy failure auto-rolls-back via restoreCfg (D-07) |
| T-04.1-16 | Tampering | hardlink/symlink escaping the work dir | mitigate | W4 `path.relative` containment (shadowBaseService.ts:237-242) validated before each `fs.link`; only `.tre` files from the detected TRE dir linked |
| T-04.1-17 | DoS | cross-volume hardlink (EXDEV) | mitigate | Stats.dev same-volume check; EXDEV → copyFile fallback with caution surfaced; checkFreeDisk on the copy fallback only |
| T-04.1-18 | Tampering | duplicate/contradictory cfg headers | mitigate | Single idempotent `[SharedFile]` owner (activatePatch); empty-file create elsewhere (Pitfall 4) |
| T-04.1-28 | Tampering | non-pristine client after Reset (.include/bump residue) | mitigate | Reset = whole-file restoreCfg of the ROOT cfg + delete toolkit cfg + patch; removes .include + bumps → byte-pristine |
| T-04.1-SC | Tampering | package installs | accept | No new packages (RESEARCH audit: none); fs.link is a Node builtin (no native pkg) |
</threat_model>

<verification>
- `vitest run src/services/shadowBaseService.test.ts test/cfgActivator.test.ts` green
- `pnpm test` full unit green at wave merge
- `pnpm -w tsc --noEmit` green
- Real hardlink + in-game load + byte-pristine Reset validated in the phase UAT (plan 11) — jsdom cannot exercise NTFS hardlinks
</verification>

<success_criteria>
DEPLOY-06 met at the service layer: hardlink/absolute-path models replace the multi-GB copy default; ROOT-cfg snapshot + whole-file restore keeps the client byte-pristine on Reset (and auto-rollback); "Deploy Baseline" = reset-to-stock; single [SharedFile] header.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-07-SUMMARY.md` when done
</output>
