# Phase 04.1: Deploy & Project UX — Discussion Log

**Date:** 2026-06-27 · Human-reference audit trail (not consumed by downstream agents).

## Framing

Most of the design was already settled before discussion (approved sketches 005-B/006-D/007/008 + 10
UAT todos with maintainer decisions + 2 of 3 ground-truth verifications already RESOLVED). Discussion
focused only on genuinely-open scope/sequencing boundaries.

## Areas discussed (4 decisions)

1. **Server-side / local-server scope**
   - Options: capture-only now · defer all to Phase 8 · full push in 4.1
   - **Chosen: Capture-only now** — store the 007 local-server config (type/path/host:port), surface it;
     no server push (Phase 8). Realizes approved 007 design cheaply; avoids unverified server work.

2. **Hardlink full-shadow (model D) scope**
   - Options: default-only this phase · include hardlink now
   - **Chosen: Include hardlink full-isolation now** — ship absolute-path no-copy default AND opt-in
     hardlink "throwaway isolated client"; retire the multi-GB physical copy.

3. **Persisted Dockview layout migration**
   - Maintainer initially confused (thought it touched the 008 design). Clarified: it's only the stale
     localStorage layout from the old 3-tab build, independent of the 008 design.
   - **Chosen: Clear stale layout on version mismatch** ("safest path forward") → dock rebuilds to the
     008 vertical-stack default; pairs with the new reset-layout affordance.

4. **Build priority (MVP vertical slices)**
   - Options: front-door + centerpiece first · engine/shadow first · planner decides
   - **Chosen: Front-door + centerpiece first** — slice 1 binding+auto-mount+combined tab; slice 2
     shadow re-arch + baseline; slice 3 stage-from-TRE + detection + polish + local-server capture.

## Deferred (redirected scope)

- Server-side TRE push/registration → Phase 8.
- e2e deploy-flow coverage → separate testing workstream.
- Viewport/terrain/perf todos that matched on area only → their own phases.
