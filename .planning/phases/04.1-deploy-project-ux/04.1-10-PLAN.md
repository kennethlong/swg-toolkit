---
phase: 04.1-deploy-project-ux
plan: 10
type: execute
wave: 8
depends_on: ["04.1-02", "04.1-03", "04.1-04", "04.1-05", "04.1-07", "04.1-08", "04.1-09"]
files_modified:
  - packages/contracts/src/ipc.ts
  - packages/backend/src/main.ts
  - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
  - packages/renderer/src/panels/deploy/StagingPanel.tsx
  - packages/renderer/src/panels/deploy/ChangesetTimelinePanel.tsx
  - packages/renderer/src/panels/deploy/DeployDialog.tsx
  - packages/renderer/src/panels/tre/TreVfsBrowser.tsx
  - packages/renderer/src/panels/deploy/NewProjectWizard.tsx
  - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx
  - packages/renderer/src/services/projectBinding.ts
autonomous: true
requirements: [DEPLOY-05, PROJ-01]
must_haves:
  truths:
    - "Each IPC channel's (args,return) type is defined once in @swg/contracts and imported by every call site"
    - "The BACKEND ipcMain handlers are bound to the same channel map — a return-type drift fails tsc on BOTH the handler side and the renderer caller side (not just the renderer)"
    - "A handler/return-type mismatch is a compile error (the silent string|null vs string[] deploy bug class is eliminated)"
    - "The retired StagingPanel.tsx + ChangesetTimelinePanel.tsx originals (replaced by the extracted bodies + unregistered from dockview in plans 03/05) are deleted with zero remaining imports"
  artifacts:
    - path: "packages/contracts/src/ipc.ts"
      provides: "IpcChannels (args+return) map + TypedIpcRenderer + TypedIpcMain helper"
      exports: ["IpcChannels", "TypedIpcRenderer"]
  key_links:
    - from: "packages/renderer/src/panels/deploy/DeployDialog.tsx"
      to: "packages/contracts/src/ipc.ts"
      via: "TypedIpcRenderer replaces inline cast"
      pattern: "TypedIpcRenderer"
    - from: "packages/backend/src/main.ts"
      to: "packages/contracts/src/ipc.ts"
      via: "ipcMain handlers typed against IpcChannels"
      pattern: "IpcChannels"
---

<objective>
Centralize the IPC channel types (Pattern 8 / D-04 slice 3): define each channel's (args,return) shape once in `@swg/contracts`, type BOTH the renderer call sites AND the backend `ipcMain` handlers against it, so a handler/return-type mismatch becomes a tsc error on either side — eliminating the bug class that caused the silent `string|null` vs `string[]` deploy failure. Then delete the now-dead `StagingPanel.tsx` + `ChangesetTimelinePanel.tsx` originals (their bodies were extracted in plan 03 and their dockview registration removed in plan 05) once nothing imports them (W5 cleanup).

Purpose: one source of truth for IPC shapes enforced on both ends; no per-file cast drift; no orphaned dead components left compiling.
Output: `ipc.ts` channel map + typed backend handlers + all renderer call sites swapped + dead originals removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-RESEARCH.md
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md

<interfaces>
Handlers (backend/src/main.ts:234-263, verified UNTYPED `ipcMain.handle('…', async () => …)`) all return `string[]`: `tre:pick-archives` (:234), `workspace:pick-dir` (:246), `workspace:pick-file` (:257). H6: these MUST be bound to the shared channel map so a drift in a handler's return type fails tsc on the backend side too — today only the renderer casts are typed.
Inline-cast sites to replace (verified): WorkspaceEntry.tsx:26-30, StagingPanel.tsx:258-262, DeployDialog.tsx:176-178, TreVfsBrowser.tsx:114-116, plus the new call sites in NewProjectWizard.tsx, ProjectBindingBar.tsx, projectBinding.ts.
ipc.ts currently holds only SAB message types (verified — no IpcChannels map) — extend it (RESEARCH §Pattern 8).
Plan 05 unregisters `staging`/`changesets` from dockview (depends_on edge added so deletion here is safe). Dead originals (post plan 03 extraction + plan 05 unregistration): `StagingPanel.tsx` (logic → `StagingPanelBody.tsx`), `ChangesetTimelinePanel.tsx` (logic → `VersionHistoryBody.tsx`). Their `*.test.tsx` files (if they import the deleted component) are orphaned and must go too.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IpcChannels map + TypedIpcRenderer in contracts AND type the backend handlers</name>
  <read_first>
    - packages/contracts/src/ipc.ts (full — extend, keep SAB types)
    - backend/src/main.ts:234-263 (the canonical handlers — bind these to the map)
    - 04.1-PATTERNS.md §contracts/src/ipc.ts (exact IpcChannels + TypedIpcRenderer shape)
  </read_first>
  <action>
    Extend `contracts/src/ipc.ts` with an `IpcChannels` interface mapping each channel `'workspace:pick-dir' | 'workspace:pick-file' | 'tre:pick-archives'` to its return type `string[]` (matching backend handlers exactly; args are none today — model as `[]`), a `TypedIpcRenderer` type with `invoke<C extends keyof IpcChannels>(channel: C): Promise<IpcChannels[C]>`, and a `TypedIpcHandler<C>` helper type `() => IpcChannels[C] | Promise<IpcChannels[C]>` for binding handlers. Keep existing SAB message exports; re-export via `contracts/src/index.ts`. H6 — then edit `backend/src/main.ts`: type each `ipcMain.handle` callback against `IpcChannels` (e.g. annotate the handler so its resolved return is `IpcChannels['workspace:pick-dir']`), so changing a handler to return the wrong shape fails `tsc`. Confirm a deliberate wrong-return in a handler would now error on the backend side.
  </action>
  <acceptance_criteria>
    - `IpcChannels` + `TypedIpcRenderer` exported from `@swg/contracts`
    - channel return types equal the backend handler return types (all `string[]`)
    - backend `ipcMain` handlers are typed against `IpcChannels` (a wrong return shape fails tsc on the backend)
    - `pnpm -w tsc --noEmit` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Single typed IPC channel map exists and is enforced on the backend handler side.</done>
</task>

<task type="auto">
  <name>Task 2: Swap all renderer inline casts to TypedIpcRenderer</name>
  <read_first>
    - 04.1-PATTERNS.md §contracts/src/ipc.ts ("Callers to update" list) + §Shared Patterns "IPC pattern"
    - WorkspaceEntry.tsx:26-30, StagingPanel.tsx:258-262, DeployDialog.tsx:176-178, TreVfsBrowser.tsx:114-116 (existing casts)
  </read_first>
  <action>
    Replace each inline `require('electron') as { ipcRenderer: { invoke(channel: '…'): Promise<…> } }` with `require('electron') as { ipcRenderer: TypedIpcRenderer }` (import `TypedIpcRenderer` from `@swg/contracts`) across all listed call sites AND the new files from plans 02/04/09 (NewProjectWizard, ProjectBindingBar, projectBinding). For `StagingPanel.tsx`, swap the cast too so the tree stays green through this task — the file is deleted in Task 3. Do not change runtime behavior — only the types. Confirm the deploy-relevant pick-dir call now type-checks as `string[]` (no `string|null` regression possible).
  </action>
  <acceptance_criteria>
    - no remaining inline per-site channel return-type casts for the three channels
    - `pnpm -w tsc --noEmit` green (a deliberate mismatch test, e.g. typing a channel wrong, would now fail on BOTH the handler and caller — verified by the centralized map)
    - `pnpm test` full unit green (no behavior change)
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit && pnpm --filter @swg/renderer test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>All IPC call sites use the centralized typed channel map.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the dead StagingPanel + ChangesetTimelinePanel originals (W5 cleanup)</name>
  <read_first>
    - 04.1-PATTERNS.md §StagingPanelBody.tsx / §VersionHistoryBody.tsx (confirm the extracted bodies fully replace the originals)
    - packages/renderer/src/workspace/WorkspaceShell.tsx (confirm plan 05 removed the dockview registration of the two originals)
  </read_first>
  <action>
    After Task 2 (so the type-touch on `StagingPanel.tsx` is done), grep the renderer package for any import or dockview registration of `StagingPanel` (the original, not `StagingPanelBody`) and `ChangesetTimelinePanel` (not `VersionHistoryBody`). Once confirmed there are zero live references, DELETE `packages/renderer/src/panels/deploy/StagingPanel.tsx` and `packages/renderer/src/panels/deploy/ChangesetTimelinePanel.tsx`, plus any now-orphaned `StagingPanel.test.tsx` / `ChangesetTimelinePanel.test.tsx` that import only the deleted components (the live behavior is covered by `StagingPanelBody.test.tsx` / `DeployPanel.test.tsx` from plan 03). Do NOT delete `StagingPanelBody.tsx`/`VersionHistoryBody.tsx`/`DeployPanel.tsx` or their tests. If any non-test file still imports an original, STOP and report (do not force-delete). Confirm tsc + full test suite stay green after removal.
  </action>
  <acceptance_criteria>
    - `StagingPanel.tsx` + `ChangesetTimelinePanel.tsx` (and their orphaned test files) deleted
    - grep for `from './StagingPanel'` / `from './ChangesetTimelinePanel'` (originals) returns zero matches in the renderer package
    - `pnpm -w tsc --noEmit` green and `pnpm test` full unit green after deletion
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit && pnpm --filter @swg/renderer test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Dead originals removed; no remaining imports; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renderer ↔ main IPC | typed contract is the only guard against shape drift between handler and caller |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-24 | Tampering | IPC return-shape drift (silent deploy bug class) | mitigate | Single `IpcChannels` map typed on BOTH the backend handlers and renderer callers → any handler/caller mismatch is a tsc compile error (Pattern 8 / H6) |
| T-04.1-SC | Tampering | package installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `pnpm -w tsc --noEmit` green
- `pnpm test` full unit green (type refactor + dead-code deletion — no behavior change)
- grep confirms zero remaining imports of the deleted originals
- backend handlers reference `IpcChannels` (a wrong handler return fails tsc)
</verification>

<success_criteria>
IPC channel types centralized and enforced on BOTH ends; the string|null vs string[] silent-bug class is compile-time-impossible; the retired StagingPanel/ChangesetTimelinePanel originals are deleted with no dangling references.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-10-SUMMARY.md` when done
</output>
