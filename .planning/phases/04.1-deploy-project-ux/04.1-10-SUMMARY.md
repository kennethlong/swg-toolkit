---
phase: "04.1"
plan: "10"
subsystem: "ipc-contracts"
tags: ["ipc", "contracts", "type-safety", "cleanup", "dead-code"]
dependency_graph:
  requires:
    - "04.1-03: StagingPanelBody/VersionHistoryBody extracted, StagingPanel/ChangesetTimelinePanel bodies moved"
    - "04.1-05: dockview registration of staging/changesets removed from WorkspaceShell"
    - "04.1-02: projectBinding.ts, treMount.ts — IPC call sites added"
    - "04.1-04: NewProjectWizard.tsx, ProjectBindingBar.tsx — IPC call sites added"
    - "04.1-09: WorkspaceEntry.tsx updated with new IPC call sites"
  provides:
    - "IpcChannels interface (workspace:pick-dir, workspace:pick-file, tre:pick-archives) in @swg/contracts"
    - "TypedIpcRenderer type (single centralized invoke type for renderer call sites)"
    - "TypedIpcHandler<C> helper type for backend handler definitions"
    - "All 7 renderer IPC call sites updated to TypedIpcRenderer"
    - "All 3 backend ipcMain handlers typed against IpcChannels"
    - "StagingPanel.tsx + ChangesetTimelinePanel.tsx + ChangesetTimelinePanel.test.tsx deleted (W5)"
  affects:
    - "All renderer files that invoke IPC channels (type mismatch now fails tsc on both ends)"
tech_stack:
  added: []
  patterns:
    - "IpcChannels map: single source of truth for renderer↔main IPC return shapes (Pattern 8 / H6)"
    - "TypedIpcRenderer: centralized invoke type replacing per-file inline casts"
    - "TypedIpcHandler<C>: backend handler callback typed against same IpcChannels map"
    - "Dead-code W5 cleanup: extracted bodies in new files + dockview unregistered = safe delete"
key_files:
  created: []
  modified:
    - packages/contracts/src/ipc.ts
    - packages/backend/src/main.ts
    - packages/renderer/src/panels/deploy/WorkspaceEntry.tsx
    - packages/renderer/src/panels/deploy/NewProjectWizard.tsx
    - packages/renderer/src/panels/deploy/ProjectBindingBar.tsx
    - packages/renderer/src/panels/deploy/StagingPanelBody.tsx
    - packages/renderer/src/panels/deploy/DeployDialog.tsx
    - packages/renderer/src/panels/tre/TreVfsBrowser.tsx
  deleted:
    - packages/renderer/src/panels/deploy/StagingPanel.tsx
    - packages/renderer/src/panels/deploy/ChangesetTimelinePanel.tsx
    - packages/renderer/src/panels/deploy/ChangesetTimelinePanel.test.tsx
key_decisions:
  - "IpcChannels + TypedIpcRenderer added to existing ipc.ts (not a new file) — keeps all IPC types co-located"
  - "TypedIpcHandler<C> models handler callback as () => IpcChannels[C] | Promise<IpcChannels[C]> — no ipcMain Event arg (unused by all three handlers)"
  - "contracts dist/ is gitignored — tsc -p packages/contracts rebuilds declarations locally (no dist committed)"
  - "ChangesetTimelinePanel.test.tsx deleted along with the original: it imports only the deleted component and its 5 behaviors are fully covered by DeployPanel.test.tsx (VersionHistoryBody-based tests)"
requirements-completed: [DEPLOY-05, PROJ-01]
duration: "~6 min"
completed: "2026-06-28"
---

# Phase 04.1 Plan 10: IPC Type Centralization + W5 Dead-Code Cleanup Summary

**Single IpcChannels map + TypedIpcRenderer enforce renderer↔main IPC return shapes at compile time on both ends; StagingPanel/ChangesetTimelinePanel originals (bodies extracted in plan 03, dockview-unregistered in plan 05) deleted with zero dangling references.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-06-28T00:37:42Z
- **Completed:** 2026-06-28T00:43:52Z
- **Tasks:** 3
- **Files modified:** 8 (+ 3 deleted)

## Accomplishments

- `IpcChannels` interface and `TypedIpcRenderer` / `TypedIpcHandler<C>` types added to `@swg/contracts/src/ipc.ts` — single source of truth for all three IPC channels
- All 3 backend `ipcMain.handle` callbacks in `backend/src/main.ts` typed against `IpcChannels<C>` so a handler/return-type drift is a tsc error on the backend side (H6, T-04.1-24)
- All 7 renderer `require('electron')` IPC call sites updated to `TypedIpcRenderer` — the `string|null` vs `string[]` silent-deploy-failure bug class is now compile-time-impossible
- `StagingPanel.tsx` + `ChangesetTimelinePanel.tsx` + orphaned `ChangesetTimelinePanel.test.tsx` deleted; grep confirms zero remaining imports in the entire renderer package
- Full test suite: 33 files / 288 tests PASSED (80/80 renderer)

## Task Commits

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | IpcChannels map + TypedIpcRenderer in contracts; type backend handlers | 1fb9673 | contracts/src/ipc.ts, backend/src/main.ts |
| 2 | Swap all renderer IPC inline casts to TypedIpcRenderer | 43f7ebc | 7 renderer files |
| 3 | Delete dead StagingPanel + ChangesetTimelinePanel originals (W5) | c97dfaf | 3 files deleted |

## Files Created/Modified

- `packages/contracts/src/ipc.ts` — added `IpcChannels`, `TypedIpcRenderer`, `TypedIpcHandler<C>` (kept existing SAB message types)
- `packages/backend/src/main.ts` — added `import type { IpcChannels }` + typed all 3 `ipcMain.handle` callbacks
- `packages/renderer/src/panels/deploy/WorkspaceEntry.tsx` — module-level cast replaced
- `packages/renderer/src/panels/deploy/NewProjectWizard.tsx` — module-level cast + in-component cast both replaced
- `packages/renderer/src/panels/deploy/ProjectBindingBar.tsx` — module-level cast replaced
- `packages/renderer/src/panels/deploy/StagingPanelBody.tsx` — inline handleAdd cast replaced
- `packages/renderer/src/panels/deploy/DeployDialog.tsx` — inline handler cast replaced
- `packages/renderer/src/panels/tre/TreVfsBrowser.tsx` — inline handleMount try-block cast replaced

**Deleted:**
- `packages/renderer/src/panels/deploy/StagingPanel.tsx` — original, body extracted to StagingPanelBody.tsx (plan 03)
- `packages/renderer/src/panels/deploy/ChangesetTimelinePanel.tsx` — original, body extracted to VersionHistoryBody.tsx (plan 03)
- `packages/renderer/src/panels/deploy/ChangesetTimelinePanel.test.tsx` — orphaned test (only importer of the deleted ChangesetTimelinePanel)

## Decisions Made

- `TypedIpcHandler<C>` uses `() => IpcChannels[C] | Promise<IpcChannels[C]>` — models the callback signature; no `ipcMain.IpcMainInvokeEvent` arg because all three current handlers ignore the event arg
- The contracts `dist/` is gitignored, so tsc on the backend rebuilds declarations locally; the local build is the gate (not a committed dist artifact)
- `ChangesetTimelinePanel.test.tsx` deleted rather than migrated: its 5 tests (node labels, active/deployed classes, click handler, branch class) are fully covered by `DeployPanel.test.tsx` via `VersionHistoryBody`

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

- The workspace-level `pnpm -w tsc --noEmit` command was intercepted by tsc as `--watch` mode. Used `npx tsc -p packages/<name>/tsconfig.json --noEmit` per-package instead — same gate, same result.
- Backend `tsconfig.json` uses project references to `contracts`, which requires the `dist/` declarations to be up-to-date. Ran `npx tsc -p packages/contracts/tsconfig.json` once to regenerate `dist/ipc.d.ts` before running the backend check. Pre-existing `preload.ts` TS2556 errors are not introduced by this plan.

## Known Stubs

None.

## Threat Flags

No new threat surface. T-04.1-24 (IPC return-shape drift) is now fully mitigated:
- `IpcChannels` map + `TypedIpcRenderer` enforce shape parity on renderer call sites
- `TypedIpcHandler<C>` enforces shape parity on backend handler side
- A deliberate wrong-return on a handler (e.g. `string | null`) now fails tsc on `backend/src/main.ts`

## Self-Check: PASSED

- `packages/contracts/src/ipc.ts` — FOUND (modified with IpcChannels + TypedIpcRenderer)
- `packages/backend/src/main.ts` — FOUND (modified with IpcChannels import + typed handlers)
- `packages/renderer/src/panels/deploy/WorkspaceEntry.tsx` — FOUND (cast updated)
- `packages/renderer/src/panels/deploy/NewProjectWizard.tsx` — FOUND (2 casts updated)
- `packages/renderer/src/panels/deploy/ProjectBindingBar.tsx` — FOUND (cast updated)
- `packages/renderer/src/panels/deploy/StagingPanelBody.tsx` — FOUND (cast updated)
- `packages/renderer/src/panels/deploy/DeployDialog.tsx` — FOUND (cast updated)
- `packages/renderer/src/panels/tre/TreVfsBrowser.tsx` — FOUND (cast updated)
- `packages/renderer/src/panels/deploy/StagingPanel.tsx` — DELETED (confirmed absent)
- `packages/renderer/src/panels/deploy/ChangesetTimelinePanel.tsx` — DELETED (confirmed absent)
- `packages/renderer/src/panels/deploy/ChangesetTimelinePanel.test.tsx` — DELETED (confirmed absent)
- Commit 1fb9673 (Task 1) — FOUND
- Commit 43f7ebc (Task 2) — FOUND
- Commit c97dfaf (Task 3) — FOUND
