---
phase: 04.1-deploy-project-ux
plan: 05
type: execute
wave: 4
depends_on: ["04.1-03"]
files_modified:
  - packages/renderer/src/workspace/workspace-config.ts
  - packages/renderer/src/workspace/WorkspaceShell.tsx
  - packages/renderer/src/workspace/workspace-config.test.ts
  - packages/renderer/src/workspace/WorkspaceShell.test.tsx
autonomous: true
requirements: [DEPLOY-05, DEPLOY-07]
must_haves:
  truths:
    - "The shell registers ONE `deploy` panel (DeployPanel) within the Inspect|Deploy group, plus a separate `vcs` tab — the old staging/changesets panels are gone"
    - "On layout-version mismatch the saved dockview layout is discarded and the 008 default rebuilds (no soft-brick from retired panel ids)"
    - "Activating Deploy auto-widens the right dock to ~440px; activating Inspector narrows it to ~290px (dynamic, not a one-time initialWidth)"
  artifacts:
    - path: "packages/renderer/src/workspace/workspace-config.ts"
      provides: "LAYOUT_VERSION=2 + buildInitialLayout deploy/vcs panels"
      contains: "deploy"
    - path: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      provides: "DeployPanel registration + version-guard onReady + active-panel auto-widen handler"
      contains: "DeployPanel"
  key_links:
    - from: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      to: "packages/renderer/src/panels/deploy/DeployPanel.tsx"
      via: "panelComponents registration"
      pattern: "deploy"
    - from: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      to: "localStorage layout-version key"
      via: "version mismatch → buildInitialLayout"
      pattern: "LAYOUT_VERSION"
    - from: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      to: "dockview right-dock group width"
      via: "onDidActivePanelChange → setSize(width)"
      pattern: "ActivePanel"
---

<objective>
Integrate the combined Deploy panel into the dock (DEPLOY-05) and ship the layout-version migration guard (DEPLOY-07 / D-03) that MUST accompany the panel swap: replace the `staging`/`changesets` dockview panels with one `deploy` panel inside the Inspect|Deploy group (keeping `vcs` separate), bump `LAYOUT_VERSION`, discard any persisted layout that references retired panel ids so returning users are not soft-bricked (Pitfall 2 — `fromJSON` does not reliably throw on unknown component ids), and add a dynamic active-panel auto-widen handler so activating Deploy widens the right dock to ~440px (M3 — `initialWidth` alone only sets it once).

Note: the version-guard ships here (with the swap) because shipping the swap without it strands saved layouts. The user-facing Reset-layout / reopen-closed-panel affordance is plan 08.

Purpose: make the centerpiece reachable in the real shell, safely.
Output: panel swap + version guard + active-panel auto-widen + their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-RESEARCH.md
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-UI-SPEC.md

<interfaces>
From plan 03: `DeployPanel` default export at panels/deploy/DeployPanel.tsx.
Verified sites: workspace-config.ts:20-22 (constants), :96-114 (buildInitialLayout staging/changesets/vcs); WorkspaceShell.tsx:29-41 (panelComponents), :46-74 (onReady fromJSON try/catch).
dockview@6.6.1 exposes `api.clear()`, `api.onDidActivePanelChange`, and group `api.setSize({ width })`. RESEARCH §Pattern 6 has the exact version-guard mechanism.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — layout-version + panel-swap + auto-widen tests</name>
  <read_first>
    - 04.1-VALIDATION.md DEPLOY-07 rows (layout-version mismatch discards + rebuilds; deploy panel registered)
    - packages/renderer/src/workspace/workspace-config.ts:96-114 (current buildInitialLayout)
    - packages/renderer/src/workspace/WorkspaceShell.tsx:46-74 (onReady)
    - 04.1-RESEARCH.md §Pattern 6 (LAYOUT_VERSION mechanism + dockview #341 rationale)
  </read_first>
  <action>
    Create `workspace-config.test.ts` asserting `buildInitialLayout` registers a `deploy` panel (440px within inspector) and a `vcs` panel and registers NO `staging`/`changesets` panels, and that `LAYOUT_VERSION` is incremented (≥2). Create `WorkspaceShell.test.tsx` (mock the dockview api: `fromJSON`/`clear`/`toJSON`/`addPanel`/`onDidActivePanelChange`/group `setSize`) asserting: a saved layout whose version matches is restored via `fromJSON`; a saved layout with a stale/missing version triggers `api.clear()`+`buildInitialLayout` (rebuild) and writes the new version key; AND activating the `deploy` panel fires the handler that calls the right-dock group `setSize({width:~440})` while activating `inspector` sets ~290 (M3). RED — these reference the not-yet-updated config/shell behavior.
  </action>
  <acceptance_criteria>
    - both test files exist and fail against current (pre-swap) behavior
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/workspace/workspace-config.test.ts src/workspace/WorkspaceShell.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>Failing tests pin the panel-swap + version-guard + auto-widen contract.</done>
</task>

<task type="auto">
  <name>Task 2: workspace-config — LAYOUT_VERSION + deploy/vcs panel swap</name>
  <read_first>
    - packages/renderer/src/workspace/workspace-config.ts:20-22 (constants), :96-114 (buildInitialLayout)
    - 04.1-PATTERNS.md §workspace-config.ts (constants + addPanel swap snippet)
    - 04.1-UI-SPEC.md width contracts (deploy panel initialWidth ~440px)
  </read_first>
  <action>
    Add `LAYOUT_VERSION = 2`, `LAYOUT_VERSION_KEY = 'swg-workspace-layout-version'` constants (keep existing `LAYOUT_STORAGE_KEY`). In `buildInitialLayout`, replace the `staging` + `changesets` `addPanel` calls with a single `deploy` panel (`component:'deploy'`, title `Deploy`, `position:{direction:'within',referencePanel:'inspector'}`, `initialWidth:440`) and keep the `vcs` panel within the inspector group. Remove the `staging`/`changesets` panel definitions only — do not touch Assets/Viewport/Inspector/data-pane panels.
  </action>
  <acceptance_criteria>
    - `buildInitialLayout` adds `deploy` (440px) + `vcs`; no `staging`/`changesets` panels
    - `LAYOUT_VERSION >= 2` exported
    - workspace-config.test.ts passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/workspace/workspace-config.test.ts && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Default layout rebuilds to the 008 deploy+vcs arrangement.</done>
</task>

<task type="auto">
  <name>Task 3: WorkspaceShell — register DeployPanel + version-guard onReady + active-panel auto-widen (GREEN)</name>
  <read_first>
    - packages/renderer/src/workspace/WorkspaceShell.tsx:29-41 (panelComponents), :46-74 (onReady)
    - 04.1-PATTERNS.md §WorkspaceShell.tsx (panel registry swap + onReady version-guard + resetLayout fn)
    - 04.1-RESEARCH.md §Pattern 6 (exact onReady logic)
    - 04.1-UI-SPEC.md Surface 6 (deploy ~440px / inspector ~290px width contracts)
  </read_first>
  <action>
    In `WorkspaceShell.tsx`: import `DeployPanel` and register `'deploy': DeployPanel` in `panelComponents`; remove the `'staging': StagingPanel` and `'changesets': ChangesetTimelinePanel` registrations (keep `'vcs': VcsPanel` and the unrelated panels). Replace the `onReady` restoration logic with the version-guard (RESEARCH §Pattern 6): read `LAYOUT_VERSION_KEY`; if a saved layout exists AND its version equals `LAYOUT_VERSION` → `fromJSON` (catch → `api.clear()`+`buildInitialLayout`); else → `buildInitialLayout` and write the current version key. Persist version on `onDidLayoutChange`. M3: register `api.onDidActivePanelChange` so when the active panel is `deploy` the right-dock group is resized to ~440px and when it is `inspector` to ~290px (use the active panel's `group.api.setSize({ width })`; guard against null group). Expose a `resetLayout()` helper using `apiRef` (clears storage + `api.clear()` + rebuild) — it will be surfaced by the plan-08 affordance, but define it here. Make all Task-1 tests GREEN.
  </action>
  <acceptance_criteria>
    - shell registers `deploy`; `staging`/`changesets` registrations removed
    - stale/missing layout version → discard + rebuild (no stranded pane); matching version → restore
    - activating deploy widens right dock to ~440px; activating inspector narrows to ~290px (M3)
    - WorkspaceShell.test.tsx green; `pnpm -w tsc --noEmit` green; full unit suite green (`pnpm test`)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/workspace/WorkspaceShell.test.tsx && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Deploy panel mounts in the real dock; retired-panel layouts auto-migrate; active-panel auto-widen wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| persisted localStorage layout → dockview | a stale/old-version layout string is deserialized at startup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-10 | DoS | stale dockview layout w/ retired panel ids | mitigate | LAYOUT_VERSION guard discards mismatched layouts and rebuilds default (Pitfall 2); fromJSON wrapped in try/catch as a second net |
| T-04.1-11 | Tampering | corrupt JSON in localStorage | mitigate | `JSON.parse` inside try/catch → `api.clear()`+rebuild on throw |
| T-04.1-SC | Tampering | package installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `vitest run src/workspace/workspace-config.test.ts src/workspace/WorkspaceShell.test.tsx` green
- `pnpm test` (full unit) green at wave merge
- `pnpm -w tsc --noEmit` green
</verification>

<success_criteria>
DEPLOY-05 reachable in the real shell as the single Deploy panel; returning users with old layouts auto-migrate without soft-brick (DEPLOY-07 layout-migration portion); the right dock auto-widens on Deploy.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-05-SUMMARY.md` when done
</output>
