---
phase: 04.1-deploy-project-ux
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - packages/renderer/src/services/projectBinding.ts
  - packages/renderer/src/services/treMount.ts
  - packages/renderer/src/panels/tre/TreVfsBrowser.tsx
  - packages/renderer/src/services/workspaceService.ts
  - packages/renderer/src/services/projectBinding.test.ts
autonomous: true
requirements: [PROJ-01]
must_haves:
  truths:
    - "Opening/creating a folder classifies it as client / tre-set / mod-project and persists clientPath+kind to .studio/workspace.json"
    - "A bound client's base TREs auto-mount into treStore on open via the same mountSearchableAsync routine the manual button uses"
    - "A non-client folder binds cleanly with clientPath=null (deploy-to-client disabled downstream)"
    - "The columnar mount-entry decoder is a shared exported util (not a private fn in TreVfsBrowser), imported by both the manual mount path and projectBinding auto-mount"
  artifacts:
    - path: "packages/renderer/src/services/projectBinding.ts"
      provides: "detectFolderKind, read/writeWorkspaceJson, initProject (detect→persist→open→auto-mount)"
      exports: ["detectFolderKind", "initProject", "readWorkspaceJson", "writeWorkspaceJson"]
    - path: "packages/renderer/src/services/treMount.ts"
      provides: "shared mount routine + extracted exported columnar decoder, both from TreVfsBrowser"
      exports: ["mountTrePaths", "decodeMountEntriesColumnar"]
  key_links:
    - from: "packages/renderer/src/services/projectBinding.ts"
      to: "packages/renderer/src/services/treMount.ts"
      via: "auto-mount call"
      pattern: "mountTrePaths"
    - from: "packages/renderer/src/panels/tre/TreVfsBrowser.tsx"
      to: "packages/renderer/src/services/treMount.ts"
      via: "manual mount imports the shared decoder (no private duplicate)"
      pattern: "decodeMountEntriesColumnar"
    - from: "packages/renderer/src/services/projectBinding.ts"
      to: ".studio/workspace.json"
      via: "atomic tmp+rename write"
      pattern: "workspace\\.json"
---

<objective>
First end-to-end vertical slice of PROJ-01: a new `projectBinding` service detects whether an opened folder is a client install, persists the binding to `.studio/workspace.json`, populates `workspaceStore` with the real `clientPath`+`kind` (today hardcoded null), and auto-mounts the bound client's base TREs into the TRE browser by reusing the exact `mountSearchableAsync` routine the manual `Mount Archive…` button uses.

Purpose: the workflow front door — bind once, see the client's VFS immediately.
Output: `projectBinding.ts`, extracted `treMount.ts` (mount routine + columnar decoder), wired `workspaceService` persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-RESEARCH.md
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-VALIDATION.md

<interfaces>
From contracts (plan 01): `WorkspaceInfo.kind`, `WorkspaceBindingMeta`.
Reused engine call sites (verified, RESEARCH §Pattern 2 + §Code Examples):
- TreVfsBrowser.tsx:132-154 — `nativeCore.mountSearchableAsync(paths, priorities)` → `getMountArchives` → `getMountEntriesColumnar` → `treStore.mountComplete(handle, archives, vfsEntries)`
- TreVfsBrowser.tsx:736 — `decodeMountEntriesColumnar(blob): VfsEntry[]` is currently a PRIVATE non-exported local fn (verified). It MUST be extracted into the shared `treMount.ts` and exported so projectBinding's auto-mount can reuse it instead of duplicating the columnar decode.
- clientLocator.ts:180 `detectClients()`, :95-138 `scanSharedFile()`, :308 `_detectTreVersion()`
- workspaceService.ts:164-176 `openComplete` call shape; :169 + :318 `clientPath:null` (the bug to fix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — projectBinding unit tests (detect / persist / auto-mount)</name>
  <read_first>
    - 04.1-VALIDATION.md Requirement→Test Map (PROJ-01 rows) — these are the exact behaviors
    - packages/renderer/test/fixtures/fakeClientDir.ts (plan 01 fixture)
    - packages/renderer/src/panels/deploy/ChangesetTimelinePanel.test.tsx:1-64 (vi.mock hoist + store-reset patterns)
    - 04.1-PATTERNS.md §Wave 0 Test Files (fs mocking + fixture patterns)
  </read_first>
  <action>
    Create `packages/renderer/src/services/projectBinding.test.ts`. Mock `@swg/native-core` (`mountSearchableAsync`/`getMountArchives`/`getMountEntriesColumnar` as `vi.fn()`), import the fixture. Write failing tests asserting: (1) `detectFolderKind` returns `'client'` for a fixture dir with cfg+Live/*.tre, `'tre-set'` for a dir with only `.tre` files, `'mod-project'` for an empty dir; (2) `writeWorkspaceJson`→`readWorkspaceJson` round-trips `{kind, clientPath, cfgPath, treDir}` atomically; (3) `initProject(clientFolder)` calls the mocked `mountSearchableAsync` with the fixture's base TRE paths and drives `treStore.mountComplete`; (4) `initProject` on a non-client folder persists `clientPath:null`, `kind:'mod-project'` and does NOT call `mountSearchableAsync`. Tests reference not-yet-existing `projectBinding` exports — they MUST fail to compile/run (RED).
  </action>
  <acceptance_criteria>
    - File exists; running it fails because `projectBinding` exports are missing (RED state confirmed)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/projectBinding.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Tests authored and failing for the right reason (missing implementation).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared TRE mount routine + columnar decoder into treMount.ts</name>
  <read_first>
    - packages/renderer/src/panels/tre/TreVfsBrowser.tsx:104-160 (manual mount flow to extract; the columnar decode + mountComplete at :132-154)
    - packages/renderer/src/panels/tre/TreVfsBrowser.tsx:736 (the PRIVATE `decodeMountEntriesColumnar` local fn — must be extracted + exported, not left private)
    - packages/renderer/src/state/treStore.ts (mountComplete signature; VfsEntry shape)
    - 04.1-PATTERNS.md §projectBinding.ts → "Auto-mount TRE pattern"
  </read_first>
  <behavior>
    - `mountTrePaths(filePaths: string[], priorities: number[])` calls `mountSearchableAsync`, maps `getMountArchives`, decodes `getMountEntriesColumnar` via the shared `decodeMountEntriesColumnar`, calls `treStore.mountComplete(handle, archives, vfsEntries)`, returns the handle.
    - `decodeMountEntriesColumnar(blob): VfsEntry[]` is exported from `treMount.ts` and produces identical output to the prior private TreVfsBrowser version.
    - TreVfsBrowser's manual `Mount Archive…` button produces identical store state after refactor (no behavior change for the manual path).
  </behavior>
  <action>
    Create `packages/renderer/src/services/treMount.ts` exporting `mountTrePaths(filePaths, priorities)`. Move the mount/decode/store-populate block out of `TreVfsBrowser.tsx:132-154` into it (require the `@swg/native-core` addon via the existing Path-B `require` pattern). CRITICAL (H3 fix): also MOVE the private `decodeMountEntriesColumnar` from `TreVfsBrowser.tsx:736` into `treMount.ts` and EXPORT it (carry the `VfsEntry` type + the columnar header offsets verbatim — entryCount/nameDataOffset/archPathDataOffset/arrayOffset and the per-entry typed-array layout at :740-779). Update `TreVfsBrowser` to import both `mountTrePaths` and `decodeMountEntriesColumnar` from `treMount.ts` (delete the now-duplicate local fn) so there is ONE owner of the mount routine AND the decoder — do NOT duplicate the decoder silently. projectBinding (Task 3) imports the same exported `decodeMountEntriesColumnar`/`mountTrePaths`. Do NOT change priority-assignment behavior of the manual flow.
  </action>
  <acceptance_criteria>
    - `mountTrePaths` AND `decodeMountEntriesColumnar` exported from `treMount.ts` and called by both TreVfsBrowser (manual) and (next task) projectBinding
    - the private `decodeMountEntriesColumnar` no longer exists in TreVfsBrowser.tsx (grep returns the import, not a local `function decodeMountEntriesColumnar`)
    - manual mount path unchanged in behavior; `pnpm -w tsc --noEmit` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Mount routine + columnar decoder centralized; TreVfsBrowser delegates to the shared util.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement projectBinding + wire workspaceService persistence (GREEN)</name>
  <read_first>
    - packages/renderer/src/services/clientLocator.ts:180-264 (detection probe order to reuse — read-only here)
    - packages/renderer/src/services/workspaceService.ts:41-43 (getStudioDir), :164-176 (openComplete), :169 + :318 (clientPath:null)
    - packages/renderer/src/services/treMount.ts (the shared `mountTrePaths` + exported `decodeMountEntriesColumnar` from Task 2)
    - 04.1-PATTERNS.md §projectBinding.ts (detection/persistence/store-population/error patterns), §Atomic file write pattern
    - 04.1-RESEARCH.md §Pattern 2 (base-TRE path derivation: enumerate resolved TRE dir, ascending priorities like the manual flow — Open Question 3 recommendation = TRE-dir order is sufficient)
  </read_first>
  <behavior>
    - `detectFolderKind(folder)` → 'client' | 'tre-set' | 'mod-project' (matches RED tests).
    - `initProject(folder)`: detect kind → resolve cfgPath/treDir → write `.studio/workspace.json` (atomic tmp+rename) → call `workspaceService.openWorkspace` so the store's `clientPath`/`kind` are populated → if client, enumerate base TRE paths from the resolved TRE dir and call `mountTrePaths` with ascending priorities. Auto-mount is UNCONDITIONAL for a client-kind binding (M5: the wizard "seed assets" choice in plan 04 does NOT gate mounting — see plan 04 Task 2).
    - Non-client folder: persists clientPath:null, kind:'mod-project', no mount.
  </behavior>
  <action>
    Implement `projectBinding.ts`: `detectFolderKind`, `readWorkspaceJson(studioDir)`/`writeWorkspaceJson(studioDir, meta)` (atomic tmp+rename, BOM-free utf8 per the Shared "Atomic file write pattern"), and `initProject(folder)` orchestrating detect→persist→open→auto-mount via `mountTrePaths` (importing it + the shared `decodeMountEntriesColumnar` from `treMount.ts`, NOT re-implementing the decode). Derive base TRE paths by reading the resolved TRE dir (Live/ or root) and assigning ascending priorities (TRE-dir order — exact `searchTree` parity is deploy-only). For a client binding, mounting is unconditional (M5). Use the error pattern from workspaceService.ts:172-177. Then edit `workspaceService.ts`: in `openWorkspace` and `createWorkspace`, replace the hardcoded `clientPath:null` (lines 169, 318) by reading `WorkspaceBindingMeta` and passing the real `clientPath`+`kind` into `openComplete`. Use inline `require('electron')` casts for any IPC (centralized later in plan 10 per D-04 Pattern 8). Do NOT relocate `getStudioDir` yet (that is plan 06 / D-06).
  </action>
  <acceptance_criteria>
    - All `projectBinding.test.ts` tests from Task 1 pass (GREEN)
    - `workspaceStore.clientPath` is the real bound path (not null) after `initProject` on a client folder
    - `vitest run src/services/projectBinding.test.ts` green; `pnpm -w tsc --noEmit` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/services/projectBinding.test.ts && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>projectBinding GREEN; workspaceService persists + populates real binding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browsed folder → renderer fs | user-picked path is read (cfg, .tre enumeration) and a `.studio/workspace.json` is written under it |
| renderer → native addon | base TRE paths cross into `mountSearchableAsync` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-02 | Tampering | `writeWorkspaceJson` target | mitigate | Write only under the resolved `studioDir`; atomic tmp+rename; never write into the client install (D-07 — workspace.json lives in `.studio`) |
| T-04.1-03 | Tampering | base-TRE path enumeration | mitigate | Enumerate only `.tre` files from the detected TRE dir; do not follow symlinks outside it |
| T-04.1-04 | DoS | malformed cfg / unreadable dir | mitigate | Reuse workspaceService error pattern (catch→store.openError→rethrow); detection never throws unguarded |
| T-04.1-SC | Tampering | package installs | accept | No new packages this plan (RESEARCH audit: none) |
</threat_model>

<verification>
- `vitest run src/services/projectBinding.test.ts` green
- `pnpm -w tsc --noEmit` green
- Manual mount path behavior unchanged (existing TRE tests still green): `pnpm --filter @swg/renderer test`
- grep confirms `decodeMountEntriesColumnar` is exported from `treMount.ts` and not redefined privately in TreVfsBrowser
</verification>

<success_criteria>
PROJ-01 binding + auto-mount works at the service layer: bind a client → `clientPath`/`kind` persisted → base TREs mounted into `treStore`. Non-client folders bind with clientPath:null. The columnar decoder is shared, not duplicated.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-02-SUMMARY.md` when done
</output>
