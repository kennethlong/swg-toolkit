---
phase: 04.1-deploy-project-ux
plan: 08
type: execute
wave: 7
depends_on: ["04.1-02", "04.1-03", "04.1-04", "04.1-05"]
files_modified:
  - packages/renderer/src/panels/deploy/StagingPanelBody.tsx
  - packages/renderer/src/panels/tre/TreVfsBrowser.tsx
  - packages/renderer/src/workspace/WorkspaceShell.tsx
  - packages/renderer/src/state/stagingStore.ts
  - packages/renderer/src/services/packPatch.ts
  - packages/renderer/src/panels/deploy/StagingPanelBody.test.tsx
autonomous: true
requirements: [DEPLOY-07]
must_haves:
  truths:
    - "Right-click a TRE-browser entry → Extract to staging adds a staged entry whose virtual path is derived from the VFS entry (no manual virtual-path prompt)"
    - "A user can reset the dock layout and reopen a closed panel from a discoverable affordance without restarting the app"
    - "The staging empty-state copy describes the real browser-driven flow"
    - "stagingStore.addEntry AND packPatch validate virtualPath through the shared isVirtualPathSafe (drive-letter-aware) — no weaker per-site check remains"
  artifacts:
    - path: "packages/renderer/src/panels/tre/TreVfsBrowser.tsx"
      provides: "Extract→Add context-menu action into stagingStore"
      contains: "stagingStore"
    - path: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      provides: "Reset layout + reopen-closed-panel affordance wired to resetLayout()"
      contains: "resetLayout"
  key_links:
    - from: "packages/renderer/src/panels/tre/TreVfsBrowser.tsx"
      to: "packages/renderer/src/state/stagingStore.ts"
      via: "Extract→Add derives vpath from entry"
      pattern: "addEntry"
    - from: "packages/renderer/src/state/stagingStore.ts"
      to: "packages/renderer/src/services/pathSafety.ts"
      via: "addEntry validates virtualPath"
      pattern: "isVirtualPathSafe"
---

<objective>
Close the staging + layout robustness gaps (DEPLOY-07): add Extract→Add directly from the TRE browser so a staged entry's virtual path is derived from the browsed entry (retiring the manual `VirtualPathModal` prompt for that flow), surface a discoverable Reset-layout / reopen-closed-panel affordance wired to the `resetLayout()` defined in plan 05 (so a closed Deploy/Inspect panel can be reopened and a stale layout reset without restarting), and complete the path-safety unification (M1) by routing `stagingStore.addEntry` and `packPatch` through the shared `isVirtualPathSafe`.

Purpose: no manual path typing; no soft-brick; one drive-letter-aware path validator everywhere.
Output: Extract→Add wiring, reset/reopen affordance, corrected empty-state copy, unified path-safety at the add + pack boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-PATTERNS.md
@.planning/phases/04.1-deploy-project-ux/04.1-UI-SPEC.md
@.planning/phases/04.1-deploy-project-ux/04.1-VALIDATION.md

<interfaces>
From plan 03: `StagingPanelBody` + the shared `packages/renderer/src/services/pathSafety.ts` exporting `isVirtualPathSafe`. From plan 04: the `TreVfsBrowser.tsx` entry rows this plan extends with an Extract action. From plan 05: `WorkspaceShell.resetLayout()` + LAYOUT_VERSION constants. (Plans 02 and 04 both also touch `TreVfsBrowser.tsx`, hence the depends_on edges — this plan layers on their final version.)
`stagingStore.addEntry` (state/stagingStore.ts) is the existing add path; `treStore.VfsEntry` carries the entry path used to derive the virtual path.
Path-safety unification (M1): `packPatch.ts:80-89` currently has a WEAKER inline guard (checks `..`, leading `/`/`\` but MISSES drive-letter `C:\`); `stagingStore.addEntry` has no guard. Both must call the shared `isVirtualPathSafe` from `pathSafety.ts` (the strongest validator, extracted in plan 03). The seal-time guard lives in plan 06.
UI-SPEC Surface 4 (stage-from-browser, no manual path) + Surface 5 (reset/reopen) + Copywriting (staging empty body corrected).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Extract→Add derives virtual path (no prompt) + shared path-safety</name>
  <read_first>
    - 04.1-VALIDATION.md DEPLOY-07 row (Extract→Add derives the virtual path from the VFS entry, no manual prompt)
    - packages/renderer/src/panels/deploy/StagingPanelBody.test.tsx (plan-03 file to extend)
    - packages/renderer/src/state/stagingStore.ts (addEntry signature)
    - packages/renderer/src/services/pathSafety.ts (shared isVirtualPathSafe) + packPatch.ts:80-89 (weak guard to replace)
  </read_first>
  <action>
    Extend `StagingPanelBody.test.tsx` (or add a focused test) asserting that an Extract→Add action invoked with a VFS entry produces a `stagingStore` entry whose `virtualPath` equals the entry's derived path (the VFS path), WITHOUT opening the `VirtualPathModal` prompt, and that an unsafe derived path (`..` OR a drive-letter `C:\`) is rejected by the shared `isVirtualPathSafe`. Add a focused test (stagingStore and/or packPatch) asserting `addEntry` rejects an unsafe virtualPath and `packPatch` rejects a `C:\…` virtualPath (which the old weak guard let through). Mock `stagingStore.addEntry` where appropriate. RED.
  </action>
  <acceptance_criteria>
    - new test(s) exist and fail (Extract→Add path-derivation + shared validator not yet wired)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/panels/deploy/StagingPanelBody.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>Browser-driven staging + unified path-safety pinned by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extract→Add from TRE browser + unify path-safety at add/pack + corrected empty-state copy</name>
  <read_first>
    - packages/renderer/src/panels/tre/TreVfsBrowser.tsx (entry rows + any existing context menu / Extract action)
    - packages/renderer/src/panels/deploy/StagingPanelBody.tsx (staging add path + empty state; imports isVirtualPathSafe from pathSafety)
    - packages/renderer/src/state/stagingStore.ts (addEntry — add the shared guard)
    - packages/renderer/src/services/packPatch.ts:80-89 (replace the weak inline guard with the shared validator)
    - packages/renderer/src/services/pathSafety.ts (shared isVirtualPathSafe)
    - 04.1-UI-SPEC.md Surface 4 + Copywriting Contract (`Right-click a file in the TRE browser → Extract to staging, or drop a replacement file here.`)
    - 04.1-PATTERNS.md §Path traversal guard
  </read_first>
  <action>
    Add an `Extract to staging` action to the TRE-browser entry context menu (and/or row hover) in `TreVfsBrowser.tsx` that derives the virtual path from the selected VFS entry's path, runs it through the shared `isVirtualPathSafe` (from `pathSafety.ts`), and calls `stagingStore.addEntry` directly — no `VirtualPathModal` prompt. M1: add the shared `isVirtualPathSafe` check inside `stagingStore.addEntry` (reject unsafe before storing) and REPLACE the weaker inline guard in `packPatch.ts:80-89` with a call to the shared validator (so a drive-letter `C:\…` is now rejected at pack time too). Update `StagingPanelBody.tsx`'s empty-state heading/body to the corrected copy (`Nothing staged` / `Right-click a file in the TRE browser → Extract to staging, or drop a replacement file here.`). Keep drag-drop-replacement working. Reject unsafe derived paths with the existing invalid-path inline message. Make Task-1 tests GREEN.
  </action>
  <acceptance_criteria>
    - Extract→Add adds a staged entry with the derived virtual path and never opens the manual prompt
    - `stagingStore.addEntry` and `packPatch` both reject a drive-letter `C:\…` virtualPath via the shared validator (no weaker guard remains in packPatch)
    - empty-state copy matches the Copywriting Contract
    - StagingPanelBody.test.tsx green
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/panels/deploy/StagingPanelBody.test.tsx && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Stage-from-TRE works with no manual path entry; path-safety unified at add + pack.</done>
</task>

<task type="auto">
  <name>Task 3: Reset-layout + reopen-closed-panel affordance</name>
  <read_first>
    - packages/renderer/src/workspace/WorkspaceShell.tsx (resetLayout() from plan 05 + apiRef + panel registry)
    - 04.1-UI-SPEC.md Surface 5 (View/window menu home + optional status-bar control; reopen-closed-panel list)
    - 04.1-RESEARCH.md §Pattern 6 (api.clear() + buildInitialLayout; reopen via addPanel)
  </read_first>
  <action>
    Surface a discoverable `Reset layout` control (View/window menu and/or a status-bar affordance per Surface 5) that calls the `resetLayout()` from plan 05. Add a `Reopen panel` submenu listing closed panels (compare registered panel ids vs `api.panels`) that re-adds a chosen panel via `api.addPanel` into the appropriate group (deploy/vcs within inspector; assets/viewport/data as applicable). Do not change the version-guard logic or the active-panel auto-widen handler (owned by plan 05). Keep it keyboard-reachable with a visible focus ring (Accessibility).
  </action>
  <acceptance_criteria>
    - Reset layout clears persisted layout + rebuilds default (delegates to resetLayout); reopen-panel re-adds a closed panel without app restart
    - affordance is discoverable (menu and/or status bar), not buried
    - `pnpm test` full unit green
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @swg/renderer test -- --run src/workspace/WorkspaceShell.test.tsx && pnpm -w tsc --noEmit</automated>
  </verify>
  <done>Closed panels reopenable + layout resettable without restart (DEPLOY-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| VFS entry path → derived virtual path | a browsed entry's path becomes a staged virtual path that is later packed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-19 | Tampering | Extract→Add derived virtual path | mitigate | Run derived path through the shared `isVirtualPathSafe` (pathSafety.ts) — reject `..`/absolute/drive-letter before addEntry; same validator now also guards stagingStore.addEntry + packPatch |
| T-04.1-20 | DoS | reopen-panel adds duplicate/invalid panel | mitigate | Reopen list only offers registered-but-closed ids; addPanel guarded against existing ids |
| T-04.1-SC | Tampering | package installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `vitest run src/panels/deploy/StagingPanelBody.test.tsx src/workspace/WorkspaceShell.test.tsx` green
- `pnpm test` full unit green
- `pnpm -w tsc --noEmit` green
- grep confirms packPatch no longer has its own inline `..`/`/` guard (uses shared isVirtualPathSafe)
</verification>

<success_criteria>
DEPLOY-07 met: stage-from-TRE with no manual path; reset-layout + reopen-closed-panel without restart; path-safety unified across add + pack (+ seal in plan 06).
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-08-SUMMARY.md` when done
</output>
