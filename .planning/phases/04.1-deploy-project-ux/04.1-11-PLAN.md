---
phase: 04.1-deploy-project-ux
plan: 11
type: execute
wave: 9
depends_on: ["04.1-10"]
files_modified: []
autonomous: false
requirements: [PROJ-01, DEPLOY-05, DEPLOY-06, DEPLOY-07]
must_haves:
  truths:
    - "The full onboarding→deploy→reset flow works in real Electron against real client bytes (not jsdom)"
    - "Deploy with the absolute-path override default loads the mod in-game; Reset restores the client BYTE-PRISTINE (root cfg whole-file, .include gone, toolkit cfg + patch removed)"
    - "Deploy Baseline performs reset-to-stock (not packPatch)"
    - "Hardlink full-shadow loads from the shadow and Reset leaves the original untouched"
  artifacts: []
  key_links: []
---

<objective>
Phase gate: a real-Electron, in-client UAT of the complete deploy/onboarding flow. jsdom unit tests cannot exercise IPC dialogs, dockview, NTFS hardlinks, the native TRE mount, or in-game cfg loading — and three real-Electron bugs slipped past jsdom in Phase 4 (memory `feedback-executor-integration-blind-spot`, `design-test-hook-points-for-harness`). This checkpoint verifies the phase end-to-end before `/gsd:verify-work`.

Purpose: prove the slices actually work together against real client bytes, with special attention to byte-pristine Reset and Deploy-Baseline-as-reset.
Output: human-verified UAT sign-off (or a gap list to route to `/gsd:plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-deploy-project-ux/04.1-VALIDATION.md
@.planning/phases/04.1-deploy-project-ux/04.1-UI-SPEC.md
@.planning/phases/04.1-deploy-project-ux/04.1-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: In-client real-Electron UAT — onboarding → deploy → reset</name>
  <what-built>
    The full Phase 04.1 surface: project↔client binding + auto-mount (PROJ-01), the one combined Deploy panel in the Inspect|Deploy group (DEPLOY-05), the lazy/virtual reversible shadow model with byte-pristine Reset (DEPLOY-06), and stage-from-TRE + reset-layout/reopen (DEPLOY-07).
  </what-built>
  <how-to-verify>
    Run the app in real Electron (`pnpm dev` or the packaged build — NOT jsdom). On BOTH SWG Infinity (swgemu.cfg, Live/, prio 60) and SWGEmu (swgemu.cfg, root, prio 27).
    BEFORE deploying: capture a byte hash of the root client cfg (e.g. swgemu.cfg) so step 5/9 can prove byte-pristine restore.
    1. First-run: the Assets welcome lists detected clients with ✓ ready / ✗ not found pills. Create a project via ＋ Project → wizard (name/location → bind client → optional local-server CAPTURE-ONLY (enter `127.0.0.1:44463`, confirm it is stored, NOT pushed) → seed assets). Confirm the bound-client chip `⛁ <client> · EERT<version>` appears and the client's base TREs auto-mount into the TRE browser (override pips render the project patch shadowing base). Confirm "start empty" still auto-mounts the TREs (M5).
    2. Combined Deploy tab: open the Inspect|Deploy group — confirm ONE Deploy panel (working changes over version history, single Baseline root node, ▸-expandable per-version deltas, sticky `Deploy v…` CTA), dock auto-widens to ~440px. No separate Staging/Changesets tabs. Confirm the Baseline node renders exactly once (not duplicated).
    3. Stage-from-TRE: right-click a browser entry → Extract to staging; confirm it lands with the derived virtual path and NO manual path prompt. Save version (seals).
    4. Deploy (absolute-path default): Deploy is enabled by default (bound client auto-selected); deploy and confirm the mod loads in-game. Verify the client install has ZERO toolkit files except the cfg edit, the cfg snapshot lives in the whitespace-free `.studio`, and the root cfg `searchTree`/`.include` chain has no space and a single `[SharedFile]` header.
    5. Reset deployment (the done-phase Reset button): confirm the root client cfg is restored BYTE-PRISTINE (hash equals the step-0 capture), the appended `.include "swgtoolkit.cfg"` line is GONE, any maxSearchPriority bump is reverted, the toolkit `swgtoolkit.cfg` + deployed patch .tre are removed, the mod no longer loads, and changesets are kept. Confirm the Reset success message matches what happened (no false "restored from backup" when it was line-surgery).
    6. Deploy Baseline = reset to stock: select the Baseline version and click `Deploy Baseline…`; confirm it performs reset-to-stock (restoreCfg + cleanup), NOT a packPatch, and does not error on the empty flatten.
    7. Cross-session Reset: deploy, CLOSE the Deploy dialog, REOPEN it, and confirm Reset is still available (record loaded from the manifest) and still restores byte-pristine.
    8. Hardlink full-shadow (opt-in, same-volume client): deploy with the hardlink-shadow model; confirm the client loads from the shadow and Reset deletes the shadow leaving the original untouched (and the EXDEV/cross-volume caution appears if applicable). Confirm a mid-deploy failure (e.g. simulate by removing write perms) auto-rolls-back the cfg.
    9. Layout robustness: close the Deploy panel and reopen it via the affordance; trigger Reset layout — confirm the 008 default rebuilds with no restart and no soft-brick; confirm a pre-existing old layout (retired panel ids) auto-migrates on launch.
    10. Non-client project: create a project with No, non-client → confirm deploy-to-client is disabled with the hint copy.
    11. Legacy-studio migration: open an existing Phase-4 project that had `<projectFolder>/.studio` → confirm its changesets are present (migrated non-destructively to the new whitespace-free studio path).
  </how-to-verify>
  <acceptance_criteria>
    - All 5 ROADMAP success criteria observed in real Electron on both clients (or specific gaps recorded)
    - Reset restores the root client cfg BYTE-PRISTINE (hash match) with the `.include` removed (verify on disk)
    - Deploy Baseline performs reset-to-stock (no packPatch, no empty-flatten error)
    - Cross-session Reset works after dialog reopen; mid-deploy failure auto-rolls-back
    - No toolkit files left in the client install after Reset
  </acceptance_criteria>
  <resume-signal>Type "approved" if all criteria pass, or describe each failing step (routes to /gsd:plan-phase --gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| toolkit → live client install | the UAT exercises the only authorized mutation (cfg edit) against a real client |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-25 | Tampering | irreversible client cfg damage | mitigate | UAT explicitly verifies whole-file ROOT-cfg snapshot restore (D-07) reproduces the original cfg byte-for-byte with the `.include` removed; client stays pristine after Reset |
| T-04.1-26 | DoS | shadow/hardlink leaves stale state | mitigate | UAT verifies Reset deletes the shadow and removes the deployed override; legacy copies cleaned |
| T-04.1-SC | Tampering | package installs | accept | No new packages introduced across the phase (RESEARCH audit: none) |
</threat_model>

<verification>
- Real-Electron observation of all 5 ROADMAP success criteria on SWG Infinity + SWGEmu
- On-disk confirmation: client pristine except cfg edit while deployed; whitespace-free `.studio`; single `[SharedFile]`; byte-exact cfg restore + `.include` removed on Reset; Deploy Baseline = reset-to-stock
</verification>

<success_criteria>
Phase 04.1 deploy/onboarding flow proven end-to-end in real Electron; Reset is byte-pristine and Deploy Baseline = reset-to-stock; any gaps recorded for `/gsd:plan-phase --gaps`.
</success_criteria>

<output>
Create `.planning/phases/04.1-deploy-project-ux/04.1-11-SUMMARY.md` when done
</output>
