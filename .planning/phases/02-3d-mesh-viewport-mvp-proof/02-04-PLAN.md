---
phase: 02-3d-mesh-viewport-mvp-proof
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
files_modified:
  - packages/native-core/modules/core/formats/Animation.h
  - packages/native-core/modules/core/formats/Animation.cpp
  - packages/native-core/modules/core/math/CompressedQuaternion.h
  - packages/native-core/modules/core/math/CompressedQuaternion.cpp
  - packages/native-core/modules/core/CMakeLists.txt
  - packages/native-core/src/anim_binding.cpp
  - packages/native-core/src/addon.cpp
  - packages/harness/test/mesh-roundtrip.test.ts
  - packages/renderer/src/panels/viewport/AnimationTransport.tsx
  - packages/renderer/src/panels/viewport/SkinnedMeshView.tsx
  - packages/renderer/src/state/viewportStore.ts
autonomous: false
requirements:
  - VIEW-03

must_haves:
  truths:
    - "parseAnimation() decodes both CKAT 0001 (int16-width, compressed quaternion, sparse per-channel) and KFAT 0003 (int32-width, raw quaternion, sparse) formats"
    - "CKAT compressed-quaternion decode is a VERBATIM PORT of CompressedQuaternion::install()+doExpand() — the 255-entry s_formatData table built from formatId|baseIndex; w=sqrt(max(0,1-(x²+y²+z²))) clamp adopted"
    - "CKAT and KFAT INFO/XFIN/QCHN/SROT byte tables are CARRIED SEPARATELY (CKAT int16 widths; KFAT int32 widths; SROT differs — CKAT = uint8 x/y/z format bytes FIRST then uint32 packed; KFAT = raw floatQuaternion)"
    - "Keys are SPARSE (frame-indexed). The renderer brackets the two nearest keys (binary search) — NO dense frameCount×jointCount×stride buffer is built"
    - "KFAT 0002 (legacy Euler) is detected and returns AnimationVariant='KFAT-0002-unsupported' with a clear error — not mis-rendered"
    - "The animation clock is driven from a ref + throttled UI sync — setTransportState is NOT called every frame"
    - "Playback in useFrame uses flat Float32Array key data + module-scope scratch Quaternion/Vector3 — zero new object allocation per frame (D-09)"
    - ".ans CORE-05 byte-exact round-trip passes for a real CKAT 0001 asset and a real KFAT 0003 asset; the typed parser does NOT apply the loader's on-load keyframe decimation (test counts expect ON-DISK counts)"
    - "ansPickerOptions is populated from the skeleton→.ans mapping (SMAT LATX / animation-table) so the picker lists real animations"
    - "User-verify: animation plays visually correctly on a real .sat + .ans without GC hitching"
  artifacts:
    - path: "packages/native-core/modules/core/math/CompressedQuaternion.h"
      provides: "CompressedQuaternion::install() (255-entry s_formatData) + doExpand() — verbatim port"
    - path: "packages/native-core/modules/core/formats/Animation.h"
      provides: "parseAnimation() returning AnimationResult with CKAT/KFAT sparse keyframe data"
    - path: "packages/renderer/src/panels/viewport/AnimationTransport.tsx"
      provides: "Full transport UX: .ans picker (populated from ansPickerOptions), scrubber, play/pause/loop/speed, skeleton-helper toggle (D-08)"
    - path: "packages/renderer/src/panels/viewport/SkinnedMeshView.tsx"
      provides: "useFrame sparse-key sampler: binary-search bracket + slerp/lerp into module scratch (VIEW-03 zero-GC); ref clock"
  key_links:
    - from: "AnimationTransport.tsx"
      to: "viewportStore.transportState"
      via: "transport buttons set discrete state; the per-frame clock lives in a ref in SkinnedMeshView and flushes currentFrame to the store THROTTLED (not per frame)"
      pattern: "setTransportState"
    - from: "SkinnedMeshView.tsx useFrame"
      to: "AnimationParseResult sparse key data"
      via: "binary-search bracket two nearest keys per joint; slerp into _scratchQuat; copy to bone.quaternion — zero-alloc"
      pattern: "_scratchQuat.*slerp|slerpQuaternions"
    - from: "packages/native-core/modules/core/math/CompressedQuaternion.cpp"
      to: "swg-client-v2 CompressedQuaternion.cpp:82-122,156-228,370-419"
      via: "verbatim port of install() + doExpand()"
      pattern: "s_formatData|install"
---

<objective>
Land the `.ans` animation parser (CKAT 0001 compressed + KFAT 0003 uncompressed; KFAT 0002 detected and declined) with byte tables and a compressed-quaternion decoder matching ground truth EXACTLY, register CORE-05 fixtures, and wire the full animation transport UX (D-08) with a sparse-key, ref-clock, zero-GC-per-frame sampler (D-09).

Purpose: Delivers VIEW-03 — the skeleton-preview + animation-playback proof. The CKAT compressed-quaternion decoder is the critical-path item (synthesis §1.6: real assets are usually CKAT). This is the phase's highest-risk plan: the cross-AI review found the original byte tables and the quantization formula FALSIFIED. This plan rebuilds them from the corrected synthesis + a verbatim port of the client decoder.

Output: CompressedQuaternion.h/cpp (verbatim install()+doExpand() port); Animation.h/cpp parser (two separate CKAT/KFAT byte tables, sparse channels); anim_binding.cpp; CORE-05 fixtures for CKAT + KFAT 0003; AnimationTransport React component; SkinnedMeshView ref-clock sparse-key sampler.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-04-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Code\SWG-Toolkit\.planning\PROJECT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-CONTEXT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-RESEARCH.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-SYNTHESIS.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-UI-SPEC.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-PATTERNS.md

<interfaces>
<!-- From 02-01 output (contracts/animation.ts) -->
<!-- AnimationVariant: 'KFAT-0003' | 'CKAT-0001' | 'KFAT-0002-unsupported' -->
<!-- AnimationJoint: { name; hasAnimatedRotation; rotationChannelIndex; translationMask; translationChannelIndex:[x,y,z] } -->
<!-- AnimationParseResult: { variant, fps, frameCount, joints:AnimationJoint[], roundTrip } -->
<!-- Binary keyframe buffers cross as ArrayBuffer (binary path) — SPARSE channel storage -->

<!-- From 02-02 output (viewportStore.ts) -->
<!-- transportState: { playing, currentFrame, totalFrames, speed, loop }; setTransportState(partial); parsedSkeleton -->
<!-- New: setParsedAnimation(anim|null), clearAnimation(), setSkeletonHelperVisible(v), ansPickerOptions:string[], setAnsPickerOptions(paths[]) -->

<!-- CKAT/KFAT byte layout — LOCKED per synthesis §1.6 (corrected 2026-06-23). Keys are SPARSE (frame-indexed). -->
<!-- On-disk quaternion order (w,x,y,z); THREE.Quaternion constructor (x,y,z,w) — reorder ONCE at parse/prebuild, not per-frame. -->

<!-- Compressed-quaternion decode: PORT CompressedQuaternion::install()+doExpand() VERBATIM. -->
<!--   packed uint32 = (x11<<21)|(y11<<10)|z10; x bits21-31 (11-bit, mask 0x3FF, sign 0x400); y bits10-20 (11-bit); z bits0-9 (10-bit, mask 0x1FF, sign 0x200). -->
<!--   format byte encodes BOTH precision level (formatId, high bits) AND base index (low bits via baseIndexMask): format = formatId | baseIndex. -->
<!--   s_formatData[0..254] built once; per level shift 0..6: baseSeparation=2/(2^shift+1); halfRange=baseSeparation; expandFactor11=halfRange/1023; expandFactor10=halfRange/511. -->
<!--   formatId per level {0:0xFE,1:0xFC,2:0xF8,3:0xF0,4:0xE0,5:0xC0,6:0x80}; baseCount=2^shift; for i in 0..baseCount-1: formatIndex=formatId|i; baseValue=-1+(i+1)*baseSeparation. -->
<!--   w = sqrt(max(0, 1-(x²+y²+z²)))  <- adopt the clamp (C++ uses bare sqrt at :379). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verbatim CompressedQuaternion port + Animation parser (CKAT 0001 + KFAT 0003) + CORE-05 fixtures</name>
  <files>
    packages/native-core/modules/core/math/CompressedQuaternion.h,
    packages/native-core/modules/core/math/CompressedQuaternion.cpp,
    packages/native-core/modules/core/formats/Animation.h,
    packages/native-core/modules/core/formats/Animation.cpp,
    packages/native-core/modules/core/CMakeLists.txt,
    packages/native-core/src/anim_binding.cpp,
    packages/native-core/src/addon.cpp,
    packages/harness/test/mesh-roundtrip.test.ts
  </files>
  <behavior>
    - CompressedQuaternion::install() builds s_formatData with 255 entries; for every used format byte the entry has a baseValue in [-1,1] and a precisionIndex in 0..6
    - doExpand(packed, xFmt, yFmt, zFmt) returns a quaternion whose magnitude is within 0.001 of 1.0 for real CKAT key data; w >= 0 (clamp applied)
    - parseAnimation on a real CKAT 0001 .ans returns variant='CKAT-0001', fps>0, frameCount>0, joints.length>0; QCHN format bytes read ONCE per channel (not per key)
    - parseAnimation on a real KFAT 0003 .ans returns variant='KFAT-0003', frameCount>0; QCHN keys are int32 frame + floatQuaternion (4×float32)
    - parseAnimation on a KFAT 0002 .ans returns variant='KFAT-0002-unsupported' without throwing
    - serializeIff(parseIff(.ans CKAT bytes)) === bytes byte-identical (SC-5); serializeIff(parseIff(.ans KFAT 0003 bytes)) === bytes (SC-5)
    - The typed parser does NOT decimate keys on load; key counts in AnimationResult match the ON-DISK counts
    - Keyframe ArrayBuffer is SPARSE (per channel: keyCount + frame indices + quaternion/value data); NOT a dense frameCount×jointCount×stride buffer
  </behavior>
  <read_first>
    packages/native-core/modules/core/formats/Mesh.h — file-header/namespace/error/function shape to follow
    packages/native-core/src/mesh_binding.cpp — extractBytes helper + ParseMesh shape to extend
    packages/native-core/src/addon.cpp — Init() block to extend
    packages/harness/test/mesh-roundtrip.test.ts — current test file to extend
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.6 (LOCKED, corrected 2026-06-23): KFAT-0003 INFO (fps + 6×int32: frameCount,transformInfoCount,rotationChannelCount,staticRotationCount,translationChannelCount,staticTranslationCount); KFAT XFIN (name+int8 hasAnimatedRotations+int32 rotationChannelIndex+uint32 translationMask+3×int32 trans indices); KFAT QCHN (int32 keyCount; per key int32 frame + floatQuaternion); KFAT SROT (floatQuaternion ×). CKAT-0001 INFO (fps + 6×int16, same field names); CKAT XFIN (name+int8+int16 rotIdx+uint8 transMask+3×int16); CKAT QCHN (int16 keyCount; uint8 xFmt,yFmt,zFmt ONCE per channel; per key int16 frame + uint32 compressedRotation); CKAT SROT (uint32 packed + uint8 x,y,z fmt). The decode/install() table spec is in §1.6 and the <interfaces> block.
    ../swg-client-v2/src/engine/shared/library/sharedMath/src/shared/CompressedQuaternion.cpp lines 82-122,156-228,370-419 — install()+doExpand() VERBATIM port source (LOCKED; correct path, NOT the old sharedUtility path)
    ../swg-client-v2/src/engine/client/library/clientSkeletalAnimation/src/shared/animation/CompressedKeyframeAnimationTemplate.cpp lines 1198-1313 (CKAT load_0001), 553-594 (QCHN), 637-660 (CHNL), 1273-1277 (SROT) — CKAT oracle
    ../swg-client-v2/src/engine/client/library/clientSkeletalAnimation/src/shared/animation/KeyframeSkeletalAnimationTemplate.cpp lines 1518-1620 (KFAT load_0003), 523-553 (QCHN), 576-607 (CHNL), 1547-1552 (XFIN), 1590 (SROT raw floatQuaternion) — KFAT oracle
    ../swg-blender-plugin/swg_scene/animation_compressed.py lines 79-86 — second oracle cross-check (Python version-incomplete per synthesis §5 — C++ is primary)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-RESEARCH.md — Pitfall 2 (CKAT critical path), Pitfall 3 (quat element-order; w clamp)
  </read_first>
  <action>
    FILE: modules/core/math/CompressedQuaternion.h + CompressedQuaternion.cpp
    VERBATIM PORT of swg-client-v2 CompressedQuaternion.cpp:82-122,156-228,370-419. Implement install() to build the static s_formatData[255] table and doExpand(uint32 packed, uint8 xFmt, uint8 yFmt, uint8 zFmt) -> quaternion (w,x,y,z). Do NOT hand-derive the constants. Follow §1.6: for baseShiftCount 0..6 (7 precision LEVELS): baseSeparation = 2/(2^shift+1); halfRange = baseSeparation; expandFactor11 = halfRange/1023; expandFactor10 = halfRange/511; formatId per level {0:0xFE,1:0xFC,2:0xF8,3:0xF0,4:0xE0,5:0xC0,6:0x80}; baseCount = 2^shift; for i in 0..baseCount-1: formatIndex = formatId | i; s_formatData[formatIndex] = { baseValue = -1 + (i+1)*baseSeparation, precisionIndex = shift, expandFactor11, expandFactor10 }. Decode: x = expand11(packed>>21, xFmt); y = expand11(packed>>10, yFmt); z = expand10(packed, zFmt); w = sqrt(max(0, 1-(x²+y²+z²))). expand11(cv,fmt): m = cv & 0x3FF; b = baseValue[fmt]; ef = expandFactor11[fmt]; (cv & 0x400) ? b - m*ef : b + m*ef. expand10(cv,fmt): m = cv & 0x1FF; b = baseValue[fmt]; ef = expandFactor10[fmt]; (cv & 0x200) ? b - m*ef : b + m*ef. Do NOT use fenced code blocks in the plan; implement against the cited source lines.

    FILE: modules/core/formats/Animation.h + Animation.cpp
    parseAnimation(root) -> AnimationResult. Root tag discriminator: CKAT → CKAT-0001 path; KFAT → read version (0002 decline / 0003 path). CARRY TWO SEPARATE BYTE TABLES — do NOT reuse one for both.

    KFAT-0002 path: detect at the FORM version tag (FORM KFAT → 0002) and return AnimationResult { variant:KFAT_0002_UNSUPPORTED, fps:0, frameCount:0, joints:{}, roundTrip } IMMEDIATELY — do NOT enter INFO/XFRM/XFIN or parse Euler channels. The 0002 XFIN layout (uint32 rotationMask + 3×int32 Euler indices) differs from 0003; applying the 0003 reader to a 0002 file silently desyncs, so the version branch MUST precede any sub-form read.

    KFAT-0003 path (INT32 widths): FORM KFAT → FORM 0003.
    - INFO: float32 fps; then 6×int32: frameCount, transformInfoCount, rotationChannelCount, staticRotationCount, translationChannelCount, staticTranslationCount. Cap transformInfoCount 2048.
    - FORM XFRM → XFIN × transformInfoCount: string name; int8 hasAnimatedRotations; int32 rotationChannelIndex; uint32 translationMask; int32 xTransChannelIndex; int32 yTransChannelIndex; int32 zTransChannelIndex. (NOT name-only.)
    - [FORM AROT → QCHN × rotationChannelCount]: int32 keyCount; per key int32 frameNumber + floatQuaternion (4×float32, (w,x,y,z)). SPARSE.
    - [SROT]: floatQuaternion (4×float32) × staticRotationCount.
    - [FORM ATRN → CHNL × translationChannelCount]: int32 keyCount; per key int32 frameNumber + float32 value. SPARSE.
    - [STRN]: float32 × staticTranslationCount.
    Oracle: KeyframeSkeletalAnimationTemplate.cpp:1518-1620,523-553,576-607,1547-1552,1590.

    CKAT-0001 path (INT16 widths): FORM CKAT → FORM 0001.
    - INFO: float32 fps; then 6×int16: frameCount, transformInfoCount, rotationChannelCount, staticRotationCount, translationChannelCount, staticTranslationCount. Cap transformInfoCount 2048.
    - FORM XFRM → XFIN × transformInfoCount: string name; int8 hasAnimatedRotations; int16 rotationChannelIndex; uint8 translationMask; int16 xTransChannelIndex; int16 yTransChannelIndex; int16 zTransChannelIndex.
    - [FORM AROT → QCHN × rotationChannelCount]: int16 keyCount; uint8 xFormat; uint8 yFormat; uint8 zFormat — ONCE PER CHANNEL (not per key); per key int16 frameNumber + uint32 compressedRotation. SPARSE. Decode each compressedRotation via CompressedQuaternion::doExpand(packed, xFormat, yFormat, zFormat).
    - [SROT]: per static rotation: uint8 xFormat + uint8 yFormat + uint8 zFormat THEN uint32 compressedRotation (formats FIRST, packed LAST — verified CompressedKeyframeAnimationTemplate.cpp:1273-1276; decode via doExpand). NOTE: order is formats-then-packed, the REVERSE of a naive packed-first read.
    - [FORM ATRN → CHNL × translationChannelCount]: int16 keyCount; per key int16 frameNumber + float32 value. SPARSE.
    - [STRN]: float32 × staticTranslationCount.
    Oracle: CompressedKeyframeAnimationTemplate.cpp:1198-1313,553-594,637-660,1273-1277.

    IR / buffer: build a SPARSE keyframe IR per joint — for each rotation channel store { keyCount, int32 frames[], (w,x,y,z) quats[] }; per translation channel store { keyCount, int32 frames[], float values[] }; static rotations/translations stored once. Pack into a flat ArrayBuffer with a small header table (per-channel offsets/counts) so the renderer can binary-search the frame index. DO NOT expand to dense per-frame. Decode CKAT compressed quats to (w,x,y,z) float32 at parse time (so the renderer never decodes per-frame). Document the (w,x,y,z) order.

    DECIMATION CAVEAT: the loader decimates rotation/translation keys on load (s_rotationCompressionFix/s_translationFix, :578,655). The TYPED parser MUST NOT apply this decimation — store ON-DISK counts. The IFF-layer SC-5 round-trip is agnostic to it.

    Security: transformInfoCount/joint cap 2048; keyCount per channel cap 100,000; XFIN names cap 256; compressed-quat decode is arithmetic only (no array OOB); bounds-check every channel index against channel counts.

    FILE: CMakeLists.txt — add math/CompressedQuaternion.cpp, formats/Animation.cpp.

    FILE: src/anim_binding.cpp — ParseAnimation: extractBytes + try/catch (iff_binding.cpp pattern). Return typed JSON (variant, fps, frameCount, joints[]) + the sparse keyframe ArrayBuffer ('keyframes') + the per-channel header table.

    FILE: src/addon.cpp — forward-declare ParseAnimation + register "parseAnimation".

    FILE: harness/test/mesh-roundtrip.test.ts — extend with:
    - '.ans CKAT 0001 byte-exact round-trip': real CKAT fixture; assertRoundTrip via parseIff/serializeIff; assert variant='CKAT-0001' and frameCount>0; assert a QCHN channel's format bytes were read once (key count matches on-disk, no decimation).
    - '.ans KFAT 0003 byte-exact round-trip': real KFAT 0003 fixture; assert variant='KFAT-0003'.
    - CKAT decode unit test: feed a real packed uint32 + its channel (xFmt,yFmt,zFmt) from the fixture; assert decoded quaternion magnitude within 0.001 of 1.0 and w >= 0.
    registerFormat: 'mesh-ans-ckat' (loaderSource 'swg-client-v2 CompressedKeyframeAnimationTemplate.cpp:1198-1313,553-594 + CompressedQuaternion.cpp:82-122,156-228,370-419'), 'mesh-ans-kfat' (loaderSource 'swg-client-v2 KeyframeSkeletalAnimationTemplate.cpp:1518-1620,523-553').

    One-time enumeration script (RESEARCH Wave 0 item A1): write a gitignored Node script enumerate-ans-variants.js that mounts the client TRE, walks all .ans entries, calls parseAnimation on each, and tallies CKAT vs KFAT-0003 vs KFAT-0002. Run once for the SUMMARY (not in the test suite).
  </action>
  <verify>
    <automated>pnpm --filter @swg/native-core build 2>&1 | tail -5 && pnpm --filter @swg/harness test 2>&1 | tail -20; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    native-core builds without C++ errors.
    CompressedQuaternion.cpp is a verbatim port: install() builds s_formatData (255 entries via formatId|baseIndex); no hand-derived baseCount=7/half_range=1.0 constants anywhere.
    CKAT unit test: decoded quaternion magnitude within 0.001 of 1.0; w >= 0 (clamp verified).
    CKAT QCHN parse reads format bytes ONCE per channel (verified: key count matches on-disk; first channel does not desync).
    KFAT-0003 INFO/XFIN/QCHN read with int32 widths; CKAT-0001 with int16 widths (two separate code paths).
    SROT differs by format: CKAT = uint8 x/y/z format bytes FIRST then uint32 packed (CompressedKeyframeAnimationTemplate.cpp:1273-1276); KFAT = raw floatQuaternion (4×float32).
    CORE-05 round-trips pass for CKAT 0001 + KFAT 0003 (byte-identical); typed parser stores on-disk counts (no decimation).
    KFAT 0002 returns 'KFAT-0002-unsupported' without throwing.
    Keyframe ArrayBuffer is sparse (per-channel keyCount+frames+data), NOT dense; registry-coverage sweep passes (eleven from 02-01/02 + two new).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AnimationTransport UI + ref-clock sparse-key zero-GC sampler (VIEW-03)</name>
  <files>
    packages/renderer/src/panels/viewport/AnimationTransport.tsx,
    packages/renderer/src/panels/viewport/SkinnedMeshView.tsx,
    packages/renderer/src/state/viewportStore.ts
  </files>
  <read_first>
    packages/renderer/src/panels/viewport/SkinnedMeshView.tsx — current state (module-scope _scratchQuat/_scratchVec3/_scratchMat4 from 02-02; useFrame to extend with the sparse sampler)
    packages/renderer/src/state/viewportStore.ts — current store (transportState; add parsedAnimation, skeletonHelperVisible, ansPickerOptions)
    packages/renderer/src/panels/ViewportPanel.tsx — chip/accessibility patterns (chipStyle, actionBtnStyle, aria-label/title) for transport controls
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-UI-SPEC.md — Surface 5 (AnimationTransport: 30px footer bar; .ans picker; ⏮▶/⏸⏭; scrubber --color-widget track + --color-accent fill; frame counter mono; loop ↺; speed 0.25×/0.5×/1×/2×; skeleton-helper ⊹; states no-skeleton/skeleton+no-anim/playing/KFAT0002)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § AnimationTransport.tsx (button a11y; scrubber keyboard ←/→/Home/End; chip-style active; GC-safe contract)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.6 interpolation (slerp rotation, lerp translation; time=frame/fps; sparse keys — bracket two nearest by binary search; no loop flag in format); §6 (w,x,y,z)→THREE (x,y,z,w) reorder
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-RESEARCH.md — anti-pattern "Allocating in useFrame"; "setTransportState per frame churns Zustand" — drive clock from a ref, throttle the UI sync
  </read_first>
  <action>
    FILE: viewportStore.ts — EXTEND: parsedAnimation (AnimationParseResult|null=null); skeletonHelperVisible (boolean=false); ansPickerOptions (string[]=[]). Actions: setParsedAnimation, clearAnimation, setSkeletonHelperVisible, setAnsPickerOptions. The transport currentFrame is updated THROTTLED from the SkinnedMeshView ref clock (e.g. once per ~100ms), NOT every frame. Import AnimationParseResult from @swg/contracts.

    ansPickerOptions population: when a skeleton/appearance loads, resolve the animation list via the LATX chain — LATX maps the skeleton → a .lat (Logical Animation Table) path; resolveEntry(.lat) via the TRE VFS → parse the .lat → collect its .ans entry paths → setAnsPickerOptions. (LATX yields a .lat path, NOT .ans directly — do NOT call parseAnimation on a .lat.) If a full .lat parser is out of scope for this phase, fall back to a documented same-name/adjacent-.ans heuristic and label it a heuristic (not the authoritative table). The picker is NOT empty when a real skeleton is loaded.

    FILE: AnimationTransport.tsx — per PATTERNS.md § AnimationTransport.tsx + UI-SPEC Surface 5. 30px footer bar. Read transportState, parsedAnimation, parsedSkeleton, skeletonHelperVisible, ansPickerOptions. Actions setTransportState, setParsedAnimation, setSkeletonHelperVisible.
    .ans picker: <select> over ansPickerOptions; selecting calls nativeCore.parseAnimation(bytes) → setParsedAnimation; resets currentFrame to 0. 'Choose an animation…' prompt when none. Variant tag (CKAT 0001 / KFAT 0003) mono next to picker. KFAT-0002 selection → VerificationStatus warn "unsupported legacy animation (KFAT 0002) — skipped"; skeleton stays in bind pose.
    Transport controls left→right: ⏮ (Previous frame, aria-label+title) → setTransportState({currentFrame: max(0,currentFrame-1)}); ▶/⏸ chip-style play toggle; ⏭ (Next frame); scrubber <input type=range min0 max totalFrames-1> (keyboard ←/→ step ±1, Home/End to ends; track --color-widget, fill --color-accent); frame counter '{currentFrame}/{totalFrames} · {(currentFrame/fps).toFixed(2)}s' (mono); loop ↺ chip; speed 0.25×/0.5×/1×/2× chips; skeleton-helper ⊹ chip.
    Transport buttons set DISCRETE transportState (play/pause/loop/speed/step) — these are user events, not per-frame. The continuous advance happens in SkinnedMeshView's ref clock.
    States per Surface 5: no skeleton → transport hidden ('Load a skinned mesh or skeleton to animate'); skeleton+no anim → frame 0 bind pose, picker prompt; playing → pause glyph + advancing scrubber; KFAT 0002 → warn + bind pose.

    FILE: SkinnedMeshView.tsx — EXTEND useFrame with the sparse-key sampler + ref clock. Module-scope scratch (_scratchQuat/_scratchVec3/_scratchMat4) ALREADY present from 02-02 — do NOT re-declare.
    REF CLOCK: keep currentFrame in a useRef (frameRef), advanced each useFrame by (fps * speed * delta) when playing; on loop wrap to 0, else clamp+pause. Flush frameRef → store.setTransportState({currentFrame}) THROTTLED (accumulate elapsed; flush at most ~10×/s) so the UI scrubber follows without per-frame Zustand churn.
    PRE-BUILD (useEffect on parsedAnimation change, NOT useFrame): from the sparse keyframe ArrayBuffer build, per joint, flat Float32Array key data — rotation: frames Int32Array + quats Float32Array (already (w,x,y,z) decoded by C++) ; translation: frames Int32Array + values Float32Array. Build a name→bone-index map (name-keyed, Pitfall 5). Store in a ref. PREFER flat Float32Array + scratch THREE objects over pre-building 10k×Quaternion (avoids a load-time GC pause); construct/reuse a small fixed set of scratch Quaternion/Vector3 for the bracketed endpoints (module scope or a fixed ref array), NOT one-per-key.
    PER-FRAME SAMPLE (only when parsedAnimation && parsedSkeleton): for each animated joint: binary-search the rotation frames for the bracket [k0,k1] around frameRef; compute frac; set scratch endpoint quaternions from the flat Float32Array (reorder (w,x,y,z)→(x,y,z,w)); THREE.Quaternion.slerpQuaternions(qA, qB, frac, _scratchQuat); bone.quaternion.copy(_scratchQuat). Translation: same bracket on translation frames; THREE.Vector3.lerpVectors(vA, vB, frac, _scratchVec3); bone.position.copy(_scratchVec3). Static rotation/translation joints: set once. bone.updateMatrixWorld(true) after.
    ANTI-PATTERN CHECK (must NOT appear in useFrame body): no `new THREE.Quaternion()`, no `new THREE.Vector3()`, no `new THREE.Matrix4()`, no array.map producing new arrays, no Object.assign, no template-string allocation, no per-frame setTransportState (throttled flush only).
    SKELETON HELPER: in a useEffect keyed on [skeletonHelperVisible, parsedSkeleton] (NOT in useFrame), mount THREE.SkeletonHelper(skinnedMeshRef.current) as a scene child when on and remove it when off — never construct the helper per-frame.
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer typecheck 2>&1 | tail -10; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    Renderer TypeScript compiles without errors.
    viewportStore has parsedAnimation, skeletonHelperVisible, ansPickerOptions + setters; currentFrame is flushed throttled (not per-frame).
    ansPickerOptions populated from the skeleton→.ans (LATX/animation-table) mapping — picker lists real animations.
    AnimationTransport renders all controls with aria-label/title; scrubber responds to ← → Home End; loop/play/helper use chipStyle.
    useFrame sampler uses binary-search bracketing of SPARSE keys + flat Float32Array data; body has ZERO `new THREE.*`, ZERO per-frame setTransportState.
    KFAT 0002 surfaces a warn in the transport bar without mis-rendering.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — animation playback without GC hitching</name>
  <action>Pause for the user to verify CKAT/KFAT animation plays correctly on a real .sat + .ans, the picker is populated, and there is no per-frame GC stutter per the how-to-verify steps. Do not auto-advance.</action>
  <what-built>
    Animation parser (CKAT 0001 + KFAT 0003, sparse, verbatim-ported compressed-quaternion decode) wired into the AnimationTransport + SkinnedMeshView ref-clock sparse sampler. User picks an .ans from the populated picker and plays it back on a loaded skeleton. Skeleton-helper toggle shows bone wire.
  </what-built>
  <how-to-verify>
    1. Open a .sat with a skinned mesh (from the 02-02 checkpoint).
    2. In the AnimationTransport bar: the .ans picker should LIST available animations (ansPickerOptions populated). Select a CKAT animation.
    3. Press Play. Confirm the mesh deforms over time, the scrubber advances, the frame counter increments, and there is NO visible GC stutter (smooth at 60fps).
    4. Drag the scrubber to a specific frame — the mesh jumps to that pose. Confirm the pose looks correct (the compressed-quaternion decode is right — no exploded/inside-out limbs).
    5. Toggle loop (↺): animation loops; toggle off: stops at last frame.
    6. 0.5× and 2× speed visibly slow/speed the animation.
    7. Toggle skeleton helper (⊹): bone wire appears/disappears.
    8. If a KFAT 0002 animation exists: select it. Confirm the ⚠ "unsupported legacy animation" warning, bind pose maintained, no crash.
    9. Chrome DevTools → Performance → record 2s of animation. Confirm no GC events in the useFrame hot path (minor GCs from other React panels OK; the Three.js loop is allocation-free). Confirm the scrubber updates are throttled (not a store write every frame).
  </how-to-verify>
  <resume-signal>Type "approved" to continue to export, or describe any issues (decode errors, GC hitching, incorrect deformation, empty picker).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .ans bytes → C++ CKAT/KFAT decoder | Untrusted binary from local TRE; compressed uint32 decode is arithmetic only (no pointer deref on field values) |
| Sparse keyframe ArrayBuffer → useFrame sampler | Per-channel header (offsets/counts) validated at parse time; useFrame only binary-searches within validated bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | formats/Animation.cpp CKAT/KFAT decode | mitigate | transformInfoCount cap 2048; keyCount per channel cap 100,000; XFIN names cap 256; channel indices bounds-checked against channel counts; packed uint32 decode is arithmetic only |
| T-02-17 | Tampering | useFrame bone index | mitigate | Joint→bone mapping name-keyed (XFIN name → Skeleton bone name); unmatched joint skipped (no OOB bone index); name map built once at pre-build |
| T-02-18 | Denial of Service | KFAT 0002 graceful decline | mitigate | KFAT 0002 returns unsupported variant immediately; no Euler parse, no loop, no crash on malformed 0002 |
| T-02-19 | Tampering | sparse keyframe OOB read in useFrame | mitigate | Per-channel header offsets/counts validated at parse time; binary search clamps frame to [firstKey,lastKey]; index never exceeds the channel's keyCount |
</threat_model>

<verification>
1. `pnpm --filter @swg/native-core build` exits 0 — CompressedQuaternion.cpp + Animation.cpp compile.
2. `pnpm --filter @swg/harness test` exits 0 — CKAT decode unit test + CKAT+KFAT-0003 round-trips + registry-coverage sweep (thirteen formats).
3. `pnpm --filter @swg/renderer typecheck` exits 0 — AnimationTransport + viewportStore extensions compile.
4. `pnpm -r test` exits 0 — full suite green.
5. Human-verify: animation plays on a real asset; no per-frame `new` and no per-frame setTransportState in useFrame; populated picker.
</verification>

<success_criteria>
- CompressedQuaternion is a verbatim port (install() 255-entry s_formatData via formatId|baseIndex; w clamp) — unit test verifies magnitude ≈ 1.0, w >= 0.
- CKAT (int16) and KFAT (int32) byte tables are SEPARATE; QCHN format bytes once per channel; SROT differs per format; keys SPARSE.
- KFAT 0002 declined gracefully; transport shows warn + bind pose.
- CORE-05 round-trips green for CKAT + KFAT 0003; typed parser preserves on-disk counts (no decimation).
- AnimationTransport has all controls + correct a11y; ansPickerOptions populated from LATX/animation-table.
- useFrame body zero-alloc + sparse-key binary-search sampling; clock from a ref; currentFrame flushed throttled.
- VIEW-03 satisfied: real animation plays correctly on a real .sat + .ans without perceptible GC hitching.
</success_criteria>

<output>
Create `.planning/phases/02-3d-mesh-viewport-mvp-proof/02-04-SUMMARY.md` when done.
</output>
