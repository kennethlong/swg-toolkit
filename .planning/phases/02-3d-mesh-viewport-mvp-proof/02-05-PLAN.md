---
phase: 02-3d-mesh-viewport-mvp-proof
plan: 05
type: execute
wave: 5
depends_on:
  - 02-03
  - 02-04
files_modified:
  - packages/renderer/src/panels/viewport/ExportDialog.tsx
  - packages/renderer/src/panels/viewport/export/buildExportScene.ts
  - packages/renderer/src/panels/viewport/export/mirrorScene.ts
  - packages/renderer/src/panels/viewport/export/exportMaterial.ts
  - packages/renderer/src/panels/viewport/export/buildAnimationClip.ts
  - packages/renderer/src/panels/viewport/Viewport.tsx
  - packages/renderer/src/panels/ViewportPanel.tsx
  - packages/renderer/src/state/viewportStore.ts
  - docs/02-formats/meshes-and-appearances.md
  - docs/02-formats/skeletons-and-animation.md
  - docs/03-rendering/shaders-and-fx.md
autonomous: false
requirements:
  - VIEW-04

# ─────────────────────────────────────────────────────────────────────────────
# REVISED 2026-06-25 after 02-04 animation rewrite + 4-AI crew review.
# Crew (Codex/Opus/Cursor/Sonnet) CONVERGED from non-overlapping angles; briefs in
# .planning/research/CONSULT-P2-05-*. Verified spec captured in memory
# `swg-gltf-export-coordinate-mirror`. Key corrections vs the original plan:
#   1. COLLADA path DROPPED — ColladaExporter was removed from three by r0.184.0. glTF (.glb) only.
#   2. Custom ShaderMaterial is NOT serialized by GLTFExporter (warn→null→no material) — MUST convert
#      to MeshStandardMaterial, AND decompress DXT CompressedTextures to RGBA before export.
#   3. Animation IR is NOT "absolute local" — keys are one factor of post·(key·bindRot·pre). The clip
#      must COMPOSE-then-mirror at the sparse keyframes (sample), not feed raw keys.
#   4. Mirror is the verified X-flip BAKED per-transform (not a root scale node): adds tangent.w flip;
#      no +90° facing rotation; no normal-map green-channel invert.
#   5. Blender-MCP export engine is OUT of scope (Phase 6 bridge) — see backlog blender-bridge-mcp.
# ─────────────────────────────────────────────────────────────────────────────

must_haves:
  truths:
    - "User can export the loaded (rigged + animated) mesh to glTF (.glb) via GLTFExporter — the only export format this phase (ColladaExporter does NOT exist in three r0.184.0; OBJ/USD and a Blender-native path are deferred to the Phase 6 Blender bridge)."
    - "glTF includes the skeleton and animation when present (D-10). All bones are present as Object3D nodes in the exported scene graph (the merged-skeleton root is mounted), or GLTFExporter silently drops joints/animation tracks."
    - "Export materials are CONVERTED: each custom SWG ShaderMaterial → a MeshStandardMaterial carrying .map (diffuse, SRGBColorSpace), .normalMap, and .emissive(>0)+.emissiveMap. GLTFExporter warns+returns null on a raw ShaderMaterial and emits the primitive WITHOUT a material. SWG specular has no glTF-PBR slot and is intentionally dropped this phase."
    - "Export textures are GPU-decompressed: DXT THREE.CompressedTexture cannot be exported — convert to an RGBA DataTexture (or draw to a canvas) before export; set texture.flipY=false for glTF UV-origin parity."
    - "The live scene is SWG-disk-native (verbatim, identity orientation — crew-verified: our loader does no axis negation). The export applies the verified X-mirror EXACTLY ONCE, BAKED into a deep-cloned export scene (never the live scene, never a root negative-scale node)."
    - "Matrix-based X-mirror (flip=diag(-1,1,1)) applied consistently and conjugated (flip·M·flip): position x→-x, normal nx→-nx, tangent (tx,ty,tz,w)→(-tx,ty,tz,-w) INCLUDING the w handedness flip, triangle winding REVERSED, bind/inverse-bind matrices, bone local transforms, AND animation tracks. No +90° facing rotation (glTF is Y-up like SWG). No normal-map green-channel invert."
    - "buildAnimationClip COMPOSES then mirrors: per channel, at each sparse keyframe f, evaluate L(f)=postMul·(key(f)·bindPoseRot·preMul) and t(f)=bindTrans+delta(f) (the shipped 02-04 composition), THEN mirror — quat (w,x,y,z)→(w,x,-y,-z)→glTF (x,-y,-z,w); translation x→-x. Raw-key mirroring is provably wrong (valid only if pre/bind/post=I). Sampling at existing keys is exact (composition & mirror commute with slerp)."
    - "Three.js bone names are stable and match the AnimationClip track paths (bone.name set from SKTM names in 02-02; tracks named `${bone.name}.quaternion` / `.position`)."
    - "User can extract the raw asset bytes from the mount via Extract… using sourceMountHandle/sourceArchiveIndex/sourceEntryIndex from viewportStore (readMountEntry → ArrayBuffer, decompressed payload)."
    - "docs/ updates replace fabricated layouts with verified synthesis §1 content and add precise 'verified for versions X against files Y (date)' callouts — NOT a blanket caveat drop; the DDS Texture.cpp:115-129 mis-citation is corrected."
  artifacts:
    - path: "packages/renderer/src/panels/viewport/export/buildExportScene.ts"
      provides: "buildExportScene(liveRoot, {includeSkeleton, includeAnimation}): deep-clone (clone geometry + skeleton once, rebind all parts) → exportMaterial conversion → mirrorScene. Returns a clean det=+1, positive-scale, textured, rigged export root. LIVE_SCENE_IS_SWG_NATIVE constant documents the single-apply decision."
    - path: "packages/renderer/src/panels/viewport/export/mirrorScene.ts"
      provides: "applyXMirror(exportRoot): bake flip=diag(-1,1,1) into geometry (pos/normal/tangent incl. w, winding), bind/inverse-bind, bone locals. No root node, no facing rotation."
    - path: "packages/renderer/src/panels/viewport/export/exportMaterial.ts"
      provides: "toStandardMaterial(swgShaderMaterial): MeshStandardMaterial with .map(sRGB)/.normalMap/.emissive+.emissiveMap; decompresses DXT CompressedTexture → RGBA DataTexture; flipY=false. Spec dropped."
    - path: "packages/renderer/src/panels/viewport/export/buildAnimationClip.ts"
      provides: "buildAnimationClip(parsedAnimation, parsedSkeleton, fps): compose-then-mirror sampler → THREE.AnimationClip (Quaternion/Vector KeyframeTracks keyed by bone name)."
    - path: "packages/renderer/src/panels/viewport/ExportDialog.tsx"
      provides: "glTF (.glb) export modal: include toggles (Skeleton/Animation), current-LOD note, coordinate note 'Y-up · X-mirror applied', progress/complete/error states (D-10). No COLLADA radio."
    - path: "docs/02-formats/meshes-and-appearances.md"
      provides: "Verified MESH/SKMG/SMAT/MLOD layouts from synthesis §1 with precise version+file+date callouts"
    - path: "docs/02-formats/skeletons-and-animation.md"
      provides: "Verified SKTM/KFAT/CKAT layouts from synthesis §1 with precise callouts"
    - path: "docs/03-rendering/shaders-and-fx.md"
      provides: "Verified SSHT/CSHD/PAL/DDS GPU-upload path; DDS NOT cited to Texture.cpp:115-129; note ShaderMaterial→MeshStandardMaterial conversion required for glTF export"
  key_links:
    - from: "ExportDialog.tsx"
      to: "export/buildExportScene.ts + GLTFExporter"
      via: "buildExportScene(liveRoot, opts) → clean mirrored+textured deep clone; await new GLTFExporter().parseAsync(exportScene, { binary:true, animations: clip?[clip]:[] })"
      pattern: "buildExportScene|GLTFExporter|parseAsync"
    - from: "export/exportMaterial.ts"
      to: "GLTFExporter material path (ShaderMaterial→null)"
      via: "toStandardMaterial() before export; DXT CompressedTexture → RGBA DataTexture; flipY=false"
      pattern: "MeshStandardMaterial|DataTexture|isCompressedTexture"
    - from: "export/buildAnimationClip.ts"
      to: "viewportStore.parsedAnimation (02-04 sparse IR) + 02-04 composition"
      via: "compose L(f)=post·(key·bindRot·pre) then mirror → QuaternionKeyframeTrack('{boneName}.quaternion', times, values)"
      pattern: "QuaternionKeyframeTrack|AnimationClip|postMul"
---

<objective>
Ship the glTF (.glb) export pipeline (VIEW-04, D-10) and the raw-asset Extract action. glTF is the
rigged+animated path and the ONLY export format this phase (ColladaExporter was removed from three by
r0.184.0; richer/native paths are the Phase 6 Blender bridge). The coordinate transform is the
crew-verified X-mirror BAKED once into a deep-cloned export scene. Materials are converted from the
custom SWG ShaderMaterial to MeshStandardMaterial (with DXT textures decompressed) because GLTFExporter
does not serialize ShaderMaterials. Correct the AI-distilled format docs with verified synthesis §1
ground truth using precise version+file+date callouts.

Purpose: Closes VIEW-04 — the user takes a rigged+animated SWG asset out of the tool in a standard
format that opens in external DCC tools. GATED on the CORRECTED 02-04 (buildAnimationClip consumes
02-04's composition). The docs update retires the "AI-proposed" caveat ONLY for formats proven this
phase, with explicit provenance.

Output: buildExportScene.ts (deep clone) + mirrorScene.ts (baked X-mirror) + exportMaterial.ts
(ShaderMaterial→Standard + DXT decompress) + buildAnimationClip.ts (compose-then-mirror) +
ExportDialog.tsx + Export…/Extract… wiring; docs corrections with precise callouts.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-05-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Code\SWG-Toolkit\.planning\PROJECT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-CONTEXT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-RESEARCH.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-SYNTHESIS.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-05-AXIOMS.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-05-01-opus-mirror-math.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-UI-SPEC.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-PATTERNS.md

<interfaces>
<!-- glTF exporter (bundled with three@0.184.0); ColladaExporter is ABSENT in this version -->
<!-- import { GLTFExporter } from 'three/examples/jsm/exporters/GLTFExporter.js' -->
<!-- const glb = await new GLTFExporter().parseAsync(exportScene, { binary:true, animations: clip?[clip]:[] }) // ArrayBuffer -->

<!-- COORDINATE CONVENTION (crew-verified 2026-06-25; memory swg-gltf-export-coordinate-mirror): -->
<!--   The live R3F scene is SWG-disk-NATIVE (verbatim, identity orientation; our loader does NO axis -->
<!--   negation — DeIndex.cpp:117-132, SkeletalMeshGen.cpp:664/698, mesh_binding.cpp:193). SWG disk is -->
<!--   X-negated vs glTF/Maya/Blender (MayaConversions.h:42; swg_scene/coords.py). So export mirrors X -->
<!--   exactly ONCE. If 02-02 ever pre-converts, do NOT mirror again — assert via LIVE_SCENE_IS_SWG_NATIVE. -->

<!-- MATRIX X-mirror — BAKE into a deep clone (flip=diag(-1,1,1)); NEVER a root scale(-1,1,1) node -->
<!-- (negative-scale + skinning breaks Blender import). Conjugate every transform M'=flip·M·flip: -->
<!--   position (x,y,z)→(-x,y,z); normal nx→-nx; tangent (tx,ty,tz,w)→(-tx,ty,tz,-w) [w MANDATORY]; -->
<!--   REVERSE triangle winding (swap 2nd/3rd index); bind & inverse-bind & bone-local matrices conjugated; -->
<!--   quat (w,x,y,z)→(w,x,-y,-z) then glTF reorder (x,-y,-z,w); translation x→-x. -->
<!--   NO +90° facing rotation. NO normal-map green-channel invert. -->

<!-- buildAnimationClip: from viewportStore.parsedAnimation (02-04 sparse IR). COMPOSE THEN MIRROR: -->
<!--   per channel, at each existing key frame f: L(f)=postMul·(key(f)·bindPoseRot·preMul); t(f)=bindTrans+delta(f); -->
<!--   then mirror as above. QuaternionKeyframeTrack('{bone.name}.quaternion', times=frames.map(f=>f/fps), values) -->
<!--   + VectorKeyframeTrack('{bone.name}.position', ...). bone.name MUST equal the SKTM bone name from 02-02. -->

<!-- exportMaterial: GLTFExporter warns+returns null on ShaderMaterial → primitive has NO material. -->
<!--   Convert: MeshStandardMaterial { map:diffuse(SRGBColorSpace), normalMap, emissive:Color(>0), emissiveMap }. -->
<!--   DXT THREE.CompressedTexture cannot be exported → decompress to RGBA DataTexture (or canvas); flipY=false. -->
<!--   SWG specular has no glTF-PBR slot → dropped this phase. -->

<!-- Extract: nativeCore.readMountEntry(sourceMountHandle, sourceArchiveIndex, sourceEntryIndex) → ArrayBuffer; -->
<!--   write via Electron dialog.showSaveDialog / showSaveFilePicker. Additive only; never touches the mount. -->

<!-- From viewportStore: resolution(.parts), parsedAnimation, parsedSkeleton, sourceMountHandle/ -->
<!--   sourceArchiveIndex/sourceEntryIndex/sourceEntryPath. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildExportScene + mirrorScene + exportMaterial + buildAnimationClip + ExportDialog + Extract… + wire into ViewportPanel</name>
  <files>
    packages/renderer/src/panels/viewport/export/buildExportScene.ts,
    packages/renderer/src/panels/viewport/export/mirrorScene.ts,
    packages/renderer/src/panels/viewport/export/exportMaterial.ts,
    packages/renderer/src/panels/viewport/export/buildAnimationClip.ts,
    packages/renderer/src/panels/viewport/ExportDialog.tsx,
    packages/renderer/src/panels/viewport/Viewport.tsx,
    packages/renderer/src/panels/ViewportPanel.tsx,
    packages/renderer/src/state/viewportStore.ts
  </files>
  <read_first>
    packages/renderer/src/panels/viewport/SkinnedMeshView.tsx — live scene build (bone.name=SKTM name; SWG_ORIENTATION identity; the post·(key·bindRot·pre) composition at ~770-811; the rotation/translation channel read at ~631-677; the merged-skeleton root mounted as `<primitive object={skeleton.bones[0]}/>`). CONFIRM verbatim/identity before writing the mirror.
    packages/renderer/src/panels/viewport/material/swgMaterial.ts — uniform names (uDiffuseMap/uNormalMap/uSpecularMap/uEmissiveMap) + which textures are CompressedTexture vs DataTexture, for exportMaterial conversion.
    packages/renderer/src/panels/viewport/Viewport.tsx — skinnedParts memo (resolution.parts → meshesByLod[selectedLod]); the R3F scene root to clone.
    packages/renderer/src/state/viewportStore.ts — resolution/parsedAnimation/parsedSkeleton + sourceMountHandle/sourceArchiveIndex/sourceEntryIndex/sourceEntryPath; add lastExportFilename + setLastExportFilename.
    packages/renderer/src/shared/AsyncProgress.tsx — progress pattern for "Exporting glTF…"
    packages/renderer/src/shared/VerificationStatus.tsx — pass/fail/warn for export complete/failure
    packages/renderer/src/panels/ViewportPanel.tsx — header button area (Export…/Extract…); actionBtnStyle (PATTERNS.md 589-603)
    .planning/research/CONSULT-P2-05-01-opus-mirror-math.md — the full derivation (tangent.w, winding, quat formula, compose-then-mirror exactness, no-facing-rotation, bake-not-root).
    .planning/research/CONSULT-P2-05-03-cursor.out — GLTFExporter reality (ShaderMaterial→null line 1587; image types; skin/anim binding).
    ../swg-blender-plugin/swg_scene/coords.py — engine↔blender = (-x,y,z) (X-negation ground truth).
  </read_first>
  <action>
    FILE: export/mirrorScene.ts — applyXMirror(root: THREE.Object3D): void. flip = diag(-1,1,1).
    For each mesh geometry (already a deep clone): position.x→-x; normal.x→-normal.x; tangent →
    (-tx,ty,tz,-w) INCLUDING the w sign flip; REVERSE winding (swap the 2nd/3rd index of every triangle
    in the Uint32 index buffer). For each bone: localMatrix' = flip·localMatrix·flip; recompose
    position/quaternion. For SkinnedMesh bind & inverse-bind matrices: M' = flip·M·flip. Keep bone.name.
    NO root scale node, NO +90° facing rotation, NO green-channel texture edit.

    FILE: export/exportMaterial.ts — toStandardMaterial(mat: THREE.ShaderMaterial): THREE.MeshStandardMaterial.
    Read uDiffuseMap/uNormalMap/uEmissiveMap from mat.uniforms. For each used texture: if
    isCompressedTexture, decode DXT to an RGBA THREE.DataTexture (reuse the renderer's DDS decode, or
    readRenderTargetPixels / a one-off WebGL decode); set colorSpace (diffuse=SRGBColorSpace, normal/
    emissive=NoColorSpace), flipY=false, needsUpdate. Return new MeshStandardMaterial({ map, normalMap,
    emissive: emissiveMap ? new Color(1,1,1) : new Color(0,0,0), emissiveMap }). SWG specular → dropped.

    FILE: export/buildExportScene.ts — buildExportScene(liveRoot, { includeSkeleton, includeAnimation }).
    1. CONVENTION CHECK: const LIVE_SCENE_IS_SWG_NATIVE = true; comment the no-double-apply decision.
    2. DEEP-CLONE: clone each mesh with geometry.clone() (NOT shared buffers — a shared mutate would
       corrupt the live viewport). Clone the skeleton/bones ONCE and rebind every part's SkinnedMesh to
       the cloned skeleton. Ensure ALL bones are present as Object3D nodes under the export root (mount
       the cloned skeleton root) — GLTFExporter drops joints/tracks for bones not in the graph.
    3. MATERIALS: replace each mesh.material with toStandardMaterial(...).
    4. MIRROR: applyXMirror(exportRoot) (exactly once). Return exportRoot. Live scene untouched.

    FILE: export/buildAnimationClip.ts — buildAnimationClip(parsedAnimation, parsedSkeleton, fps):
    THREE.AnimationClip. For each animated joint, at each EXISTING sparse key frame f: compose
    L(f)=postMul·(key(f)·bindPoseRot·preMul) and t(f)=bindTrans+delta(f) (reuse the 02-04 composition
    helpers), THEN mirror — quat (w,x,y,z)→(w,x,-y,-z)→glTF reorder (x,-y,-z,w); translation x→-x.
    Build QuaternionKeyframeTrack('{bone.name}.quaternion', times=frames.map(f=>f/fps), values) +
    VectorKeyframeTrack('{bone.name}.position', times, values). new AnimationClip('take_001', -1, tracks).
    Add an assertion/unit test: a known joint's composed local quat at a known frame equals the
    Skeleton.cpp ground-truth (L5) before the mirror — guards the "wrong composition stage" Rank-1 risk.

    FILE: ExportDialog.tsx — per PATTERNS.md + UI-SPEC Surface 7, glTF ONLY (no COLLADA radio).
    Title "Export mesh". Include toggles: Skeleton (checked if hasSkeleton), Animation (checked if
    hasAnimation), disabled when unavailable. Current-LOD note 'exports LOD {n}'. Coordinate note
    'Y-up · X-mirror applied' (--color-info). Cancel (secondary) / Export (primary --color-accent).
    Export handler (async): AsyncProgress "Exporting glTF…"; exportScene = buildExportScene(liveRef,
    {includeSkeleton, includeAnimation}); clip = includeAnimation ? buildAnimationClip(...) : null;
    glb = await new GLTFExporter().parseAsync(exportScene, { binary:true, animations: clip?[clip]:[] });
    downloadBlob(glb, '.glb'). On success: setLastExportFilename + VerificationStatus pass
    "✓ exported {filename}". On error: VerificationStatus fail + Retry; dialog stays open.

    Extract… action: read sourceMountHandle/sourceArchiveIndex/sourceEntryIndex from viewportStore; get
    a save path via Electron dialog.showSaveDialog / showSaveFilePicker; nativeCore.readMountEntry(...)
    → ArrayBuffer; write to the chosen path. Additive only; never overwrites mounted archive bytes (D-10).
    Show "✓ extracted {filename}".

    FILE: viewportStore.ts — add lastExportFilename (string|null) + setLastExportFilename.
    FILE: Viewport.tsx — mount ExportDialog when exportDialogOpen; pass the R3F scene root (useThree)
    captured from 02-02's view.
    FILE: ViewportPanel.tsx — Export… and Extract… buttons (actionBtnStyle), disabled when
    loadStatus.kind !== 'done'. Text labels per Surface 7.
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer typecheck 2>&1 | tail -10; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    Renderer TypeScript compiles without errors.
    buildExportScene deep-clones geometry (geometry.clone()) + clones+rebinds the shared skeleton; live scene untouched; LIVE_SCENE_IS_SWG_NATIVE documents the single-apply decision; all bones present in the export root.
    mirrorScene bakes the X-mirror: position.x, normal.x, tangent (incl. -w), REVERSED winding, conjugated bind/inverse-bind + bone-local matrices; no root scale node; no facing rotation; no green-channel edit.
    exportMaterial converts ShaderMaterial → MeshStandardMaterial (map sRGB / normalMap / emissive+emissiveMap), decompresses DXT CompressedTexture → RGBA DataTexture, flipY=false; specular dropped.
    buildAnimationClip composes (post·(key·bindRot·pre), additive translation) THEN mirrors, keyed by bone.name; assertion vs Skeleton.cpp ground truth present.
    ExportDialog: glTF (.glb) only (no COLLADA); coordinate note 'Y-up · X-mirror applied'; AsyncProgress during export; VerificationStatus pass/fail+Retry. GLTFExporter.parseAsync called with binary:true and animations when present.
    Extract… uses sourceMountHandle/archiveIndex/entryIndex + readMountEntry; writes to a user-chosen path; never touches the mount.
    Export…/Extract… buttons in ViewportPanel header, disabled when no asset loaded.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update AI-distilled format docs with verified synthesis §1 layouts + precise provenance callouts</name>
  <files>
    docs/02-formats/meshes-and-appearances.md,
    docs/02-formats/skeletons-and-animation.md,
    docs/03-rendering/shaders-and-fx.md
  </files>
  <read_first>
    docs/02-formats/meshes-and-appearances.md — ENTIRE FILE; identify every fabricated layout claim per synthesis §4
    docs/02-formats/skeletons-and-animation.md — ENTIRE FILE; identify fabricated claims per synthesis §4
    docs/03-rendering/shaders-and-fx.md — ENTIRE FILE; identify fabricated claims (shader-graph framing, CPU-only DXT, MeshStandardMaterial default)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1 LOCKED layouts; §4 Doc Corrections; §2 rendering decisions
    docs/00-overview/source-provenance.md — the "AI-proposed hypothesis" disclaimer format
  </read_first>
  <action>
    AGENTS.md convention: update docs when a layout is verified. Apply ONLY for formats verified this
    phase. Do NOT blanket-drop "AI-proposed" — replace each corrected section's caveat with a PRECISE
    provenance callout: "(verified for versions {X} against {swg-client-v2 File.cpp:lines} + real asset
    {fixture}, 2026-06-23)".

    FILE: docs/02-formats/meshes-and-appearances.md — replace fabricated sections with synthesis §1:
    - Skin indices: TWHD/TWDT variable-count int32 (TWDT total = transformWeightDataCount from SKMG INFO);
      a fixed vec4 skinIndex/skinWeight is DERIVED by the toolkit's de-index pass, not on disk.
      Callout: SkeletalMeshGeneratorTemplate.cpp:2247-2360,2331-2343.
    - SKMG INFO: 9×int32 + 4×int16 (NOT 8×int32) with §1.3 field order.
    - Normals: authored NORM chunk; computeVertexNormals() is an anti-pattern (both .msh and .mgn store authored normals).
    - SAT manifest: FORM SMAT → MSGN + SKTI. Callout: SkeletalAppearanceTemplate.cpp:786-1136.
    - LOD: external .lmg (FORM MLOD) + .ldt (FORM LDTB); LDTB distances inside INFO. Callout: LodDistanceTable.cpp:145-168.
    - Add FORM MESH §1.4 SPS/VTXA/INDX layout; indices widened to Uint32 in the toolkit.
    - Append precise provenance callouts; drop bare "AI-proposed" on corrected sections only.

    FILE: docs/02-formats/skeletons-and-animation.md — replace per §4:
    - .sat: FORM SMAT — skeletal APPEARANCE template (NOT an animation list). Callout: SkeletalAppearanceTemplate.cpp.
    - .skt: §1.2 SKTM→000{1,2}: INFO,NAME,PRNT,RPRE,RPST,BPTR,BPRO,[BPMJ],JROR; BPMJ mandatory v0001, absent v0002. Callout: BasicSkeletonTemplate.cpp:151-389,249-275.
    - .ans: §1.6 FORM KFAT (0003 raw quat) | CKAT (0001 compressed). Keys SPARSE (frame-indexed). On-disk quat (w,x,y,z). CKAT QCHN bytes per-CHANNEL. Callout: KeyframeSkeletalAnimationTemplate.cpp:1518-1620 + CompressedKeyframeAnimationTemplate.cpp + CompressedQuaternion.cpp.
    - Per-frame local composition: localToParentRotation = postMul·(animRot·bindPoseRot·preMul); translation additive. Callout: Skeleton.cpp:1274-1279 (crew-verified 2026-06-25).
    - SKTM dual meaning (root FORM of a .skt AND an inner name-list chunk of a .mgn).
    - Append precise callouts; drop bare "AI-proposed" on corrected sections only.

    FILE: docs/03-rendering/shaders-and-fx.md — replace per §4:
    - "Shader-graph editor for .sht" → SSHT/CSHD is a parameter/data file; HLSL lives in the referenced .eft; toolkit maps SSHT slot tags to GLSL uniforms. Callout: StaticShaderTemplate.cpp + CustomizableShaderTemplate.cpp.
    - "DXT must be CPU-decoded" → DXT1/3/5 upload compressed to the GPU via WEBGL_compressed_texture_s3tc (THREE.CompressedTexture); CPU decode is a fallback. Do NOT cite Texture.cpp:115-129 for DDS.
    - "MeshStandardMaterial default" → SWG render uses a custom THREE.ShaderMaterial. NOTE (this phase): for glTF EXPORT the ShaderMaterial is CONVERTED to MeshStandardMaterial (GLTFExporter does not serialize ShaderMaterials) and DXT CompressedTextures are decompressed to RGBA; SWG specular has no glTF-PBR slot.
    - Add CSHD pathways A (material color→uMaterialColor) / C (texture factor→uTexFactor).
    - Append precise callouts; drop bare "AI-proposed" on corrected sections only.

    NOTE: do NOT update docs for formats NOT verified this phase. Fix the DDS Texture.cpp:115-129
    mis-citation everywhere it appears in these three files.
  </action>
  <verify>
    <automated>git diff --name-only docs/ 2>&1; echo "docs-changed:$?"</automated>
  </verify>
  <acceptance_criteria>
    meshes-and-appearances.md: fabricated layouts replaced with synthesis §1; precise callouts; bare "AI-proposed" dropped on corrected sections only.
    skeletons-and-animation.md: .sat/.skt/.ans corrected (sparse per-channel; install()-table decode); SKTM dual meaning + BPMJ v0001/v0002; Skeleton.cpp:1274-1279 composition documented; precise callouts.
    shaders-and-fx.md: shader-graph framing corrected; DXT GPU upload; ShaderMaterial render + glTF-export conversion note; DDS NOT cited to Texture.cpp:115-129.
    No unrelated docs sections modified.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — glTF export + Extract + docs</name>
  <action>Pause for the user to verify glTF export (correct orientation AND chirality on an asymmetric asset, skeleton + animation, textures present), Extract raw bytes, and the corrected docs. Do not auto-advance.</action>
  <what-built>
    glTF (.glb, rigged+animated, textured) export with a BAKED X-mirror on a deep-cloned export scene
    (ShaderMaterial→MeshStandardMaterial + DXT decompress); raw-asset Extract; corrected format docs.
  </what-built>
  <how-to-verify>
    1. Open a loaded (textured, animated) .sat. Note pose/orientation.
    2. Export… → glTF (.glb), check Skeleton + Animation. Note 'Y-up · X-mirror applied'. Export.
    3. Confirm "Exporting glTF…" then "✓ exported {file.glb}".
    4. Open the .glb (gltf.report or Blender 4.x Import → glTF). Confirm: geometry upright; TEXTURED
       (diffuse + normal); NOT inside-out (toggle back-face culling in gltf.report — faces stay lit);
       skeleton present; animation plays.
    5. ASYMMETRIC asset (one-shoulder armor / asymmetric creature): confirm CHIRALITY — the asymmetric
       feature is on the correct side (matches in-game / SIE), normal-map highlights come from the right
       side (tangent.w check), no inside-out shading. THIS is the X-mirror's empirical proof.
    6. Animate an asymmetric limb (one arm): confirm it tracks its correct final position, no mirror-drift
       (the compose-then-mirror Rank-1 check).
    7. Extract… → choose a location → confirm raw bytes written (spot-check size vs source).
    8. Review docs: .sat no longer "Skeleton Animation Template"; CKAT/KFAT sparse per-channel; Skeleton.cpp
       composition documented; precise "(verified … 2026-06-23/25)" callouts; no Texture.cpp:115-129 DDS mis-citation.
    (Optional, deferred: Blender-MCP auto-verification harness — backlog blender-bridge-mcp, Phase 6.)
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 2, or describe any export/docs issues (double-mirror, inside-out faces, inverted normal-map lighting, animation drift, lost textures, docs gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glTF output → user filesystem | Export writes to a user-chosen path; never to the mounted TRE archive or client install |
| Extract… → user filesystem | Writes raw bytes to a user-chosen path; source is read-only TRE mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | export/mirrorScene.ts — coordinate transform | mitigate | X-mirror BAKED on a DEEP-cloned export scene (geometry.clone(), bone clone) — live viewport scene never mutated; LIVE_SCENE_IS_SWG_NATIVE prevents double-apply; no negative-scale root node |
| T-02-21 | Tampering | Extract… path traversal | mitigate | Destination from OS showSaveFilePicker / Electron dialog (user-approved); source bytes from nativeCore.readMountEntry(mountHandle, archiveIndex, entryIndex) — never a raw FS open or path concat |
| T-02-22 | Information Disclosure | glTF export includes all scene geometry | accept | Intentional, user-initiated on a local asset the user loaded from their own TRE |
</threat_model>

<verification>
1. `pnpm --filter @swg/renderer typecheck` exits 0.
2. `pnpm -r test` exits 0 — full Phase-2 suite green + the buildAnimationClip composition assertion.
3. Human-verify: glTF opens upright + correctly handed (asymmetric asset, not inside-out, normal-map
   lighting correct) with skeleton + animation + textures; Extract writes correct raw bytes.
4. docs/ updated: three files changed; precise callouts present; DDS mis-citation gone.
</verification>

<success_criteria>
- ExportDialog ships glTF (.glb, binary, rigged+animated, textured). No COLLADA (absent in three r0.184.0).
- Coordinate transform: X-mirror (flip·M·flip) BAKED into a deep-cloned export scene — positions/normals/
  tangents(incl. w)/winding/bind matrices/hierarchy/animation all consistent; applied exactly once; no
  negative-scale node; no facing rotation. Verified on an asymmetric asset in an external viewer.
- Materials converted ShaderMaterial→MeshStandardMaterial with DXT decompressed; textures survive into the .glb.
- buildAnimationClip composes (Skeleton.cpp L5) THEN mirrors; tracks keyed by stable SKTM bone names.
- Extract… writes raw TRE asset bytes to a user-chosen path; never touches the mount.
- docs corrected with precise "(verified … 2026-06-23/25)" callouts; DDS NOT cited to Texture.cpp:115-129.
- VIEW-04 satisfied: real rigged+animated+textured asset exported to glTF and opened externally without errors.
- Phase 2 complete: VIEW-01 ✓ (02-02), VIEW-02 ✓ (02-03), VIEW-03 ✓ (02-04), VIEW-04 ✓ (this plan).
- Tracked for Phase 6: Blender-MCP bridge (export engine + auto-verification) — backlog blender-bridge-mcp.
</success_criteria>

<output>
Create `.planning/phases/02-3d-mesh-viewport-mvp-proof/02-05-SUMMARY.md` when done.
</output>
