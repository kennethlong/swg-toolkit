---
phase: 02-3d-mesh-viewport-mvp-proof
plan: 05
type: execute
wave: 5
depends_on:
  - 02-03
  - 02-04
files_modified:
  - packages/renderer/src/panels/viewport/ExportDialog.tsx
  - packages/renderer/src/panels/viewport/export/mirrorScene.ts
  - packages/renderer/src/panels/viewport/export/buildAnimationClip.ts
  - packages/renderer/src/panels/viewport/Viewport.tsx
  - packages/renderer/src/panels/ViewportPanel.tsx
  - packages/renderer/src/state/viewportStore.ts
  - docs/02-formats/meshes-and-appearances.md
  - docs/02-formats/skeletons-and-animation.md
  - docs/03-rendering/shaders-and-fx.md
autonomous: false
requirements:
  - VIEW-04

must_haves:
  truths:
    - "User can export the loaded (rigged + animated) mesh to glTF (.glb) via GLTFExporter — the reliable skinned/animated path"
    - "User can export the loaded mesh to COLLADA (.dae) via ColladaExporter — GEOMETRY + MATERIALS ONLY; skeleton/animation are NOT exported (ColladaExporter has no skinning/animation support in three r0.184.0)"
    - "glTF includes the skeleton and animation when present (D-10); COLLADA is geometry+materials only. buildAnimationClip() (glTF path) consumes the CORRECTED 02-04 sparse-key IR — whose rotation tracks are ABSOLUTE LOCAL rotations and translation tracks are LOCAL-space, so the matrix mirror and the closed-form track mirror provably coincide"
    - "The live scene's coordinate convention is EXPLICITLY DECIDED and STATED; the export mirrors to the target convention exactly ONCE (no double-apply)"
    - "Export uses a MATRIX-based X-mirror (flip @ R @ flip, flip=diag(-1,1,1)) applied consistently to geometry vertex buffers, normals (negate x), tangents, triangle winding (reversed), bind/inverse-bind matrices, bone hierarchy, AND animation tracks — on a DEEP-CLONED / purpose-built export scene, never the live scene"
    - "Three.js bone names are stable and match the AnimationClip track paths (bone.name set from SKTM names in 02-02)"
    - "User can extract the raw asset bytes from the mount via Extract… using sourceMountHandle/sourceArchiveIndex/sourceEntryIndex from viewportStore"
    - "docs/ updates replace fabricated layouts with verified synthesis §1 content and add precise 'verified for versions X against files Y (date)' callouts — NOT a blanket caveat drop; the DDS Texture.cpp:115-129 mis-citation is corrected"
  artifacts:
    - path: "packages/renderer/src/panels/viewport/export/mirrorScene.ts"
      provides: "buildExportScene(): deep-clone + matrix-based X-mirror across geometry/normals/tangents/winding/bind matrices/hierarchy/animation"
    - path: "packages/renderer/src/panels/viewport/export/buildAnimationClip.ts"
      provides: "buildAnimationClip(): 02-04 sparse-key IR → THREE.AnimationClip (Quaternion/Vector KeyframeTracks keyed by bone name)"
    - path: "packages/renderer/src/panels/viewport/ExportDialog.tsx"
      provides: "glTF/COLLADA export modal with format radio, include toggles, coordinate note, progress/complete/error states (D-10)"
    - path: "docs/02-formats/meshes-and-appearances.md"
      provides: "Verified MESH/SKMG/SMAT/MLOD layouts from synthesis §1 with precise version+file+date callouts"
    - path: "docs/02-formats/skeletons-and-animation.md"
      provides: "Verified SKTM/KFAT/CKAT layouts from synthesis §1 with precise callouts"
    - path: "docs/03-rendering/shaders-and-fx.md"
      provides: "Verified SSHT/CSHD/PAL/DDS GPU-upload path; DDS NOT cited to Texture.cpp:115-129"
  key_links:
    - from: "ExportDialog.tsx"
      to: "export/mirrorScene.ts + GLTFExporter/ColladaExporter"
      via: "buildExportScene(live) → mirrored deep clone; GLTFExporter.parse(exportScene, ..., { binary:true, animations:[buildAnimationClip()] })"
      pattern: "buildExportScene|GLTFExporter|ColladaExporter"
    - from: "export/buildAnimationClip.ts"
      to: "viewportStore.parsedAnimation (02-04 sparse IR)"
      via: "QuaternionKeyframeTrack('{boneName}.quaternion', times, values) + VectorKeyframeTrack from sparse keys"
      pattern: "QuaternionKeyframeTrack|AnimationClip"
---

<objective>
Ship the glTF and COLLADA export pipeline (VIEW-04, D-10) and the raw-asset Extract action. glTF is the rigged+animated path; COLLADA exports GEOMETRY + MATERIALS ONLY (no rig/animation — ColladaExporter limitation in three r0.184.0). The coordinate transform is a matrix-based X-mirror applied exactly once on a deep-cloned export scene. Correct the AI-distilled format docs with the verified synthesis §1 ground truth using precise version+file+date callouts.

Purpose: Closes VIEW-04 — the user takes a rigged+animated SWG asset out of the tool in a standard format that opens in external DCC tools. This plan is GATED on the CORRECTED 02-04 (buildAnimationClip consumes 02-04's IR; codifying the wrong CKAT layout into "verified" docs is the exact de-anchoring failure to avoid). The docs update retires the "AI-proposed" caveat ONLY for formats proven this phase, with explicit provenance.

Output: mirrorScene.ts (deep-clone + matrix mirror), buildAnimationClip.ts, ExportDialog.tsx, Export…/Extract… wiring; docs corrections with precise callouts.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-05-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Code\SWG-Toolkit\.planning\PROJECT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-CONTEXT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-RESEARCH.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-SYNTHESIS.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-UI-SPEC.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-PATTERNS.md

<interfaces>
<!-- Three.js exporters (bundled with three@0.184.0) -->
<!-- import { GLTFExporter } from 'three/examples/jsm/exporters/GLTFExporter.js' -->
<!-- new GLTFExporter().parse(scene, (result)=>{ ArrayBuffer if binary }, (err)=>{}, { binary:true, animations:[clip] }) -->
<!-- import { ColladaExporter } from 'three/examples/jsm/exporters/ColladaExporter.js' -->
<!-- new ColladaExporter().parse(scene, (result)=>{ {data, textures} }, {}) — GEOMETRY + MATERIALS ONLY; ColladaExporter exports NO skeleton/animation in three r0.184.0 -->

<!-- COORDINATE CONVENTION — the double-apply decision (STATE this in the code + docs): -->
<!--   DECISION: the LIVE R3F scene built in 02-02 keeps SWG-native coordinates (left-handed, Y-up), -->
<!--   i.e. positions/quaternions are loaded as-is from disk WITHOUT mirroring (the viewport just orbits it). -->
<!--   Therefore the EXPORT mirrors exactly ONCE (live scene is NOT pre-converted). If 02-02 ever pre-converts, -->
<!--   this export must NOT mirror again — verify the live-scene convention before applying the mirror. -->
<!-- MATRIX X-mirror (LOCKED per synthesis §6, = Blender flip @ R @ flip, flip=diag(-1,1,1), export_animation.py:154-162): -->
<!--   flip = Matrix4 diag(-1,1,1,1). For each transform: M' = flip · M · flip. -->
<!--   Geometry: position.applyMatrix4(flip) (x→-x); normal.x→-normal.x; tangent.x→-tangent.x; REVERSE triangle winding (swap index b,c). -->
<!--   Bind/inverse-bind matrices: M' = flip·M·flip. Bone local transforms: same conjugation. Animation quats/positions: same. -->
<!--   For glTF additionally reorder quat IR (w,x,y,z) → (x,y,z,w). -->

<!-- buildAnimationClip: from viewportStore.parsedAnimation (02-04 CORRECTED sparse IR): -->
<!--   QuaternionKeyframeTrack('{bone.name}.quaternion', times[], values[]) + VectorKeyframeTrack('{bone.name}.position', ...) -->
<!--   times = frameIndex/fps; bone.name MUST equal the SKTM bone name set in 02-02 SkinnedMeshView. -->

<!-- Extract: nativeCore.readMountEntry(sourceMountHandle, sourceArchiveIndex, sourceEntryIndex) → ArrayBuffer; -->
<!--   these fields are in viewportStore (added in 02-02). Write via Electron dialog.showSaveDialog / showSaveFilePicker. -->

<!-- From viewportStore: resolution, parsedAnimation, parsedSkeleton, sourceMountHandle/sourceArchiveIndex/sourceEntryIndex/sourceEntryPath -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mirrorScene + buildAnimationClip + ExportDialog + Extract… + wire into ViewportPanel</name>
  <files>
    packages/renderer/src/panels/viewport/export/mirrorScene.ts,
    packages/renderer/src/panels/viewport/export/buildAnimationClip.ts,
    packages/renderer/src/panels/viewport/ExportDialog.tsx,
    packages/renderer/src/panels/viewport/Viewport.tsx,
    packages/renderer/src/panels/ViewportPanel.tsx,
    packages/renderer/src/state/viewportStore.ts
  </files>
  <read_first>
    packages/renderer/src/panels/viewport/SkinnedMeshView.tsx — how the live scene is built in 02-02 (bone.name = SKTM name; whether positions/quats are loaded RAW = SWG-native or pre-converted) — CONFIRM the live-scene convention before writing the mirror
    packages/renderer/src/state/viewportStore.ts — resolution/parsedAnimation/parsedSkeleton + sourceMountHandle/sourceArchiveIndex/sourceEntryIndex/sourceEntryPath (added in 02-02); add setLastExportFilename action
    packages/renderer/src/shared/AsyncProgress.tsx — progress pattern for "Exporting {format}…"
    packages/renderer/src/shared/VerificationStatus.tsx — pass/fail/warn for export complete/failure
    packages/renderer/src/panels/ViewportPanel.tsx — header button area (add Export…/Extract…); actionBtnStyle (PATTERNS.md 589-603)
    packages/renderer/src/panels/viewport/Viewport.tsx — mount ExportDialog when open
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-UI-SPEC.md — Surface 7 (ExportDialog anatomy: format radio glTF(.glb)/COLLADA(.dae); include toggles Skeleton/Animation; current-LOD note; coordinate note 'Y-up · X-mirror applied' --color-info; Cancel/Export; states); Copywriting Contract (Export… / Extract…)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § ExportDialog.tsx (modal; AsyncProgress; VerificationStatus; dialog button styles)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §6 coordinate convention (matrix X-mirror flip@R@flip; positions (x,y,z)→(-x,y,z); quat (w,x,y,z)→(w,x,-y,-z); glTF reorder (x,-y,-z,w))
    ../swg-blender-plugin/swg_pipeline/export_animation.py lines 154-162 — flip @ R @ flip reference (flip = diag(-1,1,1))
    ../swg-blender-plugin/swg_pipeline/coords.py lines 13-26 — coordinate reference
  </read_first>
  <action>
    FILE: export/mirrorScene.ts — buildExportScene(liveScene: THREE.Object3D, opts:{ includeSkeleton, includeAnimation }) -> THREE.Object3D.
    1. CONVENTION CHECK: this function assumes the live scene is SWG-native (NOT pre-converted) per the locked decision in <interfaces>. If 02-02's SkinnedMeshView was found to pre-convert, mirror is a no-op here — assert the convention with a comment and a single source-of-truth constant LIVE_SCENE_IS_SWG_NATIVE=true.
    2. DEEP-CLONE: build a purpose-built export scene by deep-cloning meshes (clone geometry via geometry.clone() so vertex buffers are NOT shared with the live scene — a shallow clone()+position.x mutation would corrupt the viewport). Clone the skeleton/bones too.
    3. MATRIX X-mirror: flip = new THREE.Matrix4().makeScale(-1,1,1). For each mesh geometry: apply flip to position attribute (x→-x); negate normal.x; negate tangent.x (if present); REVERSE triangle winding (swap the 2nd/3rd index of every triangle in the Uint32 index buffer) so the det=-1 mirror does not leave faces inside-out. For each bone: localMatrix' = flip · localMatrix · flip (conjugation); recompute position/quaternion from the conjugated matrix. For bind/inverse-bind matrices on the SkinnedMesh: M' = flip · M · flip. Keep bone.name unchanged (must match AnimationClip tracks).
    4. Return the mirrored export scene. The live scene is untouched.

    FILE: export/buildAnimationClip.ts — buildAnimationClip(parsedAnimation, parsedSkeleton, fps) -> THREE.AnimationClip. Consume the CORRECTED 02-04 SPARSE IR (rotation keys are ABSOLUTE LOCAL rotations; translation keys are LOCAL-space — this is the prerequisite that makes the closed-form track mirror equal the matrix conjugation; assert it against the 02-04 IR contract). For each animated joint: read its sparse rotation keys (frames[], (w,x,y,z) quats[]) → THREE.QuaternionKeyframeTrack('{bone.name}.quaternion', times=frames.map(f=>f/fps), values=flattened (x,y,z,w) reordered). Translation keys → THREE.VectorKeyframeTrack('{bone.name}.position', times, values). bone.name MUST equal the SKTM bone name set in 02-02 (track path binding). Apply the SAME X-mirror to the track values when the clip is for the mirrored export scene: quat (w,x,y,z)→(w,x,-y,-z) then glTF reorder (x,-y,-z,w); translation x→-x. new THREE.AnimationClip('take_001', -1, tracks).

    FILE: ExportDialog.tsx — per PATTERNS.md § ExportDialog.tsx + UI-SPEC Surface 7. Modal overlay (z-index high, --color-bg, --radius-sm border). Title "Export mesh" (--text-md weight 600 unless 00-UI-SPEC reserved --text-lg). Props isOpen/onClose/liveSceneRef/hasSkeleton/hasAnimation/currentLod.
    Format radio: 'glTF (.glb)' (default; mono --text-sm) | 'COLLADA (.dae)'. For COLLADA, show a small note "geometry + materials only — no rig/animation (three r0.184.0)" (--color-text-faint) and DISABLE (grey) the Skeleton + Animation toggles while COLLADA is selected. For glTF, Include toggles: Skeleton (checked if hasSkeleton), Animation (checked if hasAnimation), disabled when unavailable. Current-LOD note 'exports LOD {n}' (mono --text-xs). Coordinate note 'Y-up · X-mirror applied' (--color-info). Cancel (secondary) / Export (primary --color-accent).
    Export handler (async): set exporting → AsyncProgress "Exporting {format}…"; disable buttons. exportScene = buildExportScene(liveSceneRef.current, { includeSkeleton, includeAnimation }). animClip = includeAnimation ? buildAnimationClip(parsedAnimation, parsedSkeleton, fps) : null. glTF: new GLTFExporter().parse(exportScene, (result)=> downloadBlob(result, '.glb'), onError, { binary:true, animations: animClip ? [animClip] : [] }). COLLADA: new ColladaExporter().parse(exportScene, (result)=> downloadText(result.data, '.dae'), {}) — geometry+materials only; force includeSkeleton/includeAnimation OFF for the COLLADA path (ColladaExporter ignores skeleton/animation in r0.184.0). On success: close + setLastExportFilename + VerificationStatus pass "✓ exported {filename}". On error: VerificationStatus fail "Export failed — {reason}" + Retry; dialog stays open.

    Extract… action (in ExportDialog or a small handler in Viewport): read sourceMountHandle/sourceArchiveIndex/sourceEntryIndex from viewportStore; get a save path via Electron dialog.showSaveDialog / showSaveFilePicker; nativeCore.readMountEntry(mountHandle, archiveIndex, entryIndex) → ArrayBuffer; write to the chosen path. Additive only (new file at user-chosen location) — never overwrites mounted archive bytes (D-10). Show "✓ extracted {filename}".

    FILE: viewportStore.ts — add lastExportFilename (string|null) + setLastExportFilename.

    FILE: Viewport.tsx — mount ExportDialog when exportDialogOpen (local state); pass liveSceneRef (the R3F scene root from useThree, captured in 02-02's view).

    FILE: ViewportPanel.tsx — add Export… and Extract… buttons in the header action area (actionBtnStyle). Both disabled when loadStatus.kind !== 'done'. Text labels (not icon-only) per Surface 7. Export… → exportDialogOpen=true; Extract… → extract flow.
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer typecheck 2>&1 | tail -10; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    Renderer TypeScript compiles without errors.
    mirrorScene.ts deep-clones geometry (geometry.clone(), not shared refs); applies matrix X-mirror to positions, normal.x, tangent.x, REVERSES triangle winding, and conjugates bind/inverse-bind + bone matrices; live scene untouched.
    A single-source LIVE_SCENE_IS_SWG_NATIVE constant documents the no-double-apply decision; mirror applied exactly once.
    buildAnimationClip consumes the 02-04 sparse IR and keys tracks by bone.name (matching SKTM names); applies the same mirror to track values for the export scene.
    ExportDialog: glTF default rigged path; COLLADA shows the geometry-only note AND disables the Skeleton/Animation toggles; coordinate note 'Y-up · X-mirror applied' (--color-info); AsyncProgress during export; VerificationStatus pass/fail+Retry.
    GLTFExporter.parse called with binary:true and animations when animation present; ColladaExporter.parse called for COLLADA (geometry+materials only — no skeleton/animation in r0.184.0).
    Extract… uses sourceMountHandle/archiveIndex/entryIndex from viewportStore + readMountEntry; writes to a user-chosen path; never touches the mount.
    Export…/Extract… buttons in ViewportPanel header, disabled when no asset loaded.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update AI-distilled format docs with verified synthesis §1 layouts + precise provenance callouts</name>
  <files>
    docs/02-formats/meshes-and-appearances.md,
    docs/02-formats/skeletons-and-animation.md,
    docs/03-rendering/shaders-and-fx.md
  </files>
  <read_first>
    docs/02-formats/meshes-and-appearances.md — ENTIRE FILE; identify every fabricated layout claim per synthesis §4
    docs/02-formats/skeletons-and-animation.md — ENTIRE FILE; identify fabricated claims per synthesis §4
    docs/03-rendering/shaders-and-fx.md — ENTIRE FILE; identify fabricated claims (shader-graph framing, CPU-only DXT, MeshStandardMaterial default)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1 LOCKED layouts (use EXACTLY; §1.3 SKMG INFO 9×int32+4×int16; §1.6 CKAT/KFAT sparse per-channel + install()-table decode); §4 Doc Corrections; §2 rendering decisions
    docs/00-overview/source-provenance.md — the "AI-proposed hypothesis" disclaimer format
  </read_first>
  <action>
    AGENTS.md convention: update docs when a layout is verified. Apply ONLY for formats verified this phase. Do NOT blanket-drop "AI-proposed" — replace each corrected section's caveat with a PRECISE provenance callout of the form: "(verified for versions {X} against {swg-client-v2 File.cpp:lines} + real asset {fixture}, 2026-06-23)".

    FILE: docs/02-formats/meshes-and-appearances.md — replace fabricated sections with synthesis §1:
    - Skin indices: "TWHD/TWDT are variable-count int32 (TWDT total = transformWeightDataCount from SKMG INFO); a fixed vec4 skinIndex/skinWeight is DERIVED by the toolkit's C++ de-index+normalize pass — not on disk." Callout: SkeletalMeshGeneratorTemplate.cpp:2247-2360,2331-2343, verified 2026-06-23.
    - SKMG INFO: document as 9×int32 + 4×int16 (NOT 8×int32) with the verified field order from §1.3.
    - Normals: "authored NORM chunk; computeVertexNormals() is an anti-pattern — both .msh and .mgn store authored normals."
    - SAT manifest: "FORM SMAT → MSGN (mesh-gen paths) + SKTI (skeleton, attachmentTransformName pairs)." Callout: SkeletalAppearanceTemplate.cpp:786-1136.
    - LOD: "external file graph — .lmg (FORM MLOD) + .ldt (FORM LDTB); LDTB distances live INSIDE INFO (int16 levelCount + per-level (min,max) float32, stored as sqrt distances, squared at runtime). NOT embedded in mesh bytes." Callout: LodDistanceTable.cpp:145-168.
    - Add the FORM MESH §1.4 SPS/VTXA/INDX layout; note indices are widened to Uint32 in the toolkit.
    - Add the .apt static-appearance composed path.
    - Append precise provenance callouts; drop the bare "AI-proposed" prefix ONLY on corrected sections.

    FILE: docs/02-formats/skeletons-and-animation.md — replace per §4:
    - .sat: "FORM SMAT — skeletal APPEARANCE template referencing mesh generators + skeleton templates (NOT an animation list)." Callout: SkeletalAppearanceTemplate.cpp.
    - .skt: synthesis §1.2 SKTM→000{1,2}: INFO,NAME,PRNT,RPRE,RPST,BPTR,BPRO,[BPMJ],JROR. Note BPMJ is MANDATORY in v0001, ABSENT in v0002 (version-branch). Callout: BasicSkeletonTemplate.cpp:151-389,280-286,363-390.
    - .ans: synthesis §1.6: "FORM KFAT (0003 raw quat, int32 widths) | CKAT (0001 compressed, int16 widths). Keys are SPARSE (frame-indexed). On-disk quat (w,x,y,z). CKAT QCHN format bytes are per-CHANNEL (not per-frame). Compressed-quaternion decode = port of CompressedQuaternion::install()+doExpand(); the format byte encodes precision level AND base index." Callout: KeyframeSkeletalAnimationTemplate.cpp:1518-1620 + CompressedKeyframeAnimationTemplate.cpp:1198-1313,553-594 + CompressedQuaternion.cpp:82-122,156-228,370-419.
    - SKTM dual meaning note (delta #7): root FORM of a .skt AND an inner name-list chunk of a .mgn.
    - Append precise callouts; drop bare "AI-proposed" on corrected sections only.

    FILE: docs/03-rendering/shaders-and-fx.md — replace per §4:
    - "Shader-graph editor for .sht" → "SSHT/CSHD is a parameter/data file; HLSL lives in the referenced .eft. The toolkit maps SSHT slot tags (MAIN/NRML/CNRM/SPEC/EMIS/ENVM/MASK) to GLSL uniforms; it does NOT parse HLSL. SSHT TXMS entries are nested FORM TXM → FORM 000x → DATA then TextureList::fetch." Callout: StaticShaderTemplate.cpp:32-36,123-128,482-565 + CustomizableShaderTemplate.cpp:1246-1286.
    - "DXT must be CPU-decoded" → "DXT1/3/5 upload compressed to the GPU via WEBGL_compressed_texture_s3tc (THREE.CompressedTexture). CPU decode is a fallback only for DXT2/4 or when S3TC is absent." Callout: standard Microsoft DDS spec (do NOT cite Texture.cpp:115-129 — that is a format-conversion registration table, not a DDS reader). §2.
    - "MeshStandardMaterial default" → "SWG materials use a custom THREE.ShaderMaterial for the texture-factor multiply (final = texture(MAIN,uv) * uMaterialColor * uTexFactor) and multi-map parity; material.skinning was removed in three r140 — skinning auto-enables from skinIndex/skinWeight attributes + a bound Skeleton." §2.
    - Add CSHD pathways A (material color → uMaterialColor) / B (texture swap) / C (texture factor → uTexFactor) — A and C target DIFFERENT uniforms. §1.7.
    - Append precise callouts; drop bare "AI-proposed" on corrected sections only.

    NOTE: do NOT update docs for formats NOT verified this phase (terrain, world snapshots, audio, etc.) — they keep their AI-proposed caveats. Fix the DDS Texture.cpp:115-129 mis-citation everywhere it appears in these three files.
  </action>
  <verify>
    <automated>git diff --name-only docs/ 2>&1; echo "docs-changed:$?"</automated>
  </verify>
  <acceptance_criteria>
    docs/02-formats/meshes-and-appearances.md: fabricated layouts replaced with synthesis §1 (SKMG INFO 9×int32+4×int16; TWDT total from INFO; LDTB distances inside INFO); precise version+file+date callouts added; bare "AI-proposed" dropped on corrected sections only.
    docs/02-formats/skeletons-and-animation.md: .sat/.skt/.ans corrected (CKAT/KFAT sparse per-channel; CKAT format bytes per-channel; install()-table decode); SKTM dual meaning + BPMJ v0001/v0002 documented; precise callouts.
    docs/03-rendering/shaders-and-fx.md: shader-graph framing corrected; DXT GPU upload documented; ShaderMaterial vs MeshStandardMaterial + material.skinning-removed rationale; CSHD A/C distinct uniforms; DDS NOT cited to Texture.cpp:115-129.
    No unrelated docs sections modified. All three files internally consistent.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — glTF/COLLADA export + Extract + docs</name>
  <action>Pause for the user to verify glTF/COLLADA export (correct orientation on an asymmetric asset, skeleton + animation), Extract raw bytes, and the corrected docs per the how-to-verify steps. Do not auto-advance.</action>
  <what-built>
    glTF (rigged) + COLLADA (geometry+materials only) export with a matrix-based X-mirror on a deep-cloned export scene; raw-asset Extract via viewportStore source-entry fields; corrected format documentation with precise provenance callouts.
  </what-built>
  <how-to-verify>
    1. Open a loaded (textured, animated) .sat in the viewport. Note the current pose/orientation.
    2. Click Export…; the ExportDialog opens. Select glTF (.glb) (default), check Skeleton + Animation. Note the coordinate note "Y-up · X-mirror applied". Click Export.
    3. Confirm "Exporting glTF…" then "✓ exported {file.glb}".
    4. Open the .glb in a glTF viewer (gltf.report or Blender 4.x Import → glTF). Confirm: geometry visible and correctly oriented (NOT double-mirrored, NOT inside-out — faces shade correctly, winding reversed properly); skeleton present; animation plays.
    5. Validate with an ASYMMETRIC asset (e.g. a one-shoulder-pad armor or a creature with an asymmetric feature): confirm the export is mirrored consistently (the asymmetric feature is on the expected side, no inside-out shading).
    6. Repeat with COLLADA (.dae) → Blender Import → Collada. Geometry + materials should be correct; the rig and animation are NOT exported by ColladaExporter (expected — glTF is the rigged path).
    7. Click Extract… with the same asset loaded. OS save dialog appears; choose a location. Confirm a file is written with the correct extension and raw bytes match the source (spot-check size).
    8. Review docs/02-formats/skeletons-and-animation.md: .sat no longer "Skeleton Animation Template"; CKAT/KFAT documented as sparse per-channel with the install()-table decode; precise "(verified … 2026-06-23)" callouts present; no Texture.cpp:115-129 DDS mis-citation.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 2, or describe any export/docs issues (double-mirror, inside-out faces, wrong bone tracks, docs gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glTF/COLLADA output → user filesystem | Export writes to a user-chosen path; never to the mounted TRE archive or client install |
| Extract… → user filesystem | Writes raw bytes to a user-chosen path; source is read-only TRE mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | export/mirrorScene.ts — coordinate transform | mitigate | X-mirror applied on a DEEP-cloned export scene (geometry.clone(), bone clone) — the live viewport scene is never mutated; LIVE_SCENE_IS_SWG_NATIVE constant prevents double-apply |
| T-02-21 | Tampering | Extract… path traversal | mitigate | Destination path from OS showSaveFilePicker / Electron dialog.showSaveDialog (user-approved only); source bytes from nativeCore.readMountEntry(mountHandle, archiveIndex, entryIndex) — never a raw FS open or path concatenation |
| T-02-22 | Information Disclosure | glTF export includes all scene geometry | accept | Intentional, user-initiated (explicit Export… button) on a local asset the user loaded from their own TRE; no sensitive data |
| T-02-23 | Tampering | ColladaExporter output written as text | accept | ColladaExporter produces XML text saved to disk, not injected into the DOM — no XSS surface |
</threat_model>

<verification>
1. `pnpm --filter @swg/renderer typecheck` exits 0 — ExportDialog + mirrorScene + buildAnimationClip + ViewportPanel compile.
2. `pnpm -r test` exits 0 — full Phase-2 suite green (thirteen formats, registry-coverage sweep).
3. Human-verify: glTF opens correctly oriented (asymmetric asset validated, not inside-out) with skeleton + animation; COLLADA opens (geometry + materials only); Extract writes correct raw bytes.
4. docs/ updated: three files changed per git diff; precise callouts present; DDS mis-citation gone.
</verification>

<success_criteria>
- ExportDialog ships glTF (.glb, binary, rigged+animated) + COLLADA (.dae, geometry+materials only — no rig/animation in r0.184.0).
- Coordinate transform: matrix X-mirror (flip·M·flip) on a deep-cloned export scene; positions/normals/tangents/winding/bind matrices/hierarchy/animation all transformed consistently; applied exactly once (no double-apply). Verified on an asymmetric asset in an external viewer.
- buildAnimationClip consumes the CORRECTED 02-04 sparse IR; tracks keyed by stable SKTM bone names.
- Extract… writes raw TRE asset bytes (from viewportStore source-entry fields) to a user-chosen path; never touches the mount.
- docs corrected with precise "(verified for versions X against files Y, 2026-06-23)" callouts; DDS NOT cited to Texture.cpp:115-129.
- VIEW-04 satisfied: real rigged+animated asset exported to glTF (rig + animation) and to COLLADA (geometry + materials) and opened externally without errors.
- Phase 2 complete: VIEW-01 ✓ (02-02 static+skinned), VIEW-02 ✓ (02-03), VIEW-03 ✓ (02-04), VIEW-04 ✓ (this plan).
</success_criteria>

<output>
Create `.planning/phases/02-3d-mesh-viewport-mvp-proof/02-05-SUMMARY.md` when done.
</output>
