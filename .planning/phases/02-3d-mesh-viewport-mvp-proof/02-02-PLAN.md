---
phase: 02-3d-mesh-viewport-mvp-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/native-core/modules/core/formats/SkeletalMeshGen.h
  - packages/native-core/modules/core/formats/SkeletalMeshGen.cpp
  - packages/native-core/modules/core/formats/Skeleton.h
  - packages/native-core/modules/core/formats/Skeleton.cpp
  - packages/native-core/modules/core/formats/SkeletalAppearance.h
  - packages/native-core/modules/core/formats/SkeletalAppearance.cpp
  - packages/native-core/modules/core/formats/StaticAppearance.h
  - packages/native-core/modules/core/formats/StaticAppearance.cpp
  - packages/native-core/modules/core/CMakeLists.txt
  - packages/native-core/src/mesh_binding.cpp
  - packages/native-core/src/addon.cpp
  - packages/harness/test/mesh-roundtrip.test.ts
  - packages/renderer/src/state/viewportStore.ts
  - packages/renderer/src/panels/viewport/resolver/appearanceResolver.ts
  - packages/renderer/src/panels/viewport/Viewport.tsx
  - packages/renderer/src/panels/viewport/SkinnedMeshView.tsx
  - packages/renderer/src/panels/viewport/StaticMeshView.tsx
  - packages/renderer/src/panels/viewport/LodPicker.tsx
  - packages/renderer/src/panels/viewport/AppearancePanel.tsx
  - packages/renderer/src/panels/ViewportPanel.tsx
autonomous: false
requirements:
  - VIEW-01

must_haves:
  truths:
    - "User can open a real STATIC .msh and see it render in the R3F viewport via StaticMeshView (VIEW-01 static path; non-skinned branch — D-01 calls .msh the easy subset, and it MUST ship)"
    - "User can open a real .sat file and see the skinned mesh render in the R3F viewport with an orbit camera (VIEW-01 skinned path; D-01: skinned .mgn leads as the real proof)"
    - "User can open a composed STATIC .apt and see the resolved static appearance render (D-03 smart-open static-composed path)"
    - "User can open a leaf .mgn file and see it render standalone in bind pose (VIEW-01 leaf path; D-03 smart-open by type)"
    - "parseSkeletalMesh() returns MeshParseResult with boneNames[], vec4 skinIndex/skinWeight arrays (TWHD/TWDT normalized; XFNM→bone remap applied C++-side with resolved boneOrder), and de-indexed Uint32-indexed attribute arrays — all pre-bridge"
    - "parseSkeleton() returns SkeletonParseResult with bind-pose translations and rotations in (w,x,y,z) IR order; v0001 enters+skips mandatory BPMJ, v0002 has no BPMJ (version-branched)"
    - "parseSkeletalAppearance() returns SMAT MSGN mesh-gen paths + SKTI skeleton pairs; parseStaticAppearance() returns the APT single-string redirectTarget (resolver dispatches it by extension)"
    - "CORE-05 byte-exact round-trip passes for .mgn (SKMG) + .skt (SKTM v0001 AND v0002) + .sat (SMAT) + .apt (APT)"
    - "Appearance resolver returns missing[] list (not a crash) when a dependency is absent from the mount (D-04)"
    - "Multi-PSDT meshes render ALL shader groups (not just shaderGroups[0])"
    - "Viewport stats overlay shows verts/tris/draws and a 'binary' badge (no-JSON-for-geometry); the label is NOT 'zero-copy'"
  artifacts:
    - path: "packages/native-core/modules/core/formats/SkeletalMeshGen.h"
      provides: "parseSkeletalMesh() returning SKMG result (INFO 9×int32+4×int16) with de-indexed + vec4-normalized geometry"
    - path: "packages/native-core/modules/core/formats/Skeleton.h"
      provides: "parseSkeleton() returning SKTM rest-pose bone array; v0001/v0002 version-branched (BPMJ)"
    - path: "packages/native-core/modules/core/formats/SkeletalAppearance.h"
      provides: "parseSkeletalAppearance() returning SMAT MSGN + SKTI lists"
    - path: "packages/native-core/modules/core/formats/StaticAppearance.h"
      provides: "parseStaticAppearance() returning the APT redirectTarget string (composed-static smart-open; resolver dispatches by extension)"
    - path: "packages/renderer/src/panels/viewport/resolver/appearanceResolver.ts"
      provides: "Cross-TRE SMAT→MLOD→SKMG→SKTM→SSHT + APT→MESH dependency graph walker; returns AppearanceResolutionResult with missing[]"
    - path: "packages/renderer/src/panels/viewport/StaticMeshView.tsx"
      provides: "Non-skinned THREE.Mesh render path for static .msh / .apt"
    - path: "packages/renderer/src/panels/viewport/SkinnedMeshView.tsx"
      provides: "Three.js SkinnedMesh + Skeleton bound to parsed geometry + skeleton data (multi-group)"
  key_links:
    - from: "resolver/appearanceResolver.ts"
      to: "nativeCore.parseSkeletalAppearance / parseStaticAppearance / parseSkeleton / parseSkeletalMesh / parseMesh / parseMeshLod"
      via: "TS async graph walk; resolves by name via TRE VFS; partial resolution on missing deps (D-04)"
      pattern: "resolveAppearance"
    - from: "SkinnedMeshView.tsx"
      to: "SkinnedMesh + Skeleton"
      via: "THREE.SkinnedMesh + THREE.Skeleton; skinning auto-enables from skinIndex/skinWeight attributes + bound skeleton (NO material.skinning — removed r140)"
      pattern: "SkinnedMesh.*Skeleton"
    - from: "packages/harness/test/mesh-roundtrip.test.ts"
      to: "fixtureRegistry.ts"
      via: "registerFormat('mesh-skmg' / 'mesh-sktm-v1' / 'mesh-sktm-v2' / 'mesh-smat' / 'mesh-apt')"
      pattern: "registerFormat.*mesh-"
---

<objective>
Land the skinned mesh + skeleton + appearance parsers in C++ (SKMG, SKTM, SMAT, APT) with the full de-index + vec4-normalize pre-bridge passes. Wire the TypeScript appearance resolver (cross-TRE composed + leaf walk with D-04 partial-resolution). Ship BOTH render paths: StaticMeshView (non-skinned .msh / .apt) and SkinnedMeshView (rigged .mgn / .sat). This is the VIEW-01 proof for BOTH the static and skinned cases.

Purpose: D-01 calls static `.msh` the easy subset and skinned `.mgn` the real proof — both must ship VIEW-01. The skinned path is the zero-GC stress test; the static path is the off-by-one VTXA-stride canary (a stride error parses clean but renders garbage). When this plan completes, the user can open a static `.msh`/`.apt`, a composed `.sat`, or a leaf `.mgn`.

Output: Four C++ parsers (SkeletalMeshGen/Skeleton/SkeletalAppearance/StaticAppearance), CORE-05 registrations for .mgn/.skt(v1+v2)/.sat/.apt, TS appearance resolver (composed + static-composed + leaf), R3F Canvas + StaticMeshView + SkinnedMeshView inside ViewportPanel, viewportStore Zustand state (with mountHandle + source entry path), LodPicker, AppearancePanel.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-02-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Code\SWG-Toolkit\.planning\PROJECT.md
@D:\Code\SWG-Toolkit\.planning\ROADMAP.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-CONTEXT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-RESEARCH.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-SYNTHESIS.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-UI-SPEC.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-PATTERNS.md

<interfaces>
<!-- From 02-01-PLAN output (geometry/DeIndex.h) -->
<!-- deIndex(posPool, posCount, normPool, normCount, pidx, pidxCount, nidx, nidxCount, uvs, uvCount, prim, primCount) -> DeIndexedBuffers (Uint32 indices) -->
<!-- normalizeSkinWeightsInto(out, twhd, vertexCount, twdt_xform, twdtCount, twdt_weight, xfnm, xfnmCount, boneOrder) — boneOrder maps XFNM name → resolved skeleton bone index -->

<!-- From packages/native-core/modules/core/iff/Iff.h -->
<!-- parseIff(data, size) -> IffParseResult; serializeIff(result, data, size) -> vector<uint8_t> -->

<!-- From packages/contracts/src/mesh.ts (02-01 output) -->
<!-- MeshParseResult: { formatTag:'MESH'|'SKMG', shaderGroups:MeshShaderGroup[] (each with MeshAttributeSlice byte offsets, Uint32 indices), boneNames?, weightsTruncated?, roundTrip } -->
<!-- From packages/contracts/src/skeleton.ts -->
<!-- SkeletonParseResult: { version, bones:BoneNode[], roundTrip } -->
<!-- BoneNode: { name, parentIndex, bindTranslation:[x,y,z], bindRotation:[w,x,y,z] } -- quat IR order is (w,x,y,z) -->

<!-- Three.js r0.184.0 skinning: material.skinning was REMOVED in r140 — DO NOT set it. -->
<!--   Skinning auto-enables from skinIndex/skinWeight BufferAttributes + a SkinnedMesh bound to a Skeleton. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C++ SKMG + SKTM + SMAT + APT parsers + CORE-05 fixtures (.mgn/.skt v1+v2/.sat/.apt)</name>
  <files>
    packages/native-core/modules/core/formats/SkeletalMeshGen.h,
    packages/native-core/modules/core/formats/SkeletalMeshGen.cpp,
    packages/native-core/modules/core/formats/Skeleton.h,
    packages/native-core/modules/core/formats/Skeleton.cpp,
    packages/native-core/modules/core/formats/SkeletalAppearance.h,
    packages/native-core/modules/core/formats/SkeletalAppearance.cpp,
    packages/native-core/modules/core/formats/StaticAppearance.h,
    packages/native-core/modules/core/formats/StaticAppearance.cpp,
    packages/native-core/modules/core/CMakeLists.txt,
    packages/native-core/src/mesh_binding.cpp,
    packages/native-core/src/addon.cpp,
    packages/harness/test/mesh-roundtrip.test.ts
  </files>
  <behavior>
    - parseSkeletalMesh on a real .mgn FORM SKMG v0003/v0004 returns shaderGroups (all PSDT groups) with vertexCount > 0 and boneNames.length > 0
    - parseSkeletalMesh INFO reads 9×int32 then 4×int16 (the transformWeightDataCount field from INFO is the canonical TWDT entry count)
    - The geometry ArrayBuffer from parseSkeletalMesh contains vec4 skinIndices + skinWeights (4 per vertex; renderer-side derivation from variable TWDT, NOT a file limit)
    - normalizeSkinWeightsInto: each skinWeight vec4 sums to 1.0; all-zero-influence vertex gets weight (1,0,0,0) on a root bone (no origin collapse)
    - parseSkeleton on a real .skt v0001 returns bones.length > 0 AND advances IFF read position past the mandatory BPMJ chunk
    - parseSkeleton on a real .skt v0002 returns bones.length > 0 with NO BPMJ chunk present (version-branched)
    - parseSkeletalAppearance on a real .sat FORM SMAT returns msgnPaths and sktiPairs both non-empty
    - parseStaticAppearance on a real .apt returns a non-empty redirectTarget (the redirector string; not itself ending in '.apt')
    - serializeIff(parseIff(.mgn|.skt|.sat|.apt bytes)) === original bytes byte-identical (SC-5 gate) for each
    - FormatParseError thrown when the inner SKMG SKTM name-list chunk is mistakenly passed to parseSkeleton (the two SKTM meanings are distinct per synthesis delta #7)
  </behavior>
  <read_first>
    packages/native-core/modules/core/formats/Mesh.h — existing format parser shape (namespace/error/function signature)
    packages/native-core/modules/core/geometry/DeIndex.h — normalizeSkinWeightsInto signature + DeIndexedBuffers struct (Uint32 indices, from 02-01)
    packages/native-core/src/mesh_binding.cpp — extractBytes helper + ParseMesh shape to extend
    packages/native-core/src/addon.cpp — current Init() block to extend
    packages/harness/test/mesh-roundtrip.test.ts — current test file to extend
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.2 SKTM chunk set (INFO,NAME,PRNT,RPRE,RPST,BPTR,BPRO,[BPMJ],JROR); §1.3 SKMG INFO=9×int32+4×int16 (LOCKED, verified order) + TWDT total from INFO; §1.1 SMAT chunk set (MSGN/SKTI/INFO) — LOCKED axioms; do NOT re-derive from docs/
    ../swg-client-v2/src/engine/client/library/clientSkeletalAnimation/src/shared/appearance/SkeletalMeshGeneratorTemplate.cpp lines 2247-2360 (INFO load_0002 = 9×int32+4×int16, verified 2026-06-23), 2331-2343 (TWDT reads transformWeightDataCount pairs)
    ../swg-client-v2/src/engine/client/library/clientSkeletalAnimation/src/shared/appearance/BasicSkeletonTemplate.cpp lines 151-389 — SKTM oracle; 280-286 (v0001 BPMJ MANDATORY plain enterChunk); 363-390 (v0002 NO BPMJ)
    ../swg-client-v2/src/engine/client/library/clientSkeletalAnimation/src/shared/appearance/SkeletalAppearanceTemplate.cpp lines 786-1136 (SMAT), 824-1071 (graph)
    ../swg-client-v2/src/engine/shared/library/sharedObject/src/shared/appearance/AppearanceTemplateList.cpp:513-540 — the .apt redirector loader (FORM APT → FORM 0000 → CHUNK NAME → read_string → one redirectTarget string; FATAL if target ends in '.apt'). LOCKED oracle.
    ../swg-blender-plugin/swg_scene/mesh_skeletal.py — second oracle for SKMG chunk order cross-check
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § SkeletalMeshGen.{h,cpp} (security pattern), § harness/test/mesh-roundtrip.test.ts (registerFormat shape with loaderSource citations)
  </read_first>
  <action>
    FILE: modules/core/formats/SkeletalMeshGen.h + SkeletalMeshGen.cpp
    parseSkeletalMesh(root) -> SkeletalMeshResult (extends MeshResult with boneNames, weightsTruncated).
    FORM SKMG -> FORM 000{2,3,4}:
    1. INFO chunk: read 9×int32 THEN 4×int16 per synthesis §1.3 verified order — int32: maxTransformsPerVertex, maxTransformsPerShader, skeletonTemplateNameCount, transformNameCount, positionCount, transformWeightDataCount, normalCount, perShaderDataCount, blendTargetCount; int16: occlusionZoneCount, occlusionZoneCombinationCount, zonesThisOccludesCount, occlusionLayer. (NOT 8×int32 — the old "8×int32" desyncs INFO by one field.) Apply count-bounds checks: positionCount/normalCount cap 1M; perShaderDataCount cap 512; transformWeightDataCount cap (positionCount × 16) as a generous DoS bound.
    2. SKTM inner chunk (a flat skeleton-name list required by this mesh; NUL-terminated strings). This is NOT a FORM and NOT parseSkeleton — store as sktmNames[] (name it SktmNameList per delta #7).
    3. XFNM: transform/bone name table (NUL-terminated). Store as xfnmNames[].
    4. POSN: positionCount × 3 float32 (global pool).
    5. TWHD: positionCount × int32 (per-vertex influence count). Verify size == positionCount.
    6. TWDT: read EXACTLY transformWeightDataCount (from INFO) × (int32 transformIndex, float32 weight) pairs (SkeletalMeshGeneratorTemplate.cpp:2331-2343). This is the canonical count — NOT "sum of TWHD" and NOT a 4×posCount cap.
    7. [NORM] (only if normalCount > 0): authored normals. Do NOT synthesize via computeVertexNormals.
    8. [DOT3] (v0004): global tangent pool — STORE it (consumed by 02-03 for normal-map TBN).
    9. PSDT* (per-shader groups): each contains NAME (.sht path), PIDX (int32[]→POSN), [NIDX]→NORM, [DOT3 idx v0004], TXCI + TCSF/TCSD (UV sets), PRIM -> INFO + ITL (3×int32 shader-local indices).
    10. For EACH PSDT group: call deIndex(POSN, NORM, PIDX, NIDX, UVs, PRIM) -> DeIndexedBuffers (Uint32 indices). Emit ALL groups (multi-PSDT). Call normalizeSkinWeightsInto with boneOrder built from the resolved skeleton WHEN available at binding time (the binding accepts an optional boneOrder name→index map; when null, emit XFNM-local indices and set a flag so the resolver supplies boneOrder and the C++ remap is re-run, OR the resolver remaps before render — prefer C++-side remap with the resolved boneOrder; see ParseSkeletalMesh binding). vec4 normalization: top-4 by weight, renormalize to 1.0, zero-pad; all-zero influence → (1,0,0,0) on root bone (no origin collapse).
    Return SkeletalMeshResult { shaderGroups[] (all), boneNames (XFNM), weightsTruncated, geometry buffer with skinIndex (Uint32) + skinWeight (Float32) slices, roundTrip }.
    Oracle: SkeletalMeshGeneratorTemplate.cpp + mesh_skeletal.py cross-check.

    FILE: modules/core/formats/Skeleton.h + Skeleton.cpp
    parseSkeleton(root) -> SkeletonResult. Entry: root tag MUST be 'SKTM' AND a FORM. If root is a bare chunk tagged 'SKTM', throw FormatParseError("parseSkeleton requires FORM SKTM root, not the inner SKTM name-list chunk — inside a .mgn use SktmNameList") per delta #7.
    FORM SKTM -> FORM 000{1,2} — VERSION-BRANCH:
    - INFO: bone count int32. Cap 2048.
    - NAME: bone name strings (count == bone count).
    - PRNT: int32[] parent indices (-1 = root). Validate each parentIndex < its own bone index (acyclic).
    - RPRE/RPST: pre/post rotation quaternions (bone count × 4 float32 each) — read+store (bind pose may need them).
    - BPTR/BPRO: bind-pose translation (3f) + rotation (4f, (w,x,y,z)) per bone.
    - v0001 ONLY: BPMJ is MANDATORY (BasicSkeletonTemplate.cpp:280-286, plain enterChunk). enter+read/skip it or the IFF read position corrupts. v0002 has NO BPMJ (:363-390) — do NOT look for it. Branch on the FORM version tag.
    - JROR: joint-rotation order (read+store).
    Return SkeletonResult { version, bones[] { name, parentIndex, bindTranslation[3], bindRotation[4 (w,x,y,z)], preRot[4], postRot[4] } }. Document (w,x,y,z) order in the header.
    Oracle: BasicSkeletonTemplate.cpp:151-389,280-286,363-390.

    FILE: modules/core/formats/SkeletalAppearance.h + SkeletalAppearance.cpp
    parseSkeletalAppearance(root) -> SkeletalAppearanceResult.
    FORM SMAT -> FORM 000{1,2,3}: INFO; MSGN (mesh-gen path strings → .lmg or .mgn); SKTI ((skeletonPath, attachmentTransformName) PAIRS); [LATX] v0003 (.skt→.lat mapping — read+store for round-trip AND for ansPickerOptions in 02-04); [LDTB][SFSK][APAG] v0003 read+store.
    Return { version, msgnPaths[], sktiPairs[]{skeletonPath, attachmentTransformName}, latxMappings[] }.
    Oracle: SkeletalAppearanceTemplate.cpp:786-1136.

    FILE: modules/core/formats/StaticAppearance.h + StaticAppearance.cpp
    parseStaticAppearance(root) -> StaticAppearanceResult. An .apt is a single-string REDIRECTOR, NOT a geometry container: FORM APT → FORM 0000 → CHUNK NAME → read_string yields ONE redirectTarget path naming the real appearance template (commonly .msh, but may be .lmg / .mgn / .dtla). Error if redirectTarget itself ends in '.apt' (no multi-level indirection — AppearanceTemplateList.cpp:529). Return { version, redirectTarget }. The RESOLVER (not this parser) resolves redirectTarget by extension. This completes the composed-STATIC smart-open path (D-03). Oracle: AppearanceTemplateList.cpp:513-540; do NOT invent the layout.
    Oracle: swg-client-v2 Apt appearance-template loader (cite the real file).

    FILE: CMakeLists.txt — add SkeletalMeshGen.cpp, Skeleton.cpp, SkeletalAppearance.cpp, StaticAppearance.cpp.

    FILE: src/mesh_binding.cpp — add ParseSkeletalMesh, ParseSkeleton, ParseSkeletalAppearance, ParseStaticAppearance following extractBytes + try/catch + two-tier error + ArrayBuffer return. ParseSkeletalMesh: accept an OPTIONAL second arg = boneOrder (a JS array/object name→index from the resolved skeleton); when present, run the XFNM→bone remap C++-side inside normalizeSkinWeightsInto; when absent, emit XFNM-local skin indices + a needsBoneRemap flag. Return typed JSON (shaderGroups, boneNames, weightsTruncated, needsBoneRemap) + the geometry/skinIndex/skinWeight ArrayBuffers.

    FILE: src/addon.cpp — forward declarations + Init() registrations for parseSkeletalMesh, parseSkeleton, parseSkeletalAppearance, parseStaticAppearance.

    FILE: harness/test/mesh-roundtrip.test.ts — extend with five it() blocks (all IFF-pair round-trips):
    - '.mgn (SKMG) byte-exact round-trip': real .mgn fixture; assert byte-identical; assert parseSkeletalMesh boneNames.length > 0, shaderGroups covers ALL PSDT groups, geometry ArrayBuffer present.
    - '.skt v0001 (SKTM) byte-exact round-trip': real v0001 .skt; assert bones.length > 0 and round-trip survives the mandatory BPMJ.
    - '.skt v0002 (SKTM) byte-exact round-trip': real v0002 .skt; assert bones.length > 0, no BPMJ.
    - '.sat (SMAT) byte-exact round-trip': real .sat; assert msgnPaths.length > 0.
    - '.apt (APT) byte-exact round-trip': real .apt; assert parseStaticAppearance redirectTarget non-empty and not ending in '.apt'.
    Add registerFormat calls: 'mesh-skmg' (loaderSource 'swg-client-v2 SkeletalMeshGeneratorTemplate.cpp:2247-2360 (INFO 9×int32+4×int16, verified 2026-06-23)'), 'mesh-sktm-v1' + 'mesh-sktm-v2' (loaderSource 'swg-client-v2 BasicSkeletonTemplate.cpp:151-389,280-286,363-390'), 'mesh-smat' (loaderSource 'swg-client-v2 SkeletalAppearanceTemplate.cpp:786-1136'), 'mesh-apt' (loaderSource 'swg-client-v2 AppearanceTemplateList.cpp:513-540').
  </action>
  <verify>
    <automated>pnpm --filter @swg/native-core build 2>&1 | tail -5 && pnpm --filter @swg/harness test 2>&1 | tail -20; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    native-core builds without errors.
    All five new CORE-05 round-trips pass (.mgn / .skt v0001 / .skt v0002 / .sat / .apt byte-identical).
    registry-coverage sweep passes (six from 02-01 + five new).
    parseSkeletalMesh INFO read is 9×int32+4×int16 (asserted: the transformWeightDataCount field drives the TWDT read).
    parseSkeletalMesh returns vec4 skinIndex/skinWeight buffers; all-zero-influence vertex maps to (1,0,0,0) root, not origin.
    parseSkeleton v0001 advances past mandatory BPMJ; v0002 does not look for BPMJ (version-branched).
    Inner-SKMG-SKTM-name-list passed to parseSkeleton throws a descriptive FormatParseError (delta #7 guard).
    parseStaticAppearance returns a non-empty redirectTarget for a real .apt.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TS appearance resolver + viewportStore + R3F Viewport + StaticMeshView + SkinnedMeshView (VIEW-01 static AND skinned)</name>
  <files>
    packages/renderer/src/state/viewportStore.ts,
    packages/renderer/src/panels/viewport/resolver/appearanceResolver.ts,
    packages/renderer/src/panels/viewport/Viewport.tsx,
    packages/renderer/src/panels/viewport/StaticMeshView.tsx,
    packages/renderer/src/panels/viewport/SkinnedMeshView.tsx,
    packages/renderer/src/panels/viewport/LodPicker.tsx,
    packages/renderer/src/panels/viewport/AppearancePanel.tsx,
    packages/renderer/src/panels/ViewportPanel.tsx
  </files>
  <read_first>
    packages/renderer/src/panels/ViewportPanel.tsx — ENTIRE FILE; preserve header, chip overlays, stats, gizmo, empty-state placeholder, actionBtnStyle, chipStyle (PATTERNS.md 509-603); the stats badge says 'binary' not 'zero-copy'
    packages/renderer/src/state/iffStore.ts — Zustand 5 store pattern verbatim (PATTERNS.md § viewportStore.ts 879-933)
    packages/renderer/src/panels/iff/IffStructureTree.tsx — read-only inspector panel pattern for AppearancePanel and LodPicker
    packages/renderer/src/panels/tre/VfsSearchField.tsx — controlled-input + live-update pattern for the resolver trigger
    packages/renderer/src/state/treStore.ts — nativeCore require pattern + how mountHandle/archiveIndex/entryIndex are surfaced (the resolver and Extract need these)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-UI-SPEC.md — Surface 1 (Canvas chrome, empty state, 'binary' badge), Surface 2 (LodPicker), Surface 6 (AppearancePanel)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § Viewport.tsx, § SkinnedMeshView.tsx (module-scope scratch, useFrame GC-safe), § StaticMeshView.tsx if present else mirror SkinnedMeshView without skeleton, § LodPicker.tsx, § AppearancePanel.tsx, § appearanceResolver.ts, § viewportStore.ts
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.1 SMAT; §1.2 name-keyed bone bind; §2 GPU skinning (skinning auto-enables from attributes + bound skeleton — NO material.skinning, removed r140); RESEARCH Pitfall 5 (name-keyed bone binding), Pitfall 6 (missing-dep partial render)
  </read_first>
  <action>
    All UI follows 02-UI-SPEC.md. The existing ViewportPanel.tsx chrome is PRESERVED; the R3F Canvas mounts into the panel BODY only. The stats badge reads 'binary' (no-JSON-for-geometry), NOT 'zero-copy'.

    FILE: packages/renderer/src/state/viewportStore.ts
    Zustand 5 store following iffStore.ts. Export ViewportLoadStatus union: {kind:'idle'} | {kind:'loading';filename} | {kind:'done';filename;mode:'composed'|'composed-static'|'leaf'} | {kind:'error';filename;reason;offset?}.
    State fields: loadStatus (idle), resolution (AppearanceResolutionResult|null), parsedMesh (MeshParseResult|null), parsedSkeleton (SkeletonParseResult|null), isSkinned (boolean — drives StaticMeshView vs SkinnedMeshView selection), selectedLod (0), renderMode ('solid'|'wire'|'textured'='textured'), customizationIndices (Record<string,number>={}), transportState (defaults).
    SOURCE-ENTRY fields (for Extract in 02-05 + resolver): sourceMountHandle (string|null), sourceArchiveIndex (number|null), sourceEntryIndex (number|null), sourceEntryPath (string|null).
    Actions: beginLoad(filename, mountHandle, archiveIndex, entryIndex, entryPath), loadComplete(filename, mode, resolution, isSkinned, parsedMesh?, parsedSkeleton?), loadError(filename, reason, offset?), setSelectedLod, setRenderMode, setCustomizationIndex, setTransportState, reset().
    Import MeshParseResult/SkeletonParseResult from @swg/contracts.

    FILE: packages/renderer/src/panels/viewport/resolver/appearanceResolver.ts
    Export ResolvedMaterial { shaderResult: ShaderParseResult; slotBytes: Partial<Record<ShaderSlotName, ArrayBuffer|null>> } and AppearanceResolutionResult (meshes, skeleton, materials: ResolvedMaterial[] indexed BY SHADER-GROUP, missing[], mode:'composed'|'composed-static'|'leaf', isSkinned:boolean). 02-03 consumes materials[group].slotBytes with the SAME shader-group index semantics (see 02-PATTERNS.md § AppearanceResolutionResult).
    Export resolveAppearance(mountHandle, entryPath) -> Promise<AppearanceResolutionResult>.
    nativeCore require: parseSkeletalAppearance, parseStaticAppearance, parseMeshLod, parseSkeletalMesh, parseSkeleton, parseMesh, parseShader, parsePalette, resolveEntry (PATTERNS.md require block).
    Algorithm (D-03 smart-open + D-04 partial-resolution):
    1. Mode by extension: .sat → 'composed' (skinned); .apt → 'composed-static' (static); .mgn → 'leaf' skinned; .msh → 'leaf' static.
    2. composed (.sat): parseSkeletalAppearance → msgnPaths + sktiPairs. For each sktiPair: parseSkeleton FIRST (so boneOrder is known). Build boneOrder name→index map from the resolved skeleton. For each msgnPath: resolveEntry → if .lmg, parseMeshLod → per-level .mgn paths; else single .mgn. For each .mgn: parseSkeletalMesh(bytes, boneOrder) so the XFNM→bone remap runs C++-side with the real boneOrder. For each mesh: parseShader; for each .pal slot: parsePalette (DDS bytes fetched in 02-03, see below). isSkinned=true.
    3. composed-static (.apt): parseStaticAppearance → redirectTarget → resolveEntry(redirectTarget) → dispatch BY EXTENSION (.msh → parseMesh static; .lmg → parseMeshLod; .mgn → skinned-leaf via parseSkeletalMesh; else push to missing[]). parseShader for slots. For the .msh/.lmg static case: skeleton=null, isSkinned=false. Most .apt redirect to .msh, but do NOT hard-assume it.
    4. leaf (.mgn): parseSkeletalMesh (boneOrder empty → needsBoneRemap flag set; skeleton null = manual-attach). isSkinned=true. leaf (.msh): parseMesh. isSkinned=false.
    5. TEXTURE BYTES: for every resolved shader slot with a texturePath, fetch the .dds/.pal bytes via resolveEntry + readMountEntry and attach them to the material descriptor (materials[i].slotBytes[slot] = ArrayBuffer). This is what 02-03 consumes to build CompressedTexture — the bytes MUST be plumbed here (02-03 only reads view/panel files). On missing slot: push to missing[], leave bytes null.
    6. On any resolveEntry({found:false}): push to missing[], use null placeholder — NEVER throw (D-04).
    Return AppearanceResolutionResult.
    SECURITY: every dependency path goes through nativeCore.resolveEntry(mountHandle, name) ONLY; the resolver never calls node:fs; reject any path with a drive letter, leading '/', or '..' segment before calling resolveEntry (T-02-09).

    FILE: packages/renderer/src/panels/viewport/Viewport.tsx
    R3F Canvas mounting into ViewportPanel body (position:absolute; inset:0; overflow:hidden). Inside: drei OrbitControls (enableDamping, dampingFactor 0.05); drei Grid; 3-point lighting (key directional [5,5,3] i1.2; fill [-3,3,1] i0.4; rim [0,5,-5] i0.3; ambient i0.3). Render StaticMeshView when isSkinned===false, SkinnedMeshView when true (read isSkinned from store). 
    Wire render-mode chips to renderMode; Frame chip to fit bounding sphere; stats overlay to renderer.info.render.{triangles,vertices,calls} → 'persp · {W}×{H} · {fps} fps · {verts} v · {tris} t · {draws} dc · binary' (badge says 'binary', NOT 'zero-copy' — the binding memcpys).
    Empty/loading/error states per PATTERNS.md and UI-SPEC Surface 1 (empty copy mentions .sat/.apt compose · .mgn/.msh inspect).

    FILE: packages/renderer/src/panels/viewport/StaticMeshView.tsx
    Non-skinned render path (VIEW-01 static). Build THREE.BufferGeometry from parsedMesh geometry ArrayBuffer using the MeshShaderGroup MeshAttributeSlice offsets: Float32BufferAttribute for position/normal/uv; setIndex with Uint32Array (Uint32BufferAttribute — NOT Uint16). Render ONE THREE.Mesh PER shader group (multi-PSDT/multi-group); add all to a group. Material: MeshStandardMaterial placeholder (replaced by 02-03). Auto-frame camera on bounding box. Update stats from renderer.info each frame. Use module-scope scratch objects; never `new` in any render-loop callback.

    FILE: packages/renderer/src/panels/viewport/SkinnedMeshView.tsx
    Skinned render (VIEW-01 skinned). Module-scope scratch: const _scratchQuat = new THREE.Quaternion(); const _scratchVec3 = new THREE.Vector3(); const _scratchMat4 = new THREE.Matrix4(); (declared once at module scope — never re-created in useFrame).
    On parsedMesh change: build BufferGeometry per shader group from the ArrayBuffer slices (position/normal/uv via Float32BufferAttribute; skinIndex/skinWeight via the appropriate BufferAttribute; index via Uint32). Render ALL groups (multi-PSDT) as SkinnedMesh instances sharing one Skeleton.
    Build THREE.Skeleton from parsedSkeleton.bones: each → THREE.Bone with bone.position/quaternion from bindTranslation/bindRotation (IR is (w,x,y,z) → THREE.Quaternion(x,y,z,w)). Bone NAMES set to the SKTM bone names (stable, matched against AnimationClip track paths in 02-05). Build parent hierarchy from parentIndex.
    Build SkinnedMesh per group: new THREE.SkinnedMesh(geometry, material); mesh.bind(skeleton). Skinning auto-enables from skinIndex/skinWeight attributes + the bound Skeleton — DO NOT set material.skinning (removed in three r140; setting it is a silent no-op/throw on r0.184.0). The XFNM→bone remap is already applied C++-side (boneOrder passed at parse) for composed meshes; for leaf .mgn with no skeleton, render bind pose with identity skin (needsBoneRemap noted).
    Material: MeshStandardMaterial placeholder (replaced by 02-03). renderMode subscription (wire/solid/textured). Auto-frame camera. Update stats from renderer.info each frame.

    FILE: packages/renderer/src/panels/viewport/LodPicker.tsx
    Per PATTERNS.md § LodPicker.tsx + UI-SPEC Surface 2. Props lodLevels/selectedLod/onSelectLod. One row per level (LOD 0 first): 'LOD {n}' + 'X–Ym' distance (mono) + tri count. Selected: 2px accent left border + accent-dim bg. Empty: 'No LOD graph' when null; collapse to 'LOD 0' / '1 level' when single.

    FILE: packages/renderer/src/panels/viewport/AppearancePanel.tsx
    Per PATTERNS.md § AppearancePanel.tsx + UI-SPEC Surface 6. Read-only. Props resolution. Section head 'Appearance'; open-mode indicator ('composed (.sat)' / 'composed-static (.apt)' / 'leaf (.mgn|.msh standalone)'); resolved-graph list (VerificationStatus variant pass 'resolved ✓' per dep; variant warn 'missing: {name} — placeholder' per missing); missing-deps banner when missing.length>0; leaf mode: 'Attach skeleton…'/'Attach animation…' stub buttons (wired in 02-04).

    FILE: packages/renderer/src/panels/ViewportPanel.tsx
    Mount Viewport.tsx into the existing panel body below overlay chips (position:absolute; inset:0; z-index:1; chips at z-index 2+). Import LodPicker + AppearancePanel; wire from useViewportStore. Preserve all existing chrome verbatim. The 'binary' (not 'zero-copy') stats badge is in the existing overlay — relabel it if it currently says 'zero-copy'.
  </action>
  <verify>
    <automated>pnpm --filter @swg/renderer typecheck 2>&1 | tail -10; echo "typecheck:$?"</automated>
  </verify>
  <acceptance_criteria>
    Renderer TypeScript compiles without errors.
    viewportStore exports useViewportStore with all state fields, isSkinned, the source-entry fields (mountHandle/archiveIndex/entryIndex/entryPath), and actions typed.
    appearanceResolver.resolveAppearance returns AppearanceResolutionResult with missing[] (never throws), plumbs slot texture bytes (slotBytes ArrayBuffers) for 02-03, and rejects drive-letter/absolute/'..' paths before resolveEntry.
    Viewport renders StaticMeshView when isSkinned===false and SkinnedMeshView when true.
    StaticMeshView and SkinnedMeshView render ALL shader groups (multi-PSDT), use Uint32 indices, and contain NO material.skinning assignment.
    SkinnedMeshView module-scope scratch objects are never re-allocated inside useFrame; bone names set to SKTM names.
    LodPicker shows per-level rows with 2px accent selection; AppearancePanel shows resolved/missing graph + banner.
    Stats badge reads 'binary', not 'zero-copy'. Existing ViewportPanel chrome unchanged.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — static AND skinned render paths</name>
  <action>Pause for the user to verify both render paths (StaticMeshView for .msh/.apt, SkinnedMeshView for .mgn/.sat) per the how-to-verify steps. Do not auto-advance.</action>
  <what-built>
    The R3F viewport renders real SWG meshes via BOTH paths: StaticMeshView for static .msh/.apt and SkinnedMeshView for rigged .mgn/.sat. parseSkeletalMesh + parseSkeleton + parseStaticAppearance are wired through the resolver → viewportStore → the correct view. The user opens assets from the TRE VFS browser.
  </what-built>
  <how-to-verify>
    1. Run pnpm dev to start the Electron app.
    2. Mount a client TRE (e.g. D:\SWG Infinity\*.tre) in the TRE VFS Browser.
    3. STATIC PATH: open a static .msh (or a composed .apt). Confirm geometry renders via StaticMeshView, orbit works, AND it is NOT inside-out / garbled (a VTXA-stride off-by-one renders clean but wrong — eyeball that faces, not noise, are visible). AppearancePanel shows leaf or composed-static mode.
    4. SKINNED PATH: double-click a .sat (e.g. a creature/player .sat). Confirm transition empty → Loading → SkinnedMesh in bind pose. Orbit + zoom work. Stats overlay shows verts/tris/draws and a 'binary' badge (NOT 'zero-copy').
    5. Open a leaf .mgn directly. Confirm standalone render and AppearancePanel 'leaf' mode.
    6. Open a multi-group mesh (most player/creature meshes): confirm ALL shader groups are visible (no missing sub-parts).
    7. Missing dependency: confirm AppearancePanel shows a yellow ⚠ and the viewport renders with a placeholder (no crash).
    8. LodPicker shows levels if the .sat references an .lmg.
    9. Phase-1 IFF tree + TRE VFS browser still work unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" to continue to materials/customization, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → C++ SKMG/SKTM/SMAT/APT parsers | .mgn/.skt/.sat/.apt bytes from local TRE; untrusted binary |
| Appearance resolver → TRE VFS (native) | Path strings from parsed MSGN/SKTI/APT; resolver must not follow arbitrary FS paths — only names within the mounted TRE VFS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | formats/SkeletalMeshGen.cpp parseSkeletalMesh | mitigate | COUNT BOUNDS: positionCount cap 1M; perShaderDataCount cap 512; TWHD array == positionCount; TWDT total = transformWeightDataCount from INFO, capped positionCount×16; XFNM name count cap 1024; string paths cap 512. All via FormatParseError. |
| T-02-07 | Tampering | geometry/DeIndex.cpp PIDX/NIDX OOB | mitigate | Every PIDX[i] < posCount, NIDX[i] < normCount; out-of-range throws FormatParseError before any read; Uint32 widening prevents truncation. |
| T-02-08 | Tampering | formats/Skeleton.cpp parseSkeleton | mitigate | Bone count cap 2048; PRNT parentIndex < boneIndex (acyclic); names cap 256; v0001 BPMJ entered/skipped within bounds (no read-position corruption). |
| T-02-09 | Tampering | resolver/appearanceResolver.ts path injection | mitigate | All dependency paths via nativeCore.resolveEntry(mountHandle, name) ONLY; reject drive-letter / leading-slash / '..' inputs before resolve; never call node:fs. |
| T-02-10 | Elevation of Privilege | SkinnedMeshView.tsx bone bind | mitigate | Bone order name-keyed (XFNM name → Skeleton name → bone index); numeric index alone never used; Three.js Skeleton is JS-side (no OOB memory). |
| T-02-11 | Tampering | formats/StaticAppearance.cpp parseStaticAppearance | mitigate | Inherit IFF caps; referenced-path count cap 64; path strings cap 512 chars; FormatParseError on malformed APT. |
</threat_model>

<verification>
1. `pnpm --filter @swg/native-core build` exits 0 — four new C++ parsers compile.
2. `pnpm --filter @swg/harness test` exits 0 — eleven CORE-05 fixtures pass (six from 02-01 + five new).
3. `pnpm --filter @swg/renderer typecheck` exits 0 — store, resolver, Viewport, StaticMeshView, SkinnedMeshView, LodPicker, AppearancePanel compile.
4. `pnpm -r test` exits 0 — full suite green.
5. Human-verify: static .msh/.apt renders via StaticMeshView (not garbled); .sat renders via SkinnedMeshView; multi-group complete; 'binary' badge.
</verification>

<success_criteria>
- parseSkeletalMesh (INFO 9×int32+4×int16, TWDT from INFO) / parseSkeleton (v0001+v0002 branched) / parseSkeletalAppearance / parseStaticAppearance callable from the renderer.
- CORE-05 round-trip passes for .mgn, .skt v0001, .skt v0002, .sat, .apt.
- SKTM inner-name-list guard fires a descriptive error (delta #7).
- Resolver returns partial + missing[]; plumbs slot texture bytes; rejects unsafe paths.
- BOTH render paths ship: static .msh/.apt via StaticMeshView; skinned .mgn/.sat via SkinnedMeshView; both multi-group, Uint32 indices, no material.skinning.
- Module-scope scratch objects verified not re-allocated in useFrame.
- VIEW-01 satisfied: static AND skinned paths both confirmed rendering on real assets.
</success_criteria>

<output>
Create `.planning/phases/02-3d-mesh-viewport-mvp-proof/02-02-SUMMARY.md` when done.
</output>
