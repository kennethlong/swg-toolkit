---
phase: 02-3d-mesh-viewport-mvp-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/package.json
  - packages/contracts/src/mesh.ts
  - packages/contracts/src/skeleton.ts
  - packages/contracts/src/animation.ts
  - packages/contracts/src/material.ts
  - packages/contracts/src/index.ts
  - packages/native-core/modules/core/formats/Mesh.h
  - packages/native-core/modules/core/formats/Mesh.cpp
  - packages/native-core/modules/core/formats/MeshLod.h
  - packages/native-core/modules/core/formats/MeshLod.cpp
  - packages/native-core/modules/core/formats/Shader.h
  - packages/native-core/modules/core/formats/Shader.cpp
  - packages/native-core/modules/core/formats/Palette.h
  - packages/native-core/modules/core/formats/Palette.cpp
  - packages/native-core/modules/core/formats/Dds.h
  - packages/native-core/modules/core/formats/Dds.cpp
  - packages/native-core/modules/core/geometry/DeIndex.h
  - packages/native-core/modules/core/geometry/DeIndex.cpp
  - packages/native-core/modules/core/CMakeLists.txt
  - packages/native-core/src/mesh_binding.cpp
  - packages/native-core/src/addon.cpp
  - packages/harness/test/mesh-roundtrip.test.ts
  - packages/harness/fixtures-real/.gitkeep
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02

must_haves:
  truths:
    - "The renderer package has three/R3F/drei installed and imports compile without errors"
    - "All four contract types (mesh/skeleton/animation/material) are exported from @swg/contracts"
    - "parseMesh() returns MeshParseResult with shaderGroups and a single binary ArrayBuffer geometry payload (no JSON crosses the bridge for geometry)"
    - "A real .msh asset's serializeIff(parseIff(bytes)) round-trip is byte-identical (SC-5 gate)"
    - "FORM MESH de-indexed attribute arrays (positions/normals/uvs/Uint32 indices) are ready-to-upload — not the raw POSN pool"
    - "MeshShaderGroup carries explicit per-attribute byte offsets+counts into the single geometry ArrayBuffer (the buffer ABI consumers slice from)"
    - ".lmg MLOD and .ldt LDTB parsers return LOD-level generator paths and distance ranges; LDTB distances live INSIDE INFO (int16 levelCount + per-level (min,max) float32, stored as sqrt distances on disk, squared at runtime)"
    - ".sht SSHT/CSHD parsers return slot map (MAIN/NRML/CNRM/SPEC/EMIS/ENVM/MASK) and customization pathway; SSHT TXMS entries parsed via FORM TXM → FORM 000x → DATA then TextureList::fetch (NOT slot-tag/string/uv-set framing)"
    - ".pal RIFF PAL parser returns RGBA entry array (24-byte header + entryCount×4); versionOrComponentCount != 4 forces alpha=255 — proved by a PARSER-NATIVE round-trip (read→serialize-own-format→compare), NOT generic-IFF"
    - ".dds parser returns per-mip (offset, byteLength, format enum) table; compressed blocks pass through unchanged; proved by a PARSER-NATIVE round-trip (NOT generic-IFF, DDS is not IFF)"
  artifacts:
    - path: "packages/contracts/src/mesh.ts"
      provides: "MeshParseResult, MeshShaderGroup (with byte offsets), SkeletonParseResult, AnimationParseResult, MaterialSlotMap types"
    - path: "packages/native-core/modules/core/formats/Mesh.h"
      provides: "parseMesh() returning MeshResult with de-indexed BufferGeometry-ready attr arrays (Uint32 indices)"
    - path: "packages/native-core/modules/core/geometry/DeIndex.h"
      provides: "deIndex() + normalizeSkinWeightsInto() C++ pre-bridge passes"
    - path: "packages/native-core/src/mesh_binding.cpp"
      provides: "N-API parseMesh/parseMeshLod/parseShader/parsePalette/parseDds exports"
    - path: "packages/harness/test/mesh-roundtrip.test.ts"
      provides: "CORE-05 round-trip registrations: generic-IFF for .msh/.lmg/.ldt/.sht; PARSER-NATIVE for .pal/.dds"
  key_links:
    - from: "packages/native-core/src/mesh_binding.cpp"
      to: "modules/core/formats/Mesh.cpp + geometry/DeIndex.cpp"
      via: "parseMesh() calls parseMeshForm() then deIndex() C++-side before bridge"
      pattern: "swg_core::formats::parseMesh"
    - from: "packages/harness/test/mesh-roundtrip.test.ts"
      to: "fixtureRegistry.ts"
      via: "registerFormat('mesh-mesh', ...) with loaderSource citing swg-client-v2"
      pattern: "registerFormat"
---

<objective>
Install the R3F render stack, define the contract type surface for all Phase-2 formats, land the C++ parsers for static `.msh`, LOD graph (`.lmg`/`.ldt`), shader (`.sht` SSHT/CSHD), palette (`.pal`), and DDS header+mip table — then register each in the CORE-05 harness. True IFF formats use the generic-IFF round-trip; non-IFF `.pal`/`.dds` use parser-native round-trips.

Purpose: Establishes the typed interface contracts and the five support-format parsers that all later plans consume (materials/LOD/customization chain). The de-index utility (C++ pre-bridge pass) lands here so the skinned mesh plan can use it directly. The geometry payload crosses the N-API bridge as a single binary ArrayBuffer (binary stays binary / no JSON for geometry).

Output: @swg/contracts exports four new type files; `packages/native-core` exposes `parseMesh`, `parseMeshLod`, `parseShader`, `parsePalette`, `parseDds`; CORE-05 sweep is green for all five formats; `packages/renderer` installs three/R3F/drei.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-01-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Code\SWG-Toolkit\.planning\PROJECT.md
@D:\Code\SWG-Toolkit\.planning\ROADMAP.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-CONTEXT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-RESEARCH.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-SYNTHESIS.md

<interfaces>
<!-- Phase-1 IFF API (verified from packages/native-core/modules/core/iff/Iff.h) -->
<!-- namespace swg_core::iff -->
<!--   parseIff(const uint8_t* data, uint32_t size) -> IffParseResult -->
<!--   serializeIff(IffParseResult&, const uint8_t* srcData, uint32_t srcSize) -> vector<uint8_t> -->
<!--   Security caps: per-chunk <= 64MB; childEnd <= parentEnd; non-printable FourCC rejected (Iff.h:146-149) -->
<!-- CORE-05 harness: packages/harness/fixtureRegistry.ts -->
<!--   registerFormat(id, { parse, serialize, fixtures[], loaderSource }) -->
<!--   loaderSource MUST match /swg-client-v2|Utinni|tre_reader\.py/ -->
<!--   parse/serialize are FORMAT-NEUTRAL callbacks — for .pal/.dds, pass parsePalette/serializePalette (parser-native), NOT parseIff/serializeIff -->
<!-- Pattern analog: packages/native-core/src/iff_binding.cpp + packages/contracts/src/iff.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install R3F deps + define contract types for all Phase-2 formats</name>
  <files>
    packages/renderer/package.json,
    packages/contracts/src/mesh.ts,
    packages/contracts/src/skeleton.ts,
    packages/contracts/src/animation.ts,
    packages/contracts/src/material.ts,
    packages/contracts/src/index.ts
  </files>
  <read_first>
    packages/renderer/package.json — current deps (add three/R3F/drei here)
    packages/contracts/src/iff.ts — exact file-header and interface pattern to follow (lines 1–103)
    packages/contracts/src/index.ts — re-export barrel pattern
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § contracts/src/mesh.ts pattern (MeshShaderGroup, MeshParseResult, discriminated roundTrip)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-RESEARCH.md — § Standard Stack (exact versions) + § Architecture Patterns (binary-stays-binary; Uint32 indices; material.skinning REMOVED in r140)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1 layouts for what each type models (§1.6 CKAT/KFAT is sparse per-channel; §1.7 .pal/.dds non-IFF)
  </read_first>
  <action>
    STEP A — Install render-stack deps. In packages/renderer run: pnpm add three@0.184.0 @react-three/fiber@9.6.1 @react-three/drei@10.7.7 and pnpm add -D @types/three@0.184.1. These are pre-audited slopcheck-OK packages from the RESEARCH legitimacy audit.

    STEP B — Write packages/contracts/src/mesh.ts. Follow the iff.ts file-header pattern exactly: ground-truth citation block at top naming swg-client-v2 sources, then typed interfaces. Export:
    - MeshAttributeSlice: { offset: number; byteLength: number; componentCount: number; elementCount: number } — explicit per-attribute byte offset+count into the single geometry ArrayBuffer (the buffer ABI a view slices from).
    - MeshShaderGroup: { shaderName: string; vertexCount: number; indexCount: number; positions: MeshAttributeSlice; normals: MeshAttributeSlice | null; uvs: MeshAttributeSlice[]; indices: MeshAttributeSlice; skinIndices: MeshAttributeSlice | null; skinWeights: MeshAttributeSlice | null; hasDot3: boolean }. indices are Uint32 (indices.componentCount documents stride). Document in the file header: index type is Uint32 (source ITL indices are int32; meshes exceed 65535 verts — Uint16 silently corrupts).
    - MeshParseResult: { formatTag: 'MESH' | 'SKMG'; version: string; shaderGroups: MeshShaderGroup[]; boneNames?: string[]; weightsTruncated?: number; roundTrip: { passed: boolean; failOffset?: number } }
    Binary geometry arrays cross as ONE ArrayBuffer; per-group MeshAttributeSlice fields locate each attribute inside it — document explicitly in the file header per AGENTS.md binary-stays-binary rule (relabel any "zero-copy" prose as "binary ArrayBuffer path / no JSON for geometry": the binding memcpys into a JS-owned buffer). Ground-truth citation: swg-client-v2 MeshAppearanceTemplate.cpp + SkeletalMeshGeneratorTemplate.cpp.

    STEP C — Write packages/contracts/src/skeleton.ts. Export:
    - BoneNode: { name: string; parentIndex: number; bindTranslation: [number,number,number]; bindRotation: [number,number,number,number] } — quat order is (w,x,y,z) on disk; IR is (w,x,y,z). Document the order. Ground-truth: swg-client-v2 BasicSkeletonTemplate.cpp:151-389, SKTM chunks PRNT/RPRE/RPST/BPTR/BPRO/[BPMJ]/JROR.
    - SkeletonParseResult: { version: string; bones: BoneNode[]; roundTrip: { passed: boolean; failOffset?: number } }

    STEP D — Write packages/contracts/src/animation.ts. Export:
    - AnimationVariant: 'KFAT-0003' | 'CKAT-0001' | 'KFAT-0002-unsupported'
    - AnimationJoint: { name: string; hasAnimatedRotation: boolean; rotationChannelIndex: number; translationMask: number; translationChannelIndex: [number,number,number] }
    - AnimationParseResult: { variant: AnimationVariant; fps: number; frameCount: number; joints: AnimationJoint[]; roundTrip: { passed: boolean; failOffset?: number } }
    Keys are SPARSE (frame-indexed), not dense per-frame. Binary keyframe buffers cross as ArrayBuffer. Ground-truth: synthesis §1.6, CompressedKeyframeAnimationTemplate.cpp:1198-1313,553-594 + CompressedQuaternion.cpp:82-122,156-228,370-419.

    STEP E — Write packages/contracts/src/material.ts. Export:
    - DdsMipEntry: { offset: number; byteLength: number; width: number; height: number; format: 'DXT1'|'DXT3'|'DXT5'|'DXT2'|'DXT4'|'RGBA8' }
    - DdsParseResult: { width: number; height: number; mipCount: number; mips: DdsMipEntry[]; roundTrip: { passed: boolean; failOffset?: number } }
    - PaletteEntry: { r: number; g: number; b: number; a: number }
    - PaletteParseResult: { entryCount: number; versionOrComponentCount: number; entries: PaletteEntry[]; roundTrip: { passed: boolean; failOffset?: number } }
    - ShaderSlotName = 'MAIN'|'NRML'|'CNRM'|'SPEC'|'EMIS'|'ENVM'|'MASK'
    - ShaderCustomizationPathway = 'palette-material-color'|'palette-texture-swap'|'palette-texture-factor'
    - ShaderSlot: { slot: ShaderSlotName; texturePath: string | null; uvSet: number }
    - ShaderCustomizationVar: { name: string; pathway: ShaderCustomizationPathway; palettePath: string; defaultIndex: number; affectedSlot?: ShaderSlotName }
    - ShaderParseResult: { variant: 'SSHT'|'CSHD'; effectPath: string; slots: ShaderSlot[]; customizationVars: ShaderCustomizationVar[]; roundTrip: { passed: boolean; failOffset?: number } }
    - LodLevel: { generatorPath: string; minDist: number; maxDist: number }
    - LodParseResult: { levelCount: number; levels: LodLevel[]; roundTrip: { passed: boolean; failOffset?: number } }
    Document on DdsParseResult/PaletteParseResult that roundTrip is PARSER-NATIVE (read→serialize-own-format→compare), not generic-IFF — these are RIFF PAL / Microsoft DDS, not IFF. Ground-truth: synthesis §1.4/§1.5/§1.7, StaticShaderTemplate.cpp:32-36,123-128 + CustomizableShaderTemplate.cpp:1246-1286 + PaletteArgb.cpp:508-522 + LodDistanceTable.cpp:145-168. (DDS mip math = standard Microsoft DDS spec; do NOT cite Texture.cpp:115-129 — that is a format-conversion registration table, not a DDS reader.)

    STEP F — Update packages/contracts/src/index.ts: add export * from './mesh.js'; export * from './skeleton.js'; export * from './animation.js'; export * from './material.js'; following the existing export barrel pattern.
  </action>
  <verify>
    <automated>pnpm --filter @swg/contracts build 2>&1 | tail -5; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    packages/renderer/package.json lists three@0.184.0, @react-three/fiber@9.6.1, @react-three/drei@10.7.7, @types/three@0.184.1 (dev).
    @swg/contracts builds without TypeScript errors.
    mesh.ts MeshShaderGroup exposes MeshAttributeSlice (offset+byteLength+componentCount+elementCount) for every attribute; indices documented as Uint32.
    material.ts DdsParseResult/PaletteParseResult document roundTrip as parser-native (not IFF).
    animation.ts AnimationJoint carries hasAnimatedRotation + rotationChannelIndex + translationMask + translationChannelIndex (the sparse-channel descriptor), NOT a name-only field.
    index.ts re-exports all four new modules.
    No type file emits or references `material.skinning` (removed in three r140).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C++ static mesh + support-format parsers (MESH/MLOD/LDTB/SSHT/CSHD/PAL/DDS) + de-index utility + N-API binding</name>
  <files>
    packages/native-core/modules/core/formats/Mesh.h,
    packages/native-core/modules/core/formats/Mesh.cpp,
    packages/native-core/modules/core/formats/MeshLod.h,
    packages/native-core/modules/core/formats/MeshLod.cpp,
    packages/native-core/modules/core/formats/Shader.h,
    packages/native-core/modules/core/formats/Shader.cpp,
    packages/native-core/modules/core/formats/Palette.h,
    packages/native-core/modules/core/formats/Palette.cpp,
    packages/native-core/modules/core/formats/Dds.h,
    packages/native-core/modules/core/formats/Dds.cpp,
    packages/native-core/modules/core/geometry/DeIndex.h,
    packages/native-core/modules/core/geometry/DeIndex.cpp,
    packages/native-core/modules/core/CMakeLists.txt,
    packages/native-core/src/mesh_binding.cpp,
    packages/native-core/src/addon.cpp
  </files>
  <behavior>
    - parseMesh on a real .msh FORM MESH v0005 returns shaderGroups with vertex/index counts > 0 and a single geometry ArrayBuffer length > 0
    - Each MeshShaderGroup's MeshAttributeSlice offsets+byteLengths are non-overlapping and sum to geometry ArrayBuffer length
    - serializeIff(parseIff(mshBytes)) === mshBytes byte-identical (SC-5 round-trip)
    - parseMeshLod on a real .lmg returns LodParseResult with levelCount > 0 and generatorPath strings non-empty; distances come from LDTB INFO
    - parseShader on a real .sht SSHT returns slot array with at least MAIN slot and effectPath non-empty
    - parsePalette on a real .pal returns entryCount matching the RIFF header and entries[] length === entryCount; serializePalette(parsePalette(bytes)) === bytes (parser-native round-trip)
    - parseDds on a real .dds returns mipCount >= 1 and mips[0].format in ['DXT1','DXT3','DXT5']; serializeDds(parseDds(bytes)) === bytes (parser-native round-trip)
    - deIndex() output indices are Uint32; positions slice elementCount === de-indexed vertex count
  </behavior>
  <read_first>
    packages/native-core/modules/core/iff/Iff.h — parseIff/IffParseResult/IffNode API + security caps (Iff.h:146-149)
    packages/native-core/modules/core/compress/Zlib.h — engine-free utility header pattern (namespace/error/function signature shape)
    packages/native-core/src/iff_binding.cpp — thin-binding pattern: extractBytes helper + try/catch + ArrayBuffer return (lines 39-55, 147-224, 285-289)
    packages/native-core/src/addon.cpp — registration pattern (lines 84-119); extend Init() with new Phase-2 exports
    packages/native-core/modules/core/CMakeLists.txt — how to add new source files to the build
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § formats/Mesh.{h,cpp} (namespace/error/security-count-cap pattern); § geometry/DeIndex.{h,cpp} (DeIndexedBuffers struct, Uint32 indices, function signatures); § mesh_binding.cpp (extractBytes + ParseMesh shape)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.4 FORM MESH chunk set (VTXA flags/interleave, INDX, SPS, LSPT); §1.5 MLOD/LDTB (distances INSIDE INFO); §1.7 SSHT/CSHD/PAL/DDS layouts — these are LOCKED axioms, do NOT re-derive from docs/
    ../swg-client-v2/src/engine/client/library/clientObject/src/shared/appearance/MeshAppearanceTemplate.cpp — MESH oracle
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/StaticShaderTemplate.cpp lines 32-36,123-128,482-565 — SSHT slots + nested TXMS texture path (FORM TXM → FORM 000x → DATA then TextureList::fetch)
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/CustomizableShaderTemplate.cpp lines 1246-1286
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/PaletteArgb.cpp lines 508-522 — RIFF PAL 24-byte header; versionOrComponentCount != 4 → alpha=255
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/LodDistanceTable.cpp lines 145-168 — LDTB distances inside INFO, stored as sqrt distances, squared at runtime
  </read_first>
  <action>
    All new C++ files follow the namespace/error/function-signature shape from Iff.h and the PATTERNS.md § formats/* section. All parsers are engine-free (no N-API, no SOE headers). IFF-based parsers consume parseIff() to get the IFF tree, then do typed extraction; .pal/.dds parsers read the raw byte buffer directly (NOT IFF). Every count/array read MUST add a bounds check matching the security-cap pattern from PATTERNS.md (if count > kMax throw FormatParseError). Do NOT use fenced code blocks here.

    FILE: modules/core/formats/Mesh.h + Mesh.cpp
    Implement parseMesh(const swg_core::iff::IffNode& root) -> MeshResult.
    FORM MESH -> FORM 0005 -> [APPR] -> FORM SPS -> per-shader FORM 0001.
    Per shader group (SPS framing in .msh): extract NAME(.sht path), FORM VTXA->FORM 0003->INFO(flags,vert_count)+DATA(interleaved verts). VTXA interleave per synthesis §1.4: position(3f) always present; [transformedW]; normal(3f) if bit set; [pointSize]; color0/1 (u32) if bits set; texcoords (per set, dim=((flags>>(12+2j))&3)+1). INDX = int32 count + uint16[] indices (LSPT 0001; v0000 = int32 indices). Call deIndex() per group to produce flat attribute arrays before returning. MeshResult stores per-group: shaderName, the de-indexed geometry written into ONE shared output buffer with MeshAttributeSlice offsets (positions/normals/uvs as float32; indices as Uint32). Emit the slice metadata (offset, byteLength, componentCount, elementCount) for each attribute.
    Oracle: MeshAppearanceTemplate.cpp + ShaderPrimitiveSetTemplate.cpp + VertexBuffer.cpp:247-307.

    FILE: modules/core/geometry/DeIndex.h + DeIndex.cpp
    Implement DeIndexedBuffers struct and deIndex() per PATTERNS.md § DeIndex. Index output type is Uint32 (NOT Uint16) — source ITL indices are int32 and meshes exceed 65535 verts. Also normalizeSkinWeightsInto() per the same section (used by 02-02). For static .msh the interleaved VTXA DATA is already per-vertex; extract position/normal/uv channels per vertex by reading the flags-driven interleave, output as flat float32 arrays. PRIM/INDX indices are widened to Uint32 — never truncate to Uint16.
    Source: synthesis §1.4 VTXA interleave + RESEARCH Pattern 2/3.

    FILE: modules/core/formats/MeshLod.h + MeshLod.cpp
    parseMeshLod -> LodResult. .lmg: FORM MLOD -> FORM 0000 -> INFO(level_count int16) + NAME[] strings (one per level = generator path). .ldt: FORM LDTB -> FORM 0000 -> INFO that contains int16 levelCount FOLLOWED BY per-level (minDist float32, maxDist float32) pairs IN THE SAME INFO CHUNK (distances live inside INFO, NOT sibling chunks; LodDistanceTable.cpp:145-168). Distances are stored on disk as sqrt distances (actual distances), squared at runtime by the client — store them as-read (do NOT pre-square). Return LodResult { levelCount, generatorPaths[], distances[] }.
    Oracle: synthesis §1.5 + LodDistanceTable.cpp:145-168.

    FILE: modules/core/formats/Shader.h + Shader.cpp
    parseShader(root) -> ShaderResult. Discriminate on root tag: 'SSHT' or 'CSHD'.
    SSHT: read effectPath (the .eft reference) FIRST, then parse the TXMS form for texture slots. Per StaticShaderTemplate.cpp:482-565, TXMS entries are nested: FORM TXM → FORM 000x → DATA, then TextureList::fetch(&iff) consumes the texture reference — do NOT model entries as slot-tag/string/uv-set. Map each resolved texture to its slot tag (MAIN/NRML/CNRM/SPEC/EMIS/ENVM/MASK) and UV-set index from TCSS. Populate ShaderResult { variant:'SSHT', effectPath, slots[], customizationVars:[] }.
    CSHD: read the inner SSHT first (CSHD wraps it), then parse customization variables — pathway A = MATR/AMCL/DFCL/EMCL (palette path, defaultIndex, affects material color); pathway B = TXTR/TX1D (palette path, defaultIndex, replaces texture slot); pathway C = TFAC/PAL (palette path, defaultIndex, texture-factor tint). Tag each var with its distinct pathway (A and C are different downstream properties — do NOT collapse).
    Oracle: StaticShaderTemplate.cpp:32-36,123-128,482-565 + CustomizableShaderTemplate.cpp:1246-1286.

    FILE: modules/core/formats/Palette.h + Palette.cpp
    parsePalette(data, size) -> PaletteResult AND serializePalette(PaletteResult) -> vector<uint8_t> (parser-native round-trip pair — .pal is RIFF, NOT IFF). RIFF PAL: 24-byte header (verify 'RIFF'/'PAL '/'data' tags); read versionOrComponentCount; entryCount from the data chunk size / 4; read entryCount × (R,G,B,A) bytes. If versionOrComponentCount != 4 → force alpha=255 per PaletteArgb.cpp:508-522 (NOT "version 3"). serializePalette writes the header + entries verbatim so serialize(parse(bytes)) == bytes.
    Oracle: PaletteArgb.cpp:508-522.

    FILE: modules/core/formats/Dds.h + Dds.cpp
    parseDds(data, size) -> DdsResult AND serializeDds(DdsResult) -> vector<uint8_t> (parser-native round-trip pair — DDS is Microsoft header/block, NOT IFF). Read DDS header (128 bytes): magic 0x20534444, DWORD flags, height, width, pitchOrLinearSize, depth, mipMapCount, then DDS_PIXELFORMAT FOURCC (DXT1/DXT3/DXT5/DXT2/DXT4 or uncompressed RGBA8). Build per-mip table per the STANDARD Microsoft DDS spec: mip 0 offset = 128; size per mip = ceil(max(1,w/4)) * ceil(max(1,h/4)) * blockBytes (blockBytes=8 DXT1, 16 DXT3/DXT5/DXT2/DXT4). Compressed blocks STAY as-is — do NOT decode. serializeDds re-emits header + the unchanged block payload so serialize(parse(bytes)) == bytes.
    Oracle: standard Microsoft DDS spec (do NOT cite Texture.cpp:115-129 — that is a format-conversion table, not a DDS reader/mip-math source).

    FILE: modules/core/CMakeLists.txt
    Add formats/Mesh.cpp, formats/MeshLod.cpp, formats/Shader.cpp, formats/Palette.cpp, formats/Dds.cpp, geometry/DeIndex.cpp to the swg_core static lib source list. Create formats/ and geometry/ subdirectory entries.

    FILE: src/mesh_binding.cpp
    Follow iff_binding.cpp exactly: extractBytes helper (copy verbatim from iff_binding.cpp:39-55); one Napi::Value function per export: ParseMesh, ParseMeshLod, ParseShader, ParsePalette, ParseDds. Each: arg-count guard -> type guard -> extractBytes -> try { parse -> deIndex if mesh -> build Napi::Object result (typed metadata + MeshAttributeSlice offsets) + ONE Napi::ArrayBuffer for geometry (binary stays binary; never serialize geometry as JSON) } -> two-tier catch. Geometry crosses via Napi::ArrayBuffer::New + memcpy (PATTERNS.md § mesh_binding.cpp) — label this "binary ArrayBuffer path", not "zero-copy". Typed metadata (group count, bone names, slot map, mip table) crosses as Napi::Object/String/Number/Array.

    FILE: src/addon.cpp
    Forward-declare ParseMesh, ParseMeshLod, ParseShader, ParsePalette, ParseDds. Add to Init(): exports.Set for each. Follow the existing per-phase comment block pattern.

    Rebuild: in packages/native-core run cmake-js build (generator pinned to 'Visual Studio 17 2022' x64 per STATE.md D decision).
  </action>
  <verify>
    <automated>cd "D:\Code\SWG-Toolkit" && pnpm --filter @swg/native-core build 2>&1 | tail -10; echo "build-exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    packages/native-core builds without C++ errors.
    The addon exports parseMesh, parseMeshLod, parseShader, parsePalette, parseDds callable from Node.
    deIndex() produces flat attribute arrays with Uint32 indices (not Uint16, not the raw POSN pool) before any buffer crosses the bridge.
    Palette.cpp and Dds.cpp expose parser-native serialize functions (serializePalette/serializeDds) such that serialize(parse(bytes)) == bytes.
    LDTB parser reads distances from INSIDE INFO (int16 levelCount + per-level float32 pairs), stored as-read (not pre-squared).
    SSHT TXMS parse follows FORM TXM → FORM 000x → DATA + TextureList::fetch (StaticShaderTemplate.cpp:482-565), not a slot-tag/string/uv-set layout.
    All format parsers apply count-bounds checks per the security-cap pattern; FormatParseError thrown on malformed input.
    .msh FORM MESH v0005 parses with shaderGroups.length >= 1 and a single geometry ArrayBuffer.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CORE-05 round-trip fixtures — generic-IFF for .msh/.lmg/.ldt/.sht; PARSER-NATIVE for .pal/.dds + binary-ArrayBuffer assertion</name>
  <files>
    packages/harness/test/mesh-roundtrip.test.ts,
    packages/harness/fixtures-real/.gitkeep
  </files>
  <read_first>
    packages/harness/test/iff-roundtrip.test.ts — exact file-header, import pattern, assertRoundTrip usage, registerFormat call shape (lines 1–46, 124–141)
    packages/harness/test/tre-async-zerocopy.test.ts — binary ArrayBuffer assertion pattern (lines 158–177) — mirror this for geometry
    packages/harness/fixtureRegistry.ts — registerFormat signature, FORMAT-NEUTRAL parse/serialize callbacks, loaderSource regex /swg-client-v2|Utinni|tre_reader\.py/ (lines 78–90, 107)
    packages/harness/assertRoundTrip.ts — assertRoundTrip function signature (works with any parse/serialize pair, not only IFF)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § harness/test/mesh-roundtrip.test.ts (complete file-header + IFF-layer pair + binary-ArrayBuffer assertion pattern)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-RESEARCH.md — § Validation Architecture table (each format's real fixture + cited oracle line; .pal/.dds are parser-native)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.7 (.pal/.dds are NOT IFF — parser-native round-trip required)
  </read_first>
  <action>
    STEP A — Extract real fixtures. The gitignored packages/harness/fixtures-real/ directory is the scratch dir for real assets. Use the Phase-1 TRE extractor (nativeCore.mountSearchableAsync / readMountEntry) to extract one real asset of each format from a mounted client:
    - fixtures-real/mesh/*.msh (any FORM MESH v0005 asset)
    - fixtures-real/mesh/*.lmg (LOD file) + matching .ldt
    - fixtures-real/shader/*.sht (SSHT or CSHD)
    - fixtures-real/palette/*.pal (RIFF PAL)
    - fixtures-real/texture/*.dds (DXT1 or DXT5)
    Use the copy-real-fixtures.js pattern from Phase 1 (D-09/D-10: copies only, never mutates originals). If no standalone extractor exists, write a one-shot gitignored Node script that mounts the client TRE and extracts these into fixtures-real/ (D-10: real archives and extracted assets are never committed).

    STEP B — Write packages/harness/test/mesh-roundtrip.test.ts following iff-roundtrip.test.ts exactly. File header with cited oracle lines per PATTERNS.md. Import nativeCore (parseIff, serializeIff, parseMesh, parseMeshLod, parseShader, parsePalette, serializePalette, parseDds, serializeDds).

    ROUND-TRIP STRATEGY PER FORMAT (CRITICAL — two distinct methods):
    - .msh / .lmg / .ldt / .sht are TRUE IFF → round-trip pair = parseIff(bytes) / serializeIff(iffResult, srcBytes). This proves CONTAINER preservation. Pair it with a typed-parse behavioral assertion + (for .msh) a render-readiness check, because generic-IFF round-trip alone does NOT prove semantic correctness.
    - .pal is RIFF (NOT IFF) → round-trip pair = parsePalette(bytes) / serializePalette(result). PARSER-NATIVE.
    - .dds is Microsoft DDS (NOT IFF) → round-trip pair = parseDds(bytes) / serializeDds(result). PARSER-NATIVE.

    Per-format test blocks:
    - .msh (MESH): assertRoundTrip via IFF pair; assert result.geometry instanceof ArrayBuffer (binary path — mirror the tre-async-zerocopy assertion); assert result.shaderGroups.length > 0 and shaderGroups[0].indices.componentCount documents Uint32 (4-byte stride) — assert indices are NOT Uint16.
    - .lmg/.ldt (MLOD/LDTB): assertRoundTrip via IFF pair; assert levels.length > 0; levels[0].generatorPath non-empty; levels[0].maxDist > 0 (distance came from INFO).
    - .sht (SSHT/CSHD): assertRoundTrip via IFF pair; assert result.slots.some(s => s.slot === 'MAIN') and result.effectPath non-empty.
    - .pal: assertRoundTrip via PARSER-NATIVE pair (parsePalette/serializePalette); assert result.entries.length === result.entryCount.
    - .dds: assertRoundTrip via PARSER-NATIVE pair (parseDds/serializeDds); assert result.mips[0].format matches /DXT[135]/.

    registerFormat calls — pass the correct FORMAT-NEUTRAL parse/serialize callbacks per format:
    - 'mesh-mesh' (IFF pair) loaderSource: 'swg-client-v2 MeshAppearanceTemplate.cpp + VertexBuffer.cpp:247-307'
    - 'mesh-lmg'/'mesh-ldt' (IFF pair) loaderSource: 'swg-client-v2 LodDistanceTable.cpp:145-168'
    - 'mesh-sht' (IFF pair) loaderSource: 'swg-client-v2 StaticShaderTemplate.cpp:32-36,123-128,482-565 + CustomizableShaderTemplate.cpp:1246-1286'
    - 'mesh-pal' (PARSER-NATIVE pair parsePalette/serializePalette) loaderSource: 'swg-client-v2 PaletteArgb.cpp:508-522'
    - 'mesh-dds' (PARSER-NATIVE pair parseDds/serializeDds) loaderSource: 'swg-client-v2 standard Microsoft DDS spec (PaletteArgb/Texture format table not a DDS reader)' — ensure the string still matches the harness /swg-client-v2/ regex.

    STEP C — Binary-ArrayBuffer assertion for mesh geometry. After parseMesh(), assert result.geometry instanceof ArrayBuffer and Float32Array(result.geometry, positions.offset, positions.elementCount*positions.componentCount).length > 0 (the final flat attribute array, not the raw POSN pool size). Label the assertion comment "binary ArrayBuffer path / no JSON for geometry" — NOT "zero-copy".
  </action>
  <verify>
    <automated>pnpm --filter @swg/harness test 2>&1 | tail -20; echo "test-exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    .msh/.lmg/.ldt/.sht round-trips pass via the generic-IFF pair (serializeIff(parseIff(bytes)) === bytes byte-identical).
    .pal/.dds round-trips pass via the PARSER-NATIVE pair (serializePalette/serializeDds, NOT serializeIff) === bytes byte-identical.
    registry-coverage.test.ts sweep passes (all six format IDs have fixtures with loaderSource matching /swg-client-v2/).
    Binary-ArrayBuffer assertion passes: parseMesh returns geometry as ArrayBuffer (no JSON for geometry); indices proven Uint32 not Uint16.
    pnpm --filter @swg/harness test exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → C++ parser | Real .msh/.sht/.pal/.dds assets from a locally mounted TRE; treated as untrusted binary despite being from the user's own install |
| C++ parser → N-API bridge | Typed metadata crosses as JSON; binary payload crosses as one ArrayBuffer (memcpy into JS-owned memory); never serialize binary as JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | formats/Mesh.cpp parseShaderGroup | mitigate | Inherit Iff.h:146-149 security caps (per-chunk <= 64 MB, childEnd <= parentEnd, non-printable FourCC rejected); add count-bounds check: if (shaderGroupCount > 512) throw FormatParseError |
| T-02-02 | Tampering | geometry/DeIndex.cpp deIndex | mitigate | Bounds-check every PIDX/NIDX index against posPool.size()/normPool.size(); out-of-range index throws FormatParseError; Uint32 widening prevents silent truncation overflow of large meshes |
| T-02-03 | Tampering | formats/Dds.cpp parseDds | mitigate | Validate 128-byte DDS header magic (0x20534444); bounds-check mip table: sum of all mip byteLengths must not exceed (size - 128); reject if exceeded |
| T-02-04 | Tampering | formats/Palette.cpp parsePalette | mitigate | Validate RIFF/PAL /data tags; entryCount = dataChunkSize/4; bounds-check: entryCount * 4 + 24 must equal total file size or less; cap at 1024 entries (PaletteArgb.cpp:508-522) |
| T-02-05 | Tampering | formats/Shader.cpp parseShader | mitigate | Inherit IFF caps; cap slot count at 16, customizationVar count at 64; string paths validated as ASCII NUL-terminated within chunk bounds |
| T-02-SC | Tampering | npm/pnpm install (three, R3F, drei) | mitigate | Packages pre-vetted: slopcheck [OK] per 02-RESEARCH.md Package Legitimacy Audit; no postinstall scripts on three or R3F. |
</threat_model>

<verification>
1. `pnpm --filter @swg/contracts build` exits 0 — all four contract files compile.
2. `pnpm --filter @swg/native-core build` exits 0 — C++ parsers + binding compile.
3. `pnpm --filter @swg/harness test` exits 0 — six CORE-05 round-trips pass (4 IFF + 2 parser-native) + registry-coverage sweep.
4. `pnpm -r test` exits 0 — full suite green (all Phase-1 tests still pass).
5. Binary-ArrayBuffer assertion: parseMesh returns geometry as instanceof ArrayBuffer with Uint32 indices.
6. Manual check: parseMesh on a real .msh returns shaderGroups.length >= 1 with vertexCount > 0.
</verification>

<success_criteria>
- @swg/contracts exports MeshParseResult (with MeshAttributeSlice byte offsets), SkeletonParseResult, AnimationParseResult, ShaderParseResult, DdsParseResult, PaletteParseResult, LodParseResult.
- Native addon exposes parseMesh, parseMeshLod, parseShader, parsePalette (+ serializePalette), parseDds (+ serializeDds).
- CORE-05 round-trip: IFF pair for .msh/.lmg/.ldt/.sht; PARSER-NATIVE pair for .pal/.dds; six fixture registrations, sweep passes.
- de-index output verified: geometry ArrayBuffer contains flat attribute arrays with Uint32 indices (not raw POSN pool); binary-ArrayBuffer assertion passes.
- Security: FormatParseError thrown on malformed inputs; count-bounds checks on all array reads.
- packages/renderer has three@0.184.0 + @react-three/fiber@9.6.1 + @react-three/drei@10.7.7 installed.
</success_criteria>

<output>
Create `.planning/phases/02-3d-mesh-viewport-mvp-proof/02-01-SUMMARY.md` when done.
</output>
