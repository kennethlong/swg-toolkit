---
phase: 02-3d-mesh-viewport-mvp-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/package.json
  - packages/contracts/src/mesh.ts
  - packages/contracts/src/skeleton.ts
  - packages/contracts/src/animation.ts
  - packages/contracts/src/material.ts
  - packages/contracts/src/index.ts
  - packages/native-core/modules/core/formats/Mesh.h
  - packages/native-core/modules/core/formats/Mesh.cpp
  - packages/native-core/modules/core/formats/MeshLod.h
  - packages/native-core/modules/core/formats/MeshLod.cpp
  - packages/native-core/modules/core/formats/Shader.h
  - packages/native-core/modules/core/formats/Shader.cpp
  - packages/native-core/modules/core/formats/Palette.h
  - packages/native-core/modules/core/formats/Palette.cpp
  - packages/native-core/modules/core/formats/Dds.h
  - packages/native-core/modules/core/formats/Dds.cpp
  - packages/native-core/modules/core/geometry/DeIndex.h
  - packages/native-core/modules/core/geometry/DeIndex.cpp
  - packages/native-core/modules/core/CMakeLists.txt
  - packages/native-core/src/mesh_binding.cpp
  - packages/native-core/src/addon.cpp
  - packages/harness/test/mesh-roundtrip.test.ts
  - packages/harness/fixtures-real/.gitkeep
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02

must_haves:
  truths:
    - "The renderer package has three/R3F/drei installed and imports compile without errors"
    - "All four contract types (mesh/skeleton/animation/material) are exported from @swg/contracts"
    - "parseMesh() returns MeshParseResult with shaderGroups and an ArrayBuffer geometry payload (binary stays binary)"
    - "A real .msh asset's serializeIff(parseIff(bytes)) round-trip is byte-identical (SC-5 gate)"
    - "FORM MESH de-indexed attribute arrays (positions/normals/uvs/indices) are ready-to-upload — not the raw POSN pool"
    - ".lmg MLOD and .ldt LDTB parsers return LOD-level generator paths and distance ranges (D-02: parse ALL LOD levels; the user-selectable LodPicker UI lands in 02-02)"
    - ".sht SSHT/CSHD parsers return slot map (MAIN/NRML/SPEC/EMIS/ENVM) and customization pathway"
    - ".pal RIFF PAL parser returns RGBA entry array (24-byte header + entryCount×4)"
    - ".dds parser returns per-mip (offset, byteLength, format enum) table; compressed blocks pass through unchanged"
  artifacts:
    - path: "packages/contracts/src/mesh.ts"
      provides: "MeshParseResult, MeshShaderGroup, SkeletonParseResult, AnimationParseResult, MaterialSlotMap types"
    - path: "packages/native-core/modules/core/formats/Mesh.h"
      provides: "parseMesh() returning MeshResult with de-indexed BufferGeometry-ready attr arrays"
    - path: "packages/native-core/modules/core/geometry/DeIndex.h"
      provides: "deIndex() + normalizeSkinWeightsInto() C++ pre-bridge passes"
    - path: "packages/native-core/src/mesh_binding.cpp"
      provides: "N-API parseMesh/parseMeshLod/parseShader/parsePalette/parseDds exports"
    - path: "packages/harness/test/mesh-roundtrip.test.ts"
      provides: "CORE-05 byte-exact round-trip registrations for .msh/.lmg/.ldt/.sht/.pal/.dds"
  key_links:
    - from: "packages/native-core/src/mesh_binding.cpp"
      to: "modules/core/formats/Mesh.cpp + geometry/DeIndex.cpp"
      via: "parseMesh() calls parseMeshForm() then deIndex() C++-side before bridge"
      pattern: "swg_core::formats::parseMesh"
    - from: "packages/harness/test/mesh-roundtrip.test.ts"
      to: "fixtureRegistry.ts"
      via: "registerFormat('mesh-mesh', ...) with loaderSource citing swg-client-v2"
      pattern: "registerFormat"
---

<objective>
Install the R3F render stack, define the contract type surface for all Phase-2 formats, land the C++ parsers for static `.msh`, LOD graph (`.lmg`/`.ldt`), shader (`.sht` SSHT/CSHD), palette (`.pal`), and DDS header+mip table — then register each in the CORE-05 harness with a real-asset fixture.

Purpose: Establishes the typed interface contracts and the five support-format parsers that all later plans consume (materials/LOD/customization chain). The de-index utility (C++ pre-bridge pass) lands here too so the skinned mesh plan can use it directly.

Output: @swg/contracts exports four new type files; `packages/native-core` exposes `parseMesh`, `parseMeshLod`, `parseShader`, `parsePalette`, `parseDds`; CORE-05 sweep is green for all five formats; `packages/renderer` installs three/R3F/drei.
</objective>

<execution_context>
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-01-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Code\SWG-Toolkit\.planning\PROJECT.md
@D:\Code\SWG-Toolkit\.planning\ROADMAP.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-CONTEXT.md
@D:\Code\SWG-Toolkit\.planning\phases\02-3d-mesh-viewport-mvp-proof\02-RESEARCH.md
@D:\Code\SWG-Toolkit\.planning\research\CONSULT-P2-SYNTHESIS.md

<interfaces>
<!-- Phase-1 IFF API (verified from packages/native-core/modules/core/iff/Iff.h) -->
<!-- namespace swg_core::iff -->
<!--   parseIff(const uint8_t* data, uint32_t size) -> IffParseResult -->
<!--   serializeIff(IffParseResult&, const uint8_t* srcData, uint32_t srcSize) -> vector<uint8_t> -->
<!--   Security caps: per-chunk <= 64MB; childEnd <= parentEnd; non-printable FourCC rejected (Iff.h:146-149) -->
<!-- CORE-05 harness: packages/harness/fixtureRegistry.ts -->
<!--   registerFormat(id, { parse, serialize, fixtures[], loaderSource }) -->
<!--   loaderSource MUST match /swg-client-v2|Utinni|tre_reader\.py/ -->
<!-- Pattern analog: packages/native-core/src/iff_binding.cpp + packages/contracts/src/iff.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install R3F deps + define contract types for all Phase-2 formats</name>
  <files>
    packages/renderer/package.json,
    packages/contracts/src/mesh.ts,
    packages/contracts/src/skeleton.ts,
    packages/contracts/src/animation.ts,
    packages/contracts/src/material.ts,
    packages/contracts/src/index.ts
  </files>
  <read_first>
    packages/renderer/package.json — current deps (add three/R3F/drei here)
    packages/contracts/src/iff.ts — exact file-header and interface pattern to follow (lines 1–103)
    packages/contracts/src/index.ts — re-export barrel pattern
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § contracts/src/mesh.ts pattern (MeshShaderGroup, MeshParseResult, discriminated roundTrip)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-RESEARCH.md — § Standard Stack (exact versions) + § Architecture Patterns (binary-stays-binary, zero-copy field docs)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1 layouts for what each type models
  </read_first>
  <action>
    STEP A — Install render-stack deps. In packages/renderer run: pnpm add three@0.184.0 @react-three/fiber@9.6.1 @react-three/drei@10.7.7 and pnpm add -D @types/three@0.184.1. These are pre-audited slopcheck-OK packages from the RESEARCH legitimacy audit.

    STEP B — Write packages/contracts/src/mesh.ts. Follow the iff.ts file-header pattern exactly: ground-truth citation block at top naming swg-client-v2 sources, then typed interfaces. Export:
    - MeshShaderGroup: { shaderName: string; vertexCount: number; indexCount: number; geometryOffset: number; geometryByteLength: number; hasNormals: boolean; hasDot3: boolean; uvSetCount: number }
    - MeshParseResult: { formatTag: 'MESH' | 'SKMG'; version: string; shaderGroups: MeshShaderGroup[]; boneNames?: string[]; weightsTruncated?: number; roundTrip: { passed: boolean; failOffset?: number } }
    Binary geometry arrays (positions/normals/uvs/indices/skinIndices/skinWeights) cross as ArrayBuffer — document that explicitly in the file header per AGENTS.md zero-copy rule. Ground-truth citation: swg-client-v2 MeshAppearanceTemplate.cpp + SkeletalMeshGeneratorTemplate.cpp.

    STEP C — Write packages/contracts/src/skeleton.ts. Export:
    - BoneNode: { name: string; parentIndex: number; bindTranslation: [number,number,number]; bindRotation: [number,number,number,number] } — quat order is (w,x,y,z) on disk; IR MUST document the order (synthesis §5 open item: pick w,x,y,z and note it). Ground-truth: swg-client-v2 BasicSkeletonTemplate.cpp:151-389, SKTM chunks PRNT/BPTR/BPRO.
    - SkeletonParseResult: { version: string; bones: BoneNode[]; roundTrip: { passed: boolean; failOffset?: number } }

    STEP D — Write packages/contracts/src/animation.ts. Export:
    - AnimationVariant: 'KFAT-0003' | 'CKAT-0001' | 'KFAT-0002-unsupported'
    - AnimationJoint: { name: string; hasRotation: boolean; hasTranslation: boolean }
    - AnimationParseResult: { variant: AnimationVariant; fps: number; frameCount: number; joints: AnimationJoint[]; roundTrip: { passed: boolean; failOffset?: number } }
    Binary keyframe buffers cross as ArrayBuffer. Ground-truth: synthesis §1.6, CompressedKeyframeAnimation.cpp + CompressedQuaternion.cpp:82-100,370-419.

    STEP E — Write packages/contracts/src/material.ts. Export:
    - DdsMipEntry: { offset: number; byteLength: number; width: number; height: number; format: 'DXT1'|'DXT3'|'DXT5'|'DXT2'|'DXT4'|'RGBA8' }
    - DdsParseResult: { width: number; height: number; mipCount: number; mips: DdsMipEntry[]; roundTrip: { passed: boolean; failOffset?: number } }
    - PaletteEntry: { r: number; g: number; b: number; a: number }
    - PaletteParseResult: { entryCount: number; entries: PaletteEntry[]; roundTrip: { passed: boolean; failOffset?: number } }
    - ShaderSlotName = 'MAIN'|'NRML'|'CNRM'|'SPEC'|'EMIS'|'ENVM'|'MASK'
    - ShaderCustomizationPathway = 'palette-material-color'|'palette-texture-swap'|'palette-texture-factor'
    - ShaderSlot: { slot: ShaderSlotName; texturePath: string | null; uvSet: number }
    - ShaderCustomizationVar: { name: string; pathway: ShaderCustomizationPathway; palettePath: string; defaultIndex: number; affectedSlot?: ShaderSlotName }
    - ShaderParseResult: { variant: 'SSHT'|'CSHD'; effectPath: string; slots: ShaderSlot[]; customizationVars: ShaderCustomizationVar[]; roundTrip: { passed: boolean; failOffset?: number } }
    - LodLevel: { generatorPath: string; minDist: number; maxDist: number }
    - LodParseResult: { levelCount: number; levels: LodLevel[]; roundTrip: { passed: boolean; failOffset?: number } }
    Ground-truth: synthesis §1.4/§1.5/§1.7, StaticShaderTemplate.cpp:32-36,123-128 + CustomizableShaderTemplate.cpp:1246-1286 + PaletteArgb.cpp:517-521 + Texture.cpp:115-129 + LodDistanceTable.cpp.

    STEP F — Update packages/contracts/src/index.ts: add export * from './mesh.js'; export * from './skeleton.js'; export * from './animation.js'; export * from './material.js'; following the existing export barrel pattern.
  </action>
  <verify>
    <automated>pnpm --filter @swg/contracts build 2>&1 | tail -5; echo "exit:$?"</automated>
  </verify>
  <done>
    packages/renderer/package.json lists three@0.184.0, @react-three/fiber@9.6.1, @react-three/drei@10.7.7, @types/three@0.184.1 (dev).
    @swg/contracts builds without TypeScript errors.
    All four new type files export their interfaces with ground-truth citation comments.
    index.ts re-exports all four new modules.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C++ static mesh + support-format parsers (MESH/MLOD/LDTB/SSHT/CSHD/PAL/DDS) + de-index utility + N-API binding</name>
  <files>
    packages/native-core/modules/core/formats/Mesh.h,
    packages/native-core/modules/core/formats/Mesh.cpp,
    packages/native-core/modules/core/formats/MeshLod.h,
    packages/native-core/modules/core/formats/MeshLod.cpp,
    packages/native-core/modules/core/formats/Shader.h,
    packages/native-core/modules/core/formats/Shader.cpp,
    packages/native-core/modules/core/formats/Palette.h,
    packages/native-core/modules/core/formats/Palette.cpp,
    packages/native-core/modules/core/formats/Dds.h,
    packages/native-core/modules/core/formats/Dds.cpp,
    packages/native-core/modules/core/geometry/DeIndex.h,
    packages/native-core/modules/core/geometry/DeIndex.cpp,
    packages/native-core/modules/core/CMakeLists.txt,
    packages/native-core/src/mesh_binding.cpp,
    packages/native-core/src/addon.cpp
  </files>
  <behavior>
    - parseMesh on a real .msh FORM MESH v0005 returns shaderGroups with vertex/index counts > 0 and geometry ArrayBuffer length > 0
    - serializeIff(parseIff(mshBytes)) === mshBytes byte-identical (SC-5 round-trip)
    - parseMeshLod on a real .lmg returns LodParseResult with levelCount > 0 and generatorPath strings non-empty
    - parseShader on a real .sht SSHT returns slot array with at least MAIN slot and effectPath non-empty
    - parsePalette on a real .pal returns entryCount matching the RIFF header and entries[] length === entryCount
    - parseDds on a real .dds returns mipCount >= 1 and mips[0].format in ['DXT1','DXT3','DXT5']
    - deIndex() output positions array length === pidxCount * 3; indices array length = triangle count * 3 (shader-local)
  </behavior>
  <read_first>
    packages/native-core/modules/core/iff/Iff.h — parseIff/IffParseResult/IffNode API + security caps (Iff.h:146-149)
    packages/native-core/modules/core/compress/Zlib.h — engine-free utility header pattern (namespace/error/function signature shape)
    packages/native-core/src/iff_binding.cpp — thin-binding pattern: extractBytes helper + try/catch + ArrayBuffer return (lines 39-55, 147-224, 285-289)
    packages/native-core/src/addon.cpp — registration pattern (lines 84-119); extend the Init() with new Phase-2 exports
    packages/native-core/modules/core/CMakeLists.txt — how to add new source files to the build
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § formats/Mesh.{h,cpp} (namespace/error/security-count-cap pattern); § geometry/DeIndex.{h,cpp} (DeIndexedBuffers struct + function signatures); § mesh_binding.cpp (extractBytes + ParseMesh shape)
    .planning/research/CONSULT-P2-SYNTHESIS.md — §1.4 FORM MESH chunk set (VTXA flags/interleave, INDX, SPS, LSPT); §1.5 MLOD/LDTB; §1.7 SSHT/CSHD/PAL/DDS layouts — these are LOCKED axioms, do NOT re-derive from docs/
    ../swg-client-v2/src/engine/client/library/clientObject/src/shared/appearance/MeshAppearanceTemplate.cpp — MESH oracle
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/StaticShaderTemplate.cpp lines 32-36,123-128
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/CustomizableShaderTemplate.cpp lines 1246-1286
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/PaletteArgb.cpp lines 517-521
    ../swg-client-v2/src/engine/client/library/clientGraphics/src/shared/Texture.cpp lines 115-129
  </read_first>
  <action>
    All new C++ files follow the namespace/error/function-signature shape from Iff.h and the PATTERNS.md § formats/* section. All parsers are engine-free (no N-API, no SOE headers), consume parseIff() to get the IFF tree, then do typed extraction. Every count/array read MUST add a bounds check matching the security-cap pattern from PATTERNS.md (if count > kMax throw FormatParseError). Do NOT use fenced code blocks here; implement per the patterns cited.

    FILE: modules/core/formats/Mesh.h + Mesh.cpp
    Implement parseMesh(const swg_core::iff::IffNode& root) -> MeshResult.
    FORM MESH -> FORM 0005 -> [APPR] -> FORM SPS -> per-shader FORM 0001.
    Per PSDT (shader group, using SPS framing in .msh, NOT PSDT): extract NAME(.sht path), FORM VTXA->FORM 0003->INFO(flags,vert_count)+DATA(interleaved verts). VTXA interleave per synthesis §1.4: position(3f) always present; normal(3f) if bit set in flags; color0/1 (u32) if bits set; texcoords (per set, dim=(flags>>(12+2j)&3)+1). INDX = int32 count + uint16[] indices (LSPT 0001; v0000 has int32 indices). Call deIndex() per group to produce flat attribute arrays before returning. MeshResult stores per-group: shaderName, deIndexed geometry buffers (flat float32 positions/normals/uvs + uint16 indices).
    Oracle: swg-client-v2 MeshAppearanceTemplate.cpp + ShaderPrimitiveSetTemplate.cpp + VertexBuffer.cpp:247-307.

    FILE: modules/core/geometry/DeIndex.h + DeIndex.cpp
    Implement DeIndexedBuffers struct and deIndex() per PATTERNS.md § DeIndex exactly (posPool/normPool/pidx/nidx/uvs/prim parameters -> flat output). Also normalizeSkinWeightsInto() per the same PATTERNS.md section. .msh uses a different interleave (VTXA inline, not global POSN pool), so for static mesh deIndex() is simpler: the interleaved VTXA DATA is already per-vertex; extract position/normal/uv channels per vertex by reading the flags-driven interleave, output as flat arrays. PRIM/INDX indices are already shader-local uint16 — pass through.
    Source: synthesis §1.4 VTXA interleave + RESEARCH Pattern 2/3.

    FILE: modules/core/formats/MeshLod.h + MeshLod.cpp
    parseMeshLod(root) -> LodResult. FORM MLOD -> FORM 0000 -> INFO(level_count int16) + NAME[] strings (one per level = generator path). FORM LDTB -> FORM 0000 -> INFO(level_count) + per-level (minDist float32, maxDist float32) pairs. These may be two separate calls (one for .lmg, one for .ldt) or one function taking both roots. Return LodResult { levelCount, generatorPaths[], distances[] }.
    Oracle: synthesis §1.5 + LodDistanceTable.cpp.

    FILE: modules/core/formats/Shader.h + Shader.cpp
    parseShader(root) -> ShaderResult. Discriminate on root tag: 'SSHT' (static) or 'CSHD' (customizable).
    SSHT: read effectPath (the .eft file reference); parse TXMS form for texture slots: per slot read tag (MAIN/NRML/CNRM/SPEC/EMIS/ENVM/MASK), texture path string, UV set index from TCSS. Populate ShaderResult { variant:'SSHT', effectPath, slots[], customizationVars:[] }.
    CSHD: read the inner SSHT first (wraps it), then parse customization variables: pathway A = MATR (palette path, defaultIndex, affects material color); pathway B = TXTR/TX1D (palette path, defaultIndex, replaces texture slot); pathway C = TFAC/PAL (palette path, defaultIndex, texture factor tint).
    Oracle: StaticShaderTemplate.cpp:32-36,123-128 + CustomizableShaderTemplate.cpp:1246-1286.

    FILE: modules/core/formats/Palette.h + Palette.cpp
    parsePalette(data, size) -> PaletteResult. RIFF PAL: 24-byte header (verify 'RIFF'/'PAL '/'data' tags); entryCount from the data chunk size / 4; read entryCount × (R,G,B,A) bytes. Version 3 => alpha forced to 255 per synthesis §1.7.
    Oracle: PaletteArgb.cpp:517-521.

    FILE: modules/core/formats/Dds.h + Dds.cpp
    parseDds(data, size) -> DdsResult. Read DDS header (128 bytes): magic 0x20534444, DWORD flags, height, width, pitchOrLinearSize, depth, mipMapCount. Read DDS_PIXELFORMAT to determine format (FOURCC DXT1/DXT3/DXT5/DXT2/DXT4 or uncompressed RGBA8). Build per-mip table: mip 0 offset = 128, compute size per mip from DXT block math (ceil(max(1,w/4)) * ceil(max(1,h/4)) * blockBytes where blockBytes=8 for DXT1, 16 for DXT3/DXT5/DXT2/DXT4). Compressed blocks STAY as-is in the source buffer — do NOT decode them.
    Oracle: Texture.cpp:115-129.

    FILE: modules/core/CMakeLists.txt
    Add formats/Mesh.cpp, formats/MeshLod.cpp, formats/Shader.cpp, formats/Palette.cpp, formats/Dds.cpp, geometry/DeIndex.cpp to the swg_core static lib source list. Create formats/ and geometry/ subdirectory entries.

    FILE: src/mesh_binding.cpp
    Follow iff_binding.cpp exactly: extractBytes helper (copy verbatim from iff_binding.cpp:39-55); one Napi::Value function per export: ParseMesh, ParseMeshLod, ParseShader, ParsePalette, ParseDds. Each: arg-count guard -> type guard -> extractBytes -> try { parse -> deIndex if mesh -> build Napi::Object result (typed JSON metadata) + Napi::ArrayBuffer for geometry (geometry bytes only — never JSON for binary) } -> two-tier catch. Return contract: geometry crosses as ArrayBuffer (Napi::ArrayBuffer::New + memcpy from PATTERNS.md § mesh_binding.cpp). Typed metadata (group count, bone names, slot map, mip table) crosses as Napi::Object/Napi::String/Napi::Number/Napi::Array.

    FILE: src/addon.cpp
    Extend with forward declarations for ParseMesh, ParseMeshLod, ParseShader, ParsePalette, ParseDds (implemented in mesh_binding.cpp). Add to Init(): exports.Set("parseMesh", ...), exports.Set("parseMeshLod", ...), exports.Set("parseShader", ...), exports.Set("parsePalette", ...), exports.Set("parseDds", ...). Follow the existing per-phase comment block pattern from the current file.

    Rebuild: in packages/native-core run node-gyp rebuild or cmake-js build (per Phase-0/1 build convention). The cmake-js generator is pinned to 'Visual Studio 17 2022' x64 (STATE.md D decision).
  </action>
  <verify>
    <automated>cd "D:\Code\SWG-Toolkit" && pnpm --filter @swg/native-core build 2>&1 | tail -10; echo "build-exit:$?"</automated>
  </verify>
  <done>
    packages/native-core builds without C++ errors.
    The addon exports parseMesh, parseMeshLod, parseShader, parsePalette, parseDds callable from Node.
    deIndex() produces flat attribute arrays (not the raw POSN pool) before any buffer crosses the bridge.
    All format parsers apply count-bounds checks per the security-cap pattern; FormatParseError thrown on malformed input.
    .msh FORM MESH v0005 parses successfully with shaderGroups.length >= 1.
  </done>
</task>

<task type="auto">
  <name>Task 3: CORE-05 round-trip fixtures for .msh/.lmg/.ldt/.sht/.pal/.dds + zero-copy assertion</name>
  <files>
    packages/harness/test/mesh-roundtrip.test.ts,
    packages/harness/fixtures-real/.gitkeep
  </files>
  <read_first>
    packages/harness/test/iff-roundtrip.test.ts — exact file-header, import pattern, assertRoundTrip usage, registerFormat call shape (lines 1–46, 124–141)
    packages/harness/test/tre-async-zerocopy.test.ts — zero-copy ArrayBuffer assertion pattern (lines 158–177) — mirror this for geometry
    packages/harness/fixtureRegistry.ts — registerFormat signature, loaderSource regex /swg-client-v2|Utinni|tre_reader\.py/ (lines 78–90, 107)
    packages/harness/assertRoundTrip.ts — assertRoundTrip function signature
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-PATTERNS.md — § harness/test/mesh-roundtrip.test.ts (complete file-header + parseMgn/serializeMgn IFF-layer pair + zero-copy assertion pattern)
    .planning/phases/02-3d-mesh-viewport-mvp-proof/02-RESEARCH.md — § Validation Architecture table (each format's real fixture + cited oracle line)
  </read_first>
  <action>
    STEP A — Extract real fixtures. The gitignored packages/harness/fixtures-real/ directory is the scratch dir for real assets. Use the Phase-1 TRE extractor (nativeCore.mountSearchableAsync / readMountEntry from the existing bindings) to extract one real asset of each format from a mounted client:
    - fixtures-real/mesh/player_human_m_appearance.msh (or any FORM MESH v0005 asset)
    - fixtures-real/mesh/player_human_m_skel_l0.lmg (LOD file) + matching .ldt
    - fixtures-real/shader/any_material.sht (SSHT or CSHD)
    - fixtures-real/palette/any_palette.pal (RIFF PAL)
    - fixtures-real/texture/any_texture.dds (DXT1 or DXT5)
    Use the copy-real-fixtures.js pattern from Phase 1 (D-09/D-10: copies only, never mutates originals). If no extraction tooling exists yet as a standalone script, write a one-shot Node script that mounts the client TRE and extracts the above files into fixtures-real/ — this script is gitignored (per D-10 discipline: real .tre archives and their extracted assets are never committed).

    STEP B — Write packages/harness/test/mesh-roundtrip.test.ts. Follow the iff-roundtrip.test.ts pattern exactly:
    File header with cited oracle lines per PATTERNS.md § harness/test/mesh-roundtrip.test.ts.
    Import pattern from PATTERNS.md (nativeCore require block; include parseIff, serializeIff, parseMesh, parseMeshLod, parseShader, parsePalette, parseDds).

    Round-trip pair for each format uses the IFF layer (Pattern 1 from RESEARCH): parseFoo = parseIff(bytes), serializeFoo = serializeIff(iffResult, srcBytes). The typed parse (parseMesh etc.) is invoked separately for behavioral assertions; the IFF round-trip gives byte-exact gate for free.

    Per-format test blocks:
    - .msh (MESH): assertRoundTrip with .msh fixture; also assert result.geometry instanceof ArrayBuffer (zero-copy check — mirror PATTERNS.md § zero-copy assertion pattern); assert result.shaderGroups.length > 0.
    - .lmg/.ldt (MLOD/LDTB): assertRoundTrip; assert levels.length > 0 and levels[0].generatorPath is non-empty string.
    - .sht (SSHT/CSHD): assertRoundTrip; assert result.slots.some(s => s.slot === 'MAIN').
    - .pal: assertRoundTrip; assert result.entries.length > 0.
    - .dds: assertRoundTrip; assert result.mips[0].format matches /DXT[135]/.

    registerFormat calls for each: 'mesh-mesh', 'mesh-lmg', 'mesh-ldt', 'mesh-sht', 'mesh-pal', 'mesh-dds'. loaderSource values:
    - mesh-mesh: 'swg-client-v2 MeshAppearanceTemplate.cpp + VertexBuffer.cpp:247-307'
    - mesh-lmg/ldt: 'swg-client-v2 LodDistanceTable.cpp'
    - mesh-sht: 'swg-client-v2 StaticShaderTemplate.cpp:32-36,123-128 + CustomizableShaderTemplate.cpp:1246-1286'
    - mesh-pal: 'swg-client-v2 PaletteArgb.cpp:517-521'
    - mesh-dds: 'swg-client-v2 Texture.cpp:115-129'

    STEP C — Zero-copy assertion for mesh geometry. After parseMesh(), assert result.geometry instanceof ArrayBuffer and the underlying Float32Array(result.geometry).length > 0 and is the final flat attribute array (not the raw POSN pool size). Mirror the exact pattern from PATTERNS.md § zero-copy assertion.
  </action>
  <verify>
    <automated>pnpm --filter @swg/harness test 2>&1 | tail -20; echo "test-exit:$?"</automated>
  </verify>
  <done>
    All CORE-05 round-trip tests pass (serializeIff(parseIff(bytes)) === bytes byte-identical for .msh, .lmg, .ldt, .sht, .pal, .dds).
    registry-coverage.test.ts sweep passes (all six new format IDs have fixtures with valid loaderSource citations matching /swg-client-v2/).
    Zero-copy assertion passes: parseMesh returns geometry as ArrayBuffer (not JSON).
    pnpm --filter @swg/harness test exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → C++ parser | Real .msh/.sht/.pal/.dds assets from a locally mounted TRE; treated as untrusted binary despite being from the user's own install |
| C++ parser → N-API bridge | Typed metadata crosses as JSON; binary payload crosses as ArrayBuffer; never serialize binary as JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | formats/Mesh.cpp parseShaderGroup | mitigate | Inherit Iff.h:146-149 security caps (per-chunk <= 64 MB, childEnd <= parentEnd, non-printable FourCC rejected); add count-bounds check: if (shaderGroupCount > 512) throw FormatParseError |
| T-02-02 | Tampering | geometry/DeIndex.cpp deIndex | mitigate | Bounds-check every PIDX/NIDX index against posPool.size()/normPool.size(); out-of-range index throws FormatParseError, never reads OOB |
| T-02-03 | Tampering | formats/Dds.cpp parseDds | mitigate | Validate 128-byte DDS header magic (0x20534444); bounds-check mip table: sum of all mip byteLengths must not exceed (data+size) - 128; reject if exceeded |
| T-02-04 | Tampering | formats/Palette.cpp parsePalette | mitigate | Validate RIFF/PAL /data tags; entryCount = dataChunkSize/4; bounds-check: entryCount * 4 + 24 must equal total file size or less; cap at 1024 entries (synthesis §1.7) |
| T-02-05 | Tampering | formats/Shader.cpp parseShader | mitigate | Inherit IFF caps; cap slot count at 16, customizationVar count at 64; string paths validated as ASCII NUL-terminated within chunk bounds |
| T-02-SC | Tampering | npm/pnpm install (three, R3F, drei) | mitigate | Packages pre-vetted: slopcheck [OK] per 02-RESEARCH.md Package Legitimacy Audit; no postinstall scripts on three or R3F. Human-verify gate below. |
</threat_model>

<verification>
1. `pnpm --filter @swg/contracts build` exits 0 — all four contract files compile.
2. `pnpm --filter @swg/native-core build` exits 0 — C++ parsers + binding compile.
3. `pnpm --filter @swg/harness test` exits 0 — all six CORE-05 round-trip fixtures pass + registry-coverage sweep passes.
4. `pnpm -r test` exits 0 — full suite green (all Phase-1 tests still pass).
5. Zero-copy assertion: parseMesh returns geometry as instanceof ArrayBuffer.
6. Manual check: parseMesh on a real .msh returns shaderGroups.length >= 1 with vertexCount > 0.
</verification>

<success_criteria>
- @swg/contracts exports MeshParseResult, SkeletonParseResult, AnimationParseResult, ShaderParseResult, DdsParseResult, PaletteParseResult, LodParseResult.
- Native addon exposes parseMesh, parseMeshLod, parseShader, parsePalette, parseDds (callable from Node and Electron renderer per Path-B posture).
- CORE-05 round-trip: serializeIff(parseIff(bytes)) === bytes for each of the six new formats (six fixture registrations, sweep passes).
- de-index output verified: geometry ArrayBuffer contains flat attribute arrays (not raw POSN pool); zero-copy assertion passes.
- Security: FormatParseError thrown on malformed inputs; count-bounds checks on all array reads.
- packages/renderer has three@0.184.0 + @react-three/fiber@9.6.1 + @react-three/drei@10.7.7 installed.
</success_criteria>

<output>
Create `.planning/phases/02-3d-mesh-viewport-mvp-proof/02-01-SUMMARY.md` when done.
</output>
