---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 11
type: execute
wave: 5
depends_on: ["05-10"]
files_modified:
  - packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx
  - packages/renderer/src/panels/viewport/TransformReadoutBar.tsx
  - packages/renderer/src/shell/StatusBar.tsx
  - packages/renderer/src/panels/viewport/Viewport.tsx
  - .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md

# REVISION NOTE (--reviews replan, 2026-07-08): folds in the HIGH findings this plan directly reflects
# (05-REVIEWS.md), plus 05-CONTEXT decision #1 (target identity).
# (C) HIGH — the guard-blocked banner needs a REAL `<addr>` the channel never carried, and the
#     "expected" bytes were wrong after write #1. FIXED: 05-01 added `GUARD_ADDR` (a real x86 pointer,
#     agent-authoritative) to the channel; 05-07 added `expectedTransform()`/`expectedScale()`
#     SELECTORS on liveStore that derive from the last SUCCESSFUL write-log entry (not the stale
#     attach-time cowSnapshot). This plan's Task 1 now sources BOTH values correctly instead of
#     hand-deriving them from `cowSnapshot`/`verifiedState` as the original plan's Task 1 did.
# (B) HIGH — Scale had no guard/revert; "Revert ALL" reverted transform using CURRENT scale. FIXED:
#     05-07's `guardState` is now per-channel ({transform, scale}); `revertAll`/`revertWrite` now
#     restore the HISTORICAL scale from the write-log/snapshot. This plan's Task 1 now renders BOTH
#     guard rows (transform + scale) instead of one combined row, and Task 2's "Revert ALL" button
#     already calls the corrected liveStore action — no NEW logic here, just correct consumption +
#     correct copy (no longer implying a single combined guard state).
# Opus MEDIUM (a blocked guard also blocks its own revert) — 05-07's `revertAll`/`revertWrite` now send
#     REBASELINE_GUARD before the actual revert write. This plan's "Revert ALL" button already calls
#     that (unchanged) action — no new work here beyond confirming the button's disabled-state logic
#     never disables Revert ALL purely because the guard is blocked (it must remain clickable in that
#     exact state, since un-sticking a blocked guard IS what it is for).
# Decision #1 (target identity) — the client card's `client` row is extended with a `target` row
#     (mirrors 05-10's inline gizmo label at the HUD-card level, the authoritative full-detail
#     location) showing the SAME hasTarget/targetUnavailableOnBuild-derived copy.
# LOW hygiene (Sonnet) — client-card banners now carry `role="status"` (guard-ok/reverted) or
#     `role="alert"` (guard-blocked) for aria parity, folded in cheaply while this plan already builds
#     every banner from scratch.

must_haves:
  truths:
    - "Every element of UI-SPEC Surface 1 is present: shading chips (pre-existing, unchanged), live-sync client card (item 2, now with a target-identity row and TWO independent guard rows), gizmo-mode rail (05-10), transform gizmo (05-10), bottom transform readout bar (item 5), floating delta readout while dragging (item 6), corner axis gizmo + vp-stats (item 7 — NEW, does not exist yet), statusbar mirror (item 8)."
    - "All three B2 safety states (normal write / guard blocked / reverted) render with the exact locked copy structure, and guard-blocked shows a REAL address (from the channel's GUARD_ADDR field) + REAL expected/got bytes (expected from liveStore's expectedTransform()/expectedScale() selectors, which track the last successful write — never a placeholder, never a stale attach-time-only value)."
    - "The guard-blocked state is reported PER CHANNEL (transform vs scale independently) — Scale is never silently excluded from the guard/revert safety story (REVIEWS.md Fix B)."
    - "'Revert ALL to snapshot' restores BOTH the attach-time transform AND the attach-time (or first-write-baselined) scale — never reverting transform while leaving scale at whatever it currently is."
    - "'Revert ALL to snapshot' remains clickable even while guardState is 'blocked' — it is the user's sanctioned way to un-stick a blocked guard (Opus MEDIUM fix), never disabled purely because the guard tripped."
    - "Every surface (chip, card, readout, statusbar) switches together on live<->offline — verified by a single state change driving all four simultaneously, not four independently-wired copies of the same logic."
    - "HUD numbox/delta text updates during drag are imperative (refs/direct DOM), never per-frame React state churn (LIVE-03 SC1)."
    - "The transform readout bar's write-target indicator uses the SAME D-05 disabled-with-reason copy as the gizmo when offline ('○ Offline — attach a client to move objects live') — NOT '⇄ staging patch → staged (patch)' — and the Pos/Rot/Scale numboxes are non-interactive offline, matching the gizmo's actual disabled state (D-05 erratum; the numbox write path is identical to a gizmo drag)."
    - "The client card honestly states what object is being tracked/moved (target vs. player fallback vs. build-unavailable), mirroring 05-10's inline gizmo label at full detail (05-CONTEXT decision #1)."
  artifacts:
    - path: "packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx"
      provides: "224px top-right card: live chip, client/target/pid/fps/last-sync/COW/TWO guard rows (transform+scale), write log with per-write revert, Revert ALL (always clickable while attached), guard-blocked + reverted banners with real addr/bytes and role=alert/status"
      contains: "Revert ALL to snapshot"
    - path: "packages/renderer/src/panels/viewport/TransformReadoutBar.tsx"
      provides: "Bottom-center Pos/Rot/Scale numboxes (52px right-aligned mono, disabled offline) + disabled-with-reason/live write-target indicator + floating drag-delta readout"
      contains: "Offline — attach a client to move objects live"
  key_links:
    - from: "packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "reads writeLog/guardState{transform,scale}/cowSnapshot/expectedTransform()/expectedScale()/verifiedState.hasTarget; calls revertWrite(id)/revertAll() on button click"
      pattern: "expectedTransform"
    - from: "packages/renderer/src/shell/StatusBar.tsx"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "new mode/guard/COW segments read the SAME store fields the HUD card reads — one source of truth"
      pattern: "guardState"
---

<objective>
Complete Surface 1: the live-sync client card (with the full B2 safety-state anatomy, now correctly
sourcing a REAL guard-blocked address and a REAL, always-current "expected" baseline, and reporting
transform/scale guard state independently), the transform readout bar (with imperative drag-delta
updates and the D-05-corrected disabled-with-reason offline write-target indicator, matching the
gizmo's own offline behavior), the missing corner-axis-gizmo/vp-stats item, and the StatusBar mirror —
wiring everything into Viewport.tsx so all four HUD surfaces switch live/offline together from one
source of truth, and honestly reflect target-vs-player identity.

Purpose: this closes the remaining UI-SPEC Surface 1 elements and the D-03/D-04 safety-state UX
(SC #2 — a bad write is revertible via the changeset/snapshot system, INCLUDING scale, and INCLUDING
when the guard is currently blocked), completing LIVE-03's user-facing anatomy, and folds in the D-05
correction consistently across BOTH surfaces that share the gizmo's write path (the gizmo itself,
05-10, and this plan's readout-bar numboxes).

Output: `LiveSyncClientCard.tsx`, `TransformReadoutBar.tsx`, extended `StatusBar.tsx`, the final
Viewport.tsx overlay-mount wiring including the new corner-gizmo/vp-stats element, and a confirmed-
consistent `05-UI-SPEC.md` D-05 erratum. The HUD chrome around them (client card, readout bar,
StatusBar mirror) is this plan; the gizmo/mode-rail themselves are 05-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-10-PLAN.md
</context>

<interfaces>
UI-SPEC Surface 1 remaining items (this plan's scope; items 1/3/4 are pre-existing/05-10):

Item 2 — Live-sync client card, top-right (224px), translucent, accent-line border (dashed warn
offline); header `● Live · injected` / `○ Offline` per state-encoding table; body rows (mono):
`client`, **`target`** (NEW — 05-CONTEXT decision #1: `<templateName>` when
`verifiedState.hasTarget===true`; `none — moving player avatar` when attached with no target set;
`unavailable on this build — moving player avatar` when `verifiedState.targetUnavailableOnBuild===
true`; omitted/`n/a` when offline), `pid`, `fps` (— offline), `last sync` (`0.2s ago` / `n/a` offline),
`COW snapshot` (`✓ saved · <HH:MM:SS>` live / `○ staged` offline; offline adds `mode: file-patch
fallback` warn row); **TWO independent read-verify guard rows (REVIEWS.md Fix B — was one combined
row)**: `transform: ✓ client bytes = snapshot` / `✗ client bytes ≠ snapshot` AND `scale: ✓ client
bytes = snapshot` / `✗ client bytes ≠ snapshot` (scale row reads `— not yet written this session` if
no scale write has happened yet, since there is no scale baseline to compare against per 05-07's
lazy-capture note); session write log (`WRITE LOG · THIS SESSION`, faint uppercase mono header) — each
row: relative time + delta (`pos.x +0.42m`, mono) + right-aligned `revert` ghost button (hover→danger);
`⟲ Revert ALL to snapshot` button (danger-outline, full-width, ALWAYS clickable while attached —
REVIEWS.md Opus MEDIUM fix: never disabled purely because a guard channel is blocked, since this is
the sanctioned way to un-stick one) below the log.

Item 5 — Bottom transform readout bar, bottom-center: translucent bar, Pos/Rot/Scale groups (X/Y/Z
axis tags + 52px right-aligned mono numboxes), border-left-separated write-target indicator: **CORRECTED
per D-05 (05-CONTEXT.md; see 05-UI-SPEC.md "## Errata" item 1) — this is NOT `⇄ writing to client →
client ✓` (live) / `⇄ staging patch → staged (patch)` (offline).** The offline half of that copy is the
UI-SPEC's ORIGINAL (now-superseded) assumption. The numbox edit is the SAME write path as a gizmo drag
(same `writeTransform` call — UI-SPEC), and offline there is no write path at all (no `.ws` file target
until Phase 7 — D-05) — so the correct contract is: **live** → `⇄ writing to client → client ✓`
(accent, numboxes editable); **offline** → the SAME disabled-with-reason copy 05-10's TransformGizmo
uses, exactly `○ Offline — attach a client to move objects live` (warn/dashed, numboxes non-interactive
— no "staged (patch)" anywhere on this surface). Numboxes mirror the gizmo live during drag AND accept
typed values when live (numbox edit = same write path as a drag); offline, numboxes render disabled
(grayed, non-editable) — never a fake local-only edit.

Item 6 — Floating delta readout while dragging, top-center, ONLY during drag (drag can only happen
live, since the gizmo is disabled offline per D-05 — this readout therefore never needs an offline
variant): `Δ pos.x +0.42m → client ✓` (axis accent-bold, value mono).

Item 7 — Corner axis gizmo (bottom-right, NEW — does not exist yet, confirmed by this session's grep)
+ vp-stats (bottom-left): `persp · <W>×<H> · <fps> fps · SAB ✓` (faint mono, NEW format — distinct
from the EXISTING verts/tris/draws stats overlay, which stays as-is).

Item 8 — Statusbar mirror (extend StatusBar.tsx): mesh name · `mode: Move (W)` · `sync: ● Live ·
injected · pid <pid>` (or `○ Offline — file-patch fallback`, warn) · `COW snapshot ✓` · `guard:
read-verify ✓` (or a per-channel-aware variant `guard: read-verify ✗ (transform)` /
`guard: read-verify ✗ (scale)` / `guard: read-verify ✗ (transform, scale)` when one or both channels
are blocked — REVIEWS.md Fix B: never collapse two independent guard states into one ambiguous chip).

B2 safety states (all three; copy STRUCTURE from the Copywriting Contract, values now REAL not
placeholder — REVIEWS.md Fix C):
- Guard-blocked banner: `✗ Write refused. The object's memory changed outside the toolkit (<addr>:
  expected <bytes>, read <bytes>) — the game or another tool moved it. Nothing was written.` where
  `<addr>` is `LIVE_CHANNEL_LAYOUT.GUARD_ADDR` (a real x86 pointer, agent-published, formatted as
  `0x%08x`), `expected <bytes>` is `liveStore.expectedTransform()`/`expectedScale()` (whichever
  channel is blocked — the LAST SUCCESSFUL write's resulting value, or the attach-time snapshot if no
  write has succeeded yet; NEVER unconditionally `cowSnapshot`), and `read <bytes>` is the CURRENT
  `verifiedState.transform` (the live bytes the guard actually compared against, which is what caused
  the refusal). `role="alert"` on this banner. Actions: `↻ Re-read & retry` (ghost) + `⟲ Revert to
  snapshot` (danger-outline). NO force-write affordance.
- Reverted banner: `✓ Reverted. Transform restored from the COW snapshot taken at attach
  (<HH:MM:SS>). The in-game object snapped back — nothing is staged.` `role="status"` on this banner.
  Write-log rows go struck-through at ~0.55 opacity with revert buttons removed.

Existing StatusBar.tsx segment idiom to extend (lines 338-344, `<Dot/>` separator helper at 422-424):
```tsx
<Dot />
<span><span style={{ color: liveMode === 'live' ? 'var(--color-accent)' : 'var(--color-text-muted)' }}>
  {liveMode === 'live' ? '● Live' : '○ File-patch'}
</span></span>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LiveSyncClientCard — full anatomy + target-identity row + TWO independent guard rows + real addr/expected-bytes + all three B2 states</name>
  <files>packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (MissingDepsOverlay pattern — the absolutely-positioned overlay-outside-Canvas idiom this card follows)
    - packages/renderer/src/state/liveStore.ts (05-07 — VERIFIED shapes: status/verifiedState (incl. hasTarget/targetUnavailableOnBuild)/cowSnapshot{transform,scale}/writeLog (with resultingTransform+resultingScale)/guardState{transform,scale}/expectedTransform()/expectedScale() selectors/revertWrite/revertAll actions)
    - packages/contracts/src/live-inject.ts (05-01 — `LIVE_CHANNEL_LAYOUT.GUARD_ADDR` offset/length; this is the ONLY sanctioned source for the banner's `<addr>`)
    - 05-UI-SPEC.md Surface 1 item 2 + the B2 states block + the exact Copywriting Contract strings (do not paraphrase — the banner/row copy STRUCTURE is locked verbatim; the VALUES are now real per REVIEWS.md Fix C)
    - 05-REVIEWS.md Fix B (two independent guard rows), Fix C (real addr + correctly-derived expected bytes), Opus MEDIUM (Revert ALL always clickable) — this task closes all three
  </read_first>
  <action>
    Create LiveSyncClientCard.tsx: a 224px translucent card (`rgba(16,20,20,.78-.9)` bg +
    `backdrop-filter: blur(4-5px)` per UI-SPEC Color, accent-line solid border when
    `status.kind==='attached'` / dashed warn border otherwise). Header: live chip per the
    live/offline state-encoding table (glyph+border+label). Body rows reading from liveStore:
    `client` (the attached exe name/label — derive from whatever field the existing attach flow
    already tracks, e.g. status when attached may need a client-label field; if not currently
    tracked, source it from the `clientExe`/`pid` the attach call was made with, threading it through
    if liveStore does not already retain it — check 05-07's liveStore shape first). `target` (NEW —
    05-CONTEXT decision #1): when `status.kind==='attached'` — `verifiedState.hasTarget===true` →
    `<verifiedState.templateName>`; `targetUnavailableOnBuild===true` → `unavailable on this build —
    moving player avatar`; else → `none — moving player avatar`; when offline, omit this row entirely
    (mirrors how `pid`/`fps` already only render live). `pid`, `fps` (compute from the actual
    poll-tick rate over a short rolling window, or read from an existing fps counter if Viewport.tsx
    already tracks one — do not hardcode), `last sync` (time since the most recent `updateState`
    call, formatted `<N>s ago` or `n/a` offline), `COW snapshot` (`✓ saved · <HH:MM:SS>` once
    `cowSnapshot.transform` is non-null, `○ staged` before capture), `mode: file-patch fallback`
    (warn, offline only). TWO independent read-verify guard rows (REVIEWS.md Fix B): `transform: ✓
    client bytes = snapshot` when `guardState.transform==='ok'`, `✗ client bytes ≠ snapshot` when
    `'blocked'`; `scale: ✓ client bytes = snapshot` when `guardState.scale==='ok'` AND
    `cowSnapshot.scale !== null` (a scale baseline exists), `✗ client bytes ≠ snapshot` when
    `guardState.scale==='blocked'`, or `— not yet written this session` when `cowSnapshot.scale ===
    null` (no scale write has happened yet this session, so there is nothing to compare — an honest
    "not applicable yet" state, not a fabricated pass). Session write log: header `WRITE LOG · THIS
    SESSION` (faint uppercase mono), each `writeLog` entry rendered as relative-time + delta label
    (already formatted by liveStore per 05-07's `appendWriteLog` shape — if the delta LABEL itself is
    not yet computed there, compute it here as `<channel> <±value><unit>` per the Copywriting
    Contract, e.g. `pos.x +0.42m`) + a `revert` ghost button calling `revertWrite(entry.id)` (hover
    turns danger). `⟲ Revert ALL to snapshot` full-width danger-outline button calling `revertAll()`
    — this button's `disabled` prop is `status.kind !== 'attached'` ONLY; it must NEVER also check
    `guardState` (Opus MEDIUM fix — a blocked guard is exactly the situation this button exists to
    resolve, via the rebaseline-then-revert sequence liveStore already implements). Implement all
    three B2 states as conditionally-rendered banner blocks (state = 'normal' when both guard
    channels are 'ok', 'guard-blocked' when either is 'blocked', 'reverted' as a transient
    post-revertAll state — executor's choice of exact transition trigger, document it): normal (no
    banner, both guard rows green/not-applicable, log active); guard-blocked (card border flips to
    danger, the BLOCKED channel's guard row shows the ✗ variant, banner `role="alert"` with text
    EXACTLY `✗ Write refused. The object's memory changed outside the toolkit (<addr>: expected
    <bytes>, read <bytes>) — the game or another tool moved it. Nothing was written.` where `<addr>`
    is read from the channel's `GUARD_ADDR` field (formatted `0x` + 8 hex digits — thread this value
    through from `useChannelReader`'s decode, extending `VerifiedObjectState` or a sibling
    liveStore field if not already carried — this is a REAL value now, per 05-01/05-03, never
    derived/fabricated client-side), `expected <bytes>` is `liveStore.expectedTransform()` or
    `.expectedScale()` (whichever channel is blocked — these selectors already correctly derive
    "last successful write, or attach snapshot if none yet" per 05-07 Fix C, so this component just
    CALLS them, it does not re-derive), and `read <bytes>` is the CURRENT `verifiedState.transform`/
    a tracked current scale value (the live bytes that caused the mismatch); `↻ Re-read & retry`
    ghost button + `⟲ Revert to snapshot` danger-outline button, explicitly NO force-write
    button/prop/path anywhere in this component); reverted (accent-toned banner, `role="status"`,
    copy EXACTLY `✓ Reverted. Transform restored from the COW snapshot taken at attach
    (<HH:MM:SS>). The in-game object snapped back — nothing is staged.`, write-log rows render
    struck-through at 0.55 opacity with their revert buttons removed).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- LiveSyncClientCard</automated>
  </verify>
  <acceptance_criteria>
    - The guard-blocked banner's copy matches the locked string exactly with a REAL (non-placeholder, non-fabricated) address sourced from the channel's GUARD_ADDR field and expected/got values sourced from `expectedTransform()`/`expectedScale()` + current `verifiedState` — a test supplying known liveStore/channel values asserts the exact rendered text, including the address.
    - A test asserts the banner's `expected` value reflects the LAST write-log entry's `resultingTransform` (not `cowSnapshot`) when at least one write has succeeded before the block — proving Fix C is actually consumed, not just available.
    - Two independent guard rows are rendered (transform and scale), and a test asserts `guardState.scale==='blocked'` alone (with `guardState.transform==='ok'`) flips ONLY the scale row's glyph, leaving transform's row and the card's overall danger-border state consistent with "at least one channel blocked" (border still flips — a single blocked channel is still a blocked-guard state overall) without misreporting transform as blocked too.
    - `⟲ Revert ALL to snapshot`'s `disabled` prop is `false` whenever `status.kind==='attached'`, REGARDLESS of `guardState` value (a test with `guardState.transform==='blocked'` asserts the button remains enabled/clickable).
    - No button, prop, or code path in this file can force a write while a guard channel is 'blocked' (grep confirms zero references to any "force"/"override" write function).
    - Reverted-state write-log rows render with `text-decoration: line-through` and no `revert` button element present; the reverted banner has `role="status"`, the guard-blocked banner has `role="alert"`.
    - The `target` row renders the three distinct copy variants correctly per `hasTarget`/`targetUnavailableOnBuild` and is absent when offline.
  </acceptance_criteria>
  <done>The client card implements the full Surface 1 item-2 anatomy — target identity, two independent guard rows, and all three B2 safety states with locked, REAL-data-interpolated copy (real address, correctly-evolving expected bytes) — no force-write escape hatch exists anywhere in this component, and Revert ALL always remains the user's way out of a blocked guard.</done>
</task>

<task type="auto">
  <name>Task 2: TransformReadoutBar — numboxes, D-05-corrected write-target indicator, floating drag delta</name>
  <files>packages/renderer/src/panels/viewport/TransformReadoutBar.tsx, .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (StatsCollector's imperative useFrame-throttled-callback idiom — the exact discipline this bar's live-drag numbox updates must follow, per LIVE-03 SC1's "imperative refs/direct DOM, never per-frame React state churn" contract)
    - packages/renderer/src/hooks/useCommandWriter.ts (05-07 — writeTransform, for typed-numbox-edit = same write path as a drag)
    - 05-10-PLAN.md Task 1 (the EXACT D-05 offline copy `○ Offline — attach a client to move objects live` and the `liveStore.status.kind==='attached'` gate — this task reuses BOTH verbatim, do not diverge)
    - 05-UI-SPEC.md Surface 1 items 5-6 (exact anatomy, copy, and the 52px-numbox/axis-tag dimension lock) AND the "## Errata" item 1 (D-05 — the offline write-target correction this task implements)
  </read_first>
  <action>
    Create TransformReadoutBar.tsx: a bottom-center translucent bar with three groups (Pos/Rot/Scale),
    each showing X/Y/Z axis tags (11px colored square, axis letter, dark text `#111`, sketch-locked
    axis hexes) plus a 52px right-aligned mono numbox per axis. During an active gizmo drag, numbox
    text content updates via a DIRECT DOM ref write (`ref.current.textContent = value.toFixed(2)`),
    NOT via React state — mirror `StatsCollector`'s useFrame-throttled-callback-without-setState
    discipline exactly (this bar likely needs its own small `useFrame`-adjacent subscription to the
    live drag values TransformGizmo's `onChange` produces; the simplest wiring is for TransformGizmo
    (05-10) to also write into a small shared ref/event-emitter this bar subscribes to imperatively —
    do not thread live drag values through React props/state, since that reintroduces the per-frame
    re-render this task's must_haves forbid). Gate the numboxes' interactivity on
    `liveStore.status.kind === 'attached'` — the EXACT SAME gate 05-10's TransformGizmo uses (D-05):
    when NOT attached, render the numboxes disabled/non-editable (grayed, no click-to-edit, no typed
    commit) — a numbox edit is the same `writeTransform` write path as a gizmo drag, and offline that
    path does not exist yet (no `.ws` file target until Phase 7). When attached, numboxes ALSO accept
    typed keyboard entry (a plain `<input>` toggled via click-to-edit or always-editable, executor's
    choice matching the sketch's "numbox" idiom) — committing a typed value calls `writeTransform`
    exactly the same way a gizmo drag does (same function, same zero-alloc buffers from
    useCommandWriter), gated on the relevant guard channel the SAME way 05-10's gizmo suppresses
    writes to an already-blocked channel (Scale-group numboxes check `guardState.scale`; Pos/Rot
    numboxes check `guardState.transform`). Render the border-left-separated write-target indicator
    reading `liveStore.status.kind`: **live** → `⇄ writing to client → client ✓` (accent); **offline**
    → the SAME disabled-with-reason copy 05-10 uses, exactly `○ Offline — attach a client to move
    objects live` (warn/dashed) — **do NOT render `⇄ staging patch → staged (patch)` anywhere in this
    component** (D-05 erratum corrected here; that copy was the UI-SPEC's original, now-superseded
    assumption — see 05-UI-SPEC.md "## Errata" item 1). Render the floating delta readout (top-center,
    ONLY visible while a drag is in progress — driven by the same imperative drag-active signal, not
    React state re-renders during the drag itself, though show/hide on drag-start/drag-end IS an
    acceptable state transition since it happens once per drag, not per frame; a drag can only occur
    while attached, since the gizmo is disabled offline, so this readout has no offline variant):
    `Δ pos.x +0.42m → client ✓` (axis-colored bold label, mono value). Finally, confirm
    05-UI-SPEC.md's "## Errata" item 1 (D-05) accurately reflects this task's implemented copy/gate —
    if any detail drifted while implementing this task, correct the Errata entry and the Surface 1
    item 5 description to match; otherwise no doc edit is needed beyond confirming consistency (grep
    for "D-05" to locate the sections).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- TransformReadoutBar</automated>
  </verify>
  <acceptance_criteria>
    - A simulated 60-tick drag sequence asserts the component's React render count during the drag is O(1) (at most the drag-start/drag-end transitions), not O(60) — proving numbox updates are imperative, not state-driven.
    - Typed numbox entry (Enter to commit) calls the same `writeTransform` function a gizmo drag calls (same import, verified by a spy) — and a test asserts typed entry is IMPOSSIBLE (input disabled/no commit fires) while `liveStore.status.kind !== 'attached'`.
    - The write-target indicator's text is exactly `○ Offline — attach a client to move objects live` when offline (grep/DOM text match) — a test asserts the string `staged (patch)` never appears anywhere in this component's rendered output or source.
    - The write-target indicator flips between `→ client ✓` (live) and the D-05 offline copy when `liveStore.status.kind` changes.
    - `grep -c "D-05" .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md` is >= 1 and the cited copy matches this task's implementation verbatim.
  </acceptance_criteria>
  <done>The readout bar mirrors live drag values with zero per-frame React churn, accepts typed entry through the identical write path ONLY while attached (guard-aware per channel, matching 05-10's gizmo), shows the floating delta readout only during an active drag, and its offline write-target indicator matches the gizmo's D-05 disabled-with-reason copy exactly — no "staged (patch)" copy anywhere on this surface.</done>
</task>

<task type="auto">
  <name>Task 3: StatusBar mirror (per-channel guard chip) + corner-axis-gizmo/vp-stats + final Viewport wiring</name>
  <files>packages/renderer/src/shell/StatusBar.tsx, packages/renderer/src/panels/viewport/Viewport.tsx</files>
  <read_first>
    - packages/renderer/src/shell/StatusBar.tsx (lines 60-100 store reads, lines 330-420 existing live-mode segment + `Dot()` helper — extend in place, same idiom)
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — mount point for the new corner-axis-gizmo + vp-stats element, alongside the EXISTING verts/tris/draws StatsCollector overlay which stays unchanged)
    - This plan's Tasks 1-2 output (LiveSyncClientCard, TransformReadoutBar) and 05-10's GizmoModeRail/TransformGizmo — this task's final wiring mounts ALL of Surface 1's elements together in Viewport.tsx
  </read_first>
  <action>
    Extend StatusBar.tsx immediately adjacent to the existing live-mode segment (lines 338-344): add
    `mode: Move (W)` (reads the shared gizmo-mode state from 05-10 — thread it through liveStore or a
    small dedicated store/context if it does not already live in a shared location accessible to
    StatusBar), `sync: ● Live · injected · pid <pid>` / `sync: ○ Offline — file-patch fallback`
    (reads `status`), `COW snapshot ✓` (reads `cowSnapshot?.transform !== null`), and a PER-CHANNEL-
    AWARE guard segment (REVIEWS.md Fix B): `guard: read-verify ✓` when both `guardState.transform` and
    `guardState.scale` are 'ok' (or scale has no baseline yet — treat "not yet written" as non-blocking
    for this summary chip); `guard: read-verify ✗ (transform)` / `✗ (scale)` / `✗ (transform, scale)`
    naming exactly which channel(s) are blocked when at least one is — never a single ambiguous ✗ that
    can't distinguish which channel tripped — each using the same `<Dot/>` separator + "read store
    value, map to glyph+color+text" idiom already used throughout this file (e.g. the existing
    `staleDeployment` badge). In Viewport.tsx: add a NEW bottom-left `vp-stats` overlay (faint mono)
    reading `persp · <W>×<H> · <fps> fps · SAB ✓` — this is DISTINCT from and additional to the
    existing verts/tris/draws `StatsCollector` overlay (do not remove or repurpose that one; UI-SPEC
    item 7 is a new element). Add a bottom-right corner axis gizmo using drei's
    `GizmoHelper`/`GizmoViewport` (or the closest available drei corner-orientation-cube helper in the
    installed `@react-three/drei` version — verify the exact export name against the installed
    version's API before use) inside the `<Canvas>`. Finally, mount `LiveSyncClientCard` (top-right)
    and `TransformReadoutBar` (bottom-center) as DOM overlay siblings of `<Canvas>` (same layer as
    `MissingDepsOverlay`/`GizmoModeRail`), completing the full Surface 1 assembly: shading chips
    (pre-existing, top-left) + client card (top-right) + mode rail (left) + gizmo (in-scene) + readout
    bar (bottom-center) + floating delta (top-center, conditional) + corner gizmo (bottom-right) +
    vp-stats (bottom-left) + statusbar mirror (shell). Verify every one of these switches together
    when `liveStore.status` transitions attached<->idle (a single store change drives all of them,
    since they all read the same `useLiveStore` selectors — no per-component duplicated live/offline
    derivation logic).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- StatusBar Viewport</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms the existing verts/tris/draws StatsCollector overlay is unmodified and a SECOND, new `vp-stats` element exists with the `persp · <W>×<H> · <fps> fps · SAB ✓` format.
    - A single `useLiveStore` status transition (attached→idle) in a test flips the live-chip, client card border style, readout write-target text, AND the StatusBar sync segment simultaneously.
    - A test with `guardState = { transform: 'blocked', scale: 'ok' }` asserts the StatusBar guard segment reads exactly `guard: read-verify ✗ (transform)` — not a bare `✗` that omits which channel.
    - StatusBar's new segments follow the exact `<Dot/>` + glyph+color+text idiom (no new separator style introduced).
  </acceptance_criteria>
  <done>All of UI-SPEC Surface 1 is assembled in Viewport.tsx/StatusBar.tsx, switching live/offline from one source of truth, with a guard summary chip that names WHICH channel is blocked — LIVE-03's full HUD anatomy is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| liveStore guard/write-log state → HUD rendering | The HUD must faithfully reflect the agent's authoritative guard decision (per channel) and never offer a way around it — Revert ALL is the one sanctioned exception, and it is a re-baseline + known-good write, never an arbitrary forward force-write. |
| Channel GUARD_ADDR field → HUD banner | The banner must source its address from the real channel field, never fabricate/derive one client-side (closes REVIEWS.md Fix C's first half). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | LiveSyncClientCard guard-blocked state | mitigate | No force-write control exists anywhere in the card; the only actions are re-read/retry and revert-to-snapshot, both of which re-enter the normal guarded write path (via rebaseline-then-revert) rather than bypassing it. |
| T-05-19 | Information disclosure (low) | Guard-blocked banner's real address/bytes | accept | Displaying the live object's own memory address/bytes to the LOCAL user of a single-user desktop modding tool is not a meaningful disclosure risk — this is diagnostic information about a process the user already controls and has attached to. |
| T-05-31 | Repudiation | StatusBar per-channel guard chip | mitigate | Naming which specific channel (transform/scale) is blocked, rather than a single ambiguous glyph, prevents a user from misreading a scale-only block as "everything is fine" or vice versa — directly closes REVIEWS.md Fix B's UI-visibility half. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- LiveSyncClientCard TransformReadoutBar StatusBar Viewport` — all
green.
</verification>

<success_criteria>
- Every UI-SPEC Surface 1 element is present and switches live/offline from one source of truth.
- All three B2 safety states render with locked copy, REAL address/expected-bytes values, and no force-write escape hatch; transform and scale guard state are independently visible.
- Revert ALL is always clickable while attached, including when a guard is blocked.
- HUD updates during drag are imperative, not React-state-driven.
- The readout bar's offline write-target indicator matches the gizmo's D-05 disabled-with-reason copy exactly — no "staged (patch)" copy anywhere on this surface.
- Target-vs-player identity is honestly shown in the client card.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-11-SUMMARY.md` when done
</output>
</output>
