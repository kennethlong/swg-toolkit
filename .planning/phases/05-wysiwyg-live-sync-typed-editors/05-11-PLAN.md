---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 11
type: execute
wave: 5
depends_on: ["05-10"]
autonomous: true
requirements: [LIVE-03]
files_modified:
  - packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx
  - packages/renderer/src/panels/viewport/TransformReadoutBar.tsx
  - packages/renderer/src/shell/StatusBar.tsx
  - packages/renderer/src/panels/viewport/Viewport.tsx
  - .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md

# REVISION NOTE (--reviews replan, 2026-07-08): folds in the HIGH findings this plan directly reflects
# (05-REVIEWS.md), plus 05-CONTEXT decision #1 (target identity).
# (C) HIGH — the guard-blocked banner needs a REAL address the channel never carried, and the
#     "expected" bytes were wrong after write #1. FIXED: 05-01 added GUARD_ADDR (a real x86 pointer,
#     agent-authoritative) to the channel; 05-07 added expectedTransform()/expectedScale()
#     SELECTORS on liveStore that derive from the last SUCCESSFUL write-log entry (not the stale
#     attach-time cowSnapshot). This plan's Task 1 now sources BOTH values correctly instead of
#     hand-deriving them from cowSnapshot/verifiedState as the original plan's Task 1 did.
# (B) HIGH — Scale had no guard/revert; "Revert ALL" reverted transform using CURRENT scale. FIXED:
#     05-07's guardState is now per-channel ({transform, scale}); revertAll/revertWrite now
#     restore the HISTORICAL scale from the write-log/snapshot. This plan's Task 1 now renders BOTH
#     guard rows (transform + scale) instead of one combined row, and Task 2's "Revert ALL" button
#     already calls the corrected liveStore action — no NEW logic here, just correct consumption +
#     correct copy (no longer implying a single combined guard state).
# Opus MEDIUM (a blocked guard also blocks its own revert) — SUPERSEDED by ROUND 2 below (05-07's
#     revertAll/revertWrite now send ONE coalesced call, gated on blocked, with disclosure — this
#     plan's "Revert ALL" button still stays clickable regardless of guard state, unchanged).
# Decision #1 (target identity) — the client card's client row is extended with a target row
#     (mirrors 05-10's inline gizmo label at the HUD-card level, the authoritative full-detail
#     location) showing the SAME hasTarget/targetUnavailableOnBuild-derived copy.
# LOW hygiene (Sonnet) — client-card banners now carry role="status" (guard-ok/reverted) or
#     role="alert" (guard-blocked) for aria parity, folded in cheaply while this plan already builds
#     every banner from scratch.

# REVISION NOTE (--reviews replan, ROUND 2, 2026-07-08): folds in 05-REVIEWS.md ROUND 2 decisions
# #1/#2/#3.
# (1) Decision #1 (cross-build target row). The target row's copy is REVISED: <templateName> and
#     "none — moving player avatar" are now reachable on EITHER build (05-03 ROUND 2's real advertised
#     selected-object resolver); "unavailable on this build — moving player avatar" is now a RARER,
#     genuinely-build-capability-gap-only state (targetUnavailableOnBuild REDEFINED — true only when
#     the resolution mechanism itself is unresolved, never merely "nothing selected"). No structural
#     change to the row itself, only the copy-selection logic and the states it can actually reach.
# (2) Decision #2 (W2 fix — scaleUnavailableOnBuild takes precedence over the scale guard row's
#     tamper-implying refused state). Round-1's scale guard row only had two states (ok/refused) plus
#     "not yet written this session"; this round adds a THIRD, higher-precedence state: when
#     verifiedState.scaleUnavailableOnBuild === true, the scale row shows "unavailable on this build"
#     (a structural, non-alarming state, reusing the D-05/05-10 disabled-with-reason wording family)
#     REGARDLESS of guardState.scale — the refused/tamper variant is reserved for
#     guardState.scale === 'blocked' WITHOUT scaleUnavailableOnBuild. Precedence (highest to lowest):
#     scaleUnavailableOnBuild -> blocked (refused) -> not-yet-written -> ok. The StatusBar's
#     per-channel guard chip (Task 3) gets the identical precedence rule, so a user never reads "guard:
#     read-verify refused (scale)" when the real story is "Scale isn't wired up on this build yet."
# (3) Decision #3 (revert model — surface the discarded external change). 05-07 ROUND 2 populates
#     liveStore.lastDiscardedChange immediately before a gate-on-blocked coalesced revert send. This
#     plan's reverted-banner copy is EXTENDED (not replaced — the locked base copy stays) with an
#     additional clause naming what was discarded WHEN lastDiscardedChange !== null. When
#     lastDiscardedChange === null (the revert did not need to gate-on-blocked — guard was already
#     'ok'), the banner shows ONLY the original locked copy, unchanged, since nothing was discarded.
#     "Revert ALL to snapshot" remains ALWAYS clickable while attached, unchanged from round 1 — the
#     coalesce/gate-on-blocked logic lives entirely in 05-07's liveStore, this plan only consumes its
#     output (lastDiscardedChange) for copy, never re-derives the gating decision itself.

# REVISION NOTE (--reviews replan ROUND 4, 2026-07-12): folds in 05-REVIEWS.md round-3 maintainer
# decisions #3(1) and #5.
# (3.1) HIGH — targeting honesty, HUD mismatch warning (DECISION: OPTION (a), all four sub-items;
#     sub-items (2)/(3)/(4) land in 05-03/05-12, not here). This plan adds the FIRST sub-item: render
#     an inline `⚠ viewing <loaded>, moving <target>` warning when the toolkit's loaded viewport asset
#     name differs from `verifiedState.templateName` — BOTH names are already available to the
#     toolkit (`useViewportStore().loadStatus.filename` when `kind==='done'`; `verifiedState.templateName`
#     when `hasTarget===true`) — NO new resolver, NO new native export, a pure string-compare. This is
#     honesty, not closure: the warning discloses that the viewport's loaded mesh and the live-moving
#     object may be UNRELATED (there is still no template->live-Object* resolver on either build,
#     05-03's ROUND 4 corrected framing) — it does not create or imply a binding between them.
# (5) MEDIUM — lastDiscardedChange is now PER-CHANNEL (05-07 ROUND 4: `{ addr, transform?, scale? }`,
#     was transform-only). This plan's reverted-banner copy is updated to disclose EACH channel that
#     was actually discarded (transform, scale, or both) rather than assuming a single combined shape.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decisions #3 and #4.
# (3) MEDIUM — HUD name-only false-reassurance (Sonnet's independent-angle convergence with Opus'
#     decision #1 finding — both traced to "template-name is not a unique object identity"). The ROUND
#     4 mismatch warning compares ONLY the loaded viewport asset's basename against
#     `verifiedState.templateName`'s basename — when the names COINCIDENTALLY MATCH (e.g. two unrelated
#     `womprat.iff` instances, one loaded, a DIFFERENT one live-targeted), no warning fires and
#     `target: womprat` reads as unqualified confirmation while an unrelated object actually moves. A
#     full identity compare is NOT feasible here even with decision #1's unified `focusToken` — the
#     viewport's loaded asset is a static file with no live identity to compare against a focus token in
#     the first place. FIX (the maintainer's documented minimum): a QUIET caveat, `(name match only —
#     not a verified object identity)`, now renders adjacent to the `target` row whenever the names
#     match (the loud `⚠ viewing...` warning still owns the mismatch case) — a coincidental name match
#     can never again look like a verified identity confirmation.
# (4) Minors — APPLY ALL: `liveStore.guardAddr` is now named concretely in the guard-blocked banner's
#     address source (was the abstract channel field `GUARD_ADDR`); the reverted banner's scale
#     disclosure clause now branches on 05-07's new `lastDiscardedChange.scale.gotVerified` flag,
#     rendering an honest "last known <bytes> — not independently re-verified" copy instead of a
#     "found <bytes>" claim when no renderer-side live-scale read backs the value (05-07's ROUND 5 note
#     — Cursor MEDIUM, the maintainer's minimum alternative to growing the channel again this round);
#     liveness bit6 `agentFaultRecovered` is DOCUMENTED AGENT-ONLY telemetry (05-03's own ROUND 5 note
#     — mirrors bit5's existing treatment, no HUD consumer added by this plan).

# REVISION NOTE (--reviews replan ROUND 6, 2026-07-12): folds in 05-REVIEWS.md ROUND 5 maintainer
# decision's LOW-minor fold-in (Sonnet, "HUD cache-restore not disclosed" — round-5's per-identity
# cache now RESTORES a prior identity's writeLog on flip-back, which is CORRECT behavior (05-07's own
# ROUND 5 fix), but nothing in the HUD explained the mechanism — a modder retargeting back to a
# previously-edited object could misread the resurrected write-log entries as stale/buggy leftover data
# rather than intentional per-object session history. FIX: Task 1 adds a brief, permanent, faint
# caption under the `WRITE LOG · THIS SESSION` header disclosing that this history is per-object and
# persists across retargeting — no new liveStore field needed (05-07's Task 2, ROUND 5/6, is NOT
# reopened), this is copy-only.

must_haves:
  truths:
    - "Every element of UI-SPEC Surface 1 is present: shading chips (pre-existing, unchanged), live-sync client card (item 2, now with a CROSS-BUILD target-identity row and THREE-STATE independent guard rows), gizmo-mode rail (05-10), transform gizmo (05-10), bottom transform readout bar (item 5), floating delta readout while dragging (item 6), corner axis gizmo + vp-stats (item 7), statusbar mirror (item 8)."
    - "All three B2 safety states (normal write / guard blocked / reverted) render with the exact locked copy structure, and guard-blocked shows a REAL address (from the channel's GUARD_ADDR field) + REAL expected/got bytes (expected from liveStore's expectedTransform()/expectedScale() selectors, which track the last successful write — never a placeholder, never a stale attach-time-only value)."
    - "The guard-blocked state is reported PER CHANNEL (transform vs scale independently), and the SCALE row has a THIRD state — 'unavailable on this build' — that takes precedence over the tamper-implying refused variant whenever verifiedState.scaleUnavailableOnBuild is true, so a build-capability gap is never rendered as a tamper (REVIEWS.md Fix B + ROUND 2 W2 fix)."
    - "'Revert ALL to snapshot' restores BOTH the attach-time transform AND the attach-time (or first-write-baselined) scale — never reverting transform while leaving scale at whatever it currently is."
    - "'Revert ALL to snapshot' remains clickable even while guardState is 'blocked' — it is the user's sanctioned way to un-stick a blocked guard, never disabled purely because the guard tripped."
    - "The reverted banner names what external change was discarded (real address + expected/found bytes, sourced from liveStore.lastDiscardedChange) WHENEVER a revert needed to gate-on-blocked to un-stick the guard — PER CHANNEL (ROUND 4: lastDiscardedChange.transform and/or .scale, whichever were actually blocked, each disclosed independently) — and shows the plain locked copy, unchanged, when nothing needed to be discarded (ROUND 2 decision #3 — closes the round-1 'silent overwrite' UX gap without a force-write path)."
    - "An inline `⚠ viewing <loaded>, moving <target>` warning renders whenever the toolkit's loaded viewport asset name (from viewportStore) differs from `verifiedState.templateName` (when `hasTarget===true`) — a pure string-compare using data already available on the toolkit, no new resolver (ROUND 4, REVIEWS.md round-3 maintainer decision #3(1) — this is HONESTY about the viewport<->live-object binding gap, not a claim that it is closed; the gap remains open until Phase 7's `.ws` placement editing, 05-03's ROUND 4 corrected framing)."
    - "When the loaded viewport asset's normalized basename MATCHES `verifiedState.templateName`'s normalized basename (so the loud mismatch warning does NOT fire), a QUIET caveat `(name match only — not a verified object identity)` renders adjacent to the `target` row instead of silence (ROUND 5, REVIEWS.md round-4 maintainer decision #3 — Sonnet's same-template-coincidence finding: a name match alone can never distinguish a genuinely-loaded object from an unrelated live object that merely shares a template, e.g. two `womprat.iff` instances)."
    - "Every surface (chip, card, readout, statusbar) switches together on live<->offline — verified by a single state change driving all four simultaneously, not four independently-wired copies of the same logic."
    - "HUD numbox/delta text updates during drag are imperative (refs/direct DOM), never per-frame React state churn (LIVE-03 SC1)."
    - "The transform readout bar's write-target indicator uses the SAME D-05 disabled-with-reason copy as the gizmo when offline ('Offline — attach a client to move objects live') — NOT a staged-patch copy — and the Pos/Rot/Scale numboxes are non-interactive offline, matching the gizmo's actual disabled state (D-05 erratum; the numbox write path is identical to a gizmo drag). Scale numboxes are ALSO non-interactive (a distinct, structural disabled state) when scaleUnavailableOnBuild is true, mirroring 05-10's gizmo behavior (ROUND 2)."
    - "The client card honestly states what object is being tracked/moved, on EITHER build (target vs. player-nothing-selected vs. mechanism-unavailable), mirroring 05-10's inline gizmo label at full detail (05-CONTEXT decision #1, ROUND 2 cross-build)."
    - "A brief, permanent, faint caption renders under the 'WRITE LOG · THIS SESSION' header disclosing that the log is per-object cached history that persists across retargeting — so a modder retargeting back to a previously-edited object never misreads the resurrected write-log entries as stale or buggy data (ROUND 6, REVIEWS.md round-5 maintainer decision, Sonnet LOW 'HUD cache-restore not disclosed' — pure copy, no new liveStore field required)."
  artifacts:
    - path: "packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx"
      provides: "224px top-right card: live chip, client/target(cross-build)/pid/fps/last-sync/COW/TWO guard rows (transform + THREE-STATE scale), write log with per-write revert, Revert ALL (always clickable while attached), guard-blocked + reverted (with PER-CHANNEL discarded-change disclosure, ROUND 4) banners with real addr/bytes and role=alert/status, and an inline viewport/live-target mismatch warning (ROUND 4)"
      contains: "unavailable on this build"
    - path: "packages/renderer/src/panels/viewport/TransformReadoutBar.tsx"
      provides: "Bottom-center Pos/Rot/Scale numboxes (52px right-aligned mono, disabled offline AND disabled when Scale is build-unavailable) + disabled-with-reason/live write-target indicator + floating drag-delta readout"
      contains: "Offline — attach a client to move objects live"
  key_links:
    - from: "packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "reads writeLog/guardState{transform,scale}/cowSnapshot/expectedTransform()/expectedScale()/lastDiscardedChange{addr,transform?,scale?}/verifiedState.{hasTarget,targetUnavailableOnBuild,scaleUnavailableOnBuild}; calls revertWrite(id)/revertAll() on button click"
      pattern: "lastDiscardedChange"
    - from: "packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx"
      to: "packages/renderer/src/state/viewportStore.ts"
      via: "reads loadStatus.filename (when kind==='done') to compare against verifiedState.templateName for the mismatch warning (ROUND 4) — read-only, this plan never modifies viewportStore"
      pattern: "viewing"
    - from: "packages/renderer/src/shell/StatusBar.tsx"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "new mode/guard/COW segments read the SAME store fields the HUD card reads — one source of truth, including the scaleUnavailableOnBuild precedence rule"
      pattern: "scaleUnavailableOnBuild"
---

<objective>
Complete Surface 1: the live-sync client card (with the full B2 safety-state anatomy, a REAL
guard-blocked address, a REAL always-current "expected" baseline, transform/scale guard state reported
independently with a THIRD scale-specific "unavailable on this build" state, cross-build target
identity, and a reverted banner that discloses what an un-sticking revert discarded), the transform
readout bar (with imperative drag-delta updates, the D-05-corrected disabled-with-reason offline
write-target indicator, and the same Scale-build-availability gating as the gizmo), the corner-axis-
gizmo/vp-stats item, and the StatusBar mirror — wiring everything into Viewport.tsx so all four HUD
surfaces switch live/offline together from one source of truth, and honestly reflect target-vs-player
identity and Scale's build-availability on EITHER target.

Purpose: this closes the remaining UI-SPEC Surface 1 elements and the D-03/D-04 safety-state UX
(SC #2 — a bad write is revertible via the changeset/snapshot system, INCLUDING scale, and INCLUDING
when the guard is currently blocked, with disclosure when a revert had to override a blocked state),
completing LIVE-03's user-facing anatomy, and folds in both the D-05 correction and the ROUND 2
cross-build/scale-availability corrections consistently across every surface that shares the gizmo's
write path.

Output: `LiveSyncClientCard.tsx`, `TransformReadoutBar.tsx`, extended `StatusBar.tsx`, the final
Viewport.tsx overlay-mount wiring including the corner-gizmo/vp-stats element, and a confirmed-
consistent `05-UI-SPEC.md` errata section (D-05 plus a new ROUND 2 entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-10-PLAN.md
</context>

<interfaces>
UI-SPEC Surface 1 remaining items (this plan's scope; items 1/3/4 are pre-existing/05-10):

Item 2 — Live-sync client card, top-right (224px), translucent, accent-line border (dashed warn
offline); header live/offline glyph per state-encoding table; body rows (mono):
`client`, **`target`** (05-CONTEXT decision #1, ROUND 2 cross-build: `<templateName>` when
`verifiedState.hasTarget===true` — REACHABLE ON EITHER BUILD now; `unavailable on this build — moving
player avatar` when `verifiedState.targetUnavailableOnBuild===true` — a genuine resolution-mechanism
gap only, rarer than round 1; `none — moving player avatar` when attached, mechanism available, but
nothing currently targeted/selected — the default "no gap, just nothing picked" state; omitted/n-a
when offline), `pid`, `fps` (— offline), `last sync` (`0.2s ago` / `n/a` offline), `COW snapshot`
(`saved · <HH:MM:SS>` live / `staged` offline; offline adds `mode: file-patch fallback` warn row);
**TWO independent read-verify guard rows (REVIEWS.md Fix B — was one combined row)**: `transform:
client bytes = snapshot` / `client bytes != snapshot`; `scale:` has THREE possible states, in
PRECEDENCE ORDER (ROUND 2 — highest first): (a) `unavailable on this build` when
`verifiedState.scaleUnavailableOnBuild===true` (structural, non-alarming — REGARDLESS of
`guardState.scale`'s value); (b) `client bytes != snapshot` (refused/tamper variant) when
`scaleUnavailableOnBuild===false` AND `guardState.scale==='blocked'` (a genuine tamper); (c) `not yet
written this session` when `cowSnapshot.scale===null` (no scale write has happened yet, nothing to
compare); (d) `client bytes = snapshot` otherwise; session write log (`WRITE LOG · THIS SESSION`,
faint uppercase mono header) — each row: relative time + delta (`pos.x +0.42m`, mono) + right-aligned
`revert` ghost button (hover to danger); `Revert ALL to snapshot` button (danger-outline, full-width,
ALWAYS clickable while attached — never disabled purely because a guard channel is blocked, since this
is the sanctioned way to un-stick one) below the log.

Item 5 — Bottom transform readout bar, bottom-center: translucent bar, Pos/Rot/Scale groups (X/Y/Z
axis tags + 52px right-aligned mono numboxes), border-left-separated write-target indicator: **CORRECTED
per D-05 (05-CONTEXT.md; see 05-UI-SPEC.md "## Errata" item 1) — this is NOT the original UI-SPEC's
"writing to client / client ok" (live) vs "staging patch / staged (patch)" (offline) pairing.** Live
-> "writing to client -> client ok" (accent, numboxes editable); offline -> the SAME disabled-with-reason
copy 05-10's TransformGizmo uses, exactly "Offline — attach a client to move objects live" (warn/dashed,
numboxes non-interactive). Numboxes mirror the gizmo live during drag AND accept typed values when
live; offline, numboxes render disabled. **ROUND 2 — Scale group numboxes are ADDITIONALLY disabled (a
distinct, structural state, not the offline styling) whenever
`verifiedState.scaleUnavailableOnBuild===true`, even while otherwise live/attached** — matching 05-10's
gizmo Scale-mode behavior exactly.

Item 6 — Floating delta readout while dragging, top-center, ONLY during drag: axis-colored bold delta
label plus mono value plus "-> client ok".

Item 7 — Corner axis gizmo (bottom-right) + vp-stats (bottom-left): perspective/resolution/fps/SAB
status line (faint mono, distinct from the EXISTING verts/tris/draws stats overlay, which stays as-is).

Item 8 — Statusbar mirror (extend StatusBar.tsx): mesh name, mode segment (e.g. "Move (W)"), sync
segment ("Live - injected - pid <pid>" or "Offline - file-patch fallback", warn), COW snapshot segment,
guard segment (`guard: read-verify ok` or a per-channel-aware variant naming which channel(s) are
refused, e.g. "(transform)" / "(scale)" / "(transform, scale)" — REVIEWS.md Fix B: never collapse two
independent guard states into one ambiguous chip). **ROUND 2 — the scale half of this chip applies the
SAME precedence as the client card's scale row: when `verifiedState.scaleUnavailableOnBuild===true`,
scale is NEVER named in the refused list (it is not a guard failure) — instead, if BOTH transform is
fine and scale is merely build-unavailable, the chip reads plain "guard: read-verify ok" (nothing is
actually blocked; scale is simply not attemptable on this build, which is a DIFFERENT kind of fact, not
surfaced in this compact chip — the client card's dedicated scale row is the authoritative place for
that detail).**

B2 safety states (all three; copy STRUCTURE from the Copywriting Contract, values now REAL not
placeholder — REVIEWS.md Fix C):
- Guard-blocked banner: "Write refused. The object's memory changed outside the toolkit (<addr>:
  expected <bytes>, read <bytes>) — the game or another tool moved it. Nothing was written." where
  `<addr>` is `LIVE_CHANNEL_LAYOUT.GUARD_ADDR` (a real x86 pointer, agent-published, formatted as a
  hex address), `expected <bytes>` is `liveStore.expectedTransform()`/`.expectedScale()` (whichever
  channel is blocked), and `read <bytes>` is the CURRENT `verifiedState.transform` (the live bytes
  that caused the mismatch). `role="alert"` on this banner. Actions: "Re-read & retry" (ghost) +
  "Revert to snapshot" (danger-outline). NO force-write affordance.
- Reverted banner: "Reverted. Transform restored from the COW snapshot taken at attach (<HH:MM:SS>).
  The in-game object snapped back — nothing is staged." `role="status"` on this banner. **ROUND 2
  EXTENSION (decision #3) — when `liveStore.lastDiscardedChange !== null` (this revert needed to
  gate-on-blocked to un-stick the guard), APPEND a second sentence, same banner, same `role="status"`:**
  "An external change was discarded on revert (<addr>: expected <bytes>, found <bytes>)." sourced
  verbatim from `lastDiscardedChange.addr`/`.expected`/`.got`. When `lastDiscardedChange === null`
  (nothing needed to be discarded — the guard was already 'ok'), the banner shows ONLY the original
  sentence, unchanged. Write-log rows go struck-through at ~0.55 opacity with revert buttons removed.

Existing StatusBar.tsx segment idiom to extend (lines 338-344, `<Dot/>` separator helper at 422-424):
```tsx
<Dot />
<span><span style={{ color: liveMode === 'live' ? 'var(--color-accent)' : 'var(--color-text-muted)' }}>
  {liveMode === 'live' ? 'Live' : 'File-patch'}
</span></span>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LiveSyncClientCard — full anatomy + cross-build target-identity row + THREE-STATE scale guard row + real addr/expected-bytes + all three B2 states + discarded-change disclosure</name>
  <files>packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (MissingDepsOverlay pattern — the absolutely-positioned overlay-outside-Canvas idiom this card follows)
    - packages/renderer/src/state/liveStore.ts (05-07 ROUND 4 — VERIFIED shapes: status/verifiedState (incl. hasTarget/targetUnavailableOnBuild/scaleUnavailableOnBuild)/cowSnapshot{transform,scale,networkId,templateName}/writeLog (with resultingTransform+resultingScale)/guardState{transform,scale}/lastDiscardedChange{addr,transform?,scale?} (PER-CHANNEL)/expectedTransform()/expectedScale() selectors/revertWrite/revertAll actions)
    - packages/renderer/src/state/viewportStore.ts (`ViewportStore.loadStatus` — `{ kind: 'done'; filename: string; ... }` when an asset is loaded; this is the ONLY source for the loaded-asset name the ROUND 4 mismatch warning compares against `verifiedState.templateName` — read-only, this task never modifies viewportStore)
    - packages/contracts/src/live-inject.ts (05-01 — LIVE_CHANNEL_LAYOUT.GUARD_ADDR offset/length; this is the ONLY sanctioned source for the banner's address)
    - 05-UI-SPEC.md Surface 1 item 2 + the B2 states block + the exact Copywriting Contract strings (do not paraphrase — the banner/row copy STRUCTURE is locked verbatim; the VALUES are real per REVIEWS.md Fix C; the ROUND 2 discarded-change sentence is an EXTENSION, appended only when applicable, never replacing the locked base sentence)
    - 05-REVIEWS.md Fix B (two independent guard rows), Fix C (real addr + correctly-derived expected bytes), and ROUND 2 decisions #1 (cross-build target copy), #2 (scale row precedence), #3 (discarded-change disclosure) — this task closes all five
    - 05-REVIEWS.md ROUND 4's "THE CONVERGENT FINDING" (Sonnet's same-template-coincidence angle) and round-3 maintainer decisions #3 (the name-match-only caveat) and #4 (`liveStore.guardAddr` naming, `lastDiscardedChange.scale.gotVerified`) — ROUND 5, this task closes all three; see 05-07's ROUND 5 note for the `gotVerified` field this task consumes
  </read_first>
  <action>
    Create LiveSyncClientCard.tsx: a 224px translucent card (`rgba(16,20,20,.78-.9)` bg +
    `backdrop-filter: blur(4-5px)` per UI-SPEC Color, accent-line solid border when
    `status.kind==='attached'` / dashed warn border otherwise). Header: live chip per the
    live/offline state-encoding table. Body rows reading from liveStore: `client` (the attached exe
    name/label — source it from whatever field the existing attach flow already tracks, threading it
    through liveStore if not already retained). `target` (ROUND 2 cross-build, PRECEDENCE ORDER): when
    `status.kind==='attached'` — `verifiedState.hasTarget===true` maps to `<verifiedState.templateName>`;
    else `verifiedState.targetUnavailableOnBuild===true` maps to `unavailable on this build — moving
    player avatar`; else maps to `none — moving player avatar`; when offline, omit this row entirely.
    ROUND 4 (REVIEWS.md round-3 maintainer decision #3(1)) — immediately below the `target` row,
    conditionally render an inline mismatch warning: read `useViewportStore().loadStatus` and, when
    `loadStatus.kind === 'done'` AND `verifiedState.hasTarget === true` AND `loadStatus.filename`
    (normalized — compare basename without extension, case-insensitively; both `loadStatus.filename`
    and `verifiedState.templateName` may carry a full asset path like
    `object/creature/player/shared_sullustan_male.iff`, so normalize BOTH sides identically before
    comparing) differs from `verifiedState.templateName`'s normalized basename, render `⚠ viewing
    <loadedBasename>, moving <targetBasename>` (warn-colored, small text, same row group as `target`).
    This is a PURE DISCLOSURE — it does not gate, block, or alter the gizmo's write behavior in any
    way, and it does NOT imply the two are related; it exists precisely because the toolkit does not
    (yet) know whether they are the same object (05-03's ROUND 4 corrected framing — no
    template->live-Object* resolver exists on either build). Render nothing when `loadStatus.kind !==
    'done'` (nothing loaded in the viewport) or `hasTarget === false` (nothing to compare against).
    ROUND 5 (REVIEWS.md round-4 maintainer decision #3, Sonnet's same-template-coincidence
    false-reassurance finding): when the normalized names DO match, do NOT render nothing — a full
    identity compare is NOT feasible here (the viewport's loaded asset is a static file with no live
    identity to compare against `focusToken`; decision #1's identity unification closes the
    host/agent baseline mismatch, it does not create a live identity for a merely-loaded mesh), so per
    the maintainer's documented minimum, render a QUIET caveat instead of silence: `(name match only —
    not a verified object identity)` in the SAME small warn-adjacent styling, immediately after the
    `target: <templateName>` row's value (NOT the loud `⚠ viewing...` warning styling — this is a
    lower-urgency disclosure than an outright mismatch, but it must never be silent, since a
    same-template coincidence — e.g. two `womprat.iff` instances, one loaded in the viewport and an
    UNRELATED one currently live-targeted — previously read as unqualified confirmation). This closes
    the exact false-reassurance failure mode REVIEWS.md flagged twice (05-REVIEWS-R2.md, 05-REVIEWS-R3.md)
    and again in round 4 from Sonnet's independent angle: a coincidental name match must never look
    like a verified identity match.
    `pid`, `fps` (compute from the actual poll-tick rate or an existing counter — do not hardcode),
    `last sync` (`<N>s ago` or `n/a` offline), `COW snapshot` (`saved · <HH:MM:SS>` once
    `cowSnapshot.transform` is non-null, `staged` before capture), `mode: file-patch fallback` (warn,
    offline only). TWO independent read-verify guard rows: `transform: client bytes = snapshot` when
    `guardState.transform==='ok'`, the refused variant when `'blocked'`. `scale:` — implement the
    FOUR-BRANCH PRECEDENCE from the Interfaces section exactly (scaleUnavailableOnBuild FIRST, THEN
    blocked, THEN not-yet-written, THEN ok) — a helper function computing this precedence is
    recommended so this plan's own Task 3 (StatusBar chip) can reuse the identical logic rather than
    re-deriving it independently (export it from this file or a small shared module, executor's
    choice, but the logic must not be duplicated ad hoc). Session write log: header `WRITE LOG · THIS
    SESSION`, IMMEDIATELY followed by a small faint caption (ROUND 6, REVIEWS.md round-5 maintainer
    decision, Sonnet LOW — "HUD cache-restore not disclosed"): `Per-object history — switching target
    and back restores this object's own log.` This is a PERMANENT, always-rendered disclosure (not
    conditional on whether the CURRENT slot was just restored vs. freshly captured — 05-07's liveStore
    does not expose a "just restored" transient signal this round, and this plan does not add one; the
    caption explains the GENERAL per-identity-cache mechanism so a modder is never confused by EITHER
    case, matching the maintainer's documented minimum). Each `writeLog` entry rendered as relative-time
    + delta label + a `revert` ghost button calling `revertWrite(entry.id)`. `Revert ALL to snapshot`
    full-width danger-outline button calling
    `revertAll()` — `disabled` prop is `status.kind !== 'attached'` ONLY, NEVER also checks
    `guardState` (unchanged from round 1 — the button exists precisely to resolve a blocked state, and
    05-07 ROUND 2's liveStore action internally decides whether to gate-on-blocked; this component does
    not re-derive that decision, it just calls the action). Implement all three B2 states as
    conditionally-rendered banner blocks: normal (no banner); guard-blocked (card border flips to
    danger, the BLOCKED channel's guard row shows the refused variant — but per ROUND 2, a scale-only
    "unavailable on this build" state does NOT flip the card to guard-blocked danger styling, since it
    is not a tamper; only a genuine `guardState.transform==='blocked'` or
    `(guardState.scale==='blocked' && !scaleUnavailableOnBuild)` triggers the danger banner — banner
    `role="alert"` with the EXACT locked copy, address from `liveStore.guardAddr` (ROUND 5, REVIEWS.md
    round-4 maintainer decision #4 — named concretely, not the abstract channel field `GUARD_ADDR`;
    format via `formatAddr(liveStore.guardAddr)`, the SAME helper 05-07's `lastDiscardedChange.addr`
    already uses, so the guard-blocked banner and the reverted banner's discarded-change clause
    format the SAME address identically), `expected <bytes>` from
    `expectedTransform()`/`.expectedScale()` (whichever channel is genuinely blocked), `read <bytes>`
    from current `verifiedState`; "Re-read & retry" + "Revert to snapshot" buttons, NO force-write
    path); reverted (accent-toned banner, `role="status"`, the LOCKED base copy "Reverted. Transform
    restored from the COW snapshot taken at attach (<HH:MM:SS>). The in-game object snapped back —
    nothing is staged.", and — ROUND 2 EXTENSION, RESHAPED PER-CHANNEL ROUND 4 — IF
    `liveStore.lastDiscardedChange !== null` at the moment this banner is shown, APPEND (same banner,
    same role, do not create a second banner element) ONE clause PER PRESENT channel:
    `lastDiscardedChange.transform` present -> " An external change was discarded on revert
    (<addr>: expected <bytes>, found <bytes>)." using `.transform.expected`/`.transform.got`;
    `lastDiscardedChange.scale` present -> the SAME sentence shape using `.scale.expected`/`.scale.got`,
    UNLESS `.scale.gotVerified === false` (ROUND 5, REVIEWS.md round-4 maintainer decision #4 — no
    renderer-side live-scale channel field exists this round, see 05-07's ROUND 5 note), in which
    case use the honest variant "An external change may have affected scale on revert (<addr>: last
    known <bytes> — not independently re-verified this session)." instead of claiming `found <bytes>`
    as if it were a fresh live sample (never render `.scale.got` as a confirmed "found" value when
    `gotVerified` is `false`) (append as a second clause if BOTH transform and scale are present —
    never silently drop one channel's disclosure because the other is also present); `<addr>` is
    shared (`lastDiscardedChange.addr`) across both clauses since it names the same object. Write-log rows render struck-through at 0.55
    opacity with revert buttons removed).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- LiveSyncClientCard</automated>
  </verify>
  <acceptance_criteria>
    - The guard-blocked banner's copy matches the locked string exactly with a REAL (non-placeholder, non-fabricated) address sourced from `liveStore.guardAddr` (ROUND 5, REVIEWS.md round-4 maintainer decision #4 — grep confirms this component references `liveStore.guardAddr`/`formatAddr(liveStore.guardAddr)` by name, not a bare abstract `GUARD_ADDR` symbol) and expected/got values sourced from `expectedTransform()`/`expectedScale()` + current `verifiedState` — a test supplying known liveStore/channel values asserts the exact rendered text, including the address.
    - A test asserts the banner's `expected` value reflects the LAST write-log entry's `resultingTransform` (not `cowSnapshot`) when at least one write has succeeded before the block — proving Fix C is actually consumed, not just available.
    - A test asserts `guardState.scale==='blocked'` with `scaleUnavailableOnBuild===false` renders the scale row's refused variant AND flips the card to the guard-blocked danger state; a companion test asserts `scaleUnavailableOnBuild===true` (regardless of `guardState.scale`) renders the "unavailable on this build" variant instead and does NOT flip the card to danger styling by itself (ROUND 2 precedence).
    - `Revert ALL to snapshot`'s `disabled` prop is `false` whenever `status.kind==='attached'`, REGARDLESS of `guardState` value (a test with `guardState.transform==='blocked'` asserts the button remains enabled/clickable).
    - No button, prop, or code path in this file can force a write while a guard channel is 'blocked' (grep confirms zero references to any "force"/"override" write function).
    - A test with `lastDiscardedChange = { addr, transform: {expected, got} }` (scale undefined) asserts the reverted banner contains the locked base sentence PLUS exactly ONE discarded-change clause (transform); a companion test with `lastDiscardedChange = { addr, scale: {expected, got} }` (transform undefined) asserts exactly ONE clause naming SCALE values, not transform; a third test with BOTH `transform` and `scale` present asserts BOTH clauses render (ROUND 4 — proving per-channel disclosure, not a single combined shape). A test with `lastDiscardedChange === null` asserts the banner contains ONLY the base sentence (no discarded-change clause at all, not rendered empty).
    - A NEW test (ROUND 4, decision #3(1)) asserts the mismatch warning `⚠ viewing <loaded>, moving <target>` renders when `viewportStore.loadStatus = { kind: 'done', filename: 'object/mobile/shared_womprat.iff' }` and `verifiedState = { hasTarget: true, templateName: 'object/mobile/shared_stormtrooper.iff' }` (different basenames); a third test with `loadStatus.kind !== 'done'` OR `hasTarget === false` asserts NEITHER the loud warning NOR the ROUND 5 quiet caveat renders (nothing to compare). A test asserts the warning never calls `writeTransform`/any write function and never alters `guardState` — it is pure disclosure, not a gate.
    - A NEW test (ROUND 5, REVIEWS.md round-4 maintainer decision #3, the same-template-coincidence fix) with MATCHING basenames (case-insensitive, extension-insensitive — e.g. `viewportStore.loadStatus.filename = 'object/mobile/shared_womprat.iff'`, `verifiedState.templateName = 'object/mobile/shared_womprat.iff'`, but conceptually representing a DIFFERENT live instance) asserts the loud `⚠ viewing...` warning does NOT render (names match, so no mismatch), BUT the quiet caveat `(name match only — not a verified object identity)` DOES render adjacent to the `target` row — proving a same-template coincidence is never silently presented as a confirmed identity match. This test is the direct regression check for Sonnet's round-4 finding (05-REVIEWS.md: "target: womprat reads as confirmation while an unrelated object moves").
    - Reverted-state write-log rows render with `text-decoration: line-through` and no `revert` button element present; the reverted banner has `role="status"`, the guard-blocked banner has `role="alert"`.
    - The `target` row renders the three distinct copy variants correctly per `hasTarget`/`targetUnavailableOnBuild` (tested with BOTH a legacy-shaped and an advertised-shaped liveStore state, proving the row is build-agnostic, ROUND 2) and is absent when offline.
    - A NEW test (ROUND 6, REVIEWS.md round-5 maintainer decision, Sonnet LOW) asserts the exact caption text `Per-object history — switching target and back restores this object's own log.` renders immediately under the `WRITE LOG · THIS SESSION` header, present regardless of `writeLog`'s contents (even with an empty log, the caption still renders — it explains the mechanism, not a specific entry's provenance).
    - A NEW test (ROUND 5, REVIEWS.md round-4 maintainer decision #4, gotVerified) asserts that when `lastDiscardedChange.scale.gotVerified === false`, the reverted banner's scale clause reads the HONEST "last known <bytes> — not independently re-verified this session" variant, NEVER the "found <bytes>" phrasing (which implies a confirmed live read); a companion test with `lastDiscardedChange.transform` present (transform's `gotVerified` is implicitly `true`) asserts the transform clause DOES use "found <bytes>" — proving the two channels' disclosure honesty is independently correct, not accidentally uniform.
  </acceptance_criteria>
  <done>The client card implements the full Surface 1 item-2 anatomy — cross-build target identity, two independent guard rows (scale with its three-state precedence), and all three B2 safety states with locked, REAL-data-interpolated copy (real address named concretely as `liveStore.guardAddr`, correctly-evolving expected bytes, and a PER-CHANNEL disclosed discarded-change sentence — honest about whether scale's "got" is independently live-verified via `gotVerified` — when a revert had to gate-on-blocked, ROUND 4/5) — no force-write escape hatch exists anywhere in this component, Revert ALL always remains the user's way out of a blocked guard, an honest inline mismatch warning discloses (never gates) when the loaded viewport asset and the live-moving target diverge, AND a quiet name-match-only caveat discloses the same-template-coincidence case when the names happen to match (ROUND 5 — closes none of the underlying binding gap, only the silence around it, on both the mismatch AND the false-reassurance-on-coincidental-match sides). ROUND 6: the write log carries a permanent caption disclosing its per-object, cache-restored nature, so a retarget-back never reads as stale/buggy leftover data.</done>
</task>

<task type="auto">
  <name>Task 2: TransformReadoutBar — numboxes, D-05-corrected write-target indicator, floating drag delta, scale-unavailable-on-build gating</name>
  <files>packages/renderer/src/panels/viewport/TransformReadoutBar.tsx, .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (StatsCollector's imperative useFrame-throttled-callback idiom — the exact discipline this bar's live-drag numbox updates must follow, per LIVE-03 SC1's "imperative refs/direct DOM, never per-frame React state churn" contract)
    - packages/renderer/src/hooks/useCommandWriter.ts (05-07 — writeTransform, for typed-numbox-edit = same write path as a drag)
    - 05-10-PLAN.md Task 1 (the EXACT D-05 offline copy and the `liveStore.status.kind==='attached'` gate, PLUS the ROUND 2 `scaleUnavailableOnBuild` structural-disabled-state pattern for Scale — this task reuses ALL THREE verbatim, do not diverge)
    - 05-UI-SPEC.md Surface 1 items 5-6 (exact anatomy, copy, and the 52px-numbox/axis-tag dimension lock) AND the "## Errata" item 1 (D-05 — the offline write-target correction this task implements) AND any ROUND 2 errata entry this plan's Task 2 adds
  </read_first>
  <action>
    Create TransformReadoutBar.tsx: a bottom-center translucent bar with three groups (Pos/Rot/Scale),
    each showing X/Y/Z axis tags (11px colored square, axis letter, dark text `#111`, sketch-locked
    axis hexes) plus a 52px right-aligned mono numbox per axis. During an active gizmo drag, numbox
    text content updates via a DIRECT DOM ref write (`ref.current.textContent = value.toFixed(2)`),
    NOT via React state — mirror `StatsCollector`'s useFrame-throttled-callback-without-setState
    discipline exactly (the simplest wiring is for TransformGizmo (05-10) to also write into a small
    shared ref/event-emitter this bar subscribes to imperatively — do not thread live drag values
    through React props/state). Gate the Pos/Rot numboxes' interactivity on
    `liveStore.status.kind === 'attached'` — the EXACT SAME gate 05-10's TransformGizmo uses (D-05):
    when NOT attached, render disabled/non-editable (grayed, no click-to-edit, no typed commit).
    ADDITIONALLY (ROUND 2), gate the Scale group's numboxes on BOTH `status.kind === 'attached'` AND
    `verifiedState.scaleUnavailableOnBuild !== true` — when attached but `scaleUnavailableOnBuild ===
    true`, render the Scale numboxes in the SAME structural disabled-with-reason styling 05-10's gizmo
    uses for that state (distinct from the plain offline styling), with an adjacent label reading
    exactly `Scale unavailable on this build` (mirror 05-10's Task 1 copy verbatim — do not diverge).
    When attached AND (for Pos/Rot always, for Scale only when NOT build-unavailable) numboxes ALSO
    accept typed keyboard entry — committing a typed value calls `writeTransform` exactly the same way
    a gizmo drag does (same function, same zero-alloc buffers, flags omitted), gated on the relevant
    guard channel the SAME way 05-10's gizmo suppresses writes to an already-blocked channel
    (Scale-group numboxes check `guardState.scale`; Pos/Rot numboxes check `guardState.transform`).
    Render the border-left-separated write-target indicator reading `liveStore.status.kind`: **live**
    -> "writing to client -> client ok" (accent); **offline** -> the SAME disabled-with-reason copy
    05-10 uses, exactly "Offline — attach a client to move objects live" (warn/dashed) — **do NOT
    render any "staged (patch)" copy anywhere in this component** (D-05 erratum). Render the floating
    delta readout (top-center, ONLY visible while a drag is in progress — driven by the same
    imperative drag-active signal, not React state re-renders during the drag itself, though
    show/hide on drag-start/drag-end IS an acceptable state transition since it happens once per drag,
    not per frame; a drag can only occur while attached and, for Scale, only while
    `scaleUnavailableOnBuild` is false, so this readout inherits the same availability rules).
    Finally, confirm 05-UI-SPEC.md's "## Errata" item 1 (D-05) still accurately reflects this task's
    implemented copy/gate, and ADD a new Errata entry (item 5) documenting the ROUND 2
    scale-unavailable-on-build structural disabled state for the gizmo/readout-bar/numboxes/client-card
    scale row/StatusBar chip (a one-paragraph cross-reference to 05-03/05-07/05-10/this plan, matching
    the existing Errata entries' format) — this is the first UI-SPEC touch this review round makes;
    keep it brief, do not restate every plan's full logic, just the copy contract and precedence rule.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- TransformReadoutBar</automated>
  </verify>
  <acceptance_criteria>
    - A simulated 60-tick drag sequence asserts the component's React render count during the drag is O(1) (at most the drag-start/drag-end transitions), not O(60) — proving numbox updates are imperative, not state-driven.
    - Typed numbox entry (Enter to commit) calls the same `writeTransform` function a gizmo drag calls (same import, verified by a spy) — and a test asserts typed entry is IMPOSSIBLE (input disabled/no commit fires) while `liveStore.status.kind !== 'attached'`.
    - A NEW test (ROUND 2) asserts Scale-group numboxes are disabled and typed entry is impossible when `status.kind==='attached'` AND `verifiedState.scaleUnavailableOnBuild===true`, while Pos/Rot numboxes remain fully interactive in that same state — proving the Scale gate is independent of the Pos/Rot gate.
    - The write-target indicator's text is exactly `Offline — attach a client to move objects live` when offline (grep/DOM text match) — a test asserts the string `staged (patch)` never appears anywhere in this component's rendered output or source.
    - The write-target indicator flips between the live copy and the D-05 offline copy when `liveStore.status.kind` changes.
    - `grep -c "D-05" .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md` is >= 1 and a NEW ROUND 2 errata entry exists (grep for a distinguishing string this task adds, e.g. "scaleUnavailableOnBuild" or "unavailable on this build") with copy matching this task's implementation verbatim.
  </acceptance_criteria>
  <done>The readout bar mirrors live drag values with zero per-frame React churn, accepts typed entry through the identical write path ONLY while attached and (for Scale) only while build-available, guard-aware per channel matching 05-10's gizmo, shows the floating delta readout only during an active drag, and its offline write-target indicator matches the gizmo's D-05 disabled-with-reason copy exactly — no "staged (patch)" copy anywhere on this surface, and the UI-SPEC now documents the ROUND 2 scale-unavailable-on-build state.</done>
</task>

<task type="auto">
  <name>Task 3: StatusBar mirror (per-channel guard chip with scale-precedence) + corner-axis-gizmo/vp-stats + final Viewport wiring</name>
  <files>packages/renderer/src/shell/StatusBar.tsx, packages/renderer/src/panels/viewport/Viewport.tsx</files>
  <read_first>
    - packages/renderer/src/shell/StatusBar.tsx (lines 60-100 store reads, lines 330-420 existing live-mode segment + `Dot()` helper — extend in place, same idiom)
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — mount point for the new corner-axis-gizmo + vp-stats element, alongside the EXISTING verts/tris/draws StatsCollector overlay which stays unchanged)
    - This plan's Tasks 1-2 output (LiveSyncClientCard's scale-precedence helper, if extracted to a shared module — reuse it here rather than re-deriving the precedence logic) and 05-10's GizmoModeRail/TransformGizmo — this task's final wiring mounts ALL of Surface 1's elements together in Viewport.tsx
  </read_first>
  <action>
    Extend StatusBar.tsx immediately adjacent to the existing live-mode segment (lines 338-344): add
    a mode segment (reads the shared gizmo-mode state from 05-10 — thread it through liveStore or a
    small dedicated store/context if it does not already live in a shared location accessible to
    StatusBar), a sync segment (reads `status`), a COW-snapshot segment (reads
    `cowSnapshot?.transform !== null`), and a PER-CHANNEL-AWARE guard segment (REVIEWS.md Fix B, ROUND
    2 precedence): "guard: read-verify ok" when `guardState.transform==='ok'` AND EITHER
    `guardState.scale==='ok'` OR scale has no baseline yet OR `scaleUnavailableOnBuild===true` (a
    build-capability gap is never surfaced as a guard failure in this compact chip — ROUND 2); the
    refused variant naming exactly which channel(s) are genuinely blocked when at least one is —
    `guardState.transform==='blocked'` always qualifies; `guardState.scale==='blocked'` qualifies ONLY
    when `scaleUnavailableOnBuild===false` (reuse Task 1's precedence helper if it was extracted to a
    shared module, rather than re-deriving this logic ad hoc) — never a single ambiguous refused state
    that can't distinguish which channel tripped, each using the same `<Dot/>` separator + "read store
    value, map to glyph+color+text" idiom already used throughout this file. In Viewport.tsx: add a
    NEW bottom-left `vp-stats` overlay (faint mono) reading a perspective/resolution/fps/SAB-status
    line — this is DISTINCT from and additional to the existing verts/tris/draws `StatsCollector`
    overlay (do not remove or repurpose that one; UI-SPEC item 7 is a new element). Add a bottom-right
    corner axis gizmo using drei's `GizmoHelper`/`GizmoViewport` (or the closest available drei
    corner-orientation-cube helper in the installed `@react-three/drei` version — verify the exact
    export name against the installed version's API before use) inside the `<Canvas>`. Finally, mount
    `LiveSyncClientCard` (top-right) and `TransformReadoutBar` (bottom-center) as DOM overlay siblings
    of `<Canvas>` (same layer as `MissingDepsOverlay`/`GizmoModeRail`), completing the full Surface 1
    assembly. Verify every one of these switches together when `liveStore.status` transitions
    attached<->idle (a single store change drives all of them, since they all read the same
    `useLiveStore` selectors — no per-component duplicated live/offline derivation logic).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- StatusBar Viewport</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms the existing verts/tris/draws StatsCollector overlay is unmodified and a SECOND, new `vp-stats` element exists with the perspective/resolution/fps/SAB-status format.
    - A single `useLiveStore` status transition (attached to idle) in a test flips the live-chip, client card border style, readout write-target text, AND the StatusBar sync segment simultaneously.
    - A test with `guardState = { transform: 'blocked', scale: 'ok' }` asserts the StatusBar guard segment names transform specifically (not a bare ambiguous refused state).
    - A NEW test (ROUND 2) with `guardState = { transform: 'ok', scale: 'blocked' }` AND `scaleUnavailableOnBuild === true` asserts the StatusBar guard segment reads the PLAIN "ok" variant (scale's block is NOT named — it is a build gap, not a tamper) — and a companion test with the SAME guardState but `scaleUnavailableOnBuild === false` asserts scale IS named as refused — proving the precedence rule is actually wired into this chip, not just the client card.
    - StatusBar's new segments follow the exact `<Dot/>` + glyph+color+text idiom (no new separator style introduced).
  </acceptance_criteria>
  <done>All of UI-SPEC Surface 1 is assembled in Viewport.tsx/StatusBar.tsx, switching live/offline from one source of truth, with a guard summary chip that names WHICH channel is genuinely blocked (never a build-capability gap misreported as a tamper) — LIVE-03's full HUD anatomy is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| liveStore guard/write-log state → HUD rendering | The HUD must faithfully reflect the agent's authoritative guard decision (per channel) and never offer a way around it — Revert ALL is the one sanctioned exception, and it is a re-baseline + known-good write, never an arbitrary forward force-write. |
| Channel GUARD_ADDR field → HUD banner | The banner must source its address from the real channel field, never fabricate/derive one client-side (closes REVIEWS.md Fix C's first half). |
| liveStore.lastDiscardedChange → reverted banner | The discarded-change disclosure must source its values verbatim from liveStore's own captured snapshot (itself sourced from the same GUARD_ADDR/expectedTransform()/verifiedState fields the guard-blocked banner displays), never fabricate or approximate them (ROUND 2 decision #3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | LiveSyncClientCard guard-blocked state | mitigate | No force-write control exists anywhere in the card; the only actions are re-read/retry and revert-to-snapshot, both of which re-enter the normal guarded write path (via the coalesced gate-on-blocked revert) rather than bypassing it. |
| T-05-19 | Information disclosure (low) | Guard-blocked banner's real address/bytes | accept | Displaying the live object's own memory address/bytes to the LOCAL user of a single-user desktop modding tool is not a meaningful disclosure risk — this is diagnostic information about a process the user already controls and has attached to. |
| T-05-31 | Repudiation | StatusBar per-channel guard chip | mitigate | Naming which specific channel (transform/scale) is blocked, rather than a single ambiguous glyph, prevents a user from misreading a scale-only block as "everything is fine" or vice versa — directly closes REVIEWS.md Fix B's UI-visibility half; ROUND 2 extends this to never misreport a build-capability gap as a tamper. |
| T-05-37 | Repudiation | Reverted banner omitting a discarded external change | mitigate | `lastDiscardedChange` is rendered whenever non-null, naming the real address/expected/found bytes of whatever was overwritten by a gate-on-blocked revert — closes the ROUND 1 Sonnet HIGH "silent overwrite" UX gap without introducing any new bypass of the write guard itself (ROUND 2 decision #3). |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- LiveSyncClientCard TransformReadoutBar StatusBar Viewport` — all
green.
</verification>

<success_criteria>
- Every UI-SPEC Surface 1 element is present and switches live/offline from one source of truth.
- An inline mismatch warning honestly discloses when the loaded viewport asset differs from the live-moving target — this is disclosure, not a fix for the underlying viewport<->live-object binding gap, which remains open until Phase 7 (ROUND 4, REVIEWS.md round-3 maintainer decision #3(1)).
- A same-template-coincidence (names match but may be unrelated objects) never renders as a silent, unqualified confirmation — a quiet "(name match only — not a verified object identity)" caveat renders instead of nothing (ROUND 5, REVIEWS.md round-4 maintainer decision #3).
- The reverted banner discloses discarded external changes PER CHANNEL (transform and/or scale independently), never assuming a single combined shape (ROUND 4); the scale disclosure clause is honest about whether its "got" value is an independently-verified live read or a last-known value (ROUND 5, gotVerified, decision #4).
- All three B2 safety states render with locked copy, REAL address/expected-bytes values (address named concretely as `liveStore.guardAddr`, ROUND 5 decision #4), and no force-write escape hatch; transform and scale guard state are independently visible, with scale's build-unavailable state never misrendered as a tamper.
- Revert ALL is always clickable while attached, including when a guard is blocked, and the reverted banner discloses any discarded external change.
- HUD updates during drag are imperative, not React-state-driven.
- The readout bar's offline write-target indicator matches the gizmo's D-05 disabled-with-reason copy exactly — no "staged (patch)" copy anywhere on this surface; Scale numboxes independently gate on build-availability.
- Target-vs-player identity is honestly shown in the client card on EITHER build.
- The write log discloses its per-object, cache-restored nature via a permanent caption, so a retarget-back to a previously-edited object is never misread as stale/buggy data (ROUND 6, REVIEWS.md round-5 maintainer decision, Sonnet LOW).
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-11-SUMMARY.md` when done
</output>
</output>
</output>
