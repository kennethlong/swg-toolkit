---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 07
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - packages/renderer/src/hooks/useCommandWriter.ts
  - packages/renderer/src/state/liveStore.ts
  - packages/renderer/src/hooks/useChannelReader.ts
  - packages/renderer/src/hooks/useLiveService.ts
  - packages/renderer/src/panels/LiveInspectorPanel.tsx
autonomous: true
requirements: [LIVE-03]

must_haves:
  truths:
    - "writeTransform() reuses ONE pre-allocated buffer across every call for the session's lifetime — no new Float32Array/ArrayBuffer is allocated per drag-frame (LIVE-03 SC1 zero-allocation)."
    - "liveStore captures a COW snapshot exactly once per attach session, at the first verified read after attach — never re-captured on subsequent reads (D-03)."
    - "The guard-refused bit (GUARD_STATUS) is decoded from the same ArrayBuffer useChannelReader already polls at 60fps — no new native export was added for this direction."
    - "A detach/disconnect control exists in LiveInspectorPanel that calls the existing detachUI() export (D-04 item 2 — previously noted as landed logic with no button)."
    - "detachUI() sends a STOP_REQUESTED command before closing the channel, so the agent's poll loop exits cleanly instead of relying solely on process-exit cleanup (D-04 item 1, host-side half)."
  artifacts:
    - path: "packages/renderer/src/hooks/useCommandWriter.ts"
      provides: "writeTransform(mappingName, transform12, scale3) imperative export, zero-alloc, latest-wins"
      contains: "writeTransform"
    - path: "packages/renderer/src/state/liveStore.ts"
      provides: "cowSnapshot, writeLog, guardState fields + captureSnapshot/appendWriteLog/revertWrite/revertAll actions"
      contains: "cowSnapshot"
  key_links:
    - from: "packages/renderer/src/hooks/useCommandWriter.ts"
      to: "packages/live-inject (native addon)"
      via: "addon.writeCommand(mappingName, transformBuf, scaleBuf, flags)"
      pattern: "writeCommand"
    - from: "packages/renderer/src/hooks/useLiveService.ts"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "detachUI() writes a STOP_REQUESTED command before closeChannel"
      pattern: "STOP_REQUESTED"
---

<objective>
Build the renderer-side write path: a zero-allocation imperative command writer, the liveStore
extensions for the COW snapshot / write-log / guard state (D-03), the guard-status decode in
useChannelReader, and the clean-stop wiring for detachUI plus its missing UI control (D-04 items 1-2).

Purpose: this is the last plumbing layer before the actual gizmo/HUD components (05-10, 05-11) can
exist — it turns the native writeCommand export (05-04) and the agent's guarded apply (05-03) into a
renderer-consumable, testable API surface.

Output: `useCommandWriter.ts` (new), extended `liveStore.ts`/`useChannelReader.ts`/`useLiveService.ts`,
and a detach button in `LiveInspectorPanel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-04-PLAN.md
</context>

<interfaces>
Existing useChannelReader.ts poll shape (rAF loop, seqlock parse) — this plan extends
`parseChannelView`, does not replace the loop:
```ts
function parseChannelView(buf: ArrayBuffer): VerifiedObjectState | null {
  const view = new DataView(buf);
  const seq1 = view.getUint32(L.SEQ_COUNTER.offset, true);
  if ((seq1 & 1) !== 0) return null;
  // ... read transform/networkId/templateName/liveness ...
  const seq2 = view.getUint32(L.SEQ_COUNTER.offset, true);
  if (seq1 !== seq2) return null;
  return { networkId, templateName, transform, playerAlive };
}
```

Existing liveStore.ts shape (Zustand, `set({...})` per-action idiom) — extend with new fields/actions,
do not change the existing ones' signatures:
```ts
export interface LiveStore {
  status: ConnectionStatus; mode: InjectionMode; disabledReason: string | null;
  verifiedState: VerifiedObjectState | null; regionBytes: Uint8Array | null;
  beginAttach, attachComplete, attachError, setMode, updateState, updateRegion, detach
}
```

Existing useLiveService.ts detachUI (currently only closes the channel + resets the store — this plan
adds the stop-command send BEFORE that, in the same function):
```ts
export function detachUI(): void {
  closeActiveChannel();
  useLiveStore.getState().detach();
}
```

05-04's new export: `addon.writeCommand(name: string, transform: Float32Array /* 12 */, scale:
Float32Array /* 3 */, flags: number): void`. 05-01's contract additions:
`LIVE_CHANNEL_LAYOUT.GUARD_STATUS` (offset 388, len 4), `LIVE_CMD_FLAGS.STOP_REQUESTED = 0x1`,
`LIVE_GUARD_FLAGS.WRITE_REFUSED = 0x1`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCommandWriter.ts — zero-allocation imperative write path</name>
  <files>packages/renderer/src/hooks/useCommandWriter.ts</files>
  <behavior>
    - `writeTransform(mappingName, transform: ArrayLike<number> /* 12 */, scale: ArrayLike<number> /*
      3 */)` copies the given values into ONE pre-allocated module-level `Float32Array(12)` /
      `Float32Array(3)` pair (created once, reused for the process lifetime) and calls
      `addon.writeCommand(mappingName, preallocTransform, preallocScale, 0)` synchronously.
    - Calling `writeTransform` 1000 times in a loop must not increase the count of Float32Array
      allocations beyond the two created at module load (assert via a counting mock of the addon +
      a spy on `Float32Array` construction, or by asserting the SAME array object reference is passed
      to `addon.writeCommand` on every call).
    - `writeStop(mappingName)` calls `addon.writeCommand(mappingName, zeroedTransform, zeroedScale,
      LIVE_CMD_FLAGS.STOP_REQUESTED)` — reuses the SAME preallocated buffers (zeroed once), does not
      allocate.
  </behavior>
  <read_first>
    - packages/renderer/src/hooks/useChannelReader.ts (full file — the require('@swg/live-inject') addon-typing convention to mirror for this file's own addon import)
    - packages/renderer/src/hooks/useLiveService.ts (full file — the "plain async functions, not a React hook" convention this file's sibling functions should follow, since writeTransform is called imperatively from drag handlers, not from a component body)
    - 05-01-PLAN.md / 05-04-PLAN.md (exact writeCommand signature and LIVE_CMD_FLAGS/LIVE_GUARD_FLAGS contract)
  </read_first>
  <action>
    Create useCommandWriter.ts as a set of plain exported functions (NOT a React hook — mirrors
    useLiveService.ts's "plain async functions so button/drag handlers can call them directly"
    convention, despite the filename's `use` prefix matching this phase's PATTERNS.md naming). At
    module scope, declare `const preallocTransform = new Float32Array(12);` and
    `const preallocScale = new Float32Array(3);` — exactly once, never inside a function. Import the
    addon the same way useChannelReader.ts does (`require('@swg/live-inject')`, typed inline).
    Implement `writeTransform(mappingName: string, transform: ArrayLike<number>, scale:
    ArrayLike<number>): void`: copy `transform`'s 12 values into `preallocTransform` (a simple indexed
    loop or `.set()`), copy `scale`'s 3 values into `preallocScale`, call
    `addon.writeCommand(mappingName, preallocTransform, preallocScale, 0)`. Implement
    `writeStop(mappingName: string): void`: zero both preallocated arrays, call
    `addon.writeCommand(mappingName, preallocTransform, preallocScale,
    LIVE_CMD_FLAGS.STOP_REQUESTED)` (import `LIVE_CMD_FLAGS` from `@swg/contracts`). Both functions
    are synchronous void calls (no Promise, no async) — the gizmo drag handler (05-10) calls
    `writeTransform` directly on every drag-move event with zero await overhead.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- useCommandWriter</automated>
  </verify>
  <acceptance_criteria>
    - A test calling writeTransform 100 times asserts the addon mock always receives the SAME Float32Array object reference (identity check, not just equal values) for both transform and scale arguments.
    - writeStop's flags argument equals `LIVE_CMD_FLAGS.STOP_REQUESTED` exactly.
  </acceptance_criteria>
  <done>A synchronous, zero-allocation, latest-wins write path exists that 05-10's gizmo drag handler and 05-11's numbox typed-entry can both call directly.</done>
</task>

<task type="auto">
  <name>Task 2: liveStore COW snapshot/write-log/guard state + useChannelReader guard decode + detachUI stop-signal + detach UI control</name>
  <files>packages/renderer/src/state/liveStore.ts, packages/renderer/src/hooks/useChannelReader.ts, packages/renderer/src/hooks/useLiveService.ts, packages/renderer/src/panels/LiveInspectorPanel.tsx</files>
  <read_first>
    - packages/renderer/src/state/liveStore.ts (full file — extend in place, same `set({...})` per-action idiom already used for every existing action)
    - packages/renderer/src/hooks/useChannelReader.ts (full file — parseChannelView, the seqlock retry-read to mirror for the new GUARD_STATUS decode)
    - packages/renderer/src/hooks/useLiveService.ts (full file — detachUI/closeActiveChannel, the exact function this task extends)
    - packages/renderer/src/panels/LiveInspectorPanel.tsx (lines 1-90 — panel head structure, existing attach-trigger button styling to mirror for the new detach button; STATE 2/3 render branches further down the file where the new button belongs)
  </read_first>
  <action>
    In liveStore.ts, add to `LiveStore`: `cowSnapshot: Float32Array | null` (attach-time transform,
    D-03), `writeLog: { id: number; atMs: number; deltaLabel: string; resultingTransform:
    Float32Array }[]` (append-only per-session log, newest entries can be reverted individually),
    `guardState: 'ok' | 'blocked'`. Add actions: `captureSnapshotIfNeeded(state:
    VerifiedObjectState)` — called from the existing `updateState` action's body (do not add a
    separate call site elsewhere): if `cowSnapshot` is currently null AND `state !== null`, set
    `cowSnapshot: state.transform.slice()` (copy, not the live reference) — this makes updateState
    itself the single, correct place the D-03 "captured once, at attach" invariant lives, since
    updateState already runs on every 60fps poll tick and is naturally idempotent after the first
    capture. `appendWriteLog(entry)` — pushes to writeLog. `setGuardState(state: 'ok' | 'blocked')`.
    `revertWrite(id: number)` and `revertAll()` — both compute a target transform (revertWrite: the
    resultingTransform of the entry immediately BEFORE the given id, or `cowSnapshot` if reverting the
    first entry; revertAll: `cowSnapshot` directly) and call `useCommandWriter`'s `writeTransform`
    with that target plus the CURRENT scale (liveStore does not separately track scale history in this
    plan — reuse `verifiedState`'s current values for any field not explicitly part of the revert
    target, since Move/Rotate/Scale share one command slot per 05-01's design) — import
    useCommandWriter at the top of liveStore.ts (acceptable since useCommandWriter has no React
    dependency). Reset `cowSnapshot`/`writeLog`/`guardState` to their initial values inside the
    existing `detach()` action (extend it, do not replace). In useChannelReader.ts's
    `parseChannelView` (or a small sibling decode step called right after it), also read
    `LIVE_CHANNEL_LAYOUT.GUARD_STATUS` via `view.getUint32(offset, true)` and, in the poll loop's
    per-frame body, call `useLiveStore.getState().setGuardState((guardStatus &
    LIVE_GUARD_FLAGS.WRITE_REFUSED) !== 0 ? 'blocked' : 'ok')` — this is the renderer-side half of
    the D-03 guard observability, requiring no new native export (reads the same ArrayBuffer already
    polled). Extend `detachUI()` in useLiveService.ts: before `closeActiveChannel()`, if
    `useLiveStore.getState().status.kind === 'attached'`, call `writeStop(status.mappingName)` (import
    from useCommandWriter.ts) so the agent's poll loop observes STOP_REQUESTED and exits cleanly
    (D-04 item 1's host-side trigger — the agent-side handling was completed in 05-03). Wrap this call
    in a try/catch mirroring the existing `closeChannel` best-effort pattern (channel may already be
    gone). In LiveInspectorPanel.tsx: add a `Detach` button, styled consistently with the existing
    attach-trigger buttons (reuse the file's existing button style constant), visible only when
    `status.kind === 'attached'` (STATE 2/3), calling `detachUI()` on click — this closes the D-04
    item 2 gap ("no such button exists yet").
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- liveStore useChannelReader</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts calling `updateState` twice with two DIFFERENT transforms only sets `cowSnapshot` from the FIRST call (the second call's transform must NOT overwrite an already-captured snapshot).
    - A test asserts `setGuardState('blocked')` is called when the simulated ArrayBuffer's GUARD_STATUS bit0 is set, and `'ok'` when clear.
    - A test asserts `detachUI()` calls `writeStop(mappingName)` before `closeChannel(mappingName)` when a session is attached, and skips `writeStop` entirely when idle (no channel to signal).
    - LiveInspectorPanel renders a Detach button only in the attached state (grep/test confirms conditional render on `status.kind === 'attached'`).
  </acceptance_criteria>
  <done>liveStore fully models the D-03 COW/write-log/guard contract; the renderer observes agent-side guard refusals without new native surface; detachUI cleanly stops the agent; a real Detach button exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer write-path helpers (useCommandWriter, liveStore revert actions) → native writeCommand | These are the only renderer-side call sites that can influence the agent's next apply; both must always route through the same zero-alloc writer, never a second ad-hoc write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | liveStore revertWrite/revertAll | mitigate | Both compute their target transform from the toolkit's own write-log / cowSnapshot state (never from unvalidated external input) and route through the SAME guarded writeCommand path as a normal drag write — no separate "trusted revert" bypass of the agent's read-verify guard exists. |
| T-05-15 | DoS | detachUI stop-signal best-effort | accept | writeStop is wrapped in try/catch and detach proceeds regardless of its outcome (mirrors the existing closeChannel best-effort pattern) — a failed stop-signal degrades to the pre-existing OS-reclaim-on-process-exit behavior, not a stuck detach. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- useCommandWriter liveStore useChannelReader` — all green.
</verification>

<success_criteria>
- writeTransform/writeStop are zero-allocation and reusable across the whole session.
- liveStore correctly captures the COW snapshot exactly once and exposes guard/write-log state.
- detachUI cleanly signals the agent to stop and a Detach button exists in the UI.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-SUMMARY.md` when done
</output>
