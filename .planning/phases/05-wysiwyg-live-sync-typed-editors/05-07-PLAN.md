---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 07
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - packages/renderer/src/hooks/useCommandWriter.ts
  - packages/renderer/src/state/liveStore.ts
  - packages/renderer/src/hooks/useChannelReader.ts
  - packages/renderer/src/hooks/useLiveService.ts
  - packages/renderer/src/panels/LiveInspectorPanel.tsx
  - packages/contracts/src/live-inject.ts
autonomous: true
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): restructured from 2 tasks to 3 and folds in several
# 05-REVIEWS.md findings:
# (1) Fix B (Scale guard/revert) — cowSnapshot now stores BOTH transform AND scale; writeLog entries
#     carry resultingScale; revertWrite/revertAll restore the HISTORICAL scale from the log/snapshot,
#     NOT the current live scale (the original plan's "reuse verifiedState's CURRENT scale" was wrong
#     — it would silently drop a scale revert whenever the guard needed to restore a prior pose).
# (2) Fix C ("expected bytes" half) — this is a PURE RENDERER FIX, no channel change needed (05-01
#     already added the real GUARD_ADDR field for the "real address" half). The guard-blocked banner's
#     "expected" bytes must be derived from the last SUCCESSFUL writeLog entry, not from the attach-time
#     cowSnapshot (which goes stale after write #1, since the agent's own guard baseline evolves per
#     05-03's read-back-refresh design) — implemented here as a `expectedTransform`/`expectedScale`
#     selector liveStore exposes, consumed by 05-11.
# (3) Opus MEDIUM (blocked guard blocks its own revert) — revertAll now sends a TWO-STEP command:
#     REBASELINE_GUARD first (re-syncs the agent's expected-bytes baseline to current live bytes,
#     applies nothing), then the actual revert-to-snapshot write as a normal follow-up command — this
#     un-sticks a blocked guard via the user's own explicit action, never a silent forward force-write.
# (4) ROADMAP SC1 MEDIUM (Cursor) — "no allocation in the 60fps path" was only half-met: the WRITE path
#     (useCommandWriter) was already zero-alloc, but the READ path (useChannelReader's parseChannelView)
#     allocated a Uint8Array + a Float32Array-via-buf.slice() every RAF tick. FIX: new Task 3 makes the
#     read path reuse one module-level Float32Array (written element-by-element via DataView, no
#     buf.slice()) and drops the per-frame `new Uint8Array(buf)` for the HexInspector region feed in
#     favor of a single reused buffer, resolving SC1 for BOTH directions rather than leaving it silently
#     half-met.
# (5) Decision #1 (object targeting) — liveStore/contracts gain hasTarget/targetUnavailableOnBuild
#     fields decoded from the read frame's liveness bits (05-03 publishes them) so 05-10/05-11 can show
#     honest "target: <name>" / "target: none — showing player" / "target: unavailable on this build"
#     copy instead of silently treating the player as the target.
# Prior plan's Task 1 (useCommandWriter) is functionally unchanged except gaining writeRebaselineGuard.

must_haves:
  truths:
    - "writeTransform() reuses ONE pre-allocated buffer across every call for the session's lifetime — no new Float32Array/ArrayBuffer is allocated per drag-frame (LIVE-03 SC1 zero-allocation, write direction)."
    - "The READ path (useChannelReader) is ALSO zero-allocation per RAF tick — no per-frame Uint8Array/Float32Array/buf.slice() allocation (LIVE-03 SC1, read direction — REVIEWS.md MEDIUM fix, previously silently half-met)."
    - "liveStore captures a COW snapshot (transform AND scale) exactly once per attach session, at the first verified read after attach — never re-captured on subsequent reads (D-03)."
    - "revertWrite/revertAll restore the HISTORICAL scale associated with the target write-log entry (or the attach-time snapshot scale for revertAll), never the CURRENT live scale (REVIEWS.md Fix B)."
    - "The guard-blocked banner's 'expected' bytes are derived from the LAST SUCCESSFUL write-log entry (or the attach-time snapshot if no write has succeeded yet), never unconditionally from the attach-time snapshot alone — this stays correct after write #1 (REVIEWS.md Fix C)."
    - "revertAll sends a REBASELINE_GUARD command before the actual revert-to-snapshot write, so a blocked guard can always be un-stuck by this explicit user action without ever accepting an arbitrary forward-write target (REVIEWS.md Opus MEDIUM fix)."
    - "The guard-refused state is decoded per-channel (transform vs scale) from the same ArrayBuffer useChannelReader already polls at 60fps — no new native export was added for either direction."
    - "hasTarget/targetUnavailableOnBuild are decoded from the read frame's liveness bits and exposed on liveStore, so downstream HUD copy can honestly state what object is being moved (05-CONTEXT decision #1)."
    - "A detach/disconnect control exists in LiveInspectorPanel that calls the existing detachUI() export (D-04 item 2 — previously noted as landed logic with no button)."
    - "detachUI() sends a STOP_REQUESTED command before closing the channel, so the agent's poll loop exits cleanly instead of relying solely on process-exit cleanup (D-04 item 1, host-side half)."
  artifacts:
    - path: "packages/renderer/src/hooks/useCommandWriter.ts"
      provides: "writeTransform(mappingName, transform12, scale3) imperative export, zero-alloc, latest-wins; writeStop(mappingName); writeRebaselineGuard(mappingName)"
      contains: "writeRebaselineGuard"
    - path: "packages/renderer/src/state/liveStore.ts"
      provides: "cowSnapshot{transform,scale}, writeLog (with resultingTransform+resultingScale), guardState per-channel, captureSnapshotIfNeeded/appendWriteLog/revertWrite/revertAll actions, hasTarget/targetUnavailableOnBuild fields, expectedTransform()/expectedScale() selectors for the HUD banner"
      contains: "expectedTransform"
  key_links:
    - from: "packages/renderer/src/hooks/useCommandWriter.ts"
      to: "packages/live-inject (native addon)"
      via: "addon.writeCommand(mappingName, transformBuf, scaleBuf, flags)"
      pattern: "writeCommand"
    - from: "packages/renderer/src/state/liveStore.ts"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "revertAll() calls writeRebaselineGuard(mappingName) THEN writeTransform(mappingName, snapshotTransform, snapshotScale)"
      pattern: "writeRebaselineGuard"
    - from: "packages/renderer/src/hooks/useLiveService.ts"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "detachUI() writes a STOP_REQUESTED command before closeChannel"
      pattern: "STOP_REQUESTED"
---

<objective>
Build the renderer-side write AND read path: a zero-allocation imperative command writer (now
including a rebaseline-guard command), a zero-allocation channel READER (closing the half-met SC1
finding), the liveStore extensions for the COW snapshot / write-log / per-channel guard state / honest
target-vs-player identity (D-03, Fix B, Fix C, decision #1), and the clean-stop wiring for detachUI
plus its missing UI control (D-04 items 1-2).

Purpose: this is the last plumbing layer before the actual gizmo/HUD components (05-10, 05-11) can
exist — it turns the native writeCommand export (05-04) and the agent's guarded apply (05-03) into a
renderer-consumable, testable, ALLOCATION-HONEST API surface, and gives the HUD the real data it needs
to show correct expected/target information instead of deriving it from stale or absent state.

Output: `useCommandWriter.ts` (new, now with `writeRebaselineGuard`), extended
`liveStore.ts`/`useChannelReader.ts`/`useLiveService.ts`/`contracts/live-inject.ts`, and a detach
button in `LiveInspectorPanel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-04-PLAN.md
</context>

<interfaces>
Existing useChannelReader.ts poll shape (rAF loop, seqlock parse, VERIFIED against current source this
session — note the two allocation sites this plan's Task 3 removes):
```ts
function parseChannelView(buf: ArrayBuffer): VerifiedObjectState | null {
  const view = new DataView(buf);
  const seq1 = view.getUint32(L.SEQ_COUNTER.offset, true);
  if ((seq1 & 1) !== 0) return null;
  const transform = new Float32Array(buf.slice(L.TRANSFORM.offset, L.TRANSFORM.offset + L.TRANSFORM.length)); // <-- allocates every call
  // ... networkId, templateName, liveness ...
  const seq2 = view.getUint32(L.SEQ_COUNTER.offset, true);
  if (seq1 !== seq2) return null;
  return { networkId, templateName, transform, playerAlive };
}
export function useChannelReader(): void {
  function poll() {
    const buf = addon.readChannelView(mappingName);
    if (buf) {
      useLiveStore.getState().updateRegion(new Uint8Array(buf)); // <-- allocates every call
      const state = parseChannelView(buf);
      if (state !== null) useLiveStore.getState().updateState(state);
    }
    rafRef.current = requestAnimationFrame(poll);
  }
}
```

Existing liveStore.ts shape (Zustand, `set({...})` per-action idiom, VERIFIED against current source
this session) — extend with new fields/actions, do not change the existing ones' signatures:
```ts
export interface LiveStore {
  status: ConnectionStatus; mode: InjectionMode; disabledReason: string | null;
  verifiedState: VerifiedObjectState | null; regionBytes: Uint8Array | null;
  beginAttach, attachComplete, attachError, setMode, updateState, updateRegion, detach
}
export const useLiveStore = create<LiveStore>((set) => ({ ... }));
```

Existing useLiveService.ts detachUI (currently only closes the channel + resets the store — this plan
adds the stop-command send BEFORE that, in the same function):
```ts
export function detachUI(): void {
  closeActiveChannel();
  useLiveStore.getState().detach();
}
```

05-04's new export: `addon.writeCommand(name: string, transform: Float32Array /* 12 */, scale:
Float32Array /* 3 */, flags: number): void`. 05-01's contract additions (396-byte layout):
`LIVE_CHANNEL_LAYOUT.GUARD_STATUS` (offset 388, len 4), `.GUARD_ADDR` (offset 392, len 4 —
agent-authoritative x86 pointer), `LIVE_CMD_FLAGS.STOP_REQUESTED = 0x1`,
`LIVE_CMD_FLAGS.REBASELINE_GUARD = 0x2`, `LIVE_GUARD_FLAGS.TRANSFORM_REFUSED = 0x1`,
`LIVE_GUARD_FLAGS.SCALE_REFUSED = 0x2`. 05-03's new liveness bits: bit2 = `hasTarget`, bit3 =
`targetUnavailableOnBuild` (in the EXISTING `LIVENESS` field, offset 316 — no new offset, just new bit
semantics).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCommandWriter.ts — zero-allocation imperative write path (transform, scale, stop, rebaseline-guard)</name>
  <files>packages/renderer/src/hooks/useCommandWriter.ts</files>
  <behavior>
    - `writeTransform(mappingName, transform: ArrayLike<number> /* 12 */, scale: ArrayLike<number> /*
      3 */)` copies the given values into ONE pre-allocated module-level `Float32Array(12)` /
      `Float32Array(3)` pair (created once, reused for the process lifetime) and calls
      `addon.writeCommand(mappingName, preallocTransform, preallocScale, 0)` synchronously.
    - Calling `writeTransform` 1000 times in a loop must not increase the count of Float32Array
      allocations beyond the two created at module load (assert via a counting mock of the addon +
      a spy on `Float32Array` construction, or by asserting the SAME array object reference is passed
      to `addon.writeCommand` on every call).
    - `writeStop(mappingName)` calls `addon.writeCommand(mappingName, zeroedTransform, zeroedScale,
      LIVE_CMD_FLAGS.STOP_REQUESTED)` — reuses the SAME preallocated buffers (zeroed once), does not
      allocate.
    - `writeRebaselineGuard(mappingName)` (NEW — REVIEWS.md Opus MEDIUM fix) calls
      `addon.writeCommand(mappingName, preallocTransform, preallocScale,
      LIVE_CMD_FLAGS.REBASELINE_GUARD)` reusing the SAME preallocated buffers — their CONTENTS are
      irrelevant for this command (the agent ignores the transform/scale payload on a rebaseline
      command per 05-03, since it re-baselines from bytes it reads itself, not from the payload), so
      no zeroing/special-casing is needed beyond reusing the existing buffers.
  </behavior>
  <read_first>
    - packages/renderer/src/hooks/useChannelReader.ts (full file — the require('@swg/live-inject') addon-typing convention to mirror for this file's own addon import)
    - packages/renderer/src/hooks/useLiveService.ts (full file — the "plain async functions, not a React hook" convention this file's sibling functions should follow, since writeTransform is called imperatively from drag handlers, not from a component body)
    - 05-01-PLAN.md / 05-04-PLAN.md (exact writeCommand signature and LIVE_CMD_FLAGS/LIVE_GUARD_FLAGS contract, including the NEW REBASELINE_GUARD bit)
  </read_first>
  <action>
    Create useCommandWriter.ts as a set of plain exported functions (NOT a React hook — mirrors
    useLiveService.ts's "plain async functions so button/drag handlers can call them directly"
    convention, despite the filename's `use` prefix matching this phase's PATTERNS.md naming). At
    module scope, declare `const preallocTransform = new Float32Array(12);` and
    `const preallocScale = new Float32Array(3);` — exactly once, never inside a function. Import the
    addon the same way useChannelReader.ts does (`require('@swg/live-inject')`, typed inline).
    Implement `writeTransform(mappingName: string, transform: ArrayLike<number>, scale:
    ArrayLike<number>): void`: copy `transform`'s 12 values into `preallocTransform` (a simple indexed
    loop or `.set()`), copy `scale`'s 3 values into `preallocScale`, call
    `addon.writeCommand(mappingName, preallocTransform, preallocScale, 0)`. Implement
    `writeStop(mappingName: string): void`: zero both preallocated arrays, call
    `addon.writeCommand(mappingName, preallocTransform, preallocScale,
    LIVE_CMD_FLAGS.STOP_REQUESTED)`. Implement `writeRebaselineGuard(mappingName: string): void`: call
    `addon.writeCommand(mappingName, preallocTransform, preallocScale,
    LIVE_CMD_FLAGS.REBASELINE_GUARD)` reusing whatever the buffers currently hold (contents are
    ignored by the agent for this command kind, per 05-03). Import `LIVE_CMD_FLAGS` from
    `@swg/contracts`. All three functions are synchronous void calls (no Promise, no async) — the
    gizmo drag handler (05-10) calls `writeTransform` directly on every drag-move event with zero
    await overhead, and liveStore's `revertAll` (Task 2) calls `writeRebaselineGuard` then
    `writeTransform` as a two-step sequence.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- useCommandWriter</automated>
  </verify>
  <acceptance_criteria>
    - A test calling writeTransform 100 times asserts the addon mock always receives the SAME Float32Array object reference (identity check, not just equal values) for both transform and scale arguments.
    - writeStop's flags argument equals `LIVE_CMD_FLAGS.STOP_REQUESTED` exactly; writeRebaselineGuard's flags argument equals `LIVE_CMD_FLAGS.REBASELINE_GUARD` exactly (a distinct, non-zero, non-STOP_REQUESTED bit).
  </acceptance_criteria>
  <done>A synchronous, zero-allocation, latest-wins write path exists — including the new rebaseline-guard command — that 05-10's gizmo drag handler and 05-11's numbox typed-entry / Revert ALL button can all call directly.</done>
</task>

<task type="auto">
  <name>Task 2: liveStore COW snapshot (transform+scale)/write-log/per-channel guard state + honest target identity + rebaseline-then-revert + detachUI stop-signal + detach UI control</name>
  <files>packages/renderer/src/state/liveStore.ts, packages/renderer/src/hooks/useLiveService.ts, packages/renderer/src/panels/LiveInspectorPanel.tsx, packages/contracts/src/live-inject.ts</files>
  <read_first>
    - packages/renderer/src/state/liveStore.ts (full file — extend in place, same `set({...})` per-action idiom already used for every existing action)
    - packages/renderer/src/hooks/useLiveService.ts (full file — detachUI/closeActiveChannel, the exact function this task extends)
    - packages/renderer/src/panels/LiveInspectorPanel.tsx (lines 1-90 — panel head structure, existing attach-trigger button styling to mirror for the new detach button; STATE 2/3 render branches further down the file where the new button belongs)
    - packages/contracts/src/live-inject.ts (VerifiedObjectState — add hasTarget/targetUnavailableOnBuild)
    - 05-REVIEWS.md Fix B (scale revert), Fix C ("expected" derivation), Opus MEDIUM (rebaseline-then-revert), and 05-CONTEXT decision #1 (target identity) — this task implements all four
  </read_first>
  <action>
    In `packages/contracts/src/live-inject.ts`: add `hasTarget: boolean` and `targetUnavailableOnBuild:
    boolean` to `VerifiedObjectState` (decoded from the read frame's `LIVENESS` bits 2/3 — no new
    channel offset, just new bit semantics on the existing field; update the field's doc comment to
    describe all 4 bits). In liveStore.ts, add to `LiveStore`: `cowSnapshot: { transform: Float32Array;
    scale: Float32Array } | null` (attach-time transform AND scale, D-03 — REVISED from the original
    transform-only shape per REVIEWS.md Fix B), `writeLog: { id: number; atMs: number; deltaLabel:
    string; resultingTransform: Float32Array; resultingScale: Float32Array }[]` (append-only per-session
    log, newest entries can be reverted individually — REVISED to also carry `resultingScale`),
    `guardState: { transform: 'ok' | 'blocked'; scale: 'ok' | 'blocked' }` (REVISED from a single
    combined state to per-channel, matching 05-01/05-03's independent guard bits). Add actions:
    `captureSnapshotIfNeeded(state: VerifiedObjectState)` — called from the existing `updateState`
    action's body (do not add a separate call site elsewhere): if `cowSnapshot` is currently null AND
    `state !== null`, set `cowSnapshot: { transform: state.transform.slice(), scale: <current scale —
    NOTE: VerifiedObjectState/the read frame does not carry a live scale value (only transform);
    capture scale lazily on the FIRST scale-bearing write instead — see the note in 05-03 about scale
    having no read-back accessor. Until a scale write happens this session, `cowSnapshot.scale` stays
    `null` and revertAll simply does not touch scale (nothing to revert to) — document this as an
    explicit, non-fabricated limitation, not a bug> }` — this makes updateState itself the single,
    correct place the D-03 "captured once, at attach" invariant lives for TRANSFORM (scale's baseline
    is necessarily write-triggered, per the agent's own lack of a scale read-back). `appendWriteLog(entry)`
    — pushes to writeLog, entry now REQUIRES both `resultingTransform` AND `resultingScale` (callers —
    05-10/05-11 — always know the full command they just sent, so this is never a partial entry).
    `setGuardState(channel: 'transform' | 'scale', state: 'ok' | 'blocked')` (REVISED — per-channel
    setter, replacing the old single-argument version). Add TWO SELECTORS (not actions, pure
    derivations — REVIEWS.md Fix C): `expectedTransform(): Float32Array | null` returns
    `writeLog.length > 0 ? writeLog[writeLog.length - 1].resultingTransform : cowSnapshot?.transform ??
    null` and `expectedScale(): Float32Array | null` returns the analogous scale derivation — these are
    what 05-11's guard-blocked banner must read for its "expected" bytes, NEVER `cowSnapshot` directly
    once at least one write has succeeded (Fix C: the agent's own guard baseline evolves per write, so
    the renderer's notion of "expected" must track the SAME evolution, and the write-log already
    records exactly that). `revertWrite(id: number)` — computes the target transform/scale as the
    `resultingTransform`/`resultingScale` of the entry immediately BEFORE the given id (or
    `cowSnapshot` if reverting the first entry), calls `writeRebaselineGuard(mappingName)` THEN
    `writeTransform(mappingName, targetTransform, targetScale)` (the two-step sequence — REVISED per
    Opus MEDIUM: previously this called writeTransform alone, which could stay permanently blocked if
    the guard had already tripped; now the explicit revert action ALWAYS re-baselines first, since a
    revert is inherently a "I know what I want the object to be, override the log-drift" action, not a
    normal drag). `revertAll()` — same two-step sequence with `cowSnapshot.transform` /
    `cowSnapshot.scale` (skip the scale half of the write if `cowSnapshot.scale` is still null per the
    lazy-capture note above — send transform-only in that case, since there is nothing to revert scale
    TO). Reset `cowSnapshot`/`writeLog`/`guardState` to their initial values inside the existing
    `detach()` action (extend it, do not replace) — `guardState` resets to `{ transform: 'ok', scale:
    'ok' }`. In useChannelReader.ts's `parseChannelView` (or a small sibling decode step called right
    after it — Task 3 owns the zero-alloc rewrite of this file, this task just adds the NEW field
    reads to whatever shape Task 3 produces), also read `LIVE_CHANNEL_LAYOUT.GUARD_STATUS` via
    `view.getUint32(offset, true)` and call `useLiveStore.getState().setGuardState('transform',
    (guardStatus & LIVE_GUARD_FLAGS.TRANSFORM_REFUSED) !== 0 ? 'blocked' : 'ok')` and
    `setGuardState('scale', (guardStatus & LIVE_GUARD_FLAGS.SCALE_REFUSED) !== 0 ? 'blocked' : 'ok')`
    — this is the renderer-side half of the D-03 guard observability, requiring no new native export
    (reads the same ArrayBuffer already polled). Decode `hasTarget`/`targetUnavailableOnBuild` from
    the liveness bits into the `VerifiedObjectState` object `parseChannelView` returns (bit2/bit3 of
    the existing LIVENESS field). Extend `detachUI()` in useLiveService.ts: before
    `closeActiveChannel()`, if `useLiveStore.getState().status.kind === 'attached'`, call
    `writeStop(status.mappingName)` (import from useCommandWriter.ts) so the agent's poll loop observes
    STOP_REQUESTED and exits cleanly (D-04 item 1's host-side trigger — the agent-side handling was
    completed in 05-03). Wrap this call in a try/catch mirroring the existing `closeChannel`
    best-effort pattern (channel may already be gone). In LiveInspectorPanel.tsx: add a `Detach`
    button, styled consistently with the existing attach-trigger buttons (reuse the file's existing
    button style constant), visible only when `status.kind === 'attached'` (STATE 2/3), calling
    `detachUI()` on click — this closes the D-04 item 2 gap ("no such button exists yet").
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- liveStore useChannelReader</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts calling `updateState` twice with two DIFFERENT transforms only sets `cowSnapshot.transform` from the FIRST call (the second call's transform must NOT overwrite an already-captured snapshot).
    - A test asserts `setGuardState('scale', 'blocked')` does NOT flip `guardState.transform` — the two channels are independently settable (grep/test confirms `guardState` is an object with both keys, not a single string).
    - A test asserts `expectedTransform()` returns `cowSnapshot.transform` when `writeLog` is empty, and returns `writeLog[writeLog.length-1].resultingTransform` after at least one append — proving the Fix C "expected tracks the last successful write" derivation.
    - A test asserts `revertAll()` calls `writeRebaselineGuard(mappingName)` BEFORE `writeTransform(mappingName, ...)` (call-order assertion on the mocked useCommandWriter functions) — proving the Opus MEDIUM two-step fix.
    - A test asserts `revertWrite`/`revertAll` use the write-log/snapshot's HISTORICAL scale, never `verifiedState`'s current scale (there is no "current scale" field to accidentally read — the test asserts no code path in these two actions references `verifiedState.scale` or any similarly-named current-scale source).
    - A test asserts `detachUI()` calls `writeStop(mappingName)` before `closeChannel(mappingName)` when a session is attached, and skips `writeStop` entirely when idle (no channel to signal).
    - LiveInspectorPanel renders a Detach button only in the attached state (grep/test confirms conditional render on `status.kind === 'attached'`).
    - `VerifiedObjectState` (contracts) has `hasTarget`/`targetUnavailableOnBuild` boolean fields.
  </acceptance_criteria>
  <done>liveStore fully models the D-03 COW/write-log/per-channel-guard contract with historically-correct scale revert, the HUD can always derive a currently-accurate "expected" baseline (Fix C), a blocked guard can always be un-stuck via the user's own explicit revert action (Opus MEDIUM), target-vs-player identity is honestly exposed, detachUI cleanly stops the agent, and a real Detach button exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useChannelReader.ts — zero-allocation read path (closes the half-met SC1 finding)</name>
  <files>packages/renderer/src/hooks/useChannelReader.ts</files>
  <behavior>
    - `parseChannelView` no longer allocates a `Float32Array` via `buf.slice(...)` per call — it
      writes the 12 transform floats into ONE pre-allocated module-level `Float32Array(12)` (via
      `DataView.getFloat32` reads in a loop, or `Float32Array.set` against a `Float32Array` VIEW over
      the SAME underlying buffer when alignment allows — since the channel's `TRANSFORM` offset (4) is
      not guaranteed 4-byte-aligned relative to a fresh `ArrayBuffer` from N-API, use the safe
      per-element `DataView` read path, not an unaligned typed-array view) and returns THAT reused
      array (or a value shape referencing it) rather than a fresh allocation every tick.
    - The poll loop's `updateRegion(new Uint8Array(buf))` call (HexInspector raw-bytes feed) no longer
      allocates a new `Uint8Array` every RAF tick — reuse ONE module-level `Uint8Array` of the channel's
      fixed size (396 bytes, from `LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length`) and copy into it via `.set()`
      from a view over `buf`, OR (simpler, equally zero-alloc) pass a `Uint8Array` VIEW directly (`new
      Uint8Array(buf)` is itself cheap/non-copying when `buf` is a genuinely fresh per-call ArrayBuffer
      from the native layer — verify with `addon.readChannelView`'s actual contract: if it returns the
      SAME underlying mapped buffer every call (not a fresh copy), a `Uint8Array` VIEW constructor call
      is NOT a data-copy allocation of concern the same way `buf.slice()` is; if it returns a FRESH
      ArrayBuffer each call, minimize further copies by reusing one `Uint8Array` and `.set()`-ing into
      it). Document which case applies (inspect `channel_binding.cpp`'s `ReadChannelView` to determine
      whether it returns the same backing buffer or a fresh one each call) and implement accordingly —
      the acceptance test is behavioral (call count of `new Float32Array(...)`/`new Uint8Array(...)`
      inside the hot poll path stays at the module-load-time baseline across 100 simulated frames), not
      prescriptive about which exact zero-copy technique is used.
    - A 100-tick simulated poll loop shows ZERO net-new `Float32Array`/`Uint8Array` allocations beyond
      whatever the native `readChannelView` call itself does (which this hook does not control) — test
      via a spy/counter on the relevant constructors across the 100 ticks, asserting the count stays at
      the ~1-2 module-load-time allocations.
  </behavior>
  <read_first>
    - packages/renderer/src/hooks/useChannelReader.ts (full file — the CURRENT allocation sites this task removes: `buf.slice(...)` at the TRANSFORM read, `new Uint8Array(buf)` at the updateRegion call)
    - packages/live-inject/src/channel_binding.cpp (ReadChannelView's implementation — determine whether it returns the SAME backing ArrayBuffer/view across calls or allocates fresh each time, since that determines which zero-copy technique applies here)
    - 05-REVIEWS.md's MEDIUM finding on this exact allocation pattern (Cursor: "ROADMAP SC1 'no allocation in the 60fps path' is only half-met")
  </read_first>
  <action>
    Add module-level preallocated buffers: `const preallocTransform = new Float32Array(12);` and (if
    the investigation above determines `readChannelView` returns a FRESH buffer each call, requiring a
    copy rather than a zero-copy view) `const preallocRegionBytes = new Uint8Array(LIVE_CHANNEL_LAYOUT
    .TOTAL_SIZE.length);`. Rewrite `parseChannelView`'s transform read: replace
    `new Float32Array(buf.slice(L.TRANSFORM.offset, L.TRANSFORM.offset + L.TRANSFORM.length))` with a
    loop writing each of the 12 floats into `preallocTransform` via
    `view.getFloat32(L.TRANSFORM.offset + i * 4, true)` (mirrors the existing per-field `DataView` read
    style already used for `networkIdLo`/`networkIdHi`/`livenessFlags` in this same function — no new
    technique, just extending the existing idiom to the transform field). Return `preallocTransform`
    (or a value object referencing it) as the `transform` field of the returned `VerifiedObjectState` —
    NOTE this means the RETURNED transform is a REUSED, MUTATED-NEXT-TICK buffer; any consumer
    (liveStore's `updateState`, 05-07 Task 2's `captureSnapshotIfNeeded`) that needs to retain the
    value across ticks MUST `.slice()` it ONCE at the point of retention (this is exactly what
    `captureSnapshotIfNeeded` already does via `state.transform.slice()` — confirm that call site
    still copies, since it is the ONE place a copy is actually needed; every other consumer that reads
    `verifiedState.transform` per-render should be fine reading the live/reused buffer since React
    re-renders on the `set()` call regardless of reference identity, per Zustand's shallow-compare
    semantics — if this causes stale-render issues in testing, fall back to a single per-tick `.slice()`
    at the `updateState` call site only, still ONE allocation per tick instead of the original TWO,
    and document the tradeoff inline). For the `updateRegion` call: after inspecting
    `channel_binding.cpp`'s ReadChannelView (per `<behavior>`), implement whichever zero-copy or
    single-reused-buffer technique applies, replacing the per-tick `new Uint8Array(buf)` call. Add a
    module-level comment explaining the investigation's conclusion and why this technique was chosen
    (so a future maintainer does not accidentally reintroduce the allocation while "simplifying" this
    file).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- useChannelReader</automated>
  </verify>
  <acceptance_criteria>
    - A test simulating 100 poll ticks (mocked `addon.readChannelView` returning a valid buffer each time) asserts the total count of `new Float32Array(...)` calls attributable to the transform-read path does not grow linearly with tick count (stays at the ~1 module-load-time allocation, not ~100).
    - The same 100-tick test asserts `new Uint8Array(...)` calls in the `updateRegion` path do not grow linearly with tick count (or, if the investigation concludes `readChannelView` itself returns a fresh buffer each call making a view-only `Uint8Array` genuinely necessary per tick, the test instead documents and asserts THAT specific, unavoidable allocation is the ONLY one — i.e. the fix eliminates every AVOIDABLE allocation, and any remaining one is justified inline).
    - `captureSnapshotIfNeeded`'s existing `.slice()` call (05-07 Task 2) still produces an independent copy — a test mutating the reused transform buffer after capture asserts the captured `cowSnapshot.transform` is unaffected.
  </acceptance_criteria>
  <done>The 60fps READ path is now allocation-honest to the same standard as the write path — ROADMAP SC1 is fully met, not silently half-met, and the one remaining unavoidable allocation (if any, per the native-layer investigation) is documented rather than assumed away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer write-path helpers (useCommandWriter, liveStore revert actions) → native writeCommand | These are the only renderer-side call sites that can influence the agent's next apply; both must always route through the same zero-alloc writer, never a second ad-hoc write path. |
| liveStore's derived "expected" state → HUD (05-11) | The HUD's guard-blocked banner must never show stale (pre-write-#1) expected bytes as if they were current — the `expectedTransform()`/`expectedScale()` selectors are the SOLE sanctioned source for this, not raw `cowSnapshot` access from 05-11. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | liveStore revertWrite/revertAll | mitigate | Both compute their target transform/scale from the toolkit's own write-log / cowSnapshot state (never from unvalidated external input) and route through the SAME guarded writeCommand path (via the rebaseline-then-revert two-step) as a normal drag write — no separate "trusted revert" bypass of the agent's read-verify guard exists; REBASELINE_GUARD itself can only adopt bytes the agent reads from the live object, never accept an arbitrary forward target (enforced agent-side, 05-03). |
| T-05-15 | DoS | detachUI stop-signal best-effort | accept | writeStop is wrapped in try/catch and detach proceeds regardless of its outcome (mirrors the existing closeChannel best-effort pattern) — a failed stop-signal degrades to the pre-existing OS-reclaim-on-process-exit behavior, not a stuck detach. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- useCommandWriter liveStore useChannelReader` — all green.
</verification>

<success_criteria>
- writeTransform/writeStop/writeRebaselineGuard are zero-allocation and reusable across the whole session.
- The read path is also zero-allocation (or documented-minimal), closing the previously half-met SC1 finding.
- liveStore correctly captures the COW snapshot (transform+scale) exactly once, exposes per-channel guard/write-log state, derives an always-current "expected" baseline for the HUD, and lets an explicit revert action un-stick a blocked guard.
- detachUI cleanly signals the agent to stop and a Detach button exists in the UI.
- Target-vs-player identity is honestly exposed on liveStore for downstream HUD copy.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-SUMMARY.md` when done
</output>
</output>
