---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 07
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - packages/renderer/src/hooks/useCommandWriter.ts
  - packages/renderer/src/state/liveStore.ts
  - packages/renderer/src/hooks/useChannelReader.ts
  - packages/renderer/src/hooks/useLiveService.ts
  - packages/renderer/src/panels/LiveInspectorPanel.tsx
  - packages/contracts/src/live-inject.ts
autonomous: true
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): restructured from 2 tasks to 3 and folds in several
# 05-REVIEWS.md findings:
# (1) Fix B (Scale guard/revert) — cowSnapshot now stores BOTH transform AND scale; writeLog entries
#     carry resultingScale; revertWrite/revertAll restore the HISTORICAL scale from the log/snapshot,
#     NOT the current live scale (the original plan's "reuse verifiedState's CURRENT scale" was wrong
#     — it would silently drop a scale revert whenever the guard needed to restore a prior pose).
# (2) Fix C ("expected bytes" half) — this is a PURE RENDERER FIX, no channel change needed (05-01
#     already added the real GUARD_ADDR field for the "real address" half). The guard-blocked banner's
#     "expected" bytes must be derived from the last SUCCESSFUL writeLog entry, not from the attach-time
#     cowSnapshot (which goes stale after write #1, since the agent's own guard baseline evolves per
#     05-03's read-back-refresh design) — implemented here as a `expectedTransform`/`expectedScale`
#     selector liveStore exposes, consumed by 05-11.
# (3) Opus MEDIUM (blocked guard blocks its own revert) — SUPERSEDED by ROUND 2 item 3 below (the
#     two-step send described here is replaced with a single coalesced call).
# (4) ROADMAP SC1 MEDIUM (Cursor) — "no allocation in the 60fps path" was only half-met: the WRITE path
#     (useCommandWriter) was already zero-alloc, but the READ path (useChannelReader's parseChannelView)
#     allocated a Uint8Array + a Float32Array-via-buf.slice() every RAF tick. FIX: new Task 3 makes the
#     read path reuse one module-level Float32Array (written element-by-element via DataView, no
#     buf.slice()) and drops the per-frame `new Uint8Array(buf)` for the HexInspector region feed in
#     favor of a single reused buffer, resolving SC1 for BOTH directions rather than leaving it silently
#     half-met.
# (5) Decision #1 (object targeting) — liveStore/contracts gain hasTarget/targetUnavailableOnBuild
#     fields decoded from the read frame's liveness bits (05-03 publishes them) so 05-10/05-11 can show
#     honest "target: <name>" / "target: none — showing player" / "target: unavailable on this build"
#     copy instead of silently treating the player as the target. (ROUND 2: this decode logic is
#     BUILD-AGNOSTIC and needed NO changes when the ROUND 2 cross-build resolver landed in 05-03 — it
#     already just reads whichever bits the agent publishes, on either build.)
# Prior plan's Task 1 (useCommandWriter) is functionally unchanged except gaining writeRebaselineGuard.

# REVISION NOTE (--reviews replan ROUND 4, 2026-07-12): folds in 05-REVIEWS.md round-3 maintainer
# decisions #1 and #5 (decisions #2/#4 land entirely agent-side in 05-03; decision #3 lands in
# 05-11/05-12).
# (1) HIGH — host cowSnapshot never re-keyed (D-03 restored, host-side half). `captureSnapshotIfNeeded`
#     only ever captured cowSnapshot ONCE per attach session (guarded by `cowSnapshot === null`),
#     against whatever object was in focus at attach — never re-captured when the agent's per-frame
#     focus resolver (05-03 ROUND 2) later resolves a DIFFERENT object. FIX: cowSnapshot now also
#     records the identity (networkId + templateName) it was captured against; captureSnapshotIfNeeded
#     re-captures (fresh transform, scale reset to null pending its own first write) whenever the
#     incoming state's identity differs from cowSnapshot's recorded identity — and resets writeLog/
#     guardState/lastDiscardedChange at the same time, since those pertain to the PRIOR focus object's
#     history and would misreport against a new one. Paired with 05-03's agent-side re-key, this
#     restores D-03's locked "captured per object (once)" model end-to-end.
# (5) Wiring gaps — MEDIUM (Cursor), APPLY ALL: (a) GUARD_ADDR (offset 392) decode was previously only
#     a hedged TODO ("thread it through from useChannelReader's decode if not already carried on
#     liveStore, see Task 3") — Task 3 now explicitly decodes it into liveStore, with a synthetic-
#     buffer test. (b) `lastDiscardedChange` was transform-only, so a scale-only gated revert disclosed
#     nothing — it is now PER-CHANNEL: `{ addr: string; transform?: {expected, got}; scale?:
#     {expected, got} } | null`. (c) Liveness bit5 `scaleGuardUnavailableOnBuild` is explicitly
#     documented (not decoded) as AGENT-ONLY telemetry this round — per the maintainer's decision, it
#     is EITHER consumed as a fourth scale sub-state OR explicitly documented as agent-only until the
#     m_scale offset handoff closes; this plan takes the SECOND option (lower-risk, avoids a UI-side
#     sub-state whose only trigger — an unconfirmed advertised offset — is not independently
#     verifiable from the renderer). (d) `detachUI()` no longer sends a single fire-and-hope
#     `writeStop` — it now RETRIES on a short interval while polling for the agent's sticky
#     GUARD_FLAG_STOPPING acknowledgement (05-01/05-03), so STOP can never be silently overtaken by a
#     later drag command on the latest-wins command slot.

# REVISION NOTE (--reviews replan, ROUND 2, 2026-07-08): folds in 05-REVIEWS.md ROUND 2 decisions 2/3.
# (1) Decision 2 (W2 fix) — decode the NEW liveness bit4 `scaleUnavailableOnBuild` (05-03 publishes it,
#     zero new channel offset — same LIVENESS field at offset 316, one more bit) into
#     `VerifiedObjectState`, alongside the existing bit2/bit3 decode. No contracts LAYOUT change.
# (2) Decision 3 (revert model — coalesce + gate-on-blocked + surface discarded change). Round-1's
#     `revertWrite`/`revertAll` sent `writeRebaselineGuard` THEN the revert write, UNCONDITIONALLY on
#     EVERY revert (Sonnet HIGH: silently discards whatever external change the guard would have
#     caught, with zero disclosure) — and as TWO separate `writeCommand` calls (Opus MEDIUM: races,
#     since the agent may observe only the later of the two on the single-slot channel). FIX (both
#     halves, per 05-03's ROUND 2 agent-side coalesce): (a) `writeTransform` gains an optional 4th
#     param (`flags`, default 0) so a caller can pass `LIVE_CMD_FLAGS.REBASELINE_GUARD` ALONGSIDE the
#     real transform/scale payload in ONE `addon.writeCommand` call — `writeRebaselineGuard` (the
#     separate rebaseline-only helper) stays available as a primitive but is no longer used by
#     revertWrite/revertAll's default path; (b) `revertWrite`/`revertAll` now check
#     `guardState.transform === 'blocked' || guardState.scale === 'blocked'` BEFORE deciding whether to
#     set the flag — when NEITHER channel is blocked, they send a normal `writeTransform(mappingName,
#     target, targetScale)` call (flags=0, no rebaseline, no wasted channel traffic); when EITHER
#     channel IS blocked, they send ONE coalesced `writeTransform(mappingName, target, targetScale,
#     LIVE_CMD_FLAGS.REBASELINE_GUARD)` call; (c) BEFORE sending either variant, liveStore computes and
#     stores a `lastDiscardedChange: { addr: string; expected: Float32Array; got: Float32Array } | null`
#     snapshot describing what external change is about to be overwritten (sourced from the SAME
#     `GUARD_ADDR`/`expectedTransform()`/`verifiedState.transform` values 05-11's guard-blocked banner
#     already displays — this task just captures a copy of them at the moment of revert, so the
#     POST-revert "reverted" banner can still name what was discarded even after the guard clears) —
#     `lastDiscardedChange` is null when the revert did not need to gate-on-blocked (nothing was
#     discarded). 05-11 Task 1 consumes `lastDiscardedChange` in the reverted-banner copy.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decision #1 (the load-bearing identity-unification fix, host-side half — the FULL unification, not
# the cheaper networkId-read alternative). ROUND 4's re-key above (identity = networkId + templateName)
# was a NARROWER fix than the agent's own re-key (05-03's `s_expectedCapturedAgainst`, keyed on the raw
# `focus` pointer) — on legacy, `networkId` is ALWAYS 0 (Fix E, LOCKED), so this plan's host-side key
# collapses to `(0, templateName)`, and two same-template objects (two rocks, two crates) share ONE
# host-side identity even though the agent correctly distinguishes them. A Revert All targeting
# object-2 can therefore force-write object-1's pose onto object-2 (fails OPEN — an ordinary WYSIWYG
# workflow, not a race) — REVIEWS.md round-4 Opus MEDIUM. The SAME single-slot design also means a
# focus-flip A->B->A silently destroys A's ORIGINAL baseline and undo history (re-capturing B overwrites
# the slot; flipping back to A re-captures its ALREADY-EDITED pose with an empty log) — REVIEWS.md
# round-4 Opus MEDIUM, NEW-ISSUE. FIX (BOTH halves, per the maintainer's Path-A decision): (1) 05-01/
# 05-03 now publish `focusToken` — the agent's own truncated `addrOf(focus)`, the SAME value
# `s_expectedCapturedAgainst` re-keys on — into the read frame EVERY tick; this plan's
# `captureSnapshotIfNeeded` now re-keys on `state.focusToken` instead of `(networkId, templateName)`,
# so host and agent can NEVER disagree about which object a baseline belongs to. (2) the single
# `cowSnapshot`/`writeLog`/`guardState`/`lastDiscardedChange` slot is REPLACED with a small
# per-identity CACHE (`identityCache: Map<focusToken, IdentitySlot>`) — a focus-flip SWAPS the active
# slot (creating one on first sight of a new token, retrieving the EXISTING one on a return visit)
# rather than discarding-and-recapturing, so a flip-back to a PREVIOUSLY-seen object restores that
# object's ORIGINAL baseline and full write-log/undo history. The public top-level `cowSnapshot`/
# `writeLog`/`guardState`/`lastDiscardedChange` fields liveStore already exposes are UNCHANGED in
# shape (05-10/05-11 need NO changes) — they now always mirror the ACTIVE identity's slot in the cache,
# kept in sync by every action that used to `set({...})` directly. Task 3 gains one new decode:
# `FOCUS_TOKEN` (05-01 ROUND 5, offset 320, part of the seqlocked read frame) into
# `VerifiedObjectState.focusToken`, the SAME cadence/mechanism as the existing hasTarget/
# targetUnavailableOnBuild/scaleUnavailableOnBuild bit decodes (zero new allocation).
#
# Also folds in 05-REVIEWS.md ROUND 4 maintainer decision #4 (minors): `lastDiscardedChange.scale`
# gains an explicit `gotVerified: boolean` field (Cursor MEDIUM — `got` could silently equal
# `expected` since no renderer-side live-scale channel field exists, weakening the "found <bytes>"
# disclosure). Rather than growing the channel AGAIN this round for a single degraded field (only
# `FOCUS_TOKEN` was authorized to grow the read frame this round, decision #1), this task now marks
# the degrade EXPLICITLY (`gotVerified: false`) so 05-11 can render honest copy instead of a value
# indistinguishable from a genuine live confirmation — the documented, minimal alternative Cursor's
# own finding offered ("source scale got from a live read, OR soften the banner copy").

# REVISION NOTE (--reviews replan ROUND 6, 2026-07-12): folds in 05-REVIEWS.md ROUND 5 maintainer
# decision "(a) structural" — closing Opus's round-5 NEW-ISSUE finding: the never-invalidated
# identityCache this plan's ROUND 5 note introduced can itself fail open on POINTER-ABA (a despawned
# object's address reallocated to a DIFFERENT object — the client can reuse a freed address for an
# unrelated instance, routine in a live world). ALL round-3/4/5 CLOSED findings (both round-4 MEDIUMs,
# the overclaim purge, the HUD name-only caveat, the 400-byte layout) stay UNCHANGED — this round is
# ADDITIVE and needs NO channel-layout growth: the cross-check below reads `templateName`/`networkId`,
# fields the read frame ALREADY decodes onto `VerifiedObjectState` every tick (05-03/Task 3, unchanged).
# (1) MEDIUM (Opus, structural close, option (a)) — on every `identityCache.get(focusToken)` HIT inside
#     `captureSnapshotIfNeeded` (the flip-back branch), cross-check the stored slot's
#     `cowSnapshot.templateName`/`cowSnapshot.networkId` against the CURRENT
#     `state.templateName`/`state.networkId` BEFORE restoring it. On a MISMATCH, the token collided with
#     a DIFFERENT object than the one that originally owned the slot (the despawn-then-realloc case) —
#     evict the stale slot from `identityCache` and fall through to the ordinary "no slot exists"
#     fresh-capture branch (a SAFE MISS), exactly as if this were a genuinely new identity. This closes
#     every CROSS-TEMPLATE pointer-ABA collision that involves a focusToken transition (the normal case —
#     see the host-symmetry fold in item (3) below) for free. The narrower SAME-template +
#     legacy-networkId≡0 + realloc-at-the-same-address residue (two same-templated objects, one
#     despawns, the other's address happens to collide) is explicitly NOT addressed by this fix — it
#     needs a generation counter/despawn signal the channel does not carry this round; it is
#     accept-watched below (T-05-46) and covered by 05-12's new UAT watch item, per the maintainer's
#     explicit "residue to accept-watch, do NOT try to fully close" instruction.
# (3) LOW folds (Opus round-6 3c-i / 3a-residual / 3c, direct-applied 2026-07-13): (a) HOST-SYMMETRY —
#     the host cross-check above runs ONLY inside the `identityChanged` (focusToken-changed) branch,
#     while the agent's mirror (05-03) runs the identity compare every tick. This is safe for every
#     interleaving THIS phase can produce: both focus resolvers fall back to `player` on a null target,
#     so a despawn→retarget ALWAYS inserts an intervening frame carrying a DIFFERENT focusToken (the
#     player's) — the exact-same-address, zero-intervening-frame case that would leave focusToken
#     numerically unchanged and skip the host cross-check is UNREACHABLE here. Documented (not
#     code-changed) per Opus 3c-i option (b); if a future resolver ever removes the player-fallback,
#     symmetrize the host to compare the active slot's stored identity every tick. (b) EVICTED-SLOT
#     HONESTY — an INACTIVE slot the user intends to return to can age out of the 64-entry LRU after 64
#     other distinct targets, silently dropping its restored undo history; this is inherent to any
#     bounded cache, 64 is generous, and it only NARROWS (never widens) the ABA window. (c) the "closes
#     every cross-template collision for free" phrasing in item (1) is scoped to focusToken-transition
#     collisions per this note; neither side can silently disagree about object identity for any
#     interleaving reachable given the player-fallback resolvers.
# (2) LOW (Opus 3d) — bounded LRU. `identityCache` was never evicted within a session (unbounded growth
#     — every stale slot a future ABA landmine, and `captureSnapshotIfNeeded`'s full-Map-clone-per-flip
#     cost grew with session length). FIX: cap `identityCache` at 64 distinct identities; when inserting
#     a NEW slot would exceed the cap, evict the LEAST-RECENTLY-ACTIVATED entry first (recency = "was
#     the ACTIVE identity, most recently," not merely "was inserted most recently"). Caps memory AND
#     narrows the residual same-template ABA window (an evicted-then-recreated slot can no longer be
#     mistaken for a stale restore, since it no longer exists to be "hit").
# (3) Agent-side mirror (05-03's own ROUND 6 note, the DUAL half of this same finding) —
#     `s_expectedCapturedAgainst`'s pointer-only compare has the IDENTICAL blind spot; 05-03 now stores
#     the captured-against templateName/networkId alongside the pointer and re-captures on EITHER a
#     pointer mismatch OR an identity mismatch, closing the fail-open on both sides, not just the host's.
# (4) LOW minors folded here (do not restate 05-11/05-12's own ROUND 6 notes, see those files): the
#     command-apply focus-skew (Opus round-5 Item 1 LOW) and the same-template-realloc ABA residue are
#     added to this plan's threat register as explicit, named accept-watch entries (T-05-45/T-05-46) —
#     never silently folded into T-05-41/42's existing CLOSED entries, which stay exactly as written.

must_haves:
  truths:
    - "writeTransform() reuses ONE pre-allocated buffer across every call for the session's lifetime — no new Float32Array/ArrayBuffer is allocated per drag-frame (LIVE-03 SC1 zero-allocation, write direction). writeTransform accepts an optional flags parameter (default 0) so a caller can coalesce a REBASELINE_GUARD directive with a real payload in ONE writeCommand call (ROUND 2 decision #3)."
    - "The READ path (useChannelReader) is ALSO zero-allocation per RAF tick — no per-frame Uint8Array/Float32Array/buf.slice() allocation (LIVE-03 SC1, read direction — REVIEWS.md MEDIUM fix, previously silently half-met)."
    - "liveStore captures a COW snapshot (transform AND scale), keyed on the AGENT-PUBLISHED focusToken (05-01/05-03 ROUND 5 read-frame field) — NEVER on (networkId, templateName), which collapses to (0, templateName) on legacy and lets two same-template objects share one identity (ROUND 5, REVIEWS.md round-4 maintainer decision #1, the load-bearing identity-unification fix — supersedes ROUND 4's networkId+templateName re-key). A grep of captureSnapshotIfNeeded's identity comparison confirms it references state.focusToken and does NOT compare networkId/templateName for identity purposes (they remain stored on cowSnapshot for DISPLAY only)."
    - "The single cowSnapshot/writeLog/guardState/lastDiscardedChange slot is REPLACED by a small per-identity CACHE (identityCache: Map<focusToken, IdentitySlot>) — a focus-flip SWAPS to the cached slot for the new focusToken (creating an empty one on first sight, reusing an EXISTING one on a return visit) rather than discarding-and-recapturing (ROUND 5, REVIEWS.md round-4 maintainer decision #1 item 2 — closes the lossy focus-flip A->B->A bug: a flip back to a PREVIOUSLY-seen object restores that object's ORIGINAL baseline and full write-log, not a fresh re-capture of its current, possibly-edited pose). The public top-level cowSnapshot/writeLog/guardState/lastDiscardedChange fields are UNCHANGED in shape — they always mirror the ACTIVE identity's cache slot, so 05-10/05-11 need no changes."
    - "A two-same-template-object test proves the identity-unification fix directly: capturing object-1 (focusToken A), writing to it, then switching focus to object-2 (focusToken B, SAME templateName/networkId as object-1) and calling revertAll() sends object-2's OWN snapshot transform/scale, NEVER object-1's — proving a Revert All can no longer force-write the wrong object's pose onto a same-template sibling (REVIEWS.md round-4 Opus MEDIUM, fails-open case, CLOSED)."
    - "A flip-A-to-B-to-A test proves the per-identity cache restores rather than re-captures: capture object-A (focusToken A), append a write-log entry, switch to object-B (focusToken B, fresh capture), switch BACK to object-A (focusToken A again) — asserts cowSnapshot/writeLog for A are the ORIGINAL values from the first capture, not a fresh re-capture of A's now-edited live pose with an empty log (REVIEWS.md round-4 Opus MEDIUM, NEW-ISSUE, CLOSED)."
    - "On every identityCache HIT (a focusToken previously seen this session), captureSnapshotIfNeeded cross-checks the stored slot's cowSnapshot.templateName/cowSnapshot.networkId against the CURRENT state.templateName/state.networkId (both already decoded onto VerifiedObjectState, zero new reads) BEFORE restoring it — on a mismatch the stale slot is EVICTED and a fresh slot is created instead (a safe miss), never a restore of a different object's baseline onto a same-address-but-different-template collision (ROUND 6, REVIEWS.md round-5 maintainer decision 'option (a) structural' — closes the pointer-ABA fail-open Opus found in the never-invalidated per-identity cache: a despawned object's address reallocated to a DIFFERENT-templated object)."
    - "identityCache is bounded to a fixed capacity (64 distinct identities) via least-recently-ACTIVE eviction — inserting a new identity beyond the cap evicts the slot that has been inactive longest, so a long session can never grow the cache (or its per-flip clone cost) unboundedly, and stale slots age out rather than persisting as permanent ABA landmines (ROUND 6, REVIEWS.md round-5 Opus LOW finding 3d)."
    - "revertWrite/revertAll restore the HISTORICAL scale associated with the target write-log entry (or the attach-time snapshot scale for revertAll), never the CURRENT live scale (REVIEWS.md Fix B)."
    - "The guard-blocked banner's 'expected' bytes are derived from the LAST SUCCESSFUL write-log entry (or the attach-time snapshot if no write has succeeded yet), never unconditionally from the attach-time snapshot alone — this stays correct after write #1 (REVIEWS.md Fix C)."
    - "revertWrite/revertAll send a SINGLE coalesced writeTransform call carrying LIVE_CMD_FLAGS.REBASELINE_GUARD together with the real revert-target transform/scale payload — ONLY when the relevant guard channel is currently 'blocked' — never as two separate writeCommand calls, and never unconditionally on every revert (REVIEWS.md ROUND 2 decision #3: gates on blocked-state, and closes the two-command race by coalescing into one call)."
    - "Before sending a gated (guard-was-blocked) revert, liveStore captures lastDiscardedChange { addr: string; transform?: { expected, got }; scale?: { expected, got } } — PER-CHANNEL (ROUND 4, REVIEWS.md round-3 maintainer decision #5: was transform-only, so a scale-only gated revert disclosed nothing) — describing the external change about to be overwritten on WHICHEVER channel(s) were actually blocked, sourced from the same GUARD_ADDR/expectedTransform()/expectedScale()/verifiedState fields the guard-blocked banner already displays — this value is exposed for 05-11's reverted-banner copy to name what was discarded, never silently dropped."
    - "The guard-refused state is decoded per-channel (transform vs scale) from the same ArrayBuffer useChannelReader already polls at 60fps — no new native export was added for either direction. scaleUnavailableOnBuild (liveness bit4) is decoded alongside hasTarget/targetUnavailableOnBuild (bit2/bit3) into VerifiedObjectState — zero new channel offset (REVIEWS.md ROUND 2 decision #2)."
    - "GUARD_ADDR (offset 396, ROUND 5 shifted from 392) is EXPLICITLY decoded by useChannelReader into liveStore (a `guardAddr: number | null` field) every poll tick — ROUND 4, REVIEWS.md round-3 maintainer decision #5: this was previously a hedged TODO in liveStore's revert-action prose ('thread it through... if not already carried'), never a concretely tasked decode step. A synthetic-buffer test (a known value, e.g. 0xDEADBEEF, at offset 396) proves the decode reads the real field, not a placeholder — this is what 05-11's guard-blocked banner AND lastDiscardedChange.addr both actually source their address from."
    - "Liveness bit5 (scaleGuardUnavailableOnBuild) is NOT decoded into VerifiedObjectState this round — it is intentionally left as AGENT-ONLY telemetry (05-03 publishes it; no renderer consumer exists yet) per the maintainer's explicit 'OR document as agent-only until the offset handoff closes' option (REVIEWS.md round-3 maintainer decision #5) — a deliberate, documented scope boundary, not an oversight."
    - "hasTarget/targetUnavailableOnBuild are decoded from the read frame's liveness bits and exposed on liveStore, so downstream HUD copy can honestly state what object is being moved on EITHER build (05-CONTEXT decision #1, ROUND 2 cross-build — this decode logic itself needed no change, it is build-agnostic by construction)."
    - "A detach/disconnect control exists in LiveInspectorPanel that calls the existing detachUI() export (D-04 item 2 — previously noted as landed logic with no button)."
    - "detachUI() RESENDS a STOP_REQUESTED command on a short interval, polling the agent's sticky GUARD_FLAG_STOPPING acknowledgement bit (05-01/05-03), before closing the channel — a single fire-and-hope writeStop is NOT sufficient, since a later drag command can overtake STOP on the single latest-wins command slot before the agent's next poll (ROUND 4, REVIEWS.md round-3 maintainer decision #5); the retry-loop is bounded by a timeout and proceeds to close the channel regardless once it expires (fails safe — the OS reclaims resources on process exit either way, matching D-04 item 1's host-side half)."
  artifacts:
    - path: "packages/renderer/src/hooks/useCommandWriter.ts"
      provides: "writeTransform(mappingName, transform12, scale3, flags=0) imperative export, zero-alloc, latest-wins, flags param for coalesced REBASELINE_GUARD; writeStop(mappingName); writeRebaselineGuard(mappingName) retained as a standalone primitive"
      contains: "flags"
    - path: "packages/renderer/src/state/liveStore.ts"
      provides: "identityCache: Map<focusToken, IdentitySlot> (ROUND 5, the load-bearing identity-unification cache — replaces the single ROUND-4 slot); public cowSnapshot{transform,scale,networkId,templateName}/writeLog/guardState/lastDiscardedChange fields UNCHANGED IN SHAPE, now mirroring the ACTIVE identity's cache slot; guardAddr (explicit GUARD_ADDR decode, ROUND 4); scaleUnavailableOnBuild field; captureSnapshotIfNeeded (ROUND 5: re-keys on state.focusToken, swaps/creates the identity-cache slot; ROUND 6: cross-checks templateName/networkId on every HIT before swapping — a mismatch evicts + recreates instead of restoring — and evicts the least-recently-active slot once the cache would exceed a 64-identity cap)/appendWriteLog/revertWrite/revertAll actions (gate-on-blocked + coalesced single-call, now persisting into the active cache slot), hasTarget/targetUnavailableOnBuild fields, expectedTransform()/expectedScale() selectors for the HUD banner"
      contains: "identityCache"
  key_links:
    - from: "packages/renderer/src/hooks/useCommandWriter.ts"
      to: "packages/live-inject (native addon)"
      via: "addon.writeCommand(mappingName, transformBuf, scaleBuf, flags)"
      pattern: "writeCommand"
    - from: "packages/renderer/src/state/liveStore.ts"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "revertAll() calls writeTransform(mappingName, snapshotTransform, snapshotScale, guardIsBlocked ? LIVE_CMD_FLAGS.REBASELINE_GUARD : 0) — ONE coalesced call, never two"
      pattern: "REBASELINE_GUARD"
    - from: "packages/renderer/src/hooks/useLiveService.ts"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "detachUI() RETRIES a STOP_REQUESTED command on a short interval, polling the agent's sticky GUARD_FLAG_STOPPING acknowledgement, before closeChannel (ROUND 4)"
      pattern: "GUARD_FLAG_STOPPING"
    - from: "packages/renderer/src/hooks/useChannelReader.ts"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "state.focusToken (05-01/05-03 ROUND 5 read-frame field, decoded by Task 3) drives captureSnapshotIfNeeded's identity-cache key, NOT networkId/templateName (ROUND 5, REVIEWS.md round-4 maintainer decision #1)"
      pattern: "focusToken"
---

<objective>
Build the renderer-side write AND read path: a zero-allocation imperative command writer (now
supporting a coalesced REBASELINE_GUARD-plus-payload call), a zero-allocation channel READER (closing
the half-met SC1 finding), the liveStore extensions for the COW snapshot / write-log / per-channel
guard state / honest target-vs-player identity / scaleUnavailableOnBuild / gate-on-blocked coalesced
revert with discarded-change disclosure (D-03, Fix B, Fix C, decision #1, ROUND 2 decisions #2/#3), and
the clean-stop wiring for detachUI plus its missing UI control (D-04 items 1-2).

Purpose: this is the last plumbing layer before the actual gizmo/HUD components (05-10, 05-11) can
exist — it turns the native writeCommand export (05-04) and the agent's guarded apply (05-03) into a
renderer-consumable, testable, ALLOCATION-HONEST API surface, and gives the HUD the real data it needs
to show correct expected/target/scale-availability information and an honest account of what a revert
discarded, instead of deriving any of it from stale, absent, or silently-overwritten state.

Output: `useCommandWriter.ts` (new, now with a `flags` param on `writeTransform` and a retained
`writeRebaselineGuard` primitive), extended `liveStore.ts`/`useChannelReader.ts`/`useLiveService.ts`/
`contracts/live-inject.ts`, and a detach button in `LiveInspectorPanel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-04-PLAN.md
</context>

<interfaces>
Existing useChannelReader.ts poll shape (rAF loop, seqlock parse, VERIFIED against current source this
session — note the two allocation sites this plan's Task 3 removes):
```ts
function parseChannelView(buf: ArrayBuffer): VerifiedObjectState | null {
  const view = new DataView(buf);
  const seq1 = view.getUint32(L.SEQ_COUNTER.offset, true);
  if ((seq1 & 1) !== 0) return null;
  const transform = new Float32Array(buf.slice(L.TRANSFORM.offset, L.TRANSFORM.offset + L.TRANSFORM.length)); // <-- allocates every call
  // ... networkId, templateName, liveness ...
  const seq2 = view.getUint32(L.SEQ_COUNTER.offset, true);
  if (seq1 !== seq2) return null;
  return { networkId, templateName, transform, playerAlive };
}
export function useChannelReader(): void {
  function poll() {
    const buf = addon.readChannelView(mappingName);
    if (buf) {
      useLiveStore.getState().updateRegion(new Uint8Array(buf)); // <-- allocates every call
      const state = parseChannelView(buf);
      if (state !== null) useLiveStore.getState().updateState(state);
    }
    rafRef.current = requestAnimationFrame(poll);
  }
}
```

Existing liveStore.ts shape (Zustand, `set({...})` per-action idiom, VERIFIED against current source
this session) — extend with new fields/actions, do not change the existing ones' signatures:
```ts
export interface LiveStore {
  status: ConnectionStatus; mode: InjectionMode; disabledReason: string | null;
  verifiedState: VerifiedObjectState | null; regionBytes: Uint8Array | null;
  beginAttach, attachComplete, attachError, setMode, updateState, updateRegion, detach
}
export const useLiveStore = create<LiveStore>((set) => ({ ... }));
```

Existing useLiveService.ts detachUI (currently only closes the channel + resets the store — this plan
adds the stop-command send BEFORE that, in the same function):
```ts
export function detachUI(): void {
  closeActiveChannel();
  useLiveStore.getState().detach();
}
```

05-04's new export: `addon.writeCommand(name: string, transform: Float32Array /* 12 */, scale:
Float32Array /* 3 */, flags: number): void`. 05-01's contract additions (ROUND 5, 400-byte
layout): `LIVE_CHANNEL_LAYOUT.FOCUS_TOKEN` (offset 320, len 4 — agent-authoritative, PART OF the
seqlocked read frame, published every tick — the load-bearing identity-unification field this plan's
Task 2 re-keys `identityCache` on), `.GUARD_STATUS` (offset 392, len 4, ROUND 5 shifted from 388),
`.GUARD_ADDR` (offset 396, len 4, ROUND 5 shifted from 392 — agent-authoritative x86 pointer, updates
only on a guard-checked apply, DISTINCT from FOCUS_TOKEN which updates every tick), `LIVE_CMD_FLAGS.STOP_REQUESTED = 0x1`,
`LIVE_CMD_FLAGS.REBASELINE_GUARD = 0x2`, `LIVE_GUARD_FLAGS.TRANSFORM_REFUSED = 0x1`,
`LIVE_GUARD_FLAGS.SCALE_REFUSED = 0x2`. 05-03's liveness bits (all decoded from the SAME EXISTING
`LIVENESS` field, offset 316 — no new offset, ever): bit2 = `hasTarget`, bit3 =
`targetUnavailableOnBuild` (REDEFINED ROUND 2 — means "resolution mechanism unresolved on this build,"
true on EITHER build now, not legacy-vs-advertised split), bit4 = `scaleUnavailableOnBuild` (ROUND 2
NEW — `isAdvertisedClient() && setScale unresolved`, computed once agent-side).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCommandWriter.ts — zero-allocation imperative write path (transform+optional-flags, scale, stop, rebaseline-guard primitive)</name>
  <files>packages/renderer/src/hooks/useCommandWriter.ts</files>
  <behavior>
    - `writeTransform(mappingName, transform: ArrayLike<number> /* 12 */, scale: ArrayLike<number> /*
      3 */, flags: number = 0)` copies the given values into ONE pre-allocated module-level
      `Float32Array(12)` / `Float32Array(3)` pair (created once, reused for the process lifetime) and
      calls `addon.writeCommand(mappingName, preallocTransform, preallocScale, flags)` synchronously —
      ROUND 2: the 4th `flags` parameter is NEW and defaults to `0` (every existing call site that
      omits it is unaffected); a caller passing `LIVE_CMD_FLAGS.REBASELINE_GUARD` here sends ONE
      coalesced command carrying BOTH the rebaseline directive AND the real payload (liveStore's Task 2
      revert actions are the only callers that ever pass a non-zero flags value).
    - Calling `writeTransform` 1000 times in a loop (with or without a flags argument) must not
      increase the count of Float32Array allocations beyond the two created at module load (assert via
      a counting mock of the addon + a spy on `Float32Array` construction, or by asserting the SAME
      array object reference is passed to `addon.writeCommand` on every call).
    - `writeStop(mappingName)` calls `addon.writeCommand(mappingName, zeroedTransform, zeroedScale,
      LIVE_CMD_FLAGS.STOP_REQUESTED)` — reuses the SAME preallocated buffers (zeroed once), does not
      allocate.
    - `writeRebaselineGuard(mappingName)` (retained as a standalone primitive — REVIEWS.md ROUND 1 Opus
      MEDIUM fix originally introduced this as the sole way to rebaseline; ROUND 2's coalesced
      `writeTransform(..., flags)` is now liveStore's preferred path for an actual revert, but this
      function stays available for any future rebaseline-only-no-payload use) calls
      `addon.writeCommand(mappingName, preallocTransform, preallocScale,
      LIVE_CMD_FLAGS.REBASELINE_GUARD)` reusing the SAME preallocated buffers (their CONTENTS are
      irrelevant for a rebaseline-only command — the agent ignores the payload when there is nothing
      meaningful in it, though as of 05-03 ROUND 2 the agent's coalesce logic will attempt to apply
      WHATEVER payload accompanies the flag, so callers of this specific primitive-only helper should
      understand it is now effectively equivalent to calling `writeTransform` with stale/zeroed buffer
      contents plus the flag — liveStore's Task 2 revert actions do NOT use this helper anymore for
      that reason, they call `writeTransform` directly with a REAL payload).
  </behavior>
  <read_first>
    - packages/renderer/src/hooks/useChannelReader.ts (full file — the require('@swg/live-inject') addon-typing convention to mirror for this file's own addon import)
    - packages/renderer/src/hooks/useLiveService.ts (full file — the "plain async functions, not a React hook" convention this file's sibling functions should follow, since writeTransform is called imperatively from drag handlers, not from a component body)
    - 05-01-PLAN.md / 05-04-PLAN.md (exact writeCommand signature and LIVE_CMD_FLAGS/LIVE_GUARD_FLAGS contract, including REBASELINE_GUARD)
    - 05-03-PLAN.md's ROUND 2 REVISION NOTE (decision #3 — the agent-side coalesce logic this task's `flags` param must be compatible with: a command carrying REBASELINE_GUARD alongside a real payload is now APPLIED, not ignored)
  </read_first>
  <action>
    Create useCommandWriter.ts as a set of plain exported functions (NOT a React hook — mirrors
    useLiveService.ts's "plain async functions so button/drag handlers can call them directly"
    convention, despite the filename's `use` prefix matching this phase's PATTERNS.md naming). At
    module scope, declare `const preallocTransform = new Float32Array(12);` and
    `const preallocScale = new Float32Array(3);` — exactly once, never inside a function. Import the
    addon the same way useChannelReader.ts does (`require('@swg/live-inject')`, typed inline).
    Implement `writeTransform(mappingName: string, transform: ArrayLike<number>, scale:
    ArrayLike<number>, flags: number = 0): void`: copy `transform`'s 12 values into
    `preallocTransform` (a simple indexed loop or `.set()`), copy `scale`'s 3 values into
    `preallocScale`, call `addon.writeCommand(mappingName, preallocTransform, preallocScale, flags)`
    — the `flags` parameter is NEW this round and simply forwarded verbatim (ROUND 2 decision #3).
    Implement `writeStop(mappingName: string): void`: zero both preallocated arrays, call
    `addon.writeCommand(mappingName, preallocTransform, preallocScale,
    LIVE_CMD_FLAGS.STOP_REQUESTED)`. Implement `writeRebaselineGuard(mappingName: string): void`
    (retained primitive, see `<behavior>`): call `addon.writeCommand(mappingName, preallocTransform,
    preallocScale, LIVE_CMD_FLAGS.REBASELINE_GUARD)` reusing whatever the buffers currently hold.
    Import `LIVE_CMD_FLAGS` from `@swg/contracts`. All functions are synchronous void calls (no
    Promise, no async) — the gizmo drag handler (05-10) calls `writeTransform` directly on every
    drag-move event with zero await overhead (flags always 0 from that call site), and liveStore's
    `revertAll`/`revertWrite` (Task 2) call `writeTransform` directly with a non-zero flags argument
    ONLY when the relevant guard channel is currently blocked.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- useCommandWriter</automated>
  </verify>
  <acceptance_criteria>
    - A test calling writeTransform 100 times (with and without an explicit flags argument) asserts the addon mock always receives the SAME Float32Array object reference (identity check, not just equal values) for both transform and scale arguments.
    - A test asserts calling `writeTransform(name, t, s)` with no 4th argument passes `flags === 0` to the addon mock (the parameter default).
    - A test asserts calling `writeTransform(name, t, s, LIVE_CMD_FLAGS.REBASELINE_GUARD)` passes that exact flags value through to the addon mock, alongside the SAME (non-zeroed) transform/scale buffers as a normal call — proving this is genuinely a single coalesced call, not a rebaseline-only stub.
    - writeStop's flags argument equals `LIVE_CMD_FLAGS.STOP_REQUESTED` exactly; writeRebaselineGuard's flags argument equals `LIVE_CMD_FLAGS.REBASELINE_GUARD` exactly (a distinct, non-zero, non-STOP_REQUESTED bit).
  </acceptance_criteria>
  <done>A synchronous, zero-allocation, latest-wins write path exists — including a `flags`-parameterized `writeTransform` that lets a single call coalesce a rebaseline directive with a real payload — that 05-10's gizmo drag handler and 05-11's numbox typed-entry / Revert ALL button can all call directly.</done>
</task>

<task type="auto">
  <name>Task 2: liveStore COW snapshot (transform+scale)/write-log/per-channel guard state + scaleUnavailableOnBuild + honest target identity + gate-on-blocked coalesced revert with discarded-change disclosure + detachUI stop-signal + detach UI control</name>
  <files>packages/renderer/src/state/liveStore.ts, packages/renderer/src/hooks/useLiveService.ts, packages/renderer/src/panels/LiveInspectorPanel.tsx, packages/contracts/src/live-inject.ts</files>
  <read_first>
    - packages/renderer/src/state/liveStore.ts (full file — extend in place, same `set({...})` per-action idiom already used for every existing action)
    - packages/renderer/src/hooks/useLiveService.ts (full file — detachUI/closeActiveChannel, the exact function this task extends)
    - packages/renderer/src/panels/LiveInspectorPanel.tsx (lines 1-90 — panel head structure, existing attach-trigger button styling to mirror for the new detach button; STATE 2/3 render branches further down the file where the new button belongs)
    - packages/contracts/src/live-inject.ts (VerifiedObjectState — add hasTarget/targetUnavailableOnBuild/scaleUnavailableOnBuild)
    - 05-REVIEWS.md Fix B (scale revert), Fix C ("expected" derivation), and 05-CONTEXT decision #1 (target identity) — this task implements all three, unchanged from round 1
    - 05-REVIEWS.md ROUND 2 decision #3 (gate-on-blocked + coalesce + surface-discarded-change) and decision #2 (scaleUnavailableOnBuild decode) — this task implements both, this round
    - 05-REVIEWS.md ROUND 4's "THE CONVERGENT FINDING" and round-3 maintainer decision #1 (the host/agent identity-key mismatch — why this task now re-keys on `state.focusToken` via a per-identity cache instead of `(networkId, templateName)`) — ROUND 5, the load-bearing fix this task implements
  </read_first>
  <action>
    In `packages/contracts/src/live-inject.ts`: add `hasTarget: boolean`,
    `targetUnavailableOnBuild: boolean`, and `scaleUnavailableOnBuild: boolean` (ROUND 2 NEW) to
    `VerifiedObjectState` (all three decoded from the read frame's `LIVENESS` bits 2/3/4 — no new
    channel offset, just new bit semantics on the existing field; update the field's doc comment to
    describe all 5 bits, and note bit3's ROUND 2 redefinition: true only when the resolution MECHANISM
    is unresolved on this build, never merely "nothing currently selected"). ALSO add `focusToken:
    number` to `VerifiedObjectState` (ROUND 5, REVIEWS.md round-4 maintainer decision #1 — the
    05-01/05-03 read-frame field at offset 320; Task 3 decodes the actual byte value, this task adds
    the TYPE field it decodes into) — document it as the load-bearing per-object identity signal this
    file's `captureSnapshotIfNeeded` re-keys on, DISTINCT from `guardAddr` (which only updates on a
    guard-checked apply, not every tick). In liveStore.ts, add to
    `LiveStore`: `cowSnapshot: { transform: Float32Array; scale: Float32Array | null; networkId:
    string; templateName: string } | null` (attach-time-OR-re-key-time transform AND scale, D-03 —
    REVISED from the original transform-only shape per REVIEWS.md Fix B; ROUND 4 adds `networkId`/
    `templateName` — the identity this snapshot was captured AGAINST, so a later focus-identity change
    can be detected and trigger a re-capture, REVIEWS.md round-3 maintainer decision #1),
    `writeLog: { id: number; atMs: number; deltaLabel: string; resultingTransform: Float32Array;
    resultingScale: Float32Array }[]` (append-only per-session log, newest entries can be reverted
    individually — REVISED to also carry `resultingScale`), `guardState: { transform: 'ok' |
    'blocked'; scale: 'ok' | 'blocked' }` (REVISED from a single combined state to per-channel,
    matching 05-01/05-03's independent guard bits), and `lastDiscardedChange: { addr: string;
    transform?: { expected: Float32Array; got: Float32Array }; scale?: { expected: Float32Array; got:
    Float32Array; gotVerified: boolean } } | null` (ROUND 5: scale gains `gotVerified` — see this task's
    revertAll/revertWrite section below for why; ROUND 2 NEW, RESHAPED ROUND 4 to be PER-CHANNEL — REVIEWS.md round-3
    maintainer decision #5: the round-2 shape was transform-only, so a scale-only gated revert
    disclosed nothing; now `transform`/`scale` are each independently present only when THAT channel
    was actually blocked at the moment of the gated revert — populated immediately before a
    gate-on-blocked revert send, see below; `null` when no revert has needed to discard anything).
    Add `identityCache: Map<number, { cowSnapshot: NonNullable<LiveStore['cowSnapshot']> | null;
    writeLog: LiveStore['writeLog']; guardState: LiveStore['guardState']; lastDiscardedChange:
    LiveStore['lastDiscardedChange'] }>` and `currentFocusToken: number | null` to `LiveStore` (ROUND
    5, REVIEWS.md round-4 maintainer decision #1 — the load-bearing identity-unification fix,
    SUPERSEDES ROUND 4's single-slot re-key below). The public top-level `cowSnapshot`/`writeLog`/
    `guardState`/`lastDiscardedChange` fields KEEP THEIR EXISTING SHAPE (05-10/05-11 need no changes)
    — they now always mirror the ACTIVE identity's slot in `identityCache`. Add actions:
    `captureSnapshotIfNeeded(state: VerifiedObjectState)` — called from the existing `updateState`
    action's body (do not add a separate call site elsewhere): compute `identityChanged =
    currentFocusToken !== null && currentFocusToken !== state.focusToken` using `state.focusToken`
    (the 05-01/05-03 ROUND 5 read-frame field, decoded onto `VerifiedObjectState` by Task 3) — NEVER
    `networkId`/`templateName` for the identity comparison (ROUND 4's `(networkId, templateName)` key
    collapsed to `(0, templateName)` on legacy, REVIEWS.md round-4 Opus MEDIUM — two same-template
    objects shared one host-side identity even though the agent's own `s_expectedCapturedAgainst`
    re-key, 05-03, correctly distinguished them; `networkId`/`templateName` remain stored on
    `cowSnapshot` for DISPLAY purposes only, never for identity). If `identityChanged` is true (or
    `currentFocusToken` is still `null`, the first-ever capture case): look up `identityCache.get(
    state.focusToken)`. If a slot EXISTS (a PREVIOUSLY-seen identity — the flip-back case), ROUND 6
    (REVIEWS.md round-5 maintainer decision "option (a) structural" — closes Opus's pointer-ABA
    NEW-ISSUE finding): FIRST cross-check the found slot's `cowSnapshot.templateName`/
    `cowSnapshot.networkId` against `state.templateName`/`state.networkId` (both already decoded onto
    `VerifiedObjectState`, no new read). If they MATCH, SWAP the public
    `cowSnapshot`/`writeLog`/`guardState`/`lastDiscardedChange` fields to that EXISTING slot's values
    VERBATIM — this restores the object's ORIGINAL baseline and full write-log/undo history rather than
    re-capturing its current, possibly-edited pose (closes REVIEWS.md round-4 Opus MEDIUM NEW-ISSUE, the
    lossy focus-flip A->B->A bug). If they DO NOT MATCH (the found slot belongs to a DIFFERENT object
    than the one now presenting this `focusToken` — the despawn-then-address-reuse case, ROUND 6),
    DELETE the stale entry from `identityCache` and fall through to the CREATE branch below exactly as
    if no slot had been found (a SAFE MISS — never restore a different object's baseline onto this
    token). If NO slot exists (a genuinely NEW identity, OR immediately after a ROUND 6 cross-check
    eviction), CREATE one using the SAME first-capture shape round 1 always used — `cowSnapshot: {
    transform: state.transform.slice(), scale: null, networkId: state.networkId, templateName:
    state.templateName }`, `writeLog: []`, `guardState: { transform: 'ok', scale: 'ok' }`,
    `lastDiscardedChange: null` (scale is still captured lazily on the FIRST scale-bearing write,
    unchanged — `cowSnapshot.scale` stays `null` until then, an explicit non-fabricated limitation, not
    a bug). ROUND 6 (Opus LOW 3d, bounded LRU): BEFORE inserting the new slot, if `identityCache.size >=
    64`, evict the LEAST-RECENTLY-ACTIVE entry first — track recency via a `lastActiveMs: number` field
    on each `IdentitySlot` (stamped whenever a slot becomes the ACTIVE slot, i.e. every time this
    function swaps to it or creates it), deleting the entry with the smallest `lastActiveMs` before
    inserting the new one, so `identityCache.size` never exceeds 64 — THEN insert the new slot into
    `identityCache` keyed on `state.focusToken`. Either branch: set `currentFocusToken =
    state.focusToken` and persist the slot (existing-restored or newly-created) back into a CLONED
    `identityCache` Map, all via a single immutable `set({...})` call (Zustand does not deep-diff Map
    contents — always construct a NEW Map reference on any cache mutation, mirroring this file's
    existing immutable-update idiom). When `state.focusToken === currentFocusToken` (the ordinary
    continuing-same-object case, the overwhelming majority of ticks), do nothing here — the active slot
    is already correct. A small `commitActiveSlot(partial: Partial<IdentitySlot>)` helper is
    RECOMMENDED: merge `partial` into `identityCache.get(currentFocusToken)`, clone `identityCache`
    with the merged slot, `set({ ...partial, identityCache: newMap })` — route `appendWriteLog`,
    `setGuardState`, and `revertWrite`/`revertAll`'s `lastDiscardedChange` assignment (below) through
    it, so the public top-level fields and the cache NEVER drift apart. `appendWriteLog(entry)` —
    pushes to writeLog, entry now REQUIRES both `resultingTransform` AND `resultingScale`.
    `setGuardState(channel: 'transform' | 'scale', state: 'ok' | 'blocked')` (per-channel setter). Add
    TWO SELECTORS (not actions, pure derivations — REVIEWS.md Fix C): `expectedTransform(): Float32Array
    | null` returns `writeLog.length > 0 ? writeLog[writeLog.length - 1].resultingTransform :
    cowSnapshot?.transform ?? null` and `expectedScale(): Float32Array | null` returns the analogous
    scale derivation — these are what 05-11's guard-blocked banner must read for its "expected" bytes,
    NEVER `cowSnapshot` directly once at least one write has succeeded (Fix C).

    ROUND 2 — rewrite `revertWrite(id: number)` and `revertAll()`'s send logic per decision #3's
    gate-on-blocked + coalesce + disclose contract: compute the target transform/scale exactly as
    round 1 did (revertWrite: the `resultingTransform`/`resultingScale` of the entry immediately
    BEFORE the given id, or `cowSnapshot` if reverting the first entry; revertAll:
    `cowSnapshot.transform`/`cowSnapshot.scale`, skipping the scale half if `cowSnapshot.scale` is
    still null). BEFORE sending, check `const transformBlocked = guardState.transform === 'blocked';`
    and `const scaleBlocked = guardState.scale === 'blocked';` INDEPENDENTLY (ROUND 4, REVIEWS.md
    round-3 maintainer decision #5 — lastDiscardedChange is now PER-CHANNEL, so each channel's
    discarded-value capture is independent, not folded into one combined `isBlocked` boolean). If
    EITHER is true: set `lastDiscardedChange` to `{ addr: formatAddr(liveStore.guardAddr) /* ROUND 4:
    guardAddr is now EXPLICITLY decoded onto liveStore by Task 3 — no hedge, no "if not already
    carried" fallback */, transform: transformBlocked ? { expected: expectedTransform() ??
    cowSnapshot!.transform, got: verifiedState!.transform.slice() } : undefined, scale: scaleBlocked ?
    { expected: expectedScale() ?? cowSnapshot!.scale!, got: expectedScale() ?? cowSnapshot!.scale!,
    gotVerified: false } : undefined }` — ROUND 5 (REVIEWS.md round-4 maintainer decision #4, Cursor
    MEDIUM): the channel carries NO live-scale read frame field this round (unlike transform, which the
    renderer can always read fresh via `verifiedState.transform`), so `got` cannot be sourced from an
    independently-verified live sample without a further channel-layout growth this round explicitly
    did not budget (only `FOCUS_TOKEN` was authorized to grow the read frame, decision #1). Rather than
    silently reusing `expected` as if it were a confirmed live read (the round-4 finding's exact
    complaint — `got` could equal `expected` and quietly weaken the "found <bytes>" disclosure), this
    is now an EXPLICIT, TYPED degrade: `gotVerified: false` marks the value as NOT independently
    confirmed. `lastDiscardedChange`'s `scale` shape is therefore `{ expected: Float32Array; got:
    Float32Array; gotVerified: boolean }` (transform's shape is unchanged — `gotVerified` is implicitly
    `true` there, since `verifiedState.transform` IS a genuine live read every tick). 05-11's Task 1
    consumes `gotVerified` to render honest copy (a caveat, not a fabricated confirmation) whenever
    `false` (a per-channel snapshot of what each blocked guard was comparing at the
    moment of revert — this is what the reverted-banner will name as "discarded", per channel), THEN
    call `writeTransform(mappingName, targetTransform, targetScale,
    LIVE_CMD_FLAGS.REBASELINE_GUARD)` — ONE coalesced call (import `writeTransform` directly, not
    `writeRebaselineGuard` — the two-call sequence from round 1 is REMOVED). If NEITHER is true: set
    `lastDiscardedChange: null` (nothing was discarded) and call `writeTransform(mappingName,
    targetTransform, targetScale)` (flags omitted/0 — a normal, non-rebaselining revert; this also
    avoids sending an unnecessary REBASELINE_GUARD on every ordinary revert, per decision #3's second
    half). Reset `cowSnapshot`/`writeLog`/`guardState`/`lastDiscardedChange` to their initial values,
    AND reset `identityCache` to a fresh empty `Map()` and `currentFocusToken` to `null` (ROUND 5 —
    the per-identity cache is scoped to ONE attach session; a fresh attach starts a brand-new cache, it
    does not persist identities across detach/reattach cycles, matching D-03's model), inside the
    existing `detach()` action (extend it, do not replace) — `guardState` resets to `{ transform:
    'ok', scale: 'ok' }`, `lastDiscardedChange` resets to `null`.

    In useChannelReader.ts's `parseChannelView` (or a small sibling decode step called right after it
    — Task 3 owns the zero-alloc rewrite of this file AND the EXPLICIT `LIVE_CHANNEL_LAYOUT.GUARD_ADDR`
    decode into `liveStore.guardAddr` per this plan's ROUND 4 revision note, closing the round-3 hedge
    — this task just adds the NEW guard-state field reads to whatever shape Task 3 produces), also
    read `LIVE_CHANNEL_LAYOUT.GUARD_STATUS` via `view.getUint32(offset, true)` and call
    `useLiveStore.getState().setGuardState('transform',
    (guardStatus & LIVE_GUARD_FLAGS.TRANSFORM_REFUSED) !== 0 ? 'blocked' : 'ok')` and
    `setGuardState('scale', (guardStatus & LIVE_GUARD_FLAGS.SCALE_REFUSED) !== 0 ? 'blocked' : 'ok')`
    — this is the renderer-side half of the D-03 guard observability, requiring no new native export.
    Decode `hasTarget`/`targetUnavailableOnBuild`/`scaleUnavailableOnBuild` (ROUND 2 NEW — bit4) from
    the liveness bits into the `VerifiedObjectState` object `parseChannelView` returns (bit2/bit3/bit4
    of the existing LIVENESS field). Extend `detachUI()` in useLiveService.ts: before
    `closeActiveChannel()`, if `useLiveStore.getState().status.kind === 'attached'`, RESEND
    `writeStop(status.mappingName)` (import from useCommandWriter.ts) on a short interval (e.g. every
    ~33ms, roughly 2x the agent's 16ms poll period) WHILE polling `readChannelView`'s decoded
    `GUARD_STATUS` for the `GUARD_FLAG_STOPPING` bit (05-01/05-03) — ROUND 4, REVIEWS.md round-3
    maintainer decision #5: a SINGLE fire-and-hope `writeStop` is not sufficient, since a later drag
    command can overtake STOP on the single latest-wins command slot before the agent's next poll,
    silently starving the stop signal. Bound the retry loop with a timeout (e.g. ~500ms-1s / ~15-30
    retries) and proceed to `closeActiveChannel()` regardless once EITHER the STOPPING bit is observed
    OR the timeout expires (fails safe either way — the OS reclaims the agent's resources on process
    exit even if the acknowledgement is never observed) — this closes the class of bug where a late
    drag command overtakes a single fire-and-hope STOP on the latest-wins command slot and the agent's
    poll loop never observes STOP_REQUESTED (D-04 item 1's host-side trigger — the agent-side
    handling, including the STOPPING acknowledgement, was completed in 05-03). Wrap the retry loop in
    a try/catch mirroring the existing `closeChannel` best-effort pattern (channel may already be
    gone). In LiveInspectorPanel.tsx: add a `Detach`
    button, styled consistently with the existing attach-trigger buttons (reuse the file's existing
    button style constant), visible only when `status.kind === 'attached'` (STATE 2/3), calling
    `detachUI()` on click — this closes the D-04 item 2 gap ("no such button exists yet").
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- liveStore useChannelReader</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts calling `updateState` twice with two DIFFERENT transforms but the SAME `focusToken` only sets `cowSnapshot.transform` from the FIRST call (the second call's transform must NOT overwrite an already-captured snapshot for the SAME identity).
    - ROUND 5 (REVIEWS.md round-4 maintainer decision #1, the token-key grep): grep confirms `captureSnapshotIfNeeded`/`identityChanged` compares `state.focusToken`/`currentFocusToken`, and does NOT compare `networkId`/`templateName` anywhere in the identity-comparison expression (those fields may still appear elsewhere, e.g. inside the stored `cowSnapshot` object literal, for DISPLAY purposes only).
    - A test (supersedes the ROUND 4 version) asserts calling `updateState` with a DIFFERENT `focusToken` than `currentFocusToken`, where `identityCache` has NO existing entry for the new token, CREATES a fresh slot (`cowSnapshot` from the new state's transform, `writeLog: []`, `guardState: { transform: 'ok', scale: 'ok' }`, `lastDiscardedChange: null`) — all in the SAME `captureSnapshotIfNeeded`/`updateState` call.
    - A NEW test (ROUND 5, the two-same-template-object revert test — REVIEWS.md round-4 Opus MEDIUM, fails-open case): simulate `updateState` for object-1 (`focusToken: 1`, `templateName: "rock.iff"`, `networkId: "0"`), append a write-log entry (object-1 now has an edited pose), then simulate `updateState` for object-2 (`focusToken: 2`, SAME `templateName: "rock.iff"`, SAME `networkId: "0"` — the same-template collision case), then call `revertAll()` — asserts `writeTransform` is called with OBJECT-2's OWN `cowSnapshot.transform` (captured fresh when object-2 was first seen), NEVER object-1's transform — proving a Revert All can no longer force-write the wrong object's pose onto a same-template sibling.
    - A NEW test (ROUND 5, the flip-A-to-B-to-A test — REVIEWS.md round-4 Opus MEDIUM, NEW-ISSUE): simulate `updateState` for object-A (`focusToken: 1`), append a write-log entry to A, simulate `updateState` for object-B (`focusToken: 2`, a fresh capture), THEN simulate `updateState` for object-A AGAIN (`focusToken: 1`) — asserts the restored `cowSnapshot`/`writeLog` for A are the ORIGINAL values from A's first capture (same `cowSnapshot.transform` reference/values, `writeLog` still containing A's original entry), NOT a fresh re-capture of A's current (possibly-edited) live pose with an empty `writeLog`.
    - A NEW test (ROUND 6, REVIEWS.md round-5 maintainer decision "option (a) structural" — the cross-check-recreates test): simulate `updateState` for object-1 (`focusToken: 1`, `templateName: "rock.iff"`, `networkId: "0"`), append a write-log entry, simulate `updateState` for object-2 (`focusToken: 2`, a fresh identity), THEN simulate `updateState` again with `focusToken: 1` but a DIFFERENT `templateName` (`"crate.iff"`) — the despawn-then-address-reuse case — asserts the resulting active slot's `cowSnapshot` reflects the NEW state's transform/templateName (a fresh capture) and `writeLog` is EMPTY (not object-1's original log with its prior entry), proving a cache HIT whose stored identity fields differ from the current state RECREATES rather than restores (closes the pointer-ABA fail-open, does not merely document it).
    - A NEW test (ROUND 6, the LRU-eviction test): simulate `updateState` for 65 DISTINCT `focusToken` values in sequence (each a genuinely new identity, never previously seen) — asserts `identityCache.size` never exceeds 64 at any point during the sequence, and asserts the FIRST-seen focusToken's slot is no longer present in `identityCache` once the 65th distinct identity has been captured — proving the cache is bounded via eviction, not unbounded growth (ROUND 6, Opus LOW finding 3d).
    - A test asserts `setGuardState('scale', 'blocked')` does NOT flip `guardState.transform` — the two channels are independently settable.
    - A test asserts `expectedTransform()` returns `cowSnapshot.transform` when `writeLog` is empty, and returns `writeLog[writeLog.length-1].resultingTransform` after at least one append.
    - A test with `guardState.transform === 'ok'` and `guardState.scale === 'ok'` asserts calling `revertAll()` sends `writeTransform(mappingName, ..., ..., 0)` (or with flags omitted) — a SINGLE call, no REBASILINE_GUARD flag, and `lastDiscardedChange` is set to `null`.
    - A test with `guardState.transform === 'blocked'` (and `guardState.scale === 'ok'`) asserts calling `revertAll()` sends EXACTLY ONE `writeTransform` call carrying `LIVE_CMD_FLAGS.REBASELINE_GUARD` together with the real snapshot payload (call-count assertion proving this is ONE coalesced call, not two separate `writeCommand` invocations) — AND that `lastDiscardedChange` is populated with `addr` + a `transform` sub-object (`expected`/`got`) but `scale` is `undefined` (ROUND 4 — per-channel, not a combined shape) BEFORE that call is sent.
    - A NEW test (ROUND 4, decision #5) with `guardState.scale === 'blocked'` (and `guardState.transform === 'ok'`) asserts `lastDiscardedChange.scale` is populated (`expected`/`got`) while `lastDiscardedChange.transform` is `undefined` — proving a SCALE-ONLY blocked revert discloses something, closing the round-3 gap where `lastDiscardedChange` was transform-only and a scale-only block disclosed nothing.
    - A NEW test (ROUND 5, REVIEWS.md round-4 maintainer decision #4) asserts `lastDiscardedChange.scale.gotVerified === false` on every populated scale entry (no renderer-side live scale reader exists this round) — proving the degrade is EXPLICIT and typed, not a silent `got === expected` value indistinguishable from a genuine live confirmation.
    - A test asserts `lastDiscardedChange.addr` is sourced from `liveStore.guardAddr` (the value Task 3 explicitly decodes from `GUARD_ADDR`), not a placeholder or an unset value.
    - A test asserts `revertWrite`/`revertAll` use the write-log/snapshot's HISTORICAL scale, never `verifiedState`'s current scale.
    - A test asserts `detachUI()` calls `writeStop(mappingName)` at least once before `closeChannel(mappingName)` when a session is attached, and skips `writeStop` entirely when idle (no channel to signal).
    - A NEW test (ROUND 4, decision #5) simulates `readChannelView` NEVER reporting `GUARD_FLAG_STOPPING` set and asserts `detachUI()` calls `writeStop` MORE THAN ONCE (the retry behavior) before eventually calling `closeChannel` once the bounded timeout expires — proving this is a genuine retry loop, not a single call with a misleading name. A companion test simulates `GUARD_FLAG_STOPPING` becoming set after the SECOND `writeStop` call and asserts `closeChannel` is invoked promptly after that (not waiting for the full timeout).
    - LiveInspectorPanel renders a Detach button only in the attached state (grep/test confirms conditional render on `status.kind === 'attached'`).
    - `VerifiedObjectState` (contracts) has `hasTarget`/`targetUnavailableOnBuild`/`scaleUnavailableOnBuild` boolean fields — a test asserts bit4 of a synthetic LIVENESS value decodes to `scaleUnavailableOnBuild === true`.
  </acceptance_criteria>
  <done>liveStore fully models the D-03 COW/write-log/per-channel-guard contract, now keyed on the agent-published focusToken via a small per-identity cache (ROUND 5 — never (networkId, templateName), closing the fail-open two-same-template-object revert bug and the lossy focus-flip A->B->A bug) with historically-correct scale revert, the HUD can always derive a currently-accurate "expected" baseline (Fix C), a blocked guard can always be un-stuck via ONE coalesced, race-free revert call that only fires the rebaseline flag when genuinely needed and always discloses what was discarded (ROUND 2 decision #3), target-vs-player identity and scale build-availability are honestly exposed (ROUND 2 decision #2), detachUI cleanly stops the agent, and a real Detach button exists. ROUND 6: every identityCache HIT is cross-checked against the current templateName/networkId before being trusted (a mismatch evicts and recreates rather than restores), and the cache itself is bounded via LRU eviction — closing Opus's round-5 pointer-ABA finding structurally, with the narrower same-template-realloc residue explicitly accept-watched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useChannelReader.ts — zero-allocation read path (closes the half-met SC1 finding)</name>
  <files>packages/renderer/src/hooks/useChannelReader.ts</files>
  <behavior>
    - `parseChannelView` no longer allocates a `Float32Array` via `buf.slice(...)` per call — it
      writes the 12 transform floats into ONE pre-allocated module-level `Float32Array(12)` (via
      `DataView.getFloat32` reads in a loop, or `Float32Array.set` against a `Float32Array` VIEW over
      the SAME underlying buffer when alignment allows — since the channel's `TRANSFORM` offset (4) is
      not guaranteed 4-byte-aligned relative to a fresh `ArrayBuffer` from N-API, use the safe
      per-element `DataView` read path, not an unaligned typed-array view) and returns THAT reused
      array (or a value shape referencing it) rather than a fresh allocation every tick.
    - The poll loop's `updateRegion(new Uint8Array(buf))` call (HexInspector raw-bytes feed) no longer
      allocates a new `Uint8Array` every RAF tick — reuse ONE module-level `Uint8Array` of the channel's
      fixed size (400 bytes, ROUND 5, from `LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length`) and copy into it via `.set()`
      from a view over `buf`, OR (simpler, equally zero-alloc) pass a `Uint8Array` VIEW directly (`new
      Uint8Array(buf)` is itself cheap/non-copying when `buf` is a genuinely fresh per-call ArrayBuffer
      from the native layer — verify with `addon.readChannelView`'s actual contract: if it returns the
      SAME underlying mapped buffer every call (not a fresh copy), a `Uint8Array` VIEW constructor call
      is NOT a data-copy allocation of concern the same way `buf.slice()` is; if it returns a FRESH
      ArrayBuffer each call, minimize further copies by reusing one `Uint8Array` and `.set()`-ing into
      it). Document which case applies (inspect `channel_binding.cpp`'s `ReadChannelView` to determine
      whether it returns the same backing buffer or a fresh one each call) and implement accordingly —
      the acceptance test is behavioral (call count of `new Float32Array(...)`/`new Uint8Array(...)`
      inside the hot poll path stays at the module-load-time baseline across 100 simulated frames), not
      prescriptive about which exact zero-copy technique is used.
    - A 100-tick simulated poll loop shows ZERO net-new `Float32Array`/`Uint8Array` allocations beyond
      whatever the native `readChannelView` call itself does (which this hook does not control) — test
      via a spy/counter on the relevant constructors across the 100 ticks, asserting the count stays at
      the ~1-2 module-load-time allocations.
    - `parseChannelView`'s returned `VerifiedObjectState` includes `hasTarget`/`targetUnavailableOnBuild`
      (unchanged decode, bits 2/3) AND `scaleUnavailableOnBuild` (ROUND 2 NEW, bit4) — all three read
      from the same already-decoded liveness `uint32`, adding zero allocation cost.
    - `parseChannelView`'s returned `VerifiedObjectState` ALSO includes `focusToken` (ROUND 5,
      REVIEWS.md round-4 maintainer decision #1 — the load-bearing identity-unification field, 05-01
      offset 320, PART OF the seqlocked read frame, decoded via `view.getUint32(L.FOCUS_TOKEN.offset,
      true)` the SAME way `networkIdLo`/`networkIdHi` already are — zero additional allocation, and
      NO torn-read risk since it shares the same seqlock as transform/networkId/templateName/liveness).
  </behavior>
  <read_first>
    - packages/renderer/src/hooks/useChannelReader.ts (full file — the CURRENT allocation sites this task removes: `buf.slice(...)` at the TRANSFORM read, `new Uint8Array(buf)` at the updateRegion call)
    - packages/live-inject/src/channel_binding.cpp (ReadChannelView's implementation — determine whether it returns the SAME backing ArrayBuffer/view across calls or allocates fresh each time, since that determines which zero-copy technique applies here)
    - 05-REVIEWS.md's MEDIUM finding on this exact allocation pattern (Cursor: "ROADMAP SC1 'no allocation in the 60fps path' is only half-met")
    - This plan's Task 2 output (the exact `hasTarget`/`targetUnavailableOnBuild`/`scaleUnavailableOnBuild`/`focusToken` field names this task's decode step must also populate — `focusToken` is the type Task 2 added to `VerifiedObjectState`, this task decodes the actual byte)
    - 05-01-PLAN.md's ROUND 5 revision note (`LIVE_CHANNEL_LAYOUT.FOCUS_TOKEN`, offset 320, length 4 — the shifted 400-byte layout: COMMAND_SEQ_COUNTER now 324, COMMAND_TRANSFORM 328, COMMAND_SCALE 376, COMMAND_FLAGS 388, GUARD_STATUS 392, GUARD_ADDR 396)
  </read_first>
  <action>
    Add module-level preallocated buffers: `const preallocTransform = new Float32Array(12);` and (if
    the investigation above determines `readChannelView` returns a FRESH buffer each call, requiring a
    copy rather than a zero-copy view) `const preallocRegionBytes = new Uint8Array(LIVE_CHANNEL_LAYOUT
    .TOTAL_SIZE.length);`. Rewrite `parseChannelView`'s transform read: replace
    `new Float32Array(buf.slice(L.TRANSFORM.offset, L.TRANSFORM.offset + L.TRANSFORM.length))` with a
    loop writing each of the 12 floats into `preallocTransform` via
    `view.getFloat32(L.TRANSFORM.offset + i * 4, true)` (mirrors the existing per-field `DataView` read
    style already used for `networkIdLo`/`networkIdHi`/`livenessFlags` in this same function — no new
    technique, just extending the existing idiom to the transform field). Return `preallocTransform`
    (or a value object referencing it) as the `transform` field of the returned `VerifiedObjectState` —
    NOTE this means the RETURNED transform is a REUSED, MUTATED-NEXT-TICK buffer; any consumer
    (liveStore's `updateState`, 05-07 Task 2's `captureSnapshotIfNeeded`) that needs to retain the
    value across ticks MUST `.slice()` it ONCE at the point of retention (this is exactly what
    `captureSnapshotIfNeeded` already does via `state.transform.slice()` — confirm that call site
    still copies). For the `updateRegion` call: after inspecting `channel_binding.cpp`'s
    ReadChannelView (per `<behavior>`), implement whichever zero-copy or single-reused-buffer
    technique applies, replacing the per-tick `new Uint8Array(buf)` call. Decode
    `hasTarget`/`targetUnavailableOnBuild` (bit2/bit3) AND `scaleUnavailableOnBuild` (bit4, ROUND 2
    NEW) from the already-read `livenessFlags` uint32 into the returned `VerifiedObjectState` — all
    three are `(livenessFlags & (1 << n)) !== 0` reads, zero additional allocation. ROUND 4 (REVIEWS.md
    round-3 maintainer decision #5): ALSO read `LIVE_CHANNEL_LAYOUT.GUARD_ADDR` (offset 396, ROUND 5
    shifted from 392 — 05-01's FOCUS_TOKEN insertion moved every command-region field +4 bytes) via
    `view.getUint32(L.GUARD_ADDR.offset, true)` and expose it on `useLiveStore` as a new `guardAddr:
    number | null` field (set via a new or existing store action called from this decode step,
    mirroring how `setGuardState` is already called from the guard-status decode in Task 2) — this was
    previously only a hedged TODO in Task 2's revert-action prose ("thread it through... if not
    already carried"); it is now a concretely tasked decode, since 05-11's guard-blocked banner AND
    Task 2's `lastDiscardedChange.addr` both need a REAL value, not a placeholder. ROUND 5 (REVIEWS.md
    round-4 maintainer decision #1, the load-bearing identity-unification fix): ALSO read
    `LIVE_CHANNEL_LAYOUT.FOCUS_TOKEN` (offset 320, part of the SAME seqlocked read frame as transform/
    networkId/templateName/liveness — no separate torn-read handling needed, it rides the existing
    seqlock) via `view.getUint32(L.FOCUS_TOKEN.offset, true)` and assign it directly to the
    `focusToken` field of the returned `VerifiedObjectState` (Task 2 already added this field to the
    type) — this is the value `captureSnapshotIfNeeded` (Task 2) re-keys `identityCache` on, replacing
    `(networkId, templateName)`. Do NOT decode
    liveness bit5 (`scaleGuardUnavailableOnBuild`) into `VerifiedObjectState` this round — it is
    intentionally left AGENT-ONLY telemetry (05-03 publishes it; no renderer field consumes it yet)
    per the maintainer's explicit "OR document as agent-only" option (REVIEWS.md round-3 maintainer
    decision #5) — add a one-line comment at the liveness-decode site stating this is a deliberate,
    documented scope boundary, not a missed bit. Add a module-level comment explaining the
    investigation's conclusion for the region-bytes technique and why it was chosen.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- useChannelReader</automated>
  </verify>
  <acceptance_criteria>
    - A test simulating 100 poll ticks (mocked `addon.readChannelView` returning a valid buffer each time) asserts the total count of `new Float32Array(...)` calls attributable to the transform-read path does not grow linearly with tick count (stays at the ~1 module-load-time allocation, not ~100).
    - The same 100-tick test asserts `new Uint8Array(...)` calls in the `updateRegion` path do not grow linearly with tick count (or documents the one unavoidable native-layer allocation, per the investigation).
    - `captureSnapshotIfNeeded`'s existing `.slice()` call still produces an independent copy — a test mutating the reused transform buffer after capture asserts the captured `cowSnapshot.transform` is unaffected.
    - A test with a synthetic liveness value having bit4 set asserts the returned `VerifiedObjectState.scaleUnavailableOnBuild === true`, and with bit4 clear asserts `false` — proving the ROUND 2 decode is wired, not just declared on the type.
    - A NEW test (ROUND 4, decision #5) writes a synthetic buffer with `0xDEADBEEF` at byte offset 396 (GUARD_ADDR, ROUND 5 shifted from 392) and asserts the decoded `guardAddr` on the store equals `0xDEADBEEF` — proving this is a real, wired decode of the actual field, not a hedge or a stub.
    - A NEW test (ROUND 5, REVIEWS.md round-4 maintainer decision #1) writes a synthetic buffer with a known non-zero value (e.g. `0x12345678`) at byte offset 320 (FOCUS_TOKEN) and asserts `parseChannelView`'s returned `VerifiedObjectState.focusToken === 0x12345678` — proving this is a real, wired decode, not a hedge or a stub, and that it is read from the SAME seqlocked span as the pre-existing read-frame fields (a torn-read-rejected test case covering the extended span is acceptable evidence too).
    - A test asserts `scaleGuardUnavailableOnBuild` (bit5) is NOT present as a field on the `VerifiedObjectState` this task returns (or, if the type includes it for forward-compatibility, that this task never assigns it a value derived from bit5) — confirming the deliberate agent-only scope boundary is honored, not silently expanded.
  </acceptance_criteria>
  <done>The 60fps READ path is now allocation-honest to the same standard as the write path — ROADMAP SC1 is fully met, not silently half-met — and the read path decodes all three liveness-derived build-capability/target fields, including the ROUND 2 scaleUnavailableOnBuild bit, at zero additional allocation cost. ROUND 4: GUARD_ADDR is explicitly decoded onto liveStore (no longer a hedge, ROUND 5: now at the shifted offset 396), and liveness bit5 is deliberately left undecoded, documented as agent-only telemetry. ROUND 5: FOCUS_TOKEN (offset 320) is decoded into VerifiedObjectState.focusToken every tick — the load-bearing identity-unification value Task 2's per-identity cache re-keys on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer write-path helpers (useCommandWriter, liveStore revert actions) → native writeCommand | These are the only renderer-side call sites that can influence the agent's next apply; both must always route through the same zero-alloc writer, never a second ad-hoc write path. The `flags` parameter on `writeTransform` must never be set by any caller other than liveStore's gate-on-blocked revert logic and the explicit stop/rebaseline-primitive helpers — a gizmo drag (05-10) always calls it with flags omitted/0. |
| liveStore's derived "expected" state → HUD (05-11) | The HUD's guard-blocked banner must never show stale (pre-write-#1) expected bytes as if they were current — the `expectedTransform()`/`expectedScale()` selectors are the SOLE sanctioned source for this, not raw `cowSnapshot` access from 05-11. `lastDiscardedChange` is the SOLE sanctioned source for the reverted-banner's "what was discarded" copy — never re-derived ad hoc in 05-11. |
| Focus-identity change → identityCache swap (ROUND 5, supersedes ROUND 4; ROUND 6 adds the cross-check) | A focus-flip must SWAP to the correct per-identity cache slot atomically within `captureSnapshotIfNeeded`, keyed on `state.focusToken` — NEVER `(networkId, templateName)`, which collapses to `(0, templateName)` on legacy and lets two same-template objects share one identity (REVIEWS.md round-4 Opus MEDIUM, fails-open). A partial swap (e.g. restoring `cowSnapshot` from the cache but leaving `writeLog`/`guardState` from the PRIOR identity) would let a revert action apply the wrong object's write-log history — `commitActiveSlot`/the swap logic must update all four fields (cowSnapshot, writeLog, guardState, lastDiscardedChange) together, from the SAME cache entry, in one `set({...})` call. ROUND 6: a cache HIT must never be trusted blindly — the stored slot's templateName/networkId are cross-checked against the current state's before the swap is allowed to proceed, since a `focusToken` match alone does not prove the same object is still at that address (pointer-ABA). |
| Host/agent identity-key agreement (ROUND 5) | The host's `identityCache` key (`state.focusToken`, decoded from the read frame) and the agent's own guard-baseline re-key (`s_expectedCapturedAgainst`, 05-03) MUST be computed from the SAME underlying pointer truncation — if the host ever re-derives an identity key from a DIFFERENT signal (e.g. reverting to networkId/templateName), the two components can disagree again about which object a baseline belongs to, reopening REVIEWS.md round-4's Opus MEDIUM finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | liveStore revertWrite/revertAll | mitigate | Both compute their target transform/scale from the toolkit's own write-log / cowSnapshot state (never from unvalidated external input) and route through the SAME guarded writeCommand path as a normal drag write — no separate "trusted revert" bypass of the agent's read-verify guard exists; the coalesced REBASELINE_GUARD-plus-payload call can only ever carry the toolkit's OWN known revert target, never accept an arbitrary forward target (enforced agent-side, 05-03 ROUND 2). |
| T-05-41 | Tampering | liveStore identityCache re-key source | mitigate | ROUND 5 (REVIEWS.md round-4 maintainer decision #1, the load-bearing identity-unification fix): `captureSnapshotIfNeeded` re-keys on `state.focusToken` (the agent-published, per-tick truncated `addrOf(focus)`), NEVER `(networkId, templateName)` — closing the fail-open two-same-template-object revert bug (a Revert All force-writing object-1's pose onto a same-template object-2) that the coarser ROUND 4 key produced on legacy, where `networkId` is always 0 (Fix E). Verified by a token-key grep (the identity comparison references `focusToken`, never `networkId`/`templateName`) plus a dedicated two-same-template-object revert test. |
| T-05-42 | Repudiation (data loss) | liveStore single-slot focus-flip (ROUND 4, CLOSED this round) | mitigate | ROUND 5: the single `cowSnapshot`/`writeLog`/`guardState`/`lastDiscardedChange` slot is replaced by a small per-identity cache (`identityCache: Map<focusToken, IdentitySlot>`) — a focus-flip A->B->A now SWAPS to A's cached slot rather than re-capturing A's current (edited) pose with an empty log, closing REVIEWS.md round-4 Opus MEDIUM's NEW-ISSUE finding. Verified by a dedicated flip-A-to-B-to-A test asserting A's ORIGINAL baseline and write-log are restored, not re-captured. |
| T-05-15 | DoS | detachUI stop-signal best-effort | accept | ROUND 4 (REVIEWS.md round-3 maintainer decision #5): writeStop is now RESENT on a bounded retry loop (polling the agent's sticky GUARD_FLAG_STOPPING acknowledgement) rather than sent once — closing the class of bug where a later drag command overtakes a single STOP on the latest-wins command slot. The retry loop is itself wrapped in try/catch and detach proceeds to close the channel regardless of the outcome once the bounded timeout expires (mirrors the existing closeChannel best-effort pattern) — a failed/unacknowledged stop-signal still degrades to the pre-existing OS-reclaim-on-process-exit behavior, not a stuck detach. |
| T-05-35 | Repudiation | Gate-on-blocked revert silently overwriting an external change | mitigate | `lastDiscardedChange` is populated with the real addr/expected/got values BEFORE the coalesced revert call is sent, whenever the revert needed to gate-on-blocked — 05-11's reverted banner consumes this to name what was discarded, closing the ROUND 1 Sonnet HIGH "silent overwrite" UX gap without reintroducing a force-write path (ROUND 2 decision #3). |
| T-05-45 | Tampering | liveStore identityCache cross-template pointer-ABA | mitigate | ROUND 6 (REVIEWS.md round-5 maintainer decision "option (a) structural"): every `identityCache.get(focusToken)` HIT cross-checks the stored slot's `templateName`/`networkId` against the CURRENT read-frame state's before restoring it — a mismatch evicts the stale slot and falls through to a fresh capture (a safe miss) instead of restoring a different, despawned object's baseline onto a newly-spawned, differently-templated object that happens to have been allocated at the same address. Closes the CROSS-TEMPLATE case of Opus's round-5 pointer-ABA finding structurally, verified by the cross-check-recreates test. The cache is additionally bounded to 64 identities via least-recently-active LRU eviction, capping both memory growth and the residual ABA window (Opus LOW 3d), verified by the LRU-eviction test. |
| T-05-46 | Tampering | liveStore identityCache same-template-realloc residue | accept (watched) | The narrower residue — same template AND legacy networkId≡0 AND a realloc landing at the EXACT same address as a still-cached, despawned sibling — still collides, since T-05-45's cross-check compares templateName/networkId, which are IDENTICAL in this specific case, and is explicitly NOT closed by ROUND 6's fix. Needs a generation counter or an explicit despawn signal the channel does not carry this round; accepted per the maintainer's explicit "residue to accept-watch, do NOT try to fully close" instruction (REVIEWS.md round-5). Covered by 05-12's despawn-then-retarget UAT watch item (ROUND 6). |
| T-05-47 | Tampering (race) | Command-slot focus-skew (Revert-All-then-retarget) | accept (watched) | The command slot carries no identity — a retarget landing in the ~1-2 frame gap between sending a Revert-ALL command and the agent's `applyWrite` for that command writes the snapshot pose onto whatever `focus` resolves that tick; the guard does not catch this (it checks external tamper, not intended-vs-current identity). A deterministic bug pre-ROUND-5, now a tight race; accepted for a single-user tool per REVIEWS.md round-5 Opus Item 1 LOW — a principled close would add a command-slot `expectedFocusToken` echo field, out of scope this round since no channel growth was budgeted for it. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- useCommandWriter liveStore useChannelReader` — all green.
</verification>

<success_criteria>
- writeTransform/writeStop/writeRebaselineGuard are zero-allocation and reusable across the whole session; writeTransform's new flags parameter enables a single coalesced call.
- The read path is also zero-allocation (or documented-minimal), closing the previously half-met SC1 finding, and decodes the ROUND 2 scaleUnavailableOnBuild bit alongside the existing target bits.
- liveStore correctly captures the COW snapshot (transform+scale) into a small PER-IDENTITY CACHE, keyed on the agent-published `focusToken` — NEVER `(networkId, templateName)` — so a flip to a same-template object never force-writes the wrong object's pose, and a flip BACK to a previously-seen object restores its ORIGINAL baseline and write-log rather than re-capturing (ROUND 5, REVIEWS.md round-4 maintainer decision #1, supersedes ROUND 4's narrower re-key). liveStore exposes per-channel guard/write-log state, derives an always-current "expected" baseline for the HUD, and lets an explicit revert action un-stick a blocked guard via ONE race-free coalesced call that ONLY fires when the guard is actually blocked and ALWAYS discloses what was discarded, PER CHANNEL (ROUND 4).
- detachUI cleanly signals the agent to stop via a BOUNDED RETRY loop that cannot be silently overtaken by a later drag command on the latest-wins command slot (ROUND 4), and a Detach button exists in the UI.
- Target-vs-player identity and scale build-availability are honestly exposed on liveStore for downstream HUD copy.
- GUARD_ADDR is explicitly decoded onto liveStore (no hedge, ROUND 5: now at the shifted offset 396); FOCUS_TOKEN is explicitly decoded onto `VerifiedObjectState.focusToken` every tick (ROUND 5, offset 320 — the load-bearing identity-unification value); liveness bit5 is deliberately left undecoded and documented as agent-only telemetry this round (ROUND 4).
- A two-same-template-object revert test and a flip-A-to-B-to-A test both pass, structurally proving REVIEWS.md round-4's Opus MEDIUM findings are closed, not just documented (ROUND 5).
- Every identityCache HIT is cross-checked against the current templateName/networkId before being trusted (a mismatch evicts and recreates rather than restores), and the cache is bounded to 64 identities via LRU eviction — closing Opus's round-5 pointer-ABA finding for the cross-template case, with the narrower same-template-realloc residue explicitly accept-watched, not silently ignored (ROUND 6, REVIEWS.md round-5 maintainer decision "option (a) structural").
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-SUMMARY.md` when done
</output>
</output>
</output>
