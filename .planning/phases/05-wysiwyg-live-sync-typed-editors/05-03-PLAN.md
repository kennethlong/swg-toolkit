---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/live-inject/agent/rva_table.cpp
  - packages/live-inject/agent/agent_main.cpp
  - packages/live-inject/agent/write.cpp
  - packages/live-inject/agent/write.h
autonomous: true
requirements: [LIVE-03]
user_setup:
  - service: swg-client-v2 (maintainer's advertised client build)
    why: "D-09 requires object::setScale to be advertised via GetEngineHookPoints for Scale writes to work on the advertised target. Without this row, the agent gracefully degrades (Scale disabled-with-reason on advertised) instead of crashing (REVIEWS.md BLOCKER fix A) — this is now a documented KNOWN GAP (05-CONTEXT decision #2), not a phase-completion blocker."
    dashboard_config:
      - task: "Add an 'object::setScale' row to s_engineHookPoints[] in engine_advertise.cpp, then rebuild and redeploy the swg-client-v2 client binary to enable Scale writes on the advertised target. Optional — 05-12's UAT passes without this, recording it as a known follow-up per 05-CONTEXT decision #2."
        location: "D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp"

# REVISION NOTE (--reviews replan, 2026-07-08): folds in the review round's BLOCKER + 4 of the 5 HIGH
# findings (05-REVIEWS.md), plus 05-CONTEXT decision #1 (object targeting, new scope this session).
# (A) BLOCKER — setScale crashes the advertised client on the FIRST write of any kind. Root cause:
#     resolve() leaves a slot untouched on a name-miss, and the original Task 1 seeded setScale with
#     the legacy RVA unconditionally (non-null, so applyWrite's old "call both, unconditionally, never
#     one without the other" rule called a stale absolute address on the advertised build). FIX: seed
#     setScale = nullptr at declare time; apply the legacy RVA to it ONLY when resolveFromExe() has
#     determined the client is NOT advertised (a new one-time post-resolve step, agent_main.cpp);
#     applyWrite calls setScale ONLY if the resolved pointer is non-null (Task 2's old "never one
#     without the other" acceptance criterion is RELAXED to "never call an unresolved setter").
# (E) HIGH — legacy networkId `playerCreature + 1432` is the WRONG field. Verified independently
#     against BOTH the review's Utinni citation (game.cpp:714, object.h:80) AND this session's own
#     re-read of the SAME file: +1432 is `Game::getPlayerLookAtTargetObjectNetworkId()`'s slot — the
#     player's CURRENT TARGET, not the object's own networkId. FIX: DROP the legacy-networkId
#     implementation entirely (D-04 item 3 stays UNAVAILABLE on legacy, not wrong) — but see (New
#     scope) below, this SAME offset is the correct, grounded mechanism for object-targeting.
# (D) HIGH — guard false-fails on non-world-cell (interior) objects after write 2+ (float-rounded
#     read-back on the objectToCell path, Object.cpp:1460-1470). FIX: after a successful apply, set
#     s_expectedTransform from a FRESH getTransform_o2w read-back (what was actually stored), not from
#     cmd.transform.
# (B) HIGH — Scale had no guard/revert. FIX: write.h/.cpp (05-01) now splits the guard into
#     independent transformPassed/scalePassed; this plan wires BOTH into agent_main.cpp's apply step,
#     each gated on its own resolved-fn-pointer AND its own guard bit.
# (F) HIGH (Cursor) — STOP_REQUESTED starved when player==nullptr (agent_main.cpp's existing
#     `if (!player) { Sleep(100); continue; }` runs BEFORE any command read). FIX: read the command
#     slot (incl. STOP_REQUESTED) at the TRUE TOP of the loop, before the player-null continue.
# Opus MEDIUM — a blocked guard also blocks its own revert. FIX: agent honors CMD_FLAG_REBASELINE_GUARD
#     (05-01) by re-baselining s_expectedTransform/s_expectedScale to the CURRENT live bytes it reads
#     this same iteration, applying nothing — the renderer (05-07) then sends the actual revert target
#     as a normal follow-up command, which now passes the guard.
# (New scope — 05-CONTEXT decision #1, object targeting) The agent poll loop previously resolved ONLY
#     `getPlayer()` — a gizmo drag on any non-player mesh silently moved the player's own avatar,
#     invisibly. Source-grounded fix (Utinni game.cpp:704-746, network.cpp:39-85, object.cpp:260-268 —
#     verified this session, NOT carried from review prose): on the LEGACY (SWGEmu) path, resolve the
#     player's current in-game TARGET via `getPlayerCreatureObject()+1432` (a CachedNetworkId pointer
#     embedded in the CreatureObject struct — Utinni game.cpp:733) passed to `cachedNetworkIdGetObject`
#     (RVA 0x00B30160, __thiscall — Utinni network.cpp:39-42,74-85); this IS the correct, grounded use
#     of the +1432 offset the original 05-03 misapplied as "networkId". On the ADVERTISED
#     (swg-client-v2) path, Utinni's OWN reference code gates this off entirely
#     (`getPlayerLookAtTargetObjectNetworkId` returns 0 when `isAdvertisedClient()`, game.cpp:721-724)
#     with a comment noting "an advertised lookAt-target accessor row is the follow-on that lights this
#     up for real" — i.e. even the reference project does not have this yet. This plan does NOT
#     fabricate an advertised mechanism; it resolves the target ONLY on legacy, and on advertised
#     honestly falls back to the player (liveness bit3 = targetUnavailableOnBuild, surfaced in the HUD
#     by 05-10/05-11 — never silently pretending the player IS the target).

must_haves:
  truths:
    - "The agent resolves a 'focus object' each frame: the player's current in-game TARGET when one is set and resolvable (legacy SWGEmu only, via the grounded getPlayerCreatureObject()+1432 -> cachedNetworkIdGetObject chain), falling back to the player object otherwise — the read frame's transform/networkId/templateName and every guarded write operate on this focus object, not unconditionally the player (05-CONTEXT decision #1)."
    - "On the advertised (swg-client-v2) build, target resolution is honestly reported as unavailable (liveness bit3) rather than silently substituting the player — this mirrors Utinni's own documented gap, not a toolkit shortcut."
    - "The agent poll loop applies a pending write command (Move/Rotate via setTransform_o2w, Scale via setScale) ONLY when its OWN independent guard bit passes AND its resolved fn pointer is non-null; a write is never attempted through an unresolved setter (REVIEWS.md BLOCKER fix A)."
    - "setScale is seeded nullptr and is set to the legacy RVA ONLY when the client is confirmed non-advertised (post-resolveFromExe) — it can never be a stale absolute address on the advertised build (REVIEWS.md BLOCKER fix A)."
    - "After a successful transform apply, the guard's expected-transform baseline is refreshed from a FRESH getTransform_o2w read-back, not from the command's raw target — this survives non-world-cell (interior) objects' float-rounded storage without false-failing write N+1 (REVIEWS.md Fix D)."
    - "CMD_FLAG_REBASELINE_GUARD re-baselines the guard to current live bytes without applying any write — a blocked guard can always be un-stuck by the user's own explicit revert action, never by a silent forward force-write (REVIEWS.md Opus MEDIUM fix)."
    - "The agent stops its poll loop cleanly (no leaked thread) when the command slot's STOP_REQUESTED flag is set — checked at the TRUE TOP of every loop iteration, before the player-null early-continue (D-04 item 1, REVIEWS.md Fix F)."
    - "Legacy (SWGEmu) networkId remains UNAVAILABLE (0, sentinel not-applicable) rather than reading the wrong field — the +1432 offset is used ONLY for target resolution, never re-purposed as a networkId (REVIEWS.md Fix E; D-04 item 3 is explicitly NOT closed on legacy, corrected from the original plan)."
  artifacts:
    - path: "packages/live-inject/agent/rva_table.cpp"
      provides: "setTransform_o2w write-slot (RVA 0x00B22CC0) + setScale slot (null-seeded, legacy-only-conditionally-set to RVA 0x00B23A10) + advertised binding rows + getPlayerCreatureObject (RVA 0x004251D0) + cachedNetworkIdGetObject (RVA 0x00B30160) for target resolution"
      contains: "getPlayerCreatureObject"
    - path: "packages/live-inject/agent/agent_main.cpp"
      provides: "60fps poll loop: top-of-loop stop-signal check, target-or-player focus resolution, independently-guarded Move/Rotate/Scale apply, read-back-refreshed guard baseline, REBASELINE_GUARD handling"
      contains: "channelReadCommand"
  key_links:
    - from: "packages/live-inject/agent/agent_main.cpp"
      to: "packages/live-inject/agent/write.cpp"
      via: "checkWriteGuard(currentXform, s_expectedTransform, currentScale, s_expectedScale) called each iteration a new command is seen"
      pattern: "checkWriteGuard"
    - from: "packages/live-inject/agent/rva_table.cpp"
      to: "engine_advertise.cpp (../swg-client-v2, external repo)"
      via: "advertised name \"object::setScale\" — must be added upstream before it resolves non-null on the advertised path (now optional/known-gap, not a phase blocker)"
      pattern: "object::setScale"
---

<objective>
Wire the guarded write path into the agent's poll loop: resolve the two new setter endpoints
(`setTransform_o2w` write-variant, `setScale` — the latter NULL-SAFE per the BLOCKER fix), consume the
command slot from 05-01 each frame, gate every apply through the now-independent transform/scale guard,
resolve a target-or-player "focus object" so the write moves what the player has targeted rather than
always the player's own avatar (05-CONTEXT decision #1), add the D-04 agent-lifecycle debt (clean
stop-signal read at the true top of the loop, REBASELINE_GUARD handling), and raise the poll rate to
60fps.

Purpose: this is the load-bearing agent-side half of LIVE-03 — the point where "drag a gizmo" becomes
"the object actually moves in the running client," gated by the fail-closed guard from 05-01 (SC #2),
covering Move/Rotate/Scale on both in-scope targets per D-09, and — per this review round — doing so
WITHOUT crashing the advertised client and WITHOUT silently moving the wrong object.

Output: extended `rva_table.cpp` + `agent_main.cpp` + a completed `applyWrite` in `write.cpp`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
@D:/Code/Utinni/UtinniCore/swg/object/object.cpp
@D:/Code/Utinni/UtinniCore/swg/game/game.cpp
@D:/Code/Utinni/UtinniCore/swg/misc/network.cpp
@D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp
</context>

<interfaces>
<!-- Existing rva_table.cpp slots to extend (verbatim structure to mirror, VERIFIED against current source this session) -->
```
pGetTransform_o2w getTransform_o2w = (pGetTransform_o2w)0x00B22C80;
pGetPlayer        getPlayer        = (pGetPlayer)0x00425140;
Binding g_agentBindings[] = {
    {"object::getTransform_o2w",      (void**)&getTransform_o2w},
    {"game::getPlayer",               (void**)&getPlayer},
    ...
};
size_t g_agentBindingCount = sizeof(g_agentBindings) / sizeof(g_agentBindings[0]);
```

<!-- Existing agent_main.cpp poll loop shape (lines 132-222, VERIFIED against current source this session) — the loop this task extends -->
```
while (true) {
    void* player = swg::endpoints::getPlayer ? swg::endpoints::getPlayer() : nullptr;
    if (!player) { Sleep(100); continue; }   // <-- BUG (Fix F): this runs BEFORE any command/stop read
    // ... existing read of transform/networkId/templateName/liveness, 4-sentinel gate, channelWrite ...
    Sleep(33);  // ~30fps — this task raises to ~16ms (60fps)
}
```

<!-- Ground truth RVAs (Utinni object.cpp / game.cpp / network.cpp — ALL VERIFIED against current source this session, not carried unverified from review prose) -->
- setTransform_o2w = 0x00B22CC0 (Utinni object.cpp:148) — same address family as the existing getTransform_o2w=0x00B22C80
- setScale = 0x00B23A10 (Utinni object.cpp:155)
- getPlayer = 0x00425140 (Utinni game.cpp:73) — ALREADY bound; this is the accessor our existing "player" pointer comes from
- getPlayerCreatureObject = 0x004251D0 (Utinni game.cpp:74) — a DIFFERENT accessor than getPlayer; the CreatureObject wrapper the +1432 target-slot offset is measured FROM. NEW slot this task adds.
- +1432 target-slot offset: `(uint8_t*)getPlayerCreatureObject() + 1432` is a CachedNetworkId pointer
  embedded in the CreatureObject struct (Utinni game.cpp:726-733, comment: "the +1432 lookAt-target
  slot is a RAW CreatureObject byte-offset"). This is NOT the object's own networkId (Fix E) — it is
  the player's CURRENT TARGET slot, used ONLY for the new object-targeting mechanism below.
- cachedNetworkIdGetObject = 0x00B30160 (Utinni network.cpp:39,42 — `__thiscall`, `pThis` = the
  CachedNetworkId pointer computed above) resolves that pointer to an `Object*`. Utinni's own
  `Network::getCachedObjectById` (network.cpp:74-85) gates this call behind `!isAdvertisedClient()`
  (returns nullptr on advertised: "an UNADVERTISED SWGEmu literal... on the advertised client it lands
  on relocated code"). This plan's agent MUST apply the identical `!isAdvertisedClient()` gate before
  ever calling `cachedNetworkIdGetObject` — do not call it on the advertised path under any condition.
- Advertised-safe generic id->Object* resolver EXISTS (`network::getObjectById`, RVA 0x00B380E0,
  Utinni network.cpp:39, advertised-bound) but there is no advertised-safe way to OBTAIN the target's
  networkId as pollable data in Utinni's own reference code today — only via a `CreatureObject::setTarget`
  DETOUR (inline hook), which is new agent capability (not present in packages/live-inject) and is
  explicitly OUT of this plan's scope (would require adding a general-purpose detour/hooking
  subsystem to the x86 agent — a genuine new-capability addition, not a targeted fix). Advertised
  target resolution stays an honest, documented gap (liveness bit3) — do not fabricate a poll-based
  substitute.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve write + target-resolution endpoints (rva_table.cpp) — setTransform_o2w write-slot, null-safe setScale, target-chain RVAs</name>
  <files>packages/live-inject/agent/rva_table.cpp</files>
  <read_first>
    - packages/live-inject/agent/rva_table.cpp (full file — the exact Binding/slot pattern; every new row must follow this verbatim)
    - packages/live-inject/agent/resolve.h and resolve.cpp (full files — Binding struct, resolve()'s graceful-degrade contract: a missing advertised name NEVER nulls the slot; resolveFromExe() sets `s_advertisedClient` BEFORE calling resolve(), which is why a POST-resolve conditional-seed step is possible and correct)
    - D:/Code/Utinni/UtinniCore/swg/object/object.cpp (lines 43-190 — setTransform_o2w RVA 0x00B22CC0 at :148, setScale RVA 0x00B23A10 at :155, calling-convention typedefs)
    - D:/Code/Utinni/UtinniCore/swg/game/game.cpp (lines 41-98 for getPlayer/getPlayerCreatureObject RVAs at :73-74; lines 704-746 for the target-resolution chain and its `isAdvertisedClient()` gate)
    - D:/Code/Utinni/UtinniCore/swg/misc/network.cpp (lines 30-58, 61-100 — cachedNetworkIdGetObject RVA 0x00B30160 at :42, `Network::getCachedObjectById`'s `isAdvertisedClient()` gate at :74-85)
    - D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp (lines 568-598 — confirm current catalog; setTransform_o2w/setPosition_w ARE advertised at :578/:850; setScale is NOT — this is the gap the user_setup row documents)
  </read_first>
  <action>
    Add a new function-pointer typedef `typedef void(__thiscall* pSetTransform_o2w)(void*, const
    void*);` (opaque Transform* argument, mirrors the existing `pGetTransform_o2w` opaque-pointer
    convention exactly — do not introduce a concrete Transform struct dependency) and a slot
    `pSetTransform_o2w setTransform_o2w = (pSetTransform_o2w)0x00B22CC0;` seeded with the legacy RVA
    (this endpoint IS confirmed advertised — engine_advertise.cpp:578/850 — so unconditional seeding is
    safe: resolve() will overwrite it by name on the advertised path, and the legacy literal is correct
    on the legacy path). Add `typedef void(__thiscall* pSetScale)(void*, const void*);` (opaque Vector*
    argument) and seed it **`pSetScale setScale = nullptr;`** — REVIEWS.md BLOCKER fix A: do NOT seed
    this with the legacy RVA literal at declare time, because `resolve()`'s graceful-degrade contract
    (a missing advertised name leaves the slot UNTOUCHED) would otherwise leave a legacy absolute
    address active and callable on the advertised build, where it is not valid code. Append ONE row to
    `g_agentBindings[]` for `setTransform_o2w`: `{"object::setTransform_o2w", (void**)&setTransform_o2w}`
    — do NOT add a binding row for `setScale` (there is nothing for `resolve()` to safely overwrite a
    null slot with if the name is present, and if the name is ABSENT the graceful-degrade "leave
    untouched" behavior would leave it null anyway, which is exactly what we want on advertised — so a
    binding row is harmless either way, but the SAFETY here comes from the POST-resolve conditional-seed
    in Task 2's `agent_init`, not from this row; add the row anyway for when the maintainer adds the
    upstream catalog entry: `{"object::setScale", (void**)&setScale}`, so it resolves automatically the
    moment the upstream row exists). Add two NEW slots for the object-targeting mechanism (05-CONTEXT
    decision #1, LEGACY-ONLY — see Interfaces for the `isAdvertisedClient()` gating requirement, which
    Task 2 enforces at the call site, not here): `typedef void*(__cdecl* pGetPlayerCreatureObject)();`
    with `pGetPlayerCreatureObject getPlayerCreatureObject = (pGetPlayerCreatureObject)0x004251D0;`
    (Utinni game.cpp:74 — this accessor has NO advertised catalog name in the source read this session,
    so do NOT add a binding-array row for it; the legacy literal is only ever called from behind an
    explicit `!isAdvertisedClient()` runtime check in agent_main.cpp, so its value being "wrong" on
    advertised is inert) and `typedef void*(__thiscall* pCachedNetworkIdGetObject)(void*);` with
    `pCachedNetworkIdGetObject cachedNetworkIdGetObject = (pCachedNetworkIdGetObject)0x00B30160;`
    (Utinni network.cpp:42 — same no-binding-row, gated-call-site treatment). Do NOT add any
    `k_legacyNetworkIdOffset` constant and do NOT implement a legacy networkId read anywhere in this
    file — REVIEWS.md Fix E: the `+1432` offset is the player's TARGET slot, not the object's own
    networkId; legacy networkId stays unavailable. Update every new row's inline comment to cite the
    exact Utinni file:line the way every existing row already does, and explicitly note next to
    `getPlayerCreatureObject`/`cachedNetworkIdGetObject`: "LEGACY-ONLY — caller MUST gate on
    `!swg::endpoints::isAdvertisedClient()` before invoking; see Utinni network.cpp:74-85 for why this
    literal is unsafe on the advertised build."
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `setScale` is declared and initialized to `nullptr`, NOT to `(pSetScale)0x00B23A10` (grep confirms `pSetScale setScale = nullptr;`).
    - `g_agentBindings[]` contains exactly TWO new entries: `"object::setTransform_o2w"` and `"object::setScale"`, each with a comment citing the Utinni object.cpp line the legacy RVA was harvested from.
    - `getPlayerCreatureObject` and `cachedNetworkIdGetObject` slots exist, are NOT present in `g_agentBindings[]` (grep confirms no binding row references them — they resolve by neither name nor advertised-catalog, only by the conditional legacy literal Task 2 applies), and their declarations carry the "LEGACY-ONLY... gate on `!isAdvertisedClient()`" comment.
    - No `k_legacyNetworkIdOffset` symbol exists anywhere in this file (grep -c returns 0).
    - The x86 agent DLL still compiles cleanly (no new linkage errors).
  </acceptance_criteria>
  <done>setTransform_o2w resolves via the existing single graceful-degrade mechanism; setScale is null-safe by construction (cannot be a stale advertised-build address); the two new target-resolution RVAs exist and are documented as legacy-only-gated, with no fabricated legacy networkId offset anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poll-loop integration — top-of-loop stop signal, focus-object (target-or-player) resolution, independently-guarded apply, read-back guard baseline, REBASELINE_GUARD</name>
  <files>packages/live-inject/agent/agent_main.cpp, packages/live-inject/agent/write.cpp, packages/live-inject/agent/write.h</files>
  <behavior>
    - The VERY FIRST thing every loop iteration does (before computing `player` or checking it for
      null) is: attempt `channelReadCommand(&cmd, &cmdSeq)`; if the read succeeded and
      `cmd.flags & CMD_FLAG_STOP_REQUESTED`, immediately `channelClose()` and `return 0` — this fixes
      REVIEWS.md Fix F (Cursor HIGH): a detach while the world is not loaded (`player == nullptr`) must
      still be honored, not starved by the pre-existing `if (!player) { Sleep(100); continue; }` gate.
    - After the stop-signal check, resolve `player = getPlayer()` as before. If `player` is null,
      `Sleep(100); continue;` exactly as today (this early-continue now runs AFTER the stop-signal
      check, not before it).
    - Resolve the "focus object" for this iteration: on the LEGACY path
      (`!isAdvertisedClient()`) only, call `getPlayerCreatureObject()` (if resolved) then compute
      `cachedNetIdPtr = (uint8_t*)playerCreature + 1432` and call
      `cachedNetworkIdGetObject(cachedNetIdPtr)`; if that returns non-null, `focus = target`,
      `hasTarget = true`. On the ADVERTISED path, or when legacy resolution yields null, `focus =
      player`, `hasTarget = false`, and (advertised path only) `targetUnavailableOnBuild = true`. All
      existing reads (transform, networkId, templateName) and the sentinel gate now operate on `focus`,
      not unconditionally `player` — this is 05-CONTEXT decision #1's honest object-targeting.
    - `state.liveness` gains two bits (in addition to the existing bit0=playerNonNull, bit1=isOver):
      bit2 = `hasTarget`, bit3 = `targetUnavailableOnBuild`.
    - Legacy networkId: leave EXACTLY as today (`netId = 0` when `getNetworkId` is unresolved; the
      networkId sentinel stays not-applicable on that path) — do NOT add any offset-based read here
      (REVIEWS.md Fix E).
    - Each loop iteration (now ~16ms / 60fps): after the existing read+4-sentinel-gate+channelWrite
      step (now operating on `focus`), if a command was read this iteration (from the top-of-loop read)
      and it is NOT a stop request: if `cmd.flags & CMD_FLAG_REBASELINE_GUARD`, set
      `s_expectedTransform` = the transform just read this iteration and `s_expectedScale` = the scale
      just read this iteration (agent has no scale accessor — see write.cpp note below), clear both
      guard-refused bits via `channelWriteGuardStatus(0, addrOf(focus))`, and do NOT call any setter
      this iteration (Opus MEDIUM fix — this is the ONLY thing a REBASELINE_GUARD command does). Else,
      if `cmdSeq` is a NEW value (differs from a static `lastAppliedCmdSeq`, initialized to 0) and the
      read was not torn: run `checkWriteGuard(xform, s_expectedTransform, /* scale */ ..., s_expectedScale)`
      using the transform just read this same iteration (scale has no read-back accessor — see note);
      call `applyWrite(focus, cmd, guardResult)` (completed in write.cpp below) which independently
      calls `setTransform_o2w(focus, &cmd.transform)` ONLY if `guardResult.transformPassed` — always
      publish `channelWriteGuardStatus(flags, addrOf(focus))` reflecting BOTH bits' final state
      (TRANSFORM_REFUSED set iff `!guardResult.transformPassed`, SCALE_REFUSED set iff
      `!guardResult.scalePassed || !swg::endpoints::setScale`) — a null/unresolved `setScale` reports
      SCALE_REFUSED exactly like a guard failure would (both mean "this write did not happen"), never
      crashes. On a successful transform apply, refresh `s_expectedTransform` from a FRESH
      `getTransform_o2w(focus)` read-back taken immediately after the call (REVIEWS.md Fix D — NOT from
      `cmd.transform` — this is what survives interior/non-world-cell objects' float-rounded storage
      without false-failing write N+1). On a successful scale apply, set `s_expectedScale = cmd.scale`
      directly (no read-back accessor exists for scale; unlike transform, `setScale` sets `m_scale`
      directly with no coordinate-space transform, so this is not subject to the same rounding
      class of bug — document this reasoning inline). Set `lastAppliedCmdSeq = cmdSeq` after processing
      either branch (rebaseline or normal apply).
    - `s_expectedTransform`/`s_expectedScale` (new statics, agent_main.cpp or write.cpp — executor's
      choice of ownership, but must be initialized) are captured from the FIRST sentinel-passing
      transform/scale read after channel open (the D-03 "attach-time" snapshot) — add a static `bool
      s_expectedCaptured = false` guard so this only happens once per agent lifetime, not every loop
      iteration (scale has no read accessor to capture from at attach time — seed `s_expectedScale`
      from the FIRST command's `cmd.scale` the agent ever sees applied, i.e. leave scale guard
      effectively "pass" until the first scale write happens, since there is no way to read the live
      scale independently to baseline it at attach; document this as a scope note, not a defect —
      Scale's guard becomes meaningful starting from the toolkit's own first scale write, matching how
      COW/write-log semantics already work for a value the toolkit itself introduces).
  </behavior>
  <read_first>
    - packages/live-inject/agent/agent_main.cpp (full file — the exact loop to extend, lines 132-222 especially, VERIFIED against current source this session)
    - packages/live-inject/agent/write.h (from 05-01 — checkWriteGuard signature with independent transformPassed/scalePassed, applyWrite stub)
    - packages/live-inject/agent/channel.h (from 05-01 — LiveCommand struct, CMD_FLAG_STOP_REQUESTED, CMD_FLAG_REBASELINE_GUARD, GUARD_FLAG_TRANSFORM_REFUSED, GUARD_FLAG_SCALE_REFUSED, channelReadCommand/channelWriteGuardStatus(flags, addr) signatures)
    - This plan's Task 1 output (rva_table.cpp new slots: null-safe setScale, getPlayerCreatureObject, cachedNetworkIdGetObject)
    - resolve.h (isAdvertisedClient() — already declared, used both for the post-resolve setScale seed and the target-resolution gate)
  </read_first>
  <action>
    Add extern references at the top of agent_main.cpp (mirroring the existing extern block, lines
    68-76) for `swg::endpoints::setTransform_o2w`, `swg::endpoints::setScale`,
    `swg::endpoints::getPlayerCreatureObject`, `swg::endpoints::cachedNetworkIdGetObject`. Immediately
    after `swg::endpoints::resolveFromExe();` (agent_init Step 3, existing code) add the POST-resolve
    conditional seed that closes the BLOCKER (REVIEWS.md Fix A): `if
    (!swg::endpoints::isAdvertisedClient()) { swg::endpoints::setScale = (pSetScale)0x00B23A10; }` —
    this is the ONLY place the legacy Scale literal is ever installed, and it only runs when the client
    has already been confirmed non-advertised; `setScale` remains `nullptr` on the advertised path
    unless/until the upstream `engine_advertise.cpp` row exists AND `resolve()` finds it by name (which
    happens automatically inside `resolveFromExe()`, BEFORE this conditional-seed line runs — so if the
    advertised catalog DOES have the row, resolve() already overwrote the slot with the real advertised
    address, and this legacy-only conditional-seed is skipped entirely since `isAdvertisedClient()` is
    true). Change the poll `Sleep(33)` to `Sleep(16)` (60fps, per LIVE-03 SC1). Restructure the loop body
    per this task's `<behavior>` block exactly: stop-signal read FIRST (before `player` is even
    computed), then player-null continue, then focus-object resolution (target-or-player), then the
    existing sentinel-gated read+channelWrite (now targeting `focus`), then the command-apply block
    (rebaseline-or-guarded-apply). Implement `applyWrite` in write.cpp now that the concrete setter
    fn-pointer types exist: `void applyWrite(void* target, const LiveCommand& cmd, const GuardResult&
    guard)` internally: `if (guard.transformPassed && swg::endpoints::setTransform_o2w)
    swg::endpoints::setTransform_o2w(target, &cmd.transform);` and separately `if (guard.scalePassed &&
    swg::endpoints::setScale) swg::endpoints::setScale(target, &cmd.scale);` — each gated INDEPENDENTLY
    on both its own guard bit and its own non-null resolved pointer (REVIEWS.md BLOCKER fix A's "never
    call an unresolved setter" + Fix B's "scale guarded independently"); agent_main.cpp calls this
    single function on any guard-evaluated command (even if one or both channels fail) rather than
    calling setters inline, keeping the setter-invocation logic in one place per write.h's original
    contract. Truncate `target`'s pointer to `uint32_t` via `static_cast<uint32_t>(reinterpret_cast
    <uintptr_t>(target))` when calling `channelWriteGuardStatus(flags, addr)` (x86 process, safe
    truncation, matches channel.h's `guardAddr` field type).
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms `Sleep(16)` replaces the prior `Sleep(33)` in the poll loop.
    - grep confirms a `CMD_FLAG_STOP_REQUESTED` check exists and is the FIRST statement inside the `while(true)` loop, preceding the `player == nullptr` continue (not after it) — this is the Fix F regression test.
    - grep confirms `if (!swg::endpoints::isAdvertisedClient()) { swg::endpoints::setScale = ...0x00B23A10; }` exists immediately after `resolveFromExe()` and nowhere else assigns a non-null literal to `setScale`.
    - grep confirms `applyWrite` calls `setTransform_o2w` and `setScale` INDEPENDENTLY, each behind its own `guard.*Passed && swg::endpoints::*` non-null check — never an unconditional "call both" site.
    - grep confirms a fresh `getTransform_o2w` read-back (not `cmd.transform`) is assigned to `s_expectedTransform` on a successful transform apply.
    - grep confirms `getPlayerCreatureObject`/`cachedNetworkIdGetObject` calls are lexically inside an `if (!swg::endpoints::isAdvertisedClient())` block — never called unconditionally.
    - grep confirms no `+ 1432` arithmetic is used to populate any field named/documented as "networkId" — the only `+ 1432` use is for target resolution, feeding `cachedNetworkIdGetObject`.
    - Agent DLL builds cleanly.
  </acceptance_criteria>
  <done>The agent applies independently-guarded Move/Rotate/Scale writes at 60fps against the correctly-resolved focus object (target-or-player, legacy-grounded), never calls an unresolved setter, refreshes its transform guard baseline from a read-back (surviving interior objects), lets an explicit REBASELINE_GUARD command un-stick a blocked guard, and exits its poll loop cleanly on a stop signal even before a player exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Command slot (host-writable) → agent poll loop → live game object memory | The agent is the last line of defense before a real write touches the running game's object state; the guard MUST run before every apply, with no code path that skips it, and no code path may call an unresolved (null or cross-build-invalid) setter. |
| Legacy-only target-resolution RVAs → advertised build | Two new slots (`getPlayerCreatureObject`, `cachedNetworkIdGetObject`) hold literals that are only valid on the legacy SWGEmu build; calling them on the advertised build risks executing arbitrary relocated code — the runtime `!isAdvertisedClient()` gate is the ONLY thing preventing this, verified by acceptance-criteria grep, not just code review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | agent_main.cpp command-consumption block | mitigate | `applyWrite`'s two setter calls are each independently guarded (own guard bit + own non-null check), called from exactly one call site — verified by acceptance_criteria grep, not just code review; no alternate/debug bypass path exists. |
| T-05-05 | DoS (resource leak) | agent poll thread lifecycle | mitigate | STOP_REQUESTED is checked at the TRUE TOP of every iteration (before the player-null early-continue), guaranteeing the thread exits even if no player is currently loaded — repeated attach/detach cycles cannot accumulate poll threads (REVIEWS.md Fix F). |
| T-05-06 | Info disclosure | GUARD_ADDR (agent-published object pointer) | accept | Reporting the live object's own memory address to the LOCAL user of a single-user desktop modding tool is not a meaningful disclosure risk — diagnostic information about a process the user already controls and has attached to (matches 05-11's existing T-05-19 disposition). |
| T-05-27 | Elevation of privilege | setScale unconditional-seed regression | mitigate | The legacy Scale RVA is installed in exactly ONE place (the post-`resolveFromExe()` conditional-seed), gated on a CONFIRMED-negative `isAdvertisedClient()` result — acceptance criteria grep-verify both the null initial seed AND the single conditional-install site, closing the BLOCKER (REVIEWS.md Fix A) structurally, not just by convention. |
| T-05-28 | Spoofing/Tampering | Legacy-only target-resolution RVAs called on the wrong build | mitigate | `getPlayerCreatureObject`/`cachedNetworkIdGetObject` are called ONLY from inside an explicit `!isAdvertisedClient()` branch, verified by acceptance-criteria grep — mirrors the identical gate Utinni's own reference code applies at the same call site (network.cpp:74-85). |
</threat_model>

<verification>
Build the x86 agent DLL (`cmake --build packages/live-inject/agent/build-agent --config Release`)
with zero errors; grep-based acceptance criteria above all pass.
</verification>

<success_criteria>
- setTransform_o2w resolves via the single existing graceful-degrade mechanism on both paths; setScale is structurally incapable of holding a stale cross-build address (BLOCKER closed).
- Target resolution moves the object the player has targeted in-game (legacy) or honestly reports the gap (advertised) — never silently the wrong object.
- The guard gates every apply independently per channel, with zero bypass paths, and survives interior-object float rounding across successive writes.
- A blocked guard can always be un-stuck by the user's own explicit revert action.
- The agent exits cleanly on STOP_REQUESTED even with no player loaded, and legacy networkId is honestly UNAVAILABLE rather than sourced from the wrong field.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-SUMMARY.md` when done
</output>
</output>
