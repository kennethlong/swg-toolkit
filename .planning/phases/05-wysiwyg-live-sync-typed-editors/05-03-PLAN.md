---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/live-inject/agent/rva_table.cpp
  - packages/live-inject/agent/agent_main.cpp
  - packages/live-inject/agent/write.cpp
  - packages/live-inject/agent/write.h
  - .planning/handoff/2026-07-09-phase05-m-scale-offset-confirmation-request.md
  - .planning/handoff/README.md
autonomous: true
requirements: [LIVE-03]
user_setup:
  - service: swg-client-v2 (maintainer's advertised client build)
    why: "D-09 requires object::setScale to be advertised via GetEngineHookPoints for Scale writes to work on the advertised target. Without this row, the agent gracefully degrades (Scale disabled-with-reason on advertised, liveness bit4 scaleUnavailableOnBuild — REVIEWS.md ROUND 2 W2 fix) instead of crashing (REVIEWS.md ROUND 1 BLOCKER fix A) — this is a documented KNOWN GAP (05-CONTEXT decision #2), not a phase-completion blocker."
    dashboard_config:
      - task: "Add an 'object::setScale' row to s_engineHookPoints[] in engine_advertise.cpp, then rebuild and redeploy the swg-client-v2 client binary to enable Scale writes on the advertised target. Optional — 05-12's UAT passes without this, recording it as a known follow-up per 05-CONTEXT decision #2."
        location: "D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp"
      - task: "OPTIONAL, de-anchoring-crew CONFIRMED (2026-07-09): 'network::getObjectById' (engine_advertise.cpp:709, __cdecl static, -> NetworkIdManager::getObjectById), 'cuiHud::getTarget' (engine_advertise.cpp:705, __fastcall thunk over SwgCuiHud::getLastSelectedObject — name-mismatched in the catalog), and 'cuiHud::g_instance' (engine_advertise.cpp:706, SwgCuiHudFactory::findMediatorForCurrentHud) — line numbers CORRECTED (ROUND 5, Codex re-read of current source; keys/convention unchanged, only the citations drifted +2 from a prior review round are ALL THREE already present in the maintainer's current engine_advertise.cpp per this session's direct source read (not carried unverified from review prose). This is a confirm-not-add step; only add a row if the executor's read_first pass finds one genuinely missing on the deployed build."
        location: "D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp"

# REVISION NOTE (--reviews replan, 2026-07-08): folds in the review round's BLOCKER + 4 of the 5 HIGH
# findings (05-REVIEWS.md), plus 05-CONTEXT decision #1 (object targeting, new scope this session).
# (A) BLOCKER — setScale crashes the advertised client on the FIRST write of any kind. Root cause:
#     resolve() leaves a slot untouched on a name-miss, and the original Task 1 seeded setScale with
#     the legacy RVA unconditionally (non-null, so applyWrite's old "call both, unconditionally, never
#     one without the other" rule called a stale absolute address on the advertised build). FIX: seed
#     setScale = nullptr at declare time; apply the legacy RVA to it ONLY when resolveFromExe() has
#     determined the client is NOT advertised (a new one-time post-resolve step, agent_main.cpp);
#     applyWrite calls setScale ONLY if the resolved pointer is non-null (Task 2's old "never one
#     without the other" acceptance criterion is RELAXED to "never call an unresolved setter").
# (E) HIGH — legacy networkId `playerCreature + 1432` is the WRONG field. Verified independently
#     against BOTH the review's Utinni citation (game.cpp:714, object.h:80) AND this session's own
#     re-read of the SAME file: +1432 is `Game::getPlayerLookAtTargetObjectNetworkId()`'s slot — the
#     player's CURRENT TARGET, not the object's own networkId. FIX: DROP the legacy-networkId
#     implementation entirely (D-04 item 3 stays UNAVAILABLE on legacy, not wrong) — but see (New
#     scope) below, this SAME offset is the correct, grounded mechanism for object-targeting.
# (D) HIGH — guard false-fails on non-world-cell (interior) objects after write 2+ (float-rounded
#     read-back on the objectToCell path, Object.cpp:1460-1470). FIX: after a successful apply, set
#     s_expectedTransform from a FRESH getTransform_o2w read-back (what was actually stored), not from
#     cmd.transform.
# (B) HIGH — Scale had no guard/revert. FIX: write.h/.cpp (05-01) now splits the guard into
#     independent transformPassed/scalePassed; this plan wires BOTH into agent_main.cpp's apply step,
#     each gated on its own resolved-fn-pointer AND its own guard bit.
# (F) HIGH (Cursor) — STOP_REQUESTED starved when player==nullptr (agent_main.cpp's existing
#     `if (!player) { Sleep(100); continue; }` runs BEFORE any command read). FIX: read the command
#     slot (incl. STOP_REQUESTED) at the TRUE TOP of the loop, before the player-null continue.
# Opus MEDIUM — a blocked guard also blocks its own revert. FIX: agent honors CMD_FLAG_REBASELINE_GUARD
#     (05-01) by re-baselining s_expectedTransform/s_expectedScale to the CURRENT live bytes it reads
#     this same iteration, applying nothing — the renderer (05-07) then sends the actual revert target
#     as a normal follow-up command, which now passes the guard. (SUPERSEDED by ROUND 2 item 3 below —
#     the two-command sequence this originally described is now coalesced into one command.)
# (New scope — 05-CONTEXT decision #1, object targeting) The agent poll loop previously resolved ONLY
#     `getPlayer()` — a gizmo drag on any non-player mesh silently moved the player's own avatar,
#     invisibly. Source-grounded fix (Utinni game.cpp:704-746, network.cpp:39-85, object.cpp:260-268 —
#     verified this session, NOT carried from review prose): on the LEGACY (SWGEmu) path, resolve the
#     player's current in-game TARGET via `getPlayerCreatureObject()+1432` (a CachedNetworkId pointer
#     embedded in the CreatureObject struct — Utinni game.cpp:733) passed to `cachedNetworkIdGetObject`
#     (RVA 0x00B30160, __thiscall — Utinni network.cpp:39-42,74-85); this IS the correct, grounded use
#     of the +1432 offset the original 05-03 misapplied as "networkId". On the ADVERTISED
#     (swg-client-v2) path, Utinni's OWN reference code gates this off entirely
#     (`getPlayerLookAtTargetObjectNetworkId` returns 0 when `isAdvertisedClient()`, game.cpp:721-724)
#     with a comment noting "an advertised lookAt-target accessor row is the follow-on that lights this
#     up for real" — i.e. even the reference project does not have this yet. (SUPERSEDED by ROUND 2
#     item 1 below — the advertised build now gets a REAL, different targeting mechanism, closing this
#     gap for real rather than leaving it an honest player-fallback.)

# REVISION NOTE (--reviews replan, ROUND 2, 2026-07-08): folds in 05-REVIEWS.md ROUND 2 + the
# maintainer's decisions. ADDITIVE to everything above (the BLOCKER/HIGH fixes A-F and the legacy
# +1432 target chain all stay exactly as implemented) — this round widens what "resolving a target"
# means and corrects two behaviors.
# (1) Decision 1b (object-targeting scope — PURSUE THE STRONGER PATH, maintainer-approved, the largest
#     change this round). Round-1's "legacy +1432 lookAt-target, advertised ALWAYS falls back to
#     player" does NOT close the round-1 Sonnet HIGH finding ("the gizmo moves the wrong object") —
#     the advertised build never had a real target, only an honestly-labeled player fallback. Ground
#     truth (Codex, LOCKED — do not re-derive): an arbitrary NetworkId->Object* resolver exists on
#     BOTH builds — legacy `idManagerGetObjectById` (RVA 0x00B380E0, Utinni network.cpp:39) and
#     advertised `NetworkIdManager::getObjectById` (catalog row at engine_advertise.cpp:709, impl
#     NetworkIdManager.cpp:72-79) — AND the advertised build additionally has a direct viewed/picked
#     object path: `SwgCuiHud::getLastSelectedObject()` (engine_advertise.cpp:705-706), wrapped by
#     Utinni as `CuiManager::getSelectedObject()` (cui_manager.cpp:183-205). There is NO
#     template->live-Object* resolver on either build (do not invent one) — this plan does not attempt
#     to resolve "the mesh loaded in the viewport" to a live instance; it resolves "what the player has
#     selected/targeted in-game right now," the strongest reachable mechanism on both builds and the
#     only thing that actually satisfies "move what you're viewing" when the two happen to coincide.
#     `getObjectById` is bound on BOTH builds (legacy literal + advertised catalog row — symmetric
#     binding per this project's standing "SWGEmu is never descoped" rule, even though the ACTIVE
#     per-frame resolution mechanism differs by build, see Task 2). `getSelectedObject`/
#     `getLastSelectedObject` is bound on the ADVERTISED build only (no legacy equivalent exists in the
#     source read this session). Focus resolution becomes: LEGACY — UNCHANGED, the existing
#     `getPlayerCreatureObject()+1432 -> cachedNetworkIdGetObject` chain (still the only
#     concretely-reachable "what's targeted" mechanism on legacy; `getObjectById` is bound but has no
#     independent networkId source to feed it on legacy today, so it is NOT on the active resolution
#     path this phase — a reusable primitive for a future networkId-driven flow, not dead code).
#     ADVERTISED — NEW: call `getSelectedObject()`; if it resolves to a non-null Object*, `focus =
#     selected`, `hasTarget = true` — a REAL fix, not a relabeling, since the advertised build
#     previously had zero targeting capability. `targetUnavailableOnBuild` is REDEFINED (both builds)
#     to mean "the resolution MECHANISM itself is not wired up on this build" (an unresolved RVA/catalog
#     row), never "nothing is currently selected" — the latter is a normal `hasTarget=false` state with
#     `targetUnavailableOnBuild=false` ("attached, nothing targeted, moving player" — legitimate on
#     either build now, not a build-capability gap). CORRECTED FRAMING (ROUND 4, REVIEWS.md round-3
#     maintainer decision #3(2) — the round-2 "CORRECTS THE OVERCLAIM" text above self-contradicted:
#     it called the round-1 legacy-only fix an overclaim, then immediately overclaimed the SAME thing
#     itself about this round's resolver): this round's cross-build selected-object resolver makes the
#     "what does the player have targeted/selected" fallback REAL and SYMMETRIC on both builds (round 1
#     had it real on legacy only, honest-fallback on advertised) — it does NOT close, and was never
#     capable of closing, the viewport<->live-object BINDING gap: there is still no template->live-
#     Object* resolver on either build, so the toolkit still cannot confirm the mesh loaded in the
#     viewport is the object that moves. That gap remains OPEN and is explicitly deferred to Phase 7's
#     `.ws` placement editing (05-CONTEXT "Out of scope"). 05-11 (decision #3(1)) adds an inline
#     `⚠ viewing <loaded>, moving <target>` HUD warning precisely because this gap is not closed; 05-12
#     (decision #3(3)) adds a UAT step that deliberately exercises the mismatch case rather than
#     arranging around it. 05-10/05-11 HUD copy updates accordingly (target-name label now reachable
#     on BOTH builds, not legacy-only) — but that label names WHAT IS TARGETED IN-GAME, never asserts
#     it corresponds to the viewport's loaded asset.
# (2) Decision 2 (W2 fix — liveness-bit4 `scaleUnavailableOnBuild` split, Cursor's recommended fix,
#     ZERO channel-layout growth, no 05-01 change needed). Round-1's `SCALE_REFUSED` guard bit (0x2)
#     fired for TWO unrelated causes — a genuine tamper (`!scalePassed`) OR `setScale` simply being
#     unresolved (`!swg::endpoints::setScale`) — so on the advertised build (setScale null until the
#     optional upstream row is added) ANY Scale interaction permanently latched the FALSE "tamper"
#     banner for the whole session. FIX: `SCALE_REFUSED` is set ONLY on a genuine guard mismatch AND a
#     RESOLVED setter (`!guardResult.scalePassed && swg::endpoints::setScale != nullptr` — a write was
#     actually attempted and refused). Add a NEW liveness bit4 `scaleUnavailableOnBuild`, computed ONCE
#     right after the existing post-`resolveFromExe()` conditional-seed step (mirrors bit3's
#     one-time-computed shape): `s_scaleUnavailableOnBuild = swg::endpoints::isAdvertisedClient() &&
#     swg::endpoints::setScale == nullptr;` — OR'd into `state.liveness` every iteration thereafter. No
#     new channel offset (LIVENESS already exists at offset 316; this is bit semantics only — 05-01 is
#     UNCHANGED). 05-07 decodes bit4; 05-10/05-11 precedence: bit4 set -> "Scale unavailable on this
#     build" (reuse the D-05 disabled-with-reason idiom); `guardState.scale === 'blocked'` WITHOUT bit4
#     -> the real tamper banner.
# (3) Decision 3 (revert model — coalesce rebaseline+apply into ONE command, closing Opus's race).
#     Round-1's `CMD_FLAG_REBASELINE_GUARD` handling was mutually-exclusive with applying a write
#     ("rebaseline-only this iteration, apply nothing") — the renderer therefore had to send TWO
#     separate commands (rebaseline, then the actual revert-write) on the single-slot seqlock channel,
#     which Opus found races: the agent can observe only the LATER command (seq N+1) and miss the
#     rebaseline (seq N), leaving the revert permanently blocked. FIX (Opus's "coalesce" option, chosen
#     over ack-gating — no new channel state needed): when `cmd.flags & CMD_FLAG_REBASELINE_GUARD` is
#     set, the agent now (a) sets `s_expectedTransform` from the transform already read THIS iteration
#     for the sentinel gate (fresh live bytes — the SAME source Fix D's read-back already uses) and
#     `s_expectedScale = cmd.scale` (there is no live scale read-back to rebaseline FROM, so syncing
#     directly to the incoming target keeps both channels' rebaseline semantics consistent — this is
#     not a new gap, it mirrors the existing scale-baseline note below), then (b) falls through
#     IMMEDIATELY, same iteration, into the NORMAL `checkWriteGuard` + `applyWrite` path using these
#     just-rebaselined expected values against `cmd`'s payload (the actual revert target) — which now
#     trivially passes both channels' guard (expected == the values just set) and applies the real
#     setter call. This is ONE seqlock write cycle end-to-end (the renderer sends ONE `writeCommand`
#     call carrying both the `REBASELINE_GUARD` flag AND the real revert-target transform/scale payload
#     — 05-07 implements the renderer-side single-call change), eliminating the two-command race
#     structurally rather than requiring an ack-gate poll. The renderer (05-07) only ever sets this
#     flag when the relevant guard channel is ALREADY `'blocked'` (decision 3's "gate on blocked" half)
#     — when the guard is `'ok'`, a revert is just a normal `writeTransform` call with no flag, since
#     there is nothing to unstick (also avoids wasted channel traffic on every ordinary revert).
# (4) Minor corrections (apply all, per 05-REVIEWS.md):
#     - Fix D caveat: the read-back-refresh fix (`s_expectedTransform` from a fresh `getTransform_o2w`
#       read-back after a successful apply) survives STATIC non-world-cell/interior objects only — for
#       a dynamically-moving parent (`m_attachedToObject` = mount/vehicle/POB ship) the world-space
#       read-back drifts every frame, false-failing the guard on write N+1 independent of any tamper
#       (fails safe, but a real UX surprise worth an inline comment where the read-back is implemented).
#     - "No scale read accessor exists" OVERSTATES: `Object::getScale()` DOES exist (Object.h:512-515).
#       The accurate claim (and the reason the read-back-avoidance conclusion still stands) is "the
#       agent binds no `getScale` RVA" — `setScale` stores verbatim (`m_scale = scale;`, Object.cpp:2205)
#       so a read-back is unnecessary regardless; only the PHRASING was wrong, not the design.
#     - `+1432`: unchanged as a harvested, comment-backed constant (Utinni game.cpp:733) — it stays on
#       the critical path for LEGACY target resolution only (the new cross-build resolver above is
#       ADVERTISED-side; it does not touch or replace legacy's +1432 mechanism) — mark it "validate
#       live" explicitly in Task 1's inline comment, not just "harvested."

# REVISION NOTE (plan-check revision, 2026-07-08): closes a plan-checker BLOCKER. ROUND 2's minor
# correction above (item 4, second bullet) reasoned its way to "a read-back is unnecessary" — that
# conclusion is correct for REFRESHING the baseline after the TOOLKIT's OWN successful write (setScale
# stores verbatim, so `s_expectedScale = cmd.scale` after a successful apply is exact), but it does NOT
# hold for D-03's LOCKED read-verify GUARD, whose entire job is detecting an EXTERNAL tool changing the
# object between polls. Without a genuine live read, the guard's `liveScale` argument was fed
# `s_expectedScale` (the toolkit's own last-known value) BACK INTO ITSELF — a self-comparison that
# always trivially passes and can NEVER detect external scale tampering, unlike Transform, whose guard
# already compares a REAL `getTransform_o2w` read against `s_expectedTransform` every iteration. This is
# the exact "self-referential comparand" the maintainer flagged. Maintainer decision (option a,
# 2026-07-08): give Scale the SAME real tamper-detection parity as Transform, per D-03's locked guard
# contract, rather than leave it permanently weaker.
#
# FIX (2026-07-08, SUPERSEDED by the 2026-07-09 note below): bind `Object::getScale()` as a live-read
# accessor on BOTH builds — this original fix attempted an RVA/catalog binding for `getScale()`. See the
# 2026-07-09 REVISION NOTE: the de-anchoring crew found `getScale()` is INLINE with no standalone
# address, so the RVA/catalog-binding approach is replaced with a member-offset read below.
#
# CROSS-REPO CONSTRAINT (CLAUDE.md): unlike every other endpoint this plan binds, `Object::getScale()`'s
# RVA/catalog name is NOT already harvestable from anything in this repo — grepping Utinni's vendored
# source (the source of every OTHER legacy RVA in `rva_table.cpp`) for "getScale" returns zero hits
# (confirmed this session), and `engine_advertise.cpp`'s `s_engineHookPoints[]` has no `object::getScale`
# -shaped row either (also confirmed this session — the same kind of gap this plan's `user_setup`
# already documents for `object::setScale`). See the 2026-07-09 REVISION NOTE below for how this
# constraint is now satisfied WITHOUT a cross-repo edit dependency (a self-contained member-offset read,
# with the handoff kept only as a non-blocking confirmation backup).

# REVISION NOTE (de-anchoring crew fold-in, 2026-07-09): closes out the previous plan-check revision's
# TWO open threads using a 4-member cross-AI crew's ground-truth read (Codex, Cursor, Opus, Sonnet —
# full findings: .planning/research/CONSULT-05-SYNTHESIS.md) against ../Utinni and ../swg-client-v2
# real source. ALL of this plan's existing RVA/catalog/offset claims were CONFIRMED byte-exact by the
# crew — nothing below invalidates a previously-shipped literal; this note ADDS precision and corrects
# two prose/design gaps the crew's read surfaced.
#
# (1) getScale RE-ARCHITECTED: RVA/catalog binding -> gated m_scale MEMBER-OFFSET read (maintainer
#     decision). Ground truth (Cursor, swg-client-v2 Object.h:512-515): `Object::getScale()` is
#     `inline const Vector &getScale() const { return m_scale; }` — an INLINE accessor with no
#     standalone out-of-line address on most builds; the PRIOR revision's "bind a getScale RVA/catalog
#     row + file a handoff to get one" was chasing a symbol that may not exist as a callable address.
#     `m_scale` (swg-client-v2 Object.h:456) is a plain POD member. FIX: DROP the getScale
#     Binding-array slot entirely (both the null-safe legacy declaration in rva_table.cpp/Task 1 and
#     the resolved-fn-pointer read in agent_main.cpp/Task 2). Read `m_scale` directly by a per-build-
#     gated member-offset off the `Object*`, the SAME mechanism this file already uses for the legacy
#     `+1432` lookAt-target slot (Interfaces, "Ground truth RVAs") — this plan's own precedent already
#     pre-clears the D-01 scope question: D-01 governs WRITES ("call the setter, don't poke bytes"); a
#     READ-only offset used solely as `checkWriteGuard`'s `liveScale` comparand is out of D-01's scope,
#     exactly as the pre-existing +1432 read already is. LEGACY: use a planner-computed candidate offset
#     `kLegacyScaleOffset = 0x44` (68 decimal), derived this session by summing member sizes in Utinni's
#     own `Object` struct — the SAME hand-verified memory-layout reflection struct this file's other
#     legacy literals are harvested from (`UtinniCore/swg/object/object.h:76-107`; `scale` field at
#     line 94) — assuming standard MSVC x86 alignment (`int64_t networkId` 8-byte-aligned, no
#     `#pragma pack` found anywhere in UtinniCore, grep-confirmed this session) and `swgptr` = 4 bytes
#     (`utinni.h:36`). This is a COMPUTED, not disassembly-confirmed, value — apply the IDENTICAL
#     "VALIDATE LIVE" posture the +1432 constant already carries in this file (05-12's UAT is where a
#     wrong computed offset would surface; if it does, the live-observed value supersedes this one).
#     ADVERTISED: no legacy-struct shortcut exists (swg-client-v2 is a different, modernized codebase
#     with its own, unknown-this-session member layout) — GATE the advertised read behind a
#     `s_advertisedScaleOffsetConfirmed` bool (defaults `false`, mirrors bit4's `setScale == nullptr`
#     write-side gate shape exactly) until a real value is supplied. Liveness bit5 is REDEFINED (no
#     channel-layout growth, same bit) from "getScale RVA/catalog unresolved" to "advertised m_scale
#     offset not yet confirmed" — bit5 is now unconditionally `false` on legacy (the computed offset is
#     always attempted there, never gated) and `true` on advertised until confirmed. The cross-repo
#     handoff doc is KEPT but REFRAMED as a non-blocking AUTHORITATIVE-CONFIRMATION backup (not the
#     primary unblock mechanism it was in the prior revision): it now asks the Utinni/swg-client-v2
#     sessions to independently CONFIRM (Utinni: cross-check the planner-computed 0x44 against a live
#     debugger read; swg-client-v2: report the compiled `m_scale` member's real byte offset) the
#     per-build offsets, source-corroborating what this plan's live-validated read already establishes
#     without waiting on either session. Renamed handoff file (was
#     `2026-07-08-phase05-getscale-accessor-request.md`, now
#     `2026-07-09-phase05-m-scale-offset-confirmation-request.md`) to reflect the new, narrower ask.
#
# (2) Legacy targeting "no networkId source" premise CORRECTED (drop a false claim, no behavior
#     change — the ACTIVE legacy focus-resolution mechanism was already correct and stays UNCHANGED).
#     Ground truth (Sonnet, source-verified this session against real Utinni source, not review prose):
#     the prior revision's justification for leaving `idManagerGetObjectById` (0x00B380E0) uncalled —
#     "legacy has no independent networkId source to feed it" — is FALSE. `Object::networkId` IS a real,
#     flat `int64_t` field (`UtinniCore/swg/object/object.h:86`), and the chain this plan already binds
#     is a genuine, wired, production-shaped resolver, not a placeholder: `Game::
#     getPlayerLookAtTargetObjectNetworkId()` computes `(swgptr)playerCreature + 1432`
#     (`game.cpp:733`) -> `Game::getPlayerLookAtTargetObject()` calls `Object::getObjectById(lookAtId)`
#     (`game.cpp:745`) -> `Object::getObjectById` (`object.cpp:260-268`) calls `Network::
#     getCachedObjectById` (`network.cpp:74-85`, the single `isAdvertisedClient()`-gated choke point)
#     -> which calls THIS plan's bound `cachedNetworkIdGetObject` RVA (`network.cpp:84`, the actual
#     0x00B30160 call site; typedef at `network.cpp:35`, literal assignment at `network.cpp:42` —
#     corrects a stray `:39` mis-citation in this file's prior "Ground truth RVAs" list, which pointed
#     at `idManagerGetObjectById`'s line by mistake). CORRECTED FRAMING (replaces "legacy has no
#     independent networkId source" everywhere it appears in this file): `idManagerGetObjectById`
#     (0x00B380E0) is bound as a reusable resolver primitive per the project's "SWGEmu never descoped"
#     rule; the ACTIVE legacy focus-resolution path uses the CachedNetworkId sibling
#     `cachedNetworkIdGetObject` (0x00B30160) via the +1432 chain INSTEAD — not because legacy lacks a
#     networkId source, but because the +1432 chain is the more specific, already-correct mechanism for
#     "what is the player currently targeting." No agent_main.cpp behavior changes — the legacy
#     focus-resolution call graph this plan implements was already this real, wired path; only the
#     surrounding PROSE describing it was wrong. Also corrects a related prose gap (Opus): the reference
#     client's call graph is `+1432 -> Object::getObjectById -> Network::getCachedObjectById ->
#     cachedNetworkIdGetObject` (four frames) — this plan's agent intentionally short-circuits to
#     calling `cachedNetworkIdGetObject` directly after computing the +1432 slot address itself
#     (skipping the three C++ wrapper frames, applying the IDENTICAL `!isAdvertisedClient()` gate at the
#     same logical point) for implementation simplicity; the reference client does NOT call the raw RVA
#     directly from the +1432 site, and this file's prose must not imply otherwise. The off-thread
#     lifetime hazard (Opus MEDIUM, T-05-32) applies to this wired LEGACY path exactly as it does to the
#     advertised two-step resolver below — 05-12's UAT watch item is updated to name both.
#
# (3) Clean-cut binding refinements — CONFIRMED against engine_advertise.cpp's actual, current source
#     this session (Codex/Cursor; not inferred):
#     - `network::getObjectById` (line 709, ROUND 5 corrected — was cited 707, Codex's re-read of current source found the row +2 lines down): catalog key is EXACTLY `"network::getObjectById"`, bound to
#       `&NetworkIdManager::getObjectById`, a `static Object* NetworkIdManager::getObjectById(const
#       NetworkId&)` (`NetworkIdManager.h:22` / `.cpp:72-79`) — the source comment states the consumer
#       typedef explicitly: `Object*(__cdecl*)(const NetworkId&)`. `__cdecl` STATIC, not `__thiscall`,
#       and NOT the bare C++ symbol name — use `"network::getObjectById"` verbatim as the catalog key.
#     - Advertised selected-object resolution is a REAL TWO-STEP, not one row (corrects this plan's
#       prior single-slot framing): `"cuiHud::g_instance"` (line 706, ROUND 5 corrected from 704) = `&SwgCuiHudFactory::
#       findMediatorForCurrentHud`, a STATIC zero-arg `SwgCuiHud* findMediatorForCurrentHud()`
#       (`SwgCuiHudFactory.h:24`) that resolves the LIVE ground/space HUD mediator — call this FIRST.
#       `"cuiHud::getTarget"` (line 705, ROUND 5 corrected from 703, NAME-MISMATCHED in the catalog — it is NOT a setter) =
#       `&engine_hudGetLastSelectedObject`, a `__fastcall(SwgCuiHud* pThis, int /*edx*/)` call-through
#       thunk over `SwgCuiHud::getLastSelectedObject() const` (`SwgCuiHud.h:95`); the source comment
#       states the consumer typedef explicitly: `Object*(__thiscall*)(SwgCuiHud*)` — call this SECOND,
#       passing the `SwgCuiHud*` returned by `cuiHud::g_instance` as its sole argument. Bind BOTH rows;
#       call them in this order (g_instance -> getTarget(instance)) every time the advertised focus is
#       resolved.
#     - `cuiHud::g_instance`'s exact consumer calling convention is not spelled out as explicitly in the
#       DLL source comment as the other two rows (no `__cdecl`/`__stdcall` keyword sits next to it) —
#       this file's own established convention for zero-arg static/free accessors is `__cdecl` (see
#       `engine_advertise.cpp:148`'s `config::loadOverrideConfig` comment, and the sibling
#       already-advertised `"cuiIo::g_instance"` -> `CuiManager::getIoWin` row of the identical shape,
#       line 548) — Task 1 binds it `__cdecl` on that precedent, flagged for the executor to
#       double-check against `SwgCuiHudFactory.h` if available at execution time.
#     - `cachedNetworkIdGetObject`'s citation is corrected from `network.cpp:39,42` to `network.cpp:35`
#       (typedef) `,42` (literal assignment) — `:39` is `idManagerGetObjectById`'s line, not this one.
#     - The "+1432 offset: validate live" note stays UNCHANGED (Opus: literal 1432 is legacy-layout
#       comment-only, unprovable from source without a debugger read).

# REVISION NOTE (--reviews replan ROUND 4, 2026-07-12): folds in 05-REVIEWS.md round-3 maintainer
# decisions #1, #2, #4, #5 (decision #3 lands in 05-11/05-12, not here).
# (1) HIGH — baseline vs. per-frame focus (D-03 restored). The agent's s_expectedTransform/Scale were
#     captured ONCE PER AGENT LIFETIME against whatever was in focus at attach (the player), never
#     re-keyed when the per-frame focus resolver (ROUND 2) later resolves a DIFFERENT object — so
#     selecting any object other than the attach-focus false-blocked its first write, and revertAll
#     force-wrote the attach-time (player) pose onto it. FIX (the maintainer's MINIMUM prescribed fix):
#     a new static `s_expectedCapturedAgainst` (raw `void*`, the focus pointer the current baseline was
#     captured against) is compared to the freshly-resolved `focus` every iteration; on a mismatch,
#     `s_expectedCaptured` resets to `false`, re-triggering the SAME first-sentinel-passing-read capture
#     path attach already used — now re-armed per focus-identity change, not just once ever. Build-agnostic
#     (compares the raw resolved pointer, needs no separate networkId/templateName plumbing on either
#     build) and restores D-03's locked "captured per object (once)" model.
# (2) HIGH — gate + unify the rebaseline (closes the ungated-rebaseline guard defeat). ROUND 3's
#     REBASELINE_GUARD mutation sat in the OUTER command-read scope, gated only on "a command was read
#     this frame" — NOT on the `cmdSeq`-newness gate that protects the apply. Because the command slot
#     is latest-wins, the rebaseline silently RE-RAN every frame in the `[revert, next-command]` window,
#     adopting whatever was live at that instant (including a post-revert external tamper) as the new
#     baseline — defeating the transform guard for an unbounded window. Also, scale rebaselined to the
#     CONSTANT `cmd.scale` (the revert target) while transform rebaselined to a fresh LIVE read — an
#     inconsistency Opus and Cursor each flagged from opposite ends (Opus: transform-to-live is the bug
#     because it's ungated; Cursor: scale-to-constant is the bug because a scale-only blocked revert on
#     legacy won't un-stick). ONE FIX closes both readings: (a) the REBASELINE_GUARD mutation now sits
#     LEXICALLY INSIDE the SAME `cmdSeq`-new + non-torn guard that gates the apply — once-per-command,
#     atomic with the apply, never re-evaluated on a stale/repeated `cmdSeq`; (b) BOTH channels now
#     rebaseline to the LIVE value read THIS gated instant (`currentScale`, the genuine member-offset
#     read, replaces round 3's `cmd.scale`) — scale now un-sticks like transform, and neither channel's
#     rebaseline is ungated. See Task 2's acceptance criteria for the line-order (lexical-scope) grep
#     that proves this structurally, not just by prose.
# (4) MEDIUM — off-thread Object* UAF (T-05-32). ACCEPT-WATCHED (unchanged, defensible for a
#     single-user tool) + MITIGATE (new this round): the entire resolve->read->apply span (both
#     builds' resolver chains, including the advertised two-step's intermediate `hudInstance` pointer
#     window) is now wrapped in SEH (`__try`/`__except`) so a UAF degrades to ONE skipped frame plus a
#     transient, published fault-liveness bit, instead of crashing the injected client.
# (5) Wiring/minors — the `network.cpp:35`/`:42` cachedNetworkIdGetObject citation was ALREADY correct
#     throughout this file (re-verified this session, no change needed); the Utinni path-prefix defect
#     WAS real (`swg/cui/cui_manager.cpp` -> corrected to the real `swg/ui/cui_manager.cpp`, fixed
#     above, 2 occurrences) — see 05-01's matching ROUND 4 note for the paired STOP-sticky-bit fix this
#     plan's Task 2 now publishes via `channelWriteGuardStatus(GUARD_FLAG_STOPPING, ...)`.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decisions #1, #2, and #4 (decision #3 lands in 05-11; #4's minors also touch 05-10). ALL round-3/round-4
# CLOSED findings (Change #2 rebaseline, Change #1 agent-side re-key, Change #4 SEH span, the 396
# layout, the STOP sticky bit) stay UNCHANGED — this round is additive, not a re-open.
# (1) HIGH (load-bearing) — identity unification, agent-side half. The agent already computes a
#     truncated `addrOf(focus)` for `guardAddr`, but only publishes it on a guard-checked apply, which
#     lags the agent's true current focus. FIX: the SAME truncated value (`focusToken`) is now
#     computed and published into 05-01's NEW read-frame field EVERY iteration (not just on
#     apply), giving 05-07's host-side re-key a value that can never disagree with the agent's own
#     `s_expectedCapturedAgainst` re-key — closing the fail-open two-same-template-object revert bug
#     and the lossy focus-flip A->B->A bug (both rooted in the host previously using a coarser
#     `(networkId, templateName)` key that collapses to `(0, templateName)` on legacy). See this file's
#     Task 2 behavior/action for the exact call-site.
# (2) MEDIUM-HIGH — the banned "closes ... for real" overclaim is struck from this file's
#     success_criteria (was: "closes the round-1 Sonnet HIGH finding for real"). The scoped claim now
#     matches this file's own objective section, which already correctly distinguished the
#     "always moves the player" gap (closed) from the viewport<->live-object binding gap (still open,
#     Phase 7).
# (4) Minors — pull `getPlayer()` inside the `__try` span (the SEH block now starts at the
#     `player = getPlayer()` call, not later at focus-resolution); soften the "only identity option"
#     framing around `s_expectedCapturedAgainst` (`Object::networkId` IS a real field, object.h:86,
#     this file simply doesn't bind a live read of it); fix the advertised catalog citations
#     (`network::getObjectById`/`cuiHud::g_instance`/`cuiHud::getTarget` all cited +2 lines off —
#     corrected to :709/:706/:705 respectively per Codex's re-read of current source; keys and
#     calling conventions were already correct, only line numbers drifted).

# REVISION NOTE (--reviews replan ROUND 6, 2026-07-12): folds in 05-REVIEWS.md ROUND 5 maintainer
# decision "(a) structural" — the agent-side mirror of 05-07's host-cache fix. Opus's round-5
# NEW-ISSUE finding is a DUAL failure: `s_expectedCapturedAgainst`'s raw-pointer-only compare has the
# IDENTICAL pointer-ABA blind spot as the host's pre-ROUND-6 identityCache — a despawned object's
# address reallocated to a DIFFERENT object skips re-capture here too, so THIS object's first legit
# edit false-tamper-blocks against the PRIOR object's baseline (the guard compares the new object's
# live bytes against stale expected values). ALL round-3/4/5 CLOSED work (the gated rebaseline, SEH
# span, STOP sticky bit, the FOCUS_TOKEN per-tick publish, the 400-byte layout) stays UNCHANGED — this
# round is additive and needs no new reads: `templateName`/`networkId` are already read every iteration
# to populate the read frame, this task simply also compares them for the re-key decision.
# (1) MEDIUM (Opus, structural close, option (a), agent-side half) — store the captured-against
#     `templateName`/`networkId` alongside `s_expectedCapturedAgainst` (not just the raw pointer), and
#     re-capture (`s_expectedCaptured = false`, re-arming `s_expectedCapturedAgainst = focus` and its
#     paired identity fields) when the resolved focus's pointer DIFFERS from `s_expectedCapturedAgainst`
#     OR its `templateName`/`networkId` differ from what was captured — closing the DUAL half of Opus's
#     pointer-ABA finding (05-07's Task 2 ROUND 6 note closes the host half; together they mean neither
#     side can silently disagree about whether a resolved `focus` is the SAME object a baseline was
#     captured against). The residual same-template + legacy-networkId≡0 + realloc-at-the-same-address
#     case (identity fields identical even though the underlying object changed) is explicitly NOT
#     addressed by this fix — accept-watched below (T-05-50), mirroring 05-07's T-05-46.
# (2) LOW minors folded here (mirrors 05-07's own ROUND 6 note, do not restate its full text): the
#     command-apply focus-skew (Opus round-5 Item 1 LOW) is added to this plan's threat register as an
#     explicit accept-watch entry (T-05-50) alongside the same-template-realloc residue (T-05-49) — this
#     plan's existing T-05-39/T-05-40 CLOSED entries stay exactly as written, these are NEW, narrower,
#     named threats, never folded into the existing entries' text.

must_haves:
  truths:
    - "On the ADVERTISED build, the agent resolves a 'focus object' each frame via the TWO-STEP cuiHud::g_instance -> cuiHud::getTarget(instance) resolver (the player's currently selected/picked in-game object) when it resolves non-null — the read frame's transform/networkId/templateName and every guarded write operate on this focus object, falling back to the player when nothing is selected or either step of the mechanism is unresolved (05-CONTEXT decision #1, ROUND 2 — this is a REAL cross-build fix, not a relabeling of the round-1 player fallback; the two-step mechanism CONFIRMED against real engine_advertise.cpp source, 2026-07-09)."
    - "On the LEGACY (SWGEmu) build, focus resolution is UNCHANGED from round 1: the player's current in-game TARGET via getPlayerCreatureObject()+1432 -> cachedNetworkIdGetObject, falling back to the player otherwise. This is a REAL, wired resolver (Game::getPlayerLookAtTargetObjectNetworkId -> Object::getObjectById -> Network::getCachedObjectById -> cachedNetworkIdGetObject) — Object::networkId (object.h:86) is a genuine field, so legacy was never incapable of targeting (2026-07-09 correction of a false premise; no behavior change)."
    - "targetUnavailableOnBuild (liveness bit3) means the resolution MECHANISM itself is unresolved on this build (missing RVA/catalog row, including either half of the advertised two-step resolver) — it is FALSE whenever the mechanism resolves but simply finds nothing currently selected/targeted (that is an honest hasTarget=false, mechanism-available state, distinguishable from a genuine build-capability gap)."
    - "getObjectById (NetworkId -> Object*) is bound on BOTH builds (legacy literal RVA 0x00B380E0 + advertised catalog row 'network::getObjectById', __cdecl static) as a reusable resolver primitive, per the project's standing 'SWGEmu is never descoped' rule — even though it is not on the ACTIVE per-frame focus-resolution path this phase: legacy's active path uses the CachedNetworkId sibling cachedNetworkIdGetObject (0x00B30160) via the +1432 chain instead (Object::networkId, object.h:86, IS a real field — legacy is not incapable of targeting, this RVA is simply a different, more-generic primitive than the active mechanism), and advertised uses the two-step cuiHud::g_instance -> cuiHud::getTarget resolver directly instead."
    - "The agent poll loop applies a pending write command (Move/Rotate via setTransform_o2w, Scale via setScale) ONLY when its OWN independent guard bit passes AND its resolved fn pointer is non-null; a write is never attempted through an unresolved setter (REVIEWS.md ROUND 1 BLOCKER fix A)."
    - "setScale is seeded nullptr and is set to the legacy RVA ONLY when the client is confirmed non-advertised (post-resolveFromExe) — it can never be a stale absolute address on the advertised build (REVIEWS.md ROUND 1 BLOCKER fix A)."
    - "SCALE_REFUSED (guard bit 0x2) is set ONLY on a genuine byte-level scale-guard mismatch where setScale IS resolved (a write was actually attempted and refused) — it is NEVER set merely because setScale is unresolved; that case is reported via the separate liveness bit4 scaleUnavailableOnBuild instead (REVIEWS.md ROUND 2 W2 fix — closes the false-tamper-banner-on-every-advertised-Scale-interaction bug)."
    - "scaleUnavailableOnBuild (liveness bit4) is computed exactly once, immediately after the post-resolveFromExe setScale conditional-seed, as isAdvertisedClient() && setScale == nullptr — and is OR'd into state.liveness every iteration thereafter (mirrors bit3's one-time-computed shape, zero new channel offset)."
    - "After a successful transform apply, the guard's expected-transform baseline is refreshed from a FRESH getTransform_o2w read-back, not from the command's raw target — this survives STATIC non-world-cell (interior) objects' float-rounded storage without false-failing write N+1; a dynamically-moving parent (m_attachedToObject = mount/vehicle/POB ship) is explicitly documented as NOT covered by this fix (the world-space read-back drifts every frame in that case, false-failing the guard — fails safe, not a defect) (REVIEWS.md Fix D + ROUND 2 static-cell caveat)."
    - "CMD_FLAG_REBASELINE_GUARD, when accompanied by a real transform/scale payload in the SAME command, rebaselines BOTH channels to that iteration's FRESH LIVE bytes (transform via getTransform_o2w read-back; scale via the genuine currentScale member-offset read, ROUND 4 — no longer cmd.scale) and THEN immediately applies that same command's payload through the normal guard+apply path in the SAME iteration — coalescing rebaseline and revert-apply into ONE seqlock write cycle, never a silent forward force-write of a value the agent did not just read or receive as the explicit revert target (REVIEWS.md ROUND 2 Opus MEDIUM fix — closes the two-command race, not just the original 'blocked guard blocks its own revert' bug)."
    - "The REBASELINE_GUARD mutation is LEXICALLY INSIDE the SAME cmdSeq-new + non-torn guard that gates the apply — never in the outer 'a command was read this frame' scope — so it can execute at most ONCE per distinct command and can never silently re-adopt a post-revert external tamper as the new baseline across repeated frames on the same latest-wins command slot (REVIEWS.md ROUND 3/round-4 maintainer decision #2 — closes the ungated-rebaseline guard defeat; proven by a line-order grep, not just prose)."
    - "The agent's guard baseline (s_expectedTransform/s_expectedScale) is RE-CAPTURED whenever the resolved focus object changes mid-session (a static s_expectedCapturedAgainst pointer is compared to the freshly-resolved focus every iteration; a mismatch resets s_expectedCaptured=false) — restoring D-03's locked 'captured per object (once)' model instead of a single once-per-agent-lifetime capture against whatever was in focus at attach. Selecting a NEW object after attach is editable on its own terms, not permanently guard-blocked against the original attach-time (player) pose (REVIEWS.md round-3 maintainer decision #1)."
    - "The agent publishes focusToken — static_cast<uint32_t>(reinterpret_cast<uintptr_t>(focus)), the IDENTICAL truncation already used for guardAddr — into the read frame's NEW FOCUS_TOKEN field (05-01 ROUND 5) EVERY iteration, not just at guard-check/apply time, so the host's re-key (05-07) can never lag or disagree with the agent's own s_expectedCapturedAgainst re-key above — this is the load-bearing identity-unification fix (REVIEWS.md round-4 maintainer decision #1), closing the fail-open two-same-template-object revert bug and the lossy focus-flip A->B->A bug that a coarser (networkId, templateName) host-side key produced on legacy (where networkId is always 0, Fix E)."
    - "The agent's baseline re-key stores the captured-against templateName/networkId alongside s_expectedCapturedAgainst's raw pointer, and re-captures (s_expectedCaptured=false) when the resolved focus's pointer DIFFERS from s_expectedCapturedAgainst OR its templateName/networkId differ from what was captured — never a pointer-only compare (ROUND 6, REVIEWS.md round-5 maintainer decision 'option (a) structural' — closes the agent-side half of Opus's pointer-ABA finding: a despawned object's address reallocated to a DIFFERENT object no longer skips re-capture merely because the raw pointer happens to match a stale baseline's key)."
    - "The entire per-iteration resolve->read->apply span (both builds' focus-resolution chains, including the advertised two-step resolver's intermediate hudInstance pointer window) runs inside a __try/__except (SEH) block; an access violation degrades to one skipped frame plus a transient liveness bit6 agentFaultRecovered, never a client crash (REVIEWS.md round-3 maintainer decision #4 — mitigates the accept-watched T-05-32 off-thread UAF without claiming it is eliminated)."
    - "On honoring STOP_REQUESTED, the agent publishes GUARD_FLAG_STOPPING (0x4, 05-01) via channelWriteGuardStatus BEFORE calling channelClose()/returning — giving a host-side detach retry-loop (05-07) a sticky, independently-observable acknowledgement that a later drag command overtaking the latest-wins command slot cannot erase (REVIEWS.md round-3 maintainer decision #5)."
    - "The agent stops its poll loop cleanly (no leaked thread) when the command slot's STOP_REQUESTED flag is set — checked at the TRUE TOP of every loop iteration, before the player-null early-continue (D-04 item 1, REVIEWS.md Fix F)."
    - "Legacy (SWGEmu) networkId remains UNAVAILABLE (0, sentinel not-applicable) rather than reading the wrong field — the +1432 offset is used ONLY for target resolution, never re-purposed as a networkId (REVIEWS.md Fix E; D-04 item 3 is explicitly NOT closed on legacy, corrected from the original plan)."
    - "The agent reads a genuine live m_scale value EVERY poll iteration to feed checkWriteGuard's liveScale argument — never the agent's own s_expectedScale fed back on itself — via a per-build-gated MEMBER-OFFSET read off the focus Object*, the SAME mechanism (and D-01 scope precedent) as the legacy +1432 target-slot read: on LEGACY this is UNCONDITIONAL (kLegacyScaleOffset = 0x44, a planner-computed, VALIDATE-LIVE constant per this file's own +1432 posture); on ADVERTISED this is GATED behind a one-time-computed s_advertisedScaleOffsetConfirmed bool (defaults false, no swg-client-v2 offset known this session) — closing the parity gap between Scale's and Transform's external-tamper detection wherever the offset is confirmed (2026-07-08 plan-check revision, maintainer decision option (a); RE-ARCHITECTED 2026-07-09 from an RVA/catalog-binding to a member-offset read per the de-anchoring crew's getScale-is-inline finding)."
    - "When the advertised m_scale offset is not yet confirmed (s_advertisedScaleOffsetConfirmed === false), the scale guard degrades honestly: it never latches a false SCALE_REFUSED tamper banner (falls back to comparing s_expectedScale against itself — the pre-fix behavior, never worse), AND that degraded state is published as liveness bit5 scaleGuardUnavailableOnBuild (REDEFINED 2026-07-09: 'advertised m_scale offset not yet confirmed', unconditionally false on legacy since the computed offset is always attempted there) — kept structurally separate from bit4 scaleUnavailableOnBuild (the WRITE-side setScale==nullptr signal), whose existing meaning is unchanged."
    - "A cross-repo confirmation-request handoff document under .planning/handoff/ names the exact Object::m_scale symbol (POD member, not an accessor call), both builds' asks (legacy: cross-check the planner-computed 0x44/68 candidate offset against a live debugger read; advertised: report the compiled m_scale member's real byte offset), and why — the toolkit never assumes a direct edit to ../Utinni or ../swg-client-v2, this is a NON-BLOCKING backup CONFIRMATION (the toolkit's own live-validated member-offset read already unblocks the phase without it, per the maintainer's 2026-07-09 decision), and the advertised offset's absence does not block this phase (documented known gap, same posture as D-09's advertised-Scale-write gap)."
  artifacts:
    - path: "packages/live-inject/agent/rva_table.cpp"
      provides: "setTransform_o2w write-slot (RVA 0x00B22CC0) + setScale slot (null-seeded, legacy-only-conditionally-set to RVA 0x00B23A10) + advertised binding rows + getPlayerCreatureObject (RVA 0x004251D0) + cachedNetworkIdGetObject (RVA 0x00B30160) for legacy target resolution + getObjectById bound on BOTH builds (legacy RVA 0x00B380E0 + advertised catalog row 'network::getObjectById', __cdecl static) + the advertised TWO-STEP selected-object resolver bound on the ADVERTISED build ('cuiHud::g_instance' catalog row + 'cuiHud::getTarget' catalog row, engine_advertise.cpp:705-706, ROUND 5 corrected from 703-704). (2026-07-09 revision: NO getScale slot exists in this file anymore — the scale-guard comparand moved to a member-offset read owned by agent_main.cpp, see that artifact entry.)"
      contains: "cuiHud::getTarget"
    - path: "packages/live-inject/agent/agent_main.cpp"
      provides: "60fps poll loop: top-of-loop stop-signal check (now wrapped in SEH from the getPlayer() call onward, ROUND 5), cross-build focus resolution (advertised: TWO-STEP cuiHud::g_instance -> cuiHud::getTarget(instance)-or-player; legacy: +1432-lookAt-target-or-player, unchanged), a per-tick focusToken publish into the read frame (ROUND 5, the load-bearing identity-unification field 05-07 re-keys on), independently-guarded Move/Rotate/Scale apply with the corrected SCALE_REFUSED-vs-scaleUnavailableOnBuild split, read-back-refreshed guard baseline, coalesced REBASELINE_GUARD-plus-apply handling, and a genuine live m_scale member-offset read (legacy: unconditional kLegacyScaleOffset; advertised: gated behind s_advertisedScaleOffsetConfirmed) feeding checkWriteGuard's liveScale argument, with honest scaleGuardUnavailableOnBuild degradation (2026-07-08 revision; RE-ARCHITECTED 2026-07-09 from an RVA/catalog binding to a member-offset read)"
      contains: "kLegacyScaleOffset"
    - path: ".planning/handoff/2026-07-09-phase05-m-scale-offset-confirmation-request.md"
      provides: "Cross-repo CONFIRMATION-backup spec for Object::m_scale's per-build byte offset (legacy: cross-check the planner-computed 0x44/68 candidate against a live debugger read, routed to the Utinni session; advertised: report the compiled member's real byte offset, routed to the swg-client-v2 session), per CLAUDE.md's cross-repo handoff constraint — non-blocking, source-corroboration only (the toolkit's own live-validated member-offset read already unblocks the phase, 2026-07-09 maintainer decision)"
      contains: "m_scale"
  key_links:
    - from: "packages/live-inject/agent/agent_main.cpp"
      to: "packages/live-inject/agent/write.cpp"
      via: "checkWriteGuard(currentXform, s_expectedTransform, currentScale, s_expectedScale) called each iteration a new command is seen — including immediately after a REBASELINE_GUARD rebaseline, same iteration; currentScale is a GENUINE live m_scale member-offset read (legacy: unconditional; advertised: gated behind s_advertisedScaleOffsetConfirmed — 2026-07-09 revision), never s_expectedScale fed back on itself"
      pattern: "checkWriteGuard"
    - from: "packages/live-inject/agent/rva_table.cpp"
      to: "engine_advertise.cpp (../swg-client-v2, external repo)"
      via: "advertised catalog keys 'object::setScale' (optional/known-gap, D-09), 'network::getObjectById' (:709, ROUND 5 corrected from 707), 'cuiHud::g_instance' (:706, ROUND 5 corrected from 704, static findMediatorForCurrentHud) and 'cuiHud::getTarget' (:705, ROUND 5 corrected from 703, __fastcall thunk over getLastSelectedObject) — ALL THREE targeting rows CONFIRMED present in engine_advertise.cpp by this session's direct source read (de-anchoring crew, 2026-07-09), no longer a 'confirm at execution time' unknown; object::getScale is NO LONGER a catalog dependency of this file (2026-07-09 revision — see the m_scale member-offset read instead)"
      pattern: "cuiHud::getTarget"
    - from: "packages/live-inject/agent/agent_main.cpp"
      to: ".planning/handoff/2026-07-09-phase05-m-scale-offset-confirmation-request.md"
      via: "s_advertisedScaleOffsetConfirmed stays false (advertised scale-guard comparand degrades to scaleGuardUnavailableOnBuild) until a dedicated Utinni-session or swg-client-v2-session response supplies a confirmed per-build m_scale offset — the handoff document is the single source of truth for what's still needed; NON-BLOCKING, the legacy read is already live via kLegacyScaleOffset regardless of this handoff's status"
      pattern: "s_advertisedScaleOffsetConfirmed"
    - from: "packages/live-inject/agent/agent_main.cpp"
      to: "packages/live-inject/agent/channel.h"
      via: "state.focusToken = static_cast<uint32_t>(reinterpret_cast<uintptr_t>(focus)) assigned EVERY iteration before channelWrite(&state) (05-01 ROUND 5 read-frame field) — the load-bearing identity-unification value 05-07's host-side cowSnapshot/writeLog re-key consumes"
      pattern: "focusToken"
---

<objective>
Wire the guarded write path into the agent's poll loop: resolve the setter endpoints
(`setTransform_o2w` write-variant, `setScale` — the latter NULL-SAFE per the BLOCKER fix), consume the
command slot from 05-01 each frame, gate every apply through the now-independent transform/scale guard,
resolve a per-build "focus object" so the write moves what the player has actually targeted/selected
rather than always the player's own avatar — LEGACY via the existing +1432 lookAt-target chain, and
ADVERTISED via a REAL two-step selected-object resolver this round adds (05-CONTEXT decision #1, ROUND
2 — the stronger cross-build path Codex's ground-truth read found, closing the gap round 1's legacy-only
fix left open) — add the D-04 agent-lifecycle debt (clean stop-signal read at the true top of the loop),
split the Scale-guard-vs-unavailable-on-build signal so advertised-build Scale never shows a false
tamper banner (ROUND 2 W2 fix), coalesce the REBASELINE_GUARD un-stick sequence into one race-free
command (ROUND 2 Opus MEDIUM fix), raise the poll rate to 60fps, and — closing a plan-check BLOCKER —
read a genuine live `m_scale` member value (per-build-gated member-offset read, mirroring the +1432
precedent; RE-ARCHITECTED 2026-07-09 from an RVA/catalog binding after the de-anchoring crew confirmed
`Object::getScale()` is inline with no standalone address) so the scale guard reaches the SAME real
external-tamper-detection parity Transform's guard already has, degrading honestly (never a false
tamper, never a silently-unverified pass) wherever that offset is not yet confirmed on a build.

Purpose: this is the load-bearing agent-side half of LIVE-03 — the point where "drag a gizmo" becomes
"the object actually moves in the running client," gated by the fail-closed guard from 05-01 (SC #2),
covering Move/Rotate/Scale on both in-scope targets per D-09, doing so WITHOUT crashing the advertised
client, WITHOUT silently moving the wrong object on EITHER build, WITHOUT a false tamper banner when
Scale is simply unresolved on a build, and WITHOUT a guard that only APPEARS to protect Scale while
actually comparing the toolkit's own last-written value to itself.

Output: extended `rva_table.cpp` + `agent_main.cpp` + a completed `applyWrite` in `write.cpp` + a
cross-repo confirmation-backup handoff spec under `.planning/handoff/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
@.planning/research/CONSULT-05-SYNTHESIS.md
@D:/Code/Utinni/UtinniCore/swg/object/object.cpp
@D:/Code/Utinni/UtinniCore/swg/object/object.h
@D:/Code/Utinni/UtinniCore/swg/game/game.cpp
@D:/Code/Utinni/UtinniCore/swg/misc/network.cpp
@D:/Code/Utinni/UtinniCore/swg/misc/swg_math.h
@D:/Code/Utinni/UtinniCore/swg/ui/cui_manager.cpp
@D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp
@D:/Code/swg-client-v2/src/engine/shared/library/sharedObject/src/shared/object/Object.h
</context>

<interfaces>
<!-- Existing rva_table.cpp slots to extend (verbatim structure to mirror, VERIFIED against current source this session) -->
```
pGetTransform_o2w getTransform_o2w = (pGetTransform_o2w)0x00B22C80;
pGetPlayer        getPlayer        = (pGetPlayer)0x00425140;
Binding g_agentBindings[] = {
    {"object::getTransform_o2w",      (void**)&getTransform_o2w},
    {"game::getPlayer",               (void**)&getPlayer},
    ...
};
size_t g_agentBindingCount = sizeof(g_agentBindings) / sizeof(g_agentBindings[0]);
```

<!-- Existing agent_main.cpp poll loop shape (lines 132-222, VERIFIED against current source this session) — the loop this task extends -->
```
while (true) {
    void* player = swg::endpoints::getPlayer ? swg::endpoints::getPlayer() : nullptr;
    if (!player) { Sleep(100); continue; }   // <-- BUG (Fix F): this runs BEFORE any command/stop read
    // ... existing read of transform/networkId/templateName/liveness, 4-sentinel gate, channelWrite ...
    Sleep(33);  // ~30fps — this task raises to ~16ms (60fps)
}
```

<!-- Ground truth RVAs (Utinni object.cpp / game.cpp / network.cpp — ALL VERIFIED against current source this session, not carried unverified from review prose) -->
- setTransform_o2w = 0x00B22CC0 (Utinni object.cpp:148) — same address family as the existing getTransform_o2w=0x00B22C80
- setScale = 0x00B23A10 (Utinni object.cpp:155)
- getPlayer = 0x00425140 (Utinni game.cpp:73) — ALREADY bound; this is the accessor our existing "player" pointer comes from
- getPlayerCreatureObject = 0x004251D0 (Utinni game.cpp:74) — a DIFFERENT accessor than getPlayer; the CreatureObject wrapper the +1432 target-slot offset is measured FROM. Legacy target resolution only, UNCHANGED from round 1.
- +1432 target-slot offset: `(uint8_t*)getPlayerCreatureObject() + 1432` is a CachedNetworkId pointer
  embedded in the CreatureObject struct (Utinni game.cpp:726-733, comment: "the +1432 lookAt-target
  slot is a RAW CreatureObject byte-offset"). This is NOT the object's own networkId (Fix E) — it is
  the player's CURRENT TARGET slot, used ONLY for LEGACY object-targeting. **Comment-backed, not
  independently proven — VALIDATE LIVE** (ROUND 2 minor correction; treat as a harvested constant to
  confirm during the 05-12 UAT, not as an independently-source-proven literal).
- cachedNetworkIdGetObject = 0x00B30160 (Utinni network.cpp:35 typedef, :42 literal — `__thiscall`,
  `pThis` = the CachedNetworkId pointer computed above; corrects a stray `:39` mis-citation in a prior
  revision, which pointed at `idManagerGetObjectById`'s line by mistake) resolves that pointer to an
  `Object*`. Utinni's own `Network::getCachedObjectById` (network.cpp:74-85) gates this call behind
  `!isAdvertisedClient()` (returns nullptr on advertised: "an UNADVERTISED SWGEmu literal... on the
  advertised client it lands on relocated code"). This plan's agent MUST apply the identical
  `!isAdvertisedClient()` gate before ever calling `cachedNetworkIdGetObject` — do not call it on the
  advertised path under any condition. LEGACY-ONLY, unchanged from round 1. REAL CALL GRAPH (Opus,
  2026-07-09 — do not describe this as "the +1432 chain calls the raw RVA directly", it does not, in
  the reference client): `Game::getPlayerLookAtTargetObjectNetworkId()` computes the +1432 address
  (game.cpp:733) -> `Game::getPlayerLookAtTargetObject()` calls `Object::getObjectById(lookAtId)`
  (game.cpp:745) -> `Object::getObjectById` (object.cpp:260-268) calls `Network::getCachedObjectById`
  (network.cpp:74-85, the single guarded choke point) -> which calls THIS RVA (network.cpp:84). This
  plan's agent intentionally short-circuits to calling this RVA directly after computing the +1432
  address itself (skipping the three C++ wrapper frames, applying the identical gate at the same
  logical point) — a deliberate implementation simplification, not a misreading of the reference call
  graph.

<!-- ROUND 2 ground truth — object-targeting scope, 05-CONTEXT decision #1b (LOCKED, treat as
     verified, cited by Codex against real source this review round — do not re-derive; catalog keys
     and calling conventions further CONFIRMED by the 2026-07-09 de-anchoring crew, see below) -->
- idManagerGetObjectById = 0x00B380E0 (Utinni network.cpp:32 typedef, :39 literal —
  `pIdManagerGetObjectById = utinni::Object*(__cdecl*)(const int64_t& id)`, CONFIRMED this session
  directly from source, not inferred) — a GENERIC NetworkId->Object* resolver on the LEGACY build.
  Bind it as a reusable primitive (per this project's "SWGEmu is never descoped" rule) — it is NOT on
  the active per-frame focus-resolution path this phase. CORRECTED FRAMING (2026-07-09, drops a false
  premise a prior revision stated): this is NOT because "legacy has no independent networkId source" —
  `Object::networkId` IS a real flat `int64_t` field (object.h:86). It is because the existing +1432
  chain already resolves the target object directly via the CachedNetworkId sibling
  `cachedNetworkIdGetObject` (0x00B30160, see "Ground truth RVAs" above), a different, more-specific,
  already-active mechanism — this RVA remains a reusable primitive for a future networkId-driven flow
  (e.g. jump-to-networkId), not dead code, and not evidence of a legacy capability gap.
- Advertised getObjectById-equivalent = `NetworkIdManager::getObjectById`, catalog KEY CONFIRMED
  EXACTLY `"network::getObjectById"` (`engine_advertise.cpp:709`, ROUND 5 corrected from 707), bound to `&NetworkIdManager::
  getObjectById`, `static Object* NetworkIdManager::getObjectById(const NetworkId&)`
  (`NetworkIdManager.h:22` / `.cpp:72-79`); the source comment states the consumer typedef explicitly:
  `Object*(__cdecl*)(const NetworkId&)` — `__cdecl` STATIC, NOT `__thiscall`, and NOT the bare C++
  symbol name. Bind it as a reusable primitive on the advertised build (symmetric with the legacy
  binding above) — NOT on the active focus-resolution path this phase, for the same "more-specific
  mechanism already active" reason as legacy (the two-step `cuiHud::g_instance`/`cuiHud::getTarget`
  resolver below is what's active on advertised).
- Advertised selected/picked-object accessor is a REAL TWO-STEP (CONFIRMED 2026-07-09 — corrects this
  plan's prior single-row framing), wrapped by Utinni as `CuiManager::getSelectedObject()`
  (`cui_manager.cpp:183-205`). THIS is the ACTIVE focus-resolution mechanism for the ADVERTISED build —
  it directly answers "what does the player currently have selected/picked in-game," the closest
  advertised-build analog to legacy's lookAt-target chain, and the only reachable mechanism that can
  make the gizmo move something other than the player on the advertised build. STEP 1 — catalog key
  EXACTLY `"cuiHud::g_instance"` (`engine_advertise.cpp:706`, ROUND 5 corrected from 704), bound to `&SwgCuiHudFactory::
  findMediatorForCurrentHud`, a STATIC zero-arg `SwgCuiHud* findMediatorForCurrentHud()`
  (`SwgCuiHudFactory.h:24`) that resolves the LIVE ground/space HUD mediator instance — call this
  FIRST, every resolution. Calling convention not spelled out in the DLL source comment; bind `__cdecl`
  per this file's own established zero-arg-static-accessor convention (`engine_advertise.cpp:148`'s
  `config::loadOverrideConfig` comment; the sibling already-advertised `"cuiIo::g_instance"` ->
  `CuiManager::getIoWin` row, line 548, is the identical shape) — double-check against
  `SwgCuiHudFactory.h` if available at execution time. STEP 2 — catalog key EXACTLY
  `"cuiHud::getTarget"` (`engine_advertise.cpp:705` — ROUND 5 corrected from 703, NAME-MISMATCHED in the catalog, this is a
  getter, not a setter), bound to `&engine_hudGetLastSelectedObject`, a `__fastcall(SwgCuiHud* pThis,
  int /*edx*/)` call-through thunk over `SwgCuiHud::getLastSelectedObject() const` (`SwgCuiHud.h:95`);
  the source comment states the consumer typedef explicitly: `Object*(__thiscall*)(SwgCuiHud*)` — call
  this SECOND, passing STEP 1's returned `SwgCuiHud*` as its sole argument. Both catalog strings and
  both calling conventions are CONFIRMED directly against `engine_advertise.cpp`'s current source this
  session (Codex/Cursor) — do not fabricate alternates.
- Explicitly confirmed by Codex this round: there is NO template->live-Object* resolver on EITHER
  build — template targeting requires a known NetworkId or an enumeration/search path, neither of
  which this plan builds. Do not invent one.

<!-- MEMBER-OFFSET ground truth (de-anchoring crew, 2026-07-09) — Object::m_scale, the read-side
     guard-parity fix, RE-ARCHITECTED from an RVA/catalog-binding attempt (2026-07-08 plan-check
     revision) after the crew confirmed `Object::getScale()` is INLINE with no standalone address on
     most builds. `m_scale` is a plain POD member — read it by raw offset, the SAME mechanism (and same
     D-01 scope precedent) as this file's existing legacy `+1432` target-slot read, NOT a
     Binding-array-resolved accessor call. -->
- `Object::getScale()` — `inline const Vector &getScale() const { return m_scale; }`
  (`D:/Code/swg-client-v2/src/engine/shared/library/sharedObject/src/shared/object/Object.h:512-515`);
  `m_scale` itself declared at `Object.h:456` as `Vector m_scale;` — a plain 3-float POD member, cited
  here ONLY to explain WHY no getScale RVA/catalog binding exists (there is no standalone, addressable
  out-of-line copy to bind on either build) — this plan does NOT bind an accessor for it.
- LEGACY member-offset candidate: `kLegacyScaleOffset = 0x44` (68 decimal). Planner-computed this
  session by summing member sizes in Utinni's own `Object` struct — the SAME hand-verified
  memory-layout reflection struct this file's other legacy literals (`networkId`, `parentObject`, etc.)
  are harvested from (`D:/Code/Utinni/UtinniCore/swg/object/object.h:76-107`; `scale` field declared at
  line 94) — under the assumption of standard MSVC x86 alignment (`int64_t networkId` needs 8-byte
  alignment, inserting 4 bytes of padding after `char* name`; no `#pragma pack` directive exists
  anywhere in `UtinniCore`, grep-confirmed this session) and `swgptr` = 4 bytes (`utinni.h:36`):
  `vtbl`+`unk01`+`unk02`+`unk03`+`objectTemplate`+`unk04`+`name` = 7 × 4B = 28B; +4B alignment padding
  to 8-align `networkId` = 32B; +`networkId` (8B) = 40B; +`appearance`+`controller`+`unk07`+
  `parentObject`+`unk08`+`dpvsObjects`+`rotations` = 7 × 4B = 28B -> 68B (0x44) total before `scale`
  (`swg::math::Vector`, `swg_math.h:47-51`, 3 floats) begins. This is a COMPUTED, NOT
  disassembly-confirmed, value — apply the IDENTICAL "VALIDATE LIVE" posture the +1432 constant already
  carries in this file (05-12's UAT is where a wrong computed offset would surface as a scale-guard
  false-fail/false-pass; if it does, the live-observed value supersedes this one). Declared as a plain
  compile-time constant in agent_main.cpp (Task 2) — mirrors +1432's own inline-arithmetic idiom (NOT a
  Binding-array slot; there is nothing to "resolve" here).
- ADVERTISED member-offset: UNKNOWN this session — swg-client-v2 is a different, modernized codebase
  with its own compiled `Object` member layout; no source-derivable offset exists without disassembly
  or a debugger read against the maintainer's actual advertised build. GATE this read behind a
  `s_advertisedScaleOffsetConfirmed` bool (defaults `false`, mirrors bit4's `setScale == nullptr`
  write-side gate shape exactly) rather than guessing a value — do NOT hardcode any numeric advertised
  offset in this plan or its implementation.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve write + target-resolution endpoints (rva_table.cpp) — setTransform_o2w write-slot, null-safe setScale, legacy target-chain RVAs, cross-build getObjectById + advertised TWO-STEP selected-object resolver, plus an m_scale-offset cross-repo CONFIRMATION handoff</name>
  <files>packages/live-inject/agent/rva_table.cpp, .planning/handoff/2026-07-09-phase05-m-scale-offset-confirmation-request.md, .planning/handoff/README.md</files>
  <read_first>
    - packages/live-inject/agent/rva_table.cpp (full file — the exact Binding/slot pattern; every new row must follow this verbatim)
    - packages/live-inject/agent/resolve.h and resolve.cpp (full files — Binding struct, resolve()'s graceful-degrade contract: a missing advertised name NEVER nulls the slot; resolveFromExe() sets `s_advertisedClient` BEFORE calling resolve(), which is why a POST-resolve conditional-seed step is possible and correct)
    - D:/Code/Utinni/UtinniCore/swg/object/object.cpp (lines 43-190 — setTransform_o2w RVA 0x00B22CC0 at :148, setScale RVA 0x00B23A10 at :155, calling-convention typedefs; lines 255-268 — `Object::getObjectById`'s implementation, the missing call-graph link between game.cpp:745 and network.cpp:74-85 — cite this so the "+1432 chain calls the raw RVA directly" mischaracterization is not repeated)
    - D:/Code/Utinni/UtinniCore/swg/object/object.h (lines 76-120 — the `Object` struct's hand-verified legacy memory layout; `scale` field at :94; `getObjectById` declared at :109 — cited here for the `kLegacyScaleOffset` member-order/size computation the handoff doc's legacy ask restates)
    - D:/Code/Utinni/UtinniCore/swg/misc/swg_math.h (lines 47-65 — the `Vector` struct, 3 floats, used to size the `scale` member for the handoff doc's offset citation)
    - D:/Code/Utinni/UtinniCore/swg/game/game.cpp (lines 41-98 for getPlayer/getPlayerCreatureObject RVAs at :73-74; lines 704-746 for the legacy target-resolution chain, its `isAdvertisedClient()` gate, and the `Object::getObjectById(lookAtId)` call at :745)
    - D:/Code/Utinni/UtinniCore/swg/misc/network.cpp (lines 30-58, 61-100 — cachedNetworkIdGetObject typedef at :35, RVA 0x00B30160 literal at :42, `Network::getCachedObjectById`'s `isAdvertisedClient()` gate at :74-85, raw RVA call site at :84; lines 30-45 — idManagerGetObjectById typedef at :32 (`__cdecl`, `const int64_t&`), RVA 0x00B380E0 literal at :39)
    - D:/Code/Utinni/UtinniCore/swg/ui/cui_manager.cpp (lines 183-205 — `CuiManager::getSelectedObject()`'s wrapper shape over the advertised two-step `cuiHud::g_instance`/`cuiHud::getTarget` accessors)
    - D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp (lines 355-377 and 698-709 — the CONFIRMED catalog rows and thunk implementations for `network::getObjectById` (:709, ROUND 5 corrected from 707), `cuiHud::getTarget` (:705, ROUND 5 corrected from 703, `engine_hudGetLastSelectedObject`, :374-377), and `cuiHud::g_instance` (:706, ROUND 5 corrected from 704); lines 568-598 for setTransform_o2w/setScale confirmation)
    - D:/Code/swg-client-v2/src/engine/shared/library/sharedObject/src/shared/object/Object.h (lines 452-460 and 505-515 — `m_scale`'s declaration and `getScale()`'s inline definition; cited ONLY to justify why no getScale binding exists, this task does not bind it)
    - .planning/handoff/README.md (the existing handoff index — this task adds ONE new newest-first entry following its established format)
  </read_first>
  <action>
    Add a new function-pointer typedef `typedef void(__thiscall* pSetTransform_o2w)(void*, const
    void*);` (opaque Transform* argument, mirrors the existing `pGetTransform_o2w` opaque-pointer
    convention exactly — do not introduce a concrete Transform struct dependency) and a slot
    `pSetTransform_o2w setTransform_o2w = (pSetTransform_o2w)0x00B22CC0;` seeded with the legacy RVA
    (this endpoint IS confirmed advertised — engine_advertise.cpp:578/850 — so unconditional seeding is
    safe: resolve() will overwrite it by name on the advertised path, and the legacy literal is correct
    on the legacy path). Add `typedef void(__thiscall* pSetScale)(void*, const void*);` (opaque Vector*
    argument) and seed it **`pSetScale setScale = nullptr;`** — do NOT seed this with the legacy RVA
    literal at declare time, because `resolve()`'s graceful-degrade contract (a missing advertised name
    leaves the slot UNTOUCHED) would otherwise leave a legacy absolute address active and callable on
    the advertised build, where it is not valid code. Append ONE row to `g_agentBindings[]` for
    `setTransform_o2w`: `{"object::setTransform_o2w", (void**)&setTransform_o2w}` — do NOT add a
    binding row for `setScale` (the SAFETY here comes from the POST-resolve conditional-seed in Task
    2's `agent_init`, not from a binding row; add the row anyway for when the maintainer adds the
    upstream catalog entry: `{"object::setScale", (void**)&setScale}`, so it resolves automatically the
    moment the upstream row exists). Add two slots for LEGACY-ONLY target resolution (UNCHANGED from
    round 1 — see Interfaces for the `isAdvertisedClient()` gating requirement, which Task 2 enforces
    at the call site, not here): `typedef void*(__cdecl* pGetPlayerCreatureObject)();` with
    `pGetPlayerCreatureObject getPlayerCreatureObject = (pGetPlayerCreatureObject)0x004251D0;` (Utinni
    game.cpp:74 — no advertised catalog name in the source read this session, so do NOT add a
    binding-array row; the legacy literal is only ever called from behind an explicit
    `!isAdvertisedClient()` runtime check in agent_main.cpp) and
    `typedef void*(__thiscall* pCachedNetworkIdGetObject)(void*);` with
    `pCachedNetworkIdGetObject cachedNetworkIdGetObject = (pCachedNetworkIdGetObject)0x00B30160;`
    (Utinni network.cpp:35 typedef, :42 literal — same no-binding-row, gated-call-site treatment).
    Update BOTH slots' inline comment to add "VALIDATE LIVE — comment-backed offset, not independently
    source-proven" for the +1432 target-slot arithmetic specifically (the arithmetic itself is computed
    at the agent_main.cpp call site in Task 2, but document the caveat here where the two RVAs it
    depends on are declared).

    Add the cross-build resolver primitives per 05-CONTEXT decision #1b (bind on BOTH builds; NOT on
    the active focus-resolution path this phase — see the Interfaces section and Task 2's behavior spec
    for why): `typedef utinni::Object*(__cdecl*)(const int64_t& id) pIdManagerGetObjectById;`
    (CONFIRMED this session directly against Utinni network.cpp:32 — mirror this exact signature/
    convention, do not deviate) with `pIdManagerGetObjectById getObjectByIdLegacy =
    (pIdManagerGetObjectById)0x00B380E0;` (Utinni network.cpp:39 — LEGACY-ONLY literal, no binding
    row, same gated-call-site treatment as the two slots above — comment: "bound per project's 'SWGEmu
    never descoped' rule; the ACTIVE legacy focus path uses the CachedNetworkId sibling 0x00B30160 (via
    the +1432 chain) instead — Object::networkId (object.h:86) IS a real field, legacy is not incapable
    of targeting; this is simply a reusable, more-generic primitive for a future networkId-driven
    flow"). Add a matching advertised slot `typedef Object*(__cdecl*)(const void* networkId)
    pNetworkIdManagerGetObjectById;` (adjust the exact `NetworkId` parameter representation to whatever
    this codebase's opaque-pointer convention for a NetworkId-shaped argument already is — confirm
    against resolve.h/any existing NetworkId-shaped slot if one exists, otherwise treat it as an opaque
    pointer-sized value; the calling convention itself, `__cdecl` static, is CONFIRMED, not a guess)
    seeded to `nullptr` (mirrors `setScale`'s null-safe pattern) and add ONE binding row using the
    catalog key CONFIRMED this session as EXACTLY `{"network::getObjectById",
    (void**)&getObjectByIdAdvertised}` (engine_advertise.cpp:709, ROUND 5 corrected from 707, bound to
    `&NetworkIdManager::getObjectById`, `static Object* NetworkIdManager::getObjectById(const
    NetworkId&)`, `NetworkIdManager.h:22`/`.cpp:72-79`). Add TWO new slots for the advertised
    TWO-STEP selected-object resolver (CONFIRMED 2026-07-09, replaces this plan's prior single-slot
    framing): (1) `typedef void*(__cdecl* pCuiHudGetInstance)();` with
    `pCuiHudGetInstance cuiHudGetInstance = nullptr;` (advertised-only, catalog-resolved, null-safe
    like `setScale`) and binding row `{"cuiHud::g_instance", (void**)&cuiHudGetInstance}`
    (engine_advertise.cpp:706, ROUND 5 corrected from 704, bound to `&SwgCuiHudFactory::findMediatorForCurrentHud`, a STATIC
    zero-arg accessor — `__cdecl` per this file's own established zero-arg-static-accessor convention,
    double-check against `SwgCuiHudFactory.h` if available); (2) `typedef void*(__thiscall*
    pCuiHudGetTarget)(void*);` (opaque `SwgCuiHud*` argument, mirrors this file's opaque-pointer
    convention) with `pCuiHudGetTarget cuiHudGetTarget = nullptr;` and binding row
    `{"cuiHud::getTarget", (void**)&cuiHudGetTarget}` (engine_advertise.cpp:705 — ROUND 5 corrected from 703, NAME-MISMATCHED in
    the catalog, this is `SwgCuiHud::getLastSelectedObject() const`, `SwgCuiHud.h:95`, exposed as a
    `__fastcall(SwgCuiHud*, int)` call-through thunk whose CONFIRMED consumer typedef per the DLL
    source's own comment is `Object*(__thiscall*)(SwgCuiHud*)` — use `__thiscall` here, not
    `__fastcall`, matching that comment exactly). Both TWO-STEP slots follow the SAME graceful-degrade
    contract as every other advertised binding (resolve() leaves them null on a name-miss — Task 2 must
    never call either unresolved, and must call `cuiHudGetInstance()` FIRST, then
    `cuiHudGetTarget(instance)` SECOND, passing STEP 1's result). Do NOT add any `k_legacyNetworkIdOffset`
    constant and do NOT implement a legacy networkId read anywhere in this file — REVIEWS.md Fix E
    still applies unchanged: the `+1432` offset is the player's TARGET slot, not the object's own
    networkId; legacy networkId stays unavailable. Update every new row's inline comment to cite the
    exact Utinni/engine_advertise file:line the way every existing row already does.

    Do NOT add any `getScale`/`m_scale`-related binding, typedef, or slot to this file (2026-07-09
    revision — the scale-guard comparand is now a member-offset read owned entirely by agent_main.cpp/
    Task 2, not a Binding-array-resolved accessor; this file has NO remaining getScale-adjacent
    responsibility).

    Then create `.planning/handoff/2026-07-09-phase05-m-scale-offset-confirmation-request.md` (a NEW
    file — use the Write tool) as a concrete cross-repo CONFIRMATION-BACKUP spec (not a work-summary
    narrative like this directory's other handoffs, and NOT a blocking ask — the toolkit already
    unblocks itself with a live-validated member-offset read, see agent_main.cpp/Task 2), containing:
    the symbol (`Object::m_scale`, a plain POD `Vector` member, with the exact declaration quoted from
    swg-client-v2 `Object.h:456`, plus `getScale()`'s inline definition from `Object.h:512-515` as the
    reason NO accessor RVA/catalog binding was attempted); WHY (D-03's locked read-verify guard needs a
    genuine live-read comparand for Scale to reach the SAME external-tamper-detection parity
    Transform's guard already has via `getTransform_o2w` — cite this plan's top-of-file REVISION NOTEs);
    the planner-computed LEGACY candidate offset `0x44` (68 decimal) with its full derivation (member
    sizes summed from Utinni's own `object.h:76-107` `Object` struct, `scale` field at `object.h:94`,
    assuming standard MSVC x86 alignment — restate the exact computation from this plan's Interfaces
    section so a reader does not have to cross-reference); TWO DISTINCT asks, one per dedicated
    session, each clearly labeled NON-BLOCKING: (1) **Utinni/legacy session** — CONFIRM or CORRECT the
    planner-computed `0x44` candidate against a live debugger read of the compiled legacy client (build
    `0.0.119.798` or the maintainer's current build) — this is a narrower, cheaper ask than the
    original "discover an RVA from scratch" request, since a concrete starting hypothesis already
    exists; (2) **swg-client-v2/advertised session** — report the compiled `Object` class's `m_scale`
    member byte offset directly (no RVA/hookpoint-row addition needed — this is a read-only offset, not
    a callable accessor; the D-09 `object::setScale` write-side hookpoint-row ask stays a SEPARATE,
    already-tracked item in this plan's `user_setup` frontmatter). State explicitly that BOTH asks are
    NON-BLOCKING BACKUP CONFIRMATION — the toolkit's own live-validated member-offset read (legacy:
    unconditional; advertised: gated behind `s_advertisedScaleOffsetConfirmed`, defaulting `false` and
    therefore degrading honestly via liveness bit5 `scaleGuardUnavailableOnBuild` until a session
    responds) already unblocks the phase without either response; briefly describe that handling so a
    future reader can verify resolution. Finally, add ONE new newest-first entry to
    `.planning/handoff/README.md` for this file, following the existing index's established one-
    paragraph format exactly (status tag, one-sentence summary, both asks named, non-blocking noted,
    and a note that this SUPERSEDES the retired `2026-07-08-phase05-getscale-accessor-request.md` ask
    with a narrower, offset-only request).
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `setScale` is declared and initialized to `nullptr`, NOT to `(pSetScale)0x00B23A10` (grep confirms `pSetScale setScale = nullptr;`).
    - `g_agentBindings[]` contains exactly TWO round-1 entries (`"object::setTransform_o2w"`, `"object::setScale"`) PLUS THREE cross-build-targeting entries (`"network::getObjectById"`, `"cuiHud::g_instance"`, `"cuiHud::getTarget"`), each with a comment citing the exact source line the literal/catalog name was harvested from.
    - `getPlayerCreatureObject`, `cachedNetworkIdGetObject`, and `getObjectByIdLegacy` are all NOT present in `g_agentBindings[]` (grep confirms no binding row references any of the three — they resolve only via the conditional legacy literal Task 2 applies, gated on `!isAdvertisedClient()`), and all three carry a "LEGACY-ONLY... gate on `!isAdvertisedClient()`" comment.
    - `getObjectByIdLegacy`'s typedef matches the CONFIRMED signature `utinni::Object*(__cdecl*)(const int64_t& id)` (grep — no `uint64_t`/other calling-convention placeholder remains).
    - The three cross-build-targeting advertised slots (the `network::getObjectById`-bound slot, `cuiHudGetInstance`, `cuiHudGetTarget`) are all seeded `nullptr` at declare time (grep confirms — mirrors the `setScale` null-safe pattern, never a stale cross-build literal); `cuiHudGetTarget`'s typedef uses `__thiscall`, NOT `__fastcall` (grep — matches the DLL source's own stated consumer typedef).
    - No `object::getScale` or `object::m_scale`-shaped catalog string, typedef, or slot exists anywhere in this file (grep -c returns 0 for all three) — the scale-guard comparand is entirely agent_main.cpp's responsibility (2026-07-09 revision).
    - No `k_legacyNetworkIdOffset` symbol exists anywhere in this file (grep -c returns 0).
    - `.planning/handoff/2026-07-09-phase05-m-scale-offset-confirmation-request.md` exists and names both asks concretely (grep confirms `Utinni`, `engine_advertise.cpp`, and the literal string `Object::m_scale` all appear in the file), and states the planner-computed `0x44` candidate offset.
    - `.planning/handoff/README.md` has a new newest-first entry (first bullet under "Active handoffs") pointing at the new handoff file.
    - The x86 agent DLL still compiles cleanly (no new linkage errors).
  </acceptance_criteria>
  <done>setTransform_o2w resolves via the existing single graceful-degrade mechanism; setScale is null-safe by construction; the legacy target-resolution RVAs are unchanged from round 1; the cross-build getObjectById primitive is bound on both builds (legacy literal, advertised catalog row `network::getObjectById`, both honestly documented as reusable-not-yet-active — legacy has a real networkId field, it simply uses a more-specific active mechanism); the advertised TWO-STEP selected-object resolver (`cuiHud::g_instance` -> `cuiHud::getTarget`) is bound null-safe and ready for Task 2 to wire as the ADVERTISED build's real focus-resolution mechanism; no getScale/m_scale binding exists in this file (that responsibility moved entirely to agent_main.cpp's member-offset read); and a concrete, routable, NON-BLOCKING cross-repo confirmation handoff spec exists naming exactly what each dedicated session needs to CONFIRM about the m_scale offset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poll-loop integration — top-of-loop stop signal, cross-build focus-object resolution (TWO-STEP advertised), independently-guarded apply with the SCALE_REFUSED/scaleUnavailableOnBuild split, read-back guard baseline, coalesced REBASELINE_GUARD-plus-apply, gated m_scale member-offset read as the scale-guard comparand</name>
  <files>packages/live-inject/agent/agent_main.cpp, packages/live-inject/agent/write.cpp, packages/live-inject/agent/write.h</files>
  <behavior>
    - The VERY FIRST thing every loop iteration does (before computing `player` or checking it for
      null) is: attempt `channelReadCommand(&cmd, &cmdSeq)`; if the read succeeded and
      `cmd.flags & CMD_FLAG_STOP_REQUESTED`, FIRST call `channelWriteGuardStatus(GUARD_FLAG_STOPPING, 0)`
      (ROUND 4, REVIEWS.md round-3 maintainer decision #5 — a dedicated, sticky acknowledgement bit,
      distinct from the latest-wins command slot a later drag command could otherwise overwrite before
      the host observes STOP was honored; 05-07's `detachUI` retry-loop resends `writeStop` until it
      observes this bit), THEN `channelClose()` and `return 0` — this fixes REVIEWS.md Fix F (Cursor
      HIGH): a detach while the world is not loaded (`player == nullptr`) must still be honored, not
      starved by the pre-existing `if (!player) { Sleep(100); continue; }` gate.
    - After the stop-signal check, resolve `player = getPlayer()` as before. If `player` is null,
      `Sleep(100); continue;` exactly as today (this early-continue now runs AFTER the stop-signal
      check, not before it). ROUND 5 (REVIEWS.md round-4 maintainer decision #4, Opus LOW — `getPlayer()`
      was previously resolved BEFORE the `__try` span, so a teardown-time AV inside it would escape
      unmitigated): the `__try`/`__except` block (see the ROUND 4 SEH bullet below) now STARTS at this
      `player = getPlayer()` call, not later at focus-resolution — the null-`player` early-`continue`
      stays lexically INSIDE the `__try` block (MSVC permits `continue`/`return` inside `__try`, unlike
      `__finally`), so a fault inside `getPlayer()` itself is now caught by the SAME SEH handler as every
      other resolver call this iteration.
    - Resolve the "focus object" for this iteration — CROSS-BUILD (05-CONTEXT decision #1b): on the
      ADVERTISED path (`swg::endpoints::isAdvertisedClient()`), the resolver is a REAL TWO-STEP
      (2026-07-09 — corrects a prior single-call framing): if `swg::endpoints::cuiHudGetInstance` is
      resolved (non-null fn pointer), call it to get `hudInstance`; if `hudInstance` is non-null AND
      `swg::endpoints::cuiHudGetTarget` is resolved, call `cuiHudGetTarget(hudInstance)`; if THAT
      returns a non-null Object*, `focus = selected`, `hasTarget = true`. If EITHER of the two slots is
      UNRESOLVED (either advertised catalog row is genuinely missing at runtime), set
      `targetUnavailableOnBuild = true` and `focus = player`, `hasTarget = false` — this is the
      "mechanism itself is not wired up" case. If both slots resolve but the two-step call chain
      returns null (nothing currently selected — a normal, expected state), `focus = player`,
      `hasTarget = false`, `targetUnavailableOnBuild = false` — do NOT conflate "no selection right
      now" with "build capability gap." On the LEGACY path (`!isAdvertisedClient()`), UNCHANGED from
      round 1: call `getPlayerCreatureObject()` (if resolved) then compute
      `cachedNetIdPtr = (uint8_t*)playerCreature + 1432` and call
      `cachedNetworkIdGetObject(cachedNetIdPtr)`; if that returns non-null, `focus = target`,
      `hasTarget = true`; else `focus = player`, `hasTarget = false`, `targetUnavailableOnBuild =
      false` (legacy's mechanism is a hard literal, not catalog-dependent, so it is never reported as
      build-unavailable). All existing reads (transform, networkId, templateName) and the sentinel
      gate now operate on `focus`, not unconditionally `player` — this is 05-CONTEXT decision #1's
      honest, and now CROSS-BUILD, object-targeting. ROUND 4 RE-KEY (REVIEWS.md round-3 maintainer
      decision #1, restores locked D-03's "captured per object (once)" model): immediately after
      `focus` is resolved above, compare it against a static `s_expectedCapturedAgainst` (a raw
      `void*`, initialized `nullptr`, build-agnostic — comparing the raw resolved pointer needs no
      separate networkId/templateName plumbing on either build; this is a DESIGN CHOICE for simplicity
      and build-agnosticism, not because it is the only theoretically possible per-object identity
      signal — `Object::networkId` IS a real flat field (object.h:86, ROUND 5/Codex LOW correction),
      this file simply does not bind a live read of it this phase; see this file's ROUND 5 FOCUS_TOKEN
      note below for how this SAME raw-pointer value is now ALSO published to the host, closing the
      host/agent identity-key mismatch REVIEWS.md round-4 found). ROUND 6 (REVIEWS.md round-5
      maintainer decision "option (a) structural" — closes the agent-side half of Opus's pointer-ABA
      finding): the pointer-only compare above has the IDENTICAL blind spot as the host's pre-ROUND-6
      identityCache — a despawned object's address reallocated to a DIFFERENT object would otherwise
      skip re-capture merely because `focus` happens to equal the stale `s_expectedCapturedAgainst`
      value. FIX: also store `s_expectedCapturedAgainstTemplate` (the `templateName` string/hash
      already read this iteration for the read frame) and `s_expectedCapturedAgainstNetworkId` (the
      `networkId` already read this iteration) alongside the raw pointer at capture time — no NEW
      reads, both fields are already computed every iteration for `channelWrite`. Re-capture
      (`s_expectedCaptured = false`) when `focus != s_expectedCapturedAgainst` OR the CURRENT iteration's
      resolved `templateName`/`networkId` differ from `s_expectedCapturedAgainstTemplate`/
      `s_expectedCapturedAgainstNetworkId` — either condition alone triggers a re-key, not just the
      pointer compare: if `focus != s_expectedCapturedAgainst`, reset `s_expectedCaptured = false`
      (re-arming the FIRST-sentinel-passing-read capture the bullet below implements, so it re-triggers
      against the NEW focus this same session instead of running only once ever) and set
      `s_expectedCapturedAgainst = focus`, COPY the bytes via `strncpy(s_expectedCapturedAgainstTemplate, templateName, k_templateNameLen)` (content copy, never a pointer alias; the compare is `strncmp`, never a pointer compare — Opus 3c-ii),
      `s_expectedCapturedAgainstNetworkId = networkId`. This closes the bug where selecting
      any object other than the attach-time focus permanently guard-blocked its first write (guard
      compared the NEW object's live bytes against the OLD object's — usually the player's — baseline)
      and made `revertAll` force-write the wrong object's attach-time pose onto it.
    - `state.liveness` gains bits (in addition to the existing bit0=playerNonNull, bit1=isOver):
      bit2 = `hasTarget`, bit3 = `targetUnavailableOnBuild` (REDEFINED per ROUND 2 — see behavior
      above: true only on a genuine unresolved-mechanism case, on EITHER build now, not a
      legacy-vs-advertised split), bit4 = `scaleUnavailableOnBuild` (ROUND 2 NEW — see below), and
      bit5 = `scaleGuardUnavailableOnBuild` (see below — REDEFINED 2026-07-09 from "getScale
      RVA/catalog unresolved" to "advertised m_scale offset not yet confirmed"; a DISTINCT read-side
      signal, never conflated with bit4's write-side meaning; unconditionally `false` on legacy, since
      the legacy member-offset read is always attempted there).
    - Legacy networkId: leave EXACTLY as today (`netId = 0` when `getNetworkId` is unresolved; the
      networkId sentinel stays not-applicable on that path) — do NOT add any offset-based read here
      (REVIEWS.md Fix E).
    - ROUND 2 W2 fix — `scaleUnavailableOnBuild` (liveness bit4) is computed ONCE, immediately after
      the existing post-`resolveFromExe()` `setScale` conditional-seed step (see the action block
      below): `s_scaleUnavailableOnBuild = swg::endpoints::isAdvertisedClient() &&
      swg::endpoints::setScale == nullptr;` — a static bool set once at agent init, OR'd into
      `state.liveness` every iteration thereafter (never recomputed per-iteration; `setScale`'s
      resolved-ness cannot change after init).
    - MEMBER-OFFSET read-side scale-guard parity (RE-ARCHITECTED 2026-07-09 from an RVA/catalog-binding
      attempt): immediately alongside `s_scaleUnavailableOnBuild`'s one-time computation, ALSO compute
      `static bool s_scaleGuardUnavailableOnBuild = swg::endpoints::isAdvertisedClient() &&
      !s_advertisedScaleOffsetConfirmed;` once (a NEW static, mirroring `s_scaleUnavailableOnBuild`'s
      exact shape — `isAdvertisedClient()` short-circuits this to `false` on legacy unconditionally,
      since legacy's computed offset is always attempted, never gated; `s_advertisedScaleOffsetConfirmed`
      is a separate static bool, declared alongside it, that defaults `false` and stays `false` this
      phase — no confirmed advertised offset exists this session). Every loop iteration, immediately
      after the existing transform read (the SAME iteration that read is taken, on `focus`), compute
      `currentScale[3]`: on LEGACY (`!isAdvertisedClient()`), UNCONDITIONALLY read three floats
      starting at `(const uint8_t*)focus + kLegacyScaleOffset` (`kLegacyScaleOffset = 0x44`, see
      Interfaces — a genuine live read, mirroring the +1432 precedent's D-01 scope treatment: a
      READ-only offset for a guard comparison is out of D-01's WRITE-only scope, per this plan's own
      established precedent). On ADVERTISED, if `s_advertisedScaleOffsetConfirmed` (never true this
      phase, reserved for a future patch), read the same way at `s_advertisedScaleOffset` (a second
      static, paired with the confirmed flag); if NOT confirmed, set `currentScale = s_expectedScale`
      (self-compare fallback — the pre-fix behavior, preserved as the honest degrade path:
      `checkWriteGuard` will report `scalePassed = true` for this iteration since the values are
      byte-identical, never worse than before — but `s_scaleGuardUnavailableOnBuild` published below is
      what tells a downstream consumer this pass was UNVERIFIED, not a confirmed-clean state; do NOT
      let an unconfirmed advertised offset latch `SCALE_REFUSED`, and do NOT treat a self-compare pass
      as an independently-verified pass). `state.liveness` OR's in bit5 =
      `scaleGuardUnavailableOnBuild` every iteration from the one-time-computed static above. No
      05-07/05-10/05-11 UI wiring is required to close this fix — bit5 is published honestly for a
      future UI pass, out of THIS revision's scope.
    - ROUND 5 (REVIEWS.md round-4 maintainer decision #1, the load-bearing identity-unification fix):
      EVERY iteration, immediately after `focus` is resolved (and the re-key check above runs), compute
      `focusToken = static_cast<uint32_t>(reinterpret_cast<uintptr_t>(focus))` — the IDENTICAL
      truncation already used for `guardAddr`, just computed every tick instead of only at
      guard-check/apply time — and assign it to `state.focusToken` BEFORE this iteration's
      `channelWrite(&state)` call (05-01's ROUND 5 read-frame field), the SAME way
      `state.transform`/`state.networkId`/`state.templateName`/`state.liveness` are already populated
      every iteration. This is DISTINCT from `channelWriteGuardStatus`'s `guardAddr` publish,
      which only fires when a guard check actually runs (i.e. on a new, non-torn command) and therefore
      LAGS the agent's true current focus between commands — `focusToken` has no such lag,
      since it rides the SAME seqlocked read frame the transform/networkId/templateName/liveness fields
      already use every tick. This is the SINGLE identity signal 05-07's host-side re-key (its
      own ROUND 5 note) now keys `cowSnapshot`/`writeLog` on, instead of `(networkId, templateName)` —
      closing the fail-open two-same-template-object revert bug and the lossy focus-flip A->B->A bug
      REVIEWS.md round-4 found (both rooted in the host using a coarser identity key than the agent's
      own `s_expectedCapturedAgainst` re-key above). x64 SCOPE CAVEAT (Codex, REVIEWS.md round-5 LOW —
      keep visible, not a defect): `focusToken` is a 32-bit truncation of `addrOf(focus)`, correct and
      lossless on the CURRENT in-scope x86 (32-bit) live target (no truncation occurs — the full pointer
      fits). If an x64 client ever becomes an in-scope live-sync target (out of scope this phase per
      Phase 3 GROUNDTRUTH), a 32-bit token would alias between two distinct high-address x64 pointers
      and this field would need to widen to 64 bits (a channel-layout change) — this comment exists so
      that future work does not silently assume the truncation is always safe.
    - Each loop iteration (now ~16ms / 60fps): after the existing read+4-sentinel-gate+channelWrite
      step (now operating on `focus`), gate BOTH the rebaseline mutation and the apply behind ONE
      OUTER condition — ROUND 4 (REVIEWS.md round-3 maintainer decision #2, closes the
      ungated-rebaseline guard defeat): a command was read this iteration (from the top-of-loop read),
      it is NOT a stop request, `cmdSeq` is a NEW value (differs from a static `lastAppliedCmdSeq`,
      initialized to 0), AND the read was not torn. Round 3's rebaseline mutation sat OUTSIDE this
      newness/non-torn gate (gated only on "a command was read"), so on the single latest-wins command
      slot it silently RE-RAN every frame in the `[revert, next-command]` window, adopting whatever was
      live at that instant — including an external tamper that landed after the revert — as the new
      baseline, defeating the guard for an unbounded window. That is now structurally impossible: the
      rebaseline can only ever execute once, LEXICALLY INSIDE this SAME `cmdSeq`-new + non-torn check,
      atomic with the apply that follows it in the same gated instant (see this task's acceptance
      criteria for the line-order grep that proves the nesting, not just this prose). Inside this gate
      — ROUND 2 COALESCE (Opus MEDIUM, closes the two-command race), now GATED per ROUND 4: if
      `cmd.flags & CMD_FLAG_REBASELINE_GUARD`: set `s_expectedTransform` = the transform just read THIS
      iteration (fresh live bytes, the SAME source Fix D's read-back uses) AND `s_expectedScale` = the
      GENUINE live `currentScale` value computed THIS iteration (ROUND 4 — changed from round 3's
      `cmd.scale`; a real live scale read now exists via the member-offset read below, so BOTH channels
      rebaseline to the LIVE value at this gated instant, exactly like transform — this closes the
      transform/scale rebaseline-semantics asymmetry Opus and Cursor each flagged from opposite ends:
      scale now un-sticks like transform, and neither channel's rebaseline is ungated) — then, in the
      SAME gated iteration, immediately fall through into the normal guard+apply flow below using these
      just-rebaselined expected values as the guard baseline and `cmd`'s transform/scale as the target
      (this now trivially passes both channels' guard and applies the real revert). Do NOT skip the
      apply step for a REBASELINE_GUARD command as round 1 did — that mutually-exclusive design is what
      created the two-command race; this round's fix is coalescing, not skipping — AND, because the
      whole thing is now inside the newness gate, it can only ever coalesce ONCE per distinct command,
      never re-run on a stale/repeated `cmdSeq` in the window before the next command arrives. If
      `cmd.flags` does NOT include `CMD_FLAG_REBASELINE_GUARD`: skip straight to the normal guard+apply
      flow using the EXISTING `s_expectedTransform`/`s_expectedScale` baseline (unchanged from round 1).
      Normal guard+apply flow (runs in both cases above, only the baseline source differs, and — ROUND
      4 — always runs inside the SAME outer `cmdSeq`-new + non-torn gate, never a second, separately
      re-checked condition): run `checkWriteGuard(xform, s_expectedTransform, currentScale,
      s_expectedScale)` using the transform just read this same iteration and the `currentScale` value
      computed above (a GENUINE live m_scale member-offset read on legacy always, or on advertised when
      `s_advertisedScaleOffsetConfirmed` — 2026-07-09 revision — never the self-referential
      `s_expectedScale` fed back on itself except in the documented unconfirmed-advertised fallback);
      call `applyWrite(focus, cmd, guardResult)` (write.cpp below) which independently calls
      `setTransform_o2w(focus, &cmd.transform)` ONLY if `guardResult.transformPassed`, and
      `setScale(focus, &cmd.scale)` ONLY if `guardResult.scalePassed && swg::endpoints::setScale` —
      always publish `channelWriteGuardStatus(flags, addrOf(focus))` reflecting BOTH bits' final state:
      TRANSFORM_REFUSED set iff `!guardResult.transformPassed`; **SCALE_REFUSED set iff
      `!guardResult.scalePassed && swg::endpoints::setScale != nullptr`** (ROUND 2 W2 fix — a null/
      unresolved `setScale` NEVER sets SCALE_REFUSED anymore; that case is communicated ONLY via the
      one-time `scaleUnavailableOnBuild` liveness bit4, never via a per-write guard-refused flag, so it
      can never be mistaken for a tamper). On a successful transform apply, refresh
      `s_expectedTransform` from a FRESH `getTransform_o2w(focus)` read-back taken immediately after
      the call (REVIEWS.md Fix D — NOT from `cmd.transform`— survives STATIC interior/non-world-cell
      objects' float-rounded storage without false-failing write N+1; ROUND 2 CAVEAT, add as an inline
      comment at this exact call site: for a dynamically-moving parent, `m_attachedToObject` =
      mount/vehicle/POB ship, the world-space read-back drifts every frame and WILL false-fail the
      guard on write N+1 — fails safe, documented, not a defect to fix here). On a successful scale
      apply, set `s_expectedScale = cmd.scale` directly (no read-back NEEDED for the baseline-refresh
      purpose — `setScale` stores verbatim, `m_scale = scale;`, Object.cpp:2205, so `cmd.scale` IS what
      was actually stored; this is UNCHANGED by the plan-check revision, which adds a live READ solely
      for the GUARD comparison every iteration, a different purpose from this baseline-refresh step).
      Set `lastAppliedCmdSeq = cmdSeq` after processing either branch (rebaseline-then-apply, or normal
      apply) — ROUND 4: this assignment now sits inside the SAME outer gate too, so it can never
      advance on a stale/repeated `cmdSeq`.
    - `s_expectedTransform`/`s_expectedScale` (new statics, agent_main.cpp or write.cpp — executor's
      choice of ownership, but must be initialized) are captured from the FIRST sentinel-passing
      transform/scale read after channel open OR after a focus-identity change (the D-03 "captured per
      object, once" snapshot — ROUND 4, REVIEWS.md round-3 maintainer decision #1: NO LONGER "once per
      agent lifetime" — the static `bool s_expectedCaptured = false` guard is now ALSO reset to
      `false` by the re-key check in the focus-resolution bullet above, so the FIRST
      sentinel-passing read against a NEWLY-selected focus object re-triggers this SAME capture path,
      exactly as it did at original attach). For the attach-time (or re-key-time) SCALE capture
      specifically: on LEGACY (always) or on ADVERTISED when `s_advertisedScaleOffsetConfirmed`, seed
      `s_expectedScale` from that FIRST live `currentScale` read against the (possibly new) focus (a
      genuine member-offset read); when the advertised offset is NOT confirmed, fall back to round 1's
      documented scope note — seed `s_expectedScale` from the FIRST command's `cmd.scale` the agent
      ever sees applied against this focus (leave the scale guard effectively "pass" until the first
      scale write happens against it, since there is no confirmed way to independently read live scale
      to baseline it on that build — this residual gap is exactly what `scaleGuardUnavailableOnBuild`
      reports).
    - ROUND 4 (REVIEWS.md round-3 maintainer decision #4 — accept-watched + MITIGATE T-05-32's
      off-thread UAF): wrap the ENTIRE getPlayer->resolve->read->apply span for this iteration — ROUND 5
      (REVIEWS.md round-4 maintainer decision #4): the span now STARTS at the `player = getPlayer()`
      call itself (see the top-of-loop bullet above), not just at focus-resolution — the
      focus-resolution calls (the advertised two-step `cuiHudGetInstance()`/`cuiHudGetTarget(hudInstance)`
      pair and its intermediate `hudInstance` pointer window; the legacy
      `getPlayerCreatureObject()`/`cachedNetworkIdGetObject()` chain) through the transform/scale reads,
      the sentinel gate, and the guarded `applyWrite` call — in ONE `__try { ... } __except
      (EXCEPTION_EXECUTE_HANDLER) { ... }` (SEH) block. On an access violation (a
      freed/relocated `Object*`/`SwgCuiHud*` dereferenced mid-poll by the sim thread tearing the object
      down — exactly the hazard T-05-32 accepts as watched), the `__except` handler must NOT call any
      setter, must NOT let the fault escape and crash the host client; it sets a local "skip this
      frame" flag (so this SAME iteration's read/apply are abandoned, never retried against a
      known-bad pointer), publishes a NEW, TRANSIENT liveness bit6 `agentFaultRecovered` (OR'd in for
      exactly the iteration the fault was caught, then cleared the NEXT iteration — not sticky, unlike
      GUARD_STATUS's STOPPING bit; ROUND 5, REVIEWS.md round-4 maintainer decision #4 — this bit is
      DOCUMENTED AGENT-ONLY telemetry this round, the SAME posture 05-07 already applies to liveness
      bit5 `scaleGuardUnavailableOnBuild` — no renderer/HUD consumer decodes it, and none is added by
      this plan; a future UI pass may surface it, this is a deliberate, documented scope boundary, not
      an oversight), and falls through to the existing `Sleep(16)`/loop-continue at the
      bottom — a bounded, observable degrade (one skipped frame) instead of an unmitigated client
      crash. This does not eliminate the accept-watched risk (a torn write mid-setter-call before the
      exception is still possible in principle); it bounds the blast radius of the specific hazard SEH
      can catch (an access violation on the resolved pointer).
  </behavior>
  <read_first>
    - packages/live-inject/agent/agent_main.cpp (full file — the exact loop to extend, lines 132-222 especially, VERIFIED against current source this session)
    - packages/live-inject/agent/write.h (from 05-01 — checkWriteGuard signature with independent transformPassed/scalePassed, applyWrite stub — UNCHANGED by this revision)
    - packages/live-inject/agent/channel.h (from 05-01 — LiveCommand struct, CMD_FLAG_STOP_REQUESTED, CMD_FLAG_REBASELINE_GUARD, GUARD_FLAG_TRANSFORM_REFUSED, GUARD_FLAG_SCALE_REFUSED, channelReadCommand/channelWriteGuardStatus(flags, addr) signatures)
    - This plan's Task 1 output (rva_table.cpp new slots: null-safe setScale, getPlayerCreatureObject, cachedNetworkIdGetObject, the cross-build getObjectById-both-builds primitive, and the advertised TWO-STEP selected-object resolver slots `cuiHudGetInstance`/`cuiHudGetTarget` — NO getScale slot exists, see this plan's top-of-file REVISION NOTE)
    - resolve.h (isAdvertisedClient() — already declared, used for the post-resolve setScale seed, the legacy target-resolution gate, and now ALSO the advertised focus-resolution branch and the ROUND 2/plan-check scaleUnavailableOnBuild/scaleGuardUnavailableOnBuild computations)
    - D:/Code/swg-client-v2/src/engine/shared/library/sharedObject/src/shared/object/Object.h (lines 452-460, 505-515 — `m_scale`'s declaration and `getScale()`'s inline definition, cited ONLY to explain why no accessor binding exists; this task does not call getScale)
    - D:/Code/Utinni/UtinniCore/swg/object/object.h (lines 76-107 — the `Object` struct legacy memory layout `kLegacyScaleOffset` (0x44) is computed from; `scale` field at :94)
  </read_first>
  <action>
    Add extern references at the top of agent_main.cpp (mirroring the existing extern block, lines
    68-76) for `swg::endpoints::setTransform_o2w`, `swg::endpoints::setScale`,
    `swg::endpoints::getPlayerCreatureObject`, `swg::endpoints::cachedNetworkIdGetObject`, and the
    advertised TWO-STEP `swg::endpoints::cuiHudGetInstance`/`swg::endpoints::cuiHudGetTarget` (from
    Task 1 — name them to match whatever Task 1 actually declared). Do NOT add an extern for
    `getScale` — no such slot exists in this file (2026-07-09 revision). Immediately after
    `swg::endpoints::resolveFromExe();` (agent_init Step 3, existing code) add the POST-resolve
    conditional seed that closes the BLOCKER (REVIEWS.md Fix A): `if
    (!swg::endpoints::isAdvertisedClient()) { swg::endpoints::setScale = (pSetScale)0x00B23A10; }` —
    UNCHANGED from round 1. Immediately after THAT line, add the ROUND 2 W2 fix's one-time computation:
    `static bool s_scaleUnavailableOnBuild = swg::endpoints::isAdvertisedClient() &&
    swg::endpoints::setScale == nullptr;` (this correctly evaluates AFTER the conditional seed above
    has had its chance to set `setScale` on the legacy path, and AFTER `resolveFromExe()` has had its
    chance to resolve it by catalog name on the advertised path — so this line's value is stable and
    correct for the rest of the agent's lifetime). Immediately after THAT line, add TWO new statics
    (RE-ARCHITECTED 2026-07-09 from an RVA/catalog-binding attempt): `static bool
    s_advertisedScaleOffsetConfirmed = false;` (defaults false — no confirmed advertised `m_scale`
    offset exists this session; a future patch flips this alongside setting a real
    `s_advertisedScaleOffset` value) and, using it, `static bool s_scaleGuardUnavailableOnBuild =
    swg::endpoints::isAdvertisedClient() && !s_advertisedScaleOffsetConfirmed;` (same
    one-time-computed timing rationale as `s_scaleUnavailableOnBuild` — short-circuits to `false` on
    legacy unconditionally, since the legacy member-offset read is always attempted; a NEW, DISTINCT
    static, never merged with `s_scaleUnavailableOnBuild`). Also declare `constexpr uint32_t
    kLegacyScaleOffset = 0x44;` near the top of the file alongside other magic constants (mirrors the
    +1432 inline-arithmetic idiom — a planner-computed, VALIDATE-LIVE candidate, see this plan's
    Interfaces section and top-of-file REVISION NOTE for its derivation) and a paired
    `static float s_advertisedScaleOffset = 0;`-shaped placeholder ONLY if needed to keep the
    gated-read code path compiling (unused while `s_advertisedScaleOffsetConfirmed` stays `false` — do
    not populate it with a guessed value). Change the poll `Sleep(33)` to `Sleep(16)` (60fps, per
    LIVE-03 SC1). Restructure the loop body per this task's `<behavior>` block exactly: stop-signal
    read FIRST (before `player` is even computed), then player-null continue, then CROSS-BUILD
    focus-object resolution (advertised: the TWO-STEP `cuiHudGetInstance()` -> `cuiHudGetTarget(instance)`
    -or-player, with the three-way hasTarget/targetUnavailableOnBuild distinction; legacy: unchanged
    +1432-lookAt-target-or-player), then the ROUND 5 `focusToken` computation (`static_cast<uint32_t>(
    reinterpret_cast<uintptr_t>(focus))`, assigned to `state.focusToken` every iteration — the
    load-bearing identity-unification field 05-07 re-keys on), then the existing sentinel-gated
    read+channelWrite (now targeting `focus`, with `scaleUnavailableOnBuild` AND `scaleGuardUnavailableOnBuild` OR'd into the published
    liveness every iteration, bits 4 and 5 respectively), then the current-scale live-read step
    (`currentScale` from the `kLegacyScaleOffset` member-offset read on legacy unconditionally, or from
    `s_advertisedScaleOffset` on advertised only when `s_advertisedScaleOffsetConfirmed`, else
    `s_expectedScale`), then the command-apply block (coalesced rebaseline-then-apply, or normal apply,
    now calling `checkWriteGuard` with the genuine `currentScale` value). Implement `applyWrite` in
    write.cpp now that the concrete setter fn-pointer types exist: `void applyWrite(void* target, const
    LiveCommand& cmd, const GuardResult& guard)` internally: `if (guard.transformPassed &&
    swg::endpoints::setTransform_o2w) swg::endpoints::setTransform_o2w(target, &cmd.transform);` and
    separately `if (guard.scalePassed && swg::endpoints::setScale) swg::endpoints::setScale(target,
    &cmd.scale);` — each gated INDEPENDENTLY on both its own guard bit and its own non-null resolved
    pointer (unchanged from round 1 — write.h/write.cpp's `applyWrite`/`checkWriteGuard` SIGNATURES are
    completely unchanged by the plan-check revision; only THIS file's CALLER now feeds
    `checkWriteGuard` a genuine third argument); agent_main.cpp calls this single function on any
    guard-evaluated command (even if one or both channels fail) rather than calling setters inline. In
    the guard-status-publishing step (the `channelWriteGuardStatus` call, NOT inside `applyWrite`
    itself — this logic lives in agent_main.cpp where both `guardResult` and
    `swg::endpoints::setScale`'s resolved-ness are both in scope), compute SCALE_REFUSED per the ROUND 2
    W2 fix formula given in `<behavior>` above — `!guardResult.scalePassed &&
    swg::endpoints::setScale != nullptr` — replacing round 1's `!guardResult.scalePassed ||
    !swg::endpoints::setScale` formula exactly (the change is `||` become `&&` plus the negation moving
    onto the resolved-check, i.e. "refused" now requires BOTH a genuine mismatch AND a resolved setter,
    not either alone). Truncate `target`'s pointer to `uint32_t` via `static_cast<uint32_t>(reinterpret_
    cast<uintptr_t>(target))` when calling `channelWriteGuardStatus(flags, addr)` (x86 process, safe
    truncation, matches channel.h's `guardAddr` field type) — unchanged from round 1.

    ROUND 4 additions (REVIEWS.md round-3 maintainer decisions #1/#2/#4/#5 — implement exactly per
    this task's `<behavior>` block): declare `static void* s_expectedCapturedAgainst = nullptr;`
    alongside `s_expectedCaptured`. ROUND 6 (REVIEWS.md round-5 maintainer decision "option (a)
    structural"): ALSO declare a STABLY-OWNED COPY of the captured template name — `static char
    s_expectedCapturedAgainstTemplate[k_templateNameLen] = {0};`, a fixed buffer the agent OWNS and
    `strncpy`s into at capture time (NEVER a `const char*` alias into a client/frame buffer whose bytes
    or address may change between ticks — a raw pointer compare is the Opus 3c-ii silent-failure trap) —
    and `static int64_t s_expectedCapturedAgainstNetworkId = 0;`, paired with
    `s_expectedCapturedAgainst`. Immediately after `focus` is resolved each iteration, compare it to
    `s_expectedCapturedAgainst` AND compare the current iteration's resolved `templateName`/`networkId`
    to `s_expectedCapturedAgainstTemplate`/`s_expectedCapturedAgainstNetworkId`. The templateName
    comparison MUST be a CONTENT compare — `strncmp(templateName, s_expectedCapturedAgainstTemplate,
    k_templateNameLen) != 0` — NEVER a raw `const char*` pointer compare: a pointer compare silently
    no-ops the fix (reused frame buffer → pointer always equal → agent-side ABA reopens) OR re-captures
    every tick (fresh per-frame string → pointer always differs → defeats tamper detection), the exact
    silent failure Opus 3c-ii names. On EITHER mismatch (pointer OR identity content), set
    `s_expectedCaptured = false` (re-arming the attach-time-equivalent capture) THEN update ALL THREE
    statics — `s_expectedCapturedAgainst = focus`, `strncpy(s_expectedCapturedAgainstTemplate,
    templateName, k_templateNameLen)` (COPY the bytes into the owned buffer, never alias the pointer),
    `s_expectedCapturedAgainstNetworkId = networkId` (decision #1's original pointer-only re-key, ROUND
    6-extended to also close the pointer-ABA dual blind spot Opus found — see this file's ROUND 6
    REVISION NOTE). Restructure the
    command-apply block so the ENTIRE
    `cmd.flags & CMD_FLAG_REBASELINE_GUARD` mutation sits LEXICALLY INSIDE the `cmdSeq`-new +
    non-torn check that also gates `checkWriteGuard`/`applyWrite`/`lastAppliedCmdSeq = cmdSeq` — do
    NOT leave the REBASELINE_GUARD branch in the outer 'a command was read this iteration' scope
    (decision #2 — the gating fix); when rebaselining, assign `s_expectedScale = currentScale` (the
    live member-offset read computed earlier this same iteration), NOT `cmd.scale` (decision #2's
    '(b) rebaseline both channels to the live value' — scale now un-sticks exactly like transform).
    Wrap the resolve->read->apply span in `__try`/`__except (EXCEPTION_EXECUTE_HANDLER)`, publish
    liveness bit6 `agentFaultRecovered` transiently on catch, skip the write, never let the fault
    escape (decision #4). In the top-of-loop STOP_REQUESTED branch, call
    `channelWriteGuardStatus(GUARD_FLAG_STOPPING, 0)` BEFORE `channelClose()`/`return 0` (decision #5
    — 05-01's new GUARD_STATUS bit2).
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms `Sleep(16)` replaces the prior `Sleep(33)` in the poll loop.
    - grep confirms a `CMD_FLAG_STOP_REQUESTED` check exists and is the FIRST statement inside the `while(true)` loop, preceding the `player == nullptr` continue.
    - grep confirms `if (!swg::endpoints::isAdvertisedClient()) { swg::endpoints::setScale = ...0x00B23A10; }` exists immediately after `resolveFromExe()`, and a `s_scaleUnavailableOnBuild` static bool is computed exactly once, immediately after that line, as `isAdvertisedClient() && setScale == nullptr`.
    - grep confirms `s_scaleGuardUnavailableOnBuild` is computed exactly once, immediately after `s_scaleUnavailableOnBuild`, as `swg::endpoints::isAdvertisedClient() && !s_advertisedScaleOffsetConfirmed` — a DISTINCT static from `s_scaleUnavailableOnBuild` (grep confirms both symbols exist, are not aliases of each other, and are OR'd into different liveness bits); grep confirms `s_advertisedScaleOffsetConfirmed` defaults `false` and is never set `true` anywhere in this file (2026-07-09 — no confirmed advertised offset exists this session).
    - grep confirms `SCALE_REFUSED` (or its symbolic constant) is set using an `&&`-combined condition requiring BOTH `!guardResult.scalePassed` AND `swg::endpoints::setScale != nullptr` — NOT an `||`-combined condition (the round-1 formula must not remain anywhere in this file).
    - grep confirms `state.liveness` (or the equivalent published liveness value) is OR'd with a bit4 value derived from `s_scaleUnavailableOnBuild` AND a SEPARATE bit5 value derived from `s_scaleGuardUnavailableOnBuild`, every iteration.
    - grep confirms `checkWriteGuard(` is called with a THIRD argument that is a live-scale local (e.g. `currentScale`) assigned from a `kLegacyScaleOffset` member-offset read on legacy (unconditional) or `s_advertisedScaleOffset` on advertised (only when `s_advertisedScaleOffsetConfirmed`), and from `s_expectedScale` only in the unconfirmed-advertised fallback — NEVER a bare `s_expectedScale` literal or an elided `...`/placeholder in that argument position anywhere in this file; grep confirms no call site references `swg::endpoints::getScale` (the symbol does not exist in this file, 2026-07-09).
    - grep confirms the ADVERTISED focus-resolution branch calls `cuiHudGetInstance()` FIRST and `cuiHudGetTarget(instance)` SECOND (not the legacy `cachedNetworkIdGetObject`/+1432 chain) when `isAdvertisedClient()` is true, and that BOTH call sites check their respective slot is non-null BEFORE calling it (never an unconditional call to a possibly-unresolved advertised endpoint).
    - grep confirms `targetUnavailableOnBuild` is set to `true` ONLY on the branch where either of the two advertised resolver slots (`cuiHudGetInstance`, `cuiHudGetTarget`) is unresolved (null fn pointer), OR the equivalent legacy-unresolved branch — never on the branch where both slots resolve but the two-step call returns a null Object* (that branch sets `hasTarget=false` with `targetUnavailableOnBuild` left `false`).
    - grep confirms a command carrying `CMD_FLAG_REBASELINE_GUARD` proceeds to call `applyWrite`/the setter calls in the SAME iteration after rebaselining (no `continue`/early-return that skips the apply step for a rebaseline command) — this is the ROUND 2 coalesce regression test; the round-1 "do NOT call any setter this iteration" comment/logic must not remain.
    - grep confirms a fresh `getTransform_o2w` read-back (not `cmd.transform`) is assigned to `s_expectedTransform` on a successful transform apply, with an adjacent inline comment naming the static-cell-only caveat (dynamically-moving parent / `m_attachedToObject` is NOT covered).
    - grep confirms the phrases "no scale read accessor exists" and "agent binds no getScale RVA" do NOT appear anywhere in this file's comments (2026-07-09 — getScale is no longer a concept this file references at all); the corrected phrasing ("advertised m_scale offset not yet confirmed") is used instead.
    - grep confirms `getPlayerCreatureObject`/`cachedNetworkIdGetObject` calls remain lexically inside an `if (!swg::endpoints::isAdvertisedClient())` block — never called unconditionally.
    - grep confirms no `+ 1432` arithmetic is used to populate any field named/documented as "networkId" — the only `+ 1432` use is for LEGACY target resolution, feeding `cachedNetworkIdGetObject`, with an adjacent "VALIDATE LIVE" comment.
    - grep confirms `kLegacyScaleOffset` equals `0x44` and carries a "VALIDATE LIVE... computed from Utinni's Object struct" comment.
    - ROUND 4 (decision #1, re-key): grep confirms a static `s_expectedCapturedAgainst` symbol exists, is compared against `focus` every iteration, and `s_expectedCaptured = false` is assigned on the mismatch branch — proving the baseline re-key is wired, not just declared.
    - ROUND 6 (REVIEWS.md round-5 maintainer decision "option (a) structural", the identity-cross-check grep): grep confirms `s_expectedCapturedAgainstTemplate` and `s_expectedCapturedAgainstNetworkId` symbols exist, are assigned alongside `s_expectedCapturedAgainst` at every re-key site, and that the mismatch condition gating `s_expectedCaptured = false` is an OR of the pointer compare AND an identity-field compare (grep -n confirms both `s_expectedCapturedAgainst` and `s_expectedCapturedAgainstTemplate`/`s_expectedCapturedAgainstNetworkId` appear within the SAME conditional expression, not just declared as unused siblings) — proving the re-key condition genuinely includes the identity cross-check, not just the pointer compare.
    - ROUND 6 (BEHAVIORAL test — a symbol grep CANNOT distinguish a content compare from a pointer compare, Opus 3c-ii): a test exercising the agent re-key decision (TS port of the predicate, per this project's C++-predicate test convention) MUST assert that two successive iterations sharing the SAME `focusToken` but DIFFERENT `templateName` CONTENT trigger a re-capture (`s_expectedCaptured` reset to false), AND that two iterations with the same `focusToken` and IDENTICAL `templateName` content do NOT re-capture — proving the templateName comparison is by CONTENT (strncmp against the owned copy), closing both the silent-no-op and the constant-re-capture failure modes. A `const char*` pointer compare fails this test.
    - ROUND 5 (REVIEWS.md round-4 maintainer decision #1, focusToken publish): grep confirms `state.focusToken` (or the equivalent read-frame field assignment) is set from `static_cast<uint32_t>(reinterpret_cast<uintptr_t>(focus))` EVERY iteration, lexically BEFORE the iteration's `channelWrite(&state)` call — not only inside the guard-check-gated branch that also calls `channelWriteGuardStatus` — proving `focusToken` publishes every tick, independent of whether a guard check runs that iteration.
    - ROUND 4 (decision #2, lexical-scope gate): grep -n confirms the line number of the `cmd.flags & CMD_FLAG_REBASELINE_GUARD` mutation of `s_expectedTransform`/`s_expectedScale` is GREATER than the line number of the `cmdSeq`-newness check it is gated by (e.g. `cmdSeq != lastAppliedCmdSeq` or equivalent) and LESS than the line number of `lastAppliedCmdSeq = cmdSeq` — proving the rebaseline sits lexically INSIDE the newness+non-torn guard, not in an outer scope (this is the structural proof REVIEWS.md round-3 finding #2 requires, not just prose).
    - ROUND 4 (decision #2, live-scale rebaseline): grep confirms the REBASELINE_GUARD branch assigns `s_expectedScale = currentScale` (the live member-offset read) and that NO `s_expectedScale = cmd.scale` assignment remains anywhere inside that branch.
    - ROUND 4 (decision #4, SEH): grep confirms `__try`, `__except`, `EXCEPTION_EXECUTE_HANDLER`, and `agentFaultRecovered` all appear in agent_main.cpp, and that the `__try` block's span includes the `player = getPlayer()` call (ROUND 5, decision #4), the focus-resolution calls, AND the `applyWrite` call (not just some of them) — grep -n confirms the line number of `getPlayer()` is GREATER than the line number of the opening `__try` and LESS than the line number of the closing `__except`.
    - ROUND 5 (decision #4, bit6 documentation): grep confirms an inline comment near the `agentFaultRecovered` assignment states this bit is DOCUMENTED AGENT-ONLY telemetry this round (no renderer/HUD consumer), mirroring bit5's existing agent-only treatment — proving the scope boundary is a deliberate, written decision, not a silent gap.
    - ROUND 4 (decision #5, STOP sticky bit): grep confirms `channelWriteGuardStatus(GUARD_FLAG_STOPPING` (or equivalent) appears BEFORE `channelClose()` inside the top-of-loop `CMD_FLAG_STOP_REQUESTED` branch.
    - Agent DLL builds cleanly.
    - ROUND 5 (REVIEWS.md round-4 maintainer decision #2, overclaim purge): the active must_haves/success_criteria/threat_model sections state the viewport<->live-object binding gap remains OPEN (deferred to Phase 7) and do NOT claim this round resolves it. The earlier round-1-closure overclaim phrasing (quoted verbatim in REVIEWS.md round-4 decision #2) survives only inside `#`-prefixed archived revision-note lines. Verify by reading those active sections against REVIEWS.md's quoted phrase.
  </acceptance_criteria>
  <done>The agent applies independently-guarded Move/Rotate/Scale writes at 60fps against the correctly-resolved, CROSS-BUILD focus object (advertised: real TWO-STEP selected-object resolution; legacy: unchanged lookAt-target chain), never calls an unresolved setter, refreshes its transform guard baseline from a read-back (surviving STATIC interior objects, with the dynamic-parent caveat documented), never shows a false Scale tamper banner on a build where Scale is simply unresolved (the scaleUnavailableOnBuild bit carries that signal instead), lets an explicit REBASELINE_GUARD-plus-payload command un-stick a blocked guard in ONE race-free seqlock cycle, exits its poll loop cleanly on a stop signal even before a player exists, AND — closing the plan-check BLOCKER — feeds the scale guard a genuine live `m_scale` member-offset read wherever that offset is confirmed for the current build (legacy: unconditionally; advertised: gated behind `s_advertisedScaleOffsetConfirmed`) for genuine external-tamper-detection parity with Transform, degrading honestly via the `scaleGuardUnavailableOnBuild` liveness bit (never a false tamper, never a silently-unverified pass) wherever it is not; resolves the ADVERTISED focus object via the CONFIRMED two-step `cuiHud::g_instance` -> `cuiHud::getTarget` catalog rows. ROUND 4: re-keys its guard baseline whenever the resolved focus object changes mid-session (restoring D-03's per-object model, closing the "can only edit the attach-focus object" bug); gates the REBASELINE_GUARD mutation lexically inside the same cmdSeq-newness guard that protects the apply, and rebaselines BOTH channels to the live value at that gated instant (closing the ungated-rebaseline guard defeat and the transform/scale rebaseline asymmetry); bounds the accept-watched off-thread UAF in SEH so a fault degrades to one skipped frame, never a client crash; and publishes a sticky STOPPING acknowledgement so a host detach retry-loop can never have STOP silently overtaken on the latest-wins command slot. ROUND 6: the baseline re-key now also cross-checks templateName/networkId alongside the pointer compare, closing the agent-side half of Opus's round-5 pointer-ABA finding (a despawned object's address reallocated to a DIFFERENT object no longer skips re-capture) — mirrors 05-07's host-side identityCache cross-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command slot (host-writable) → agent poll loop → live game object memory | The agent is the last line of defense before a real write touches the running game's object state; the guard MUST run before every apply, with no code path that skips it, and no code path may call an unresolved (null or cross-build-invalid) setter. |
| Legacy-only target-resolution RVAs → advertised build | Two legacy-only slots (`getPlayerCreatureObject`, `cachedNetworkIdGetObject`) plus the legacy `getObjectByIdLegacy` primitive hold literals that are only valid on the legacy SWGEmu build; calling any of them on the advertised build risks executing arbitrary relocated code — the runtime `!isAdvertisedClient()` gate is the ONLY thing preventing this, verified by acceptance-criteria grep, not just code review. |
| Advertised-only target-resolution slots → legacy build | The advertised TWO-STEP `cuiHudGetInstance`/`cuiHudGetTarget` slots plus the advertised `getObjectById`-equivalent are null-seeded and catalog-resolved; calling any of them on the legacy build (where they are never resolved) would dereference a null function pointer — every call site MUST check non-null first, verified by acceptance-criteria grep. |
| External tool / the game itself → live object memory → agent's guard comparand | The whole point of D-03's guard is to detect a THIRD PARTY changing the object between polls; a guard comparand that is sourced from the toolkit's own state (rather than a genuine live read) can never observe a third party's change — this is the exact class of defect the plan-check revision closes for Scale, now via a per-build-gated member-offset read rather than an accessor-call binding (2026-07-09). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | agent_main.cpp command-consumption block | mitigate | `applyWrite`'s two setter calls are each independently guarded (own guard bit + own non-null check), called from exactly one call site — verified by acceptance_criteria grep, not just code review; no alternate/debug bypass path exists. |
| T-05-05 | DoS (resource leak) | agent poll thread lifecycle | mitigate | STOP_REQUESTED is checked at the TRUE TOP of every iteration (before the player-null early-continue), guaranteeing the thread exits even if no player is currently loaded — repeated attach/detach cycles cannot accumulate poll threads (REVIEWS.md Fix F). ROUND 4: the agent additionally publishes a sticky GUARD_FLAG_STOPPING acknowledgement bit before closing, so a host detach retry-loop can confirm STOP was actually honored even if a later drag command overtook the STOP command on the latest-wins slot (REVIEWS.md round-3 maintainer decision #5). |
| T-05-06 | Info disclosure | GUARD_ADDR (agent-published object pointer) | accept | Reporting the live object's own memory address to the LOCAL user of a single-user desktop modding tool is not a meaningful disclosure risk — diagnostic information about a process the user already controls and has attached to (matches 05-11's existing T-05-19 disposition). |
| T-05-27 | Elevation of privilege | setScale unconditional-seed regression | mitigate | The legacy Scale RVA is installed in exactly ONE place (the post-`resolveFromExe()` conditional-seed), gated on a CONFIRMED-negative `isAdvertisedClient()` result — acceptance criteria grep-verify both the null initial seed AND the single conditional-install site, closing the BLOCKER (REVIEWS.md Fix A) structurally, not just by convention. |
| T-05-28 | Spoofing/Tampering | Legacy-only target-resolution RVAs called on the wrong build | mitigate | `getPlayerCreatureObject`/`cachedNetworkIdGetObject`/`getObjectByIdLegacy` are called ONLY from inside an explicit `!isAdvertisedClient()` branch, verified by acceptance-criteria grep — mirrors the identical gate Utinni's own reference code applies at the same call site (network.cpp:74-85). |
| T-05-29 | Spoofing/Tampering | Advertised TWO-STEP target-resolution slots called before resolution | mitigate | `cuiHudGetInstance`/`cuiHudGetTarget`/advertised-`getObjectById` are null-seeded at declare time and only ever invoked after an explicit non-null check (BOTH steps of the two-step chain gated independently) — a missing upstream catalog row degrades to the honest `targetUnavailableOnBuild` liveness bit, never a crash or a silent wrong-target write. |
| T-05-32 | DoS (crash) | Off-thread setter invocation under sustained load, amplified by the cross-build resolver chain (BOTH legacy and advertised) | accept (watched) + mitigate | Architecturally sound per D-01 (the client's own setter is called, which internally handles its own notification/dirty-flag bookkeeping) but genuinely unproven against a real client until 05-12's UAT sustained-drag watch item — the advertised two-step `cuiHudGetInstance()`/`cuiHudGetTarget()` calls AND the legacy `getPlayerCreatureObject()`/`cachedNetworkIdGetObject()` chain both add per-frame resolution steps to the same pre-existing race window (a raw `Object*` resolved then written while the sim thread may free/relocate it) — accepted as the correct design with an explicit, EXPANDED verification step covering BOTH builds' resolver paths, not silently assumed safe on either (2026-07-09: corrects a prior framing that named only the advertised path). ROUND 4 MITIGATION (REVIEWS.md round-3 maintainer decision #4): the entire resolve->read->apply span, including the advertised two-step's intermediate `hudInstance` pointer window, is wrapped in `__try`/`__except` (SEH) — an access violation now degrades to one skipped frame plus a transient `agentFaultRecovered` liveness bit, never an unhandled fault that crashes the injected client; this bounds the blast radius, it does not eliminate the underlying accept-watched risk (a torn write mid-setter-call before the exception fires is still possible in principle). |
| T-05-33 | Repudiation | SCALE_REFUSED vs scaleUnavailableOnBuild conflation | mitigate | SCALE_REFUSED requires BOTH a genuine byte mismatch AND a resolved setter (an actual attempted-and-refused write); an unresolved setter alone is reported exclusively via the one-time `scaleUnavailableOnBuild` liveness bit — the two signals can never be confused by a downstream consumer, closing the false-tamper-banner class of bug (REVIEWS.md ROUND 2 W2 fix). |
| T-05-34 | Tampering | Coalesced REBASELINE_GUARD-plus-apply | mitigate | ROUND 4 (REVIEWS.md round-3 maintainer decision #2, supersedes the round-2/round-3 text below): the rebaseline mutation is now LEXICALLY INSIDE the SAME `cmdSeq`-new + non-torn guard that gates the apply, so it executes at most ONCE per distinct command — round 3's version sat OUTSIDE that guard and silently RE-RAN every frame in the `[revert, next-command]` window, letting a later external tamper get adopted as the new baseline before the user's next drag; that window is now closed structurally (verified by a line-order acceptance grep, not just prose). BOTH channels rebaseline to bytes read/computed THIS SAME gated iteration (transform: fresh `getTransform_o2w` read; scale: the genuine `currentScale` member-offset read, no longer the command's raw `cmd.scale` constant) — it never accepts a value asserted by any OTHER, later command; the apply step that follows uses the ordinary guarded `applyWrite` path, not a separate unguarded write function (REVIEWS.md ROUND 2 Opus MEDIUM fix, coalesce option, as re-gated by ROUND 4). |
| T-05-37 | Tampering | agent_main.cpp scale-guard live-read source | mitigate | `currentScale` (the guard's `liveScale` argument) is sourced from a genuine `m_scale` member-offset read every iteration — unconditionally on legacy (`kLegacyScaleOffset`), gated behind `s_advertisedScaleOffsetConfirmed` on advertised — reaching the SAME external-tamper-detection parity Transform's guard already had wherever confirmed, falling back to a self-compare (`scalePassed` forced true) ONLY when the advertised offset is genuinely unconfirmed, which is published honestly via the `scaleGuardUnavailableOnBuild` liveness bit rather than silently indistinguishable from a verified pass (2026-07-08 plan-check revision, maintainer decision option (a); RE-ARCHITECTED 2026-07-09 from an accessor-call binding to a member-offset read). |
| T-05-38 | Spoofing/Tampering | agent_main.cpp advertised m_scale offset read before confirmation | mitigate | The advertised member-offset read is gated behind `s_advertisedScaleOffsetConfirmed` (defaults `false`, no confirmed advertised offset exists this session) and is never attempted while unconfirmed — an unconfirmed advertised build degrades to the honest `scaleGuardUnavailableOnBuild` liveness bit, never a read at an unvalidated offset (2026-07-09, RE-ARCHITECTED from a null-fn-pointer gate to a confirmed-offset gate; same mitigation shape as T-05-29's null-check idiom for the advertised target-resolution slots). |
| T-05-39 | Tampering | agent_main.cpp single-attach-lifetime guard baseline | mitigate | The guard baseline (`s_expectedTransform`/`s_expectedScale`) is now re-captured whenever the resolved `focus` pointer changes (a static `s_expectedCapturedAgainst` comparison, reset-driven), not just once at attach — closing the ROUND 3 gap where selecting a NEW object after attach compared its live bytes against the ORIGINAL (attach-time/player) baseline, false-blocking its first write and risking a force-write of the wrong object's pose via `revertAll` (REVIEWS.md round-3 maintainer decision #1). |
| T-05-40 | Tampering | Host/agent identity-key mismatch (focusToken vs the host's prior (networkId, templateName) key) | mitigate | ROUND 5 (REVIEWS.md round-4 maintainer decision #1): the agent publishes `focusToken` — the SAME truncated `addrOf(focus)` value `s_expectedCapturedAgainst` re-keys on — into the read frame EVERY iteration, so 05-07's host-side `cowSnapshot`/`writeLog` re-key can key on the IDENTICAL identity signal instead of the coarser `(networkId, templateName)` pair that collapses to `(0, templateName)` on legacy (networkId always 0, Fix E) and lets two same-template objects share one host-side identity. This closes the fail-open two-same-template-object revert bug (a Revert All force-writing object-1's pose onto object-2) and the lossy focus-flip A->B->A bug (05-07's per-identity cache, its own ROUND 5 note) structurally, not by convention — verified by the token-key grep, the two-object revert test, and the flip A->B->A test, all specified in 05-07's Task 2 acceptance criteria. |
| T-05-49 | Tampering | agent_main.cpp s_expectedCapturedAgainst cross-template pointer-ABA | mitigate | ROUND 6 (REVIEWS.md round-5 maintainer decision "option (a) structural", the agent-side half — mirrors 05-07's T-05-45): the re-key mismatch condition now compares BOTH the raw pointer AND the captured-against templateName/networkId; a despawned object's address reallocated to a DIFFERENT-templated object now triggers re-capture instead of silently reusing a stale baseline (`s_expectedCaptured` stays incorrectly `true`, comparing the NEW object's live bytes against the OLD object's baseline). Closes the agent-side half of Opus's round-5 pointer-ABA finding, verified by the identity-cross-check grep. |
| T-05-50 | Tampering | agent-side same-template-realloc residue (mirrors 05-07's T-05-46) | accept (watched) | The narrower residue — same template AND legacy networkId≡0 AND a realloc landing at the EXACT same address as a still-baselined, despawned sibling — still skips re-capture, since T-05-49's identity cross-check compares templateName/networkId, which are IDENTICAL in this specific case. Needs a generation counter or an explicit despawn signal the channel does not carry this round; accepted per the maintainer's explicit "residue to accept-watch, do NOT try to fully close" instruction (REVIEWS.md round-5). Covered by 05-12's despawn-then-retarget UAT watch item (ROUND 6). |
| T-05-51 | Tampering (race) | Command-slot focus-skew (Revert-All-then-retarget, mirrors 05-07's T-05-47) | accept (watched) | The command slot carries no identity — a retarget landing in the ~1-2 frame gap between the host sending a Revert-ALL command and this agent's `applyWrite` for that command applies the snapshot pose to whatever `focus` resolves that tick, since the guard checks external tamper, not intended-vs-current identity. A deterministic bug pre-ROUND-5, now a tight race; accepted for a single-user tool per REVIEWS.md round-5 Opus Item 1 LOW — a principled close would add a command-slot `expectedFocusToken` echo field to the channel (out of scope this round, no growth was budgeted for it). |
</threat_model>

<verification>
Build the x86 agent DLL (`cmake --build packages/live-inject/agent/build-agent --config Release`)
with zero errors; grep-based acceptance criteria above all pass.
</verification>

<success_criteria>
- setTransform_o2w resolves via the single existing graceful-degrade mechanism on both paths; setScale is structurally incapable of holding a stale cross-build address (BLOCKER closed).
- Cross-build target resolution moves the object the player has actually targeted/selected in-game — legacy via the unchanged lookAt-target chain, advertised via the REAL two-step selected-object resolver — never silently the wrong object, and never conflating "nothing selected" with "build cannot select" (ROUND 2 — this closes the narrower "always moves the player, never what's targeted/selected" gap on BOTH builds; it does NOT close, and never claimed to close, the SEPARATE viewport<->live-object binding gap, which has no resolver on either build and stays open until Phase 7's `.ws` placement editing — ROUND 5 scoped language, removing the earlier round-1-closure overclaim framing per REVIEWS.md round-4 maintainer decision #2. The legacy mechanism's grounding was independently re-verified against real source by a 4-member de-anchoring crew, 2026-07-09, confirming Object::networkId is a genuine field even though the more-specific +1432 chain, not the generic getObjectById primitive, is what's actively wired).
- The guard gates every apply independently per channel, with zero bypass paths, and survives STATIC interior-object float rounding across successive writes (dynamic-parent caveat documented, not silently assumed away).
- SCALE_REFUSED never fires for a build where Scale is simply unresolved — that state is honestly reported via the separate scaleUnavailableOnBuild liveness bit (ROUND 2 W2 fix).
- A blocked guard can always be un-stuck by the user's own explicit revert action, via ONE race-free coalesced command (ROUND 2 Opus MEDIUM fix).
- The agent exits cleanly on STOP_REQUESTED even with no player loaded, and legacy networkId is honestly UNAVAILABLE rather than sourced from the wrong field.
- The scale read-verify guard reaches genuine external-tamper-detection parity with the transform guard via a per-build-gated `m_scale` member-offset read (legacy: unconditional; advertised: gated behind offset confirmation); where the advertised offset is not yet confirmed, the gap is honest and observable (`scaleGuardUnavailableOnBuild`), never a silent pass mistaken for a verified one (2026-07-08 plan-check revision, closes the BLOCKER; RE-ARCHITECTED 2026-07-09 from an RVA/catalog-binding attempt after the de-anchoring crew confirmed getScale is inline with no standalone address).
- The guard baseline re-keys on focus-identity change, so selecting a new object mid-session is editable on its own terms, not permanently guard-blocked against the attach-time (player) baseline (REVIEWS.md round-3 maintainer decision #1).
- The REBASELINE_GUARD mutation is structurally confined inside the same cmdSeq-newness guard that protects the apply, and both channels rebaseline to the live value at that gated instant — closing the ungated-rebaseline guard defeat and the transform/scale rebaseline asymmetry in one fix (REVIEWS.md round-3 maintainer decision #2).
- The off-thread UAF risk (T-05-32) remains accept-watched but is now bounded by SEH: a fault degrades to one skipped frame plus a transient liveness bit, never a client crash (REVIEWS.md round-3 maintainer decision #4).
- STOP_REQUESTED cannot be silently overtaken by a later drag command on the latest-wins slot — the agent publishes a sticky STOPPING acknowledgement bit a host retry-loop can observe (REVIEWS.md round-3 maintainer decision #5).
- The baseline re-key's mismatch condition is a pointer OR identity compare, never pointer-only — closing the agent-side half of Opus's round-5 pointer-ABA finding (ROUND 6, REVIEWS.md round-5 maintainer decision "option (a) structural"); the residual same-template-realloc case and the command-apply focus-skew race are both explicit, named accept-watch threat-register entries, not silently omitted.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-SUMMARY.md` when done
</output>
