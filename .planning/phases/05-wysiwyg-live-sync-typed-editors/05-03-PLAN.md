---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/live-inject/agent/rva_table.cpp
  - packages/live-inject/agent/agent_main.cpp
  - packages/live-inject/agent/write.cpp
  - packages/live-inject/agent/write.h
autonomous: true
requirements: [LIVE-03]
user_setup:
  - service: swg-client-v2 (maintainer's advertised client build)
    why: "D-09 requires object::setScale to be advertised via GetEngineHookPoints; this session's research found it absent from the current catalog dump."
    dashboard_config:
      - task: "Add an 'object::setScale' row to s_engineHookPoints[] in engine_advertise.cpp, then rebuild and redeploy the swg-client-v2 client binary before the manual UAT (05-12) can exercise Scale on the advertised target."
        location: "D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp"

must_haves:
  truths:
    - "The agent poll loop applies a pending write command (Move/Rotate via setTransform_o2w, Scale via setScale) ONLY when the read-verify guard passes; on mismatch it sets GUARD_FLAG_WRITE_REFUSED and does not call any setter."
    - "Scale writes go through their own setScale(Vector) call, never through the 48-byte Transform payload (D-09) — on BOTH the legacy SWGEmu RVA path and the swg-client-v2 advertised path."
    - "The agent stops its poll loop cleanly (no leaked thread) when the command slot's STOP_REQUESTED flag is set (D-04 item 1)."
    - "Legacy (SWGEmu) networkId reads the real 64-bit playerCreature-offset value instead of always returning 0 (D-04 item 3)."
  artifacts:
    - path: "packages/live-inject/agent/rva_table.cpp"
      provides: "setTransform_o2w write-slot (RVA 0x00B22CC0) + setScale slot (RVA 0x00B23A10) + advertised binding rows + legacy networkId offset constant"
      contains: "setScale"
    - path: "packages/live-inject/agent/agent_main.cpp"
      provides: "60fps poll loop consuming the command slot, guard-gated apply, stop-signal exit, legacy 64-bit networkId read"
      contains: "channelReadCommand"
  key_links:
    - from: "packages/live-inject/agent/agent_main.cpp"
      to: "packages/live-inject/agent/write.cpp"
      via: "checkWriteGuard(currentXform, s_expectedTransform) called each iteration a new command is seen"
      pattern: "checkWriteGuard"
    - from: "packages/live-inject/agent/rva_table.cpp"
      to: "engine_advertise.cpp (../swg-client-v2, external repo)"
      via: "advertised name \"object::setScale\" — must be added upstream before it resolves on the advertised path"
      pattern: "object::setScale"
---

<objective>
Wire the guarded write path into the agent's poll loop: resolve the two new setter endpoints
(`setTransform_o2w` write-variant, `setScale`) on both the legacy SWGEmu RVA path and the advertised
swg-client-v2 path, consume the command slot from 05-01 each frame, gate every apply through the
read-verify guard, add the D-04 agent-lifecycle debt (clean stop-signal, legacy 64-bit networkId), and
raise the poll rate to 60fps.

Purpose: this is the load-bearing agent-side half of LIVE-03 — the point where "drag a gizmo" becomes
"the object actually moves in the running client," gated by the fail-closed guard from 05-01 (SC #2)
and covering Move/Rotate/Scale on both in-scope targets per D-09 (corrected 2026-07-08 — Scale is NOT
descoped).

Output: extended `rva_table.cpp` + `agent_main.cpp` + a completed `applyWrite` in `write.cpp`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@D:/Code/Utinni/UtinniCore/swg/object/object.cpp
@D:/Code/Utinni/UtinniCore/swg/game/game.cpp
@D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp
</context>

<interfaces>
<!-- Existing rva_table.cpp slots to extend (verbatim structure to mirror) -->
```
pGetTransform_o2w getTransform_o2w = (pGetTransform_o2w)0x00B22C80;
Binding g_agentBindings[] = {
    {"object::getTransform_o2w",      (void**)&getTransform_o2w},
    ...
};
size_t g_agentBindingCount = sizeof(g_agentBindings) / sizeof(g_agentBindings[0]);
```

<!-- Existing agent_main.cpp poll loop shape (lines 132-222) — the loop this task extends -->
```
while (true) {
    void* player = swg::endpoints::getPlayer ? swg::endpoints::getPlayer() : nullptr;
    if (!player) { Sleep(100); continue; }
    // ... existing read of transform/networkId/templateName/liveness, 4-sentinel gate, channelWrite ...
    Sleep(33);  // ~30fps — this task raises to ~16ms (60fps)
}
```

<!-- Ground truth RVAs (Utinni object.cpp) -->
- setTransform_o2w = 0x00B22CC0 (object.cpp:148) — same address family as the existing getTransform_o2w=0x00B22C80
- setScale = 0x00B23A10 (object.cpp:155)
- Legacy networkId: playerCreature struct offset +1432 (game.cpp:702-711) — direct in-process memory read, NOT a function call
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve write endpoints (rva_table.cpp) — setTransform_o2w write-slot, setScale, legacy networkId offset</name>
  <files>packages/live-inject/agent/rva_table.cpp</files>
  <read_first>
    - packages/live-inject/agent/rva_table.cpp (full file — the exact Binding/slot pattern; every new row must follow this verbatim)
    - packages/live-inject/agent/resolve.h (Binding struct, resolve() graceful-degrade contract — a missing advertised name NEVER nulls the slot)
    - D:/Code/Utinni/UtinniCore/swg/object/object.cpp (lines 43-190 — setTransform_o2w RVA 0x00B22CC0 at :148, setScale RVA 0x00B23A10 at :155, calling-convention typedefs)
    - D:/Code/Utinni/UtinniCore/swg/game/game.cpp (lines 41-98, esp. 702-711 — legacy networkId via playerCreature+1432 direct offset read, NOT an RVA function call)
    - D:/Code/swg-client-v2/src/game/client/application/SwgClient/src/win32/engine_advertise.cpp (lines 568-598 — confirm current catalog; setTransform_o2w/setPosition_w ARE advertised at :578/:850; setScale is NOT — this is the gap the user_setup row documents)
  </read_first>
  <action>
    Add a new function-pointer typedef `typedef void(__thiscall* pSetTransform_o2w)(void*, const
    void*);` (opaque Transform* argument, mirrors the existing `pGetTransform_o2w` opaque-pointer
    convention exactly — do not introduce a concrete Transform struct dependency) and a slot
    `pSetTransform_o2w setTransform_o2w = (pSetTransform_o2w)0x00B22CC0;` seeded with the legacy RVA.
    Add `typedef void(__thiscall* pSetScale)(void*, const void*);` (opaque Vector* argument) and
    `pSetScale setScale = (pSetScale)0x00B23A10;`. Append two rows to `g_agentBindings[]`:
    `{"object::setTransform_o2w", (void**)&setTransform_o2w}` (this name IS confirmed advertised —
    engine_advertise.cpp:578/850) and `{"object::setScale", (void**)&setScale}` (this name is NOT
    currently in the advertised catalog this session read — per `resolve()`'s graceful-degrade
    contract, a missing name simply leaves the legacy RVA literal active and untouched; this row is
    still correct to add now so it resolves automatically the moment the maintainer adds the
    upstream row per this plan's `user_setup`). Update `g_agentBindingCount` is automatic (sizeof
    arithmetic, no manual count to bump). Add a legacy-path constant for the D-04 networkId gap:
    `extern const uintptr_t k_legacyNetworkIdOffset = 1432;  // playerCreature+1432, Utinni game.cpp:702-711 — direct in-process offset read, no RVA function call exists for this on the legacy path` —
    declare it `extern` (external linkage) exactly like the existing `k_mainLoopCounter_addr` pattern
    (line 68 of the current file) so agent_main.cpp (Task 2) can reference it. Update every new row's
    inline comment to cite the exact Utinni file:line the way every existing row already does.
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `g_agentBindings[]` contains exactly two new entries: "object::setTransform_o2w" and "object::setScale", each with a comment citing the Utinni object.cpp line the legacy RVA was harvested from.
    - `k_legacyNetworkIdOffset` is declared `extern const uintptr_t` with value 1432 and a comment citing game.cpp:702-711.
    - The x86 agent DLL still compiles cleanly (no new linkage errors).
  </acceptance_criteria>
  <done>Both write endpoints are resolvable via the existing single graceful-degrade mechanism (legacy RVA active by default, advertised name overwrites it when present) — no second resolution mechanism was introduced (Pitfall 5).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poll-loop integration — guarded command consumption, Scale write, stop-signal, legacy networkId</name>
  <files>packages/live-inject/agent/agent_main.cpp, packages/live-inject/agent/write.cpp, packages/live-inject/agent/write.h</files>
  <behavior>
    - Each loop iteration (now ~16ms / 60fps): after the existing read+4-sentinel-gate+channelWrite
      step, call `channelReadCommand(&cmd, &cmdSeq)`. If `cmd.flags & CMD_FLAG_STOP_REQUESTED`: break
      out of the loop (do not process any further command), call `channelClose()`, and let the
      function return 0 — this is the D-04.1 clean stop-signal, reusing the existing loop rather than
      spawning any new thread.
    - If `cmdSeq` is a NEW value (differs from a static `lastAppliedCmdSeq`, initialized to 0) AND the
      read was not torn: run `checkWriteGuard(xform, s_expectedTransform)` (from write.cpp/05-01) using
      the transform just read this same iteration. On pass: call `setTransform_o2w(player,
      &cmd.transform)` AND `setScale(player, &cmd.scale)` (both, unconditionally — the command always
      carries the full target pose per 05-01's design, so both setters are called every applied
      command; this correctly handles Move/Rotate/Scale/Universal gizmo modes without a mode
      discriminator), update `s_expectedTransform = cmd.transform`, `channelWriteGuardStatus(0)`
      (clear WRITE_REFUSED), and set `lastAppliedCmdSeq = cmdSeq`. On fail: call
      `channelWriteGuardStatus(GUARD_FLAG_WRITE_REFUSED)` and do NOT call any setter — this is the
      fail-closed path (D-03 SC #2, no force-write escape hatch exists anywhere in this loop).
    - `s_expectedTransform` (a new static `float s_expectedTransform[3][4]` in agent_main.cpp or
      write.cpp — executor's choice of ownership, but must be initialized) is captured from the FIRST
      sentinel-passing transform read after channel open (the D-03 "attach-time" snapshot) — add a
      static `bool s_expectedCaptured = false` guard so this only happens once per agent lifetime, not
      every loop iteration.
    - Legacy networkId (D-04 item 3): when `swg::endpoints::getNetworkId` is null (legacy path), read
      8 bytes directly at `player_ptr + k_legacyNetworkIdOffset` via
      `*reinterpret_cast<const volatile uint64_t*>(reinterpret_cast<uintptr_t>(player) + swg::endpoints::k_legacyNetworkIdOffset)`
      instead of leaving `netId = 0` — this is a direct in-process memory read (the agent runs inside
      the target process), not a ReadProcessMemory call.
  </behavior>
  <read_first>
    - packages/live-inject/agent/agent_main.cpp (full file — the exact loop to extend, lines 132-222 especially)
    - packages/live-inject/agent/write.h (from 05-01 — checkWriteGuard signature, applyWrite stub)
    - packages/live-inject/agent/channel.h (from 05-01 — LiveCommand struct, CMD_FLAG_STOP_REQUESTED, GUARD_FLAG_WRITE_REFUSED, channelReadCommand/channelWriteGuardStatus signatures)
    - This plan's Task 1 output (rva_table.cpp new slots + k_legacyNetworkIdOffset)
  </read_first>
  <action>
    Add extern references at the top of agent_main.cpp (mirroring the existing extern block, lines
    68-76) for `swg::endpoints::setTransform_o2w`, `swg::endpoints::setScale`, and
    `swg::endpoints::k_legacyNetworkIdOffset`. Change the poll `Sleep(33)` to `Sleep(16)` (60fps, per
    LIVE-03 SC1). Implement the command-consumption block described in this task's `<behavior>`
    exactly after the existing sentinel-gated `channelWrite(&state)` call, inside the same
    `while(true)` loop — no new thread, no new loop. Implement the legacy-networkId direct-offset read
    exactly where the existing `netId = 0` fallback currently sits (inside the `if
    (swg::endpoints::getNetworkId) {...} else {...}` — the `else` branch currently does nothing; add
    the direct read there). Move `checkWriteGuard`'s actual invocation into agent_main.cpp (it is a
    pure comparator from write.h, called with the two `float[3][4]` values agent_main.cpp already has
    in scope) — complete write.cpp's previously-stubbed `applyWrite` function now that the concrete
    setter fn-pointer types exist: `void applyWrite(void* player, const LiveCommand& cmd)` internally
    calls `swg::endpoints::setTransform_o2w(player, &cmd.transform)` and
    `swg::endpoints::setScale(player, &cmd.scale)`; agent_main.cpp calls this single function on a
    guard pass rather than calling both setters inline (keeps the setter-invocation logic in one
    place per write.h's original contract). Add the stop-signal check as the FIRST thing checked each
    loop iteration after computing `player` (so a stop request is honored even before a valid player
    exists), per this task's `<behavior>`.
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/agent/build-agent --config Release 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms `Sleep(16)` replaces the prior `Sleep(33)` in the poll loop.
    - grep confirms a `CMD_FLAG_STOP_REQUESTED` check exists and precedes a `channelClose()` + `return 0` exit path.
    - grep confirms `applyWrite` calls both `setTransform_o2w` and `setScale` — never one without the other — and that this call site is reached ONLY inside the `checkWriteGuard(...).passed` branch (no call site outside that guard).
    - grep confirms the legacy networkId `else` branch now dereferences `k_legacyNetworkIdOffset` rather than leaving netId unset.
    - Agent DLL builds cleanly.
  </acceptance_criteria>
  <done>The agent applies guarded Move/Rotate/Scale writes at 60fps, exits its poll loop cleanly on a stop signal, and reports a real legacy networkId — all three D-04 debt items plus the D-09 Scale write path are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Command slot (host-writable) → agent poll loop → live game object memory | The agent is the last line of defense before a real write touches the running game's object state; the guard MUST run before every apply, with no code path that skips it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | agent_main.cpp command-consumption block | mitigate | `applyWrite` (both setters) is called from exactly one call site, guarded by `checkWriteGuard(...).passed` — verified by acceptance_criteria grep, not just code review; no alternate/debug bypass path exists. |
| T-05-05 | DoS (resource leak) | agent poll thread lifecycle | mitigate | STOP_REQUESTED is checked at the top of every iteration (before the player-null early-continue), guaranteeing the thread exits even if no player is currently loaded — repeated attach/detach cycles cannot accumulate poll threads. |
| T-05-06 | Info disclosure | legacy networkId direct offset read | accept | Reading a fixed, publicly-known (Utinni-documented) struct offset inside the toolkit's own injected agent, in a single-user local desktop tool — no new remote attack surface versus the already-accepted Phase-3 posture. |
</threat_model>

<verification>
Build the x86 agent DLL (`cmake --build packages/live-inject/agent/build-agent --config Release`)
with zero errors; grep-based acceptance criteria above all pass.
</verification>

<success_criteria>
- setTransform_o2w (write) and setScale resolve via the single existing graceful-degrade mechanism on both legacy and advertised paths (Scale's advertised resolution depends on the upstream engine_advertise.cpp row documented in `user_setup`).
- The guard gates every apply with zero bypass paths.
- The agent exits cleanly on STOP_REQUESTED and reports a real legacy networkId.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-SUMMARY.md` when done
</output>
