---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 06
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - packages/renderer/src/shared/VerificationStatus.tsx
  - packages/renderer/src/panels/editors/shared/GateBar.tsx
  - packages/renderer/src/panels/editors/shared/FailBanner.tsx
  - packages/renderer/src/panels/editors/dtiiTypeSpec.ts
  - packages/renderer/src/panels/editors/dtiiTypeSpec.test.ts
  - packages/renderer/src/panels/editors/DatatableGridEditor.tsx
autonomous: true
requirements: [DATA-01]

# REVISION NOTE (--reviews replan, 2026-07-08): folds in two 05-REVIEWS.md MEDIUM findings (Sonnet).
# (1) "DTII type-spec sub-parser (enum labels / bitvector / [default] suffix) has no task/artifact/
#     fixture — nontrivial DataTableColumnType.cpp chomp/getDelimStr porting hidden inline in the
#     already-large 05-06 UI task, with zero fixture coverage." FIX: carved into a NEW Task 2,
#     `dtiiTypeSpec.ts` — a pure, independently-tested module (mirrors the native
#     `physicalTypeForSpec` dispatch but for the FULL semantic type-spec string: enum label maps,
#     bitvector flag maps, PackedObjVars marker), consumed by Task 3's grid (was Task 2).
# (2) "`z(tableName)` DTII column can crash the edit widget — 05-06 groups z(...) with e(...) Enum for
#     widget dispatch without special-casing z(...)'s syntax (bare table name, no key=value) against
#     the e(a=0,b=1,...) label-map parser." FIX: dtiiTypeSpec.ts's parser special-cases a leading
#     'z(' as a DISTINCT variant (`{ kind: 'enum-table'; tableName: string }`) BEFORE attempting the
#     e(...) key=value grammar — Task 3's grid renders z(...) columns as read-only numeric (no
#     dropdown, no cross-table DataTableManager resolution — D-12/Open Question 2's safe default),
#     never routes them through the enum-label-map widget.
# Plan restructured from 2 tasks to 3 to give the sub-parser its own fixture-covered artifact per the
# review; total scope is unchanged otherwise (Task 1 = shared gate components, unchanged from before).

must_haves:
  truths:
    - "GateBar/FailBanner are ONE shared implementation reused by both the DTII editor (this plan) and the .stf editor (05-09) — sibling anatomy, not two parallel implementations (UI-SPEC Component Inventory)."
    - "The gate is a first-class 4-state machine (not run / running / pass / fail) with glyph+border-style+text triple-encoding, not color alone (UI-SPEC Surface 2 item 7, Accessibility Rule 1)."
    - "A pure, independently-tested dtiiTypeSpec.ts module parses the full type-spec grammar (enum label maps, bitvector flag maps, hashstring/bool/packedObjVars markers) SEPARATELY from the grid component, with its own fixture-backed test coverage (REVIEWS.md MEDIUM — sub-parser carve-out)."
    - "z(tableName) is parsed as a DISTINCT variant from e(a=0,b=1,...) and is NEVER run through the enum key=value label-map grammar — the grid renders it as read-only numeric, not a mis-parsed/throwing dropdown (REVIEWS.md MEDIUM — z(...) crash fix)."
    - "The grid is virtualized using the existing VfsTree/HexInspector ROW_HEIGHT+OVERSCAN+ResizeObserver idiom — real datatables have thousands of rows."
  artifacts:
    - path: "packages/renderer/src/panels/editors/shared/GateBar.tsx"
      provides: "Footer gate-state chip (not run/running/pass/fail) — shared by DTII and .stf"
      contains: "round-trip gate"
    - path: "packages/renderer/src/panels/editors/dtiiTypeSpec.ts"
      provides: "parseTypeSpec(raw: string) -> TypeSpecInfo discriminated union (int/float/string/hashstring/bool/enum/bitvector/packedObjVars/enum-table), with z(...) parsed as a distinct enum-table variant"
      contains: "enum-table"
    - path: "packages/renderer/src/panels/editors/DatatableGridEditor.tsx"
      provides: "Dockview tab title + mod-dot, crumb bar (FORM DTII ▸ FORM 0001 ▸ DATA, Grid|Hex toggle, action buttons), grid toolbar (Filter rows…, +Row/-Row, count chip), virtualized typed grid (type badges, sort, double-click edit, modified triple-encoding), cell widgets driven by dtiiTypeSpec.ts"
      min_lines: 150
  key_links:
    - from: "packages/renderer/src/panels/editors/DatatableGridEditor.tsx"
      to: "packages/renderer/src/panels/editors/dtiiTypeSpec.ts"
      via: "parseTypeSpec(column.typeSpec) drives which cell widget renders"
      pattern: "parseTypeSpec"
    - from: "packages/renderer/src/panels/editors/DatatableGridEditor.tsx"
      to: "packages/renderer/src/panels/editors/shared/GateBar.tsx"
      via: "renders <GateBar> in its footer"
      pattern: "GateBar"
    - from: "packages/renderer/src/panels/editors/shared/GateBar.tsx"
      to: "packages/renderer/src/shared/VerificationStatus.tsx"
      via: "extends VerificationVariant with a 'running' state rather than a parallel pill component"
      pattern: "'running'"
---

<objective>
Build the shared gate-state UI (`GateBar`/`FailBanner`, extending the existing `VerificationStatus`
pill), a dedicated, independently-tested DTII type-spec interpreter module (`dtiiTypeSpec.ts` — carved
out per this review round so the nontrivial enum/bitvector grammar has its own fixture coverage, not
buried inline in the grid component), and the DTII grid editor's core interaction surface (crumb bar,
toolbar, virtualized typed grid with sort/edit/modified-encoding) — the first half of Surface 2
(UI-SPEC 014-D), and the reference implementation the `.stf` editor (05-09) will reuse.

Purpose: DATA-01 requires a real, editable, virtualized datatable grid. This is also the natural
"spine" plan per CONTEXT's Claude's-Discretion note — the shared gate machinery is built once here as
the reference consumer, then reused by `.stf` (05-09) as the sibling consumer.

Output: `GateBar.tsx`, `FailBanner.tsx` (new shared components), an extended `VerificationStatus.tsx`,
`dtiiTypeSpec.ts` (new, pure, tested), and `DatatableGridEditor.tsx`'s crumb/toolbar/grid anatomy
(schema rail, hex toggle, and gate-bar WIRING to the native round-trip land in 05-08, which depends on
this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
</context>

<interfaces>
Existing VerificationStatus.tsx (full contract to extend, not replace):
```tsx
export type VerificationVariant = 'pass' | 'fail' | 'warn' | 'parse-error' | 'neutral';
const VARIANT_CONFIG: Record<VerificationVariant, { glyph: string; colorVar: string; ariaRole?: string }> = {
  pass: { glyph: '✓', colorVar: 'var(--color-accent)' },
  fail: { glyph: '✕', colorVar: 'var(--color-danger)' },
  ...
};
export default function VerificationStatus({ variant, caption, ariaLabel, compact }): React.ReactElement
```

VfsTree.tsx virtualization scaffold to copy verbatim (constant names + math, only ROW_HEIGHT value and
row-renderer differ per surface): `ROW_HEIGHT = 30; OVERSCAN = 8;` + `ResizeObserver` -> `viewHeight`
state; `onScroll` -> `scrollTop` state; `startRow`/`endRow`/`topPad`/`bottomPad` derivation; top-pad
spacer + windowed rows + bottom-pad spacer inside a `totalHeight`-tall inner div.

UI-SPEC Surface 2 anatomy (05-UI-SPEC.md, this plan covers items 1-4; items 5-8 are 05-08):
1. Dockview tab: `<file>.iff — Datatable` + warn `●` mod-dot.
2. Crumb bar (`--color-header` bg, mono): `FORM DTII ▸ FORM 0001 ▸ DATA` · `Grid | Hex` segmented
   toggle · spacer · `⇄ Compare to base` (ghost) · `＋ Stage` (ghost) · `Save · run gate` (primary).
3. Grid toolbar: `Filter rows…` text input · `＋ Row` · `− Row` · right-aligned count chip
   `N rows · M cols[ · K cell(s) modified]`.
4. The grid: sticky header (column name + type badge `s`/`i`/`f` + extended badges for the D-07
   widened set + accent sort arrow), sticky left row-number column (warn `●` when row modified),
   cells (mono, hover widget bg, `cursor: cell`, double-click to edit — inline input, accent-line
   border, Enter commits/Escape cancels/blur commits, typed coercion per column type). Modified-cell
   triple-encoding: inset 3px warn left border + `rgba(230,180,80,0.07)` bg + `" ●"` warn suffix →
   row-number `●` → tab `●`. Row click selects (accent-dim row).

Type badge hexes (UI-SPEC Color, sketch-locked, do NOT theme-switch): `s` `#7fb2ff` on
`rgba(120,180,255,.13)` · `i` `#7dd68a` on `rgba(160,255,170,.10)` · `f` `#e8b46a` on
`rgba(255,200,120,.12)`. D-07 widens the badge SET beyond s/i/f (Enum/Bool/BitVector/HashString/
PackedObjVars all need a distinct letter+color pair within the same idiom) — this plan's Task 3
defines the full badge set; z(...)-sourced Enum uses the same letter as plain Enum for the BADGE
(both are `getBasicType()===Int`, D-12) but a DIFFERENT (read-only, no-dropdown) WIDGET per
`dtiiTypeSpec.ts`'s `enum-table` variant (REVIEWS.md MEDIUM fix — badge sameness is cosmetic and
fine; widget behavior must diverge).

05-02's per-cell native output (consumed here): each cell is `{ type: 'int'|'float'|'string', value,
byteOffset, byteLength }`; each column is `{ name, typeSpec }` where `typeSpec` is the RAW type-spec
string this plan's `dtiiTypeSpec.ts` interprets (native does NOT interpret it beyond the 3-way
physical dispatch, per D-12).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Shared gate-state components — extend VerificationStatus, add GateBar + FailBanner</name>
  <files>packages/renderer/src/shared/VerificationStatus.tsx, packages/renderer/src/panels/editors/shared/GateBar.tsx, packages/renderer/src/panels/editors/shared/FailBanner.tsx</files>
  <read_first>
    - packages/renderer/src/shared/VerificationStatus.tsx (full file — the ONE existing shared glyph+color+caption status-pill component in this codebase)
    - 05-UI-SPEC.md Surface 2 item 7 (Gate bar state table) and item 8 (Fail banner copy contract)
    - 05-UI-SPEC.md Copywriting Contract table (exact gate-state strings for both DTII and .stf — this plan's GateBar accepts copy as props so both editors can supply their own state strings against the SAME component)
  </read_first>
  <action>
    Extend `VerificationVariant` in VerificationStatus.tsx with a `'running'` member and its
    `VARIANT_CONFIG` entry (info-colored text, `--color-info`) — do NOT build a second pill component;
    this project has exactly one status-pill implementation and this phase's gate states are a
    superset of what it already models (not-run maps to 'neutral', pass/fail map to existing
    variants, running is the one new state). Because 'running' additionally needs a DASHED border
    (not just a color change — UI-SPEC: "running: info text, dashed border"), add an optional
    `dashedBorder?: boolean` prop to `VerificationStatusProps` that wraps the pill in a bordered
    `<span>` when true, defaulting to false so every existing call site (IffStructureTree etc.) is
    unaffected. Create GateBar.tsx: a footer bar (`--color-header` bg, per UI-SPEC) accepting props
    `{ state: 'not-run' | 'running' | 'pass' | 'fail'; notRunLabel: string; runningLabel: string;
    passLabel: string; failLabel: string; onSaveRunGate?: () => void }` — renders a `VerificationStatus`
    instance mapped from `state` (not-run→neutral, running→running+dashedBorder, pass→pass,
    fail→fail) with the caller-supplied label text for that state (this is how DTII's "round-trip
    gate: not run" / "✓ byte-exact round-trip (<N> B)" and .stf's "gate: rebuilding index +
    payload…" / "✓ byte-exact round-trip · CRC index rebuilt" share ONE component with different
    copy). When `state==='pass'`, ALSO render the `→ staged in working changes` info chip
    immediately adjacent (per UI-SPEC — appears only on pass). Create FailBanner.tsx: a danger-tinted
    (`rgba(230,90,90,.08)` bg per UI-SPEC Color) banner accepting `{ message: string; actions:
    Array<{ label: string; onClick: () => void; variant?: 'default' | 'danger' }> }`, rendering the
    message text and a row of action buttons (DTII uses `Jump to bytes` + `Revert cell`; .stf inherits
    the same anatomy per UI-SPEC Surface 3 item 6) with `role="alert"` on the banner container
    (Accessibility contract). Neither GateBar nor FailBanner import anything DTII-specific or
    .stf-specific — both are pure presentational components driven entirely by props, so 05-09 can
    import them unchanged.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- VerificationStatus</automated>
  </verify>
  <acceptance_criteria>
    - VerificationVariant includes 'running' and existing call sites (grep for VerificationStatus usages outside this plan's new files) still compile without prop changes.
    - GateBar renders four visually distinct states (not-run/running/pass/fail) verified by a snapshot or explicit prop-driven test per state.
    - FailBanner's root element has `role="alert"`.
  </acceptance_criteria>
  <done>One shared gate-state implementation exists, ready for both DTII (this plan's Task 3, wired in 05-08) and .stf (05-09) to consume without divergence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dtiiTypeSpec.ts — pure type-spec grammar interpreter (enum labels, bitvector flags, z(...) special-cased) with fixture coverage</name>
  <files>packages/renderer/src/panels/editors/dtiiTypeSpec.ts, packages/renderer/src/panels/editors/dtiiTypeSpec.test.ts</files>
  <behavior>
    - `parseTypeSpec(raw: string)` returns a discriminated union: `{kind:'int'}` (raw==='i'),
      `{kind:'float'}` (raw==='f'), `{kind:'string'}` (raw==='s'), `{kind:'hashstring'}` (raw starts
      'h'), `{kind:'bool'}` (raw starts 'b'), `{kind:'packedObjVars'}` (raw starts 'p'),
      `{kind:'enum'; labels: Record<string, number>}` for `e(a=0,b=1,...)` (parses the
      comma-separated `label=value` pairs into a map), `{kind:'bitvector'; flags: Record<string,
      number>}` for `v(a=1,b=2,...)` (parses `label=bitIndexOrMask` pairs — port the EXACT semantic
      from DataTableColumnType.cpp: value is a 1-based bit index, wire value is `1 << (bit-1)`, so
      `flags` stores the RAW parsed integer and the module also exports a `bitVectorFlagToMask(bit:
      number)` helper), `{kind:'enum-table'; tableName: string}` for `z(tableName)` — Test: parsing
      `z(faction_table.iff)` MUST return `{kind:'enum-table', tableName:'faction_table.iff'}`, NEVER
      attempt the `e(...)` label=value grammar on it (the `z(` prefix check runs BEFORE the `e(` check
      in the dispatch, and `z(...)`'s inner content is a bare table name with no `=` — a regression
      test asserts parsing it does NOT throw and does NOT return an `enum` variant).
    - Edge cases: an unrecognized/malformed type-spec (e.g. empty string, or `e(` with no closing
      paren) returns `{kind:'unknown'; raw: string}` rather than throwing — the UI layer (Task 3)
      decides how to degrade (read-only fallback), this module never throws on malformed input (it is
      a display/widget-selection helper, not a validating parser — the native layer, 05-02, already
      enforces the physical 3-way dispatch and is the actual safety boundary).
    - A `[default]`-suffix marker (e.g. `s[required]`, `e(a=0,b=1)[a]`) is parsed as an optional
      trailing `defaultLabel?: string` field on any variant that has one in a real type-spec string
      (per `DataTableColumnType.cpp`'s bracket-suffix convention) — captured but not required to be
      acted on by Task 3 in this plan (SchemaRail/05-08 may surface it later); the parser must not
      choke on its presence.
  </behavior>
  <read_first>
    - D:/Code/swg-client-v2/src/engine/shared/library/sharedUtility/src/shared/DataTableColumnType.cpp (lines 84-232 — chomp/getDelimStr dispatch, enum/bitvector label-map parsing, bracket-suffix convention; this is the EXACT grammar to port, semantically — not the physical read/write, just the type-spec STRING interpretation)
    - 05-RESEARCH.md "Pattern 2: DTII column-type interpretation" (the verified dispatch table this module implements)
    - 05-REVIEWS.md's two MEDIUM findings this task resolves (DTII type-spec sub-parser carve-out; z(...) crash fix)
  </read_first>
  <action>
    Create dtiiTypeSpec.ts as a pure, framework-free TS module (no React, no native-core import) —
    export the `TypeSpecInfo` discriminated union described in `<behavior>` and the `parseTypeSpec`
    function. Implement dispatch in this ORDER (matching the review's required z(...) special-casing):
    check for a leading `z(` FIRST (extract the table name between the parens, trimming whitespace,
    strip an optional bracket-suffix default marker) and return `enum-table` immediately — do this
    BEFORE attempting the `e(` branch, so a `z(...)` string is structurally impossible to reach the
    `e(...)` key=value parser. Then check `e(`, `v(`, single-char `i`/`f`/`s`/`h`/`b`/`p` in the order
    DataTableColumnType.cpp's dispatch uses. For `e(...)`/`v(...)`, split on top-level commas (respect
    that labels/values themselves never contain commas per the real grammar — no need for
    quote/escape handling), then split each entry on `=` into label/value, `parseInt` the value.
    Export `bitVectorFlagToMask(bit: number): number` returning `1 << (bit - 1)` (ported from the
    verified `DT_BitVector` wire-encoding note in RESEARCH.md's Code Examples). Create
    dtiiTypeSpec.test.ts with fixture-style test cases covering: every single-char type ('i','f','s',
    'h','b','p'), a multi-entry `e(a=0,b=1,c=2)` (asserts the exact label→value map), a multi-entry
    `v(a=1,b=2,d=4)` (asserts the exact label→bit map AND `bitVectorFlagToMask(2) === 2`,
    `bitVectorFlagToMask(3) === 4`), the z(...) special case (`z(faction_table.iff)` → `enum-table`,
    never `enum`), a malformed/empty string (→ `unknown`, no throw), and a bracket-suffix default
    marker on at least one variant (`s[required]` → `{kind:'string', defaultLabel:'required'}` or
    equivalent — parsed, not choked on).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- dtiiTypeSpec</automated>
  </verify>
  <acceptance_criteria>
    - `parseTypeSpec('z(faction_table.iff)')` returns `{kind:'enum-table', tableName:'faction_table.iff'}` — a test asserts this exact shape and that it is NEVER `{kind:'enum', ...}`.
    - `parseTypeSpec('e(a=0,b=1,c=2)')` returns a label map with exactly 3 entries matching the literal values.
    - `parseTypeSpec('v(a=1,b=2,d=4)')` combined with `bitVectorFlagToMask` reproduces the OR'd-flags wire encoding from RESEARCH.md's dispatch table.
    - `parseTypeSpec('')` and `parseTypeSpec('e(malformed')` both return `{kind:'unknown', ...}` without throwing.
    - Module has zero React/native-core imports (grep confirms — pure TS only).
  </acceptance_criteria>
  <done>The nontrivial DTII type-spec grammar is a pure, independently-tested module with real fixture coverage — no longer buried inline in the grid component — and z(...) is structurally incapable of reaching the enum label-map parser.</done>
</task>

<task type="auto">
  <name>Task 3: DatatableGridEditor — crumb bar, toolbar, virtualized typed grid (widgets driven by dtiiTypeSpec.ts)</name>
  <files>packages/renderer/src/panels/editors/DatatableGridEditor.tsx</files>
  <read_first>
    - packages/renderer/src/panels/tre/VfsTree.tsx (full file — ROW_HEIGHT/OVERSCAN/ResizeObserver virtualization scaffold, lines 43-118, to copy verbatim; VfsRow's isSelected/isStaged/hover idioms, lines 257-443, for row-state styling structure)
    - packages/renderer/src/panels/DatatablePanel.tsx (full file — the placeholder this editor supersedes; note its `useNoProjectDimStyle` import, not reused here since this is a main-editor-group tab, not a dim-gated bottom panel)
    - 05-UI-SPEC.md Surface 2 items 1-4 (exact crumb/toolbar/grid anatomy + Copywriting Contract table for exact strings) and D-07's type-badge-widening note in 05-CONTEXT.md
    - This plan's Task 1 output (GateBar import path — GateBar itself is wired in 05-08, but the component must exist for this file to import its type, if the executor chooses to stub the footer now) and Task 2 output (`dtiiTypeSpec.ts` — the ONLY place cell-widget selection logic should live; do not re-implement type-spec parsing inline in this file)
  </read_first>
  <action>
    Create DatatableGridEditor.tsx as a dockview panel component (`IDockviewPanelProps`) accepting the
    parsed DTII result (columns+rows, from 05-02's `parseDataTable` shape, including per-cell
    `byteOffset`/`byteLength`) plus the source virtual path as panel params. Render, top to bottom:
    (1) a crumb bar matching UI-SPEC copy exactly — `FORM DTII ▸ FORM 0001 ▸ DATA` (mono, tag-bold) ·
    a `Grid | Hex` segmented toggle (local state, Hex view itself is wired in 05-08 — this task renders
    the toggle control and a placeholder Hex pane target) · spacer · `⇄ Compare to base` ghost button
    (disabled/tooltip per UI-SPEC Flagged Assumption 7 — sketch 015 not yet designed, do not drop the
    button) · `＋ Stage` ghost button (wired in 05-08, stub the handler here) · `Save · run gate`
    primary button (accent-dim bg per UI-SPEC Color "Accent reserved for" #1, wired in 05-08). (2) A
    grid toolbar: `Filter rows…` text input filtering by the FIRST (name) column, `＋ Row` / `− Row`
    buttons (in-memory only — mutating local edit-buffer state, no disk/staging effect per the UI-SPEC
    destructive-actions table — "removal is an in-memory edit, protected by the gate"), and a
    right-aligned count chip `<N> rows · <M> cols[ · <K> cell(s) modified]` (faint mono). (3) The
    virtualized grid itself, copying VfsTree.tsx's ROW_HEIGHT/OVERSCAN/ResizeObserver scaffold
    verbatim (only ROW_HEIGHT value — 5px/10px cell padding per UI-SPEC — and the row-renderer
    differ): sticky header row with one column per DTII column, each header cell showing the column
    name plus a type badge derived by calling `parseTypeSpec(column.typeSpec)` (from Task 2 — do NOT
    re-implement the dispatch inline). Define the FULL widened badge set per D-07 keyed off
    `TypeSpecInfo.kind` (not just s/i/f): `int`→`i`(`#7dd68a`/`rgba(160,255,170,.10)`),
    `float`→`f`(`#e8b46a`/`rgba(255,200,120,.12)`), `string`→`s`(`#7fb2ff`/`rgba(120,180,255,.13)`),
    `hashstring`→`h`, `bool`→`b`, `enum`→`e`, `bitvector`→`v`, `packedObjVars`→`p`, `enum-table`→`e`
    (SAME badge letter/color as plain `enum` per D-12 — both are `getBasicType()===Int` — but see cell
    rendering below for the WIDGET divergence) — each badge is a `700 10px mono` pill per UI-SPEC
    Typography; pick a consistent accent-family color per badge (documented inline) since the sketch
    only locked s/i/f — this is the D-07 in-idiom extension the CONTEXT flags as the planner's job.
    Accent sort arrow (`▲`/`▼`) on the sorted column header, click-to-sort/toggle-direction. Sticky
    left row-number column (faint mono, right-aligned, `--color-header` bg) showing a warn `●` prefix
    when any cell in that row is modified. Cell rendering: mono text, `cursor: cell`, hover
    `--color-widget` bg, double-click opens an inline widget selected by `parseTypeSpec(column.
    typeSpec).kind` — `int`/`float`: numeric `<input>`; `string`/`hashstring`/`packedObjVars`: text
    `<input>` (packedObjVars gets the structured name|type|value editor as the D-07 "full inline
    edit" requirement — build a minimal pipe-delimited-field editor, not a bare text box); `bool`: a
    checkbox bound to the underlying int being 0/1; `enum`: a `<select>` populated from
    `TypeSpecInfo.labels`, committing the looked-up int value; `bitvector`: a multi-select checkbox
    group over `TypeSpecInfo.flags`, committing the OR'd `bitVectorFlagToMask` result; **`enum-table`:
    a READ-ONLY numeric display — no dropdown, no cross-table `DataTableManager` resolution (D-12/Open
    Question 2's safe default) — REVIEWS.md MEDIUM fix: this is the ONLY widget divergence from plain
    `enum`, and it is enforced by construction since `dtiiTypeSpec.ts` returns a DISTINCT `kind` for
    z(...) that this switch never routes into the `enum` branch.** Enter commits/Escape
    cancels/blur commits for all widgets. Modified-cell triple-encoding exactly as specified: inset 3px
    warn left border + `rgba(230,180,80,0.07)` bg + `" ●"` warn suffix, propagating to the row-number
    `●` and (via a prop callback up to the dockview tab title) the tab `●`. Row click sets a
    `selectedRowIndex` local/lifted state (accent-dim row bg) — 05-08's SchemaRail reads this to
    populate its "Selected row" section.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- DatatableGridEditor</automated>
  </verify>
  <acceptance_criteria>
    - The grid uses the same ROW_HEIGHT/OVERSCAN constant names and windowing math as VfsTree.tsx (grep confirms `OVERSCAN` present).
    - Every one of the 9 non-Comment type-spec kinds (int/float/string/hashstring/bool/enum/bitvector/packedObjVars/enum-table) has a distinct badge+widget pairing defined in one place (not scattered inline literals), and NONE of this dispatch logic is duplicated from `dtiiTypeSpec.ts` (grep confirms `parseTypeSpec` is imported, not re-implemented).
    - A cell whose column typeSpec is `z(...)` renders a read-only numeric display with no `<select>`/dropdown element present (component test asserts no interactive enum widget for that column).
    - A modified cell shows all three encodings simultaneously (border + tint + glyph) — verified by a component test asserting the rendered className/style + text content, not just a snapshot.
    - `Filter rows…` with no matches renders `No rows match "<query>"` (muted, centered) per the Copywriting Contract — never a silently blank grid.
  </acceptance_criteria>
  <done>DatatableGridEditor renders the full crumb/toolbar/grid anatomy from UI-SPEC Surface 2 items 1-4, with the D-07 widened type-badge set and per-type inline editors driven entirely by the tested `dtiiTypeSpec.ts` module (z(...) safely read-only), ready for 05-08 to complete with the schema rail, hex toggle, and gate wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-typed cell edit → in-memory DataTableResult | Edits must be coerced/validated per column type before they can ever reach the native serializer (05-08's gate wiring), never trusted as raw strings. |
| Type-spec string (from a real or hand-crafted .iff) → widget dispatch | A malformed/unexpected type-spec string must degrade to `unknown`/read-only, never crash the editor — `dtiiTypeSpec.ts`'s explicit non-throwing contract is the mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | DatatableGridEditor inline cell editors | mitigate | Typed coercion (parseInt/parseFloat for numeric physical types) happens at commit time in this component, before any value reaches the edit-buffer that 05-08's gate/serialize step consumes — an invalid numeric entry is rejected/reverted at the input, never silently coerced to NaN/0. |
| T-05-29 | DoS (parse crash) | dtiiTypeSpec.ts parseTypeSpec | mitigate | Malformed/unrecognized type-spec strings return `{kind:'unknown'}` rather than throwing (acceptance-criteria-tested), and `z(...)` is structurally routed away from the `e(...)` key=value grammar before that parser ever sees it — closes REVIEWS.md's z(...) crash finding. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- VerificationStatus dtiiTypeSpec DatatableGridEditor` — green.
</verification>

<success_criteria>
- GateBar/FailBanner are a single shared implementation, importable unchanged by 05-09.
- dtiiTypeSpec.ts is a pure, independently-tested module covering the full type-spec grammar, with z(...) structurally distinct from enum.
- DatatableGridEditor's crumb/toolbar/grid anatomy matches UI-SPEC Surface 2 items 1-4 element-for-element.
- All 9 non-Comment DTII type-spec kinds have distinct, documented badge+widget encodings.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-06-SUMMARY.md` when done
</output>
</output>
