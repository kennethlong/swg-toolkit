---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 06
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - packages/renderer/src/shared/VerificationStatus.tsx
  - packages/renderer/src/panels/editors/shared/GateBar.tsx
  - packages/renderer/src/panels/editors/shared/FailBanner.tsx
  - packages/renderer/src/panels/editors/DatatableGridEditor.tsx
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "GateBar/FailBanner are ONE shared implementation reused by both the DTII editor (this plan) and the .stf editor (05-09) — sibling anatomy, not two parallel implementations (UI-SPEC Component Inventory)."
    - "The gate is a first-class 4-state machine (not run / running / pass / fail) with glyph+border-style+text triple-encoding, not color alone (UI-SPEC Surface 2 item 7, Accessibility Rule 1)."
    - "The grid is virtualized using the existing VfsTree/HexInspector ROW_HEIGHT+OVERSCAN+ResizeObserver idiom — real datatables have thousands of rows."
  artifacts:
    - path: "packages/renderer/src/panels/editors/shared/GateBar.tsx"
      provides: "Footer gate-state chip (not run/running/pass/fail) — shared by DTII and .stf"
      contains: "round-trip gate"
    - path: "packages/renderer/src/panels/editors/DatatableGridEditor.tsx"
      provides: "Dockview tab title + mod-dot, crumb bar (FORM DTII ▸ FORM 0001 ▸ DATA, Grid|Hex toggle, action buttons), grid toolbar (Filter rows…, +Row/-Row, count chip), virtualized typed grid (type badges, sort, double-click edit, modified triple-encoding)"
      min_lines: 150
  key_links:
    - from: "packages/renderer/src/panels/editors/DatatableGridEditor.tsx"
      to: "packages/renderer/src/panels/editors/shared/GateBar.tsx"
      via: "renders <GateBar> in its footer"
      pattern: "GateBar"
    - from: "packages/renderer/src/panels/editors/shared/GateBar.tsx"
      to: "packages/renderer/src/shared/VerificationStatus.tsx"
      via: "extends VerificationVariant with a 'running' state rather than a parallel pill component"
      pattern: "'running'"
---

<objective>
Build the shared gate-state UI (`GateBar`/`FailBanner`, extending the existing `VerificationStatus`
pill) and the DTII grid editor's core interaction surface (crumb bar, toolbar, virtualized typed grid
with sort/edit/modified-encoding) — the first half of Surface 2 (UI-SPEC 014-D), and the reference
implementation the `.stf` editor (05-09) will reuse.

Purpose: DATA-01 requires a real, editable, virtualized datatable grid. This is also the natural
"spine" plan per CONTEXT's Claude's-Discretion note — the shared gate machinery is built once here as
the reference consumer, then reused by `.stf` (05-09) as the sibling consumer.

Output: `GateBar.tsx`, `FailBanner.tsx` (new shared components), an extended `VerificationStatus.tsx`,
and `DatatableGridEditor.tsx`'s crumb/toolbar/grid anatomy (schema rail, hex toggle, and gate-bar
WIRING to the native round-trip land in 05-08, which depends on this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
</context>

<interfaces>
Existing VerificationStatus.tsx (full contract to extend, not replace):
```tsx
export type VerificationVariant = 'pass' | 'fail' | 'warn' | 'parse-error' | 'neutral';
const VARIANT_CONFIG: Record<VerificationVariant, { glyph: string; colorVar: string; ariaRole?: string }> = {
  pass: { glyph: '✓', colorVar: 'var(--color-accent)' },
  fail: { glyph: '✕', colorVar: 'var(--color-danger)' },
  ...
};
export default function VerificationStatus({ variant, caption, ariaLabel, compact }): React.ReactElement
```

VfsTree.tsx virtualization scaffold to copy verbatim (constant names + math, only ROW_HEIGHT value and
row-renderer differ per surface): `ROW_HEIGHT = 30; OVERSCAN = 8;` + `ResizeObserver` -> `viewHeight`
state; `onScroll` -> `scrollTop` state; `startRow`/`endRow`/`topPad`/`bottomPad` derivation; top-pad
spacer + windowed rows + bottom-pad spacer inside a `totalHeight`-tall inner div.

UI-SPEC Surface 2 anatomy (05-UI-SPEC.md, this plan covers items 1-4; items 5-8 are 05-08):
1. Dockview tab: `<file>.iff — Datatable` + warn `●` mod-dot.
2. Crumb bar (`--color-header` bg, mono): `FORM DTII ▸ FORM 0001 ▸ DATA` · `Grid | Hex` segmented
   toggle · spacer · `⇄ Compare to base` (ghost) · `＋ Stage` (ghost) · `Save · run gate` (primary).
3. Grid toolbar: `Filter rows…` text input · `＋ Row` · `− Row` · right-aligned count chip
   `N rows · M cols[ · K cell(s) modified]`.
4. The grid: sticky header (column name + type badge `s`/`i`/`f` + extended badges for the D-07
   widened set + accent sort arrow), sticky left row-number column (warn `●` when row modified),
   cells (mono, hover widget bg, `cursor: cell`, double-click to edit — inline input, accent-line
   border, Enter commits/Escape cancels/blur commits, typed coercion per column type). Modified-cell
   triple-encoding: inset 3px warn left border + `rgba(230,180,80,0.07)` bg + `" ●"` warn suffix →
   row-number `●` → tab `●`. Row click selects (accent-dim row).

Type badge hexes (UI-SPEC Color, sketch-locked, do NOT theme-switch): `s` `#7fb2ff` on
`rgba(120,180,255,.13)` · `i` `#7dd68a` on `rgba(160,255,170,.10)` · `f` `#e8b46a` on
`rgba(255,200,120,.12)`. D-07 widens the badge SET beyond s/i/f (Enum/Bool/BitVector/HashString/
PackedObjVars all need a distinct letter+color pair within the same idiom) — this plan's Task 2
defines the full badge set; z(...)-sourced Enum uses the same letter as plain Enum (both are
`getBasicType()===Int`, D-12).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Shared gate-state components — extend VerificationStatus, add GateBar + FailBanner</name>
  <files>packages/renderer/src/shared/VerificationStatus.tsx, packages/renderer/src/panels/editors/shared/GateBar.tsx, packages/renderer/src/panels/editors/shared/FailBanner.tsx</files>
  <read_first>
    - packages/renderer/src/shared/VerificationStatus.tsx (full file — the ONE existing shared glyph+color+caption status-pill component in this codebase)
    - 05-UI-SPEC.md Surface 2 item 7 (Gate bar state table) and item 8 (Fail banner copy contract)
    - 05-UI-SPEC.md Copywriting Contract table (exact gate-state strings for both DTII and .stf — this plan's GateBar accepts copy as props so both editors can supply their own state strings against the SAME component)
  </read_first>
  <action>
    Extend `VerificationVariant` in VerificationStatus.tsx with a `'running'` member and its
    `VARIANT_CONFIG` entry (info-colored text, `--color-info`) — do NOT build a second pill component;
    this project has exactly one status-pill implementation and this phase's gate states are a
    superset of what it already models (not-run maps to 'neutral', pass/fail map to existing
    variants, running is the one new state). Because 'running' additionally needs a DASHED border
    (not just a color change — UI-SPEC: "running: info text, dashed border"), add an optional
    `dashedBorder?: boolean` prop to `VerificationStatusProps` that wraps the pill in a bordered
    `<span>` when true, defaulting to false so every existing call site (IffStructureTree etc.) is
    unaffected. Create GateBar.tsx: a footer bar (`--color-header` bg, per UI-SPEC) accepting props
    `{ state: 'not-run' | 'running' | 'pass' | 'fail'; notRunLabel: string; runningLabel: string;
    passLabel: string; failLabel: string; onSaveRunGate?: () => void }` — renders a `VerificationStatus`
    instance mapped from `state` (not-run→neutral, running→running+dashedBorder, pass→pass,
    fail→fail) with the caller-supplied label text for that state (this is how DTII's "round-trip
    gate: not run" / "✓ byte-exact round-trip (<N> B)" and .stf's "gate: rebuilding index +
    payload…" / "✓ byte-exact round-trip · CRC index rebuilt" share ONE component with different
    copy). When `state==='pass'`, ALSO render the `→ staged in working changes` info chip
    immediately adjacent (per UI-SPEC — appears only on pass). Create FailBanner.tsx: a danger-tinted
    (`rgba(230,90,90,.08)` bg per UI-SPEC Color) banner accepting `{ message: string; actions:
    Array<{ label: string; onClick: () => void; variant?: 'default' | 'danger' }> }`, rendering the
    message text and a row of action buttons (DTII uses `Jump to bytes` + `Revert cell`; .stf inherits
    the same anatomy per UI-SPEC Surface 3 item 6) with `role="alert"` on the banner container
    (Accessibility contract). Neither GateBar nor FailBanner import anything DTII-specific or
    .stf-specific — both are pure presentational components driven entirely by props, so 05-09 can
    import them unchanged.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- VerificationStatus</automated>
  </verify>
  <acceptance_criteria>
    - VerificationVariant includes 'running' and existing call sites (grep for VerificationStatus usages outside this plan's new files) still compile without prop changes.
    - GateBar renders four visually distinct states (not-run/running/pass/fail) verified by a snapshot or explicit prop-driven test per state.
    - FailBanner's root element has `role="alert"`.
  </acceptance_criteria>
  <done>One shared gate-state implementation exists, ready for both DTII (this plan's Task 2/3, wired in 05-08) and .stf (05-09) to consume without divergence.</done>
</task>

<task type="auto">
  <name>Task 2: DatatableGridEditor — crumb bar, toolbar, virtualized typed grid</name>
  <files>packages/renderer/src/panels/editors/DatatableGridEditor.tsx</files>
  <read_first>
    - packages/renderer/src/panels/tre/VfsTree.tsx (full file — ROW_HEIGHT/OVERSCAN/ResizeObserver virtualization scaffold, lines 43-118, to copy verbatim; VfsRow's isSelected/isStaged/hover idioms, lines 257-443, for row-state styling structure)
    - packages/renderer/src/panels/DatatablePanel.tsx (full file — the placeholder this editor supersedes; note its `useNoProjectDimStyle` import, not reused here since this is a main-editor-group tab, not a dim-gated bottom panel)
    - 05-UI-SPEC.md Surface 2 items 1-4 (exact crumb/toolbar/grid anatomy + Copywriting Contract table for exact strings) and D-07's type-badge-widening note in 05-CONTEXT.md
    - This plan's Task 1 output (GateBar import path — GateBar itself is wired in 05-08, but the component must exist for this file to import its type, if the executor chooses to stub the footer now)
  </read_first>
  <action>
    Create DatatableGridEditor.tsx as a dockview panel component (`IDockviewPanelProps`) accepting the
    parsed DTII result (columns+rows, from 05-02's `parseDataTable` shape) plus the source virtual
    path as panel params. Render, top to bottom: (1) a crumb bar matching UI-SPEC copy exactly —
    `FORM DTII ▸ FORM 0001 ▸ DATA` (mono, tag-bold) · a `Grid | Hex` segmented toggle (local state,
    Hex view itself is wired in 05-08 — this task renders the toggle control and a placeholder Hex
    pane target) · spacer · `⇄ Compare to base` ghost button (disabled/tooltip per UI-SPEC Flagged
    Assumption 7 — sketch 015 not yet designed, do not drop the button) · `＋ Stage` ghost button
    (wired in 05-08, stub the handler here) · `Save · run gate` primary button (accent-dim bg per
    UI-SPEC Color "Accent reserved for" #1, wired in 05-08). (2) A grid toolbar: `Filter rows…` text
    input filtering by the FIRST (name) column, `＋ Row` / `− Row` buttons (in-memory only —
    mutating local edit-buffer state, no disk/staging effect per the UI-SPEC destructive-actions
    table — "removal is an in-memory edit, protected by the gate"), and a right-aligned count chip
    `<N> rows · <M> cols[ · <K> cell(s) modified]` (faint mono). (3) The virtualized grid itself,
    copying VfsTree.tsx's ROW_HEIGHT/OVERSCAN/ResizeObserver scaffold verbatim (only ROW_HEIGHT value
    — 5px/10px cell padding per UI-SPEC — and the row-renderer differ): sticky header row with one
    column per DTII column, each header cell showing the column name plus a type badge. Define the
    FULL widened badge set per D-07 (not just s/i/f): `s`(String, `#7fb2ff`/`rgba(120,180,255,.13)`),
    `i`(Int, `#7dd68a`/`rgba(160,255,170,.10)`), `f`(Float, `#e8b46a`/`rgba(255,200,120,.12)`),
    `h`(HashString — reuse the `i` accent family since it is physically Int, but with a distinct
    letter badge so the UI never claims it is a plain int), `b`(Bool), `e`(Enum, incl. z(...)-sourced
    per D-12 — same letter/color, no cross-table distinction at this layer), `v`(BitVector),
    `p`(PackedObjVars) — each badge is a `700 10px mono` pill per UI-SPEC Typography; pick a
    consistent accent-family color per badge (documented inline) since the sketch only locked s/i/f —
    this is the D-07 in-idiom extension the CONTEXT flags as the planner's job. Accent sort arrow
    (`▲`/`▼`) on the sorted column header, click-to-sort/toggle-direction. Sticky left row-number
    column (faint mono, right-aligned, `--color-header` bg) showing a warn `●` prefix when any cell in
    that row is modified. Cell rendering: mono text, `cursor: cell`, hover `--color-widget` bg,
    double-click opens an inline `<input>` (accent-line border) — Enter commits the edit into local
    state (typed coercion: `i`→parseInt, `f`→parseFloat, everything else stays string-backed per
    D-12's 3-physical-types rule — even Bool/Enum/BitVector store their underlying int value in this
    task; richer widget-level editors — checkbox for Bool, dropdown for Enum, multi-select for
    BitVector, structured editor for PackedObjVars — are the D-07 "full inline edit" requirement and
    MUST also land in this same file: replace the generic numeric input with the type-appropriate
    widget per column, driven by parsing the column's typeSpec string per the dispatch table in
    05-02's Interfaces), Escape cancels, blur commits. Modified-cell triple-encoding exactly as
    specified: inset 3px warn left border + `rgba(230,180,80,0.07)` bg + `" ●"` warn suffix,
    propagating to the row-number `●` and (via a prop callback up to the dockview tab title) the tab
    `●`. Row click sets a `selectedRowIndex` local/lifted state (accent-dim row bg) — 05-08's
    SchemaRail reads this to populate its "Selected row" section.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- DatatableGridEditor</automated>
  </verify>
  <acceptance_criteria>
    - The grid uses the same ROW_HEIGHT/OVERSCAN constant names and windowing math as VfsTree.tsx (grep confirms `OVERSCAN` present).
    - Every one of the 8 non-Comment type-spec letters (s/i/f/h/b/e/v/p) has a distinct badge letter+color pair defined in one place (not scattered inline literals).
    - A modified cell shows all three encodings simultaneously (border + tint + glyph) — verified by a component test asserting the rendered className/style + text content, not just a snapshot.
    - `Filter rows…` with no matches renders `No rows match "<query>"` (muted, centered) per the Copywriting Contract — never a silently blank grid.
  </acceptance_criteria>
  <done>DatatableGridEditor renders the full crumb/toolbar/grid anatomy from UI-SPEC Surface 2 items 1-4, with the D-07 widened type-badge set and per-type inline editors, ready for 05-08 to complete with the schema rail, hex toggle, and gate wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-typed cell edit → in-memory DataTableResult | Edits must be coerced/validated per column type before they can ever reach the native serializer (05-08's gate wiring), never trusted as raw strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | DatatableGridEditor inline cell editors | mitigate | Typed coercion (parseInt/parseFloat for numeric physical types) happens at commit time in this component, before any value reaches the edit-buffer that 05-08's gate/serialize step consumes — an invalid numeric entry is rejected/reverted at the input, never silently coerced to NaN/0. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- VerificationStatus DatatableGridEditor` — green.
</verification>

<success_criteria>
- GateBar/FailBanner are a single shared implementation, importable unchanged by 05-09.
- DatatableGridEditor's crumb/toolbar/grid anatomy matches UI-SPEC Surface 2 items 1-4 element-for-element.
- All 8 non-Comment DTII type-spec letters have distinct, documented badge encodings.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-06-SUMMARY.md` when done
</output>
