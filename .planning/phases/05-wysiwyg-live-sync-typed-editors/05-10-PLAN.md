---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 10
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - packages/renderer/src/panels/viewport/TransformGizmo.tsx
  - packages/renderer/src/panels/viewport/GizmoModeRail.tsx
  - packages/renderer/src/panels/viewport/Viewport.tsx
autonomous: true
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): folds in two 05-REVIEWS.md findings plus 05-CONTEXT
# decision #1 (object targeting, new scope this session).
# (1) Sonnet HIGH — "the live gizmo always moves the player avatar, never the viewed mesh." Round-1's
#     fix (legacy-only +1432 lookAt-target) did NOT fully resolve this — see the ROUND 2 REVISION NOTE
#     below for the actual closing fix.
# (2) Cursor MEDIUM — "gizmo doesn't gate writes on guardState==='blocked'; writes still fire, agent
#     refuses (functional but wastes channel traffic)." FIX: Task 1's onChange handler now checks
#     `liveStore.guardState.transform === 'ok'` (for Move/Rotate) / `.scale === 'ok'` (for Scale mode)
#     before calling writeTransform — this is a pure traffic-suppression optimization, NOT a new gate
#     (the agent's own guard remains the sole safety authority; this just stops sending writes the
#     agent is known to already be refusing).
# guardState is now per-channel (05-07 Task 2) — this plan's acceptance criteria updated accordingly.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decision #2 (purge the surviving overclaim). Strikes the banned "closes REVIEWS.md Sonnet HIGH
# finding for real this round" phrase from the T-05-30 threat-register row below, and DELETES this
# note's own round-4 (a)/(b) split — that split was NOT authorized by the reviewers' own reconciliation
# (05-REVIEWS-R3.md:214-217, which is explicit this is a single mislabeled finding, not two) and
# re-litigated a settled question instead of simply striking the overclaimed phrase. The corrected,
# scoped claim (this plan closes "the gizmo always moves the player, never what's targeted/selected" on
# BOTH builds; it does NOT close, and never claimed to close, the SEPARATE viewport<->live-object
# binding gap, which stays open until Phase 7's `.ws` placement editing) already matches this file's
# own objective/purpose section below — no prose in the objective needed to change, only the T-05-30
# threat row's residual overclaim phrase.

# REVISION NOTE (--reviews replan, ROUND 2, 2026-07-08): folds in 05-REVIEWS.md ROUND 2 decisions #1
# and #2.
# (1) Decision #1 (object-targeting scope, PURSUE THE STRONGER PATH — the actual closing fix for the
#     Sonnet HIGH finding above). 05-03 ROUND 2 adds a REAL cross-build focus-resolution mechanism:
#     LEGACY unchanged (+1432 lookAt-target chain), ADVERTISED now resolves via a real
#     getSelectedObject() accessor (previously: always fell back to the player, honestly labeled but
#     functionally identical to the round-1 bug for the advertised build). This plan's target-identity
#     label copy is updated to reflect that BOTH builds can now show a real `target: <name>` — it was
#     previously worded as legacy-only. `targetUnavailableOnBuild` is REDEFINED (05-03 ROUND 2): true
#     only when the resolution MECHANISM itself is unresolved (missing RVA/catalog row), never merely
#     "nothing is currently selected" — this plan's copy distinguishes the two cases, since they are
#     now both reachable on either build.
# (2) Decision #2 (W2 fix) — Scale writes on the advertised build, before the optional upstream
#     `object::setScale` catalog row exists, are now signaled via the NEW liveness bit4
#     `scaleUnavailableOnBuild` (05-03/05-07 ROUND 2), NOT via the `guardState.scale === 'blocked'`
#     tamper-style guard state used for round-1's traffic-suppression check. FIX: Task 1's Scale-mode
#     gating now checks `scaleUnavailableOnBuild` FIRST (renders a disabled-with-reason "Scale
#     unavailable on this build" state, reusing the D-05 offline-copy PATTERN, and suppresses ALL Scale
#     writes unconditionally in that state) — only when `scaleUnavailableOnBuild` is false does it fall
#     through to the EXISTING `guardState.scale==='blocked'` traffic-suppression check (a genuine
#     tamper, not a build-capability gap). This closes the false-tamper-implying UX the round-1 gizmo
#     would otherwise have shown on every advertised-build Scale attempt (the guard-blocked traffic
#     suppression and the "unavailable on this build" state look and behave differently — the former is
#     transient/tamper-related, the latter is a structural, build-level disabled state, matching how
#     D-05's offline state is rendered).

must_haves:
  truths:
    - "Dragging the gizmo calls useCommandWriter's writeTransform on every drag-move event with zero per-frame allocation — no new object is created inside the drag handler — UNLESS the relevant guard channel (transform for Move/Rotate, scale for Scale mode) is already 'blocked', in which case the write is suppressed client-side (traffic optimization; the agent's own guard remains authoritative regardless)."
    - "Offline (not injected) the gizmo renders but is non-interactive with the exact copy '○ Offline — attach a client to move objects live' — D-05, NOT staged, NOT silently working."
    - "When attached, the gizmo/rail area honestly states what object is being moved, on EITHER build: `target: <templateName>` when a real target/selection is resolved (legacy via the lookAt-target chain, advertised via the real selected-object resolver — ROUND 2, both are now reachable), `target: none — moving player avatar` when attached with the resolution mechanism available but nothing currently targeted/selected (a normal state on either build, not a capability gap), or `target: unavailable on this build — moving player avatar` ONLY when the resolution mechanism itself is unresolved (a genuine build-capability gap, e.g. a missing advertised catalog row) — never silently implying the viewport's loaded mesh is what moves without confirming the two actually correspond (05-CONTEXT decision #1, ROUND 2 cross-build)."
    - "The gizmo has four modes (Move/Rotate/Scale/Universal) with keyboard shortcuts W/E/R/Q live, and Scale mode is a fully real, in-scope write mode — never disabled purely due to phase scope (D-09). On the advertised build, BEFORE the optional upstream object::setScale catalog row exists, Scale mode instead renders a STRUCTURAL disabled-with-reason state reading 'Scale unavailable on this build' (sourced from liveStore.verifiedState.scaleUnavailableOnBuild, ROUND 2 W2 fix) — this is DISTINCT from and takes precedence over the ordinary guard-blocked traffic-suppression state, which remains reserved for a genuine tamper."
    - "Each axis (X/Y/Z) carries its letter label on the arrow itself, not color alone (UI-SPEC Accessibility Rule 1)."
  artifacts:
    - path: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      provides: "drei TransformControls wrapper restyled to sketch axis hexes + letter labels, four modes, offline-disabled-with-reason state, scale-unavailable-on-build disabled-with-reason state (ROUND 2), guard-aware write suppression, honest cross-build target-identity label"
      contains: "Scale unavailable on this build"
  key_links:
    - from: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "onChange/onDrag handler calls writeTransform(mappingName, transform12, scale3) imperatively, gated on the relevant guard channel not being 'blocked' AND (for scale mode) scaleUnavailableOnBuild not being true"
      pattern: "writeTransform"
    - from: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "reads hasTarget/targetUnavailableOnBuild/scaleUnavailableOnBuild/guardState for honest cross-build labeling + write suppression"
      pattern: "scaleUnavailableOnBuild"
    - from: "packages/renderer/src/panels/viewport/Viewport.tsx"
      to: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      via: "mounted inside the R3F scene alongside GizmoModeRail as an absolutely-positioned DOM overlay sibling"
      pattern: "TransformGizmo"
---

<objective>
Build the interactive transform gizmo (drei `TransformControls` restyled per sketch 011-B) and its
mode-selection rail — the core drag-to-write interaction that makes LIVE-03's headline claim ("drag a
viewport gizmo and see the object move") a reality, wired into the existing Viewport.tsx scene — honest
about WHICH object it is actually moving on EITHER build (ROUND 2 cross-build resolver), and honest
about Scale's build-availability as a structural state distinct from an ordinary guard-blocked tamper
(ROUND 2 W2 fix).

Purpose: this is the first genuinely new user-visible capability this phase adds to the viewport — a
real object can now be moved via direct manipulation, gated correctly offline (D-05), covering all
four modes including Scale (D-09, in scope on both targets), and — closing Sonnet's HIGH review
finding for the "always moves the player, never what's targeted/selected" gap, on EITHER build (round
1's legacy-only fix did not reach the advertised build; ROUND 2's cross-build resolver does). This does
NOT claim to close the SEPARATE viewport<->live-object binding gap (whether the mesh loaded in the
toolkit's viewport corresponds to whatever is targeted/selected in-game) — that gap has no resolver on
either build and remains open until Phase 7's `.ws` placement editing (05-11 adds an inline mismatch
warning precisely because this gap is not closed by this plan).

Output: `TransformGizmo.tsx`, `GizmoModeRail.tsx`, and their mount points in `Viewport.tsx`. The HUD
chrome around them (client card, readout bar, StatusBar mirror) is 05-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-PLAN.md
</context>

<interfaces>
Existing Viewport.tsx overlay/imperative patterns to mirror (full file read this session):
- `MissingDepsOverlay` (lines 103-128): absolutely-positioned DOM overlay reading a Zustand store, rendered as a SIBLING of `<Canvas>`, not inside it.
- `StatsCollector` (lines 50-71): `useFrame`-throttled imperative callback, NOT per-frame React state — the exact discipline TransformGizmo's readout-mirroring must follow (05-11 uses this same idiom for the readout bar; this plan's gizmo itself uses `onChange`/`onDrag` callbacks from drei's TransformControls, which are already imperative by design).

05-07's exported write API (import from `../../hooks/useCommandWriter`):
`writeTransform(mappingName: string, transform: ArrayLike<number> /* 12 */, scale: ArrayLike<number>
/* 3 */, flags?: number): void` — synchronous, zero-allocation, call directly from the drag handler
(this plan's gizmo ALWAYS omits `flags`/passes 0 — the `flags` param exists for liveStore's revert
actions only, ROUND 2).

liveStore fields this plan reads (from 05-07, VERIFIED shapes): `status` (attached/idle/connecting/
error — gates online/offline), `verifiedState.transform` (current live pose, when attached),
`verifiedState.hasTarget`/`verifiedState.targetUnavailableOnBuild` (05-CONTEXT decision #1 — REDEFINED
ROUND 2: `targetUnavailableOnBuild` true means the resolution MECHANISM is unresolved on this build,
reachable on EITHER build now, not legacy-vs-advertised), `verifiedState.scaleUnavailableOnBuild`
(ROUND 2 NEW — true when `isAdvertisedClient() && setScale` is unresolved), `guardState: { transform:
'ok'|'blocked'; scale: 'ok'|'blocked' }` (per-channel — 05-11 surfaces the full detail in the HUD
banner; this plan uses it ONLY to suppress redundant writes to an already-blocked channel, never as a
hard gate the user cannot override by fixing the underlying tamper and letting the guard clear).

UI-SPEC sketch-locked fixed hexes (do NOT theme-switch): X `#e0584f` / Y `#6db33f` / Z `#4a8cff` —
each axis ALWAYS carries its letter label; color never carries axis identity alone.

Live/offline state-encoding table (UI-SPEC, applies to every surface including this one):
| Channel | Live | Offline |
|---|---|---|
| Glyph | ● (accent) | ○ (warn) |
| Border | solid accent-line | dashed warn |
| Write target | → client ✓ | → staged (patch) [EDITORS ONLY — D-05: the GIZMO uses disabled-with-reason instead, see below] |

D-05 (ERRATUM applied here, not the UI-SPEC's literal gizmo copy): offline, the gizmo renders
non-interactive with copy `○ Offline — attach a client to move objects live` — no staging, no fake
local-only move.

05-CONTEXT decision #1 (target identity — ROUND 2 REVISED, cross-build): when
`status.kind === 'attached'`, the gizmo area additionally shows ONE of: `target: <templateName>` (when
`verifiedState.hasTarget` is true — reachable on EITHER build now, ROUND 2), `target: none — moving
player avatar` (when attached, resolution mechanism available, but nothing currently targeted/
selected — a normal state on either build), or `target: unavailable on this build — moving player
avatar` (ONLY when `verifiedState.targetUnavailableOnBuild` is true — the resolution mechanism itself
is genuinely unresolved, a rarer state than round 1's "always true on advertised"). This is a SMALL
inline label, not a new panel — 05-11 owns the full client-card anatomy; this plan just ensures the
gizmo's own immediate vicinity is never silent about what it moves.

ROUND 2 NEW — Scale build-availability (D-09 corrected, W2 fix): when `mode === 'scale'` AND
`verifiedState.scaleUnavailableOnBuild === true` (advertised build, upstream catalog row not yet
added), the gizmo's Scale handles render in the SAME structural disabled-with-reason idiom D-05 uses
for offline (dimmed/non-interactive), with an inline label reading exactly `Scale unavailable on this
build` — writes are suppressed UNCONDITIONALLY in this state (do not even attempt a guard-gated write).
This is DISTINCT from the ordinary `guardState.scale === 'blocked'` traffic-suppression check below,
which only applies when `scaleUnavailableOnBuild` is false (a genuine tamper case, not a build gap).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TransformGizmo — restyled drei TransformControls, four modes, offline disabled-with-reason, scale-unavailable-on-build disabled-with-reason, honest cross-build target label, guard-aware write suppression</name>
  <files>packages/renderer/src/panels/viewport/TransformGizmo.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — SceneContent, the mesh/scene object this gizmo attaches to, existing R3F patterns)
    - packages/renderer/src/hooks/useCommandWriter.ts (05-07 — writeTransform signature, including the optional flags param this plan never uses)
    - packages/renderer/src/state/liveStore.ts (05-07 — status/verifiedState (incl. hasTarget/targetUnavailableOnBuild/scaleUnavailableOnBuild)/cowSnapshot/guardState fields, VERIFIED per-channel shape)
    - 05-UI-SPEC.md Surface 1 items 3-4 (gizmo-mode rail + transform gizmo anatomy) and the Flagged Assumption 1 (drei TransformControls base is explicitly permitted)
    - 05-REVIEWS.md's Sonnet HIGH finding (target identity) and Cursor MEDIUM finding (guard-aware write suppression) from ROUND 1, PLUS 05-REVIEWS.md ROUND 2's Headline finding (the actual cross-build closing fix) and W2 fix (scaleUnavailableOnBuild) — this task closes all four
  </read_first>
  <action>
    Create TransformGizmo.tsx wrapping `@react-three/drei`'s `TransformControls`, attached to the
    currently-loaded viewport object (the same object `Viewport.tsx`'s `SceneContent` already
    renders — read its ref/group from the existing scene graph rather than introducing a new object
    picker; per this phase's scope, the gizmo target is always the single displayed/live object, no
    multi-object selection system is introduced). Accept a `mode: 'translate' | 'rotate' | 'scale'`
    prop (drei's native mode names; map GizmoModeRail's Move/Rotate/Scale/Universal labels to these —
    'Universal' shows translate+rotate+scale handles simultaneously if drei supports a combined mode,
    otherwise cycle through the three on repeated Q presses or render all three gizmo controls
    stacked — pick the simpler, well-supported drei option and document the choice). Restyle per the
    sketch: apply the locked axis hexes (X `#e0584f`/Y `#6db33f`/Z `#4a8cff`) to the corresponding
    TransformControls handle materials, add a text-sprite or HTML label per axis showing its letter
    (X/Y/Z) — TransformControls does not natively render letter labels, so either use drei's `Html`
    helper anchored to each axis handle's local position, or overlay them via a small custom shader —
    pick whichever this codebase's existing text-in-3D-scene precedent uses, if any (grep for `Html`
    usage from drei elsewhere in this codebase first). Widen the hit area (drei exposes a `size` prop
    that scales the whole gizmo, or per-axis hit padding — check the installed `@react-three/drei`
    version's API and use whatever knob it exposes). On `onChange` (drei's live-drag callback, fired
    continuously during a drag): read the object's current world transform (position+quaternion->3x4
    matrix, row-major, matching the SAME 48-byte layout convention `LIVE_CHANNEL_LAYOUT.TRANSFORM`
    uses — write this conversion as a small pure helper function, unit-testable) and the object's
    current scale (3 floats). Before calling `writeTransform`, gate per mode: if `mode === 'scale'`,
    FIRST check `liveStore.verifiedState?.scaleUnavailableOnBuild === true` (ROUND 2 W2 fix) — if so,
    render the Scale handles in the D-05-style structural disabled state (dimmed/non-interactive, inline
    label `Scale unavailable on this build`) and SKIP the write unconditionally (do not even reach the
    guard check below); otherwise fall through to the ordinary guard check
    `liveStore.guardState.scale === 'blocked'` — if blocked, SKIP the write this frame (Cursor MEDIUM
    fix: pure traffic suppression, the agent's own guard remains the sole safety authority regardless).
    For `translate`/`rotate`/`universal` modes, check ONLY `liveStore.guardState.transform ===
    'blocked'` (scaleUnavailableOnBuild is irrelevant to these modes) — skip the write if blocked.
    Otherwise call `writeTransform(mappingName, transform12, scale3)` (flags omitted — this call site
    never coalesces a rebaseline) — reuse ONE preallocated local array for the THREE.js-to-row-major
    conversion (do not allocate a new Float32Array inside onChange). A subsequent guard-clearing action
    (e.g. Revert ALL via 05-11) will un-block a guard-blocked channel automatically on the NEXT
    drag-move event — no special re-enable logic needed. Gate interactivity on
    `liveStore.status.kind === 'attached'`: when NOT attached, render the gizmo with `enabled={false}`
    (drei's disable prop) and render a small inline label near the gizmo (or via the mode rail area)
    reading exactly `○ Offline — attach a client to move objects live` (D-05 — do not render any
    "staged (patch)" copy for the gizmo itself). When ATTACHED, additionally render the honest,
    CROSS-BUILD target-identity label (ROUND 2 — see Interfaces): `target: <templateName>` when
    `hasTarget===true`; `target: unavailable on this build — moving player avatar` when
    `targetUnavailableOnBuild===true` (a genuine resolution-mechanism gap); else `target: none —
    moving player avatar` (attached, mechanism available, nothing currently targeted/selected — a
    normal state, not a gap) — the SAME small inline area, mutually exclusive with the offline copy.
    This label is now meaningful and reachable with a REAL target name on EITHER build (ROUND 2 closes
    the narrower "always moves the player, never what's targeted/selected" gap on BOTH builds —
    round 1's legacy-only mechanism left the advertised build permanently in the "moving player
    avatar" state with no path to a real target; this does NOT close, and never claimed to close, the
    SEPARATE viewport<->live-object binding gap, which stays open until Phase 7's `.ws` placement
    editing — REVIEWS.md round-4 maintainer decision #2, ROUND 5 removed the earlier round-1-closure overclaim framing).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- TransformGizmo</automated>
  </verify>
  <acceptance_criteria>
    - A test simulating a drag-change event while `status.kind==='attached'` and `guardState.transform==='ok'` asserts `writeTransform` was called with a 12-length and a 3-length array.
    - A test simulating a drag-change event in `translate` mode while `guardState.transform==='blocked'` asserts `writeTransform` was NOT called (traffic suppression) — and a companion test in `scale` mode while `guardState.scale==='blocked'` AND `scaleUnavailableOnBuild===false` (but `guardState.transform==='ok'`) shows Scale-mode writes are independently suppressed without affecting Move/Rotate.
    - A NEW test (ROUND 2) asserts that in `scale` mode with `scaleUnavailableOnBuild===true`, writeTransform is NEVER called regardless of `guardState.scale`'s value, and the rendered output contains the exact text `Scale unavailable on this build` — proving this state takes precedence over and is distinct from the ordinary guard-blocked traffic-suppression state.
    - A test asserts `writeTransform` is NEVER called while `status.kind!=='attached'` (the gizmo is disabled offline — no fallback write path exists).
    - The offline label text matches `○ Offline — attach a client to move objects live` exactly (grep/DOM text match).
    - When attached with `verifiedState.hasTarget===true`, the rendered output contains `target: ` followed by the templateName; when attached with `hasTarget===false` and `targetUnavailableOnBuild===false`, it contains `target: none — moving player avatar`; when attached with `targetUnavailableOnBuild===true`, it contains `target: unavailable on this build — moving player avatar` (component test asserts all three branches render distinct, correct text — never silently omitting this label while attached). This is tested with BOTH a simulated legacy-shaped and an advertised-shaped liveStore state, proving the label is build-agnostic (ROUND 2).
    - Each axis handle has an associated letter label element (X/Y/Z) discoverable in the rendered output, not only a color.
    - ROUND 5 (REVIEWS.md round-4 maintainer decision #2, overclaim purge): the active threat_model (T-05-30) and this task's `<action>` state the viewport<->live-object binding gap remains OPEN (deferred to Phase 7) and do NOT claim this round resolves it; the earlier round-1-closure overclaim phrasing (quoted verbatim in REVIEWS.md round-4 decision #2) survives only inside `#`-prefixed archived revision-note lines. Verify by reading those active sections against REVIEWS.md's quoted phrase.
  </acceptance_criteria>
  <done>The gizmo is interactive only when live, writes through the zero-alloc path (suppressing redundant writes to a known-blocked guard channel, and unconditionally suppressing Scale writes when Scale is structurally unavailable on this build), is honest about its offline state per D-05, and is honest about WHAT it is moving on EITHER build per 05-CONTEXT decision #1 ROUND 2 — no fake staging, no silent wrong-object moves, no conflation of "nothing selected" with "build can't select."</done>
</task>

<task type="auto">
  <name>Task 2: GizmoModeRail + Viewport mount wiring</name>
  <files>packages/renderer/src/panels/viewport/GizmoModeRail.tsx, packages/renderer/src/panels/viewport/Viewport.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — where absolutely-positioned overlay siblings of `<Canvas>` are rendered, e.g. `MissingDepsOverlay`'s mount point, to mirror for this rail's mount point)
    - This plan's Task 1 output (TransformGizmo's `mode` prop contract)
    - 05-UI-SPEC.md Surface 1 item 3 (exact rail anatomy: 40px vertical buttons, icon over key label, active state styling, keyboard shortcuts)
  </read_first>
  <action>
    Create GizmoModeRail.tsx: a left-edge, vertically-centered rail of four 40px-wide buttons — `✥ W`
    Move, `⟳ E` Rotate, `⤢ R` Scale, `✛ Q` Universal — using local or lifted state for the active
    mode (lift it to a small local hook/context shared with TransformGizmo, or accept it as a
    controlled prop from Viewport.tsx — executor's choice, but the active mode must be a SINGLE
    source of truth consumed by both this rail and TransformGizmo, not two independently-tracked
    states that can drift). Active button styling: `--color-accent-dim` fill + accent border + accent
    text (per UI-SPEC Color "Accent reserved for #4"). Wire keyboard shortcuts W/E/R/Q (global
    keydown listener scoped to when the viewport panel has focus/is visible — mirror whatever
    existing keyboard-shortcut pattern this codebase already uses elsewhere, if any). The Scale (R)
    button is ALWAYS selectable regardless of `scaleUnavailableOnBuild` (D-09 — Scale remains an
    in-scope MODE at all times; only the WRITE is suppressed within TransformGizmo per Task 1's
    structural disabled state, not the mode-selection affordance itself). Use `role="radiogroup"` on
    the rail container and `aria-pressed` per button (UI-SPEC Accessibility contract) — additionally
    apply the SAME `aria-pressed`/`role="radiogroup"` parity to the DTII/`.stf` editors' Grid|Hex
    toggle if that toggle does not already have it (05-06/05-08 built it without an explicit
    accessibility acceptance criterion — a one-line, low-cost fix while this task is already touching
    the identical a11y pattern; skip only if 05-06/05-08 already landed it, verified by grep first).
    Mount both `GizmoModeRail` and `TransformGizmo` in Viewport.tsx: `GizmoModeRail` as an
    absolutely-positioned DOM sibling of `<Canvas>` (same layer as `MissingDepsOverlay`),
    `TransformGizmo` INSIDE the R3F `<Canvas>`'s scene tree (it is a 3D object, not a DOM overlay) —
    pass the shared `mode` state down into `TransformGizmo`'s `mode` prop.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- GizmoModeRail Viewport</automated>
  </verify>
  <acceptance_criteria>
    - Pressing "R" while the viewport has focus activates Scale mode (asserted via a keydown-simulation test) — Scale is not disabled or grayed out at the MODE-SELECTION level (D-09), even when `scaleUnavailableOnBuild===true` (only the WRITE is suppressed, per Task 1, not the mode switch).
    - The rail has `role="radiogroup"` and exactly one button has `aria-pressed="true"` at any time.
    - GizmoModeRail and TransformGizmo share one mode value (a test changing the rail's active button asserts TransformGizmo's `mode` prop updates to match).
  </acceptance_criteria>
  <done>The mode rail and gizmo are mounted in the viewport, share one source of truth for the active mode, and Scale is a fully selectable, non-disabled mode per the corrected D-09 (its WRITE behavior, not its selectability, is what varies by build-availability).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User gizmo drag (untrusted input magnitude/rate) → writeTransform → shared memory | The gizmo can generate writes at up to 60fps; the write path must remain zero-allocation and must never bypass the offline gate or the scale-unavailable-on-build gate. Client-side guard-channel suppression is a traffic optimization ONLY — it must never be treated as the actual safety boundary (the agent's own guard is, always). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering | TransformGizmo offline gate | mitigate | `enabled={liveStore.status.kind==='attached'}` is the single gate; acceptance criteria assert zero writeTransform calls while not attached — no secondary "preview" write path exists that could leak a write attempt offline. |
| T-05-17 | DoS (perf) | onChange drag handler allocation | mitigate | One preallocated conversion buffer is reused per component instance; acceptance criteria verify writeTransform receives fixed-length arrays without asserting object identity here (identity is 05-07's concern) but the handler itself must not `new Float32Array(...)` per call — code-reviewed via the task's action text, not just tested. |
| T-05-30 | Spoofing (wrong object moved) | Target-vs-player identity | mitigate | The gizmo ALWAYS renders an honest `target: ...` label while attached (never silently omitted), sourced from 05-03's agent-resolved, source-grounded target chain — REAL on EITHER build as of ROUND 2, never a toolkit-side guess about what the viewport mesh corresponds to (05-CONTEXT decision #1 — closes the narrower "always moves the player, never what's targeted/selected" gap on BOTH builds; it does NOT close, and never claimed to close, the SEPARATE viewport<->live-object binding gap, which stays open until Phase 7's `.ws` placement editing — REVIEWS.md round-4 maintainer decision #2, ROUND 5 removed the earlier round-1-closure overclaim framing). |
| T-05-36 | DoS (misleading state) | Scale write suppression during a build-capability gap | mitigate | `scaleUnavailableOnBuild` is checked and rendered as a STRUCTURAL disabled state (distinct wording/styling from a transient guard-blocked tamper state), preventing a user from misreading "Scale doesn't work on this build yet" as "something tampered with my object" (ROUND 2 W2 fix, closes the false-tamper-banner UX risk at the gizmo's own point of interaction, mirrored again at 05-11's HUD level). |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- TransformGizmo GizmoModeRail Viewport` — all green.
</verification>

<success_criteria>
- The gizmo writes through the zero-alloc path only while attached; offline it is honestly disabled with the D-05 copy; a known-blocked guard channel suppresses redundant writes on the client side; Scale on an advertised build without the upstream catalog row shows a distinct, structural "unavailable on this build" state rather than a false tamper implication.
- All four modes are selectable, with Scale fully in scope (not disabled) per the corrected D-09.
- Axis letters are always visible alongside color.
- The gizmo always honestly labels what object it is moving (target vs. player fallback vs. mechanism-unavailable) on EITHER build (ROUND 2).
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-10-SUMMARY.md` when done
</output>
</output>
</output>
