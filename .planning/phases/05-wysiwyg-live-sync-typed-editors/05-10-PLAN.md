---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 10
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - packages/renderer/src/panels/viewport/TransformGizmo.tsx
  - packages/renderer/src/panels/viewport/GizmoModeRail.tsx
  - packages/renderer/src/panels/viewport/Viewport.tsx
autonomous: true
requirements: [LIVE-03]

must_haves:
  truths:
    - "Dragging the gizmo calls useCommandWriter's writeTransform on every drag-move event with zero per-frame allocation — no new object is created inside the drag handler."
    - "Offline (not injected) the gizmo renders but is non-interactive with the exact copy '○ Offline — attach a client to move objects live' — D-05, NOT staged, NOT silently working."
    - "The gizmo has four modes (Move/Rotate/Scale/Universal) with keyboard shortcuts W/E/R/Q live, and Scale mode is a fully real, in-scope write mode — never disabled-with-reason on either in-scope target (D-09, corrected 2026-07-08)."
    - "Each axis (X/Y/Z) carries its letter label on the arrow itself, not color alone (UI-SPEC Accessibility Rule 1)."
  artifacts:
    - path: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      provides: "drei TransformControls wrapper restyled to sketch axis hexes + letter labels, four modes, offline-disabled-with-reason state"
      contains: "Offline — attach a client to move objects live"
    - path: "packages/renderer/src/panels/viewport/GizmoModeRail.tsx"
      provides: "40px vertical rail, Move(W)/Rotate(E)/Scale(R)/Universal(Q) buttons, active-state accent-dim styling, keyboard shortcuts"
      contains: "Universal (Q)"
  key_links:
    - from: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "onChange/onDrag handler calls writeTransform(mappingName, transform12, scale3) imperatively"
      pattern: "writeTransform"
    - from: "packages/renderer/src/panels/viewport/Viewport.tsx"
      to: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      via: "mounted inside the R3F scene alongside GizmoModeRail as an absolutely-positioned DOM overlay sibling"
      pattern: "TransformGizmo"
---

<objective>
Build the interactive transform gizmo (drei `TransformControls` restyled per sketch 011-B) and its
mode-selection rail — the core drag-to-write interaction that makes LIVE-03's headline claim ("drag a
viewport gizmo and see the object move") a reality, wired into the existing Viewport.tsx scene.

Purpose: this is the first genuinely new user-visible capability this phase adds to the viewport — a
real object can now be moved via direct manipulation, gated correctly offline (D-05) and covering all
four modes including Scale (D-09, in scope on both targets).

Output: `TransformGizmo.tsx`, `GizmoModeRail.tsx`, and their mount points in `Viewport.tsx`. The HUD
chrome around them (client card, readout bar, StatusBar mirror) is 05-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-PLAN.md
</context>

<interfaces>
Existing Viewport.tsx overlay/imperative patterns to mirror (full file read this session):
- `MissingDepsOverlay` (lines 103-128): absolutely-positioned DOM overlay reading a Zustand store, rendered as a SIBLING of `<Canvas>`, not inside it.
- `StatsCollector` (lines 50-71): `useFrame`-throttled imperative callback, NOT per-frame React state — the exact discipline TransformGizmo's readout-mirroring must follow (05-11 uses this same idiom for the readout bar; this plan's gizmo itself uses `onChange`/`onDrag` callbacks from drei's TransformControls, which are already imperative by design).

05-07's exported write API (import from `../../hooks/useCommandWriter`):
`writeTransform(mappingName: string, transform: ArrayLike<number> /* 12 */, scale: ArrayLike<number>
/* 3 */): void` — synchronous, zero-allocation, call directly from the drag handler.

liveStore fields this plan reads (from 05-07): `status` (attached/idle/connecting/error — gates
online/offline), `verifiedState.transform` (current live pose, when attached), `guardState` ('ok' |
'blocked' — 05-11 surfaces this in the HUD, this plan does not need to react to it beyond not
crashing if a write is refused).

UI-SPEC sketch-locked fixed hexes (do NOT theme-switch): X `#e0584f` / Y `#6db33f` / Z `#4a8cff` —
each axis ALWAYS carries its letter label; color never carries axis identity alone.

Live/offline state-encoding table (UI-SPEC, applies to every surface including this one):
| Channel | Live | Offline |
|---|---|---|
| Glyph | ● (accent) | ○ (warn) |
| Border | solid accent-line | dashed warn |
| Write target | → client ✓ | → staged (patch) [EDITORS ONLY — D-05: the GIZMO uses disabled-with-reason instead, see below] |

D-05 (ERRATUM applied here, not the UI-SPEC's literal gizmo copy): offline, the gizmo renders
non-interactive with copy `○ Offline — attach a client to move objects live` — no staging, no fake
local-only move.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TransformGizmo — restyled drei TransformControls, four modes, offline disabled-with-reason</name>
  <files>packages/renderer/src/panels/viewport/TransformGizmo.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — SceneContent, the mesh/scene object this gizmo attaches to, existing R3F patterns)
    - packages/renderer/src/hooks/useCommandWriter.ts (05-07 — writeTransform signature)
    - packages/renderer/src/state/liveStore.ts (05-07 — status/verifiedState/cowSnapshot fields)
    - 05-UI-SPEC.md Surface 1 items 3-4 (gizmo-mode rail + transform gizmo anatomy) and the Flagged Assumption 1 (drei TransformControls base is explicitly permitted)
  </read_first>
  <action>
    Create TransformGizmo.tsx wrapping `@react-three/drei`'s `TransformControls`, attached to the
    currently-loaded viewport object (the same object `Viewport.tsx`'s `SceneContent` already
    renders — read its ref/group from the existing scene graph rather than introducing a new object
    picker; per this phase's scope, the gizmo target is always the single displayed/live object, no
    multi-object selection system is introduced). Accept a `mode: 'translate' | 'rotate' | 'scale'`
    prop (drei's native mode names; map GizmoModeRail's Move/Rotate/Scale/Universal labels to these —
    'Universal' shows translate+rotate+scale handles simultaneously if drei supports a combined mode,
    otherwise cycle through the three on repeated Q presses or render all three gizmo controls
    stacked — pick the simpler, well-supported drei option and document the choice). Restyle per the
    sketch: apply the locked axis hexes (X `#e0584f`/Y `#6db33f`/Z `#4a8cff`) to the corresponding
    TransformControls handle materials, add a text-sprite or HTML label per axis showing its letter
    (X/Y/Z) — TransformControls does not natively render letter labels, so either use drei's `Html`
    helper anchored to each axis handle's local position, or overlay them via a small custom shader —
    pick whichever this codebase's existing text-in-3D-scene precedent uses, if any (grep for `Html`
    usage from drei elsewhere in this codebase first). Widen the hit area (drei exposes a `size` prop
    that scales the whole gizmo, or per-axis hit padding — check the installed `@react-three/drei`
    version's API and use whatever knob it exposes to get "generous hit area, hover thickening 2.6→4.2
    line width, enlarged label on hover" as close as the library allows; document any UI-SPEC detail
    the library cannot express exactly). On `onChange` (drei's live-drag callback, fired continuously
    during a drag): read the object's current world transform (position+quaternion->3x4 matrix,
    row-major, matching the SAME 48-byte layout convention `LIVE_CHANNEL_LAYOUT.TRANSFORM` uses — this
    requires converting THREE.js's transform representation into that row-major float[3][4] shape;
    write this conversion as a small pure helper function, unit-testable) and the object's current
    scale (3 floats), then call `writeTransform(mappingName, transform12, scale3)` — reuse ONE
    preallocated local array for this conversion (do not allocate a new Float32Array inside onChange).
    Gate interactivity on `liveStore.status.kind === 'attached'`: when NOT attached, render the gizmo
    with `enabled={false}` (drei's disable prop) and render a small inline label near the gizmo (or
    via the mode rail area) reading exactly `○ Offline — attach a client to move objects live` (D-05
    — do not render any "staged (patch)" copy for the gizmo itself, that phrasing is reserved for the
    two typed editors per this phase's errata).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- TransformGizmo</automated>
  </verify>
  <acceptance_criteria>
    - A test simulating a drag-change event while `status.kind==='attached'` asserts `writeTransform` was called with a 12-length and a 3-length array.
    - A test asserts `writeTransform` is NEVER called while `status.kind!=='attached'` (the gizmo is disabled offline — no fallback write path exists).
    - The offline label text matches `○ Offline — attach a client to move objects live` exactly (grep/DOM text match).
    - Each axis handle has an associated letter label element (X/Y/Z) discoverable in the rendered output, not only a color.
  </acceptance_criteria>
  <done>The gizmo is interactive only when live, writes through the zero-alloc path, and is honest about its offline state per D-05 — no fake staging.</done>
</task>

<task type="auto">
  <name>Task 2: GizmoModeRail + Viewport mount wiring</name>
  <files>packages/renderer/src/panels/viewport/GizmoModeRail.tsx, packages/renderer/src/panels/viewport/Viewport.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — where absolutely-positioned overlay siblings of `<Canvas>` are rendered, e.g. `MissingDepsOverlay`'s mount point, to mirror for this rail's mount point)
    - This plan's Task 1 output (TransformGizmo's `mode` prop contract)
    - 05-UI-SPEC.md Surface 1 item 3 (exact rail anatomy: 40px vertical buttons, icon over key label, active state styling, keyboard shortcuts)
  </read_first>
  <action>
    Create GizmoModeRail.tsx: a left-edge, vertically-centered rail of four 40px-wide buttons — `✥ W`
    Move, `⟳ E` Rotate, `⤢ R` Scale, `✛ Q` Universal — using local or lifted state for the active
    mode (lift it to a small local hook/context shared with TransformGizmo, or accept it as a
    controlled prop from Viewport.tsx — executor's choice, but the active mode must be a SINGLE
    source of truth consumed by both this rail and TransformGizmo, not two independently-tracked
    states that can drift). Active button styling: `--color-accent-dim` fill + accent border + accent
    text (per UI-SPEC Color "Accent reserved for #4"). Wire keyboard shortcuts W/E/R/Q (global
    keydown listener scoped to when the viewport panel has focus/is visible — mirror whatever
    existing keyboard-shortcut pattern this codebase already uses elsewhere, if any, rather than
    inventing a new global-listener convention). Use `role="radiogroup"` on the rail container and
    `aria-pressed` per button (UI-SPEC Accessibility contract). Mount both `GizmoModeRail` and
    `TransformGizmo` in Viewport.tsx: `GizmoModeRail` as an absolutely-positioned DOM sibling of
    `<Canvas>` (same layer as `MissingDepsOverlay`), `TransformGizmo` INSIDE the R3F `<Canvas>`'s
    scene tree (it is a 3D object, not a DOM overlay) — pass the shared `mode` state down into
    `TransformGizmo`'s `mode` prop.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- GizmoModeRail Viewport</automated>
  </verify>
  <acceptance_criteria>
    - Pressing "R" while the viewport has focus activates Scale mode (asserted via a keydown-simulation test) — Scale is not disabled or grayed out (D-09).
    - The rail has `role="radiogroup"` and exactly one button has `aria-pressed="true"` at any time.
    - GizmoModeRail and TransformGizmo share one mode value (a test changing the rail's active button asserts TransformGizmo's `mode` prop updates to match).
  </acceptance_criteria>
  <done>The mode rail and gizmo are mounted in the viewport, share one source of truth for the active mode, and Scale is a fully live, non-disabled mode per the corrected D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User gizmo drag (untrusted input magnitude/rate) → writeTransform → shared memory | The gizmo can generate writes at up to 60fps; the write path must remain zero-allocation and must never bypass the offline gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering | TransformGizmo offline gate | mitigate | `enabled={liveStore.status.kind==='attached'}` is the single gate; acceptance criteria assert zero writeTransform calls while not attached — no secondary "preview" write path exists that could leak a write attempt offline. |
| T-05-17 | DoS (perf) | onChange drag handler allocation | mitigate | One preallocated conversion buffer is reused per component instance; acceptance criteria verify writeTransform receives fixed-length arrays without asserting object identity here (identity is 05-07's concern) but the handler itself must not `new Float32Array(...)` per call — code-reviewed via the task's action text, not just tested. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- TransformGizmo GizmoModeRail Viewport` — all green.
</verification>

<success_criteria>
- The gizmo writes through the zero-alloc path only while attached; offline it is honestly disabled with the D-05 copy.
- All four modes exist and work, with Scale fully in scope (not disabled) per the corrected D-09.
- Axis letters are always visible alongside color.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-10-SUMMARY.md` when done
</output>
