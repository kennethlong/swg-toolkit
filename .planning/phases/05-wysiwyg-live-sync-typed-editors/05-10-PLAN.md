---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 10
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - packages/renderer/src/panels/viewport/TransformGizmo.tsx
  - packages/renderer/src/panels/viewport/GizmoModeRail.tsx
  - packages/renderer/src/panels/viewport/Viewport.tsx
autonomous: true
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): folds in two 05-REVIEWS.md findings plus 05-CONTEXT
# decision #1 (object targeting, new scope this session).
# (1) Sonnet HIGH — "the live gizmo always moves the player avatar, never the viewed mesh." RESOLVED
#     by 05-03's target-or-player focus resolution (legacy-grounded, advertised-honest-gap) + 05-07's
#     liveStore.hasTarget/targetUnavailableOnBuild exposure. This plan's job is the LAST MILE: the
#     gizmo/HUD must say what it is actually moving, truthfully, on both builds. Task 1 adds the
#     honest target-identity label alongside the existing D-05 offline copy.
# (2) Cursor MEDIUM — "gizmo doesn't gate writes on guardState==='blocked'; writes still fire, agent
#     refuses (functional but wastes channel traffic)." FIX: Task 1's onChange handler now checks
#     `liveStore.guardState.transform === 'ok'` (for Move/Rotate) / `.scale === 'ok'` (for Scale mode)
#     before calling writeTransform — this is a pure traffic-suppression optimization, NOT a new gate
#     (the agent's own guard remains the sole safety authority; this just stops sending writes the
#     agent is known to already be refusing).
# guardState is now per-channel (05-07 Task 2) — this plan's acceptance criteria updated accordingly.

must_haves:
  truths:
    - "Dragging the gizmo calls useCommandWriter's writeTransform on every drag-move event with zero per-frame allocation — no new object is created inside the drag handler — UNLESS the relevant guard channel (transform for Move/Rotate, scale for Scale mode) is already 'blocked', in which case the write is suppressed client-side (traffic optimization; the agent's own guard remains authoritative regardless)."
    - "Offline (not injected) the gizmo renders but is non-interactive with the exact copy '○ Offline — attach a client to move objects live' — D-05, NOT staged, NOT silently working."
    - "When attached, the gizmo/rail area honestly states what object is being moved: the player's in-game TARGET when one is resolved (legacy SWGEmu), the PLAYER itself as a fallback when no target is set or on the advertised build (with an explicit note when target-tracking is unavailable on that build) — never silently implying the viewport's loaded mesh is what moves without confirming the two actually correspond (05-CONTEXT decision #1)."
    - "The gizmo has four modes (Move/Rotate/Scale/Universal) with keyboard shortcuts W/E/R/Q live, and Scale mode is a fully real, in-scope write mode — never disabled-with-reason on either in-scope target purely due to scope (D-09, corrected 2026-07-08); Scale MAY show a guard-blocked traffic-suppression state like any other channel, which is not the same as being out of scope."
    - "Each axis (X/Y/Z) carries its letter label on the arrow itself, not color alone (UI-SPEC Accessibility Rule 1)."
  artifacts:
    - path: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      provides: "drei TransformControls wrapper restyled to sketch axis hexes + letter labels, four modes, offline-disabled-with-reason state, guard-aware write suppression, honest target-identity label"
      contains: "Offline — attach a client to move objects live"
    - path: "packages/renderer/src/panels/viewport/GizmoModeRail.tsx"
      provides: "40px vertical rail, Move(W)/Rotate(E)/Scale(R)/Universal(Q) buttons, active-state accent-dim styling, keyboard shortcuts"
      contains: "Universal (Q)"
  key_links:
    - from: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      to: "packages/renderer/src/hooks/useCommandWriter.ts"
      via: "onChange/onDrag handler calls writeTransform(mappingName, transform12, scale3) imperatively, gated on the relevant guard channel not being 'blocked'"
      pattern: "writeTransform"
    - from: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      to: "packages/renderer/src/state/liveStore.ts"
      via: "reads hasTarget/targetUnavailableOnBuild/guardState for honest labeling + write suppression"
      pattern: "hasTarget"
    - from: "packages/renderer/src/panels/viewport/Viewport.tsx"
      to: "packages/renderer/src/panels/viewport/TransformGizmo.tsx"
      via: "mounted inside the R3F scene alongside GizmoModeRail as an absolutely-positioned DOM overlay sibling"
      pattern: "TransformGizmo"
---

<objective>
Build the interactive transform gizmo (drei `TransformControls` restyled per sketch 011-B) and its
mode-selection rail — the core drag-to-write interaction that makes LIVE-03's headline claim ("drag a
viewport gizmo and see the object move") a reality, wired into the existing Viewport.tsx scene, and —
per this review round — honest about WHICH object it is actually moving and traffic-aware about a
known-blocked guard channel.

Purpose: this is the first genuinely new user-visible capability this phase adds to the viewport — a
real object can now be moved via direct manipulation, gated correctly offline (D-05), covering all
four modes including Scale (D-09, in scope on both targets), and — closing Sonnet's HIGH review
finding — never silently moving the player when the user believes they are moving the object they are
viewing.

Output: `TransformGizmo.tsx`, `GizmoModeRail.tsx`, and their mount points in `Viewport.tsx`. The HUD
chrome around them (client card, readout bar, StatusBar mirror) is 05-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-PLAN.md
</context>

<interfaces>
Existing Viewport.tsx overlay/imperative patterns to mirror (full file read this session):
- `MissingDepsOverlay` (lines 103-128): absolutely-positioned DOM overlay reading a Zustand store, rendered as a SIBLING of `<Canvas>`, not inside it.
- `StatsCollector` (lines 50-71): `useFrame`-throttled imperative callback, NOT per-frame React state — the exact discipline TransformGizmo's readout-mirroring must follow (05-11 uses this same idiom for the readout bar; this plan's gizmo itself uses `onChange`/`onDrag` callbacks from drei's TransformControls, which are already imperative by design).

05-07's exported write API (import from `../../hooks/useCommandWriter`):
`writeTransform(mappingName: string, transform: ArrayLike<number> /* 12 */, scale: ArrayLike<number>
/* 3 */): void` — synchronous, zero-allocation, call directly from the drag handler.

liveStore fields this plan reads (from 05-07, VERIFIED shapes): `status` (attached/idle/connecting/
error — gates online/offline), `verifiedState.transform` (current live pose, when attached),
`verifiedState.hasTarget`/`verifiedState.targetUnavailableOnBuild` (05-CONTEXT decision #1 — whether
the currently-tracked object is the player's actual in-game target or a player fallback, and whether
target-tracking is even possible on the attached build), `guardState: { transform: 'ok'|'blocked';
scale: 'ok'|'blocked' }` (per-channel — 05-11 surfaces the full detail in the HUD banner; this plan
uses it ONLY to suppress redundant writes to an already-blocked channel, never as a hard gate the user
cannot override by fixing the underlying tamper and letting the guard clear).

UI-SPEC sketch-locked fixed hexes (do NOT theme-switch): X `#e0584f` / Y `#6db33f` / Z `#4a8cff` —
each axis ALWAYS carries its letter label; color never carries axis identity alone.

Live/offline state-encoding table (UI-SPEC, applies to every surface including this one):
| Channel | Live | Offline |
|---|---|---|
| Glyph | ● (accent) | ○ (warn) |
| Border | solid accent-line | dashed warn |
| Write target | → client ✓ | → staged (patch) [EDITORS ONLY — D-05: the GIZMO uses disabled-with-reason instead, see below] |

D-05 (ERRATUM applied here, not the UI-SPEC's literal gizmo copy): offline, the gizmo renders
non-interactive with copy `○ Offline — attach a client to move objects live` — no staging, no fake
local-only move.

05-CONTEXT decision #1 (target identity — NEW this review round, source-grounded in 05-03): when
`status.kind === 'attached'`, the gizmo area additionally shows ONE of: `target: <templateName>` (when
`verifiedState.hasTarget` is true — the object being moved is the player's actual in-game target,
legacy SWGEmu only), `target: none — moving player avatar` (when attached, no target set, or on the
advertised build where target-tracking is a documented gap — `verifiedState.targetUnavailableOnBuild`
true), or the existing offline copy when not attached at all. This is a SMALL inline label, not a new
panel — 05-11 owns the full client-card anatomy; this plan just ensures the gizmo's own immediate
vicinity is never silent about what it moves.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TransformGizmo — restyled drei TransformControls, four modes, offline disabled-with-reason, honest target label, guard-aware write suppression</name>
  <files>packages/renderer/src/panels/viewport/TransformGizmo.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — SceneContent, the mesh/scene object this gizmo attaches to, existing R3F patterns)
    - packages/renderer/src/hooks/useCommandWriter.ts (05-07 — writeTransform signature)
    - packages/renderer/src/state/liveStore.ts (05-07 — status/verifiedState/cowSnapshot/guardState fields, VERIFIED per-channel shape)
    - 05-UI-SPEC.md Surface 1 items 3-4 (gizmo-mode rail + transform gizmo anatomy) and the Flagged Assumption 1 (drei TransformControls base is explicitly permitted)
    - 05-REVIEWS.md's Sonnet HIGH finding (target identity) and Cursor MEDIUM finding (guard-aware write suppression) — this task closes both
  </read_first>
  <action>
    Create TransformGizmo.tsx wrapping `@react-three/drei`'s `TransformControls`, attached to the
    currently-loaded viewport object (the same object `Viewport.tsx`'s `SceneContent` already
    renders — read its ref/group from the existing scene graph rather than introducing a new object
    picker; per this phase's scope, the gizmo target is always the single displayed/live object, no
    multi-object selection system is introduced). Accept a `mode: 'translate' | 'rotate' | 'scale'`
    prop (drei's native mode names; map GizmoModeRail's Move/Rotate/Scale/Universal labels to these —
    'Universal' shows translate+rotate+scale handles simultaneously if drei supports a combined mode,
    otherwise cycle through the three on repeated Q presses or render all three gizmo controls
    stacked — pick the simpler, well-supported drei option and document the choice). Restyle per the
    sketch: apply the locked axis hexes (X `#e0584f`/Y `#6db33f`/Z `#4a8cff`) to the corresponding
    TransformControls handle materials, add a text-sprite or HTML label per axis showing its letter
    (X/Y/Z) — TransformControls does not natively render letter labels, so either use drei's `Html`
    helper anchored to each axis handle's local position, or overlay them via a small custom shader —
    pick whichever this codebase's existing text-in-3D-scene precedent uses, if any (grep for `Html`
    usage from drei elsewhere in this codebase first). Widen the hit area (drei exposes a `size` prop
    that scales the whole gizmo, or per-axis hit padding — check the installed `@react-three/drei`
    version's API and use whatever knob it exposes to get "generous hit area, hover thickening 2.6→4.2
    line width, enlarged label on hover" as close as the library allows; document any UI-SPEC detail
    the library cannot express exactly). On `onChange` (drei's live-drag callback, fired continuously
    during a drag): read the object's current world transform (position+quaternion->3x4 matrix,
    row-major, matching the SAME 48-byte layout convention `LIVE_CHANNEL_LAYOUT.TRANSFORM` uses — this
    requires converting THREE.js's transform representation into that row-major float[3][4] shape;
    write this conversion as a small pure helper function, unit-testable) and the object's current
    scale (3 floats). Before calling `writeTransform`, check the RELEVANT guard channel for the active
    `mode` (`scale` mode checks `liveStore.guardState.scale`; `translate`/`rotate`/`universal` check
    `liveStore.guardState.transform`) — if that channel is `'blocked'`, SKIP the write this frame
    (Cursor MEDIUM fix: pure traffic suppression, the agent's own guard remains the sole safety
    authority regardless of this client-side check; a subsequent guard-clearing action, e.g. Revert
    ALL via 05-11, will un-block it and writes resume automatically on the NEXT drag-move event — no
    special re-enable logic is needed here since this check re-evaluates every frame). Otherwise call
    `writeTransform(mappingName, transform12, scale3)` — reuse ONE preallocated local array for the
    THREE.js-to-row-major conversion (do not allocate a new Float32Array inside onChange). Gate
    interactivity on `liveStore.status.kind === 'attached'`: when NOT attached, render the gizmo with
    `enabled={false}` (drei's disable prop) and render a small inline label near the gizmo (or via the
    mode rail area) reading exactly `○ Offline — attach a client to move objects live` (D-05 — do not
    render any "staged (patch)" copy for the gizmo itself, that phrasing is reserved for the two typed
    editors per this phase's errata). When ATTACHED, additionally render the honest target-identity
    label per this plan's Interfaces section (`target: <templateName>` /
    `target: none — moving player avatar` — the SAME small inline area, mutually exclusive with the
    offline copy since they're gated on the same `status.kind` check) — 05-CONTEXT decision #1: never
    let the user believe the gizmo moves "the object in the viewport" without this label confirming
    what it actually resolved to.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- TransformGizmo</automated>
  </verify>
  <acceptance_criteria>
    - A test simulating a drag-change event while `status.kind==='attached'` and `guardState.transform==='ok'` asserts `writeTransform` was called with a 12-length and a 3-length array.
    - A test simulating a drag-change event in `translate` mode while `guardState.transform==='blocked'` asserts `writeTransform` was NOT called (traffic suppression) — and a companion test in `scale` mode while `guardState.scale==='blocked'` (but `guardState.transform==='ok'`) shows Scale-mode writes are independently suppressed without affecting Move/Rotate.
    - A test asserts `writeTransform` is NEVER called while `status.kind!=='attached'` (the gizmo is disabled offline — no fallback write path exists).
    - The offline label text matches `○ Offline — attach a client to move objects live` exactly (grep/DOM text match).
    - When attached with `verifiedState.hasTarget===true`, the rendered output contains `target: ` followed by the templateName; when attached with `hasTarget===false`, it contains `target: none — moving player avatar` (component test asserts both branches render distinct, correct text — never silently omitting this label while attached).
    - Each axis handle has an associated letter label element (X/Y/Z) discoverable in the rendered output, not only a color.
  </acceptance_criteria>
  <done>The gizmo is interactive only when live, writes through the zero-alloc path (suppressing redundant writes to a known-blocked guard channel), is honest about its offline state per D-05, and is honest about WHAT it is moving per 05-CONTEXT decision #1 — no fake staging, no silent wrong-object moves.</done>
</task>

<task type="auto">
  <name>Task 2: GizmoModeRail + Viewport mount wiring</name>
  <files>packages/renderer/src/panels/viewport/GizmoModeRail.tsx, packages/renderer/src/panels/viewport/Viewport.tsx</files>
  <read_first>
    - packages/renderer/src/panels/viewport/Viewport.tsx (full file — where absolutely-positioned overlay siblings of `<Canvas>` are rendered, e.g. `MissingDepsOverlay`'s mount point, to mirror for this rail's mount point)
    - This plan's Task 1 output (TransformGizmo's `mode` prop contract)
    - 05-UI-SPEC.md Surface 1 item 3 (exact rail anatomy: 40px vertical buttons, icon over key label, active state styling, keyboard shortcuts)
  </read_first>
  <action>
    Create GizmoModeRail.tsx: a left-edge, vertically-centered rail of four 40px-wide buttons — `✥ W`
    Move, `⟳ E` Rotate, `⤢ R` Scale, `✛ Q` Universal — using local or lifted state for the active
    mode (lift it to a small local hook/context shared with TransformGizmo, or accept it as a
    controlled prop from Viewport.tsx — executor's choice, but the active mode must be a SINGLE
    source of truth consumed by both this rail and TransformGizmo, not two independently-tracked
    states that can drift). Active button styling: `--color-accent-dim` fill + accent border + accent
    text (per UI-SPEC Color "Accent reserved for #4"). Wire keyboard shortcuts W/E/R/Q (global
    keydown listener scoped to when the viewport panel has focus/is visible — mirror whatever
    existing keyboard-shortcut pattern this codebase already uses elsewhere, if any, rather than
    inventing a new global-listener convention). Use `role="radiogroup"` on the rail container and
    `aria-pressed` per button (UI-SPEC Accessibility contract) — additionally apply the SAME
    `aria-pressed`/`role="radiogroup"` parity to the DTII/`.stf` editors' Grid|Hex toggle if that
    toggle does not already have it (05-06/05-08 built it without an explicit accessibility
    acceptance criterion — a one-line, low-cost fix while this task is already touching the identical
    a11y pattern; skip only if 05-06/05-08 already landed it, verified by grep first). Mount both
    `GizmoModeRail` and `TransformGizmo` in Viewport.tsx: `GizmoModeRail` as an absolutely-positioned
    DOM sibling of `<Canvas>` (same layer as `MissingDepsOverlay`), `TransformGizmo` INSIDE the R3F
    `<Canvas>`'s scene tree (it is a 3D object, not a DOM overlay) — pass the shared `mode` state down
    into `TransformGizmo`'s `mode` prop.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- GizmoModeRail Viewport</automated>
  </verify>
  <acceptance_criteria>
    - Pressing "R" while the viewport has focus activates Scale mode (asserted via a keydown-simulation test) — Scale is not disabled or grayed out (D-09).
    - The rail has `role="radiogroup"` and exactly one button has `aria-pressed="true"` at any time.
    - GizmoModeRail and TransformGizmo share one mode value (a test changing the rail's active button asserts TransformGizmo's `mode` prop updates to match).
  </acceptance_criteria>
  <done>The mode rail and gizmo are mounted in the viewport, share one source of truth for the active mode, and Scale is a fully live, non-disabled mode per the corrected D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User gizmo drag (untrusted input magnitude/rate) → writeTransform → shared memory | The gizmo can generate writes at up to 60fps; the write path must remain zero-allocation and must never bypass the offline gate. Client-side guard-channel suppression is a traffic optimization ONLY — it must never be treated as the actual safety boundary (the agent's own guard is, always). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering | TransformGizmo offline gate | mitigate | `enabled={liveStore.status.kind==='attached'}` is the single gate; acceptance criteria assert zero writeTransform calls while not attached — no secondary "preview" write path exists that could leak a write attempt offline. |
| T-05-17 | DoS (perf) | onChange drag handler allocation | mitigate | One preallocated conversion buffer is reused per component instance; acceptance criteria verify writeTransform receives fixed-length arrays without asserting object identity here (identity is 05-07's concern) but the handler itself must not `new Float32Array(...)` per call — code-reviewed via the task's action text, not just tested. |
| T-05-30 | Spoofing (wrong object moved) | Target-vs-player identity | mitigate | The gizmo ALWAYS renders an honest `target: ...` label while attached (never silently omitted), sourced from 05-03's agent-resolved, source-grounded target chain — never a toolkit-side guess about what the viewport mesh corresponds to (05-CONTEXT decision #1, closes REVIEWS.md Sonnet HIGH finding). |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- TransformGizmo GizmoModeRail Viewport` — all green.
</verification>

<success_criteria>
- The gizmo writes through the zero-alloc path only while attached; offline it is honestly disabled with the D-05 copy; a known-blocked guard channel suppresses redundant writes on the client side.
- All four modes exist and work, with Scale fully in scope (not disabled) per the corrected D-09.
- Axis letters are always visible alongside color.
- The gizmo always honestly labels what object it is moving (target vs. player fallback) while attached.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-10-SUMMARY.md` when done
</output>
</output>
