---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 12
type: execute
wave: 6
depends_on: ["05-11"]
files_modified:
  - packages/live-inject/test/soak.test.ts
autonomous: false
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): folds in 05-CONTEXT decision #2 (advertised Scale =
# graceful degrade + documented known gap, NOT a hard phase-completion blocker) and several
# REVIEWS.md findings that land specifically in this UAT's steps/watch items.
# - Decision #2: Step 3's language is corrected to match the ACTUAL post-fix behavior (05-03's
#   null-seeded setScale + independently-gated applyWrite): on the advertised build, before the
#   upstream engine_advertise.cpp row exists, a Scale write is CLEANLY REFUSED (SCALE_REFUSED guard
#   bit set, no setter called, no crash) — this is a real, working graceful-degrade, not a "silent
#   no-op" (the ORIGINAL plan's wrong failure-mode description, which Opus/Codex flagged and which
#   this fix's null-seed design now makes actually true). UAT PASSES without advertised Scale working;
#   it is recorded as a known follow-up per decision #2, not a blocking failure — Move/Rotate on BOTH
#   targets and Scale on the LEGACY target are the hard bar.
# - REVIEWS.md Opus MEDIUM (off-thread setter invocation is new/unproven) — added as an explicit watch
#   item in step 2/3's sustained-dragging pass, not just implied.
# - Decision #1 (object targeting) — added step 1b confirming the honest target/player label.
# - REVIEWS.md Fix F (stop-signal starvation on null player) — added step 7b, a regression check
#   specifically for detaching while the world is not loaded (the exact scenario the bug would have
#   silently failed on).
# - REVIEWS.md Fix D (interior-object guard false-fail) — added a note to step 2 to prefer testing
#   against BOTH a world-cell object and, if practical, an interior/building-interior object, since
#   that is the scenario the original guard design would have false-failed on write 2+.

must_haves:
  truths:
    - "A GC-pressure soak test proves the command-slot's native ArrayBuffer/GC-guard survives repeated forced collection without dangling the native pointer (LIVE-03 SC1)."
    - "A human confirms live in-world: dragging the gizmo moves the CORRECTLY-IDENTIFIED object (target-or-player, honestly labeled) in a running client with zero restart, on BOTH the advertised (swg-client-v2) and legacy (SWGEmu) targets, for Move/Rotate; Scale is confirmed on the LEGACY target as a hard bar, and on the ADVERTISED target Scale's clean graceful-degrade (no crash, honest disabled/refused state) is confirmed as the documented known-gap outcome per 05-CONTEXT decision #2."
    - "A human confirms the fail-closed guard independently for transform AND scale: forcing a byte mismatch on either channel refuses that write with the real address + expected/got bytes shown, and Revert ALL restores the attach-time transform AND scale — and remains clickable/effective even while a guard is blocked."
    - "A human confirms clean detach even when triggered while the world is not loaded (STOP_REQUESTED starvation regression check, REVIEWS.md Fix F)."
  artifacts:
    - path: "packages/live-inject/test/soak.test.ts"
      provides: "Automated GC-pressure soak assertion (repeated global.gc()-forced collection cycles while the command-slot channel remains open and writable)"
      contains: "soak"
---

<objective>
Close LIVE-03 with the two verifications that cannot be automated away: a GC-pressure soak test
proving the command-slot binding never dangles under memory pressure, and a maintainer-driven in-world
UAT proving the drag-to-move loop actually works against both real, running SWG clients — including
Scale (hard bar on legacy, documented graceful-degrade-not-crash on advertised per decision #2),
correct object-targeting (decision #1), and the review round's stop-signal/guard-revert fixes.

Purpose: per the standing quality gate, "checkpoint + implementation in same plan" is a split signal —
this plan is deliberately implementation-free (one small automated soak test) plus the blocking
human-verify checkpoint the prior 11 plans built toward.

Output: a passing GC-pressure soak test, and an approved manual UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-VALIDATION.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-11-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GC-pressure soak test</name>
  <files>packages/live-inject/test/soak.test.ts</files>
  <read_first>
    - packages/live-inject/src/channel_binding.cpp (05-04 — the Napi::Reference GC-guard pattern this test exercises: abRef refcount=1, Reset() in cleanupChannel, finalizer-owns-view-lifetime)
    - 05-RESEARCH.md's Security Domain "Dangling native pointer on GC of the command-slot ArrayBuffer under memory pressure" threat entry and its prescribed mitigation
    - 05-VALIDATION.md Wave 0 Requirements ("GC-pressure soak test harness: native pointer survives GC without dangling")
  </read_first>
  <action>
    Create soak.test.ts: open a channel via `addon.openChannel(name)`, then run N (e.g. 500-1000)
    iterations of: call `addon.writeCommand(name, transform12, scale3, 0)`, force garbage collection
    (`global.gc()` — requires running vitest with `--expose-gc`; add that flag to this package's test
    script/config if not already present), call `addon.readChannelView(name)` and assert it still
    returns a valid, non-null 396-byte ArrayBuffer (05-01's revised layout — GUARD_ADDR included) with
    the just-written values readable back out correctly (seqlock even, transform matches). This proves
    the `Napi::Reference` refcount=1 guard keeps the ArrayBuffer alive across GC pressure while C++
    still holds the view pointer, and that the JS-side re-acquisition of the buffer via
    `readChannelView` (a fresh `abRef.Value()` call each time, per the existing Pitfall-5 discipline
    this codebase already follows) never returns a dangling reference. Finish with
    `addon.closeChannel(name)` and assert a subsequent `readChannelView(name)` returns null (clean
    teardown, no dangling handle).
  </action>
  <verify>
    <automated>pnpm --filter @swg/live-inject test -- soak</automated>
  </verify>
  <acceptance_criteria>
    - The soak test runs at least 500 write+GC+read cycles without a crash, a null-where-valid-expected read, or a value mismatch.
    - The test explicitly forces GC (not just relies on incidental collection timing) via `global.gc()` with the runner configured for `--expose-gc`.
    - Every `readChannelView` call in this test returns a buffer of length 396 (not 392, not 320).
  </acceptance_criteria>
  <done>The command-slot binding is proven GC-safe under forced collection pressure — LIVE-03 SC1's "survives a GC-pressure soak test without dangling the native pointer" clause is automated, not just asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer in-world UAT — target-correct drag-to-move on both targets, independent transform/scale guard fail-closed, Revert ALL (incl. while blocked), clean detach (incl. null-player), advertised-Scale known-gap confirmation</name>
  <files>packages/renderer/src/panels/viewport/TransformGizmo.tsx, packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx, packages/live-inject/agent/agent_main.cpp</files>
  <action>
    Pause for human verification. Claude has built and automated-tested the full LIVE-03 write loop
    (05-01 through 05-11) plus this plan's GC-pressure soak test; the maintainer manually confirms the
    end-to-end in-world behavior per how-to-verify before LIVE-03 is considered done. Do NOT
    auto-approve — this is a blocking gate, since no automated substitute exists for confirming a real
    object moves in a real running client. Per 05-CONTEXT decision #2: advertised-build Scale is a
    DOCUMENTED KNOWN GAP, not a phase-completion blocker — the UAT PASSES as long as (a) Move/Rotate
    work on BOTH targets, (b) Scale works on the LEGACY target, and (c) Scale on the ADVERTISED target
    cleanly, honestly refuses (no crash, no silent no-op — a real SCALE_REFUSED guard state visible in
    the HUD) when the upstream `engine_advertise.cpp` row has not yet been added. Record the advertised
    Scale outcome (working, if the maintainer added the upstream row; or the documented graceful-refuse
    gap, if not) as a note either way — never as a failure of steps (a)/(b).
  </action>
  <what-built>
    The complete LIVE-03 WYSIWYG write loop: viewport gizmo (Move/Rotate/Scale/Universal) writing
    through a zero-allocation, 60fps command channel into the injected agent, which resolves the
    correct target-or-player focus object (05-CONTEXT decision #1), read-verify-guards every write
    INDEPENDENTLY per channel (transform, scale) against the attach-time/last-successful-write
    baseline before calling the client's own setTransform_o2w/setScale setters — plus the full HUD
    (client card with real guard-blocked address/expected-bytes and target identity, readout bar,
    StatusBar mirror) and the D-04 agent-lifecycle fixes (clean stop-signal honored even before a
    player exists, detach control, legacy 64-bit networkId intentionally left UNAVAILABLE rather than
    wrong per REVIEWS.md Fix E).
  </what-built>
  <how-to-verify>
    1. Launch or attach to a running **swg-client-v2** (advertised) client via the existing
       attach/launch UI (LiveInspectorPanel). Confirm the Live-Sync client card shows `● Live ·
       injected` and BOTH read-verify guard rows (`transform: ✓ client bytes = snapshot`; `scale: —
       not yet written this session` until a scale write happens).
    1b. **Target identity (05-CONTEXT decision #1):** confirm the client card's `target` row and the
       gizmo's inline label state one of: the actual in-game targeted object's template name (legacy
       SWGEmu, when you have something targeted in-game), or `none — moving player avatar` /
       `unavailable on this build — moving player avatar` (advertised, or legacy with nothing
       targeted). Target something in-game (legacy client only) and confirm the label updates to name
       that object, and dragging the gizmo moves THAT object, not the player, when a target is active.
    2. With an object loaded in the viewport (matching the live focus object from step 1b), drag the
       Move gizmo. Confirm the object visibly moves in the running client window in real time, the
       bottom readout bar's Pos numboxes update live, and a new row appears in the client card's write
       log. **Watch item (REVIEWS.md Opus MEDIUM — off-thread setter invocation is new/unproven):**
       sustain dragging for at least 30-60 seconds continuously and confirm no crash/hitch — this
       exercises the agent calling engine setters from its own poll thread rather than the game's main
       loop, which is architecturally sound per D-01 but had no prior in-world proof before this
       phase. If practical, repeat this step against BOTH a world-cell (outdoor) object AND an
       interior/building-interior object — the guard's read-back-refresh fix (REVIEWS.md Fix D)
       specifically targets the interior case, where earlier designs would false-fail the guard on
       write 2+.
    3. Switch to Rotate (E) and Scale (R) modes and repeat — confirm both work on the LEGACY SWGEmu
       target (step 4). On the ADVERTISED target, attempt a Scale drag: if the maintainer has added
       the `object::setScale` row to `engine_advertise.cpp` and rebuilt (per 05-03's `user_setup`),
       confirm Scale works identically to Move/Rotate; if NOT, confirm the write is CLEANLY REFUSED —
       the HUD's scale guard row / StatusBar guard chip shows a refused state naming `scale`
       specifically, NO crash occurs, and this is recorded as the documented known gap (05-CONTEXT
       decision #2), not a toolkit defect.
    4. Repeat steps 1-3 against a running **legacy SWGEmu** client (build 0.0.119.798 or the
       maintainer's current build) — confirm Move/Rotate/Scale all work via the legacy RVA path (this
       is the hard bar for Scale; it has no upstream dependency on this target).
    5. Force a guard-blocked scenario on the TRANSFORM channel (e.g. have another tool or the game
       itself move the object between writes, or manually poke the object's position via an external
       means) and confirm: the card border turns danger, the `transform` guard row shows `✗ client
       bytes ≠ snapshot` (the `scale` row is UNAFFECTED — still showing its own independent state),
       and the banner shows the EXACT locked copy with a REAL memory address (not a placeholder) and
       real expected/got byte values — confirm there is NO way to force the write through. If
       practical, repeat for the SCALE channel independently (force an external scale change) and
       confirm the `scale` row alone flips, `transform` stays green.
    6. Click `⟲ Revert ALL to snapshot` WHILE the guard is still in the blocked state from step 5 and
       confirm: the button is clickable (not disabled), the object snaps back to its attach-time pose
       AND scale in the running client, the banner switches to the reverted-state copy, and the
       write log's prior entries render struck-through — this specifically proves REVIEWS.md's Opus
       MEDIUM fix (a blocked guard no longer permanently blocks its own revert).
    7. Click the new `Detach` control in LiveInspectorPanel and confirm the session cleanly returns to
       idle/file-patch mode with no lingering agent thread (repeat attach→detach 3+ times and confirm
       no growing resource usage / no stale channel errors on re-attach).
    7b. **Stop-signal starvation regression (REVIEWS.md Fix F):** attach to a client BEFORE a
       character/world has fully loaded (or force this by attaching immediately after launch, before
       entering the world), then immediately click `Detach`. Confirm the detach completes promptly
       (not hung/stuck) — this is the exact scenario the original stop-signal-after-player-null-check
       ordering bug would have silently failed (the poll loop would starve on `Sleep(100); continue;`
       and never observe STOP_REQUESTED until a player eventually loaded).
  </how-to-verify>
  <resume-signal>Reply "approved" once steps 1-2, 3 (legacy Scale)/4, 5-7, and 7b all pass on both targets, noting the advertised-Scale outcome from step 3 as either "working" or "documented known gap (clean refuse, no crash)" — either is an approval per decision #2. Otherwise describe exactly which step/target failed and whether it is a toolkit defect vs. the documented upstream engine_advertise.cpp dependency for advertised-Scale.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human UAT operator ↔ a real, running game client | This is the final gate before LIVE-03 is considered done; no automated substitute exists for the actual in-world visual confirmation, including the off-thread-setter-invocation and stop-signal-starvation classes of bug that only manifest against a real client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | DoS (memory) | Long-running soak / repeated attach-detach | mitigate | Task 1's automated soak covers GC pressure; the UAT's step 7/7b covers the repeated-attach-detach and null-player-detach resource-leak classes the automated soak does not exercise (real OS thread/handle lifecycle). |
| T-05-32 | DoS (crash) | Off-thread setter invocation under sustained load | accept (watched) | Architecturally sound per D-01 (the client's own setter is called, which internally handles its own notification/dirty-flag bookkeeping) but genuinely unproven against a real client until this UAT's sustained-drag watch item (step 2) — accepted as the correct design with an explicit verification step, not silently assumed safe. |
</threat_model>

<verification>
`pnpm --filter @swg/live-inject test -- soak` green, followed by the human-verify checkpoint above.
</verification>

<success_criteria>
- The GC-pressure soak test passes against the 396-byte layout.
- The maintainer approves the in-world UAT: Move/Rotate on both targets, Scale on legacy (hard bar), advertised Scale's outcome recorded as working-or-documented-gap (decision #2), target identity correctly labeled, independent transform/scale guard fail-closed with real address/bytes, Revert ALL works even while blocked, and detach is clean including the null-player-starvation regression case.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-12-SUMMARY.md` when done
</output>
</output>
