---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 12
type: execute
wave: 6
depends_on: ["05-11"]
files_modified:
  - packages/live-inject/test/soak.test.ts
autonomous: false
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): folds in 05-CONTEXT decision #2 (advertised Scale =
# graceful degrade + documented known gap, NOT a hard phase-completion blocker) and several
# REVIEWS.md findings that land specifically in this UAT's steps/watch items.
# - Decision #2: Step 3's language is corrected to match the ACTUAL post-fix behavior (05-03's
#   null-seeded setScale + independently-gated applyWrite): on the advertised build, before the
#   upstream engine_advertise.cpp row exists, a Scale write is CLEANLY REFUSED (SCALE_REFUSED guard
#   bit set, no setter called, no crash) — this is a real, working graceful-degrade, not a "silent
#   no-op" (the ORIGINAL plan's wrong failure-mode description, which Opus/Codex flagged and which
#   this fix's null-seed design now makes actually true). UAT PASSES without advertised Scale working;
#   it is recorded as a known follow-up per decision #2, not a blocking failure — Move/Rotate on BOTH
#   targets and Scale on the LEGACY target are the hard bar.
# - REVIEWS.md Opus MEDIUM (off-thread setter invocation is new/unproven) — added as an explicit watch
#   item in step 2/3's sustained-dragging pass, not just implied.
# - Decision #1 (object targeting) — added step 1b confirming the honest target/player label.
# - REVIEWS.md Fix F (stop-signal starvation on null player) — added step 7b, a regression check
#   specifically for detaching while the world is not loaded (the exact scenario the bug would have
#   silently failed on).
# - REVIEWS.md Fix D (interior-object guard false-fail) — added a note to step 2 to prefer testing
#   against BOTH a world-cell object and, if practical, an interior/building-interior object, since
#   that is the scenario the original guard design would have false-failed on write 2+.

# REVISION NOTE (--reviews replan, ROUND 2, 2026-07-08): folds in 05-REVIEWS.md ROUND 2 decisions.
# - Decision #1 (object targeting, PURSUE THE STRONGER PATH) — step 1b is REWRITTEN: round 1's step
#   1b only tested the LEGACY-only lookAt-target chain and described the advertised build as an
#   honest player-fallback. ROUND 2's 05-03 adds a REAL advertised-build selected-object resolver
#   (getSelectedObject) — step 1b now tests BOTH builds' real targeting mechanisms, not one real +
#   one honest-fallback.
# - Decision #2 (W2 fix) — step 3's language is REWRITTEN to describe the actual ROUND 2 behavior:
#   before the upstream setScale catalog row exists, the advertised build shows a STRUCTURAL "Scale
#   unavailable on this build" state (liveness bit4, scaleUnavailableOnBuild) — NOT a guard-refused
#   tamper banner. This is a stronger, more honest UX than round 1's fix (which still routed the
#   unresolved-setter case through the tamper-style SCALE_REFUSED guard bit).
# - Opus MEDIUM watch item (off-thread setter race) — EXPANDED per the maintainer's explicit ROUND 2
#   instruction: the new cross-build resolver chain (getSelectedObject on advertised; the unchanged
#   +1432 chain on legacy) amplifies this pre-existing race, since resolution now runs through an
#   additional accessor call per frame on the advertised build. Step 2 gains an explicit sub-item
#   naming this.
# - Step 6 (Revert ALL while blocked) is REWORDED to describe the ROUND 2 coalesced single-command
#   behavior (05-07's gate-on-blocked + 05-03's coalesce) instead of round 1's two-command sequence,
#   and to confirm the reverted banner's discarded-change disclosure (05-11 ROUND 2 decision #3).

# REVISION NOTE (de-anchoring crew fold-in, 2026-07-09): folds in .planning/research/CONSULT-05-SYNTHESIS.md
# per the maintainer's Decision 2 (legacy targeting parity). The off-thread/sim-thread lifetime hazard
# (Opus MEDIUM, T-05-32) was previously worded in this plan's step 2 watch item as amplified ONLY by
# "the ADDITIONAL per-frame getSelectedObject() resolver call on the advertised build" — that framing
# under-covered the LEGACY path, which resolves a raw `Object*` via the SAME class of per-frame
# resolver call chain (getPlayerCreatureObject() -> +1432 -> cachedNetworkIdGetObject) and is exposed
# to the IDENTICAL sim-thread free/relocate hazard once that resolved pointer is written through. This
# was never a legacy behavior change (05-03's legacy focus-resolution mechanism is unchanged from round
# 1) — it is a WATCH-ITEM COVERAGE gap: 05-03's crew-verified 2026-07-09 revision corrected a false
# "legacy has no independent networkId source" premise and re-confirmed the +1432 chain is a genuine,
# wired resolver (not dead code), which makes explicit that the SAME lifetime-hazard class applies to
# it. Step 2's watch item below now names BOTH builds' resolver chains explicitly, and the threat
# register's T-05-32 mitigation text is corrected to match (see 05-03-PLAN.md's matching T-05-32 update
# for the paired change).

# REVISION NOTE (--reviews replan ROUND 4, 2026-07-12): folds in 05-REVIEWS.md round-3 maintainer
# decisions #3(3)/(4) and #5.
# (3.3) HIGH — deliberate mismatch UAT step. Step 2 previously read "with an object loaded in the
#     viewport (matching the live focus object from step 1b)" — a maintainer decision flagged this as
#     GAP-HIDING wording: it pre-arranges the ONE scenario (viewport asset happens to match the live
#     target) where the viewport<->live-object binding gap is invisible, so sign-off would never
#     surface it. FIX: that wording is REPLACED (see step 2 below), and a NEW step 1c is added that
#     DELIBERATELY tests the MISMATCH case (load asset X in the viewport, select an unrelated object Y
#     in-game, confirm the 05-11 ROUND 4 `⚠ viewing X, moving Y` warning fires) and records the
#     maintainer's explicit acknowledgement that the viewport<->live-object binding gap remains open
#     (deferred to Phase 7's `.ws` placement editing) — this is honesty-by-construction, not a pass/
#     fail bar on closing the gap itself.
# (3.4) Soften LIVE-03 "done"/"complete" language throughout this UAT to explicitly name the deferred
#     viewport<->live-object binding as a KNOWN, DOCUMENTED, non-blocking gap (same posture as
#     decision #2's advertised-Scale known gap) — never implying LIVE-03 asserts "the viewport shows
#     what moves," only "the toolkit can move what's targeted/selected in-game, live, guarded, and now
#     honestly labeled including when it does NOT match the viewport."
# (4) MEDIUM — SEH/UAF watch item, named explicitly. 05-03 ROUND 4 wraps the resolve->read->apply span
#     in SEH; step 2/4's off-thread-setter watch item is EXTENDED to name the SPECIFIC mitigation now
#     in place (a caught access violation should degrade to ONE skipped frame + a transient
#     `agentFaultRecovered` liveness bit, NEVER a client crash) and to explicitly name the intermediate
#     `hudInstance` pointer window (the advertised two-step resolver's most exposed intermediate
#     value) as the specific thing being watched.
# (5) MEDIUM — STOP sticky-bit retry, named in step 7/7b. detachUI (05-07 ROUND 4) now retries
#     writeStop while polling the agent's sticky GUARD_FLAG_STOPPING bit (05-01/05-03) instead of a
#     single fire-and-hope send — step 7's repeated attach/detach check and step 7b's null-player
#     regression check both implicitly exercise this; no NEW step is needed, but the watch-item
#     language below now names what "clean detach" structurally relies on.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decision #1 (the load-bearing identity-unification fix). 05-01's channel layout grows AGAIN this
# round, 396 -> 400 bytes (a NEW `FOCUS_TOKEN` read-frame field, offset 320) — this plan's Task 1
# soak test's byte-size assertions are updated from 396 to 400 throughout. No NEW UAT step is added for
# decision #1 itself — the two-same-template-object revert test and the flip-A-to-B-to-A test are
# BOTH automated unit tests in 05-07's Task 2 (liveStore, no live client needed), not UAT steps; the
# maintainer's existing step 5/6 (guard-blocked + Revert ALL) already exercises the live write/revert
# path this fix hardens, and no additional manual verification is needed beyond it.

# REVISION NOTE (--reviews replan ROUND 6, 2026-07-12): folds in 05-REVIEWS.md ROUND 5 maintainer
# decision "(a) structural" (the pointer-ABA close, 05-03/05-07's own ROUND 6 notes) — no NEW channel
# growth this round (still 400 bytes, unchanged from ROUND 5). Opus's round-5 finding accepted a
# residual, narrower case as accept-watched rather than fully closed (same-template + legacy
# networkId≡0 + realloc-at-the-same-address) — per the maintainer's explicit instruction, this residue
# is covered by a NEW UAT watch item (step 8 below) rather than a fully-automated test, since it
# requires a genuine despawn+respawn sequence against a real running client. Step 8 does not gate
# approval on reproducing the narrow residue itself (that would require engineering an exact address
# coincidence, impractical to force deliberately) — it gates on confirming the ORDINARY case (a
# cross-template despawn-then-retarget) behaves correctly, which the automated cross-check-recreates
# test (05-07 Task 2) already proves structurally; this step is the live-client corroboration.

must_haves:
  truths:
    - "A GC-pressure soak test proves the command-slot's native ArrayBuffer/GC-guard survives repeated forced collection without dangling the native pointer (LIVE-03 SC1)."
    - "A human confirms live in-world: dragging the gizmo moves the CORRECTLY-IDENTIFIED object (target-or-player, honestly labeled) in a running client with zero restart, on BOTH the advertised (swg-client-v2) and legacy (SWGEmu) targets, for Move/Rotate; Scale is confirmed on the LEGACY target as a hard bar, and on the ADVERTISED target Scale's clean graceful-degrade (a structural 'unavailable on this build' state, ROUND 2 — no crash, no false tamper banner) is confirmed as the documented known-gap outcome per 05-CONTEXT decision #2."
    - "A human confirms real, cross-build object targeting: on the LEGACY build via the lookAt-target chain, AND on the ADVERTISED build via the ROUND 2 selected-object resolver — both are real mechanisms this round, not one real plus one honest fallback (05-CONTEXT decision #1b)."
    - "A human confirms the fail-closed guard independently for transform AND scale: forcing a byte mismatch on either channel refuses that write with the real address + expected/got bytes shown, and Revert ALL restores the attach-time transform AND scale via ONE coalesced command — and remains clickable/effective even while a guard is blocked, disclosing any discarded external change in the reverted banner."
    - "A human confirms clean detach even when triggered while the world is not loaded (STOP_REQUESTED starvation regression check, REVIEWS.md Fix F) — and confirms detachUI's bounded STOP retry-loop (05-07 ROUND 4) actually completes the detach, not just that SOME detach eventually happens."
    - "A human DELIBERATELY exercises the viewport<->live-object MISMATCH case (load asset X in the viewport, select an unrelated object Y in-game) and confirms the 05-11 ROUND 4 inline mismatch warning fires — this is a REQUIRED step (not optional/best-effort), and the maintainer's approval response explicitly acknowledges the binding gap remains open/deferred to Phase 7, rather than the UAT being worded around the one scenario where the gap is invisible (REVIEWS.md round-3 maintainer decision #3(3))."
    - "The sustained-drag off-thread-setter watch item additionally confirms the SEH mitigation (05-03 ROUND 4): if a fault is observed (rare, not expected to reproduce reliably in a UAT pass), it degrades to a skipped frame with no visible client crash, not an unhandled exception — the intermediate `hudInstance` pointer window (advertised two-step resolver) is the specific value named as under watch (REVIEWS.md round-3 maintainer decision #4)."
    - "The sustained-dragging off-thread-setter watch item (step 2) is exercised against BOTH builds' resolver chains — LEGACY (getPlayerCreatureObject -> +1432 -> cachedNetworkIdGetObject) AND ADVERTISED (the two-step cuiHud::g_instance -> cuiHud::getTarget resolver) — not just the advertised path, since both resolve a raw Object* subject to the same sim-thread free/relocate hazard once written through (2026-07-09 de-anchoring crew coverage fix)."
    - "A human exercises a despawn-then-retarget sequence (edit object A, force A to despawn, target a freshly-spawned object) and confirms no stale pose or write-log history from A bleeds onto the new object — corroborating, against a real client, the automated cross-check-recreates fix in 05-07/05-03's identityCache/s_expectedCapturedAgainst re-key (ROUND 6, REVIEWS.md round-5 maintainer decision 'option (a) structural')."
  artifacts:
    - path: "packages/live-inject/test/soak.test.ts"
      provides: "Automated GC-pressure soak assertion (repeated global.gc()-forced collection cycles while the command-slot channel remains open and writable)"
      contains: "soak"
---

<objective>
Close LIVE-03's build-and-guard verification with the two checks that cannot be automated away: a
GC-pressure soak test proving the command-slot binding never dangles under memory pressure, and a
maintainer-driven in-world UAT proving the drag-to-move loop actually works against both real, running
SWG clients — including Scale (hard bar on legacy, documented structural graceful-degrade on
advertised per decision #2), REAL cross-build object-targeting (decision #1b, ROUND 2 — both builds
now have a genuine resolver, not one real + one honest-fallback), the review round's
stop-signal/guard-revert/off-thread-race watch items (now explicitly covering BOTH builds' resolver
chains, 2026-07-09 de-anchoring crew coverage fix, PLUS the ROUND 4 SEH mitigation), and — ROUND 4,
REVIEWS.md round-3 maintainer decision #3 — a DELIBERATE viewport<->live-object mismatch check with
explicit maintainer acknowledgement that the binding gap itself stays open, deferred to Phase 7's
`.ws` placement editing, PLUS — ROUND 6, REVIEWS.md round-5 maintainer decision "option (a)
structural" — a despawn-then-retarget watch item corroborating the identityCache/s_expectedCapturedAgainst
pointer-ABA cross-check against a real client. "LIVE-03 done" here means the guarded, cross-build,
honestly-labeled write loop is proven live — NOT that the viewport shows what moves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-VALIDATION.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-03-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-07-PLAN.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-11-PLAN.md
@.planning/research/CONSULT-05-SYNTHESIS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GC-pressure soak test</name>
  <files>packages/live-inject/test/soak.test.ts</files>
  <read_first>
    - packages/live-inject/src/channel_binding.cpp (05-04 — the Napi::Reference GC-guard pattern this test exercises: abRef refcount=1, Reset() in cleanupChannel, finalizer-owns-view-lifetime)
    - 05-RESEARCH.md's Security Domain "Dangling native pointer on GC of the command-slot ArrayBuffer under memory pressure" threat entry and its prescribed mitigation
    - 05-VALIDATION.md Wave 0 Requirements ("GC-pressure soak test harness: native pointer survives GC without dangling")
  </read_first>
  <action>
    Create soak.test.ts: open a channel via `addon.openChannel(name)`, then run N (e.g. 500-1000)
    iterations of: call `addon.writeCommand(name, transform12, scale3, 0)`, force garbage collection
    (`global.gc()` — requires running vitest with `--expose-gc`; add that flag to this package's test
    script/config if not already present), call `addon.readChannelView(name)` and assert it still
    returns a valid, non-null 400-byte ArrayBuffer (05-01's ROUND 5 layout — FOCUS_TOKEN + GUARD_ADDR
    included, grown from 396 to 400 this round) with the just-written values readable back out correctly (seqlock even,
    transform matches). This proves the `Napi::Reference` refcount=1 guard keeps the ArrayBuffer alive
    across GC pressure while C++ still holds the view pointer, and that the JS-side re-acquisition of
    the buffer via `readChannelView` never returns a dangling reference. Finish with
    `addon.closeChannel(name)` and assert a subsequent `readChannelView(name)` returns null (clean
    teardown, no dangling handle).
  </action>
  <verify>
    <automated>pnpm --filter @swg/live-inject test -- soak</automated>
  </verify>
  <acceptance_criteria>
    - The soak test runs at least 500 write+GC+read cycles without a crash, a null-where-valid-expected read, or a value mismatch.
    - The test explicitly forces GC (not just relies on incidental collection timing) via `global.gc()` with the runner configured for `--expose-gc`.
    - Every `readChannelView` call in this test returns a buffer of length 400 (not 396, not 392, not 320).
  </acceptance_criteria>
  <done>The command-slot binding is proven GC-safe under forced collection pressure — LIVE-03 SC1's "survives a GC-pressure soak test without dangling the native pointer" clause is automated, not just asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer in-world UAT — REAL cross-build target-correct drag-to-move, independent transform/scale guard fail-closed, coalesced Revert ALL with discarded-change disclosure (incl. while blocked), clean detach (incl. null-player), advertised-Scale structural known-gap confirmation</name>
  <files>packages/renderer/src/panels/viewport/TransformGizmo.tsx, packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx, packages/live-inject/agent/agent_main.cpp</files>
  <action>
    Pause for human verification. Claude has built and automated-tested the full LIVE-03 write loop
    (05-01 through 05-11, including this round's cross-build resolver, W2 scale-availability split, and
    coalesced revert model) plus this plan's GC-pressure soak test; the maintainer manually confirms
    the end-to-end in-world behavior per how-to-verify before LIVE-03 is considered done. Do NOT
    auto-approve — this is a blocking gate, since no automated substitute exists for confirming a real
    object moves in a real running client. Per 05-CONTEXT decision #2: advertised-build Scale is a
    DOCUMENTED KNOWN GAP, not a phase-completion blocker — the UAT PASSES as long as (a) Move/Rotate
    work on BOTH targets with REAL cross-build target resolution (not just an honest player fallback on
    one build), (b) Scale works on the LEGACY target, and (c) Scale on the ADVERTISED target cleanly,
    honestly shows the STRUCTURAL "Scale unavailable on this build" state (ROUND 2 — no crash, no
    false tamper banner, no silent no-op) when the upstream `engine_advertise.cpp` row has not yet been
    added. Record the advertised Scale outcome (working, if the maintainer added the upstream row; or
    the documented structural gap, if not) as a note either way — never as a failure of steps (a)/(b).
  </action>
  <what-built>
    The complete LIVE-03 WYSIWYG write loop: viewport gizmo (Move/Rotate/Scale/Universal) writing
    through a zero-allocation, 60fps command channel into the injected agent, which resolves a REAL
    focus object on EITHER build (legacy: the lookAt-target chain; advertised: the ROUND 2 two-step
    `cuiHud::g_instance` -> `cuiHud::getTarget` resolver — 05-CONTEXT decision #1b, the stronger
    cross-build path, catalog rows CONFIRMED against real engine_advertise.cpp source by the 2026-07-09
    de-anchoring crew), read-verify-guards every write INDEPENDENTLY per channel (transform, scale —
    with scale's build-unavailability honestly split from a genuine tamper via the ROUND 2
    scaleUnavailableOnBuild bit, and the scale-guard comparand itself sourced from a genuine live
    `m_scale` member-offset read, gated per build — 2026-07-09 revision) against the
    attach-time/last-successful-write baseline before calling the client's own setTransform_o2w/
    setScale setters — plus the full HUD (client card with real guard-blocked address/expected-bytes,
    cross-build target identity, and a reverted banner that discloses any discarded external change),
    a ONE-COMMAND coalesced Revert ALL that only rebaselines when genuinely needed, and the D-04
    agent-lifecycle fixes (clean stop-signal honored even before a player exists, detach control,
    legacy 64-bit networkId intentionally left UNAVAILABLE rather than wrong per REVIEWS.md Fix E).
  </what-built>
  <how-to-verify>
    1. Launch or attach to a running **swg-client-v2** (advertised) client via the existing
       attach/launch UI (LiveInspectorPanel). Confirm the Live-Sync client card shows the live/injected
       header and BOTH read-verify guard rows (`transform: client bytes = snapshot`; `scale: not yet
       written this session` until a scale write happens, or `unavailable on this build` if the
       upstream setScale row is absent — ROUND 2 precedence).
    1b. **REAL cross-build target identity (05-CONTEXT decision #1b, ROUND 2 — this step is REWRITTEN
       from round 1, which only tested the legacy chain).** On the ADVERTISED client: select/target an
       object in-game (via the normal in-game targeting/selection UI) and confirm the client card's
       `target` row AND the gizmo's inline label update to name that object's template — this is a
       REAL, two-step mechanism this round (`cuiHud::g_instance` -> `cuiHud::getTarget`), not an honest
       player-fallback placeholder; drag the gizmo and confirm the SELECTED object moves, not the
       player. Then deselect (or target nothing) and confirm the label falls back to `none — moving
       player avatar` and the gizmo now moves the player. On the LEGACY (SWGEmu) client: repeat the
       same test via legacy's in-game targeting — confirm the label names the actual targeted object
       (unchanged mechanism from round 1; this is a genuine, wired resolver — Game::
       getPlayerLookAtTargetObjectNetworkId -> Object::getObjectById -> Network::getCachedObjectById ->
       cachedNetworkIdGetObject — re-confirmed against real Utinni source by the 2026-07-09 de-anchoring
       crew, not a placeholder) and dragging moves THAT object. If either build's resolver RVA/catalog
       row is genuinely unresolved (a real build-capability gap, not "nothing selected"), confirm the
       label instead reads `unavailable on this build — moving player avatar` and note which
       build/row in the approval response.
    1c. **DELIBERATE viewport<->live-object MISMATCH check (REVIEWS.md round-3 maintainer decision
       #3(3), REQUIRED, not optional).** Load a DIFFERENT asset in the toolkit's viewport than
       whatever is currently targeted/selected in-game (e.g. viewport shows a Stormtrooper mesh,
       in-game target/selection is an unrelated womprat or the player). Confirm the client card
       renders the inline `⚠ viewing <loaded>, moving <target>` mismatch warning (05-11 ROUND 4) —
       this is the HONEST disclosure that dragging the gizmo does NOT move the mesh shown in the
       viewport, only whatever is live-targeted/selected. This step deliberately does NOT arrange for
       the two to match (that is step 2's job) — it exists specifically so the viewport<->live-object
       binding gap cannot be signed off invisibly. In the approval response, explicitly ACKNOWLEDGE
       that this warning fired correctly AND that the viewport<->live-object binding gap remains open
       (deferred to Phase 7's `.ws` placement editing) — this is a documented, non-blocking known
       limitation, not a toolkit defect, and is not a pass/fail bar on closing the gap itself.
    2. With an object loaded in the viewport (the live-targeted-or-player object per step 1b need NOT
       match the viewport asset for this step — step 1c already covered the deliberate mismatch case;
       this step is free to use either a matching or non-matching pairing), drag the Move gizmo. Confirm the object visibly moves in the running client window in real time, the
       bottom readout bar's Pos numboxes update live, and a new row appears in the client card's write
       log. **Watch item (REVIEWS.md Opus MEDIUM, EXPANDED ROUND 2, coverage corrected 2026-07-09 —
       off-thread setter invocation is new/unproven, and BOTH builds' cross-build resolver chains
       amplify it, not just the advertised one):** sustain dragging for at least 30-60 seconds
       continuously and confirm no crash/hitch — this exercises the agent calling engine setters from
       its own poll thread rather than the game's main loop, AND the additional per-frame resolver call
       widens the same class of race on WHICHEVER build you are testing: on ADVERTISED, the two-step
       `cuiHudGetInstance()`/`cuiHudGetTarget()` calls; on LEGACY, the `getPlayerCreatureObject()`/
       `+1432`/`cachedNetworkIdGetObject()` chain — BOTH resolve a raw `Object*` that the sim thread may
       free or relocate between resolution and the setter call, so run this sustained-drag watch item
       against BOTH builds in step 4, not just here. Architecturally sound per D-01, and now bounded by SEH
       (05-03 ROUND 4 — REVIEWS.md round-3 maintainer decision #4): if a fault IS observed (rare, not
       expected to reproduce reliably in a single UAT pass), confirm it degrades to a single visible
       hitch/skipped-frame rather than a client crash — the intermediate `hudInstance` pointer window
       (the advertised two-step resolver's `cuiHudGetInstance()` -> `cuiHudGetTarget(hudInstance)`
       call pair) is the specific value under watch on the advertised build. This mitigation bounds
       the blast radius, it does not claim the underlying race is eliminated — still genuinely
       unproven in-world before this UAT beyond the SEH bound itself. If practical, repeat this step
       against BOTH a world-cell
       (outdoor) object AND an interior/building-interior object — the guard's read-back-refresh fix
       (REVIEWS.md Fix D) specifically targets the interior case (STATIC only — a dynamically-moving
       parent, e.g. a mount/vehicle, is explicitly NOT covered and may false-fail the guard on write
       N+1; note if you observe this, it is documented expected behavior, not a defect).
    3. Switch to Rotate (E) and Scale (R) modes and repeat — confirm both work on the LEGACY SWGEmu
       target (step 4). On the ADVERTISED target, attempt a Scale drag: if the maintainer has added
       the `object::setScale` row to `engine_advertise.cpp` and rebuilt (per 05-03's `user_setup`),
       confirm Scale works identically to Move/Rotate; if NOT, confirm the gizmo/readout-bar/client-card
       ALL show the STRUCTURAL "Scale unavailable on this build" state (ROUND 2 — distinct styling from
       a guard-blocked tamper banner, NO crash, NO false "the game or another tool moved it" copy), and
       this is recorded as the documented known gap (05-CONTEXT decision #2), not a toolkit defect.
    4. Repeat steps 1-3 against a running **legacy SWGEmu** client (build 0.0.119.798 or the
       maintainer's current build) — confirm Move/Rotate/Scale all work via the legacy RVA path (this
       is the hard bar for Scale; it has no upstream dependency on this target), AND repeat step 2's
       sustained-drag off-thread-setter watch item here specifically for the LEGACY resolver chain
       (`getPlayerCreatureObject`/`+1432`/`cachedNetworkIdGetObject`), per the 2026-07-09 coverage fix —
       do not skip this sub-check just because it already passed on the advertised build in step 2.
    5. Force a guard-blocked scenario on the TRANSFORM channel (e.g. have another tool or the game
       itself move the object between writes, or manually poke the object's position via an external
       means) and confirm: the card border turns danger, the `transform` guard row shows the refused
       variant (the `scale` row is UNAFFECTED — still showing its own independent state), and the
       banner shows the EXACT locked copy with a REAL memory address (not a placeholder) and real
       expected/got byte values — confirm there is NO way to force the write through. If practical,
       repeat for the SCALE channel independently (force an external scale change on a build where
       Scale is genuinely working, not build-unavailable) and confirm the `scale` row alone flips,
       `transform` stays green. On whichever build has a confirmed live `m_scale` offset (legacy always;
       advertised only if a session has supplied one), this SCALE forcing test proves the guard is
       reading REAL live bytes, not comparing the toolkit's own last-written value to itself
       (2026-07-09 revision) — note in the approval response which build this was exercised on.
    6. Click `Revert ALL to snapshot` WHILE the guard is still in the blocked state from step 5 and
       confirm: the button is clickable (not disabled), the object snaps back to its attach-time pose
       AND scale in the running client via a SINGLE observable write (ROUND 2 — the rebaseline and the
       revert-to-snapshot write now happen as ONE coalesced command, not two separate commands with a
       visible delay/race between them), the banner switches to the reverted-state copy AND additionally
       names the discarded external change (real address + expected/found bytes — ROUND 2 decision #3:
       confirm this disclosure sentence appears, not just the base "Reverted..." sentence), and the
       write log's prior entries render struck-through — this specifically proves REVIEWS.md's Opus
       MEDIUM fix is closed (no two-command race) AND that the revert is no longer a SILENT overwrite.
    7. Click the new `Detach` control in LiveInspectorPanel and confirm the session cleanly returns to
       idle/file-patch mode with no lingering agent thread (repeat attach→detach 3+ times and confirm
       no growing resource usage / no stale channel errors on re-attach). This exercises detachUI's
       bounded STOP retry-loop (05-07 ROUND 4, REVIEWS.md round-3 maintainer decision #5) — the
       detach should complete promptly on every repetition, not just eventually via the OS-reclaim
       fallback.
    7b. **Stop-signal starvation regression (REVIEWS.md Fix F):** attach to a client BEFORE a
       character/world has fully loaded (or force this by attaching immediately after launch, before
       entering the world), then immediately click `Detach`. Confirm the detach completes promptly
       (not hung/stuck) — this is the exact scenario the original stop-signal-after-player-null-check
       ordering bug would have silently failed (the poll loop would starve on `Sleep(100); continue;`
       and never observe STOP_REQUESTED until a player eventually loaded).
    8. **Despawn-then-retarget pointer-ABA watch item (ROUND 6, REVIEWS.md round-5 maintainer decision
       "option (a) structural" — corroborates 05-07/05-03's cross-check-recreates fix against a real
       client).** Target object A, edit it (drag it so a write-log entry exists and a COW snapshot is
       captured), then force A to despawn (e.g. let a mob die, walk out of cell range, or any other
       in-game action that removes A from the world). Target a FRESHLY-SPAWNED, DIFFERENT object
       (ideally of a different template than A, to exercise the ordinary case the automated fix
       closes) and confirm: the client card's `target`/COW-snapshot/write-log rows all reflect the
       NEW object — a fresh capture, empty write log, NOT A's stale pose or write-log history bleeding
       through. This exercises the identityCache (05-07)/`s_expectedCapturedAgainst` (05-03)
       cross-check structurally proven by their own unit/grep tests; this step is the live-client
       corroboration, not a new pass/fail bar beyond what those tests already establish. Note in the
       approval response whether the new object's history was clean. (The narrower same-template +
       legacy-networkId≡0 + realloc-at-the-exact-same-address residue this fix does NOT close is
       accept-watched per REVIEWS.md round-5 — do not attempt to engineer that exact address
       coincidence deliberately; it is not a pass/fail bar on this step.)
  </how-to-verify>
  <resume-signal>Reply "approved" once steps 1-2, 1b (real cross-build targeting on BOTH builds), 1c
  (deliberate mismatch check, with explicit acknowledgement the binding gap remains open), 3
  (legacy Scale)/4 (including the LEGACY sustained-drag watch item), 5-7, 7b, and 8 (despawn-then-retarget
  watch item, ROUND 6) all pass on both targets, noting the advertised-Scale outcome from step 3 as
  either "working" or "documented structural known gap (Scale unavailable on this build, no crash)" —
  either is an approval per decision #2. Otherwise describe exactly which step/target failed and
  whether it is a toolkit defect vs. the documented upstream engine_advertise.cpp dependency for
  advertised-Scale (or an unresolved advertised getSelectedObject/getObjectById catalog row — also a
  documented, non-blocking, build-capability gap if genuinely absent upstream).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human UAT operator ↔ a real, running game client | This is the final gate before LIVE-03 is considered done; no automated substitute exists for the actual in-world visual confirmation, including the off-thread-setter-invocation (amplified by BOTH builds' cross-build resolver chains — coverage corrected 2026-07-09) and stop-signal-starvation classes of bug that only manifest against a real client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | DoS (memory) | Long-running soak / repeated attach-detach | mitigate | Task 1's automated soak covers GC pressure; the UAT's step 7/7b covers the repeated-attach-detach and null-player-detach resource-leak classes the automated soak does not exercise (real OS thread/handle lifecycle). |
| T-05-32 | DoS (crash) | Off-thread setter invocation under sustained load, amplified by BOTH cross-build resolver chains | accept (watched) + mitigate | Architecturally sound per D-01 (the client's own setter is called, which internally handles its own notification/dirty-flag bookkeeping) but genuinely unproven against a real client until this UAT's sustained-drag watch item (steps 2 and 4) — the advertised two-step `cuiHudGetInstance()`/`cuiHudGetTarget()` calls AND the legacy `getPlayerCreatureObject()`/`+1432`/`cachedNetworkIdGetObject()` chain each add a per-frame resolution step to the same pre-existing race window, accepted as the correct design with an explicit, EXPANDED verification step covering BOTH builds (2026-07-09: corrects a prior framing that named only the advertised path — the legacy resolver was always equally exposed, since it was already a real, wired mechanism per 05-03's corrected framing), not silently assumed safe on either. ROUND 4 (REVIEWS.md round-3 maintainer decision #4): 05-03 now wraps the resolve->read->apply span in SEH, bounding a caught fault to one skipped frame + a transient liveness bit rather than a client crash — this UAT's watch item (steps 2/4) is the first real-client confirmation of that bound, naming the `hudInstance` pointer window specifically. |
| T-05-48 | Tampering | Same-template-realloc pointer-ABA residue (05-07's T-05-46 / 05-03's T-05-50) | accept (watched) | This UAT's step 8 corroborates the ORDINARY cross-template despawn-then-retarget case against a real client (structurally closed by 05-07/05-03's ROUND 6 cross-checks); the NARROWER same-template + legacy-networkId≡0 + realloc-at-the-exact-same-address residue is explicitly NOT engineered or gated on in this UAT — it needs a generation counter or despawn signal out of scope this round, and is accepted per the maintainer's explicit "residue to accept-watch, do NOT try to fully close" instruction (REVIEWS.md round-5). |
</threat_model>

<verification>
`pnpm --filter @swg/live-inject test -- soak` green, followed by the human-verify checkpoint above.
</verification>

<success_criteria>
- The GC-pressure soak test passes against the 400-byte layout (ROUND 5).
- The maintainer approves the in-world UAT: Move/Rotate on both targets with REAL cross-build target resolution, Scale on legacy (hard bar), advertised Scale's outcome recorded as working-or-documented-structural-gap (decision #2), independent transform/scale guard fail-closed with real address/bytes, a coalesced Revert ALL that works even while blocked and discloses any discarded change PER CHANNEL (ROUND 4), detach is clean via the bounded STOP retry-loop including the null-player-starvation regression case, and the sustained-drag off-thread-setter watch item was exercised against BOTH builds' resolver chains AND confirms the ROUND 4 SEH bound (2026-07-09 coverage fix + ROUND 4 mitigation).
- The maintainer deliberately exercises the viewport<->live-object mismatch case (step 1c), confirms the honest warning fires, and explicitly acknowledges the binding gap remains open/deferred to Phase 7 — LIVE-03 is approved as "the guarded, cross-build, honestly-labeled write loop works live," NOT as "the viewport shows what moves" (REVIEWS.md round-3 maintainer decision #3).
- The maintainer exercises the despawn-then-retarget watch item (step 8) and confirms no stale pose/history from a despawned object bleeds onto a freshly-spawned, differently-targeted object — corroborating 05-07/05-03's ROUND 6 pointer-ABA cross-check against a real client, with the narrower same-template-realloc residue explicitly accept-watched, not gated on (ROUND 6, REVIEWS.md round-5 maintainer decision "option (a) structural").
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-12-SUMMARY.md` when done
</output>
