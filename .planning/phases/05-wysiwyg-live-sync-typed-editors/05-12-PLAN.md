---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 12
type: execute
wave: 6
depends_on: ["05-11"]
files_modified:
  - packages/live-inject/test/soak.test.ts
autonomous: false
requirements: [LIVE-03]

must_haves:
  truths:
    - "A GC-pressure soak test proves the command-slot's native ArrayBuffer/GC-guard survives repeated forced collection without dangling the native pointer (LIVE-03 SC1)."
    - "A human confirms live in-world: dragging the gizmo moves the object in a running client with zero restart, on BOTH the advertised (swg-client-v2) and legacy (SWGEmu) targets, INCLUDING Scale (D-09)."
    - "A human confirms the fail-closed guard: forcing a byte mismatch refuses the write with the real address + expected/got bytes shown, and Revert ALL restores the attach-time transform."
  artifacts:
    - path: "packages/live-inject/test/soak.test.ts"
      provides: "Automated GC-pressure soak assertion (repeated global.gc()-forced collection cycles while the command-slot channel remains open and writable)"
      contains: "soak"
---

<objective>
Close LIVE-03 with the two verifications that cannot be automated away: a GC-pressure soak test
proving the command-slot binding never dangles under memory pressure, and a maintainer-driven in-world
UAT proving the drag-to-move loop actually works against both real, running SWG clients — including
Scale, which is fully in scope per the corrected D-09.

Purpose: per the standing quality gate, "checkpoint + implementation in same plan" is a split signal —
this plan is deliberately implementation-free (one small automated soak test) plus the blocking
human-verify checkpoint the prior 11 plans built toward.

Output: a passing GC-pressure soak test, and an approved manual UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-VALIDATION.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-11-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GC-pressure soak test</name>
  <files>packages/live-inject/test/soak.test.ts</files>
  <read_first>
    - packages/live-inject/src/channel_binding.cpp (05-04 — the Napi::Reference GC-guard pattern this test exercises: abRef refcount=1, Reset() in cleanupChannel, finalizer-owns-view-lifetime)
    - 05-RESEARCH.md's Security Domain "Dangling native pointer on GC of the command-slot ArrayBuffer under memory pressure" threat entry and its prescribed mitigation
    - 05-VALIDATION.md Wave 0 Requirements ("GC-pressure soak test harness: native pointer survives GC without dangling")
  </read_first>
  <action>
    Create soak.test.ts: open a channel via `addon.openChannel(name)`, then run N (e.g. 500-1000)
    iterations of: call `addon.writeCommand(name, transform12, scale3, 0)`, force garbage collection
    (`global.gc()` — requires running vitest with `--expose-gc`; add that flag to this package's test
    script/config if not already present), call `addon.readChannelView(name)` and assert it still
    returns a valid, non-null 392-byte ArrayBuffer with the just-written values readable back out
    correctly (seqlock even, transform matches). This proves the `Napi::Reference` refcount=1 guard
    keeps the ArrayBuffer alive across GC pressure while C++ still holds the view pointer, and that
    the JS-side re-acquisition of the buffer via `readChannelView` (a fresh `abRef.Value()` call each
    time, per the existing Pitfall-5 discipline this codebase already follows) never returns a
    dangling reference. Finish with `addon.closeChannel(name)` and assert a subsequent
    `readChannelView(name)` returns null (clean teardown, no dangling handle).
  </action>
  <verify>
    <automated>pnpm --filter @swg/live-inject test -- soak</automated>
  </verify>
  <acceptance_criteria>
    - The soak test runs at least 500 write+GC+read cycles without a crash, a null-where-valid-expected read, or a value mismatch.
    - The test explicitly forces GC (not just relies on incidental collection timing) via `global.gc()` with the runner configured for `--expose-gc`.
  </acceptance_criteria>
  <done>The command-slot binding is proven GC-safe under forced collection pressure — LIVE-03 SC1's "survives a GC-pressure soak test without dangling the native pointer" clause is automated, not just asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer in-world UAT — drag-to-move on both targets, guard fail-closed, Revert ALL, clean detach</name>
  <files>packages/renderer/src/panels/viewport/TransformGizmo.tsx, packages/renderer/src/panels/viewport/LiveSyncClientCard.tsx, packages/live-inject/agent/agent_main.cpp</files>
  <action>
    Pause for human verification. Claude has built and automated-tested the full LIVE-03 write loop
    (05-01 through 05-11) plus this plan's GC-pressure soak test; the maintainer manually confirms the
    end-to-end in-world behavior per how-to-verify before LIVE-03 is considered done. Do NOT
    auto-approve — this is a blocking gate, since no automated substitute exists for confirming a real
    object moves in a real running client. A partial pass (e.g. advertised-Scale not yet working
    because the upstream `engine_advertise.cpp` row from 05-03's `user_setup` has not been added/
    rebuilt yet) is a valid, documented outcome — record it as a known follow-up, not a plan failure,
    as long as legacy-Scale and both targets' Move/Rotate all pass.
  </action>
  <what-built>
    The complete LIVE-03 WYSIWYG write loop: viewport gizmo (Move/Rotate/Scale/Universal) writing
    through a zero-allocation, 60fps command channel into the injected agent, which read-verify-guards
    every write against the attach-time snapshot before calling the client's own
    setTransform_o2w/setScale setters — plus the full HUD (client card, readout bar, StatusBar mirror)
    and the D-04 agent-lifecycle fixes (clean stop-signal, detach control, legacy 64-bit networkId).
  </what-built>
  <how-to-verify>
    1. Launch or attach to a running **swg-client-v2** (advertised) client via the existing
       attach/launch UI (LiveInspectorPanel). Confirm the Live-Sync client card shows `● Live ·
       injected` and the read-verify guard row shows `✓ client bytes = snapshot`.
    2. With an object loaded in the viewport (matching the live player), drag the Move gizmo. Confirm
       the object visibly moves in the running client window in real time, the bottom readout bar's
       Pos numboxes update live, and a new row appears in the client card's write log.
    3. Switch to Rotate (E) and Scale (R) modes and repeat — confirm both work, including Scale (this
       requires the maintainer to have added the `object::setScale` row to
       `engine_advertise.cpp` and rebuilt the client, per this phase's `05-03-PLAN.md` `user_setup`
       note — if that upstream change has not been made yet, Scale on the ADVERTISED target is
       expected to silently no-op at the resolver level per the existing graceful-degrade contract;
       note this explicitly in the UAT result rather than treating it as a toolkit defect).
    4. Repeat steps 1-3 against a running **legacy SWGEmu** client (build 0.0.119.798 or the
       maintainer's current build) — confirm Move/Rotate/Scale all work via the legacy RVA path (Scale
       has no upstream dependency on this target).
    5. Force a guard-blocked scenario (e.g. have another tool or the game itself move the object
       between writes, or manually poke the object's position via an external means) and confirm: the
       card border turns danger, the guard row shows `✗ client bytes ≠ snapshot`, and the banner shows
       the EXACT locked copy with a real memory address and real expected/got byte values — confirm
       there is NO way to force the write through.
    6. Click `⟲ Revert ALL to snapshot` and confirm the object snaps back to its attach-time pose in
       the running client, the banner switches to the reverted-state copy, and the write log's prior
       entries render struck-through.
    7. Click the new `Detach` control in LiveInspectorPanel and confirm the session cleanly returns to
       idle/file-patch mode with no lingering agent thread (repeat attach→detach 3+ times and confirm
       no growing resource usage / no stale channel errors on re-attach).
  </how-to-verify>
  <resume-signal>Reply "approved" once all 7 steps pass on both targets, or describe exactly which step/target failed (and whether it was a toolkit defect vs. the documented upstream engine_advertise.cpp dependency for advertised-Scale).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human UAT operator ↔ a real, running game client | This is the final gate before LIVE-03 is considered done; no automated substitute exists for the actual in-world visual confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | DoS (memory) | Long-running soak / repeated attach-detach | mitigate | Task 1's automated soak covers GC pressure; the UAT's step 7 covers the repeated-attach-detach resource-leak class the automated soak does not exercise (real OS thread/handle lifecycle). |
</threat_model>

<verification>
`pnpm --filter @swg/live-inject test -- soak` green, followed by the human-verify checkpoint above.
</verification>

<success_criteria>
- The GC-pressure soak test passes.
- The maintainer approves the in-world UAT on both targets, with Scale explicitly exercised.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-12-SUMMARY.md` when done
</output>
