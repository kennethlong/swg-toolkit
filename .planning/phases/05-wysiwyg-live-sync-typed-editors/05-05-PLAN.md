---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 05
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - packages/native-core/modules/core/formats/StringTable.h
  - packages/native-core/modules/core/formats/StringTable.cpp
  - packages/native-core/src/stf_binding.cpp
  - packages/native-core/src/addon.cpp
  - packages/harness/test/stf-roundtrip.test.ts
  - packages/harness/scripts/extract-stf-fixtures.cjs
  - .gitignore
  - .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
autonomous: true
requirements: [DATA-02]

# REVISION NOTE (plan-check revision, 2026-07-08): Blocker 1 — this plan previously built ONLY a
# committed synthetic fixture (Task 3), omitting REQUIREMENTS.md's standing "real-asset byte-exact
# round-trip" gate. Fixed via new Task 4, following Phase 1's two-layer fixture pattern (D-09/D-10):
# synthetic-committed (Task 3, CI-enforced) PLUS gitignored real-asset (Task 4, local/opt-in — the
# actual "done" gate). Warning 1 — Task 3 also now amends 05-UI-SPEC.md's `.stf` layout/magic/
# sourceCrc copy per D-10/D-11 (the doc itself was already corrected with an Errata section in this
# revision; Task 3 verifies/cites it, since Task 3 is the one proving the two-section layout this
# correction depends on).

must_haves:
  truths:
    - "The .stf magic is checked as the 4-byte integer 0xABCD (little-endian bytes CD AB 00 00), never as an ASCII 'STF ' tag (D-11 erratum vs the original UI-SPEC assumption)."
    - "A .stf file is modeled and round-tripped as TWO independently-ordered sections — an id-ascending string table and a separate name-ascending key-to-id map — not one flat table (D-11/Pitfall 4)."
    - "sourceCrc is preserved byte-identical through parse->serialize by default; the native serializer never recomputes it from the row's own text (D-10)."
    - "A byte-exact round-trip fixture whose id-order and name-order DIFFER is committed and passes — a fixture where both orders coincidentally match would not catch the two-section ordering bug (Pitfall 4 warning sign)."
    - "A real extracted `.stf` file from an installed client (`D:/SWG Infinity` or `D:/SWGEmu Client/SWGEmu`) round-trips byte-exact via the gitignored `fixtures-real/stf/` local-only lane (Phase 1 two-layer fixture precedent, D-09/D-10) — this is the standing REQUIREMENTS.md 'done' gate for DATA-02, distinct from and in addition to the committed synthetic CI fixture; the test skips cleanly (never fails) when the real asset is absent on a given machine."
  artifacts:
    - path: "packages/native-core/modules/core/formats/StringTable.h"
      provides: "parseStf / serializeStf declarations; StfEntry (id, sourceCrc, text) + StfResult (byId, nameToId) structs"
      contains: "serializeStf"
    - path: "packages/harness/test/stf-roundtrip.test.ts"
      provides: "registerFormat('stf', ...) with a differing-order fixture + a raw-byte (not re-parsed-model) round-trip assertion + a dedicated sourceCrc-preservation test + a real-asset fixture lane (Task 4)"
    - path: "packages/harness/scripts/extract-stf-fixtures.cjs"
      provides: "One-time extraction script (mirrors extract-effect-fixtures.cjs's env-overridable-root + loud-not-found convention) that mounts an installed client's TRE set and copies one real .stf into fixtures-real/stf/"
      contains: "fixtures-real/stf"
  key_links:
    - from: "packages/harness/test/stf-roundtrip.test.ts"
      to: "packages/harness/fixtureRegistry.ts"
      via: "registerFormat('stf', { parse, serialize, fixtures, loaderSource })"
      pattern: "registerFormat\\('stf'"
    - from: "packages/native-core/modules/core/formats/StringTable.cpp"
      to: "packages/native-core/modules/core/tre/Crc.cpp"
      via: "reuse the existing verified CRC32 table/polynomial (0x04C11DB7) — do not port a second CRC implementation"
      pattern: "0x04C11DB7"
---

<objective>
Build the native (C++20, engine-free) `.stf` localized-string-table parser + serializer inside
`native-core`, wire it through an N-API binding, and register it in the CORE-05 fixture registry with
a byte-exact round-trip gate — both the committed-synthetic (CI, proving the two-independently-
ordered-sections layout / D-11 and sourceCrc-preservation / D-10) AND the gitignored real-asset
(local/opt-in, the actual "done" gate per REQUIREMENTS.md) layer — the format foundation for the
DATA-02 strings editor.

Purpose: this session's ground-truth research falsified the approved UI-SPEC's assumed `.stf` layout
(flat `key|crc32|text` table, ASCII "STF " magic) at the byte level. Building against that assumption
would produce a parser that reads real files correctly by luck (or not at all) and definitely fails a
byte-exact round-trip. This plan builds against the VERIFIED layout instead, and REQUIREMENTS.md's
standing gate additionally requires proving it against a REAL extracted asset, not only a synthesized
one — Phase 1 established the two-layer (synthetic-committed + gitignored-real) pattern this plan
follows (D-09/D-10).

Output: `StringTable.h/.cpp`, `stf_binding.cpp`, a committed synthetic fixture with deliberately
differing id/name orders, a gitignored real-asset extraction script/fixture lane, a passing
`stf-roundtrip.test.ts`, and a small `.stf` layout/magic/sourceCrc amendment to `05-UI-SPEC.md`
(D-10/D-11 errata).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedStringTable.cpp
@D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedString.cpp
@D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedStringTableReaderWriter.cpp
@D:/Code/SWG-Toolkit/packages/native-core/modules/core/tre/Crc.cpp
</context>

<interfaces>
Full verified binary layout (05-RESEARCH.md "Code Examples — .stf: full verified binary layout",
citing LocalizedStringTable.cpp:368-405,227-308, LocalizedString.cpp:233-279,
LocalizedStringTableReaderWriter.cpp:107-203):

```
Offset  Size            Field                 Notes
0       4               magic                 long, value 0xABCD (LE bytes CD AB 00 00) -- NOT ascii "STF "
4       1               version               char, value 1 (FILE_VERSION)
5       4               next_unique_id        unsigned long (id_type)
9       4               num_entries           unsigned long (id_type)
        -- STRING SECTION (num_entries times), ASCENDING id order --
        4               id                    unsigned long
        4               sourceCrc             unsigned long -- CRC of the SOURCE-language text, NOT self-hash
        4               buflen                unsigned long -- length in UTF-16 code units (char16_t), NOT bytes
        buflen*2        text                  UTF-16LE (char16_t[], no null terminator on disk)
        -- NAME-MAP SECTION (num_entries times), ASCENDING name order --
        4               id                    unsigned long -- which string this name refers to
        4               buflen                unsigned long -- length in ASCII bytes
        buflen          name                  ASCII bytes (no null terminator on disk)
```

Analog header structure to mirror (PATTERNS.md "StringTable.h" — Palette.h is the closest existing
analog: PARSER-NATIVE raw-bytes format, NOT an IFF tree, with a `serializeX` sibling for round-trip):
```cpp
#include "Mesh.h"  // FormatParseError
namespace swg_core { namespace formats {
struct StfEntry { uint32_t id; uint32_t sourceCrc; std::u16string text; };
struct StfResult { uint32_t nextUniqueId; std::vector<StfEntry> byId; std::vector<std::pair<std::string,uint32_t>> nameToId; };
StfResult parseStf(const uint8_t* data, uint32_t size);
std::vector<uint8_t> serializeStf(const StfResult& table);  // MUST preserve sourceCrc verbatim (D-10)
}}
```

CRC32 to reuse (do not re-port): `packages/native-core/modules/core/tre/Crc.cpp` — forward polynomial
`0x04C11DB7`, already verified against `swg-client-v2 Crc.cpp` and used by the existing TRE codec.
`LocalizedString.cpp`'s `generateCrc()` uses this same table/algorithm for `sourceCrc`.

Real-asset fixture lane precedent (Task 4 — Phase 1 two-layer pattern, D-09/D-10): see
`packages/harness/fixtures-real/README.md`, `packages/harness/fixtures-real/.gitignore`, root
`.gitignore`'s "Real SWG asset fixtures" section, `packages/harness/scripts/extract-ans-fixtures.cjs`
(original extraction-script shape) and `packages/harness/scripts/extract-effect-fixtures.cjs`
(04.4-05 — the MORE RECENT env-overridable-root + loud-non-zero-not-found-exit convention; prefer
this one) and `packages/harness/test/mesh-roundtrip.test.ts`'s `loadFixture` helper (lines ~137-151)
+ its EFCT-gated `describe` block (skips cleanly, never fails, when the real file is absent).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Native .stf parser + serializer (StringTable.h/.cpp)</name>
  <files>packages/native-core/modules/core/formats/StringTable.h, packages/native-core/modules/core/formats/StringTable.cpp</files>
  <read_first>
    - D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedStringTable.cpp (lines 72,77,227-308,368-405,496-499 — magic/version constants, load_0001, openLoadFile, both sections' iteration order)
    - D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedString.cpp (lines 20-95,178-329 — per-string id/sourceCrc/buflen/text load; id_type/crc_type sizes from LocalizedString.h:41-42,66-72)
    - D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedStringTableReaderWriter.cpp (lines 107-203 — the write-side serializer oracle, BOTH sections)
    - packages/native-core/modules/core/formats/Palette.h and Palette.cpp (full files — the exact PARSER-NATIVE raw-bytes analog: no IFF tree, parse+serialize pair, header doc-comment convention)
  </read_first>
  <action>
    Create StringTable.h with the header doc-comment structure from Palette.h (port-source citations,
    NOTE about this being a PARSER-NATIVE format with magic 0xABCD, not IFF). Define
    `struct StfEntry { uint32_t id; uint32_t sourceCrc; std::u16string text; }` and
    `struct StfResult { uint32_t nextUniqueId; std::vector<StfEntry> byId; std::vector<std::pair<
    std::string, uint32_t>> nameToId; }` (byId is already in id-ascending order as read from disk;
    nameToId is already in name-ascending order as read from disk — preserve source order at parse
    time rather than re-sorting, so a round-trip with zero edits is trivially identity). Declare
    `parseStf(const uint8_t* data, uint32_t size)` and `serializeStf(const StfResult&)`. Implement
    parseStf in StringTable.cpp per the exact layout in Interfaces above: read magic as a raw 4-byte
    little-endian uint32 and throw FormatParseError if it is not exactly 0xABCD (bounds-check `size >=
    9` before even reading the header); read version (1 byte, expect 1); read next_unique_id (4B);
    read num_entries (4B); loop num_entries times reading the string section (id 4B, sourceCrc 4B,
    buflen 4B — bounds-check `buflen*2` against remaining buffer size BEFORE allocating/decoding the
    UTF-16LE text, per the standing security posture); loop num_entries times reading the name-map
    section (id 4B, buflen 4B — bounds-check similarly, then read buflen ASCII bytes). Implement
    serializeStf as the exact inverse: write magic 0xABCD, version 1, next_unique_id, num_entries,
    then re-emit the string section in `byId`'s CURRENT order (which must remain id-ascending — the
    caller/editor is responsible for keeping byId sorted after edits; this function does not
    re-sort, mirroring the client's own map-ordered-container behavior) writing id/sourceCrc(VERBATIM
    — never recomputed from text)/buflen(computed from the CURRENT text length in UTF-16 code
    units)/text, then re-emit the name-map section in `nameToId`'s current order writing
    id/buflen(from name.length())/name bytes. Add a small exported helper
    `uint32_t recomputeSourceCrcFromText(const std::u16string& text)` (D-10's explicit opt-in
    "mark re-synced to source" action — NOT called by serializeStf itself, only by a future explicit
    UI action) that ports the SAME CRC32 table/polynomial already implemented in
    `packages/native-core/modules/core/tre/Crc.cpp` (0x04C11DB7) — do not write a second CRC
    implementation; include or reference that file's table directly.
  </action>
  <verify>
    <automated>cmake --build packages/native-core/build --config Release --target swg_native_core 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - parseStf throws FormatParseError (not a crash, not a silent wrong-parse) when the first 4 bytes are not 0xABCD little-endian.
    - parseStf throws FormatParseError when a string-section buflen*2 or name-section buflen would read past the end of the buffer.
    - serializeStf never calls any CRC-computation function on the default path — grep confirms `recomputeSourceCrcFromText` has exactly one call site and it is NOT inside `serializeStf`.
  </acceptance_criteria>
  <done>StringTable.h/.cpp exist, compile, correctly model the two-independently-ordered-sections layout, and never silently corrupt sourceCrc.</done>
</task>

<task type="auto">
  <name>Task 2: N-API binding + addon registration</name>
  <files>packages/native-core/src/stf_binding.cpp, packages/native-core/src/addon.cpp</files>
  <read_first>
    - packages/native-core/src/mesh_binding.cpp (lines 392-442 ParsePalette — the exact raw-bytes-binding + roundTripBytes-tail pattern to mirror, since .stf is PARSER-NATIVE like Palette, not IFF-tree-based like Effect)
    - packages/native-core/src/addon.cpp (full file — registration convention; append, do not restructure)
  </read_first>
  <action>
    Create stf_binding.cpp with `Napi::Value ParseStf(const Napi::CallbackInfo& info)` mirroring
    ParsePalette's shape exactly (raw bytes in, no IFF tree needed): validate `info[0]` is
    bytes, call `extractBytes` (reuse the existing helper from the mesh-binding pattern), wrap
    `swg_core::formats::parseStf` in the standard two-tier try/catch. Build the return object:
    `nextUniqueId`, `entries` (array of `{id, sourceCrc, text}` in byId order — decode `std::u16string`
    to a JS string via UTF-16LE, which is the native platform encoding so this should be a direct
    copy, not a re-encode), `nameMap` (array of `{id, name}` in nameToId order). Add
    `roundTripBytes` to the result via `swg_core::formats::serializeStf(stfResult)` (mirror
    ParsePalette's roundTripBytes tail, mesh_binding.cpp:433-439) so the same object returned by
    ParseStf can be fed directly into the CORE-05 gate without a separate serialize call — but ALSO
    add a standalone `Napi::Value SerializeStf(const Napi::CallbackInfo& info)` export that accepts a
    JS-side EDITED representation (entries/nameMap in the same shape) and returns fresh bytes, since
    the DATA-02 editor (05-09) needs to serialize an edited buffer, not just prove round-trip
    identity on an unedited one. Register both `parseStf` and `serializeStf` in addon.cpp under a new
    "Phase 5 Plan 05-05 .stf localized string table (implemented in stf_binding.cpp)" comment block,
    following the exact forward-declaration + exports.Set convention already used for every other
    format.
  </action>
  <verify>
    <automated>cmake --build packages/native-core/build --config Release --target swg_native_core 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `require('../native-core/index.js').parseStf` and `.serializeStf` are both callable functions after build.
    - `parseStf(bytes).roundTripBytes` is an ArrayBuffer (binary-stays-binary rule).
    - Calling parseStf with a truncated buflen field throws a JS Error (not a crash).
  </acceptance_criteria>
  <done>parseStf/serializeStf are exported from the native-core addon following the exact PARSER-NATIVE binding convention used by parsePalette/parseDds.</done>
</task>

<task type="auto">
  <name>Task 3: CORE-05 synthetic round-trip fixture with differing orders + .stf layout doc amendment</name>
  <files>packages/harness/test/stf-roundtrip.test.ts, .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md</files>
  <read_first>
    - packages/harness/test/mesh-roundtrip.test.ts (lines 391-435 the RIFF PAL synthetic-fixture describe block — the closest existing "hand-built small PARSER-NATIVE fixture, always committed" model to copy)
    - packages/harness/fixtureRegistry.ts (full file — registerFormat/assertSweep contract, CITATION_RE)
    - packages/harness/assertRoundTrip.ts (full file — byte-exact assertion; this format's test MUST use this, not a re-parsed logical-model comparator, per Pitfall 4's explicit warning sign)
    - .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md (the "## Errata" section, items 3-4 — the D-10/D-11 corrections already folded into the doc; this task's job is to confirm/keep them consistent with the real layout, not author new prose)
  </read_first>
  <action>
    Create stf-roundtrip.test.ts. Hand-build ONE small synthetic `.stf` fixture inline as raw bytes
    (D-09 convention — synthesize from a byte recipe, never hand-copy an external golden) with AT
    LEAST 3 entries whose id-ascending order and name-ascending order DIFFER — for example: id=3
    name="zebra", id=1 name="apple", id=2 name="mango" (id order: 1,2,3 — name order: apple, mango,
    zebra — these two orderings visit the entries in different sequences, which is the case that
    would silently break if the implementation collapsed to one shared order). Write the header
    (magic 0xABCD as 4 LE bytes, version=1, next_unique_id, num_entries=3), then the string section
    in id-ascending order (1, 2, 3) with distinct non-zero sourceCrc values per entry (not all
    0xFFFFFFFF — prove sourceCrc is preserved as an arbitrary stored value, not always the null
    default) and short UTF-16LE text per entry, then the name-map section in name-ascending order
    (apple->1, mango->2, zebra->3). Call `nativeCore.parseStf(bytes)` and assert:
    `entries.length===3`, `entries` reflects id-ascending order (1,2,3), `nameMap` reflects
    name-ascending order (apple,mango,zebra), and each entry's `sourceCrc` matches exactly what was
    written (not recomputed). Then `registerFormat('stf', { parse: (b) => nativeCore.parseStf(b),
    serialize: (parsed) => new Uint8Array(nativeCore.serializeStf(parsed)), fixtures: [{ name:
    'synthetic-differing-orders', bytes: fixtureBytes, loaderSource:
    'swg-client-v2 LocalizedStringTableReaderWriter.cpp:107-203' }], loaderSource:
    'swg-client-v2 LocalizedStringTable.cpp:72,77,227-308,368-405' })`. Add a direct
    `assertRoundTrip(entry.parse, entry.serialize, fixtureBytes)` call in this file (not only relying
    on the shared registry-coverage sweep) using RAW BYTE comparison — this is the exact mechanism
    that catches the Pitfall-4 failure mode (a naive "same rows, same text" comparator would pass
    while producing non-identical bytes; assertRoundTrip's byte-for-byte scan does not have this
    blind spot). Add a dedicated test asserting the magic bytes at offset 0 are exactly `0xCD 0xAB
    0x00 0x00` (proving the 4-byte-integer interpretation, not an ASCII check). Add a dedicated
    sourceCrc-preservation test: parse the fixture, then serialize WITHOUT modifying any entry, and
    assert every sourceCrc byte in the output exactly matches the input (this is the D-10 default-save
    contract, distinct from the general round-trip assertion). Finally, confirm 05-UI-SPEC.md's
    "## Errata" items 3-4 (D-11 two-section layout + magic; D-10 sourceCrc preservation) accurately
    reflect this task's verified layout — if any detail drifted while implementing this task, correct
    the Errata entries to match the now-implemented ground truth; otherwise no doc edit is needed
    beyond confirming consistency (grep for "D-10" / "D-11" to locate the sections).
  </action>
  <verify>
    <automated>pnpm --filter @swg/harness test -- stf-roundtrip && pnpm --filter @swg/harness test -- registry-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @swg/harness test -- stf-roundtrip` passes with the byte-exact round-trip assertion green.
    - The magic-bytes test explicitly checks `CD AB 00 00`, not an ASCII string comparison.
    - registry-coverage.test.ts's sweep reports no 'stf' violations.
    - A test exists proving the fixture's id-order and name-order are NOT the same permutation (guards against someone weakening the fixture later).
    - `grep -Ec "D-10|D-11" .planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md` is >= 2 and the cited layout/CRC semantics match this task's implementation.
  </acceptance_criteria>
  <done>.stf is a fully registered CORE-05 format: two-section-aware parse/serialize, byte-exact round-trip proven against an order-differentiating synthetic fixture, sourceCrc preserved verbatim by default — DATA-02's synthetic-fixture format foundation is complete, and the UI-SPEC's layout/CRC errata are confirmed consistent with the implementation.</done>
</task>

<task type="auto">
  <name>Task 4: Real-asset round-trip fixture — the standing DATA-02 "done" gate (local/opt-in)</name>
  <files>packages/harness/scripts/extract-stf-fixtures.cjs, packages/harness/test/stf-roundtrip.test.ts, .gitignore</files>
  <read_first>
    - packages/harness/scripts/extract-ans-fixtures.cjs (full file — the original extraction-script shape: mount TRE set, search, resolve, read, write to fixtures-real/&lt;domain&gt;/)
    - packages/harness/scripts/extract-effect-fixtures.cjs (04.4-05 PLAN Task 1 — the MORE RECENT env-overridable-client-root + loud non-zero-exit not-found convention; prefer mirroring this one)
    - packages/harness/test/mesh-roundtrip.test.ts (lines 137-151 `loadFixture` helper — reads fixtures-real/&lt;relPath&gt;, returns null/SKIPS if absent; grep for its EFCT-gated `describe` block as the closest existing "real-asset CORE-05 gate, local-only, skips cleanly when absent" model to copy)
    - .gitignore (root — the "Real SWG asset fixtures" section; per-extension globs under fixtures-real/&lt;domain&gt;/*.ext — add a new line for this format's domain, do not weaken the existing entries)
    - D:/Code/swg-client-v2/src/external/ours/library/localization/src/shared/LocalizedStringTableReaderWriter.cpp (already cited in Task 1 — the SAME oracle this real-asset test proves against, now on a real client-compiled file instead of a synthesized one)
  </read_first>
  <action>
    Create extract-stf-fixtures.cjs mirroring extract-effect-fixtures.cjs's shape: resolve a client
    root from an env var (`SWG_STF_FIXTURE_CLIENT_ROOT`), falling back in order to
    `D:/SWG Infinity/SWG Infinity/Live` then `D:/SWGEmu Client/SWGEmu` (try both), mount its `.tre`
    files via `nativeCore.mountTreMount`, search for entries ending `.stf` (SWG localized strings
    live under a `string/<locale>/` VFS prefix — inspect a few real search hits first to confirm),
    pick the first (or first two, to also exercise a translated-locale sourceCrc-preservation check
    if time allows) entry that parses successfully via `nativeCore.parseStf` (proving it is a real,
    well-formed `.stf`, magic 0xABCD), write it/them to
    `packages/harness/fixtures-real/stf/<basename>.stf`, and log the entry count. If no client root
    can be mounted or no valid `.stf` entry is found after searching, `console.error` naming the
    searched root(s)/pattern and `process.exit(1)` — never silently do nothing (mirror 04.4-05's fix
    for exactly this failure mode). Extend stf-roundtrip.test.ts: add a
    `loadFixture('stf/<name>.stf')`-style helper (mirror mesh-roundtrip.test.ts's `loadFixture`
    exactly — resolve relative to `fixtures-real/`, return null if absent) and a conditional block
    that, when the real fixture IS present, parses it, serializes it WITHOUT modification, and
    asserts BOTH byte-exact round-trip equality (via the SAME `assertRoundTrip` helper Task 3's
    synthetic test uses — do not hand-roll a second comparator) AND sourceCrc preservation across
    every entry — when ABSENT, the block SKIPS cleanly (a passing test suite, not a failing one, on
    a machine without the client installed). This is the actual standing "done" gate for DATA-02 per
    REQUIREMENTS.md (byte-exact round-trip on a REAL extracted asset), matching Phase 1's committed-
    synthetic-plus-gitignored-real two-layer fixture pattern (`01-04-PLAN.md`'s `fixtures-real/`
    precedent; `04.4-05-PLAN.md`'s real+synthetic split). Add `packages/harness/fixtures-real/stf/*.stf`
    to the root `.gitignore`'s "Real SWG asset fixtures" section, following the exact
    per-extension-per-domain convention already used for mesh/texture/shader/effect — never commit
    the real asset itself. Register the real fixture as an ADDITIONAL entry in the SAME
    `registerFormat('stf', ...)` call from Task 3 (the `fixtures` array can hold multiple entries),
    guarding the push with the same existence check `loadFixture` uses, so registry-coverage's sweep
    picks it up when available without failing on machines where it is absent.
  </action>
  <verify>
    <automated>pnpm --filter @swg/harness test -- stf-roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - Running `node scripts/extract-stf-fixtures.cjs` on a machine with an installed client populates `packages/harness/fixtures-real/stf/<name>.stf`.
    - Running the extraction script with a deliberately-wrong `SWG_STF_FIXTURE_CLIENT_ROOT` exits non-zero with a message naming the searched root(s) and pattern (verifies the not-found path, matching 04.4-05's fix).
    - `pnpm --filter @swg/harness test -- stf-roundtrip` PASSES on a machine WITHOUT the real fixture (the real-asset assertions cleanly SKIP, no false failure); on a machine WITH the fixture extracted, the SAME command additionally EXECUTES and PASSES the real byte-exact round-trip AND sourceCrc-preservation assertions.
    - `git status` never shows the extracted real `.stf` asset as untracked-to-be-added — the new `.gitignore` line covers it.
  </acceptance_criteria>
  <done>DATA-02's standing byte-exact round-trip gate now has BOTH layers per REQUIREMENTS.md: a committed synthetic fixture (Task 3, CI-enforced) and a gitignored real-asset fixture (this task, local/opt-in, the actual "done" proof) — matching Phase 1's established two-layer pattern (D-09/D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Mounted/opened .stf bytes (third-party or hand-crafted) → native parser | Same modding-tool threat model as DTII: a hand-crafted malicious `.stf` with attacker-controlled buflen fields is realistic, not hypothetical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering / DoS | StringTable.cpp parse path | mitigate | Bounds-check every buflen field (string-section UTF-16 length and name-section ASCII length) against remaining buffer size before allocating/decoding, mirroring DataTable.cpp's discipline (05-02) and the existing IFF reader. |
| T-05-13 | Tampering | StringTable.cpp serializeStf | mitigate | `recomputeSourceCrcFromText` is a separate, explicitly-named function with exactly one intended call site (a future opt-in UI action, not the default save path) — a naive auto-CRC-on-save regression is structurally hard to introduce by accident, and the acceptance criteria grep-check this. |
</threat_model>

<verification>
Run `pnpm --filter @swg/harness test -- stf-roundtrip registry-coverage` — both green, no CORE-05
sweep violations for the new 'stf' format. On a machine with an installed client, additionally run
`node packages/harness/scripts/extract-stf-fixtures.cjs` first to populate the real-asset lane.
</verification>

<success_criteria>
- Native StringTable parser/serializer exist, compile, and are exported via N-API.
- A committed synthetic fixture with differing id/name orders round-trips byte-exact.
- A gitignored real-asset fixture (extracted from an installed client) round-trips byte-exact and
  preserves sourceCrc — the standing REQUIREMENTS.md "done" gate — and skips cleanly (never fails)
  when absent.
- sourceCrc is proven preserved verbatim on a no-edit round-trip.
- 'stf' is registered in fixtureRegistry.ts with a valid swg-client-v2 loaderSource citation.
- 05-UI-SPEC.md's D-10/D-11 layout/CRC errata are confirmed consistent with the implementation.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-05-SUMMARY.md` when done
</output>
