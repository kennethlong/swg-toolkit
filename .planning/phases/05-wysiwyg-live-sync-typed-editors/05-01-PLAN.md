---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/live-inject.ts
  - packages/live-inject/agent/channel.h
  - packages/live-inject/agent/channel.cpp
  - packages/live-inject/agent/write.h
  - packages/live-inject/agent/write.cpp
  - packages/live-inject/test/channel-layout.test.ts
  - packages/live-inject/test/write-guard.test.ts
autonomous: true
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): folds in cross-AI review fixes B/C (05-REVIEWS.md).
# (1) Fix C — the locked guard-blocked banner needs a REAL memory address the channel never carried.
#     Added GUARD_ADDR (offset 392, 4 bytes, x86 pointer) — agent-authoritative, host-read-only, same
#     mechanism as GUARD_STATUS (InterlockedExchange, no seqlock needed for one aligned word).
#     TOTAL_SIZE grows 392 -> 396. (The "expected bytes" half of Fix C is a renderer-only fix — see
#     05-07 REVISION NOTE — no channel change needed for it.)
# (2) Fix B — Scale is a first-class write channel with NO guard. checkWriteGuard is split into an
#     independent transform check and an independent scale check (GuardResult now carries
#     transformPassed/scalePassed separately); GUARD_STATUS gains a second bit (bit1 = SCALE_REFUSED,
#     bit0 renamed TRANSFORM_REFUSED, same bit value 0x1 as the old WRITE_REFUSED for compat).
# (3) Opus MEDIUM — a blocked guard also blocks its own revert. Added CMD_FLAG_REBASELINE_GUARD
#     (cmdFlags bit1) so an explicit "Revert ALL" can re-baseline the agent's expected-bytes state to
#     current live bytes without ever accepting an arbitrary forward-write (05-03/05-07 consume this).
# Executor note: 05-RESEARCH.md's Pattern-1 struct sketch (376 bytes, no COMMAND_SCALE/GUARD_ADDR) is
# STALE — this plan's 396-byte layout is authoritative (RESEARCH.md predates the D-09 Scale-in-scope
# correction and this review round's GUARD_ADDR/SCALE_REFUSED additions).

# REVISION NOTE (plan-check revision, 2026-07-08): closes a plan-checker WARNING against T-05-26 in
# this plan's threat register below. T-05-26's wording asserted rebaseline "never accepts values from
# the command payload as the new baseline" — the ROUND 2 coalesce fix (05-03/05-07, see their own
# REVISION NOTEs) makes that literally untrue for the SCALE channel specifically: on a coalesced
# rebaseline, the agent sets `s_expectedScale = cmd.scale` (there is no live scale read-back to
# rebaseline FROM, unlike transform, which rebaselines from a fresh live read). T-05-26's Mitigation
# Plan text below is corrected to state the REAL invariant — "only ever carries the toolkit's own known
# revert target" — matching 05-07's T-05-14 wording exactly. This is a WORDING fix only; no design
# change (05-03/05-07's ROUND 2 coalesce behavior is unchanged and was already correct).
#
# Also see 05-03's own top-of-file REVISION NOTE for the companion BLOCKER fix this same revision round
# closes (genuine live-read scale-guard parity, maintainer decision option (a), 2026-07-08). This
# plan's `write.h`/`write.cpp` `checkWriteGuard` signature required NO change for that fix — it already
# accepted a genuine `liveScale` parameter; the historical gap was entirely in what 05-03's CALLER fed
# that parameter (`s_expectedScale` fed back on itself), not in this module's contract. Task 2's
# `<behavior>` block below gains one clarifying bullet documenting that this comparator's exactness
# guarantee always applied equally to scale — see the bullet marked "Scale-channel parity note".
#
# NOTE (de-anchoring crew fold-in, 2026-07-09): 05-03's caller-side fix (the "Scale-channel parity
# note" below) originally described the caller as binding an `Object::getScale()` RVA/catalog accessor.
# 05-03 was RE-ARCHITECTED that session to a per-build-gated `m_scale` member-offset read instead (the
# crew found `getScale()` is inline with no standalone address) — this module's contract is UNCHANGED
# either way (`checkWriteGuard` already accepted a genuine `liveScale` float[3], regardless of how the
# caller sources it); only the wording below is updated to match.

# REVISION NOTE (--reviews replan ROUND 4, 2026-07-12): folds in 05-REVIEWS.md round-3 maintainer
# decision #5 (STOP sticky bit). A late gizmo-drag writeCommand (cmdSeq N+1) can overtake a prior
# writeStop (cmdSeq N) on the single latest-wins command slot before the agent polls again, so the
# agent may observe only the drag and silently miss STOP. FIX (zero channel-layout growth): GUARD_STATUS
# (offset 388, already agent-authoritative, InterlockedExchange, no seqlock) gains a THIRD bit,
# `STOPPING` (0x4), alongside the existing TRANSFORM_REFUSED (0x1)/SCALE_REFUSED (0x2). The agent (05-03)
# sets this bit once, immediately before honoring STOP_REQUESTED and closing its channel handle. A host
# detach retry-loop (05-07's `detachUI`) resends `writeStop` on a short interval while polling this bit,
# so STOP can never be silently overtaken — it keeps re-asserting until acknowledged. No new offset, no
# TOTAL_SIZE change (396 stays 396) — this is bit semantics only on the field 05-01 already defined.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decision #1 (the load-bearing identity-unification fix — the FULL unification, not the cheaper
# networkId-read alternative). Round 4's host re-key (05-07) compared `(networkId, templateName)`
# while the agent re-keys its own guard baseline on the raw `focus` pointer (05-03) — on legacy,
# `networkId` is always 0 (Fix E, LOCKED), so two same-template objects (e.g. two rocks) collapse to
# the SAME host-side key, and a Revert All can force-write object-1's pose onto object-2 (fails open,
# ordinary workflow — Opus, round-4 MEDIUM). A single cowSnapshot/writeLog slot also means a focus-flip
# A->B->A destroys A's original baseline and undo history (Opus, round-4 MEDIUM, NEW-ISSUE). FIX: the
# channel gains a NEW read-frame field, `FOCUS_TOKEN` — the agent's own truncated `addrOf(focus)`
# (the SAME value it already computes for `guardAddr`, see this file's ROUND-1/Fix-C note above), now
# published into the SEQLOCKED READ FRAME every tick (not just into GUARD_ADDR at apply time, which
# only updates on a guard-checked command and therefore lags the agent's actual current focus). This is
# a deliberate LAYOUT GROWTH: `FOCUS_TOKEN` is inserted at offset 320 (immediately after LIVENESS,
# BEFORE the command-slot region), which shifts EVERY command-region field +4 bytes:
# `COMMAND_SEQ_COUNTER` 320->324, `COMMAND_TRANSFORM` 324->328, `COMMAND_SCALE` 372->376,
# `COMMAND_FLAGS` 384->388, `GUARD_STATUS` 388->392, `GUARD_ADDR` 392->396. `LIVE_READFRAME_BYTES`
# grows 316->320 (the memcpy span channelWrite copies now includes `focusToken`, atomically covered by
# the SAME seqlock as transform/networkId/templateName/liveness — no separate publish call needed
# beyond setting the struct field before the existing `channelWrite` call). `TOTAL_SIZE` grows
# 396->400. This growth is applied consistently across 05-01 (this file, the layout source of truth),
# 05-03 (agent computes+publishes `focusToken` every iteration, not just at apply), 05-04 (host
# `CHANNEL_BYTE_SIZE` bump only — `WriteCommand`'s host-writable span shifts to 324-391, still never
# touching agent-authoritative `GUARD_STATUS`/`GUARD_ADDR`), and 05-07 (host re-keys `cowSnapshot`/
# `writeLog` on the published `focusToken`, NOT `(networkId, templateName)`, and replaces the single
# snapshot/writeLog slot with a small per-identity cache so a focus-flip A->B->A restores rather than
# re-captures). See each file's own ROUND 5 note for its half of this change.

must_haves:
  truths:
    - "The shared file-mapping carries a second, independent seqlock region (offset 324+; offset 320 is the agent-only read-frame FOCUS_TOKEN, NOT part of the host-written command region — ROUND 5) that the host writes and the agent reads, in the SAME named mapping as the existing read frame — no second CreateFileMappingA call exists anywhere in the codebase."
    - "channelWrite's byte count is a named constant (not sizeof(LiveState) arithmetic), so extending the struct for the command slot cannot silently corrupt the read-frame write span."
    - "A pure, Win32-free guard comparator exists that independently reports transform-pass/fail and scale-pass/fail when live bytes differ from the agent's locally-tracked expected transform/scale — mirroring the existing sentinels.h/.cpp testability discipline. Scale is never silently ungated (fixes REVIEWS.md Fix B). This comparator's `liveScale` parameter is always a genuine live-read value once wired by its caller (05-03) — never the agent's own `expectedScale` fed back on itself (2026-07-08 plan-check revision closes that caller-side gap; this module's own contract was already correct)."
    - "GUARD_ADDR carries the real x86 address of the object last write-guard-checked, so the locked banner copy '<addr>: expected <bytes>, read <bytes>' has a real value to interpolate (fixes REVIEWS.md Fix C)."
    - "FOCUS_TOKEN carries the agent's truncated addrOf(focus) pointer, published into the SEQLOCKED READ FRAME every tick (not just into GUARD_ADDR at apply time) — this is the single identity signal both the agent's own guard baseline (05-03) and the host's cowSnapshot/writeLog re-key (05-07) key on, so host and agent can never disagree about which object a baseline belongs to (ROUND 5, REVIEWS.md round-4 maintainer decision #1 — the load-bearing identity-unification fix, closing the fail-open two-same-template-object revert bug and the lossy focus-flip A->B->A bug)."
    - "GUARD_STATUS carries a THIRD, agent-published sticky bit (STOPPING, 0x4) that the agent sets once, right before it honors STOP_REQUESTED and closes its channel handle — giving a host-side detach retry-loop a reliable, independently-observable acknowledgement that survives a later drag command overtaking the latest-wins cmdSeq slot (REVIEWS.md round-3 maintainer decision #5)."
  artifacts:
    - path: "packages/contracts/src/live-inject.ts"
      provides: "LIVE_CHANNEL_LAYOUT extended with FOCUS_TOKEN (ROUND 5, read-frame, per-tick)/COMMAND_SEQ_COUNTER/COMMAND_TRANSFORM/COMMAND_SCALE/COMMAND_FLAGS/GUARD_STATUS/GUARD_ADDR, TOTAL_SIZE=400; LIVE_CMD_FLAGS (STOP_REQUESTED, REBASELINE_GUARD) and LIVE_GUARD_FLAGS (TRANSFORM_REFUSED, SCALE_REFUSED) bit constants"
      contains: "FOCUS_TOKEN"
    - path: "packages/live-inject/agent/channel.h"
      provides: "LiveState struct extended with focusToken (ROUND 5, read-frame, published every tick via the existing channelWrite call)/cmdSeqCounter/cmdTransform/cmdScale/cmdFlags/guardStatus/guardAddr; LiveCommand struct; channelReadCommand + channelWriteGuardStatus(flags, addr) declarations; LIVE_READFRAME_BYTES constant (now 320)"
      contains: "focusToken"
    - path: "packages/live-inject/agent/write.h"
      provides: "Pure GuardResult checkWriteGuard(liveXform, expectedXform, liveScale, expectedScale) contract with independent transformPassed/scalePassed"
      contains: "transformPassed"
    - path: "packages/live-inject/agent/write.cpp"
      provides: "checkWriteGuard implementation (transform+scale independently gated) + applyWrite (setter-invocation wrapper, takes fn pointers as params)"
  key_links:
    - from: "packages/live-inject/agent/channel.cpp"
      to: "packages/contracts/src/live-inject.ts"
      via: "static_assert offsetof(...) matching LIVE_CHANNEL_LAYOUT offsets"
      pattern: "static_assert.*offsetof\\(LiveState"
    - from: "packages/live-inject/test/write-guard.test.ts"
      to: "packages/live-inject/agent/write.cpp"
      via: "TS port of checkWriteGuard mirrors the C++ logic exactly (project convention — no native compile needed for vitest)"
      pattern: "checkWriteGuard"
---

<objective>
Extend the existing Phase-3 seqlock read channel with a second, independent seqlock region in the
SAME named file-mapping that carries the toolkit-to-agent write command (D-02), and add the pure,
testable read-verify guard comparator the agent will use before applying any write (D-03) — now
covering transform AND scale independently, and carrying a real diagnostic address (REVIEWS.md Fix
B/C).

This plan is pure contract + native-core-adjacent (`packages/live-inject`) plumbing — no Win32 calls,
no rva_table/agent_main integration yet (that is 05-03), no host N-API export yet (that is 05-04). It
establishes the byte layout and the guard algorithm both later plans build on.

Purpose: LIVE-03 requires a zero-allocation, 60fps command path from the renderer into the injected
agent, and a fail-closed guard (SC #2) that never has a force-write escape hatch, but that DOES let an
explicit "Revert ALL" re-baseline itself (REVIEWS.md Opus MEDIUM finding) without ever accepting an
arbitrary forward write. Both must be correct at the byte level before any C++ agent logic consumes
them.

Output: Extended `LIVE_CHANNEL_LAYOUT` contract (320→400 bytes, including the ROUND 5 FOCUS_TOKEN
read-frame field — REVIEWS.md round-4 maintainer decision #1), extended `LiveState` struct + seqlock
helpers, and a new pure `write.h/.cpp` guard module with independent transform/scale gating.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-REVIEWS.md
</context>

<interfaces>
<!-- Existing layout this plan extends (VERIFIED against current source this session). Executor must NOT invent a parallel layout. -->

From packages/contracts/src/live-inject.ts (current, verified):
```
export const LIVE_CHANNEL_LAYOUT = {
  SEQ_COUNTER:   { offset: 0,   length: 4   },
  TRANSFORM:     { offset: 4,   length: 48  },
  NETWORK_ID:    { offset: 52,  length: 8   },
  TEMPLATE_NAME: { offset: 60,  length: 256 },
  LIVENESS:      { offset: 316, length: 4   },
  TOTAL_SIZE:    { offset: 0,   length: 320 },
} as const;
```

From packages/live-inject/agent/channel.h (current struct, `#pragma pack(push, 4)`, verified):
```
struct LiveState {
    LONG      seqCounter;        // offset   0
    float     transform[3][4];   // offset   4
    uint64_t  networkId;         // offset  52
    char      templateName[256]; // offset  60
    uint32_t  liveness;          // offset 316  — bit0=playerNonNull, bit1=isOver
};
```

From packages/live-inject/agent/channel.cpp (current `channelWrite`, THE BUG TO AVOID, verified):
```
void channelWrite(const LiveState* state) {
    ...
    InterlockedIncrement(seq);
    std::memcpy(static_cast<char*>(s_view) + sizeof(LONG), &state->transform,
                sizeof(LiveState) - sizeof(LONG));   // <-- once LiveState grows, this over-copies
    InterlockedIncrement(seq);
}
```

From packages/live-inject/agent/sentinels.h (the pure-comparator testability idiom to mirror):
```
struct SentinelResult { bool passed; const char* failReason; };
SentinelResult checkTransform(const float* mat3x4);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the channel layout (contract + agent struct + fixed channelWrite byte count + FOCUS_TOKEN + GUARD_ADDR)</name>
  <files>packages/contracts/src/live-inject.ts, packages/live-inject/agent/channel.h, packages/live-inject/agent/channel.cpp, packages/live-inject/test/channel-layout.test.ts</files>
  <read_first>
    - packages/contracts/src/live-inject.ts (full file — LIVE_CHANNEL_LAYOUT, ENGINE_ENDPOINT_NAMES, VerifiedObjectState)
    - packages/live-inject/agent/channel.h (full file — LiveState struct, #pragma pack rationale comment)
    - packages/live-inject/agent/channel.cpp (full file — static_asserts lines 25-34, channelWrite lines 69-89, channelOpen/channelClose)
    - packages/live-inject/test/channel-layout.test.ts (full file — existing offset + seqlock-simulation tests to extend, NOT replace)
    - 05-RESEARCH.md "Pattern 1: Command-slot write channel" (376-byte sketch — SUPERSEDED, do not use its byte count) and 05-REVIEWS.md's Consensus Fixes B and C (the reasons GUARD_ADDR and the transform/scale-split guard exist), plus 05-REVIEWS.md ROUND 4's "THE CONVERGENT FINDING" and round-3 maintainer decision #1 (the reason FOCUS_TOKEN exists — ROUND 5)
  </read_first>
  <action>
    Design decision (planner-derived per CONTEXT "Claude's Discretion — exact rotation/scale channel
    encoding... planner's to derive"): the command slot carries the FULL target state on every write
    (transform AND scale together), not a mode-tagged partial update — this avoids needing a
    write-kind discriminator bit and matches "renderer maintains one authoritative local pose".
    Extend `LIVE_CHANNEL_LAYOUT` in live-inject.ts with SEVEN new entries appended after LIVENESS, and
    grow TOTAL_SIZE from 320 to 400: `FOCUS_TOKEN` (offset 320, length 4 — ROUND 5, REVIEWS.md round-4
    maintainer decision #1, the load-bearing identity-unification fix: the agent's own truncated
    `addrOf(focus)` pointer, published into the SEQLOCKED READ FRAME every tick — not just into
    GUARD_ADDR at apply time, which only updates when a guard check actually runs and therefore lags
    the agent's true current focus; this is what lets 05-07's host re-key `cowSnapshot`/`writeLog` on
    the SAME identity the agent's own guard baseline uses, so host and agent can never disagree about
    which object a baseline belongs to. x64 SCOPE CAVEAT (Codex, REVIEWS.md round-5 LOW — keep visible,
    not a defect): this 32-bit field assumes the x86 (32-bit) live target this phase targets, where the
    truncation is lossless; if an x64 client is ever supported (out of scope per Phase 3 GROUNDTRUTH),
    FOCUS_TOKEN would need to widen to 64 bits, a channel-layout change, to avoid aliasing between two
    distinct high-address x64 pointers), `COMMAND_SEQ_COUNTER` (offset 324, length 4),
    `COMMAND_TRANSFORM` (offset 328, length 48 — same float[3][4] row-major layout as TRANSFORM),
    `COMMAND_SCALE` (offset 376, length 12 — float[3] x/y/z), `COMMAND_FLAGS` (offset 388, length 4),
    `GUARD_STATUS` (offset 392, length 4), `GUARD_ADDR` (offset 396, length 4 — x86 process pointer of
    the object the agent last write-guard-checked; 0 when no object has been checked yet this session
    — REVIEWS.md Fix C: this remains the field the locked guard-blocked banner copy `<addr>: expected
    <bytes>, read <bytes>` sources its address from — DISTINCT from `FOCUS_TOKEN`: both are the SAME
    truncation of the SAME resolved pointer, they simply update on different cadences (every tick vs.
    only on a guard-checked command)). Add two bit-flag const objects: `LIVE_CMD_FLAGS` with
    `STOP_REQUESTED: 0x1` (D-04 clean agent stop-signal) and `REBASELINE_GUARD: 0x2` (REVIEWS.md Opus
    MEDIUM — an explicit "Revert ALL" command asks the agent to re-baseline its expected-bytes state to
    the CURRENT live bytes without applying any transform this iteration; this is what un-sticks a
    blocked guard for the user's own revert action without ever accepting an arbitrary forward-write
    target — 05-03 implements the agent-side handling, 05-07 implements the renderer-side two-step
    send); and `LIVE_GUARD_FLAGS` with `TRANSFORM_REFUSED: 0x1` (renamed from the original single
    WRITE_REFUSED bit, same bit value — no compat break) and `SCALE_REFUSED: 0x2` (REVIEWS.md Fix B —
    scale is guarded independently of transform; either bit may be set alone), and `STOPPING: 0x4`
    (REVIEWS.md round-3 maintainer decision #5 — a dedicated, agent-published sticky acknowledgement
    bit distinct from the latest-wins command slot: the agent sets it once, immediately before it
    honors a STOP_REQUESTED command and closes its own channel handle, so a host-side detach retry-loop
    (05-07) can observe a genuine "the agent is stopping" signal that a later drag command overwriting
    `cmdFlags` on the SAME command slot cannot silently erase — STOP is resent by the host until this
    bit is observed, never a fire-and-hope single write). `LIVE_CMD_FLAGS`/`LIVE_GUARD_FLAGS` plus
    `GUARD_ADDR` remain agent-authoritative, host-observed, single-word published fields — the agent
    sets them; the renderer reads them from the SAME ArrayBuffer it already polls via readChannelView,
    no new native export needed for either direction. `FOCUS_TOKEN`, by contrast, IS part of the
    seqlocked read frame (grouped with transform/networkId/templateName/liveness, not with the
    single-word guard fields) — it is written by the SAME `channelWrite` call that already publishes
    those fields every tick, covered by the SAME seqlock, so the host never observes a torn combination
    of `focusToken` against the rest of the read frame. Document in the file header comment why this is
    one struct with two independent seqlock regions rather than two mappings (mirrors RESEARCH.md
    "Anti-Patterns to Avoid — treating the command slot as a second CreateFileMappingA call").
    In channel.h: append `uint32_t focusToken;` to `LiveState` immediately after the existing
    `liveness` field (offset 320 — keep `#pragma pack(push,4)`; ROUND 5, ahead of the command-slot
    fields, which all shift +4 as a result), THEN append the (now-shifted) command-slot fields: `LONG
    cmdSeqCounter` (offset 324), `float cmdTransform[3][4]` (offset 328), `float cmdScale[3]` (offset
    376), `uint32_t cmdFlags` (offset 388), `uint32_t guardStatus` (offset 392), `uint32_t guardAddr`
    (offset 396 — truncate the agent's own `void*` object pointer to `uint32_t` via
    `static_cast<uint32_t>(reinterpret_cast<uintptr_t>(ptr))`, safe because the injected agent always
    runs inside the x86 (32-bit) SWG client process; `focusToken` uses the IDENTICAL truncation of the
    IDENTICAL resolved pointer, just written every iteration rather than only at guard-check time). Add
    `static constexpr size_t LIVE_READFRAME_BYTES = 320;` (transform+networkId+templateName+liveness+
    focusToken — ROUND 5: grew from 316 to 320 to include the new field — the span channelWrite must
    copy, independent of struct size) and `static constexpr uint32_t CMD_FLAG_STOP_REQUESTED = 0x1;` /
    `static constexpr uint32_t CMD_FLAG_REBASELINE_GUARD = 0x2;` / `static constexpr uint32_t
    GUARD_FLAG_TRANSFORM_REFUSED = 0x1;` / `static constexpr uint32_t GUARD_FLAG_SCALE_REFUSED = 0x2;`
    / `static constexpr uint32_t GUARD_FLAG_STOPPING = 0x4;` mirroring the TS consts exactly. Add a
    `struct LiveCommand { float transform[3][4]; float scale[3]; uint32_t flags; };` plain-data return
    type. Declare `bool channelReadCommand(LiveCommand* out, uint32_t* outCmdSeq);` (agent-side seqlock
    retry-read of the command region, returns false on torn/mid-write read — mirror the read-side retry
    idiom the RENDERER already uses in useChannelReader.ts's parseChannelView, applied here in C++) and
    `void channelWriteGuardStatus(uint32_t flags, uint32_t addr);` (agent writes its own guard outcome
    AND the checked object's address back — both are single aligned uint32 words, use
    `InterlockedExchange` for each, no seqlock needed since each is independently a single aligned
    32-bit word and a torn READ of one stale-but-valid word is harmless — the renderer already
    tolerates reading `guardStatus` without a seqlock per the original plan). In channel.cpp: fix the
    load-bearing bug — change channelWrite's memcpy byte count from `sizeof(LiveState) - sizeof(LONG)`
    to the new `LIVE_READFRAME_BYTES` constant, so extending LiveState for the command slot can never
    silently make channelWrite over-copy into the command region; the agent's caller must set
    `state.focusToken` (the truncated `addrOf(focus)`) EVERY iteration before calling `channelWrite`,
    the SAME way it already sets transform/networkId/templateName/liveness (05-03 wires this). Add
    matching `static_assert(offsetof(LiveState, focusToken) == 320, ...)` through `guardAddr == 396`
    (mirror the existing four asserts' style exactly — SEVEN new asserts total, one per new field) and
    update the top-of-file `static_assert(sizeof(LiveState) == 320, ...)` to `400`. Implement
    `channelReadCommand`/`channelWriteGuardStatus` against the same `s_view` pointer channelOpen
    already mapped. Extend channel-layout.test.ts (do not replace the existing four tests) with: a test
    asserting `LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length === 400`, a test asserting all SEVEN new offsets
    (including `FOCUS_TOKEN` at 320) match this action's values, a test asserting
    `LIVE_GUARD_FLAGS.TRANSFORM_REFUSED !== LIVE_GUARD_FLAGS.SCALE_REFUSED` (distinct bits), and a
    seqlock-simulation test (mirror the existing "seqlock write→read round-trips LiveState values"
    test, lines 32-98) that simulates a HOST write into the command region (seq odd → payload → seq
    even) and a subsequent read, including a torn-read case (seq left odd) that must be rejected — this
    is the JS-side proof of the same protocol channelReadCommand implements in C++. ADD a NEW test
    (ROUND 5) simulating an AGENT write of the read frame that includes a non-zero `FOCUS_TOKEN` value
    and asserting it round-trips through the SAME seqlock read path the existing transform/networkId
    fields already use — proving `focusToken` is genuinely part of the coherent read-frame snapshot,
    not a separately-timed field.
  </action>
  <verify>
    <automated>pnpm --filter @swg/contracts build && pnpm --filter @swg/live-inject test -- channel-layout</automated>
  </verify>
  <acceptance_criteria>
    - `LIVE_CHANNEL_LAYOUT.TOTAL_SIZE.length === 400` and all SEVEN new field offsets/lengths match the values in this task (FOCUS_TOKEN at offset 320, length 4; GUARD_ADDR at offset 396, length 4).
    - channel.cpp's channelWrite references `LIVE_READFRAME_BYTES`, not `sizeof(LiveState) - sizeof(LONG)`, and `LIVE_READFRAME_BYTES === 320`.
    - Seven new `static_assert(offsetof(LiveState, ...) == ...)` lines exist in channel.cpp, one per new field (starting with `focusToken == 320`), and the top-level size assert reads 400.
    - `LIVE_GUARD_FLAGS.TRANSFORM_REFUSED`, `.SCALE_REFUSED`, and `.STOPPING` are three distinct, non-zero bit values (STOPPING === 0x4).
    - channel-layout.test.ts has a passing torn-read-rejected case for the command region (grep confirms a test whose seq is left odd and asserts the read is rejected).
    - channel-layout.test.ts has a passing case (ROUND 5) proving a non-zero `FOCUS_TOKEN` value round-trips through the SAME seqlock read path as the pre-existing read-frame fields (transform/networkId/templateName/liveness), not a separately-timed publish.
  </acceptance_criteria>
  <done>Contract + native struct agree on a 400-byte layout with independent transform/scale guard bits, a real diagnostic address field, AND a per-tick FOCUS_TOKEN read-frame field (ROUND 5 — the load-bearing identity-unification primitive 05-03/05-07 build on); channelWrite cannot regress into the command region; the offset/seqlock proof is TS-verified without needing a native compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure read-verify guard module (write.h/.cpp) — independent transform/scale gating + TS-ported unit test</name>
  <files>packages/live-inject/agent/write.h, packages/live-inject/agent/write.cpp, packages/live-inject/test/write-guard.test.ts</files>
  <behavior>
    - `checkWriteGuard(liveXform[3][4], expectedXform[3][4], liveScale[3], expectedScale[3])` returns
      `{ transformPassed: true, scalePassed: true }` when every element of liveXform equals
      expectedXform AND every element of liveScale equals expectedScale (bytewise/exact float compare
      — the agent captured both from its own prior read, so an exact match is the correct condition,
      not a tolerance compare).
    - `transformPassed` and `scalePassed` are independent: a transform mismatch with a scale match
      returns `{ transformPassed: false, scalePassed: true, failReason: "..." }` (and vice versa) —
      REVIEWS.md Fix B: Scale must never be silently ungated by a transform-only check, and a
      transform-only external change must not needlessly block a legitimate scale write or vice versa.
    - `failReason` names WHICH channel(s) mismatched (e.g. "transform mismatch", "scale mismatch",
      "transform and scale mismatch") when any check fails — this is the fail-closed condition (D-03
      SC #2): something other than the toolkit's own last write changed the object.
    - `expectedXform`/`expectedScale` start uninitialized/unset until the agent's first
      passing-sentinel read after attach (the D-03 "attach-time" capture) — this task only implements
      the comparator; capture timing is wired in 05-03's agent_main.cpp integration.
    - Scale-channel parity note (2026-07-08 plan-check revision; wording corrected 2026-07-09): this
      comparator has ALWAYS applied the identical exactness rule to `liveScale`/`expectedScale` as to
      transform — there is no "scale gets a looser check" branch anywhere in this module. The
      historical gap this revision round closes lived entirely in 05-03's CALLER, which fed
      `expectedScale` back into the `liveScale` argument (a self-comparing no-op that could never
      detect an externally-driven scale change, since a value always equals itself). 05-03 now wires a
      genuine live-read `liveScale` value, via a per-build-gated `m_scale` member-offset read — not an
      accessor call, see 05-03's 2026-07-09 REVISION NOTE) so an external scale tamper is caught with
      the SAME reliability as an external transform tamper — this module's contract did not change to
      make that true; only its caller did.
  </behavior>
  <read_first>
    - packages/live-inject/agent/sentinels.h and sentinels.cpp (full files — the exact pure/testable pattern to mirror: SentinelResult, no Win32 calls, byte-buffer-only signatures)
    - packages/live-inject/test/sentinels.test.ts (the TS-port-of-C++-logic convention this project uses INSTEAD of compiling C++ for vitest — read the file header comment explaining why)
    - 05-REVIEWS.md Consensus Fix B (why the guard must cover scale independently) and this plan's Task 1 output (LiveCommand, GUARD_FLAG_TRANSFORM_REFUSED/GUARD_FLAG_SCALE_REFUSED)
  </read_first>
  <action>
    Create write.h declaring: `struct GuardResult { bool transformPassed; bool scalePassed; const
    char* failReason; };  GuardResult checkWriteGuard(const float liveXform[3][4], const float
    expectedXform[3][4], const float liveScale[3], const float expectedScale[3]);` and a second
    declaration `void applyWrite(void* target, const LiveCommand& cmd, const GuardResult& guard, /*
    setter fn pointers passed by the caller, resolved in rva_table.cpp — do not resolve endpoints here
    */);` — the apply-invocation function is declared but its actual implementation (calling the real
    `setTransform_o2w`/`setScale` fn pointers, each gated on its OWN guard bit AND its OWN non-null
    resolved-fn check per REVIEWS.md Fix A) is completed in 05-03 alongside the fn-pointer typedefs it
    needs; this task's write.cpp implements ONLY checkWriteGuard fully, with applyWrite left as a thin
    declaration (no definition yet, or a TODO-stub definition that 05-03 replaces — executor's choice,
    but write.h's signature must not change in 05-03). Implement checkWriteGuard in write.cpp: iterate
    all 12 transform floats — on any mismatch set `transformPassed = false`; iterate all 3 scale floats
    — on any mismatch set `scalePassed = false`; build `failReason` describing which channel(s) failed
    (or `nullptr` if both passed). Include `TRANSFORM_BYTE_SIZE`-equivalent from sentinels.h if useful
    for consistency, but this function takes raw `float[3][4]`/`float[3]` (not byte buffers) since the
    caller (agent_main.cpp, in 05-03) already has typed data from its own transform/scale reads. Do not
    add any Windows.h include to write.h (lean-header contract, mirrors resolve.h/sentinels.h). Create
    write-guard.test.ts as a TS port mirroring sentinels.test.ts's file-header convention exactly
    (comment explaining this avoids a native compile cycle for vitest): implement a TS
    `checkWriteGuard(live: Float32Array, expected: Float32Array, liveScale: Float32Array, expectedScale:
    Float32Array)` matching the C++ logic element-for-element, with tests for: both match → both pass;
    transform-only mismatch → `transformPassed:false, scalePassed:true`; scale-only mismatch →
    `transformPassed:true, scalePassed:false`; both mismatch → both false; and a case proving
    floating-point exactness (not tolerance) is used — an object moved by a tiny externally-driven
    delta (e.g. by the game's own physics) must still be caught, since ANY difference means "not ours."
  </action>
  <verify>
    <automated>pnpm --filter @swg/live-inject test -- write-guard</automated>
  </verify>
  <acceptance_criteria>
    - write.h has zero includes of Windows.h (grep -v confirms).
    - checkWriteGuard is pure (no side effects, no I/O) and callable with two `float[3][4]` and two `float[3]` arguments, returning independent transformPassed/scalePassed booleans.
    - write-guard.test.ts has a passing case for: both-pass, transform-only-fail, scale-only-fail, both-fail (4 distinct scenarios minimum, plus per-element mismatch coverage for at least the transform channel).
    - `pnpm --filter @swg/live-inject test` is green.
  </acceptance_criteria>
  <done>A pure, testable read-verify guard exists that independently gates transform and scale writes (D-03, REVIEWS.md Fix B), proven via a TS port per this project's established sentinels.h testing convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer (untrusted-by-convention JS) → shared file-mapping | The command region is written by host-process JS reachable from the renderer bundle; malformed or hostile values must not corrupt the read-frame region or crash the agent. |
| Shared file-mapping → injected agent (in-game process) | The agent trusts only its own locally-captured `expectedXform`/`expectedScale`, never an externally-asserted "this is fine" flag — the guard is the only gate, and `REBASELINE_GUARD` can only reset the baseline to the CURRENT live bytes, never accept an arbitrary forward-write target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | channel.cpp channelWrite | mitigate | Byte count is the named `LIVE_READFRAME_BYTES` constant, not struct-size arithmetic — extending the struct can never silently widen the read-frame write span into the new command region (Task 1). |
| T-05-02 | Tampering | write.cpp checkWriteGuard | mitigate | Exact (not tolerance) float compare against the agent's own last-known-good transform AND scale, independently — any external change, however small, fails closed on that channel; no bypass path exists at this layer. |
| T-05-03 | Repudiation/DoS | channel.h struct evolution | mitigate | Seven new `static_assert(offsetof(...))` lines (ROUND 5: adds FOCUS_TOKEN@320 + the +4-shifted command/guard fields on top of the round-4 set) catch any future accidental reordering/padding regression at compile time, mirroring the existing four asserts' proven pattern. |
| T-05-26 | Elevation of privilege | CMD_FLAG_REBASELINE_GUARD | mitigate | Rebaseline only ever adopts bytes the agent reads/computes ITSELF this gated iteration — transform from a fresh `getTransform_o2w` read, scale from the per-build-gated `m_scale` member-offset read (05-03 ROUND 4 decision #2 added the genuine live scale read specifically so scale rebaselines from a LIVE value exactly like transform, not from `cmd.scale`) — so it can only ever carry the object's OWN current live state, never an arbitrary forward-write value asserted by some OTHER, later command (REVIEWS.md Opus MEDIUM fix + ROUND 2 coalesce + ROUND 4 gate-and-unify, implemented agent-side in 05-03). Wording reconciled with 05-07's T-05-14 by the 2026-07-08 plan-check revision and updated for the ROUND 4 live-scale-read design — no executable-behavior change (05-03's acceptance criteria are the source of truth). |
</threat_model>

<verification>
Run `pnpm --filter @swg/contracts build && pnpm --filter @swg/live-inject test` — all four existing
channel-layout tests plus the new command-region tests, and all write-guard tests, must be green.
</verification>

<success_criteria>
- LIVE_CHANNEL_LAYOUT and the native LiveState struct agree on a 400-byte layout with seven new named fields, including a real diagnostic address (GUARD_ADDR) AND a per-tick focus-identity field (FOCUS_TOKEN, ROUND 5).
- channelWrite can no longer regress into the command region as the struct grows; FOCUS_TOKEN is part of the SAME seqlocked read-frame span as transform/networkId/templateName/liveness, never a separately-timed publish.
- A pure, Win32-free checkWriteGuard exists, independently gates transform and scale, and is proven via a TS port, ready for 05-03 to wire into the agent poll loop.
- GUARD_STATUS carries the STOPPING bit (0x4) so a STOP signal is a dedicated sticky flag independent of `cmdSeq`, preventing a late drag from overtaking STOP on the latest-wins slot (ROUND 4 decision #5; this bit alone is zero channel growth — 396 bytes stays 396 for THIS bit specifically; the SEPARATE ROUND 5 FOCUS_TOKEN addition is what grows the layout to 400).
- FOCUS_TOKEN gives 05-03 and 05-07 a single, agent-authoritative identity signal to re-key on, closing the fail-open two-same-template-object revert bug and the lossy focus-flip A->B->A bug (ROUND 5, REVIEWS.md round-4 maintainer decision #1).
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-SUMMARY.md` when done
</output>
