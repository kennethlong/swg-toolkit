---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/live-inject/src/channel_binding.cpp
  - packages/live-inject/src/addon.cpp
autonomous: true
requirements: [LIVE-03]

# REVISION NOTE (--reviews replan, 2026-07-08): 05-01's layout grew 392 -> 396 bytes (added GUARD_ADDR
# at offset 392, REVIEWS.md Fix C). This plan's ONLY change is the CHANNEL_BYTE_SIZE constant and the
# accompanying comment — GUARD_ADDR itself is agent-authoritative (written by channelWriteGuardStatus
# in 05-01/05-03, alongside guardStatus), so WriteCommand's write span (320-387) is UNCHANGED; the host
# never writes GUARD_STATUS or GUARD_ADDR, it only maps enough bytes to read them back via the existing
# readChannelView.

# REVISION NOTE (--reviews replan, ROUND 2, 2026-07-08): folds in 05-REVIEWS.md's LOW/hygiene finding
# (Cursor) — this plan's own prose previously said the WriteCommand write span was "320-391" and that
# "GUARD_STATUS at 388-391 is the last byte this export ever writes," which contradicts the actual
# memcpy calls specified in Task 1's action text (which only ever write offsets 320-387: cmdSeqCounter
# 320-323, cmdTransform 324-371, cmdScale 372-383, cmdFlags 384-387). GUARD_STATUS (388-391) was NEVER
# written by WriteCommand's memcpy sequence — it is agent-authoritative, exactly like GUARD_ADDR
# (392-395), written only via `channelWriteGuardStatus` (05-01/05-03). Fixed throughout this file: every
# "391" reference corrected to "387", and the frontmatter/action text now explicitly names BOTH
# GUARD_STATUS (388-391) and GUARD_ADDR (392-395) as agent-authoritative/host-read-only, not just
# GUARD_ADDR. No functional/byte-layout code change — the actual memcpy offsets specified in Task 1 were
# always correct at 320-387; only the prose describing the span was wrong.

# REVISION NOTE (--reviews replan ROUND 4, 2026-07-12): 05-REVIEWS.md round-3 maintainer decision #5
# (STOP sticky bit) lands agent-side in 05-03 (publishes GUARD_FLAG_STOPPING, 05-01's new GUARD_STATUS
# bit2) and host-side in 05-07 (detachUI's retry-loop resends writeStop while polling for that bit).
# This plan needs NO functional change: `WriteCommand` is already a plain, stateless, idempotently-
# callable seqlock write (Task 1) — calling it repeatedly with the same STOP payload from a host retry
# loop is exactly the pattern it already supports, with no new export, no new argument, and no new
# byte-layout change. Confirmed here, not silently assumed, so the STOP-retry fix's host-side half
# (05-07) has no undocumented dependency on this file changing.

# REVISION NOTE (--reviews replan ROUND 5, 2026-07-12): folds in 05-REVIEWS.md ROUND 4 maintainer
# decision #1 (identity unification — see 05-01's own ROUND 5 note for the full rationale). 05-01's
# layout grows AGAIN, 396 -> 400 bytes, via a NEW read-frame field `FOCUS_TOKEN` inserted at offset 320
# (immediately after LIVENESS, BEFORE the command-slot region) — this SHIFTS every command-region field
# +4 bytes: `COMMAND_SEQ_COUNTER` 320->324, `COMMAND_TRANSFORM` 324->328, `COMMAND_SCALE` 372->376,
# `COMMAND_FLAGS` 384->388, `GUARD_STATUS` 388->392, `GUARD_ADDR` 392->396. Unlike the 05-01 ROUND-1
# GUARD_ADDR growth (which needed only a `CHANNEL_BYTE_SIZE` bump, since GUARD_ADDR was appended AFTER
# every host-writable field), THIS growth inserts a field BEFORE the command region, so `WriteCommand`'s
# actual write OFFSETS shift, not just the total mapped size. Task 1 below is updated throughout:
# `CHANNEL_BYTE_SIZE` grows 396 -> 400; `WriteCommand`'s seqlock write now spans offsets 324-391 (was
# 320-387); `GUARD_STATUS` (392-395, was 388-391) and `GUARD_ADDR` (396-399, was 392-395) remain
# agent-authoritative and are STILL never written by this export — only their absolute offsets moved.
# `FOCUS_TOKEN` (offset 320-323) is itself agent-authoritative (part of the READ frame, published by
# the agent's own `channelWrite`, see 05-01/05-03) and is likewise never written by `WriteCommand` — it
# is read back by the renderer via the existing `readChannelView` export, no new host export needed.

must_haves:
  truths:
    - "A host-side writeCommand(name, transformBytes, scaleBytes, flags) export exists and writes into the SAME named mapping openChannel already created — no second CreateFileMappingA call is added anywhere."
    - "CHANNEL_BYTE_SIZE grows from 320 to 400 in lockstep with the 05-01 contract (ROUND 5: 396 -> 400, the FOCUS_TOKEN read-frame field), and this is the single source of truth channel_binding.cpp uses (per the file's own header comment about a single named mapping)."
    - "WriteCommand's write span is exactly offsets 324-391 (cmdSeqCounter/cmdTransform/cmdScale/cmdFlags — ROUND 5: shifted +4 from 320-387 because FOCUS_TOKEN was inserted before the command region in 05-01) — FOCUS_TOKEN (320-323), GUARD_STATUS (392-395), and GUARD_ADDR (396-399) are ALL agent-authoritative and read-only from the host's perspective; WriteCommand never touches any of them."
  artifacts:
    - path: "packages/live-inject/src/channel_binding.cpp"
      provides: "WriteCommand N-API export (seqlock write at offset 324, ROUND 5) using the same GC-guard (Napi::Reference) pattern as OpenChannel; CHANNEL_BYTE_SIZE=400"
      contains: "WriteCommand"
  key_links:
    - from: "packages/live-inject/src/addon.cpp"
      to: "packages/live-inject/src/channel_binding.cpp"
      via: "exports.Set(\"writeCommand\", Napi::Function::New(env, WriteCommand))"
      pattern: "writeCommand"
---

<objective>
Add the host-side `writeCommand` N-API export that lets the renderer (via `packages/live-inject`)
write into the command-slot region of the already-open shared mapping — the host-side half of the
toolkit-to-agent write direction (D-02), completing the plumbing 05-01 defined and 05-03 consumes.

Purpose: LIVE-03's write path needs a synchronous, zero-allocation host export the renderer calls on
every gizmo-drag/numbox-edit event; this plan is the last piece of native plumbing before the renderer
hook (05-07) can exist.

Output: `channel_binding.cpp` gains `WriteCommand`; `CHANNEL_BYTE_SIZE` grows to 400 (05-01's ROUND 5
layout, FOCUS_TOKEN + GUARD_ADDR included); `addon.cpp` registers the new export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
</context>

<interfaces>
Existing OpenChannel GC-guard pattern to reuse verbatim (channel_binding.cpp lines 83-141):
`static constexpr size_t CHANNEL_BYTE_SIZE = 320;` (this plan changes it to 400). `struct
ChannelState { HANDLE hMap = nullptr; Napi::Reference<Napi::ArrayBuffer> abRef; };` keyed in
`s_channels` (an `unordered_map<std::string, ChannelState>`) by mapping name. `OpenChannel` calls
`CreateFileMappingA(..., CHANNEL_BYTE_SIZE, name)`, `MapViewOfFile(..., CHANNEL_BYTE_SIZE)`, wraps the
view in `Napi::ArrayBuffer::New(env, view, CHANNEL_BYTE_SIZE, finalizer)`, and stores
`cs.abRef = Napi::Reference<Napi::ArrayBuffer>::New(ab, 1)`.

05-01's contract this plan writes against (`packages/contracts/src/live-inject.ts`, ROUND 5 400-byte
layout): `LIVE_CHANNEL_LAYOUT.FOCUS_TOKEN` (offset 320, len 4 — AGENT-WRITTEN, part of the read frame,
NEVER written by this export), `.COMMAND_SEQ_COUNTER` (offset 324, len 4), `.COMMAND_TRANSFORM` (offset
328, len 48), `.COMMAND_SCALE` (offset 376, len 12), `.COMMAND_FLAGS` (offset 388, len 4 — this field
ends at byte 391, inclusive), `.GUARD_STATUS` (offset 392, len 4 — AGENT-WRITTEN, ends at byte 395),
`.GUARD_ADDR` (offset 396, len 4 — AGENT-WRITTEN, ends at byte 399). FOCUS_TOKEN, GUARD_STATUS, and
GUARD_ADDR are all read via the existing readChannelView export but NEVER written here.
`LIVE_CMD_FLAGS.STOP_REQUESTED = 0x1`, `LIVE_CMD_FLAGS.REBASELINE_GUARD = 0x2`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WriteCommand export + CHANNEL_BYTE_SIZE bump + addon registration</name>
  <files>packages/live-inject/src/channel_binding.cpp, packages/live-inject/src/addon.cpp</files>
  <read_first>
    - packages/live-inject/src/channel_binding.cpp (full file — OpenChannel/CloseChannel/ReadChannelView, the exact GC-guard pattern, CHANNEL_BYTE_SIZE constant)
    - packages/live-inject/src/addon.cpp (full file — forward-declaration + exports.Set registration convention)
    - This phase's 05-01-PLAN.md (the exact offsets this export must write to, ROUND 5 shifted layout; note FOCUS_TOKEN at 320-323, GUARD_STATUS at 392-395, AND GUARD_ADDR at 396-399 are all agent-authoritative and NOT written by this export)
  </read_first>
  <action>
    Change `CHANNEL_BYTE_SIZE` from 320 to 400 (single source of truth per the file's own header
    comment — this one constant drives both `CreateFileMappingA`'s size and `MapViewOfFile`'s size in
    OpenChannel, so no other change is needed there; growing it to 400 rather than 392 ensures the
    mapped view is large enough for the renderer to read FOCUS_TOKEN, GUARD_STATUS, and GUARD_ADDR via
    the existing readChannelView export, even though WriteCommand itself never touches any of the
    three). Add `Napi::Value WriteCommand(const Napi::CallbackInfo& info)`: validate `info[0]` is a
    string (channel name), `info[1]` is a Float32Array/ArrayBuffer of exactly 12 floats (the target
    transform), `info[2]` is a Float32Array/ArrayBuffer of exactly 3 floats (the target scale), and
    `info[3]` is a number (flags, defaults to 0 if omitted — carries STOP_REQUESTED or
    REBASELINE_GUARD, and per 05-03's ROUND 2 coalesce fix, REBASELINE_GUARD may accompany a REAL
    transform/scale payload in the SAME call — this export does not interpret flags, it just writes
    whatever the caller passed). Look up the channel in `s_channels` by name (reuse the existing map —
    do NOT create a second registry); if not found or its `abRef` is empty, throw a JS Error
    ("writeCommand: channel not open — call openChannel first"). Obtain the raw view pointer from the
    existing `abRef.Value().Data()` (the same buffer ReadChannelView already exposes). Perform the
    seqlock write at offset 324 (ROUND 5 — shifted +4 from the pre-05-01-ROUND-5 offset 320, because
    05-01 inserted the new `FOCUS_TOKEN` read-frame field at offset 320, immediately BEFORE the command
    region), spanning EXACTLY the command region (324-391 — cmdSeqCounter/cmdTransform/cmdScale/
    cmdFlags; FOCUS_TOKEN at 320-323, GUARD_STATUS at 392-395, and GUARD_ADDR at 396-399 are ALL
    agent-authoritative and are NEVER written by this export): cast `view + 324` to `volatile LONG*`,
    `InterlockedIncrement` it (seq goes odd), `memcpy(view + 328, transformData, 48)`,
    `memcpy(view + 376, scaleData, 12)`, `memcpy(view + 388, &flags, 4)`, then `InterlockedIncrement`
    the same LONG again (seq goes even) — mirror the exact increment-write-increment shape of the
    agent's own `channelWrite` in channel.cpp (05-01), just run from the host side into the SAME
    mapping, at the (now-shifted) command-slot offset instead of offset 0. Return `env.Undefined()`. In
    addon.cpp: add a forward declaration `Napi::Value WriteCommand(const Napi::CallbackInfo& info);`
    grouped with the existing "File-mapping channel" declarations, and register it —
    `exports.Set("writeCommand", Napi::Function::New(env, WriteCommand));` — immediately after the
    existing `readChannelView` registration line. Update channel_binding.cpp's own header doc-comment
    (which currently lists exactly three exports) to add `writeCommand(name, transformBytes,
    scaleBytes, flags) -> undefined` to that list, noting CHANNEL_BYTE_SIZE=400 and that offset 320-323
    (FOCUS_TOKEN) and offsets 392-399 (GUARD_STATUS + GUARD_ADDR) are agent-authoritative, read-only
    from this file's perspective.
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/build --config Release --target swg_live_inject 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `require('@swg/live-inject').writeCommand` is a callable function after build.
    - `CHANNEL_BYTE_SIZE === 400` (grep confirms — not 396, not 388, not 320).
    - Calling `writeCommand` on a channel name that was never opened throws a JS Error containing "channel not open" (no native crash, no silent no-op).
    - After `openChannel(name)` then `writeCommand(name, transform12, scale3, 0)`, `readChannelView(name)` returns a 400-byte ArrayBuffer whose bytes at offset 324 show an even seq counter and the written transform/scale bytes at offsets 328/376; bytes 320-323 (FOCUS_TOKEN) and 392-399 (GUARD_STATUS + GUARD_ADDR) remain whatever the agent last wrote (or zero if no agent has written yet) — WriteCommand never touches any of them.
    - grep confirms the highest byte offset literal used in any WriteCommand memcpy/InterlockedIncrement call is 388 (the start of the 4-byte cmdFlags write, ending at byte 391) — no WriteCommand call site references offset 392, 395, 396, or 399 (those belong to the agent-authoritative GUARD_STATUS/GUARD_ADDR fields).
    - grep confirms WriteCommand's seqlock word is incremented at offset 324, NOT offset 320 (320 is now the agent-authoritative FOCUS_TOKEN field, ROUND 5 — WriteCommand never writes to it).
  </acceptance_criteria>
  <done>writeCommand is exported, registered, and provably writes into the same shared mapping openChannel/readChannelView already use, spanning exactly the command region (324-391, ROUND 5 shifted) — the host-side half of the D-02 write path is complete, with no confusion between the host-written span and the agent-authoritative FOCUS_TOKEN/GUARD_STATUS/GUARD_ADDR fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer JS (host process) → shared file-mapping command region | writeCommand is the only host-side entry point that can influence the agent's next apply; it must never be reachable with an unopened/stale channel name, and must never write past the command region into agent-owned FOCUS_TOKEN/GUARD_STATUS/GUARD_ADDR territory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | WriteCommand argument validation | mitigate | Fixed-size buffer length checks (exactly 12 floats / 3 floats) before any memcpy — a malformed call throws rather than writing a partial/garbled command that the agent could misinterpret. |
| T-05-08 | DoS | GC lifecycle of the mapped ArrayBuffer | mitigate | Reuses the existing `Napi::Reference` GC-guard (no new lifetime-management scheme introduced), matching this project's established Pitfall-5 discipline. |
</threat_model>

<verification>
Build `@swg/live-inject` and confirm `writeCommand` round-trips through `readChannelView` as described
in the acceptance criteria above.
</verification>

<success_criteria>
- writeCommand exists, is registered, and writes into the single existing shared mapping, spanning exactly the command region (324-391, ROUND 5 shifted +4 from 320-387 — never FOCUS_TOKEN/GUARD_STATUS/GUARD_ADDR).
- CHANNEL_BYTE_SIZE is 400 everywhere it is used in this file.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-04-SUMMARY.md` when done
</output>
