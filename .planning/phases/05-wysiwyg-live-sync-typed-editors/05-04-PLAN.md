---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/live-inject/src/channel_binding.cpp
  - packages/live-inject/src/addon.cpp
autonomous: true
requirements: [LIVE-03]

must_haves:
  truths:
    - "A host-side writeCommand(name, transformBytes, scaleBytes, flags) export exists and writes into the SAME named mapping openChannel already created — no second CreateFileMappingA call is added anywhere."
    - "CHANNEL_BYTE_SIZE grows from 320 to 392 in lockstep with the 05-01 contract, and this is the single source of truth channel_binding.cpp uses (per the file's own header comment about a single named mapping)."
  artifacts:
    - path: "packages/live-inject/src/channel_binding.cpp"
      provides: "WriteCommand N-API export (seqlock write at offset 320) using the same GC-guard (Napi::Reference) pattern as OpenChannel"
      contains: "WriteCommand"
  key_links:
    - from: "packages/live-inject/src/addon.cpp"
      to: "packages/live-inject/src/channel_binding.cpp"
      via: "exports.Set(\"writeCommand\", Napi::Function::New(env, WriteCommand))"
      pattern: "writeCommand"
---

<objective>
Add the host-side `writeCommand` N-API export that lets the renderer (via `packages/live-inject`)
write into the command-slot region of the already-open shared mapping — the host-side half of the
toolkit-to-agent write direction (D-02), completing the plumbing 05-01 defined and 05-03 consumes.

Purpose: LIVE-03's write path needs a synchronous, zero-allocation host export the renderer calls on
every gizmo-drag/numbox-edit event; this plan is the last piece of native plumbing before the renderer
hook (05-07) can exist.

Output: `channel_binding.cpp` gains `WriteCommand`; `CHANNEL_BYTE_SIZE` grows to 392; `addon.cpp`
registers the new export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-RESEARCH.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-01-PLAN.md
</context>

<interfaces>
Existing OpenChannel GC-guard pattern to reuse verbatim (channel_binding.cpp lines 83-141):
`static constexpr size_t CHANNEL_BYTE_SIZE = 320;` (this plan changes it to 392). `struct
ChannelState { HANDLE hMap = nullptr; Napi::Reference<Napi::ArrayBuffer> abRef; };` keyed in
`s_channels` (an `unordered_map<std::string, ChannelState>`) by mapping name. `OpenChannel` calls
`CreateFileMappingA(..., CHANNEL_BYTE_SIZE, name)`, `MapViewOfFile(..., CHANNEL_BYTE_SIZE)`, wraps the
view in `Napi::ArrayBuffer::New(env, view, CHANNEL_BYTE_SIZE, finalizer)`, and stores
`cs.abRef = Napi::Reference<Napi::ArrayBuffer>::New(ab, 1)`.

05-01's contract this plan writes against (`packages/contracts/src/live-inject.ts`):
`LIVE_CHANNEL_LAYOUT.COMMAND_SEQ_COUNTER` (offset 320, len 4), `.COMMAND_TRANSFORM` (offset 324, len
48), `.COMMAND_SCALE` (offset 372, len 12), `.COMMAND_FLAGS` (offset 384, len 4), `.GUARD_STATUS`
(offset 388, len 4). `LIVE_CMD_FLAGS.STOP_REQUESTED = 0x1`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WriteCommand export + CHANNEL_BYTE_SIZE bump + addon registration</name>
  <files>packages/live-inject/src/channel_binding.cpp, packages/live-inject/src/addon.cpp</files>
  <read_first>
    - packages/live-inject/src/channel_binding.cpp (full file — OpenChannel/CloseChannel/ReadChannelView, the exact GC-guard pattern, CHANNEL_BYTE_SIZE constant)
    - packages/live-inject/src/addon.cpp (full file — forward-declaration + exports.Set registration convention)
    - This phase's 05-01-PLAN.md (the exact offsets this export must write to)
  </read_first>
  <action>
    Change `CHANNEL_BYTE_SIZE` from 320 to 392 (single source of truth per the file's own header
    comment — this one constant drives both `CreateFileMappingA`'s size and `MapViewOfFile`'s size in
    OpenChannel, so no other change is needed there). Add
    `Napi::Value WriteCommand(const Napi::CallbackInfo& info)`: validate `info[0]` is a string
    (channel name), `info[1]` is a Float32Array/ArrayBuffer of exactly 12 floats (the target
    transform), `info[2]` is a Float32Array/ArrayBuffer of exactly 3 floats (the target scale), and
    `info[3]` is a number (flags, defaults to 0 if omitted — carries the STOP_REQUESTED bit). Look up
    the channel in `s_channels` by name (reuse the existing map — do NOT create a second registry);
    if not found or its `abRef` is empty, throw a JS Error ("writeCommand: channel not open — call
    openChannel first"). Obtain the raw view pointer from the existing `abRef.Value().Data()` (the
    same buffer ReadChannelView already exposes). Perform the seqlock write at offset 320: cast
    `view + 320` to `volatile LONG*`, `InterlockedIncrement` it (seq goes odd), `memcpy(view + 324,
    transformData, 48)`, `memcpy(view + 372, scaleData, 12)`, `memcpy(view + 384, &flags, 4)`, then
    `InterlockedIncrement` the same LONG again (seq goes even) — mirror the exact
    increment-write-increment shape of the agent's own `channelWrite` in channel.cpp (05-01), just run
    from the host side into the SAME mapping, at the command-slot offset instead of offset 0. Return
    `env.Undefined()`. In addon.cpp: add a forward declaration
    `Napi::Value WriteCommand(const Napi::CallbackInfo& info);` grouped with the existing
    "File-mapping channel" declarations, and register it —
    `exports.Set("writeCommand", Napi::Function::New(env, WriteCommand));` — immediately after the
    existing `readChannelView` registration line. Update channel_binding.cpp's own header doc-comment
    (which currently lists exactly three exports) to add `writeCommand(name, transformBytes,
    scaleBytes, flags) -> undefined` to that list.
  </action>
  <verify>
    <automated>cmake --build packages/live-inject/build --config Release --target swg_live_inject 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `require('@swg/live-inject').writeCommand` is a callable function after build.
    - Calling `writeCommand` on a channel name that was never opened throws a JS Error containing "channel not open" (no native crash, no silent no-op).
    - After `openChannel(name)` then `writeCommand(name, transform12, scale3, 0)`, `readChannelView(name)` returns a 392-byte ArrayBuffer whose bytes at offset 320 show an even seq counter and the written transform/scale bytes at offsets 324/372.
  </acceptance_criteria>
  <done>writeCommand is exported, registered, and provably writes into the same shared mapping openChannel/readChannelView already use — the host-side half of the D-02 write path is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer JS (host process) → shared file-mapping command region | writeCommand is the only host-side entry point that can influence the agent's next apply; it must never be reachable with an unopened/stale channel name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | WriteCommand argument validation | mitigate | Fixed-size buffer length checks (exactly 12 floats / 3 floats) before any memcpy — a malformed call throws rather than writing a partial/garbled command that the agent could misinterpret. |
| T-05-08 | DoS | GC lifecycle of the mapped ArrayBuffer | mitigate | Reuses the existing `Napi::Reference` GC-guard (no new lifetime-management scheme introduced), matching this project's established Pitfall-5 discipline. |
</threat_model>

<verification>
Build `@swg/live-inject` and confirm `writeCommand` round-trips through `readChannelView` as described
in the acceptance criteria above.
</verification>

<success_criteria>
- writeCommand exists, is registered, and writes into the single existing shared mapping.
- CHANNEL_BYTE_SIZE is 392 everywhere it is used in this file.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-04-SUMMARY.md` when done
</output>
