---
phase: 05-wysiwyg-live-sync-typed-editors
plan: 08
type: execute
wave: 3
depends_on: ["05-06"]
files_modified:
  - packages/renderer/src/panels/editors/SchemaRail.tsx
  - packages/renderer/src/panels/editors/DatatableGridEditor.tsx
  - packages/renderer/src/shell/WorkspaceShell.tsx
  - packages/renderer/src/shell/editorTabs.ts
  - packages/renderer/src/panels/tre/VfsTree.tsx
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "Every UI-SPEC Surface 2 element (items 1-8, both editor anatomy and the B-state gate machine) is present — schema rail, hex-view toggle target, and the gate-bar/fail-banner WIRING to the real native round-trip (not a stub)."
    - "Save · run gate calls serializeDataTable then re-parses/byte-compares against the freshly-serialized bytes using the SAME assertRoundTrip mechanism the CORE-05 harness uses — a fail NEVER stages the file (hard invariant, not just UI copy)."
    - "Double-clicking a .iff file whose FORM tag is DTII in the TRE VFS browser opens DatatableGridEditor as a main-editor-group dockview tab (not the retired bottom-pane placeholder)."
  artifacts:
    - path: "packages/renderer/src/panels/editors/SchemaRail.tsx"
      provides: "Schema · COLS/TYPE, Selected row, Round-trip gate collapsible sections (250px right rail)"
      contains: "Schema"
    - path: "packages/renderer/src/shell/editorTabs.ts"
      provides: "openEditorTab(dockApi, {id, title, component, params}) — the shared dockview-tab-opening helper both DTII and .stf (05-09) use"
      contains: "openEditorTab"
  key_links:
    - from: "packages/renderer/src/panels/editors/DatatableGridEditor.tsx"
      to: "packages/native-core (native addon)"
      via: "addon.serializeDataTable(editedResult) -> bytes, re-parsed and byte-compared for the gate"
      pattern: "serializeDataTable"
    - from: "packages/renderer/src/panels/tre/VfsTree.tsx"
      to: "packages/renderer/src/shell/editorTabs.ts"
      via: "double-click on a DTII-tagged .iff entry calls openEditorTab(...)"
      pattern: "openEditorTab"
---

<objective>
Complete the DTII grid editor: the schema rail, the Grid|Hex toggle's real Hex-view target, the
gate-bar/fail-banner wired to the actual native round-trip (Save · run gate), staging integration, and
opening the editor as a dynamic main-editor-group dockview tab from the TRE VFS browser — retiring the
`DatatablePanel.tsx` placeholder.

Purpose: 05-06 built the grid's core interaction; this plan closes the remaining UI-SPEC Surface 2
elements (items 5-8) and makes the whole surface actually reachable and functional end-to-end,
completing DATA-01.

Output: `SchemaRail.tsx`, a completed `DatatableGridEditor.tsx` (hex toggle target + gate wiring +
staging), a new shared `editorTabs.ts` dockview-tab-opening helper, and `WorkspaceShell.tsx` +
`VfsTree.tsx` wiring to open it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-CONTEXT.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-UI-SPEC.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-PATTERNS.md
@.planning/phases/05-wysiwyg-live-sync-typed-editors/05-06-PLAN.md
</context>

<interfaces>
UI-SPEC Surface 2 items 5-8 (this plan's scope):
5. Schema rail (right, 250px, `--color-surface-2`, each section collapsible with rotating `▾`
   twisty): `Schema · COLS / TYPE` (one row per column: type badge + name + storage type, e.g.
   "ascii·z"/"int32"/"float32", faint) · `Selected row` (vertical key/value inspector of the selected
   row; modified values in warn; empty state `Click a row…`) · `Round-trip gate` (kv rows: `last run`
   (never/just now), `result` (—/✓ byte-exact/✗ mismatch @0x<off>), `bytes` (`DATA <N> B`)).
6. Hex view (toggle target): 3-column hex dump — faint offset gutter, bytes grouped 8+8, accent ASCII
   gutter — with the edited cell's bytes highlighted (warn tint) and caption `highlighted: <col>
   (<type>) row <n> — the cell you edit in Grid view is these bytes`.
7. Gate bar (footer): not run / running: `round-trip gate: re-encoding DTII…` (dashed, info) / pass:
   `✓ byte-exact round-trip (<N> B)` + `→ staged in working changes` chip, ALL modified marks clear /
   fail: `✗ round-trip mismatch — not staged` (danger), fail banner shows, file is NOT staged.
8. Fail banner: `✗ Round-trip gate FAILED at DATA+0x<offset> — expected <bytes>, wrote <bytes>. Not
   staged.` Actions: `Jump to bytes` (switches to Hex view, lands on the mismatching bytes) +
   `Revert cell`.

Existing HexInspector.tsx (reuse directly, do not build a second hex renderer) — full file already
read this session (PATTERNS.md): sticky ruler, selected-range highlight, hover cross-highlight,
`BYTES_PER_ROW=16`/`ROW_HEIGHT=18`, takes bytes + a `selectedRange` prop.

Existing stagingStore.ts entry-upsert pattern to reuse exactly (not re-derive):
```ts
addEntry: (e: StagingEntry) => {
  if (!isVirtualPathSafe(e.virtualPath)) return;
  set((state) => ({ entries: [...state.entries.filter((x) => x.virtualPath !== e.virtualPath), e] }));
},
```
`StagingEntry` shape (contracts/staging.ts): `{ virtualPath, action: 'add'|'modify'|'delete',
replacementFilePath?, sha256? }`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SchemaRail + Hex view toggle target</name>
  <files>packages/renderer/src/panels/editors/SchemaRail.tsx, packages/renderer/src/panels/editors/DatatableGridEditor.tsx</files>
  <read_first>
    - packages/renderer/src/panels/tre/ShadowChainDetail.tsx (full file — the fixed detail-panel-outside-the-scrolling-list pattern to mirror for the rail's three collapsible sections)
    - packages/renderer/src/panels/iff/HexInspector.tsx (full file — reuse directly; note its exact props signature for bytes + selectedRange)
    - 05-06-PLAN.md's DatatableGridEditor (the Grid|Hex toggle control already built there — this task wires the Hex branch's actual content)
  </read_first>
  <action>
    Create SchemaRail.tsx: a 250px right-side panel (`--color-surface-2` bg) with three collapsible
    sections (rotating `▾` twisty, mirroring ShadowChainDetail.tsx's collapse idiom), accepting props
    `{ columns: DataTableColumn[]; selectedRow: DataTableCell[] | null; gateState: {...} }`.
    `Schema · COLS / TYPE`: one row per column — type badge (reuse the exact badge component/colors
    from 05-06's grid header) + column name + a derived "storage type" label (`ascii` for String
    physical type, `int32` for Int, `float32` for Float — faint text, per UI-SPEC). `Selected row`:
    when `selectedRow` is null render `Click a row…` (faint, per Copywriting Contract); otherwise a
    vertical key/value list (column name : cell value), with modified cells' values rendered in warn
    color. `Round-trip gate`: kv rows `last run` (`never` / a formatted timestamp), `result` (`—` /
    `✓ byte-exact` / `✗ mismatch @0x<off>`), `bytes` (`DATA <N> B`) — driven by props from the gate
    state Task 2 wires up (this task only renders the display, Task 2 supplies real values). In
    DatatableGridEditor.tsx, mount SchemaRail to the right of the grid (flex row layout, grid flexes,
    rail is fixed 250px) and wire its `columns`/`selectedRow` props from the grid's existing column
    list and `selectedRowIndex` state (from 05-06). For the Hex view branch of the Grid|Hex toggle:
    when active, render `<HexInspector>` fed the RAW bytes of the currently-open .iff file's DATA
    chunk region (or the whole file if a simpler prop shape is more consistent with HexInspector's
    existing usage elsewhere — check its call sites) with a `selectedRange` computed from whichever
    cell was last double-click-edited (byte offset of that cell within the ROWS chunk — this requires
    tracking each cell's source byte offset from the parse result; if `parseDataTable`'s current
    return shape does not carry per-cell byte offsets, add that field to the native binding's return
    object in a follow-up note rather than fabricating offsets client-side) and a caption `highlighted:
    <col> (<type>) row <n> — the cell you edit in Grid view is these bytes`.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- SchemaRail DatatableGridEditor</automated>
  </verify>
  <acceptance_criteria>
    - SchemaRail renders `Click a row…` when selectedRow is null, and a populated kv list otherwise.
    - The Hex view branch renders the actual HexInspector component (not a placeholder div) and passes it real byte data.
    - All three SchemaRail sections are independently collapsible (clicking one twisty does not affect the others' expanded state).
  </acceptance_criteria>
  <done>SchemaRail and the Hex view toggle target exist and are wired to the grid's live selection state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate-bar wiring to the real native round-trip + staging integration</name>
  <files>packages/renderer/src/panels/editors/DatatableGridEditor.tsx</files>
  <behavior>
    - Clicking `Save · run gate` transitions the gate to 'running', calls
      `addon.serializeDataTable(currentEditBuffer)`, then re-parses the resulting bytes via
      `addon.parseDataTable` and byte-compares the re-serialized output against itself for stability
      (mirrors `assertRoundTrip`'s logic, run client-side against the SAME native functions the
      CORE-05 harness uses — not a re-implemented comparator).
    - On byte-exact success: gate transitions to 'pass', a `stagingStore.addEntry(...)` call is made
      with the newly-serialized bytes as the replacement file, ALL modified-cell/row/tab marks clear,
      and the `→ staged in working changes` chip appears.
    - On mismatch: gate transitions to 'fail', FailBanner renders naming the first differing byte
      offset + expected/got bytes, and — this is the hard invariant — `stagingStore.addEntry` is
      NEVER called on this path (test this by asserting the staging mock has zero calls after a
      forced-fail scenario).
  </behavior>
  <read_first>
    - packages/harness/assertRoundTrip.ts (full file — the exact byte-diff diagnostic format to mirror for the FailBanner message, so the UI's offset/hex-window language matches the CORE-05 harness's own diagnostics)
    - packages/renderer/src/state/stagingStore.ts (full file — addEntry's exact signature and path-safety gate, reused not re-derived)
    - This plan's Task 1 output (SchemaRail's gate-state props) and 05-06's GateBar/FailBanner components
  </read_first>
  <action>
    Wire `Save · run gate`'s click handler in DatatableGridEditor.tsx: build the edited
    `DataTableResult` shape from current grid state (columns + rows, reflecting all in-memory
    edits/added/removed rows), call `addon.serializeDataTable(edited)` to get bytes, then call
    `addon.parseDataTable(addon.parseIff(bytes), bytes)` and re-`serializeDataTable` that to prove
    stability, byte-comparing the two serialize outputs (reuse `assertRoundTrip`'s comparison logic —
    import it from `@swg/harness` if that package is consumable from the renderer, or port its exact
    byte-scan+hex-window logic inline if a cross-package import is not viable; do not write a looser
    ad-hoc comparator). Update the GateBar's `state` prop through 'running' then 'pass'/'fail'
    accordingly. On pass: call `useStagingStore.getState().addEntry({ virtualPath: currentFilePath,
    action: 'modify', replacementFilePath: <a temp file written with the serialized bytes, OR pass
    bytes directly if StagingEntry / packPatch supports in-memory replacement — check existing
    staging flow for the exact mechanism DeployPanel's other editors use and match it exactly, do not
    invent a new staging mechanism>, sha256: <computed the same way StagingPanelBody's
    computeSha256 helper does> })`, clear all modified-state tracking (cell/row/tab), and update
    SchemaRail's Round-trip gate section (`last run: just now`, `result: ✓ byte-exact`, `bytes: DATA
    <N> B`). On fail: render FailBanner with the exact copy `✗ Round-trip gate FAILED at DATA+0x
    <offset> — expected <bytes>, wrote <bytes>. Not staged.`, wire its `Jump to bytes` action to
    switch to Hex view with `selectedRange` at the mismatching offset, and `Revert cell` to undo the
    most recently edited cell (grid state already tracks this from 05-06's modified-cell logic).
    Wire `＋ Stage` (the ghost button, distinct from the gate's auto-stage-on-pass) to immediately
    run the SAME gate-then-stage flow as `Save · run gate` (per UI-SPEC, both actions lead to the
    identical staged-or-not outcome — `＋ Stage` is not a separate weaker path).
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- DatatableGridEditor</automated>
  </verify>
  <acceptance_criteria>
    - A forced-mismatch test (mock serializeDataTable to return corrupted bytes on the second call) asserts stagingStore.addEntry is called ZERO times and the gate state is 'fail'.
    - A clean round-trip test asserts stagingStore.addEntry is called EXACTLY once and the gate state is 'pass'.
    - The FailBanner message format matches assertRoundTrip.ts's hex-window diagnostic style (offset in 0x-prefixed hex, expected/actual byte rows).
  </acceptance_criteria>
  <done>Save · run gate is a real re-encode-and-prove-byte-exactness operation, never a bare "save"; pass stages, fail never does — the CORE-05 standing gate is surfaced as UI, not just documented.</done>
</task>

<task type="auto">
  <name>Task 3: Dockview tab opening + VfsTree wiring + retire DatatablePanel placeholder</name>
  <files>packages/renderer/src/shell/editorTabs.ts, packages/renderer/src/shell/WorkspaceShell.tsx, packages/renderer/src/panels/tre/VfsTree.tsx</files>
  <read_first>
    - packages/renderer/src/shell/WorkspaceShell.tsx (the dockview panel-component registration map + LAYOUT_VERSION guard convention — find the exact registration block for existing panels like DatatablePanel/LiveInspectorPanel to mirror and to REMOVE the retired one from)
    - packages/renderer/src/panels/tre/VfsTree.tsx (full file — the double-click / row-action handler where "open in an editor" wiring belongs; the existing Extract→Add staging action as the closest analog for "an action driven from a VFS row")
    - dockview-react's addPanel/addGroup API surface used elsewhere in this codebase for opening a dynamic panel into the main editor group (grep for any existing per-file dynamic-tab precedent; if none exists, this is a new pattern this task establishes for editorTabs.ts to own)
  </read_first>
  <action>
    Create editorTabs.ts exporting `openEditorTab(dockApi, { id, title, component, params }):
    void` — a small shared helper that checks whether a panel with `id` already exists in the dockview
    api (activate it if so, no duplicate tabs for the same file) and otherwise calls the dockview
    api's panel-add method targeting the main editor group, registering it under `component` (the
    dockview component key already mapped to `DatatableGridEditor` in WorkspaceShell.tsx's component
    registry) with `params`. This helper is intentionally generic — 05-09's `.stf` editor calls the
    SAME function with its own id/component/params, which is why this file lives in `shell/` rather
    than `panels/editors/`. In WorkspaceShell.tsx: register `'datatable-grid-editor'` in the dockview
    component map pointing at `DatatableGridEditor`, and REMOVE the old `'datatable'` panel
    registration for `DatatablePanel.tsx` from the persistent bottom-pane layout (the 008-S8 trio
    becomes `Console | Log` only — bump `LAYOUT_VERSION` following the exact precedent already used
    for prior panel-id swaps, e.g. the 1→2 bump documented in STATE.md for the deploy-panel swap, so
    existing users' saved layouts migrate cleanly instead of referencing a dead panel id). Delete
    `packages/renderer/src/panels/DatatablePanel.tsx` (superseded — UI-SPEC Flagged Assumption 2
    resolves to "retire", since main-editor-group tabs are the locked anatomy and the bottom-pane
    placeholder has no room for the full Surface 2 anatomy). In VfsTree.tsx: add a double-click handler
    (or extend the existing row action affordance) on `.iff` entries whose resolved FORM tag is
    `DTII` (resolve this via the existing IFF-peek mechanism already used elsewhere in this codebase
    for format detection — do not add a second detection mechanism) that calls
    `openEditorTab(dockApi, { id: 'dtii:' + virtualPath, title: fileName + ' — Datatable', component:
    'datatable-grid-editor', params: { virtualPath, iffResult, dataTableResult } })` after resolving
    and parsing the entry's bytes via the existing `readMountEntry` + `parseIff` +
    `parseDataTable` chain.
  </action>
  <verify>
    <automated>pnpm --filter renderer test -- editorTabs WorkspaceShell VfsTree</automated>
  </verify>
  <acceptance_criteria>
    - `packages/renderer/src/panels/DatatablePanel.tsx` no longer exists (grep confirms).
    - WorkspaceShell.tsx's LAYOUT_VERSION constant is bumped from its current value.
    - Double-clicking a DTII-tagged .iff row in VfsTree opens exactly one tab per virtualPath (a second double-click on the same file activates the existing tab, verified by a test asserting the dockview api's add-panel is called only once across two double-clicks on the same path).
  </acceptance_criteria>
  <done>DatatableGridEditor is reachable from the VFS browser as a real main-editor-group tab, and the superseded placeholder is gone — DATA-01's editor is fully wired end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Edited grid state → native serializeDataTable → stagingStore | The gate is the sole gatekeeper between an in-memory edit and anything reaching disk/deploy; no code path may call stagingStore.addEntry without first passing the byte-exact re-encode proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | Tampering | DatatableGridEditor Save · run gate | mitigate | `stagingStore.addEntry` has exactly one call site in this file, inside the gate-pass branch — enforced by the acceptance-criteria test asserting zero staging calls on a forced-fail path. |
| T-05-22 | Tampering | VfsTree staged file path | mitigate | The staged replacement path/virtualPath is derived from the already-resolved, already-validated VFS entry path (isVirtualPathSafe already gates stagingStore.addEntry per the existing shared validator) — no new path-construction logic is introduced here. |
</threat_model>

<verification>
Run `pnpm --filter renderer test -- SchemaRail DatatableGridEditor editorTabs WorkspaceShell VfsTree`
— all green.
</verification>

<success_criteria>
- All 8 UI-SPEC Surface 2 elements are present and functional.
- Save · run gate is a real byte-exact proof, never staging on failure.
- The editor opens as a real dockview tab from the VFS browser; the placeholder is retired.
</success_criteria>

<output>
Create `.planning/phases/05-wysiwyg-live-sync-typed-editors/05-08-SUMMARY.md` when done
</output>
