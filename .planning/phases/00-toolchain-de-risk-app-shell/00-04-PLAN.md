---
phase: 00-toolchain-de-risk-app-shell
plan: 04
type: execute
wave: 3
depends_on:
  - 00-01
  - 00-03
files_modified:
  - packages/renderer/src/main.tsx
  - packages/renderer/src/App.tsx
  - packages/renderer/src/index.css
  - packages/renderer/src/themes/default.css
  - packages/renderer/src/themes/cyan.css
  - packages/renderer/src/themes/swg-green.css
  - packages/renderer/src/themes/amber.css
  - packages/renderer/src/themes/blue.css
  - packages/renderer/src/themes/high-contrast.css
  - packages/renderer/src/workspace/WorkspaceShell.tsx
  - packages/renderer/src/workspace/workspace-config.ts
  - packages/renderer/src/panels/SidebarPanel.tsx
  - packages/renderer/src/panels/ViewportPanel.tsx
  - packages/renderer/src/panels/InspectorPanel.tsx
  - packages/renderer/src/panels/DataPanel.tsx
  - packages/renderer/src/shell/Titlebar.tsx
  - packages/renderer/src/shell/StatusBar.tsx
autonomous: false
requirements:
  - FND-05

must_haves:
  truths:
    - "App renders with #181818 background (--color-bg) and no white flash on load"
    - "DockviewReact renders four panels: Assets (left), Viewport (center), Inspector (right), Data (bottom)"
    - "Default layout is 240px left / 1fr center / 280px right / 200px bottom — set via addPanel initialWidth/initialHeight (240/280) and bottom group initialHeight 200"
    - "Dockview layout serializes to localStorage on change and restores from it on reload"
    - "Theme picker in titlebar persists the active theme to localStorage key swg-active-theme"
    - "Swapping theme updates --color-accent and related tokens immediately without reload"
    - "Every icon-only control listed in UI-SPEC Rule 5 has BOTH aria-label and title (audited against the Rule 5 table, not a line-count heuristic)"
    - "StatusBar is the SINGLE owner of the SAB test hooks: it sets window.__sabValue (0xDEAD it read), window.__sabIsShared, window.__crossWriteOk, window.__zeroCopy, window.__transport via the IN-PROCESS Path B addon (require @swg/native-core) — ViewportPanel does NOT set these [PATH B ADAPTATION: replaces old onSabPort IPC model]"
    - "StatusBar shows live values for crossOriginIsolated, SAB, and addon status"
    - "StatusBar performs the in-process NONCE cross-write proof: nativeCore.writeSab(sab,0,0xDEAD) → C++→JS; renderer writes per-run nonce → nativeCore.readSab() === nonce (JS→C++); Worker reads 0xDEAD (intra-cluster) [PATH B ADAPTATION: replaces old crossWriteSab() IPC call]"
    - "StatusBar distinguishes zero-copy states: shared (nonce matched) vs copy (mismatch) vs error (addon threw) — 'error' is NOT 'copy'"
    - "Panel collapse toggle hides panel body and changes glyph from down-chevron to right-chevron"
    - "ViewportPanel shows Phase 0 placeholder text and reads the wiring-proof status from a shared source (not by owning the SAB hook)"
  artifacts:
    - path: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      provides: "DockviewReact wrapper with onReady persistence"
      contains: "fromJSON"
    - path: "packages/renderer/src/workspace/workspace-config.ts"
      provides: "INITIAL_LAYOUT (with initialWidth/initialHeight), LAYOUT_STORAGE_KEY, THEME_STORAGE_KEY, panelComponents"
    - path: "packages/renderer/src/themes/default.css"
      provides: "CSS custom property token definitions (all --color-*, --space-*, --text-*, --font-*)"
    - path: "packages/renderer/src/shell/StatusBar.tsx"
      provides: "Live wiring-proof indicators + SINGLE owner of window.__sabValue / __sabIsShared / __crossWriteOk test hooks + NONCE cross-write driver"
    - path: "packages/renderer/src/shell/Titlebar.tsx"
      provides: "App name, menu bar, theme picker, window controls"
  key_links:
    - from: "packages/renderer/src/App.tsx"
      to: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      via: "JSX component render"
      pattern: "<WorkspaceShell"
    - from: "packages/renderer/src/workspace/WorkspaceShell.tsx"
      to: "localStorage"
      via: "api.toJSON() / api.fromJSON() in onReady + onDidLayoutChange"
      pattern: "toJSON|fromJSON"
    - from: "packages/renderer/src/App.tsx"
      to: "data-theme attribute"
      via: "document.documentElement.dataset.theme from localStorage swg-active-theme"
      pattern: "swg-active-theme"
    - from: "packages/renderer/src/shell/StatusBar.tsx"
      to: "require('@swg/native-core') → allocateSab/writeSab/readSab in-process"
      via: "React useEffect — single owner of SAB hooks + in-process Path B nonce proof"
      pattern: "allocateSab|readSab|__sabValue"
      note: "PATH B ADAPTATION: old onSabPort/crossWriteSab IPC model DELETED (utility-worker.ts removed in 00-03)"
---

<objective>
Build the dark dockable workspace shell (FND-05): React renderer entry, DockviewReact four-panel layout with persistence and explicit panel sizing, the CSS token system from UI-SPEC.md, the four panel components with their Phase 0 seed states, the Titlebar with theme picker, and the StatusBar — which is the single owner of the SAB test hooks and the in-process Path B zero-copy proof.

PATH B ADAPTATION (executed 2026-06-22): The original plan described the old cross-process model (onSabPort IPC / crossWriteSab() IPC / utility-process SAB relay). That model is DELETED — utility-worker.ts was removed in Plan 00-03. The StatusBar was adapted to use the IN-PROCESS Path B transport: require('@swg/native-core') directly in the renderer (nodeIntegration:true), run allocateSab/writeSab/readSab in-process, prove same-memory with a per-run nonce. The shell deliverables (Dockview 4-panel layout, 5 themes, persistence, Titlebar, Rule 5 aria-labels) are UNCHANGED.

Purpose: Delivers the visible product of Phase 0 — a working dark tool shell with dockable, persistent panels. The StatusBar exposes the wiring proof (crossOriginIsolated, SAB, zero-copy) visually and owns the E2E test hooks. The five themes, the Rule 5 aria-label set, the statusbar content, and the gizmo are locked deliverables in the approved 00-UI-SPEC.md (Design Contract).

Output: 17 renderer source files; bootable dark dockable shell; theme system; layout + theme persistence; StatusBar consuming the SAB port from Plan 03 and driving the nonce cross-write proof (and distinguishing shared / copy / timeout).
</objective>

<execution_context>
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-CONTEXT.md
</execution_context>

<context>
@D:/Code/SWG-Toolkit/.planning/PROJECT.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-03-SUMMARY.md

<interfaces>
window.api — from Plan 03's preload.ts:
  hello: () => Promise<string>
  onSabPort: (cb: (sab: SharedArrayBuffer) => void) => void
  crossWriteSab: () => Promise<number>   // ARGLESS (review fix MEDIUM-4): the renderer wrote its nonce into Int32[1] itself; this asks the utility to re-read & ack. The nonce is NEVER sent over IPC.

SAB_LAYOUT — from Plan 01 contracts/:
  import { SAB_LAYOUT } from '@swg/contracts'
  SAB_LAYOUT.HELLO_SENTINEL.offset === 0     // byte 0: C++ wrote 0xDEAD; renderer reads
  SAB_LAYOUT.RENDERER_SENTINEL.offset === 4  // byte 4: renderer writes a PER-RUN NONCE; utility re-reads (nonce cross-write proof)

dockview API (RESEARCH Pattern 4): import { DockviewReact, DockviewReadyEvent, DockviewApi } from 'dockview-react'
  event.api.fromJSON(parsed) / event.api.toJSON() / event.api.onDidLayoutChange()
  addPanel supports initialWidth / initialHeight (review LOW / Cursor — set explicit 240/280/200 px sizing)

panelComponents = { sidebar: SidebarPanel, viewport: ViewportPanel, data: DataPanel, inspector: InspectorPanel }

UI-SPEC token system (ALL values from 00-UI-SPEC.md §Color/§Spacing/§Typography — do not invent):
  --color-bg #181818  --color-surface #242424  --color-surface-2 #2b2b2b  --color-header #323232
  --color-header-2 #3a3a3a  --color-widget #3a3a3a  --color-widget-hover #474747
  --color-border #121212  --color-border-soft #3c3c3c
  --color-text #e6e6e6  --color-text-muted #9a9a9a  --color-text-faint #6f6f6f
  --color-accent #38bec9  --color-accent-hover #5ad6e0  --color-accent-text #04201f
  --color-accent-dim rgba(56,190,201,0.16)  --color-accent-line rgba(56,190,201,0.45)
  --color-danger #e0584f  --color-warn #e0a13a  --color-info #4a8cff
  --space-1 3px ... --space-8 24px   --text-xs 11px --text-sm 12px --text-base 13px --text-md 14px
  --font-mono 'JetBrains Mono',... --font-sans Inter,...
  --titlebar-h 30px --tabstrip-h 30px --statusbar-h 22px --activity-w 46px
  --radius-sm 3px --radius-md 6px --shadow-panel 0 2px 8px rgba(0,0,0,0.35)
  --focus-ring 0 0 0 1px var(--color-accent), 0 0 0 3px var(--color-accent-dim)
  --drop-fill rgba(56,190,201,0.18)  --drop-line rgba(56,190,201,0.85)

Theme swap: data-theme attribute on <html>; each non-default theme is a [data-theme="..."] block in index.css.
Storage keys: 'swg-workspace-layout' (dockview JSON), 'swg-active-theme' (cyan|swg-green|amber|blue|high-contrast).
Five themes are LOCKED by 00-UI-SPEC.md Accessibility Rule 3 (cyan default + swg-green + amber + blue + high-contrast).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS token system — default.css, theme overrides, Tailwind v4 entry, dockview theme</name>
  <files>
    packages/renderer/src/index.css
    packages/renderer/src/themes/default.css
    packages/renderer/src/themes/cyan.css
    packages/renderer/src/themes/swg-green.css
    packages/renderer/src/themes/amber.css
    packages/renderer/src/themes/blue.css
    packages/renderer/src/themes/high-contrast.css
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
    (sections: Design System, Spacing Scale, Typography, Color — full token reference,
     Accessibility §Rule 2 + §Rule 3 + §Rule 4, Theme Persistence Contract, High-Contrast Theme Overrides)
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
    (section: Pattern 4 §Tailwind v4 dark theme overlay CSS)
  </read_first>
  <action>
    Create the CSS token system. ALL token values come from 00-UI-SPEC.md §Color/§Spacing/§Typography — do not invent values.

    themes/default.css: define all custom properties on :root (every token in the interfaces block + --font-mono, --font-sans, all --space-*, --text-*, --radius-*, --shadow-panel, --focus-ring, --drop-fill, --drop-line, chrome heights). Single source of truth for token values.

    themes/cyan.css: a single comment "/* cyan = default — no overrides */" (default.css IS cyan).

    themes/swg-green.css: override accent tokens only — --color-accent #3cc24a; --color-accent-hover #5adb68; --color-accent-text #0a1f0a; --color-accent-dim rgba(60,194,74,0.16); --color-accent-line rgba(60,194,74,0.45).
    themes/amber.css: --color-accent #e0a13a; --color-accent-hover #f0b74d; --color-accent-text #1a0f00; --color-accent-dim rgba(224,161,58,0.16); --color-accent-line rgba(224,161,58,0.45).
    themes/blue.css: --color-accent #4a8cff; --color-accent-hover #6fa3ff; --color-accent-text #00092e; --color-accent-dim rgba(74,140,255,0.16); --color-accent-line rgba(74,140,255,0.45).
    themes/high-contrast.css: full override from 00-UI-SPEC.md §High-Contrast Theme Overrides — --color-bg #000000; --color-surface #101012; --color-border #7a7a82; --color-border-soft #5a5a62; --color-text #ffffff; --color-text-muted #dcdce2; --color-text-faint #aeaeb6; --color-accent #4fe3f0; --focus-ring 0 0 0 2px #000, 0 0 0 4px var(--color-accent).
    (Five themes are the locked 00-UI-SPEC Rule 3 deliverable — keep all five.)

    index.css:
      @import "tailwindcss";
      @import "dockview-react/dist/styles/dockview.css";
      @import "./themes/default.css";
    Then [data-theme="swg-green"]{...}, [data-theme="amber"]{...}, [data-theme="blue"]{...}, [data-theme="high-contrast"]{...} blocks (paste each theme's tokens; cyan needs no block).
    Then .dockview-theme-dark overrides mapping --dv-* to the token vars (RESEARCH Pattern 4).
    body/html reset: margin 0; padding 0; background var(--color-bg); color var(--color-text); font-family var(--font-sans); height 100%; overflow hidden.
    Global focus ring (Accessibility Rule 2): *:focus-visible { outline: none; box-shadow: var(--focus-ring); }
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && grep -c "color-bg\|color-surface\|color-accent" packages/renderer/src/themes/default.css && grep -c "data-theme" packages/renderer/src/index.css && grep "focus-ring" packages/renderer/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - default.css defines all tokens from the interfaces block (grep --color-bg, --color-accent, --space-1, --font-mono, --focus-ring, --drop-fill)
    - index.css imports tailwindcss, dockview CSS, and default.css
    - [data-theme="swg-green"], [data-theme="amber"], [data-theme="blue"], [data-theme="high-contrast"] blocks present in index.css
    - high-contrast.css overrides --color-bg to #000000 and --focus-ring to the thicker 4px version
    - .dockview-theme-dark overrides --dv-background-color to var(--color-bg)
    - *:focus-visible applies box-shadow: var(--focus-ring) globally
  </acceptance_criteria>
  <done>CSS token system complete; all 5 (locked) themes defined; dockview dark theme overridden; focus ring global; Tailwind v4 entry wired.</done>
</task>

<task type="auto">
  <name>Task 2: Structural shell — WorkspaceShell, workspace-config (sized panels), four panel stubs, persistence</name>
  <files>
    packages/renderer/src/workspace/WorkspaceShell.tsx
    packages/renderer/src/workspace/workspace-config.ts
    packages/renderer/src/panels/SidebarPanel.tsx
    packages/renderer/src/panels/ViewportPanel.tsx
    packages/renderer/src/panels/InspectorPanel.tsx
    packages/renderer/src/panels/DataPanel.tsx
    packages/renderer/src/main.tsx
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
    (sections: Layout Contract, Panel Content Contracts — all four panels, Component Inventory, Interaction §collapse)
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
    (section: Pattern 4 — DockviewReact onReady/fromJSON/toJSON, Pitfall 5 — register all components before fromJSON)
  </read_first>
  <action>
    Create the structural shell. Titlebar, StatusBar, and App.tsx (theme persistence + the SAB hooks) are Task 3.

    workspace-config.ts: export LAYOUT_STORAGE_KEY = 'swg-workspace-layout', THEME_STORAGE_KEY = 'swg-active-theme'. export THEMES = ['cyan','swg-green','amber','blue','high-contrast'] as const; type ThemeName = typeof THEMES[number]. export panelComponents mapping all four ids (registered before fromJSON — Pitfall 5). export INITIAL_LAYOUT(api) that runs the addPanel calls with EXPLICIT sizing (review LOW / Cursor): sidebar { direction:'left', initialWidth: 240 }, viewport (center, fills 1fr), data { direction:'below', referencePanel:'viewport', initialHeight: 200 }, inspector { direction:'right', initialWidth: 280 }.

    WorkspaceShell.tsx: import DockviewReact, DockviewReadyEvent, DockviewApi from 'dockview-react'. useRef<DockviewApi|null>. onReady: store api; read LAYOUT_STORAGE_KEY; if present wrap api.fromJSON(JSON.parse(saved)) in try/catch falling back to INITIAL_LAYOUT(api); else INITIAL_LAYOUT(api). Register api.onDidLayoutChange(() => localStorage.setItem(LAYOUT_STORAGE_KEY, JSON.stringify(api.toJSON()))). Render <DockviewReact className="dockview-theme-dark" components={panelComponents} onReady={onReady} style={{ flex:1, minHeight:0 }} />.

    SidebarPanel.tsx (Assets): header tab "Assets"; actions Split (⊟ aria-label/title "Split panel"), Collapse (▾ aria-label/title "Collapse panel"). Body seed: "No archive mounted" + "File → Open Archive to browse assets". Collapse: useState(false); toggle hides body and swaps glyph to ▸ (aria-label/title "Expand panel"). Code comment: // Accessibility Rule 5: aria-label + title on all icon-only controls.

    ViewportPanel.tsx: header tab "Viewport" with × close (aria-label/title "Close tab"); action Maximize ⤢ (aria-label/title "Maximize panel"). Body radial gradient (#2a2e26 -> #141414) + 28x28 dot overlay. DISPLAY-ONLY wiring status: ViewportPanel does NOT own or set window.__sabValue (StatusBar owns it — Task 3). It reads a shared status via a small Zustand store or a window.__wiringStatus object that StatusBar populates, OR simply renders props passed down. Show: if wiring proven -> "crossOriginIsolated ✓" / "SAB round-trip: PASS" / "native-core: utility-process ✓" (--color-accent); else "Viewport — Phase 0" / "3D canvas ready" / "Waiting for asset". Chips top-left Solid|Wire|Textured (stateful). Chips top-right ⟲ Orbit / ✥ Pan / ⛶ Frame (each aria-label/title per Rule 5). Stats overlay bottom-left "persp · {W}×{H} · — fps · SAB {state}" (--font-mono --text-xs). Gizmo bottom-right 48x48 SVG axes (Y-up #7ec94e, X #e0584f, Z #4a8cff) — locked by 00-UI-SPEC §Viewport. Code comment: // window.__sabValue is owned by StatusBar (single owner) — do not set it here.

    InspectorPanel.tsx: header "Inspector"; action Collapse ▾ (aria-label/title "Collapse panel"). Body seed: "No selection" + "Select an asset in the Assets panel". Rule 5 comment.

    DataPanel.tsx: header tabs Datatable|Console|Log|+ (+ aria-label/title "Add tab"); stateful active tab. Datatable seed: "[no datatable loaded]" + "Mount an archive and open a .iff datatable" (--font-mono --text-xs). Console: the five Phase 0 seed lines from 00-UI-SPEC §Console seed state with correct token coloring (timestamps --color-text-faint, success tokens --color-accent). Log: "[log]  contracts/ types compiled · renderer + backend in sync".

    main.tsx: ReactDOM.createRoot(document.getElementById('root')!).render(<StrictMode><App/></StrictMode>). Import React, ReactDOM, App (from ./App — created in Task 3), and index.css.
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && grep "fromJSON" packages/renderer/src/workspace/WorkspaceShell.tsx && grep "toJSON" packages/renderer/src/workspace/WorkspaceShell.tsx && grep "initialWidth\|initialHeight" packages/renderer/src/workspace/workspace-config.ts && grep "LAYOUT_STORAGE_KEY\|THEME_STORAGE_KEY" packages/renderer/src/workspace/workspace-config.ts && test "$(grep -c '__sabValue' packages/renderer/src/panels/ViewportPanel.tsx)" = "0" && echo "viewport does not own sab hook"</automated>
  </verify>
  <acceptance_criteria>
    - WorkspaceShell.tsx calls api.fromJSON() in onReady and api.toJSON() in onDidLayoutChange, with try/catch + INITIAL_LAYOUT fallback
    - workspace-config.ts defines LAYOUT_STORAGE_KEY, THEME_STORAGE_KEY, panelComponents (all four ids), and INITIAL_LAYOUT with explicit initialWidth/initialHeight (240/280/200)
    - All four panel files exist with seed/empty state content from 00-UI-SPEC copywriting
    - SidebarPanel and InspectorPanel have a collapse toggle with chevron glyph swap
    - ViewportPanel does NOT set window.__sabValue (grep -c returns 0) — StatusBar is the single owner (Task 3)
    - All icon-only controls in panels have aria-label AND title attributes
    - DataPanel Console tab shows the five Phase 0 seed lines with correct copy
    - main.tsx imports App and renders with StrictMode
  </acceptance_criteria>
  <done>WorkspaceShell with dockview persistence + explicitly sized panels; four panel stubs with seed states + collapse; ViewportPanel is display-only for SAB status; main.tsx wired.</done>
</task>

<task type="auto">
  <name>Task 3: Interactive chrome — Titlebar (theme picker), StatusBar (SAB hooks + NONCE cross-write), App.tsx</name>
  <files>
    packages/renderer/src/App.tsx
    packages/renderer/src/shell/Titlebar.tsx
    packages/renderer/src/shell/StatusBar.tsx
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
    (sections: Titlebar, Status Bar, Theme Persistence Contract, Accessibility Rules 1–5 incl. the Rule 5 control table, Copywriting)
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-03-SUMMARY.md
    (window.api.crossWriteSab signature — ARGLESS — + nonce cross-write proof semantics + any cross-write FINDING recorded by Plan 03's architecture gate)
    packages/renderer/src/workspace/workspace-config.ts (THEME_STORAGE_KEY, ThemeName, THEMES)
  </read_first>
  <action>
    Create the interactive chrome. StatusBar is the SINGLE owner of the SAB test hooks and drives the renderer side of the NONCE cross-write proof (review fix HIGH-4b — single owner is the component that receives onSabPort; review fix MEDIUM-4 — per-run nonce, NOT the 0xBEEF literal).

    App.tsx: functional component. State activeTheme (ThemeName). On mount: read THEME_STORAGE_KEY (default 'cyan'); set document.documentElement.dataset.theme synchronously before first paint (no accent flash). handleThemeChange: update state + dataset.theme + localStorage. Render full-height flex column (100vh, column, overflow hidden): <Titlebar activeTheme onThemeChange/>, <WorkspaceShell/> (flex 1, minHeight 0), <StatusBar/>.

    Titlebar.tsx: props activeTheme: ThemeName, onThemeChange. Per 00-UI-SPEC §Titlebar: logo 16x16 gradient accent->#3a7a1f; app name "SWG Toolkit" (--text-sm w600, letter-spacing .03em); menu File|Edit|View|Asset|Window|Help (visual chrome, no dropdowns in Phase 0); theme picker ◐ glyph + <select> over THEMES (aria-label/title "Select theme"), value=activeTheme, onChange=onThemeChange(e.target.value as ThemeName); window controls three 13x13 circles (minimize #e0a13a aria-label/title "Minimize window", maximize #7ec94e "Maximize window", close #e0584f "Close window"). CSS height var(--titlebar-h); background #1c1c1c; border-bottom 1px var(--color-border); -webkit-app-region: drag on container, no-drag on picker + window controls. Rule 5 comment.

    StatusBar.tsx — Phase 0 wiring-proof surface AND the single owner of the SAB hooks + NONCE cross-write driver. State: crossOriginIsolated, sabValue (number|null), crossWriteState ('pending'|'shared'|'copy'|'error'|null), addonStatus, helloValue. On mount (useEffect [], import { SAB_LAYOUT } from '@swg/contracts'):
      setCrossOriginIsolated(self.crossOriginIsolated)
      window.api.hello().then(setHelloValue)
      window.api.onSabPort(async (sab) => {
        const view = new Int32Array(sab)
        const helloRead = view[SAB_LAYOUT.HELLO_SENTINEL.offset / 4]   // expect 0xDEAD
        setSabValue(helloRead); setAddonStatus('utility-process ✓')
        ;(window as any).__sabValue = helloRead                         // TEST HOOK (single owner) for e2e/03-sab-roundtrip
        ;(window as any).__sabIsShared = (sab instanceof SharedArrayBuffer)  // TEST HOOK: instanceof assertion source
        // NONCE CROSS-WRITE PROOF (review fix HIGH-3 + MEDIUM-4): write a per-run RANDOM nonce, then ask the
        // utility to re-read it. Do NOT pass the nonce to crossWriteSab() (it is argless) — the utility re-reads
        // view[1]. Asserting echoed === nonce closes the echo-of-arg false-pass: a copy-only utility that echoed
        // an IPC arg cannot match a nonce it was never told.
        const nonce = (Math.floor(Math.random() * 0x7fffffff)) | 0     // per-run, never sent over IPC
        view[SAB_LAYOUT.RENDERER_SENTINEL.offset / 4] = nonce
        try {
          const echoed = await window.api.crossWriteSab()              // ARGLESS — utility re-reads view[1]
          const ok = (echoed === nonce)
          setCrossWriteState(ok ? 'shared' : 'copy')                   // shared vs copy (review fix LOW)
          ;(window as any).__crossWriteOk = ok                          // TEST HOOK (single owner) for e2e/03-sab-roundtrip
          ;(window as any).__crossWriteState = ok ? 'shared' : 'copy'   // TEST HOOK: triage detail
        } catch (e) {
          setCrossWriteState('error')                                  // timeout / no ack — DISTINCT from copy (review fix LOW)
          ;(window as any).__crossWriteOk = false
          ;(window as any).__crossWriteState = 'error'                  // a rejected/timed-out cross-write is NOT a copy
        }
      })
    Add // TEST HOOK comments on the four window.* assignments. These hooks live ONLY here (StatusBar) — the single owner. NOTE the distinction (review fix LOW / Opus): __crossWriteState is 'shared' | 'copy' | 'error' — a timeout/rejection ('error') means "no ack (port/crash/throws)" and must NOT be reported as a copy; both set __crossWriteOk false but the triage path differs.
    Render (left->right, --font-mono --text-xs --color-text-muted, padding 0 12px, gap 16px, height var(--statusbar-h)): selection placeholder "shared_landspeeder.msh" · "4,812 verts" (locked statusbar seed per 00-UI-SPEC §Status Bar) · "addon: " + accent addonStatus or "loading…" · "crossOriginIsolated: " + accent/danger boolean · "SAB: " + accent "60 fps" when sabValue!=null else "—" · "cross-write: " + (crossWriteState===null||crossWriteState==='pending' ? "…" : crossWriteState==='shared' ? accent "shared ✓" : crossWriteState==='copy' ? danger "copy ✗" : warn "no ack ✗") · right-aligned "pnpm · contracts " + accent "✓". Background #1c1c1c; border-top 1px var(--color-border).

    REAL ARIA AUDIT (replaces the prior wc -l >= 10 theater gate — review MEDIUM / Sonnet): after writing the three files, verify EACH control named in 00-UI-SPEC Rule 5's table has both aria-label and title. Do this by enumerating the required labels (Split panel, Collapse/Expand panel, Maximize panel, Close panel, Close tab, Minimize/Maximize/Close window, Orbit/Pan/Frame camera, Select theme, Add tab) and grepping that each exact aria-label string is present across the renderer source. The gate is presence-of-each-required-label, NOT a count.
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && pnpm --filter @swg/renderer exec tsc --noEmit 2>&1 | grep -iE "error" | head -10; for L in "Split panel" "Collapse panel" "Maximize panel" "Close tab" "Minimize window" "Maximize window" "Close window" "Orbit camera" "Pan camera" "Frame selection" "Select theme"; do grep -rqF "aria-label=\"$L\"" packages/renderer/src || echo "MISSING aria-label: $L"; done; grep "dataset.theme\|data-theme" packages/renderer/src/App.tsx && grep -E "nonce|crossWriteState|__crossWriteOk" packages/renderer/src/shell/StatusBar.tsx && test "$(grep -c '0xBEEF' packages/renderer/src/shell/StatusBar.tsx)" = "0" && echo "no-hardcoded-beef-uses-nonce"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @swg/renderer exec tsc --noEmit exits 0
    - App.tsx sets document.documentElement.dataset.theme on mount from localStorage before first paint
    - Titlebar.tsx renders the theme <select> with aria-label="Select theme" and title="Select theme"
    - StatusBar is the ONLY file that assigns window.__sabValue, window.__sabIsShared, window.__crossWriteOk, and window.__crossWriteState (single owner — grep these in StatusBar returns matches; grep in ViewportPanel returns 0)
    - StatusBar reads self.crossOriginIsolated; calls window.api.hello(); calls window.api.onSabPort; writes a PER-RUN RANDOM nonce (NOT the 0xBEEF literal — grep -c '0xBEEF' returns 0) into view[1]; calls window.api.crossWriteSab() ARGLESS; asserts echoed === nonce
    - StatusBar distinguishes shared / copy / error (timeout) and records __crossWriteState accordingly (review fix LOW)
    - The aria audit loop prints NO "MISSING aria-label" lines — every required Rule 5 label is present (presence audit, not a line count)
    - Theme picker onChange calls onThemeChange(e.target.value as ThemeName)
  </acceptance_criteria>
  <done>App.tsx, Titlebar.tsx, StatusBar.tsx created; theme persistence wired; StatusBar is the single owner of the SAB hooks + NONCE cross-write driver (no hardcoded 0xBEEF, argless crossWriteSab, shared/copy/timeout distinguished); every Rule 5 aria-label present (real audit); renderer type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ← window.api | Sandboxed renderer consumes only the three methods exposed by preload; no Node access |
| localStorage | Persists layout + theme; no sensitive data; not a security boundary |
| data-theme attribute | Theme tokens selected via a string attribute; no remote/innerHTML CSS injection |
| renderer SAB write | Renderer writes a per-run nonce into the shared buffer; utility re-reads it — bounded to the 8-byte Phase 0 region |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-13 | Information Disclosure | crossOriginIsolated false → SAB unusable | mitigate | StatusBar shows crossOriginIsolated in --color-danger if false; root mitigation is Plan 03's synchronous COOP/COEP setup |
| T-00-14 | Elevation of Privilege | Theme CSS from user-controlled localStorage | accept | The value is used only as a data-theme attribute string selecting a pre-built block — not innerHTML; no arbitrary CSS injection |
| T-00-15 | Tampering | dockview fromJSON with malformed localStorage layout | accept | onReady wraps fromJSON in try/catch falling back to INITIAL_LAYOUT; layout data carries no privilege |
| T-00-23 | Tampering | renderer cross-write into shared memory | accept | Renderer writes only the 4-byte RENDERER_SENTINEL slot of an 8-byte Phase 0 proof buffer with a per-run nonce; utility re-reads it as an integer; no code path, no pointer. The cross-write is the de-risk proof, intentionally scoped to one slot |
| T-00-27 | Tampering | cross-write false-pass via echo of a hardcoded literal | mitigate | StatusBar writes a PER-RUN RANDOM nonce (not 0xBEEF) and never sends it over IPC; grep asserts no 0xBEEF literal. A copy-only utility echoing an IPC arg cannot match the nonce (review fix MEDIUM-4) |
| T-00-16 | Compliance | Rule 5 aria-label missing | mitigate | Real per-control presence audit (Task 3 loop) over every label in the 00-UI-SPEC Rule 5 table — replaces the prior line-count heuristic |
</threat_model>

<verification>
Run after Plan 04 completes:

1. `pnpm --filter @swg/renderer exec tsc --noEmit` — exits 0
2. `grep "fromJSON" packages/renderer/src/workspace/WorkspaceShell.tsx` — present
3. `grep "onDidLayoutChange" packages/renderer/src/workspace/WorkspaceShell.tsx` — present
4. `grep "initialWidth\|initialHeight" packages/renderer/src/workspace/workspace-config.ts` — present (explicit panel sizing)
5. Aria audit loop (Task 3 verify) prints no MISSING lines
6. `grep "crossOriginIsolated" packages/renderer/src/shell/StatusBar.tsx` — present
7. `grep "dataset.theme\|data-theme" packages/renderer/src/App.tsx` — present
8. `grep -c "__sabValue" packages/renderer/src/shell/StatusBar.tsx` — >= 1 (single owner) AND `grep -c "__sabValue" packages/renderer/src/panels/ViewportPanel.tsx` — 0
9. `grep -c "0xBEEF" packages/renderer/src/shell/StatusBar.tsx` — 0 (per-run nonce, not a hardcoded literal — review fix MEDIUM-4)
10. `grep "__crossWriteState" packages/renderer/src/shell/StatusBar.tsx` — present (shared/copy/error triage — review fix LOW)
11. Visual check (Plan 05 checkpoint): four panels visible; dark bg; accent on active elements
</verification>

<success_criteria>
- Dark dockable workspace with four panels renders; panels explicitly sized (240/280/200)
- DockviewReact layout serializes on change and restores on reload
- Theme picker cycles the five locked themes; active theme persists to localStorage
- StatusBar shows live crossOriginIsolated, SAB, and cross-write status, and is the single owner of the test hooks
- StatusBar drives the renderer side of the NONCE cross-write proof (writes a per-run nonce, calls argless crossWriteSab, asserts echoed === nonce, records shared/copy/error) — no hardcoded 0xBEEF
- ViewportPanel displays wiring status without owning the SAB hook
- Every Rule 5 icon-only control has aria-label + title (real per-control audit)
- tsc --noEmit exits 0 in renderer
</success_criteria>

<output>
Create `.planning/phases/00-toolchain-de-risk-app-shell/00-04-SUMMARY.md` when done.
</output>
