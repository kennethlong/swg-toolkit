---
phase: 00-toolchain-de-risk-app-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01
  - 00-02
files_modified:
  - packages/backend/src/main.ts
  - packages/backend/src/preload.ts
  - packages/native-core/src/sab-rw.cpp
  - packages/native-core/src/addon.cpp
  - packages/native-core/index.d.ts
  - packages/native-core/CMakeLists.txt
  - packages/renderer/index.html
  - packages/renderer/src/main.tsx
  - vite.main.config.ts
  - vite.preload.config.ts
  - vite.renderer.config.ts
files_removed:
  - packages/backend/src/utility-worker.ts
autonomous: true
requirements:
  - FND-01
  - FND-03

must_haves:
  truths:
    - "BrowserWindow created with sandbox:false + nodeIntegration:true + contextIsolation:false (Path B fallback posture — preferred posture contextBridge cannot carry a C++ SAB across isolated-world boundaries)"
    - "session.webRequest.onHeadersReceived registered BEFORE win.loadURL() (RESEARCH Pitfall 1)"
    - "COOP header 'Cross-Origin-Opener-Policy: same-origin' injected on all responses"
    - "COEP header 'Cross-Origin-Embedder-Policy: require-corp' injected on all responses"
    - "crossOriginIsolated===true in the renderer (confirmed by running the proof)"
    - "native addon loaded directly in the renderer via require('@swg/native-core') (nodeIntegration:true, no IPC relay)"
    - "utility-worker.ts REMOVED — utility process is NOT on the data path"
    - "allocateSab(8) returns instanceof SharedArrayBuffer in the renderer process (in-process, no clone)"
    - "writeSab(sab,0,0xDEAD): C++ writes 0xDEAD → Int32Array(sab)[0]===0xDEAD from renderer (C++ → JS same memory)"
    - "readSab(sab,1): renderer writes per-run nonce → C++ readSab returns that exact nonce (JS → C++ same memory)"
    - "new Worker() receives the same SAB → reads 0xDEAD from slot 0 (intra-cluster SAB share)"
    - "window.__transport='B-native-in-renderer', window.__zeroCopy=true, window.__sabValue=0xDEAD, window.__crossWriteOk=true, window.__sabIsShared=true"
    - "revised FND-01: addon in renderer (not utility); preload is logging-only; no contextBridge API surface"
  artifacts:
    - path: "packages/backend/src/main.ts"
      provides: "Electron main process — BrowserWindow (fallback B posture), COOP/COEP, loadURL last. No utility fork."
      contains: "onHeadersReceived"
    - path: "packages/backend/src/preload.ts"
      provides: "Preload — logging/COI check only. No contextBridge. Renderer accesses addon directly."
    - path: "packages/native-core/src/sab-rw.cpp"
      provides: "writeSab() + readSab() C++ exports for bidirectional proof"
    - path: "packages/renderer/index.html"
      provides: "Phase-0 proof entry (replaced by Plan 00-04 React shell)"
    - path: "packages/renderer/src/main.tsx"
      provides: "Bidirectional same-memory proof + window.__* test hooks for 00-05 E2E"

  key_links:
    - from: "packages/renderer/src/main.tsx"
      to: "packages/native-core"
      via: "require('@swg/native-core') — nodeIntegration:true, no IPC"
      pattern: "require.*native-core"
    - from: "packages/backend/src/main.ts"
      to: "session.webRequest.onHeadersReceived"
      via: "synchronous call before win.loadURL()"
      pattern: "onHeadersReceived"
---

<objective>
Rewrite Phase-0 Plan-03 for Path B (native-in-renderer, true zero-copy). The cross-process SAB
relay model is FALSIFIED (Electron 42 throws "An object could not be cloned" for any SAB across
the utility-process boundary). Path B colocates the native addon in the renderer process cluster:
the addon is required() directly in the renderer (nodeIntegration:true fallback posture), allocates
the SAB in-process, and the renderer reads/writes the same memory with no IPC, no copy, ~10,600 MB/s.

Two new bidirectional addon exports (writeSab/readSab) drive the full same-memory proof:
  C++ writeSab → renderer reads (C++ → JS direction)
  Renderer writes nonce → C++ readSab sees it (JS → C++ direction)
  Web Worker receives the SAB → reads the C++ sentinel (intra-cluster share)

The proof is verified by RUNNING the app; captured log evidence in 00-03-SUMMARY.md.
The preferred posture (contextBridge) was tried first and empirically rejected; the fallback
(nodeIntegration:true) is the posture in effect, documented with residual risk.
</objective>

<context>
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-REPLAN.md
@D:/Code/SWG-Toolkit/.planning/research/CONSULT-P0SAB-SYNTHESIS.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: writeSab/readSab — extend native-core for bidirectional proof</name>
  <files>
    packages/native-core/src/sab-rw.cpp
    packages/native-core/src/addon.cpp
    packages/native-core/index.d.ts
    packages/native-core/CMakeLists.txt
    packages/native-core/test/hello.test.ts
  </files>
  <behavior>
    - writeSab(sab, int32Index, value): void — C++ writes Int32 into SAB slot
    - readSab(sab, int32Index): number — C++ reads Int32 from SAB slot
    - Both use Napi::SharedArrayBuffer (NAPI_EXPERIMENTAL)
    - CMakeLists.txt adds workspace-root napi.h path (pnpm hoisted layout)
    - 14/14 unit tests pass (8 original + 6 new for writeSab/readSab)
  </behavior>
  <done>Addon rebuilt, prebuild refreshed, 14/14 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: main.ts — Path B BrowserWindow + COOP/COEP, no utility fork</name>
  <files>packages/backend/src/main.ts</files>
  <behavior>
    - BrowserWindow: sandbox:false + contextIsolation:false + nodeIntegration:true (fallback B)
    - setupCrossOriginIsolation() called FIRST (COOP/COEP before loadURL)
    - No utilityProcess.fork(), no MessageChannelMain, no demux, no pending-promise maps
    - loadURL/loadFile LAST
  </behavior>
  <done>main.ts rewritten for Path B; utility relay machinery removed; COOP/COEP kept.</done>
</task>

<task type="auto">
  <name>Task 3: preload.ts — logging/COI check only (fallback posture, no contextBridge)</name>
  <files>packages/backend/src/preload.ts</files>
  <behavior>
    - No contextBridge.exposeInMainWorld() — renderer has Node, accesses addon directly
    - DOMContentLoaded: log crossOriginIsolated status and posture
  </behavior>
  <done>preload.ts simplified to logging; no API surface exposed.</done>
</task>

<task type="auto">
  <name>Task 4: renderer proof entry — bidirectional same-memory proof + window.__* hooks</name>
  <files>
    packages/renderer/index.html
    packages/renderer/src/main.tsx
  </files>
  <behavior>
    - require('@swg/native-core') directly (nodeIntegration:true)
    - Run 5-step proof: hello + allocateSab + writeSab(0xDEAD) + readSab(nonce) + Worker
    - Set window.__transport, __zeroCopy, __sabValue, __crossWriteOk, __sabIsShared
  </behavior>
  <done>Proof entry created; 5/5 assertions pass at runtime.</done>
</task>

<task type="auto">
  <name>Task 5: PROVE BY RUNNING — capture proof log evidence</name>
  <behavior>
    - ELECTRON_ENABLE_LOGGING=1 pnpm start; wait for proof output; grep proof markers
    - All 5 assertions: crossOriginIsolated=true, hello=pong, SAB instanceof, C++→JS, JS→C++, Worker
    - Capture log to /tmp/00-03-verify.log; kill Electron
  </behavior>
  <done>PROVEN: captured log shows all 5 proof assertions PASS. Evidence in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Path B fallback posture)

| Boundary | Description |
|----------|-------------|
| renderer world (nodeIntegration:true) | Full Node access in renderer. Mitigated: trusted desktop app; no external web content. |
| COOP/COEP headers → renderer isolation | Security boundary for SharedArrayBuffer access; set on every response (independent of posture). |
| shared memory (SAB) → renderer ↔ Worker | Both sides read/write the same backing store; bidirectional proof confirms same memory. |

## STRIDE Threat Register (Path B)

| Threat ID | Category | Component | Disposition | Notes |
|-----------|----------|-----------|-------------|-------|
| T-00-06 | Elevation | nodeIntegration:true | accepted (revised FND-01) | Renderer has Node. Trusted desktop app; mitigated by not loading external web content in this window. |
| T-00-08 | Info Disclosure | COOP/COEP not set | mitigate | setupCrossOriginIsolation() called FIRST; crossOriginIsolated=true confirmed by proof. |
| T-00-11 | DoS | COOP/COEP after loadURL | mitigate | onHeadersReceived before loadURL; verified by grep + runtime proof. |
| T-00-12 | Forward-looking | Live memory injection | transfer | Win32 OpenProcess/WriteProcessMemory not present in Phase 0; Phase 3 gates behind explicit user confirmation. |
</threat_model>

<verification>
Run after Plan 03 completes:
1. grep -n "onHeadersReceived" packages/backend/src/main.ts — before loadURL
2. grep -n "nodeIntegration.*true\|sandbox.*false" packages/backend/src/main.ts
3. ls packages/native-core/src/sab-rw.cpp — present
4. pnpm vitest run packages/native-core/test/hello.test.ts — 14/14 pass
5. pnpm start → log: crossOriginIsolated=true, ALL PROOF ASSERTIONS PASSED
</verification>

<success_criteria>
- native-core has writeSab/readSab, rebuilt, prebuild refreshed, 14/14 tests green
- Path B fallback posture chosen empirically (preferred posture contextBridge fails for C++ SAB)
- main.ts rewritten for B; utility SAB relay REMOVED; COOP/COEP-before-load kept
- renderer proof runs all 5 assertions; window.__* hooks set
- PROVEN BY RUNNING: log shows crossOriginIsolated=true + all proof assertions pass
- 00-03-PLAN.md rewritten for Path B; 00-03-SUMMARY.md written; STATE/ROADMAP updated
</success_criteria>

<output>
.planning/phases/00-toolchain-de-risk-app-shell/00-03-SUMMARY.md
</output>
