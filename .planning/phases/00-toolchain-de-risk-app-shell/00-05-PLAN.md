---
phase: 00-toolchain-de-risk-app-shell
plan: 05
type: execute
wave: 4
depends_on:
  - 00-01
  - 00-02
  - 00-03
  - 00-04
files_modified:
  - e2e/01-boot.spec.ts
  - e2e/02-isolation.spec.ts
  - e2e/03-sab-roundtrip.spec.ts
  - e2e/04-workspace.spec.ts
  - e2e/05-packaged.spec.ts
  - .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05

must_haves:
  truths:
    - "01-boot.spec.ts: contextIsolation === true asserted via Electron webPreferences API"
    - "01-boot.spec.ts: nodeIntegration === false asserted"
    - "01-boot.spec.ts: window.require === undefined in the renderer (Node not in renderer)"
    - "02-isolation.spec.ts: crossOriginIsolated === true evaluated in renderer page context"
    - "02-isolation.spec.ts: new SharedArrayBuffer(4) does not throw in renderer"
    - "03-sab-roundtrip.spec.ts: renderer reads Int32Array[0] === 0xDEAD (57005) from C++-written SAB"
    - "03-sab-roundtrip.spec.ts: contracts/ tsc --noEmit passes (FND-04 verified in CI)"
    - "04-workspace.spec.ts: four panel headings visible (Assets, Viewport, Inspector, Data or tab labels)"
    - "04-workspace.spec.ts: layout key 'swg-workspace-layout' exists in localStorage after first render"
    - "04-workspace.spec.ts: layout JSON in localStorage matches toJSON() output (restores correctly)"
    - "05-packaged.spec.ts: pnpm package produces a packaged executable in out/"
    - "05-packaged.spec.ts: crossOriginIsolated === true in the packaged renderer (COOP/COEP not just dev-server headers)"
    - "05-packaged.spec.ts: SAB sentinel (window.__sabValue) === 0xDEAD (57005) in the packaged renderer"
    - "All five Phase 0 success criteria have a green automated test"
  artifacts:
    - path: "e2e/01-boot.spec.ts"
      provides: "SC-1: Electron security posture assertions"
    - path: "e2e/02-isolation.spec.ts"
      provides: "SC-3: crossOriginIsolated + SAB allocatable"
    - path: "e2e/03-sab-roundtrip.spec.ts"
      provides: "SC-4: C++ SAB byte-pattern round-trip + contracts/ compile"
    - path: "e2e/04-workspace.spec.ts"
      provides: "SC-5: dockview panels visible + layout persistence"
    - path: "e2e/05-packaged.spec.ts"
      provides: "SC-3 packaged: crossOriginIsolated + SAB round-trip in packaged binary (not dev server)"
    - path: ".planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md"
      provides: "Updated per-task verification map including 05-packaged.spec.ts; nyquist_compliant: true"
  key_links:
    - from: "e2e/01-boot.spec.ts"
      to: "packages/backend/src/main.ts"
      via: "electronApp.windows()[0] webPreferences inspection"
      pattern: "webPreferences.*contextIsolation"
    - from: "e2e/02-isolation.spec.ts"
      to: "packages/backend/src/main.ts"
      via: "page.evaluate(() => self.crossOriginIsolated)"
      pattern: "crossOriginIsolated"
    - from: "e2e/03-sab-roundtrip.spec.ts"
      to: "packages/backend/src/utility-worker.ts"
      via: "page.evaluate() reading Int32Array from window.api.onSabPort SAB"
      pattern: "0xDEAD|57005"
    - from: "e2e/05-packaged.spec.ts"
      to: "out/"
      via: "electron.launch({ executablePath: out/<platform>/swg-toolkit })"
      pattern: "executablePath.*out"
---

<objective>
Write the complete E2E test suite (Playwright + Electron) that objectively verifies all five Phase 0 success criteria, add a packaged-build spec that proves COOP/COEP headers and the SAB sentinel work in the real packaged binary (not the dev server), then update VALIDATION.md to mark the phase Nyquist-compliant.

Purpose: Without automated verification, the phase cannot be closed. These specs are the machine-readable proof that SC-1 through SC-5 are all met. The packaged-build spec de-risks the critical known gap: Electron Forge's dev server forwards headers automatically, but a packaging bug or missing forge.config.ts setting could silently break `crossOriginIsolated` in the shipped binary. The checkpoint at the end of this plan gives the user a visual/functional confirmation pass before the phase is declared complete.

Output: Five Playwright spec files covering all success criteria including packaged-binary coverage; updated VALIDATION.md; a human-verify checkpoint for the visual shell and docking interactions that automation cannot cover.
</objective>

<execution_context>
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
</execution_context>

<context>
@D:/Code/SWG-Toolkit/.planning/PROJECT.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-01-SUMMARY.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-03-SUMMARY.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-04-SUMMARY.md

<interfaces>
<!-- Playwright Electron fixture — from Plan 01 e2e/fixtures/electron-helpers.ts -->
import { test, expect } from '../fixtures/electron-helpers'
// test provides: electronApp (ElectronApplication), window (Page)
// electronApp.windows()[0] — the main BrowserWindow
// window.evaluate(fn) — evaluate JS in the renderer context
// electronApp.evaluate(fn) — evaluate JS in the main process context

<!-- Electron webPreferences inspection via Playwright -->
const webPrefs = await electronApp.evaluate(async ({ BrowserWindow }) => {
  const win = BrowserWindow.getAllWindows()[0]
  return win.webContents.getWebPreferences()
})
// webPrefs.contextIsolation, webPrefs.nodeIntegration, webPrefs.sandbox

<!-- crossOriginIsolated in renderer -->
const isolated = await window.evaluate(() => self.crossOriginIsolated)

<!-- SharedArrayBuffer in renderer -->
const sabWorks = await window.evaluate(() => {
  try { new SharedArrayBuffer(4); return true } catch { return false }
})

<!-- SAB round-trip: reading the sentinel written by C++ -->
// The renderer stores the SAB when onSabPort fires.
// The test needs to wait for the SAB to arrive and then read it.
// Pattern: window.waitForFunction(() => window.__sabValue !== undefined, { timeout: 5000 })
// The renderer (ViewportPanel or StatusBar) should expose window.__sabValue for test access.
// Implementation note: in Plan 04's ViewportPanel, add:
//   window.__sabValue = new Int32Array(sab)[0]  // for E2E test access only
// This is a test hook; document it with a // TEST HOOK comment.

<!-- contracts/ compile check -->
// Run as a shell command within the test or as a beforeAll:
// const result = spawnSync('pnpm', ['--filter', '@swg/contracts', 'exec', 'tsc', '--noEmit'])
// expect(result.status).toBe(0)

<!-- dockview panel labels — what to grep for in the DOM -->
// DockviewReact renders panel tabs with the panel id or title.
// The panels are added with ids: 'sidebar', 'viewport', 'data', 'inspector'
// grep the DOM: page.locator('[data-dv-id="sidebar"]') or page.getByText('Assets')
// Use the tab label text: 'Assets', 'Viewport', 'Inspector', 'Datatable' (first tab of DataPanel)

<!-- Packaged app launch (05-packaged.spec.ts) -->
// electron-forge package produces the output in out/<platform>-<arch>/<appName>.exe (Windows)
// or out/<platform>-<arch>/<appName> (Linux/macOS).
// Playwright launches the packaged binary directly (no dev server, no Vite):
import { _electron as electron } from '@playwright/test'
import * as path from 'path'
import * as fs from 'fs'
// Resolve the packaged executable:
function findPackagedExe(outDir: string): string {
  // Scan out/ subdirectories for the executable
  // On Windows: out/swg-toolkit-win32-x64/swg-toolkit.exe
  // On macOS: out/swg-toolkit-darwin-x64/swg-toolkit.app/Contents/MacOS/swg-toolkit
  // On Linux: out/swg-toolkit-linux-x64/swg-toolkit
  // Use fs.readdirSync(outDir) to find the first match
}
// Launch packaged app:
const app = await electron.launch({ executablePath: findPackagedExe(OUT_DIR) })
// The packaged app does NOT use Vite dev server; COOP/COEP must be set by main.ts interceptHeaders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E specs for SC-1 through SC-4 (security posture, isolation, SAB round-trip, contracts)</name>
  <files>
    e2e/01-boot.spec.ts
    e2e/02-isolation.spec.ts
    e2e/03-sab-roundtrip.spec.ts
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
    (section: Success Criterion → Test Map — all rows for SC-1, SC-2, SC-3, SC-4)
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
    (section: Validation Architecture)
    e2e/fixtures/electron-helpers.ts (the fixture to import)
    packages/renderer/src/panels/ViewportPanel.tsx (where window.__sabValue test hook must be placed)
    packages/renderer/src/shell/StatusBar.tsx (confirms SAB delivery)
  </read_first>
  <action>
    Write three Playwright spec files. All use the fixture from e2e/fixtures/electron-helpers.ts.

    --- e2e/01-boot.spec.ts (SC-1: Electron security posture) ---
    Import { test, expect } from '../fixtures/electron-helpers'.
    Test 'contextIsolation is true': use electronApp.evaluate to read BrowserWindow webPreferences; expect webPrefs.contextIsolation to be true.
    Test 'nodeIntegration is false': same; expect webPrefs.nodeIntegration to be false.
    Test 'sandbox is true': same; expect webPrefs.sandbox to be true.
    Test 'preload security — window.require is undefined in renderer': window.evaluate(() => typeof (window as any).require); expect result to be 'undefined'.
    Test 'preload security — window.process is undefined in renderer': window.evaluate(() => typeof (window as any).process); expect 'undefined'.
    Test 'window.api.hello() returns pong': window.evaluate(() => (window as any).api.hello()); expect result to be 'pong'.
    (SC-2 hello round-trip proven here alongside boot spec — collocated because it uses the same window.)

    --- e2e/02-isolation.spec.ts (SC-3: cross-origin isolation) ---
    Test 'crossOriginIsolated is true in renderer': window.evaluate(() => self.crossOriginIsolated); expect true.
    Test 'new SharedArrayBuffer(4) does not throw in renderer': window.evaluate(() => { try { new SharedArrayBuffer(4); return true } catch { return false } }); expect true.
    Test 'SharedArrayBuffer byteLength readable': window.evaluate(() => new SharedArrayBuffer(4).byteLength); expect 4.

    --- e2e/03-sab-roundtrip.spec.ts (SC-4: SAB byte-pattern round-trip + contracts compile) ---
    Before all tests: confirm ViewportPanel has exposed window.__sabValue (the test hook set by C++ sentinel). Wait for it: page.waitForFunction(() => typeof (window as any).__sabValue === 'number', { timeout: 10000 }).
    Test 'C++ writes 0xDEAD sentinel to SAB — renderer reads it': window.evaluate(() => (window as any).__sabValue); expect 57005 (0xDEAD decimal).
    Test 'contracts/ package compiles — tsc --noEmit exits 0': use Node child_process.spawnSync inside the test (not electronApp.evaluate; run in test process): spawnSync('pnpm', ['--filter', '@swg/contracts', 'exec', 'tsc', '--noEmit'], { cwd: process.cwd(), stdio: 'pipe' }); expect status 0.
    Test 'SAB_LAYOUT.HELLO_SENTINEL.offset is 0': import SAB_LAYOUT from '@swg/contracts' at top of file; expect SAB_LAYOUT.HELLO_SENTINEL.offset to be 0.

    REQUIRED: Before running 03-sab-roundtrip.spec.ts, ensure ViewportPanel.tsx (Plan 04) has the test hook: after receiving the SAB via onSabPort, add `(window as any).__sabValue = new Int32Array(sab)[0]` with comment `// TEST HOOK: expose SAB sentinel for Playwright e2e/03-sab-roundtrip.spec.ts`. If ViewportPanel.tsx already has this (added in Plan 04), leave it. If not, add it now via Edit.

    Run full suite: `pnpm playwright test e2e/01-boot.spec.ts e2e/02-isolation.spec.ts e2e/03-sab-roundtrip.spec.ts`
    All tests must PASS before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && pnpm playwright test e2e/01-boot.spec.ts e2e/02-isolation.spec.ts e2e/03-sab-roundtrip.spec.ts 2>&1 | grep -E "passed|failed|PASS|FAIL" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 01-boot.spec.ts: contextIsolation === true; nodeIntegration === false; sandbox === true; window.require === undefined; window.process === undefined; hello() returns 'pong'
    - 02-isolation.spec.ts: crossOriginIsolated === true; new SharedArrayBuffer(4) succeeds; byteLength === 4
    - 03-sab-roundtrip.spec.ts: window.__sabValue === 57005 (0xDEAD); tsc --noEmit exits 0; SAB_LAYOUT.HELLO_SENTINEL.offset === 0
    - pnpm playwright test for all three specs exits 0 (all green)
    - ViewportPanel.tsx contains window.__sabValue = new Int32Array(sab)[0] with TEST HOOK comment
  </acceptance_criteria>
  <done>SC-1, SC-2, SC-3, SC-4 all verified green by automated Playwright tests.</done>
</task>

<task type="auto">
  <name>Task 2: E2E spec for SC-5 (workspace panels + layout persistence) + VALIDATION.md update</name>
  <files>
    e2e/04-workspace.spec.ts
    .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
    (section: Panel Content Contracts — Assets, Viewport, Inspector, Data panel labels)
  </read_first>
  <action>
    --- e2e/04-workspace.spec.ts (SC-5: dockview panels + persistence) ---
    Import { test, expect } from '../fixtures/electron-helpers'.

    Test 'Assets panel visible': page.getByText('Assets'); expect(locator).toBeVisible().
    Test 'Viewport panel visible': page.getByText('Viewport'); expect(locator).toBeVisible().
    Test 'Inspector panel visible': page.getByText('Inspector'); expect(locator).toBeVisible().
    Test 'Data panel visible (Datatable tab)': page.getByText('Datatable'); expect(locator).toBeVisible().
    Test 'dark background applied': page.evaluate(() => getComputedStyle(document.body).backgroundColor); expect result to include '24, 24, 24' (hex #181818 = rgb(24,24,24)) or check document.documentElement for --color-bg CSS var value.
    Test 'layout serialized to localStorage after render': page.evaluate(() => localStorage.getItem('swg-workspace-layout')); expect result NOT to be null; parse it and confirm it is valid JSON with panels property.
    Test 'layout restores after reload': save the layout JSON via page.evaluate(() => localStorage.getItem('swg-workspace-layout')); reload page (page.reload()); re-read localStorage; confirm layout JSON still matches; confirm panels still visible (getByText('Assets').toBeVisible()).
    Test 'theme picker exists in titlebar': page.locator('select[aria-label="Select theme"]'); toBeVisible().
    Test 'theme change persists to localStorage': page.evaluate(() => { (document.querySelector('select[aria-label="Select theme"]') as HTMLSelectElement).value = 'amber'; document.querySelector('select[aria-label="Select theme"]')!.dispatchEvent(new Event('change')); return localStorage.getItem('swg-active-theme') }); expect 'amber'.

    Run: `pnpm playwright test e2e/04-workspace.spec.ts` — all tests must PASS.

    --- Update .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md ---
    Read the file first (it is a draft with placeholder rows). Replace the entire Per-Task Verification Map table with a fully populated version mapping each task across Plans 01–05 to its automated command. Include Task 3 from Plan 05 (05-packaged.spec.ts). Set frontmatter: nyquist_compliant: true, wave_0_complete: true. Fill in the Test Infrastructure section: Framework = Vitest 4.1.9 + @playwright/test 1.61.0; Config = vitest.config.ts + playwright.config.ts; quick run = pnpm vitest run; full suite = pnpm vitest run && pnpm playwright test. Confirm: no 3 consecutive tasks lack automated verification (Nyquist Dimension 8). Update the Validation Sign-Off checklist to mark all items complete.

    Note: Leave the Per-Task Verification Map row for 05-packaged.spec.ts as status "⬜ pending" — it will be marked green when Task 3 completes.
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && pnpm playwright test e2e/04-workspace.spec.ts 2>&1 | grep -E "passed|failed" | tail -3 && grep "nyquist_compliant.*true" .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All tests in 04-workspace.spec.ts pass: 4 panels visible, localStorage layout present and valid JSON, layout survives reload, theme picker exists, theme change persists
    - pnpm playwright test exits 0 for spec files 01-04
    - 00-VALIDATION.md: nyquist_compliant: true in frontmatter
    - 00-VALIDATION.md: wave_0_complete: true in frontmatter
    - 00-VALIDATION.md: Per-Task Verification Map table filled with all Plan 01–05 tasks (including the packaged spec row)
    - 00-VALIDATION.md: Validation Sign-Off checklist: all items marked complete
    - No 3 consecutive tasks in the verification map lack an automated command
  </acceptance_criteria>
  <done>SC-5 verified green; VALIDATION.md complete and nyquist_compliant: true.</done>
</task>

<task type="auto">
  <name>Task 3: Packaged-build E2E spec — crossOriginIsolated + SAB sentinel in the real packaged binary</name>
  <files>
    e2e/05-packaged.spec.ts
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
    (sections: COOP/COEP — dev-server header forwarding vs. protocol interceptor, Pitfall — packaged
     builds and header injection; specifically: RESEARCH.md flags dev-server COOP/COEP forwarding as
     MEDIUM confidence because Forge's dev server intercepts the renderer's local HTTP requests but the
     packaged app uses file:// — main.ts must call session.defaultSession.protocol.interceptFileProtocol
     or webContents.session.webRequest.onHeadersReceived to inject headers for the packaged build.
     Verify that Plan 03's main.ts uses webRequest.onHeadersReceived or equivalent, not only the
     Vite devServer.headers option.)
    packages/backend/src/main.ts (confirm COOP/COEP is set via webRequest or protocol handler, not just Vite devServer.headers)
    forge.config.ts (confirm packagerConfig.asar is set and AutoUnpackNativesPlugin is present)
  </read_first>
  <action>
    Run the Electron Forge package step to produce the packaged binary, then launch Playwright against it to assert that COOP/COEP headers are active and the SAB round-trip sentinel reads correctly — without the Vite dev server in the loop.

    This spec directly de-risks the known MEDIUM-confidence gap: Forge's Vite plugin injects COOP/COEP headers via devServer.headers during development. In the packaged binary, the Vite server is gone; the headers must be delivered by main.ts via Electron's webRequest or protocol interceptor. If that path is missing or broken, crossOriginIsolated will be false in production even when dev tests pass.

    --- e2e/05-packaged.spec.ts ---

    Imports: import { test, expect } from '@playwright/test' (NOT from electron-helpers — this spec manages its own app lifetime because it uses the packaged binary, not the dev tree). Import { _electron as electron } from '@playwright/test'. Import * as path from 'path'. Import * as fs from 'fs'. Import * as child_process from 'child_process'.

    const REPO_ROOT = path.resolve(__dirname, '..')
    const OUT_DIR = path.join(REPO_ROOT, 'out')

    Helper findPackagedExe(outDir: string): string — scans out/ subdirectories:
      - On win32: finds the first *.exe in out/swg-toolkit-win32-*/
      - On darwin: finds the first *.app/Contents/MacOS/* in out/swg-toolkit-darwin-*/
      - On linux: finds the first non-extension executable in out/swg-toolkit-linux-*/
      Throws a descriptive error if none found (message should tell the user to run `pnpm package` first).

    test.beforeAll(async () => {
      // Step 1: run electron-forge package to produce the packaged app
      // Use child_process.execSync so the test blocks until packaging completes.
      // Timeout: 120 000 ms (packaging can take ~60s on first run with native rebuild)
      const result = child_process.spawnSync('pnpm', ['package'], {
        cwd: REPO_ROOT,
        stdio: 'pipe',
        timeout: 120_000,
        shell: process.platform === 'win32',
      })
      if (result.status !== 0) {
        throw new Error(`pnpm package failed:\n${result.stderr?.toString()}`)
      }
    })

    test.describe('packaged binary — COOP/COEP + SAB round-trip', () => {
      let app: Awaited<ReturnType<typeof electron.launch>>
      let window: import('@playwright/test').Page

      test.beforeAll(async () => {
        const exePath = findPackagedExe(OUT_DIR)
        app = await electron.launch({ executablePath: exePath })
        window = await app.firstWindow()
        await window.waitForLoadState('domcontentloaded')
      })

      test.afterAll(async () => { await app?.close() })

      test('out/ directory exists and contains a packaged executable', async () => {
        expect(fs.existsSync(OUT_DIR)).toBe(true)
        const exePath = findPackagedExe(OUT_DIR)
        expect(fs.existsSync(exePath)).toBe(true)
      })

      test('packaged renderer: crossOriginIsolated === true', async () => {
        const isolated = await window.evaluate(() => self.crossOriginIsolated)
        expect(isolated).toBe(true)
      })

      test('packaged renderer: new SharedArrayBuffer(4) does not throw', async () => {
        const works = await window.evaluate(() => {
          try { new SharedArrayBuffer(4); return true } catch { return false }
        })
        expect(works).toBe(true)
      })

      test('packaged renderer: SAB sentinel (window.__sabValue) === 0xDEAD', async () => {
        // Wait for the utility process to deliver the SAB port (up to 15s — packaged launch is slower)
        await window.waitForFunction(
          () => typeof (window as any).__sabValue === 'number',
          { timeout: 15_000 }
        )
        const sentinel = await window.evaluate(() => (window as any).__sabValue)
        expect(sentinel).toBe(0xDEAD) // 57005
      })
    })

    After writing the spec, run it: `pnpm playwright test e2e/05-packaged.spec.ts --timeout=180000`

    If the packaged build fails because COOP/COEP headers are not set (crossOriginIsolated === false): inspect packages/backend/src/main.ts. The headers MUST be set via webRequest.onHeadersReceived or Electron's protocol.handle — the Vite devServer.headers path does not apply to packaged builds. Add or fix the header injection in main.ts if missing, then re-run.

    Record the outcome in .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md: update the 05-packaged.spec.ts row status from ⬜ pending to ✅ green (or ❌ red if it exposes a bug — in that case surface the failure to the user before marking the phase complete).
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && pnpm playwright test e2e/05-packaged.spec.ts --timeout=180000 2>&1 | grep -E "passed|failed|PASS|FAIL" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm package (electron-forge package) exits 0 and produces the packaged executable in out/
    - out/ directory exists and contains at least one executable matching the platform pattern
    - packaged renderer: crossOriginIsolated === true (COOP/COEP headers active without Vite dev server)
    - packaged renderer: new SharedArrayBuffer(4) does not throw
    - packaged renderer: window.__sabValue === 57005 (0xDEAD sentinel from C++ addon)
    - pnpm playwright test e2e/05-packaged.spec.ts exits 0 (4/4 tests green)
    - 00-VALIDATION.md 05-packaged.spec.ts row updated to ✅ green
    - If crossOriginIsolated === false in packaged build: bug surfaced and main.ts webRequest.onHeadersReceived path fixed before task is marked done
  </acceptance_criteria>
  <done>Packaged binary verified: crossOriginIsolated true, SAB round-trip sentinel 0xDEAD confirmed in the real packaged Electron app. COOP/COEP header injection is proven to work without the Vite dev server.</done>
</task>

</tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01–05 delivered: pnpm monorepo scaffold, cmake-js C++ addon (hello + allocateSab), Electron security posture (contextIsolation, COOP/COEP), utility-process → SAB → renderer pipeline, dark dockable workspace shell with 4 panels, theme system, persistence, and an automated E2E test suite including packaged-binary verification.

    Automated verification: pnpm vitest run and pnpm playwright test are both green (8 vitest unit tests + 5 Playwright spec files, ~19 individual assertions including packaged-build coverage).
  </what-built>
  <how-to-verify>
    1. Run the full test suite: `pnpm vitest run && pnpm playwright test`
       Expected: all green, no failures. (05-packaged.spec.ts will rebuild the package on first run — allow ~2 minutes.)

    2. Launch the app: `pnpm start`
       Check:
       a. Dark background (#181818) appears immediately — no white flash.
       b. Four panels visible: Assets (left), Viewport (center), Inspector (right), Data pane (bottom with Datatable/Console/Log tabs).
       c. Viewport center shows wiring-proof overlay: "crossOriginIsolated ✓", "SAB round-trip: PASS", "native-core: utility-process ✓" (these appear once the SAB port arrives, ~1 second after launch).
       d. Console tab (Data pane) shows the five Phase 0 seed messages with correct color coding (timestamps faint, success tokens cyan).
       e. StatusBar (bottom) shows: "crossOriginIsolated: true" in cyan accent, "SAB: 60 fps" in cyan, "addon: utility-process ✓" in cyan.

    3. Drag a panel to a different position (e.g., drag the Inspector to the center area). Confirm the 5-way drop zone overlay appears (dashed accent border + drop hint text). Drop it. Confirm panel docks to the new location.

    4. Close the app and relaunch. Confirm the layout from step 3 is restored (panel is in its new position).

    5. Use the theme picker in the titlebar. Switch to "Amber" — confirm accent color changes from cyan (#38bec9) to amber (#e0a13a) immediately throughout the UI (active tab underline, statusbar accent values, accordion icons if visible). Close and relaunch — confirm amber is still the active theme.

    6. Tab through interactive elements with the keyboard. Confirm every button and select shows a visible focus ring (2-layer glow) when focused.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct. Describe any issues if not.</resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Electron app | Playwright launches Electron; test code runs in test runner, not in the app |
| page.evaluate() | Executes JS in the renderer sandbox — does not grant Node access |
| packaged binary → file system | Packaged build reads from the ASAR bundle; main.ts header injection must run before any page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-17 | Tampering | window.__sabValue test hook exposing internal state | accept | The hook sets a read-only integer on window for test access only. No sensitive data (0xDEAD is a hardcoded test sentinel). Renderer is sandboxed; main process not affected. Low risk. |
| T-00-18 | Information Disclosure | Playwright test accessing webPreferences | accept | electronApp.evaluate runs in the main process context (trusted). It reads webPreferences to assert security settings — this is the CORRECT way to verify the security posture. Not a vulnerability. |
| T-00-19 | Tampering | E2E tests passing despite security regression | mitigate | Tests assert contextIsolation === true and nodeIntegration === false via BrowserWindow.getAllWindows()[0].webContents.getWebPreferences(). If a future change inadvertently disables these, tests fail. This is the intended protection. |
| T-00-20 | Tampering | COOP/COEP headers missing in packaged build (dev-server headers not forwarded) | mitigate | 05-packaged.spec.ts asserts crossOriginIsolated === true against the real packaged binary. If main.ts uses Vite devServer.headers only (not webRequest.onHeadersReceived), this test will catch the regression. Acceptance criterion: spec must pass before phase is closed. |
</threat_model>

<verification>
Final phase gate — run ALL of the following and confirm green:

1. `pnpm vitest run` — 8/8 unit tests pass (native-core hello + allocateSab)
2. `pnpm playwright test` — all five spec files pass (including 05-packaged.spec.ts)
3. `grep "nyquist_compliant.*true" .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md`
4. `pnpm --filter @swg/contracts exec tsc --noEmit` — exits 0
5. `pnpm --filter @swg/backend exec tsc --noEmit` — exits 0
6. `pnpm --filter @swg/renderer exec tsc --noEmit` — exits 0
7. `pnpm start` — app launches; human checkpoint approved
</verification>

<success_criteria>
- All five Phase 0 success criteria have green automated tests:
  SC-1 (security posture): 01-boot.spec.ts green
  SC-2 (hello round-trip): 01-boot.spec.ts window.api.hello() green
  SC-3 (crossOriginIsolated): 02-isolation.spec.ts green
  SC-4 (SAB round-trip + contracts): 03-sab-roundtrip.spec.ts green
  SC-5 (workspace panels + persistence): 04-workspace.spec.ts green
  SC-3 packaged (crossOriginIsolated in real binary): 05-packaged.spec.ts green
- VALIDATION.md: nyquist_compliant: true; all Per-Task Verification Map rows filled including packaged spec
- Human checkpoint: visual shell confirmed correct; docking works; theme + layout survive restart
- Phase 0 declared complete; /gsd:verify-work can be run
</success_criteria>

<output>
Create `.planning/phases/00-toolchain-de-risk-app-shell/00-05-SUMMARY.md` when done.
</output>
