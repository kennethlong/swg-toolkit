---
phase: 00-toolchain-de-risk-app-shell
plan: 05
type: execute
wave: 4
depends_on:
  - 00-01
  - 00-02
  - 00-03
  - 00-04
files_modified:
  - e2e/01-boot.spec.ts
  - e2e/02-isolation.spec.ts
  - e2e/03-sab-roundtrip.spec.ts
  - e2e/04-workspace.spec.ts
  - e2e/05-packaged.spec.ts
  - .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05

must_haves:
  truths:
    - "01-boot.spec.ts: contextIsolation === true asserted via Electron webPreferences API"
    - "01-boot.spec.ts: nodeIntegration === false asserted; window.require === undefined in renderer"
    - "01-boot.spec.ts: window.api.hello() returns 'pong' (no relay race)"
    - "01-boot.spec.ts: PRELOAD ALLOWLIST proven by Object.keys(window.api) — the surface is EXACTLY ['hello','onSabPort','crossWriteSab'] (length 3), not a grep proxy (review fix MEDIUM / Opus)"
    - "02-isolation.spec.ts: crossOriginIsolated === true in renderer; new SharedArrayBuffer(4) does not throw"
    - "03-sab-roundtrip.spec.ts: renderer reads Int32Array[0] === 0xDEAD (57005) from the C++-written SAB"
    - "03-sab-roundtrip.spec.ts: window.__sabIsShared === true (arrived object instanceof SharedArrayBuffer)"
    - "03-sab-roundtrip.spec.ts: window.__crossWriteOk === true (utility re-read the renderer's PER-RUN NONCE — same-memory sharing, not a copy or an arg-echo); window.__crossWriteState === 'shared'"
    - "03-sab-roundtrip.spec.ts: contracts/ tsc --noEmit passes (FND-04)"
    - "04-workspace.spec.ts: four panels visible; layout 'swg-workspace-layout' present in localStorage"
    - "04-workspace.spec.ts: SC-5 RESTART — layout survives a REAL close + relaunch (app.close() then a fresh electron.launch), not page.reload(), against the REAL app.getPath('userData') default path (not only an injected temp dir) — review fix MEDIUM / Opus"
    - "05-packaged.spec.ts: a packaged executable exists (built once out-of-band via package:ci / PACKAGED_EXE_PATH — NOT inside test beforeAll)"
    - "05-packaged.spec.ts: crossOriginIsolated === true in the packaged renderer (file:// COOP/COEP path with a documented fallback priority ladder, not dev-server headers)"
    - "05-packaged.spec.ts: window.__crossWriteOk === true in the packaged renderer (cross-write holds in the real packaged binary — the packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild, round-3 / Cursor CUR-1)"
    - "05-packaged is a HARD gate: in CI (Plan 01 workflow) a SKIP of 05-packaged is treated as a FAIL; the human checkpoint builds the exe and treats a skip/no-run as a fail (review fix HIGH-2)"
    - "Nyquist sign-off is performed against the FILLED 00-VALIDATION.md by a step independent of the test-authoring tasks, and certifies EXACTLY what was proven: FND-02 resolution-path non-circular + packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild via the packaged gate (round-3 / Cursor CUR-1 — NOT a separate Electron-ABI load) — NOT a full no-compiler proof, NOT any proxy (review fix HIGH-1)"
    - "All five Phase 0 success criteria have a green automated test"
  artifacts:
    - path: "e2e/01-boot.spec.ts"
      provides: "SC-1: Electron security posture assertions + Object.keys(window.api) allowlist"
    - path: "e2e/02-isolation.spec.ts"
      provides: "SC-3: crossOriginIsolated + SAB allocatable"
    - path: "e2e/03-sab-roundtrip.spec.ts"
      provides: "SC-4: C++ SAB byte-pattern round-trip + instanceof + NONCE cross-write sharing proof + contracts compile"
    - path: "e2e/04-workspace.spec.ts"
      provides: "SC-5: dockview panels visible + REAL close/relaunch persistence against the real userData path"
    - path: "e2e/05-packaged.spec.ts"
      provides: "SC-3 packaged: crossOriginIsolated + cross-write in the packaged binary (hard gate)"
    - path: ".planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md"
      provides: "Filled per-task verification map; nyquist_compliant set in an independent sign-off step (certifying only what was proven)"
  key_links:
    - from: "e2e/01-boot.spec.ts"
      to: "packages/backend/src/main.ts"
      via: "electronApp.evaluate webPreferences inspection + Object.keys(window.api)"
      pattern: "webPreferences|Object.keys"
    - from: "e2e/03-sab-roundtrip.spec.ts"
      to: "packages/renderer/src/shell/StatusBar.tsx"
      via: "page reads window.__sabValue / __sabIsShared / __crossWriteOk / __crossWriteState (set by StatusBar — single owner)"
      pattern: "__crossWriteOk|__sabIsShared|0xDEAD|57005"
    - from: "e2e/05-packaged.spec.ts"
      to: "PACKAGED_EXE_PATH / out/"
      via: "electron.launch({ executablePath })"
      pattern: "PACKAGED_EXE_PATH|executablePath"
---

<objective>
Write the complete E2E test suite (Playwright + Electron) that objectively verifies all five Phase 0 success criteria — including the same-memory NONCE cross-write that distinguishes zero-copy sharing from a copy (and from an arg-echo), a REAL close/relaunch restart test for layout persistence against the real userData path, an Object.keys(window.api) allowlist assertion, and a packaged-binary HARD gate that proves COOP/COEP + the cross-write hold in the shipped file:// build — then perform an INDEPENDENT Nyquist sign-off against the filled VALIDATION.md that certifies only what was proven.

Purpose: Without automated verification the phase cannot close. Round 1 closed three proof gaps; round 2 tightens the remaining proxies (review 00-REVIEWS.md): (1) SC-4 cross-write now asserts a PER-RUN NONCE so a copy-only utility echoing the IPC arg cannot false-pass (review fix MEDIUM-4); (2) SC-5 restart asserts the REAL app.getPath('userData') default path so a production "writes to default-temp / non-persisted path" bug cannot hide behind an injected temp dir (review fix MEDIUM / Opus); (3) the preload allowlist is proven by enumerating Object.keys(window.api) rather than a grep that passes for one fat exposure (review fix MEDIUM / Opus); (4) 05-packaged is made a HARD gate — a skip is a FAIL in CI and at the human checkpoint (review fix HIGH-2); (5) the Nyquist flip certifies EXACTLY what FND-02 proved (resolution-path non-circular + packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild — round-3 / Cursor CUR-1, NOT a separate "Electron-ABI" build), never a proxy, matching the 00-02 honest scoping (review fix HIGH-1). The packaged file:// COOP/COEP path has a documented fallback priority ladder, not a single mechanism (review fix LOW / Sonnet).

Output: Five Playwright spec files (nonce cross-write + instanceof + real restart against real userData + allowlist + packaged hard gate); the filled VALIDATION.md; an independent Nyquist sign-off task that does not certify proxies; a human-verify checkpoint for the visual shell, docking, and the packaged hard gate.
</objective>

<execution_context>
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
</execution_context>

<context>
@D:/Code/SWG-Toolkit/.planning/PROJECT.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-01-SUMMARY.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-02-SUMMARY.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-03-SUMMARY.md
@D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-04-SUMMARY.md

<interfaces>
Playwright Electron fixture (from Plan 01 e2e/fixtures/electron-helpers.ts):
  import { test, expect } from '../fixtures/electron-helpers'   // provides electronApp + window
  electronApp.evaluate(fn)  // main-process context (read webPreferences, app.getPath)
  window.evaluate(fn)       // renderer context

webPreferences inspection:
  const p = await electronApp.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0].webContents.getWebPreferences())
  // p.contextIsolation / p.nodeIntegration / p.sandbox

Preload allowlist (review fix MEDIUM / Opus): assert the SURFACE, not a grep.
  const keys = await window.evaluate(() => Object.keys((window as any).api).sort())
  expect(keys).toEqual(['crossWriteSab','hello','onSabPort'])   // EXACTLY three; length 3

SAB test hooks — set ONLY by StatusBar (Plan 04, single owner). The specs READ them; they do NOT add them:
  window.__sabValue      : number   // the 0xDEAD (57005) the renderer read from the SAB
  window.__sabIsShared   : boolean  // (sab instanceof SharedArrayBuffer)
  window.__crossWriteOk  : boolean  // utility re-read the renderer's PER-RUN NONCE (same-memory proof, not echo)
  window.__crossWriteState : 'shared' | 'copy' | 'error'   // triage: shared vs copy vs timeout/no-ack (review fix LOW)
  Wait for them: page.waitForFunction(() => typeof window.__crossWriteOk === 'boolean', { timeout: 15000 })

contracts/ compile check (FND-04): spawnSync('pnpm', ['--filter','@swg/contracts','exec','tsc','--noEmit'])

dockview panel labels: page.getByText('Assets'|'Viewport'|'Inspector'|'Datatable')

Real userData path (review fix MEDIUM / Opus) — for SC-5 restart:
  const userData = await app.evaluate(({ app }) => app.getPath('userData'))
  // Use the REAL default userData (do not only inject a temp --user-data-dir); the test must prove
  // localStorage persists at the path the SHIPPED app uses, so a "writes to temp" bug cannot hide.

Packaged app (05-packaged.spec.ts) — built OUT-OF-BAND, not in beforeAll, HARD gate:
  The packaged binary is produced by `pnpm package:ci` (Plan 01 script) BEFORE the spec runs (the Plan 01
  CI workflow builds it and sets PACKAGED_EXE_PATH; the human checkpoint also builds it). The spec resolves
  the executable via PACKAGED_EXE_PATH if set, else scans out/. It does NOT run pnpm package itself.
  In CI, PACKAGED_EXE_PATH is always set so the spec's skip branch is never taken; a skip in CI is a FAIL
  (review fix HIGH-2 — enforced by the Plan 01 workflow parsing the reporter for executed assertions).
  import { _electron as electron } from '@playwright/test'
  const exe = process.env.PACKAGED_EXE_PATH ?? findPackagedExe(path.join(REPO_ROOT, 'out'))
  const app = await electron.launch({ executablePath: exe })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-1..SC-4 specs — security posture + allowlist, isolation, SAB nonce cross-write + instanceof, contracts</name>
  <files>
    e2e/01-boot.spec.ts
    e2e/02-isolation.spec.ts
    e2e/03-sab-roundtrip.spec.ts
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
    (section: Success Criterion → Test Map — SC-1..SC-4)
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
    (section: Validation Architecture)
    e2e/fixtures/electron-helpers.ts
    packages/renderer/src/shell/StatusBar.tsx (single owner of __sabValue / __sabIsShared / __crossWriteOk / __crossWriteState — the specs READ these)
    packages/backend/src/preload.ts (the three-method window.api surface — the allowlist assertion enumerates it)
  </read_first>
  <action>
    Write three Playwright specs using the fixture from e2e/fixtures/electron-helpers.ts.

    01-boot.spec.ts (SC-1 + SC-2): assert via electronApp.evaluate webPreferences contextIsolation === true, nodeIntegration === false, sandbox === true. window.evaluate typeof window.require === 'undefined'; typeof window.process === 'undefined'. window.evaluate(() => window.api.hello()) === 'pong' (SC-2; no relay race since Plan 03 demuxes).
      PRELOAD ALLOWLIST (review fix MEDIUM / Opus — prove the surface, not a grep): const keys = await window.evaluate(() => Object.keys((window as any).api).sort()); expect(keys).toEqual(['crossWriteSab','hello','onSabPort']); expect(keys.length).toBe(3). A grep -c exposeInMainWorld == 1 passes for ONE FAT exposure; this enumerates the actual exposed surface so an over-broad bridge fails the test.

    02-isolation.spec.ts (SC-3): window.evaluate(() => self.crossOriginIsolated) === true; new SharedArrayBuffer(4) does not throw; byteLength === 4.

    03-sab-roundtrip.spec.ts (SC-4 — NONCE cross-write proof, the heart of the phase):
      beforeEach/at start: await page.waitForFunction(() => typeof window.__crossWriteOk === 'boolean', { timeout: 15000 }) (StatusBar sets all hooks once the SAB + cross-write complete).
      Test 'C++ wrote 0xDEAD; renderer read it': window.evaluate(() => window.__sabValue) === 57005 (0xDEAD).
      Test 'arrived object is a real SharedArrayBuffer': window.evaluate(() => window.__sabIsShared) === true (instanceof — review HIGH-3; a structured-clone copy of a non-SAB would fail this).
      Test 'SAME-MEMORY nonce cross-write holds (zero-copy, not a copy or an echo)': window.evaluate(() => window.__crossWriteOk) === true AND window.evaluate(() => window.__crossWriteState) === 'shared'. The renderer wrote a per-run random nonce into view[1] and did NOT send it over IPC (Plan 04); the utility re-read view[1] (Plan 03). If __crossWriteOk is FALSE, do NOT silently pass — fail the test and surface in the SUMMARY whether the state was 'copy' (cross-process SAB copy-only at the utility boundary) or 'error' (THROWS/timeout/no-ack) — these are DISTINCT findings with distinct pivots (review fix LOW + HIGH-3 / Cursor's escalation / genuine de-risk finding from Plan 03's architecture gate).
      Test 'contracts/ compiles': spawnSync('pnpm', ['--filter','@swg/contracts','exec','tsc','--noEmit'], { cwd: process.cwd() }).status === 0 (FND-04).
      Test 'SAB_LAYOUT offsets': import { SAB_LAYOUT } from '@swg/contracts'; SAB_LAYOUT.HELLO_SENTINEL.offset === 0 && SAB_LAYOUT.RENDERER_SENTINEL.offset === 4.

    DO NOT add or Edit window.__sabValue into ViewportPanel.tsx. Plan 04's StatusBar is the single owner of all hooks (review fix HIGH-4b). The spec only READS them. If a hook is missing at runtime, that is a Plan 04 defect to fix in Plan 04 — not patched here.

    Run: pnpm playwright test e2e/01-boot.spec.ts e2e/02-isolation.spec.ts e2e/03-sab-roundtrip.spec.ts — all green.
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && pnpm playwright test e2e/01-boot.spec.ts e2e/02-isolation.spec.ts e2e/03-sab-roundtrip.spec.ts 2>&1 | grep -E "passed|failed" | tail -3 && grep -E "Object.keys" e2e/01-boot.spec.ts && grep -E "__crossWriteOk|__crossWriteState|__sabIsShared" e2e/03-sab-roundtrip.spec.ts && test "$(grep -rc '__sabValue' packages/renderer/src/panels/ViewportPanel.tsx)" = "0" && echo "viewport hook-free"</automated>
  </verify>
  <acceptance_criteria>
    - 01-boot.spec.ts asserts contextIsolation true, nodeIntegration false, sandbox true, window.require/process undefined, hello() === 'pong'
    - 01-boot.spec.ts asserts Object.keys(window.api).sort() === ['crossWriteSab','hello','onSabPort'] (length 3 — review fix MEDIUM / Opus; not a grep proxy)
    - 02-isolation.spec.ts asserts crossOriginIsolated true, SharedArrayBuffer(4) ok, byteLength 4
    - 03-sab-roundtrip.spec.ts asserts __sabValue === 57005, __sabIsShared === true, __crossWriteOk === true, __crossWriteState === 'shared', contracts tsc 0, SAB_LAYOUT offsets 0/4
    - 03-sab-roundtrip.spec.ts does NOT contain a fallback that masks __crossWriteOk === false (a false cross-write must fail the test) and distinguishes 'copy' vs 'error' in its failure message
    - This plan does NOT add window.__sabValue to ViewportPanel.tsx (single-owner rule respected)
    - pnpm playwright test for all three specs exits 0
  </acceptance_criteria>
  <done>SC-1..SC-4 green, including the Object.keys allowlist + instanceof + same-memory NONCE cross-write (shared, not copy/echo); no test hook added here; no masking fallback.</done>
</task>

<task type="auto">
  <name>Task 2: SC-5 spec — panels + REAL close/relaunch persistence (real userData path) + fill VALIDATION.md</name>
  <files>
    e2e/04-workspace.spec.ts
    .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-UI-SPEC.md
    (section: Panel Content Contracts — panel labels; Theme Persistence Contract)
  </read_first>
  <action>
    04-workspace.spec.ts (SC-5): import { _electron as electron, test, expect } from '@playwright/test' (this spec manages its own app lifetime so it can do a real relaunch).

      Test 'four panels visible': launch app; expect getByText('Assets'), getByText('Viewport'), getByText('Inspector'), getByText('Datatable') all visible.
      Test 'dark background': page.evaluate(() => getComputedStyle(document.body).backgroundColor) includes 'rgb(24, 24, 24)'.
      Test 'layout serialized to localStorage': page.evaluate(() => localStorage.getItem('swg-workspace-layout')) not null and parses to JSON with a panels/grid property.
      Test 'theme change persists': set the select value to 'amber', dispatch change, read localStorage 'swg-active-theme' === 'amber'.
      Test 'SC-5 REAL RESTART — layout survives close + relaunch against the REAL userData path' (review fix MEDIUM / Opus — replaces page.reload() AND removes the injected-temp masking):
        1. Launch the app WITHOUT injecting a temp --user-data-dir, so it uses the SHIPPED default app.getPath('userData'). Capture that path: const userData = await app.evaluate(({ app }) => app.getPath('userData')) — assert it is the real default (not a temp dir the test created). This closes the masking hole where a production "writes to default-temp / non-persisted path" bug could hide behind an injected dir.
        2. In the launched app, move a panel (or capture the current toJSON-backed localStorage 'swg-workspace-layout' string) and set 'swg-active-theme' to 'amber'.
        3. await app.close() — fully terminate the Electron process (re-inits the main process on next launch).
        4. Relaunch with NO injected user-data-dir (same default userData path): const app2 = await electron.launch({ /* default userData */ }).
        5. const win2 = await app2.firstWindow(); assert localStorage 'swg-workspace-layout' still equals the captured string AND getByText('Assets') visible AND 'swg-active-theme' still 'amber' (theme + layout both survive a true restart against the real path).
        6. await app2.close().
        NOTE: to keep CI hermetic, the test may set an ISOLATED HOME/APPDATA env for the run so the "default" userData is a clean per-run location — but it must let Electron compute app.getPath('userData') from that env (i.e. exercise the real getPath code path), NOT pass an explicit --user-data-dir that bypasses getPath. Document this in a comment. SELF-POLICE (round-3 / Opus OPUS-1): the literal string '--user-data-dir' MUST NOT appear anywhere in this spec — a negative grep in the verify enforces it, so a careless executor cannot regress the real-userData seam to an injected dir (review fix MEDIUM / Opus — the point is to exercise the real persistence path, not a stub).

    Run: pnpm playwright test e2e/04-workspace.spec.ts — all green.

    Then FILL .planning/phases/00-toolchain-de-risk-app-shell/00-VALIDATION.md (do NOT flip nyquist here — that is Task 4): replace the placeholder Per-Task Verification Map with a fully populated table mapping every task across Plans 01–05 (including 00-02 Task 4 non-circular FND-02 proof, 05-packaged.spec.ts, the nonce cross-write rows, the Object.keys allowlist row, and the real-userData restart row) to its automated command, requirement, and threat ref. Fill Test Infrastructure (Vitest 4.1.9 + @playwright/test 1.61.0; configs vitest.config.ts + playwright.config.ts; quick = pnpm vitest run; full = pnpm vitest run && pnpm playwright test). Set wave_0_complete: true. Mark the 05-packaged.spec.ts row ⬜ pending (Task 3 fills it) and leave nyquist_compliant FALSE (Task 4 flips it after an independent review).
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && pnpm playwright test --forbid-only e2e/04-workspace.spec.ts 2>&1 | grep -E "passed|failed" | tail -3 && grep -i "close\|relaunch\|firstWindow" e2e/04-workspace.spec.ts && grep -E "getPath\('userData'\)|getPath\(.userData" e2e/04-workspace.spec.ts && test "$(grep -c 'page.reload' e2e/04-workspace.spec.ts)" = "0" && test "$(grep -c -- '--user-data-dir' e2e/04-workspace.spec.ts)" = "0" && echo "real-restart-real-userData-not-reload-no-userdatadir"</automated>
  </verify>
  <acceptance_criteria>
    - 04-workspace.spec.ts asserts four panels visible, dark bg, layout in localStorage, theme persists
    - SC-5 persistence is proven by a REAL app.close() + relaunch — grep -c 'page.reload' returns 0
    - The test asserts against the real app.getPath('userData') default path (not only an injected temp --user-data-dir) — review fix MEDIUM / Opus
    - NEGATIVE GREP (round-3 / Opus OPUS-1): grep -c -- '--user-data-dir' in e2e/04-workspace.spec.ts returns 0 — the restart spec must exercise the real getPath('userData') seam via an isolated HOME/APPDATA env, NOT an explicit --user-data-dir that bypasses getPath; the negative grep self-polices the seam
    - 00-VALIDATION.md Per-Task Verification Map filled for all Plan 01–05 tasks incl. the non-circular FND-02 proof, the packaged row, the nonce cross-write rows, the Object.keys allowlist row, and the real-userData restart row; wave_0_complete: true
    - 00-VALIDATION.md nyquist_compliant remains FALSE at the end of this task (flipped only by Task 4)
  </acceptance_criteria>
  <done>SC-5 proven by a real close/relaunch against the real userData path (not reload, not an injected temp masking); VALIDATION.md filled; Nyquist deferred to the independent Task 4.</done>
</task>

<task type="auto">
  <name>Task 3: Packaged-build spec — crossOriginIsolated + cross-write in the real file:// binary (built out-of-band, HARD gate)</name>
  <files>
    e2e/05-packaged.spec.ts
  </files>
  <read_first>
    D:/Code/SWG-Toolkit/.planning/phases/00-toolchain-de-risk-app-shell/00-RESEARCH.md
    (sections: Pattern 2 — COOP/COEP dev vs packaged + protocol.handle/meta-tag fallback; Pitfall 1/2)
    packages/backend/src/main.ts (confirm COOP/COEP via webRequest.onHeadersReceived; note the file:// caveat below)
    forge.config.ts (confirm packagerConfig.asar + AutoUnpackNativesPlugin)
    .github/workflows/ci.yml (confirm CI builds package:ci and treats a 05-packaged SKIP as a FAIL — review fix HIGH-2)
  </read_first>
  <action>
    The packaged binary is produced OUT-OF-BAND, NOT inside test.beforeAll (review HIGH-5 / Sonnet — a 60–120s native rebuild embedded in beforeAll gets disabled). Build it once via `pnpm package:ci` as a CI step or during the human checkpoint, then point this spec at it via PACKAGED_EXE_PATH. This spec is a HARD gate (review fix HIGH-2): in CI a SKIP of this spec is treated as a FAIL (the Plan 01 workflow always sets PACKAGED_EXE_PATH and parses the reporter for executed assertions).

    e2e/05-packaged.spec.ts: import { _electron as electron, test, expect } from '@playwright/test'; path, fs, child_process.
      const REPO_ROOT = path.resolve(__dirname, '..'); const OUT_DIR = path.join(REPO_ROOT, 'out').
      findPackagedExe(outDir): scan for win32 *.exe / darwin *.app/Contents/MacOS/* / linux executable; throw a descriptive error telling the user to run `pnpm package:ci` (or set PACKAGED_EXE_PATH) if none found.
      Resolve the exe: const exe = process.env.PACKAGED_EXE_PATH ?? findPackagedExe(OUT_DIR). Do NOT spawn `pnpm package` in beforeAll. If the exe is absent AND not in CI, test.skip with a clear message ("set PACKAGED_EXE_PATH or run pnpm package:ci") so local dev iteration is not blocked by a 2-min build. In CI PACKAGED_EXE_PATH is always set, so the skip branch is never taken there — the Plan 01 workflow fails the job if 05-packaged skips or runs 0 assertions (review fix HIGH-2).
      beforeAll: app = await electron.launch({ executablePath: exe }); window = await app.firstWindow(); await window.waitForLoadState('domcontentloaded'). afterAll: app?.close().
      Test 'packaged renderer crossOriginIsolated === true': window.evaluate(() => self.crossOriginIsolated) === true. This is the ESSENTIAL file:// gate, not a confirmation (review MEDIUM / Cursor): Forge's dev-server header forwarding does NOT apply to file://; the headers must be delivered by main.ts. If this assertion is FALSE in the packaged build, surface it and apply the DOCUMENTED FILE:// FALLBACK PRIORITY LADDER (review fix LOW / Sonnet — try in order, record which worked in the SUMMARY):
          1. onHeadersReceived for file:// — keep the existing webRequest.onHeadersReceived if it fires for file:// responses (simplest; no scheme change).
          2. protocol.handle('app://') — register a privileged 'app://' scheme via protocol.registerSchemesAsPrivileged BEFORE app.ready, then protocol.handle('app://') injecting COOP/COEP, and serve the renderer from app:// (RESEARCH Pattern 2). Most robust; more code.
          3. COOP/COEP <meta> tag in index.html — LAST RESORT only; note the isolation tradeoff (a meta-tag COEP is weaker than a header and may not enable crossOriginIsolated in all cases) in the SUMMARY.
        The fallback is owned by Plan 03's main.ts (the header-injection code lives there); this spec asserts the OUTCOME and records which rung of the ladder delivered it.
      Test 'packaged renderer SharedArrayBuffer(4) ok'.
      Test 'packaged renderer cross-write holds (packaged runtime load)': await window.waitForFunction(() => typeof window.__crossWriteOk === 'boolean', { timeout: 20000 }); expect window.__crossWriteOk === true AND window.__crossWriteState === 'shared' AND window.__sabValue === 57005 (the same-memory NONCE proof holds in the shipped binary — the only place the packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild is exercised, per the FND-02 scoping in 00-02; round-3 / Cursor CUR-1 — there is no separate Electron-ABI build, just a runtime load of the one artifact). If __crossWriteState is 'copy' or 'error', fail and surface the distinct finding (matches Plan 03's architecture gate).

    Run with the packaged exe present: PACKAGED_EXE_PATH=... pnpm playwright test e2e/05-packaged.spec.ts.
    Record the outcome in 00-VALIDATION.md (05-packaged.spec.ts row -> ✅ green, or ❌ + surfaced finding).
  </action>
  <verify>
    <automated>cd /d/Code/SWG-Toolkit && grep -E "PACKAGED_EXE_PATH" e2e/05-packaged.spec.ts && grep -E "crossOriginIsolated|__crossWriteOk|__crossWriteState" e2e/05-packaged.spec.ts && test "$(grep -c \"spawnSync.*package\\|execSync.*package\" e2e/05-packaged.spec.ts)" = "0" && echo "package-build-decoupled"</automated>
  </verify>
  <acceptance_criteria>
    - 05-packaged.spec.ts resolves the executable via PACKAGED_EXE_PATH (falling back to an out/ scan) and does NOT run pnpm package inside the spec (grep for spawnSync/execSync 'package' returns 0)
    - When the packaged exe is absent locally, the spec test.skips with an actionable message; in CI PACKAGED_EXE_PATH is set so it never skips, and a CI skip is a FAIL (review fix HIGH-2, enforced by the Plan 01 workflow)
    - Asserts packaged crossOriginIsolated === true (file:// path) with a DOCUMENTED fallback priority ladder (onHeadersReceived → protocol.handle('app://') → <meta>) recorded in the SUMMARY if the first rung fails
    - Asserts packaged window.__crossWriteOk === true, __crossWriteState === 'shared', and window.__sabValue === 57005 (nonce cross-write holds in the real packaged binary — the packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild, round-3 / Cursor CUR-1)
    - 00-VALIDATION.md 05-packaged.spec.ts row updated to ✅ green (or ❌ + surfaced finding with copy-vs-error distinction)
  </acceptance_criteria>
  <done>Packaged spec reads PACKAGED_EXE_PATH (build decoupled); HARD gate (skip = fail in CI); asserts crossOriginIsolated + nonce cross-write in the real file:// binary (packaged-Electron runtime load of the single --napi prebuild); file:// fallback priority ladder documented.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Independent Nyquist sign-off against the filled VALIDATION.md (certify only what was proven)</name>
  <decision>Is Phase 0 Nyquist-compliant? (Flip 00-VALIDATION.md nyquist_compliant: true ONLY if this independent review passes AND the sign-off certifies EXACTLY what was proven — never a proxy.)</decision>
  <context>
    Review fix MEDIUM (Opus + Sonnet): the Nyquist flip must NOT be self-certified by the same task that
    authored the tests. This is a separate, independent sign-off step that audits the now-FILLED
    00-VALIDATION.md (Task 2) against the Nyquist criteria — it does not write any tests.

    MACHINE-INDEPENDENCE (round-3 / Opus OPUS-2, optional belt-and-suspenders): because Task 2 and this Task 4
    run in the same plan / same executor agent, independence currently leans on the human signoff resume-signal
    (present, gate="blocking"). If the phase-close gsd-verifier runs (a DIFFERENT agent), prefer routing the
    nyquist_compliant flip through it rather than relying solely on the human resume-signal — this gives
    machine-independent certification. Keep this lightweight: it is an optional hardening, not a restructure;
    if it complicates the close-out, the human-gated independence above is sufficient.

    Review fix HIGH-1 (Opus + Sonnet): the sign-off must NOT certify FND-02 as a "full no-compiler proof"
    (that was the round-1 proxy the Nyquist gate falsely certified). FND-02 is certified ONLY as: (a) the
    resolution path is NON-CIRCULAR (00-02 Task 4 moved build/ aside and asserted the load came from
    prebuilds/), and (b) the packaged-Electron RUNTIME LOAD of the SINGLE ABI-stable --napi prebuild is
    proven by the 05-packaged HARD gate (round-3 / Cursor CUR-1 — N-API is ABI-stable across Node + Electron,
    so this is one artifact loaded under Electron at runtime, NOT a distinct "Electron-ABI" build). The full
    no-compiler-MACHINE proof (no MSVC present at all) is explicitly DEFERRED to a toolchain-free CI runner.
    The sign-off bullet wording MUST match the 00-02 SUMMARY exactly.

    Audit checklist (confirm each against the filled VALIDATION.md + the green suite):
    - Every Plan 01–05 task has an <automated> verify or a Wave 0 dependency (no MISSING).
    - No 3 consecutive tasks lack an automated verify (sampling continuity).
    - All five SCs map to a green spec: SC-1→01-boot (incl. Object.keys allowlist), SC-2→01-boot hello,
      SC-3→02-isolation (+05 packaged), SC-4→03-sab-roundtrip (NONCE cross-write + instanceof, NOT a value
      copy or arg-echo, __crossWriteState === 'shared'), SC-5→04-workspace (REAL restart against real userData).
    - FND-02: certified as resolution-path non-circular (00-02 Task 4) + packaged-Electron RUNTIME LOAD of the
      single ABI-stable --napi prebuild via the 05-packaged hard gate (round-3 / Cursor CUR-1 — not a separate
      "Electron-ABI" build). NOT certified as a full no-compiler-machine proof. Wording matches the 00-02 SUMMARY.
    - The 05-packaged HARD gate is GREEN (not skipped) — a skip is a fail (review fix HIGH-2). Confirm CI ran it.
    - If Plan 03's architecture gate recorded a cross-write FINDING (copy/throws), the "live-sync proven"/D-04
      claim is BLOCKED — do NOT pass Nyquist as if D-04 were proven; pass only the SCs that ARE green and
      record the finding.
    - No watch-mode flags in any verify command.
  </context>
  <options>
    <option id="signoff-pass">
      <name>Pass — set nyquist_compliant: true (certifying only what was proven)</name>
      <pros>All criteria above hold; the suite is green incl. the nonce cross-write + real-userData restart + the packaged HARD gate; FND-02 certified as non-circular-resolution + packaged-Electron-runtime-load of the single ABI-stable --napi prebuild (round-3 / Cursor CUR-1; not a proxy, not a separate Electron-ABI build)</pros>
      <cons>None if the audit genuinely passes</cons>
    </option>
    <option id="signoff-fail">
      <name>Fail — leave nyquist_compliant: false and list the gaps</name>
      <pros>Honest gate; blocks a false "Phase 0 complete" (e.g. cross-write copy-only finding, packaged gate skipped, or FND-02 not non-circular)</pros>
      <cons>Phase 0 stays open until the gaps are resolved</cons>
    </option>
  </options>
  <resume-signal>Reply "signoff-pass" (and the executor sets nyquist_compliant: true with the FND-02 wording matching the 00-02 SUMMARY — non-circular resolution + packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild, NOT a full no-compiler proof, NOT a separate Electron-ABI build) or "signoff-fail" with the specific failing criteria.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual shell + docking + restart + PACKAGED HARD GATE human verification</name>
  <what-built>
    Plans 01–05 delivered: pnpm monorepo scaffold (+ .nvmrc/engines/check-prereqs/CI with a packaged hard gate),
    cmake-js C++ addon (hello + allocateSab WITH NAPI_EXPERIMENTAL) + prebuildify/node-gyp-build distribution with
    a NON-CIRCULAR resolution proof, Electron security posture (contextIsolation, COOP/COEP), the utility-process →
    SAB → renderer pipeline WITH a same-memory NONCE cross-write proof (and a Plan 03 architecture gate), the dark
    dockable workspace shell (4 panels, themes, persistence), and an automated E2E suite including the Object.keys
    allowlist, a real close/relaunch restart against the real userData path, and a packaged-binary HARD gate.

    Automated verification: pnpm vitest run + pnpm playwright test are green (unit tests + 5 Playwright specs incl.
    nonce cross-write, instanceof, real restart, and the packaged file:// hard gate).
  </what-built>
  <how-to-verify>
    1. Build the packaged binary out-of-band so the packaged HARD gate can run: `pnpm package:ci`, then
       `PACKAGED_EXE_PATH=<out/.../swg-toolkit.exe> pnpm playwright test e2e/05-packaged.spec.ts` — expect green.
       A SKIP here is a FAIL (review fix HIGH-2): if the spec skips, the packaged exe was not built/resolved — build it
       and re-run; do NOT accept a skipped packaged gate as a pass.
    2. `pnpm vitest run && pnpm playwright test e2e/01-boot.spec.ts e2e/02-isolation.spec.ts e2e/03-sab-roundtrip.spec.ts e2e/04-workspace.spec.ts` — expect all green.
    3. `pnpm start`. Check: (a) dark #181818 background, no white flash; (b) four panels (Assets/Viewport/Inspector/Data with Datatable/Console/Log tabs); (c) Viewport center shows "crossOriginIsolated ✓ / SAB round-trip: PASS / native-core: utility-process ✓" after ~1s; (d) Console tab shows the five seed lines; (e) StatusBar shows "crossOriginIsolated: true", "SAB: 60 fps", "cross-write: shared ✓", "addon: utility-process ✓" in cyan (if it shows "copy ✗" or "no ack ✗", that is the Plan 03 architecture FINDING — do NOT approve as live-sync proven).
    4. Drag the Inspector to the center; confirm the 5-way drop-zone overlay appears; drop it.
    5. Close and relaunch; confirm the moved layout is restored (this is the human counterpart to the automated real-restart test).
    6. Switch theme to Amber; confirm accent changes immediately; close/relaunch; confirm Amber persists.
    7. Tab through controls; confirm every button/select shows a visible focus ring.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct AND the packaged hard gate ran green (not skipped). Otherwise describe issues (including any cross-write copy/no-ack finding).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Electron app | Playwright launches Electron; test code runs in the runner, not the app |
| page.evaluate() | Runs JS in the renderer sandbox; no Node access |
| packaged binary → file system | Packaged build reads from the ASAR bundle; main.ts header injection (or app:// handler) must run before any page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-17 | Tampering | window.__sabValue/__crossWriteOk test hooks | accept | Read-only test sentinels set by StatusBar (single owner); 0xDEAD + a per-run nonce are test values; renderer sandboxed; no sensitive data |
| T-00-18 | Information Disclosure | Playwright reading webPreferences | accept | electronApp.evaluate runs in the trusted main process; reading webPreferences is the correct way to verify the security posture |
| T-00-19 | Tampering | E2E passing despite a security regression | mitigate | Specs assert contextIsolation true / nodeIntegration false via getWebPreferences() AND Object.keys(window.api) === the three-method allowlist (review fix MEDIUM / Opus); a regression or an over-broad bridge fails the suite |
| T-00-20 | Information Disclosure | COOP/COEP missing in the packaged file:// build | mitigate | 05-packaged.spec.ts asserts crossOriginIsolated === true against the real binary with a documented file:// fallback priority ladder (onHeadersReceived → app:// → meta); the ESSENTIAL hard gate, not a confirmation (Cursor); a CI skip is a fail (review fix HIGH-2) |
| T-00-24 | Tampering | cross-write masked as passing when copy-only or echo | mitigate | 03/05 assert __crossWriteOk === true AND __crossWriteState === 'shared' with NO masking fallback; a per-run nonce defeats arg-echo; a copy-only/throws boundary fails the test and is surfaced as a distinct finding (copy vs error) |
| T-00-28 | Tampering | SC-5 persistence masked by an injected temp userData dir | mitigate | 04-workspace asserts against the real app.getPath('userData') path (review fix MEDIUM / Opus); a production "writes to default-temp" bug cannot hide behind an injected --user-data-dir |
</threat_model>

<verification>
Final phase gate — run ALL and confirm green:

1. `pnpm vitest run` — unit tests pass (incl. 00-02 non-circular resolve-prebuild proof)
2. `pnpm playwright test e2e/01-boot.spec.ts e2e/02-isolation.spec.ts e2e/03-sab-roundtrip.spec.ts e2e/04-workspace.spec.ts` — all pass (incl. Object.keys allowlist + nonce cross-write + real-userData restart)
3. `pnpm package:ci && PACKAGED_EXE_PATH=<out exe> pnpm playwright test e2e/05-packaged.spec.ts` — green (HARD gate; a skip is a fail), or finding surfaced
4. `grep "nyquist_compliant.*true" 00-VALIDATION.md` — set ONLY after Task 4 independent sign-off, certifying FND-02 as non-circular-resolution + packaged-Electron-runtime-load of the single ABI-stable --napi prebuild (round-3 / Cursor CUR-1; not a proxy)
5. `pnpm -r exec tsc --noEmit` — contracts + backend + renderer all 0
6. `pnpm start` — human checkpoint approved (incl. packaged hard gate green, not skipped)
</verification>

<success_criteria>
- All five Phase 0 SCs have green automated tests:
  SC-1 security posture + Object.keys allowlist (01-boot), SC-2 hello (01-boot), SC-3 crossOriginIsolated (02-isolation + 05 packaged),
  SC-4 SAB NONCE cross-write + instanceof — sharing NOT a copy/echo, __crossWriteState 'shared' (03-sab-roundtrip),
  SC-5 REAL close/relaunch persistence against the real userData path (04-workspace)
- Packaged file:// HARD gate: crossOriginIsolated + nonce cross-write hold in the real binary (05-packaged), build decoupled via package:ci/PACKAGED_EXE_PATH, a skip is a fail (review fix HIGH-2), file:// fallback ladder documented
- Nyquist sign-off performed independently (Task 4) against the filled VALIDATION.md, certifying ONLY what was proven (FND-02 non-circular resolution + packaged-Electron RUNTIME LOAD of the single ABI-stable --napi prebuild — round-3 / Cursor CUR-1, NOT a full no-compiler proof, NOT a proxy, NOT a separate Electron-ABI build — review fix HIGH-1) before nyquist_compliant: true
- Human checkpoint: visual shell, docking, a real restart, and the packaged hard gate (green, not skipped) confirmed
- Phase 0 declared complete; /gsd:verify-work can run
</success_criteria>

<output>
Create `.planning/phases/00-toolchain-de-risk-app-shell/00-05-SUMMARY.md` when done.
</output>
